Run 245 — Release-binary modeled governance trust-state mutation applier evidence

You are continuing QBIND after Run 244.

Run 240 is fully closed at source/test level.
Run 241 is fully closed as release-binary evidence for Run 240.
Run 242 is fully closed as source/test governance execution mutation-engine boundary work.
Run 243 is fully closed as release-binary evidence for Run 242.
Run 244 is fully closed as source/test modeled governance trust-state mutation applier boundary work.

Strict workflow:

* Execution-first.
* Narrow scope only.
* No broad redesign.
* No silent regressions.
* No hype or overclaiming.
* Keep Full C4 OPEN and C5 OPEN unless real production evidence closes them.
* MainNet peer-driven apply remains refused.
* Fixture/local/loopback material remains DevNet/TestNet evidence-only.
* Rejected paths must remain non-mutating.
* Do not weaken Runs 070, 130–244.
* Release-binary evidence only, unless a tiny docs-only typo fix is needed.

Current state:

* Run 238 defined the typed durable replay-state backend contract.
* Run 239 release-evidenced the durable backend boundary.
* Run 240 wired the Run 238 durable backend into the Run 236 / 232 / 230 replay/freshness + after-success-only consume runtime path as the durable state provider.
* Run 241 release-evidenced the Run 240 durable runtime integration.
* Run 242 added the source/test governance execution mutation-engine boundary in `crates/qbind-node/src/pqc_governance_execution_mutation_engine.rs`.
* Run 243 release-evidenced the Run 242 mutation-engine boundary on real `target/release/qbind-node` plus a release-built helper.
* Run 244 added the source/test modeled governance trust-state mutation applier boundary in `crates/qbind-node/src/pqc_governance_modeled_trust_mutation_applier.rs`.
* Run 244 adds a modeled in-memory state transition shape after all Run 242 mutation-engine gates have passed: snapshot modeled trust state, apply a modeled trust-state update, report success/failure/rollback/rollback-failed/ambiguous windows, and project through Run 242 into Run 240 durable completion semantics.
* Run 244 has no release-binary evidence yet.
* No real governance execution engine is implemented.
* No real production mutation engine is implemented.
* No real on-chain governance proof verifier is implemented.
* No real persistent replay backend is implemented.
* No RocksDB/file/schema/migration/storage-format change is implemented.
* No KMS/HSM/RemoteSigner backend is implemented.
* No MainNet governance enablement.
* No MainNet peer-driven apply enablement.
* Validator-set rotation remains unsupported.
* Full C4 remains OPEN.
* C5 remains OPEN.

Objective:

Produce release-binary evidence for the Run 244 modeled governance trust-state mutation applier boundary, mirroring the Run 239 / Run 241 / Run 243 release-evidence pattern.

Run 245 must prove on real `target/release/qbind-node` plus a release-built helper that the Run 244 modeled applier boundary is present in release-built code, that the helper can exercise the modeled-applier-composed durable runtime path through production library symbols, and that all fail-closed/non-mutating invariants remain true in release mode.

Run 245 must not implement a real production mutation engine, must not wire production runtime mutation, must not call Run 070, must not mutate `LivePqcTrustState`, must not perform a real trust swap, must not evict sessions, must not write sequence files, must not write authority markers, must not touch RocksDB, and must not enable MainNet governance or MainNet peer-driven apply.

Preflight docs hygiene:

* Search the updated Run 244 docs for the typo:

  * `` `Production`/`MainNetModeledMutationUnavailable` ``
* If present, fix it narrowly to:

  * `` `ProductionModeledMutationUnavailable` / `MainNetModeledMutationUnavailable` ``
* This is docs-only hygiene and must not be treated as a behavior change.

Required deliverables:

1. Add a release helper:

   `crates/qbind-node/examples/run_245_modeled_governance_trust_mutation_applier_release_binary_helper.rs`

   The helper must link against release-built production library symbols and exercise:

   * `pqc_governance_modeled_trust_mutation_applier`
   * `ModeledGovernanceTrustState`
   * `ModeledGovernanceTrustSnapshot`
   * `ModeledGovernanceTrustRoot`
   * `ModeledTrustRootStatus`
   * `ModeledGovernanceTrustMutation`
   * `ModeledGovernanceTrustMutationInput`
   * `ModeledGovernanceTrustMutationExpectations`
   * `ModeledGovernanceTrustMutationPolicy`
   * `ModeledGovernanceTrustMutationSurface`
   * `ModeledGovernanceTrustMutationEnvironmentBinding`
   * `ModeledGovernanceTrustMutationRuntimeBinding`
   * `ModeledTrustMutationAction`
   * `ModeledTrustMutationOutcome`
   * `ModeledGovernanceTrustMutationApplier`
   * `FixtureModeledTrustMutationApplier`
   * `ProductionModeledTrustMutationApplier`
   * `MainNetModeledTrustMutationApplier`
   * `evaluate_modeled_trust_mutation`
   * `recover_modeled_trust_mutation`
   * `map_modeled_outcome_to_mutation_engine_outcome`
   * `project_modeled_outcome_to_durable_completion`
   * `modeled_outcome_authorizes_durable_consume`
   * all grep-verifiable invariant helpers from Run 244

2. Add a release harness:

   `scripts/devnet/run_245_modeled_governance_trust_mutation_applier_release_binary.sh`

   The harness must:

   * build `target/release/qbind-node`;
   * build the Run 245 release helper;
   * capture git commit, rustc version, cargo version;
   * capture SHA-256 and ELF Build ID for `target/release/qbind-node`;
   * capture SHA-256 and ELF Build ID for the Run 245 helper;
   * run real-binary surface scenarios;
   * run the helper corpus in release mode;
   * run source-reachability greps for the Run 244 symbols;
   * run a denylist proving no active production/MainNet enablement claims;
   * run the required regression test corpus;
   * write generated evidence into an ignored evidence directory.

3. Add evidence archive directory:

   `docs/devnet/run_245_modeled_governance_trust_mutation_applier_release_binary/`

   Track only:

   * `README.md`
   * `summary.txt`
   * `.gitignore`

   Generated artifacts must be ignored, following the Run 241 / Run 243 convention.

4. Add canonical evidence report:

   `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_245.md`

Release-helper corpus:

The helper must include accepted/compatible, rejection, recovery, projection, modeled-state, and reachability tables.

Accepted / compatible cases must prove at minimum:

* Disabled policy preserves legacy bypass and performs no modeled mutation.
* Disabled applier kind preserves legacy bypass and performs no modeled mutation.
* DevNet fixture modeled add-root succeeds and mutates only `ModeledGovernanceTrustState`.
* TestNet fixture modeled add-root succeeds and mutates only `ModeledGovernanceTrustState`.
* Modeled retire-root succeeds in fixture state only.
* Modeled revoke-root succeeds in fixture state only.
* Modeled emergency-revoke-root succeeds in fixture state only.
* Modeled noop succeeds without state drift.
* Duplicate root is handled idempotently under an explicit typed outcome.
* Modeled apply success maps to `GovernanceMutationOutcome::MutationAppliedSuccessfully`.
* Modeled apply success projects to consume-eligible Run 240 durable completion.
* Existing Run 244 source/test behavior remains compatible.
* Existing Run 242 mutation-engine behavior remains compatible.
* Existing Run 240 durable completion behavior remains compatible.
* Production modeled applier path is reachable but unavailable/fail-closed.
* MainNet modeled applier path is reachable but unavailable/fail-closed.
* MainNet peer-driven apply refused before snapshot and before applier invocation.
* Validator-set rotation unsupported.
* Policy-change unsupported.

Rejected cases must prove at minimum:

* wrong environment rejected before snapshot;
* wrong chain rejected before snapshot;
* wrong genesis rejected before snapshot;
* wrong governance surface rejected before snapshot;
* wrong mutation surface rejected before snapshot;
* wrong candidate digest rejected before snapshot;
* wrong decision digest rejected before snapshot;
* wrong proposal id rejected before snapshot;
* wrong decision id rejected before snapshot;
* wrong authority-domain sequence rejected before snapshot;
* wrong lifecycle action rejected before snapshot;
* malformed modeled mutation rejected before snapshot;
* read-only validation surface rejected before snapshot;
* retiring missing root snapshots then rejects-before-apply with modeled state unchanged;
* revoking missing root snapshots then rejects-before-apply with modeled state unchanged;
* stale durable state cannot reach consume;
* consumed durable state cannot reach consume;
* superseded durable state cannot reach consume;
* backend unavailable cannot reach consume;
* apply failure rolls back modeled state and never consumes;
* rollback success never consumes;
* rollback failure produces fatal/ambiguous fail-closed and never consumes;
* unknown crash window fails closed;
* local operator key cannot satisfy MainNet authority;
* peer majority cannot satisfy MainNet authority;
* production modeled applier unavailable;
* MainNet modeled applier unavailable;
* validator-set rotation attempt unsupported;
* policy-change attempt unsupported;
* every rejected path produces no Run 070 call, no `LivePqcTrustState` mutation, no real trust swap, no session eviction, no sequence write, no marker write, no durable consume, and no applier invocation where rejection happens before apply.

Recovery cases must prove at minimum:

* before-snapshot window recovers as not-attempted / no consume;
* after-snapshot-before-apply window rolls back modeled state / no consume;
* after-apply-before-report window fails closed unless an explicit success report exists;
* after-report-success recovers as modeled applied;
* after-report-ambiguous window fails closed;
* rollback-failed window is fatal/fail-closed;
* unknown window fails closed;
* production/MainNet recovery classification unavailable/fail-closed;
* MainNet peer-driven apply refusal precedes recovery classification where applicable.

Projection cases must prove at minimum:

* only `ModeledMutationApplied` maps to `MutationAppliedSuccessfully`;
* only `ModeledMutationApplied` projects to consume-eligible durable completion;
* `ModeledMutationNotAttempted` does not consume;
* `ModeledMutationRejectedBeforeSnapshot` does not consume;
* `ModeledMutationRejectedBeforeApply` does not consume;
* `ModeledMutationApplyFailed` does not consume;
* `ModeledMutationRolledBack` does not consume;
* `ModeledMutationRollbackFailedFatal` does not consume;
* `ModeledMutationAmbiguousFailClosed` does not consume;
* `ProductionModeledMutationUnavailable` does not consume;
* `MainNetModeledMutationUnavailable` does not consume;
* `MainNetPeerDrivenApplyRefused` does not consume;
* `ValidatorSetRotationUnsupported` does not consume;
* `PolicyChangeUnsupported` does not consume.

Modeled-state cases must prove at minimum:

* add-root changes only the modeled in-memory state;
* retire-root changes only the modeled in-memory state;
* revoke-root changes only the modeled in-memory state;
* emergency-revoke-root changes only the modeled in-memory state;
* noop produces no modeled state drift;
* rejected-before-snapshot leaves modeled state unchanged;
* rejected-before-apply leaves modeled state unchanged after snapshot;
* apply failure rolls back modeled state;
* rollback failure does not claim success and never consumes;
* no fixture case mutates `LivePqcTrustState`;
* no fixture case writes sequence/marker/durable state.

Real release-binary surface scenarios:

The harness must run the real `target/release/qbind-node` for at least:

* `--help` / default help surface: no modeled-applier enablement banner or visible public flag drift.
* default DevNet startup parse/smoke surface: no modeled-applier enablement claim.
* default TestNet startup parse/smoke surface: no modeled-applier enablement claim.
* default MainNet startup parse/smoke surface: no modeled-applier enablement claim.
* invalid governance-execution selector or equivalent hidden selector parse path remains fail-closed before modeled mutation if applicable.
* any Run 244/245 hidden selector or helper-only path must not appear as a public production enablement surface.

Denylist:

Prove the captured real-binary logs and helper logs do not contain active/enabled claims for:

* real production mutation engine enabled;
* modeled applier production enabled;
* MainNet modeled applier enabled;
* MainNet mutation engine enabled;
* MainNet governance enabled;
* MainNet peer-driven apply enabled;
* real governance execution engine enabled;
* real on-chain governance proof verifier enabled;
* real persistent replay backend enabled;
* RocksDB replay backend enabled;
* file replay backend enabled;
* schema migration enabled;
* storage-format migration enabled;
* KMS/HSM backend enabled;
* RemoteSigner backend enabled;
* validator-set rotation enabled;
* policy-change action enabled;
* autonomous apply;
* apply-on-receipt;
* peer-majority authority;
* Run 070 apply from the modeled-applier boundary;
* LivePqcTrustState mutation from the modeled-applier boundary;
* real trust swap from the modeled-applier boundary;
* session eviction from the modeled-applier boundary;
* marker write from the modeled-applier boundary;
* sequence write from the modeled-applier boundary;
* durable consume by the modeled-applier boundary;
* DummySig / DummyKem / DummyAead active on production path.

Source/release reachability proof:

The harness must grep production source for the Run 244 symbols and prove the release helper exercises them in release mode.

Required greps include:

* `pqc_governance_modeled_trust_mutation_applier`
* `ModeledGovernanceTrustState`
* `ModeledGovernanceTrustSnapshot`
* `ModeledGovernanceTrustRoot`
* `ModeledTrustRootStatus`
* `ModeledGovernanceTrustMutation`
* `ModeledGovernanceTrustMutationInput`
* `ModeledGovernanceTrustMutationExpectations`
* `ModeledGovernanceTrustMutationPolicy`
* `ModeledGovernanceTrustMutationSurface`
* `ModeledGovernanceTrustMutationEnvironmentBinding`
* `ModeledGovernanceTrustMutationRuntimeBinding`
* `ModeledTrustMutationAction`
* `ModeledTrustMutationOutcome`
* `ModeledGovernanceTrustMutationApplier`
* `FixtureModeledTrustMutationApplier`
* `ProductionModeledTrustMutationApplier`
* `MainNetModeledTrustMutationApplier`
* `evaluate_modeled_trust_mutation`
* `recover_modeled_trust_mutation`
* `map_modeled_outcome_to_mutation_engine_outcome`
* `project_modeled_outcome_to_durable_completion`
* `modeled_outcome_authorizes_durable_consume`
* `modeled_trust_applier_rejection_is_non_mutating`
* `modeled_trust_applier_never_calls_run_070`
* `modeled_trust_applier_never_mutates_live_pqc_trust_state`
* `modeled_trust_applier_success_required_before_durable_consume`
* `modeled_trust_applier_failure_never_consumes`
* `modeled_trust_applier_rollback_never_consumes`
* `modeled_trust_applier_ambiguous_window_fails_closed`
* `production_mainnet_modeled_trust_applier_unavailable`
* `mainnet_peer_driven_apply_refused_by_modeled_trust_applier`
* `validator_set_rotation_unsupported_by_modeled_trust_applier`
* `policy_change_unsupported_by_modeled_trust_applier`
* `modeled_trust_applier_no_rocksdb_file_schema_migration_change`
* `local_operator_cannot_satisfy_modeled_trust_applier_authority`
* `peer_majority_cannot_satisfy_modeled_trust_applier_authority`

Validation commands:

Run at minimum:

* `cargo build -p qbind-node --release --bin qbind-node`
* `cargo build -p qbind-node --release --example run_245_modeled_governance_trust_mutation_applier_release_binary_helper`
* `bash scripts/devnet/run_245_modeled_governance_trust_mutation_applier_release_binary.sh`
* `cargo test -p qbind-node --test run_244_modeled_governance_trust_mutation_applier_tests`
* `cargo test -p qbind-node --test run_242_governance_execution_mutation_engine_tests`
* `cargo test -p qbind-node --test run_240_governance_evaluator_replay_durable_runtime_integration_tests`
* `cargo test -p qbind-node --test run_238_governance_evaluator_replay_durable_backend_tests`
* `cargo test -p qbind-node --test run_236_governance_evaluator_replay_consume_runtime_integration_tests`
* `cargo test -p qbind-node --test run_234_governance_evaluator_replay_consume_boundary_tests`
* `cargo test -p qbind-node --test run_232_governance_evaluator_replay_runtime_integration_tests`
* `cargo test -p qbind-node --test run_230_governance_evaluator_replay_state_tests`
* `cargo test -p qbind-node --test run_228_peer_evaluator_context_representation_tests`
* `cargo test -p qbind-node --test run_226_governance_evaluator_runtime_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_224_governance_evaluator_runtime_integration_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

Documentation:

Create:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_245.md`
* `docs/devnet/run_245_modeled_governance_trust_mutation_applier_release_binary/README.md`
* `docs/devnet/run_245_modeled_governance_trust_mutation_applier_release_binary/summary.txt`
* `docs/devnet/run_245_modeled_governance_trust_mutation_applier_release_binary/.gitignore`

Update narrowly:

* `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
* `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
* `docs/whitepaper/contradiction.md`

Documentation must state:

* Run 245 is release-binary evidence for Run 244.
* No production mutating behavior is enabled.
* Any production-source module remains pure/source-test bounded and fail-closed.
* The modeled applier boundary is release-evidenced, not production-enabled.
* The modeled applier mutates only modeled in-memory state in DevNet/TestNet fixture helper cases.
* The modeled applier does not mutate `LivePqcTrustState`.
* The modeled applier does not call Run 070.
* The modeled applier does not perform a real trust swap.
* The modeled applier does not evict sessions.
* The modeled applier does not write sequence files.
* The modeled applier does not write authority markers.
* The modeled applier does not perform durable consume by itself.
* No real production mutation engine is implemented.
* No real governance execution engine is implemented.
* No real on-chain governance proof verifier is implemented.
* No real persistent replay backend is implemented.
* No RocksDB/file/schema/migration/storage-format change is implemented.
* No KMS/HSM/RemoteSigner backend is implemented.
* No MainNet governance enablement.
* No MainNet peer-driven apply enablement.
* No validator-set rotation.
* Rejected modeled-applier paths are non-mutating.
* Modeled apply success is required before durable consume.
* Failed apply, rollback, rollback failure, and ambiguous windows never consume.
* MainNet peer-driven apply remains refused before any snapshot or applier invocation.
* Full C4 remains OPEN.
* C5 remains OPEN.

Contradiction handling:

* Crosscheck Run 245 against Runs 050–244.
* If no contradiction is found, add a “no Run 245 contradiction” entry to `docs/whitepaper/contradiction.md`.
* If any contradiction is found, do not hide it. Record it explicitly with the exact file/function/doc reference and recommend the smallest safe fix.

Strict non-goals:

* Do not implement a real production mutation engine.
* Do not wire production runtime mutation.
* Do not call Run 070.
* Do not mutate `LivePqcTrustState`.
* Do not perform a real trust swap.
* Do not evict sessions.
* Do not write sequence files.
* Do not write authority markers.
* Do not implement a real governance execution engine.
* Do not implement a real on-chain governance proof verifier.
* Do not implement validator-set rotation.
* Do not implement a persistent replay backend.
* Do not add RocksDB/file/schema/migration/storage-format changes.
* Do not change wire/schema/marker/sequence/trust-bundle formats.
* Do not enable MainNet governance.
* Do not enable MainNet peer-driven apply.
* Do not add autonomous apply or apply-on-receipt.
* Do not let peer majority satisfy authority.
* Do not weaken any fail-closed behavior from Runs 070 or 130–244.

Expected verdict:

Positive for release-binary evidence, if the release helper/harness and regression corpus pass.

Run 245 should close the Run 244 release-binary evidence gap only. It must not claim Full C4 or C5 closure.

Final response format:

1. Exact verdict.
2. Files changed.
3. Release artifacts and hashes.
4. Helper corpus results.
5. Real-binary scenarios.
6. Tests run and results.
7. Security invariants preserved.
8. Honest limitations.
9. C4/C5 status.
10. Suggested Run 246 next step.
