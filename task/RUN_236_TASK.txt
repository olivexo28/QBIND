Task: Run 236 — Source/Test Governance Evaluator Replay Consume Runtime Integration

Current accepted state:

* Run 230: source/test governance evaluator replay/freshness state boundary landed.
* Run 231: release-binary replay/freshness state evidence fully closed.
* Run 232: source/test governance evaluator replay/freshness runtime integration landed.
* Run 233: release-binary replay/freshness runtime integration evidence fully closed.
* Run 234: source/test post-mutation replay consume boundary landed.
* Run 235: release-binary post-mutation replay consume boundary evidence fully closed:

  * release-built helper exercises Run 234 consume-boundary symbols;
  * consume is release-evidenced as after-success-only;
  * deferred, validation-only, failed apply, rollback, unsupported surface, and MainNet refused outcomes do not consume;
  * fixture consume remains DevNet/TestNet evidence-only;
  * production/MainNet consume remains unavailable/fail-closed;
  * no storage/schema/migration/RocksDB/file-format change;
  * no wire/schema/marker/sequence/trust-bundle change;
  * MainNet peer-driven apply remains refused;
  * Full C4 remains OPEN;
  * C5 remains OPEN.
* Current remaining gap:
  The consume boundary is proven standalone, but it is not yet integrated into the replay/freshness runtime path as a modeled post-success step. The runtime path needs a source/test integration surface that ties:

  1. replay/freshness `ProceedFresh`;
  2. mutation authorization;
  3. mutation completion status;
  4. after-success-only fixture consume.

Objective:
Integrate the Run 234 post-mutation consume boundary into the governance evaluator replay/freshness runtime integration path at source/test level.

This run must model the full source/test lifecycle:

* validate replay/freshness;
* authorize mutation only on fresh;
* model mutation completion;
* consume only after successful mutation completion;
* keep consume unavailable/fail-closed for production/MainNet;
* keep validation-only, failed, rollback, unsupported, deferred, and MainNet-refused paths non-consuming.

Release-binary evidence is deferred to Run 237.

Strict scope:

* Source/test only.
* No release-binary harness.
* No real governance execution engine.
* No real on-chain governance proof verifier.
* No real mutation engine implementation.
* No MainNet governance enablement.
* No MainNet peer-driven apply enablement.
* No validator-set rotation.
* No KMS/HSM backend implementation.
* No RemoteSigner backend implementation.
* No RocksDB schema change.
* No file-format change.
* No database migration.
* No wire/schema/marker/sequence/trust-bundle change.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* Do not weaken Runs 070, 130–235.
* Do not claim full C4 or C5 closure.

Required source behavior:

1. Add a focused pure integration module, preferably:
   `crates/qbind-node/src/pqc_governance_evaluator_replay_consume_runtime_integration.rs`

2. Compose:

   * Run 232 replay/freshness runtime integration;
   * Run 234 post-mutation consume boundary;
   * Run 230 replay state reader/writer traits;
   * Run 224 / Run 226 / Run 228 surfaces where relevant.

3. Define typed input:

   * replay runtime integration input;
   * consume boundary input;
   * mutation surface;
   * mutation completion status;
   * fixture/production/MainNet consume writer selector;
   * validation surface;
   * environment;
   * chain id;
   * genesis hash.

4. Define typed outcome:

   * `ProceedLegacyBypassNoConsume`;
   * `ProceedDeferredNoConsume`;
   * `ProceedValidationOnlyNoConsume`;
   * `ProceedFreshMutationAuthorized`;
   * `ConsumeFixtureAfterMutationSuccess`;
   * `DoNotConsumeBeforeApply`;
   * `DoNotConsumeApplyFailed`;
   * `DoNotConsumeRolledBack`;
   * `DoNotConsumeUnsupportedSurface`;
   * `DoNotConsumeMainNetRefused`;
   * `ReplayRuntimeFailClosed`;
   * `ConsumeFailClosed`;
   * `ProductionConsumeUnavailable`;
   * `MainNetConsumeUnavailable`;
   * `MainNetPeerDrivenApplyRefused`.

5. Enforce ordering:

   * selector resolution;
   * runtime/evaluator/decision validation;
   * replay/freshness validation;
   * mutation authorization only on fresh;
   * mutation completion status evaluation;
   * consume only after `AppliedSuccessfully`;
   * fixture consume only after success;
   * production/MainNet consume unavailable/fail-closed.

6. Preserve non-mutation:
   This integration layer must not itself perform:

   * Run 070 call;
   * live trust swap;
   * session eviction;
   * sequence write;
   * marker write.

7. Preserve consume safety:

   * validation-only does not consume;
   * deferred does not consume;
   * authorized-but-not-applied does not consume;
   * failed apply does not consume;
   * rollback does not consume;
   * unsupported surface does not consume;
   * MainNet refused does not consume.

8. Preserve MainNet refusal:
   MainNet peer-driven apply remains refused and must not consume even if replay/freshness is fresh.

Required accepted scenarios:
A1. legacy bypass produces no consume.

A2. deferred replay/freshness produces no consume and no mutation authorization.

A3. validation-only fresh decision produces no consume.

A4. fresh decision authorizes mutation but does not consume before apply.

A5. fresh decision plus `AppliedSuccessfully` consumes in DevNet fixture only.

A6. fresh decision plus `AppliedSuccessfully` consumes in TestNet fixture only.

A7. after fixture consume, same decision validates as already consumed / fail-closed through Run 230 state.

A8. read-only validation path never consumes.

A9. failed apply never consumes.

A10. rollback never consumes.

A11. unsupported surface never consumes.

A12. MainNet refused never consumes.

A13. production consume writer path is reached and fails closed unavailable.

A14. MainNet consume writer path is reached and fails closed unavailable/refused.

A15. MainNet peer-driven apply remains refused even when replay state is fresh and mutation completion is modeled as successful.

A16. Run 232 replay/freshness runtime integration remains compatible.

A17. Run 234 consume boundary remains compatible.

Required rejection scenarios:
R1. expired decision rejected before consume.

R2. stale decision rejected before consume.

R3. replayed decision rejected before consume.

R4. already-consumed decision rejected before consume.

R5. superseded decision rejected before consume.

R6. wrong environment rejected before consume.

R7. wrong chain rejected before consume.

R8. wrong genesis rejected before consume.

R9. wrong validation surface rejected before consume.

R10. wrong mutation surface rejected before consume.

R11. wrong source identity digest rejected before consume.

R12. wrong request digest rejected before consume.

R13. wrong response digest rejected before consume.

R14. wrong transcript digest rejected before consume.

R15. wrong proposal id rejected before consume.

R16. wrong decision id rejected before consume.

R17. wrong lifecycle action rejected before consume.

R18. wrong candidate digest rejected before consume.

R19. wrong authority-domain sequence rejected before consume.

R20. wrong replay nonce rejected before consume.

R21. malformed replay state rejected before consume.

R22. malformed consume state rejected.

R23. consume attempted before apply rejected.

R24. consume attempted after failed apply rejected.

R25. consume attempted after rollback rejected.

R26. consume attempted on validation-only surface rejected.

R27. consume attempted on unsupported surface rejected.

R28. production consume unavailable rejected.

R29. MainNet consume unavailable/refused rejected.

R30. local operator cannot satisfy consume policy.

R31. peer majority cannot satisfy consume policy.

R32. validator-set rotation unsupported rejected.

R33. policy-change action unsupported rejected.

R34. rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

R35. MainNet peer-driven apply remains refused and does not consume even if replay state is fresh.

Required tests:
Add focused tests, preferably:

`crates/qbind-node/tests/run_236_governance_evaluator_replay_consume_runtime_integration_tests.rs`

Minimum coverage:

* A1–A17.
* R1–R35.
* ordering tests proving consume happens only after successful mutation completion.
* no-consume tests for deferred, validation-only, failed, rollback, unsupported, and MainNet-refused paths.
* fixture consume updates state only after success.
* production/MainNet consume unavailable tests.
* non-mutation tests.
* MainNet refusal tests.
* compatibility with Runs 235, 234, 233, 232, 231, 230, 228, 226, 224.

Required documentation:
Add:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_236.md`

Update:

* `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
* `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
* `docs/whitepaper/contradiction.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

Documentation must state:

* Run 236 is source/test replay consume runtime integration work.
* Consume is integrated as an after-success-only post-mutation step.
* Fresh is required before mutation authorization.
* Deferred, validation-only, before-apply, failed apply, rollback, unsupported surface, and MainNet refused outcomes do not consume.
* Fixture consume remains DevNet/TestNet source-test only.
* Production/MainNet consume remains unavailable/fail-closed.
* No RocksDB/file/schema/migration/storage-format change is implemented.
* No real governance engine, mutation engine, or on-chain proof verifier is implemented.
* MainNet peer-driven apply remains refused.
* Validator-set rotation remains unsupported.
* Release-binary consume-runtime-integration evidence is deferred to Run 237.
* Full C4 remains OPEN.
* C5 remains OPEN.

Validation commands:
Run at minimum:

* `cargo build -p qbind-node --lib`
* `cargo test -p qbind-node --test run_236_governance_evaluator_replay_consume_runtime_integration_tests`
* `cargo test -p qbind-node --test run_234_governance_evaluator_replay_consume_boundary_tests`
* `cargo test -p qbind-node --test run_232_governance_evaluator_replay_runtime_integration_tests`
* `cargo test -p qbind-node --test run_230_governance_evaluator_replay_state_tests`
* `cargo test -p qbind-node --test run_228_peer_evaluator_context_representation_tests`
* `cargo test -p qbind-node --test run_226_governance_evaluator_runtime_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_224_governance_evaluator_runtime_integration_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

Acceptance criteria:
Run 236 is acceptable only if:

1. Replay consume is integrated as a post-success-only runtime step.
2. Fresh is required before mutation authorization.
3. Consume is allowed only after successful mutation completion.
4. Deferred is not consumed.
5. Validation-only is not consumed.
6. Failed/rolled-back mutation is not consumed.
7. Fixture consume remains DevNet/TestNet source-test only.
8. Production/MainNet consume remains unavailable/fail-closed.
9. Rejections are non-mutating.
10. MainNet peer-driven apply remains refused and does not consume.
11. No storage/schema/migration/RocksDB/file-format change is claimed.
12. Release-binary evidence is deferred to Run 237.
13. No full C4 or C5 closure is claimed.
