Task: Run 210 — Release-Binary Custody-Attestation Policy Selector Evidence

Current accepted state:

* Run 205: source/test production custody-attestation verifier skeleton landed.
* Run 206: release-binary custody-attestation verifier-boundary evidence fully closed.
* Run 207: source/test custody-attestation payload/carrying and production-context wiring landed.
* Run 208: release-binary custody-attestation payload/carrying evidence fully closed.
* Run 209: source/test hidden custody-attestation policy selector landed:

  * module `pqc_custody_attestation_policy_surface.rs`;
  * hidden CLI flag:
    `--p2p-trust-bundle-custody-attestation-policy <POLICY>`;
  * env var:
    `QBIND_P2P_TRUST_BUNDLE_CUSTODY_ATTESTATION_POLICY=<POLICY>`;
  * supported values:
    `disabled`;
    `fixture-attestation-allowed`;
    `remote-signer-attestation-required`;
    `kms-attestation-required`;
    `hsm-attestation-required`;
    `production-attestation-required`;
    `mainnet-production-attestation-required`;
  * CLI-over-env precedence;
  * invalid values fail closed with typed parse errors;
  * default remains `CustodyAttestationPolicy::Disabled`;
  * seven preflight wrappers route selected policy into all seven Run 207 production v2 marker-decision contexts;
  * 51 tests passed;
  * full lib suite 1335 passed.
* No real cloud-KMS attestation verifier exists.
* No real PKCS#11 attestation verifier exists.
* No real HSM-vendor attestation verifier exists.
* No real KMS/HSM backend exists.
* No real RemoteSigner backend exists.
* governance execution remains unimplemented.
* real on-chain proof verification remains unimplemented.
* validator-set rotation remains open.
* MainNet peer-driven apply remains refused.
* Full C4 remains OPEN.
* C5 remains OPEN.

Objective:
Produce release-binary evidence that real `target/release/qbind-node` surfaces exercise the hidden custody-attestation policy selector from Run 209 and route the selected policy into production preflight contexts.

This run must prove:

1. default behavior remains `CustodyAttestationPolicy::Disabled`;
2. hidden CLI selector works;
3. env selector works;
4. CLI-over-env precedence is deterministic;
5. invalid selector values fail closed;
6. selected policy reaches production preflight contexts;
7. fixture attestation passes only where policy allows;
8. production/cloud-KMS/PKCS#11/HSM/RemoteSigner attestation remains fail-closed as unavailable;
9. MainNet peer-driven apply remains refused even with `mainnet-production-attestation-required`;
10. no real KMS/HSM attestation verifier, RemoteSigner backend, governance execution, real on-chain proof verifier, or validator-set rotation is claimed.

Strict scope:

* Release-binary evidence only.
* Use real `target/release/qbind-node`.
* Use release-built helper(s) to mint custody-attestation-carrying sidecars / peer-candidate material.
* No production source change unless a tiny harness-only fix is required.
* No real cloud-KMS attestation verifier.
* No real PKCS#11 attestation verifier.
* No real HSM-vendor attestation verifier.
* No real KMS implementation.
* No real HSM implementation.
* No real RemoteSigner backend.
* No networked signer daemon.
* No production signing key custody.
* No MainNet peer-driven apply enablement.
* No governance execution engine.
* No real on-chain proof verifier.
* No validator-set rotation.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* No schema/wire change.
* No authority-marker schema change.
* No sequence-file schema change.
* No trust-bundle core schema change.
* No authority lifecycle semantics change.
* Do not weaken Runs 070, 130–209.
* Do not claim full C4 or C5 closure.

Required deliverables:

1. Add release-binary helper:
   `crates/qbind-node/examples/run_210_custody_attestation_policy_release_binary_helper.rs`

2. Add release-binary harness:
   `scripts/devnet/run_210_custody_attestation_policy_release_binary.sh`

3. Add evidence archive:
   `docs/devnet/run_210_custody_attestation_policy_release_binary/`

4. Add canonical evidence report:
   `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_210.md`

5. Update narrowly:

   * `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
   * `docs/whitepaper/contradiction.md`
   * `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
   * `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
   * `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

6. Crosscheck all new/modified code and docs against existing design/spec. If contradictions or inconsistencies are found, record them in `contradiction.md` before finalizing.

Required release-built helper matrix:
The helper must exercise the Run 209 custody-attestation policy selector and Run 207 custody-attestation payload/call-site routing in release mode.

Accepted / compatible selector cases:
A1. unset CLI/env resolves to `CustodyAttestationPolicy::Disabled`.

A2. CLI `disabled` resolves to Disabled.

A3. env `disabled` resolves to Disabled.

A4. CLI `fixture-attestation-allowed` resolves to FixtureAttestationAllowed and accepts DevNet fixture attestation.

A5. env `fixture-attestation-allowed` resolves to FixtureAttestationAllowed and accepts TestNet fixture attestation.

A6. CLI `remote-signer-attestation-required` resolves to RemoteSignerAttestationRequired and reaches RemoteSigner attestation unavailable outcome.

A7. CLI `kms-attestation-required` resolves to KmsAttestationRequired and reaches KMS attestation unavailable outcome.

A8. CLI `hsm-attestation-required` resolves to HsmAttestationRequired and reaches HSM attestation unavailable outcome.

A9. CLI `production-attestation-required` resolves to ProductionAttestationRequired and reaches production attestation unavailable outcome.

A10. env `mainnet-production-attestation-required` resolves to MainnetProductionAttestationRequired and reaches MainNet production attestation unavailable / refusal outcome.

A11. CLI-over-env precedence is deterministic:

* env sets `fixture-attestation-allowed`;
* CLI sets `disabled`;
* resolved policy is Disabled.

A12. no-attestation payload remains compatible under default Disabled.

A13. GenesisBound / EmergencyCouncil / OnChainGovernance proof behavior remains unchanged when custody-attestation policy is Disabled.

A14. Run 193 custody-policy selector behavior remains compatible.

A15. Run 199 RemoteSigner-policy selector behavior remains compatible.

Required rejection cases:
R1. invalid CLI selector value rejected with typed parse error.

R2. invalid env selector value rejected with typed parse error.

R3. unrelated CLI/env does not enable custody-attestation policy.

R4. no-attestation payload rejected under FixtureAttestationAllowed.

R5. no-attestation payload rejected under ProductionAttestationRequired.

R6. fixture attestation rejected under ProductionAttestationRequired.

R7. fixture attestation rejected under MainnetProductionAttestationRequired.

R8. RemoteSigner attestation rejected as unavailable.

R9. KMS attestation rejected as unavailable.

R10. HSM attestation rejected as unavailable.

R11. cloud-KMS attestation rejected as unavailable.

R12. PKCS#11 HSM attestation rejected as unavailable.

R13. production attestation rejected as unavailable.

R14. MainNet production attestation rejected as unavailable.

R15. malformed custody-attestation material rejected.

R16. wrong environment rejected.

R17. wrong chain rejected.

R18. wrong genesis rejected.

R19. wrong authority root rejected.

R20. wrong signing-key fingerprint rejected.

R21. wrong custody class rejected.

R22. wrong backend/provider/signer id rejected.

R23. wrong key id / key label rejected.

R24. wrong suite rejected.

R25. wrong lifecycle action rejected.

R26. wrong candidate digest rejected.

R27. wrong authority-domain sequence rejected.

R28. wrong governance proof digest rejected.

R29. wrong request digest rejected.

R30. wrong response digest rejected.

R31. wrong transcript digest rejected.

R32. stale/replayed attestation rejected.

R33. expired attestation rejected.

R34. invalid attestation commitment rejected.

R35. local operator cannot satisfy production attestation.

R36. peer majority / gossip count cannot satisfy production attestation.

R37. validation-only rejection writes no marker and no sequence.

R38. mutating rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

R39. invalid live inbound `0x05` custody-attestation candidate is not propagated, staged, or applied, if live surface is included.

R40. MainNet peer-driven apply remains refused even with MainnetProductionAttestationRequired and fixture attestation.

Required real binary checks:
Use real `target/release/qbind-node` to prove:

* hidden CLI flag is present but hidden from normal help, if feasible to check.
* env selector activates custody-attestation policy.
* CLI selector activates custody-attestation policy.
* CLI-over-env precedence works.
* invalid selector value fails closed.
* MainNet peer-driven apply remains refused.
* default behavior does not expose or enable production custody attestation.
* no `custody attestation active` / `production attestation active` / `KMS attestation active` / `HSM attestation active` / `PKCS#11 active` claim is emitted.
* no real KMS/HSM active claim is emitted.
* existing KMS/HSM backend behavior from Run 204 remains compatible.
* existing RemoteSigner transport behavior from Run 202 remains compatible.
* existing custody policy selector behavior from Run 193 remains compatible.
* existing governance fixture proof paths remain compatible.
* no governance execution claim is emitted.
* no validator-set rotation claim is emitted.

Required source/release reachability proof:
The report must include source grep/call-site proof for:

* `pqc_custody_attestation_policy_surface`;
* `QBIND_P2P_TRUST_BUNDLE_CUSTODY_ATTESTATION_POLICY`;
* `p2p_trust_bundle_custody_attestation_policy`;
* `custody_attestation_policy_from_selector`;
* `custody_attestation_policy_from_cli_or_env`;
* seven per-surface custody-attestation-policy preflight wrappers;
* `CustodyAttestationPolicy::{Disabled, FixtureAttestationAllowed, RemoteSignerAttestationRequired, KmsAttestationRequired, HsmAttestationRequired, ProductionAttestationRequired, MainnetProductionAttestationRequired}`;
* Run 207 custody-attestation payload routing helpers;
* production/cloud-KMS/PKCS#11/HSM/RemoteSigner attestation unavailable variants;
* MainNet attestation refusal helper.

Required mutation / no-mutation proof:
For rejected custody-attestation-policy scenarios:

* no Run 070 apply call;
* no live trust swap;
* no session eviction;
* no sequence write;
* no marker write;
* marker bytes unchanged where present;
* sequence bytes unchanged where present;
* no `.tmp` residue;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead.

For any accepted fixture attestation mutating compatibility scenario:

* selector resolution occurs before custody-attestation material parse;
* custody-attestation material parse occurs before marker decision;
* attestation verification occurs before apply/mutation;
* custody validation occurs before apply/mutation;
* backend / RemoteSigner validation occurs before apply/mutation where applicable;
* lifecycle validation occurs before apply/mutation;
* governance validation occurs before apply/mutation where applicable;
* Run 055 sequence commit succeeds if a mutating path is exercised;
* v2 marker persists strictly after sequence commit.

Required denylist:
Across the run prove:

* no MainNet apply;
* no autonomous apply;
* no apply on receipt;
* no peer-majority authority;
* no real KMS backend claim;
* no real HSM backend claim;
* no KMS/HSM active claim;
* no cloud KMS active claim;
* no PKCS#11 active claim;
* no real RemoteSigner backend claim;
* no real custody attestation claim;
* no production custody attestation active claim;
* no real governance execution claim;
* no real on-chain governance proof claim;
* no validator-set rotation claim;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead;
* no schema/wire/metric drift;
* no marker write before sequence commit;
* no sequence write on validation-only surfaces;
* no marker write on validation-only surfaces.

Required captured metadata:

* qbind-node SHA-256 and ELF Build ID;
* helper SHA-256 and ELF Build ID;
* git commit hash;
* rustc/cargo versions;
* exact commands;
* stdout/stderr logs;
* per-scenario exit codes;
* custody-attestation policy values;
* custody-attestation fixture evidence/input/payload paths and hashes;
* marker/sequence JSON and SHA before/after where applicable;
* data-dir inventories;
* denylist grep results.

Validation commands:
Run at minimum:

* `cargo build --release -p qbind-node --bin qbind-node`
* `cargo build --release -p qbind-node --example run_210_custody_attestation_policy_release_binary_helper`
* `bash scripts/devnet/run_210_custody_attestation_policy_release_binary.sh`
* `cargo test -p qbind-node --test run_209_custody_attestation_policy_selector_tests`
* `cargo test -p qbind-node --test run_207_custody_attestation_payload_callsite_tests`
* `cargo test -p qbind-node --test run_205_custody_attestation_verifier_tests`
* `cargo test -p qbind-node --test run_203_kms_hsm_backend_boundary_tests`
* `cargo test -p qbind-node --test run_201_remote_signer_transport_boundary_tests`
* `cargo test -p qbind-node --test run_198_remote_signer_policy_selector_tests`
* `cargo test -p qbind-node --test run_196_remote_signer_payload_callsite_tests`
* `cargo test -p qbind-node --test run_194_remote_authority_signer_boundary_tests`
* `cargo test -p qbind-node --test run_192_authority_custody_policy_selector_tests`
* `cargo test -p qbind-node --test run_190_authority_custody_payload_callsite_tests`
* `cargo test -p qbind-node --test run_188_authority_custody_boundary_tests`
* `cargo test -p qbind-node --test run_186_onchain_governance_production_verifier_boundary_tests`
* `cargo test -p qbind-node --test run_184_onchain_governance_payload_carrying_tests`
* `cargo test -p qbind-node --test run_182_onchain_governance_production_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_180_onchain_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_178_onchain_governance_proof_tests`
* `cargo test -p qbind-node --test run_176_live_0x05_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_173_validation_only_governance_required_policy_tests`
* `cargo test -p qbind-node --test run_171_governance_required_policy_selector_tests`
* `cargo test -p qbind-node --test run_169_governance_proof_loader_surface_integration_tests`
* `cargo test -p qbind-node --test run_167_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_165_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_163_governance_authority_verifier_tests`
* `cargo test -p qbind-node --test run_161_lifecycle_marker_integration_tests`
* `cargo test -p qbind-node --test run_159_authority_signing_key_lifecycle_tests`
* `cargo test -p qbind-node --test run_157_unified_testnet_fixture_universe_tests`
* `cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests`
* `cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests`
* `cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests`
* `cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests`
* `cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests`
* `cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Documentation requirements:
`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_210.md` must state:

* Run 210 is release-binary custody-attestation policy selector evidence.
* Default remains `CustodyAttestationPolicy::Disabled`.
* Hidden CLI/env selectors activate custody-attestation policies.
* CLI-over-env precedence is deterministic.
* Invalid values fail closed.
* Fixture attestation remains DevNet/TestNet evidence-only.
* Fixture attestation cannot satisfy MainNet production attestation.
* Production/cloud-KMS/PKCS#11/HSM/RemoteSigner attestation remains unavailable/fail-closed.
* MainNet peer-driven apply remains refused.
* No real cloud-KMS attestation verifier is implemented.
* No real PKCS#11 attestation verifier is implemented.
* No real HSM-vendor attestation verifier is implemented.
* No real KMS/HSM backend is implemented.
* No real RemoteSigner backend is implemented.
* governance execution remains unimplemented.
* real on-chain proof verification remains unimplemented.
* validator-set rotation remains open.
* existing custody/KMS-HSM/RemoteSigner/governance proof paths remain compatible.
* full C4 remains open.
* C5 remains open.

Acceptance criteria:
Run 210 is acceptable only if:

1. Real release binaries exercise the hidden custody-attestation policy selector.
2. Default Disabled remains proven.
3. CLI selector and env selector both work.
4. CLI-over-env precedence is proven.
5. Invalid selector values fail closed.
6. Fixture attestation passes only where selected policy allows.
7. Production/cloud-KMS/PKCS#11/HSM/RemoteSigner attestation remains fail-closed as unavailable.
8. MainNet peer-driven apply remains refused even with MainnetProductionAttestationRequired and fixture attestation.
9. Rejected custody-attestation-policy cases produce no mutation.
10. Existing custody/KMS-HSM/RemoteSigner/governance proof paths remain compatible.
11. No real KMS/HSM attestation / RemoteSigner backend / governance execution / real on-chain proof / validator-set rotation claim is made.
12. No full C4 or C5 closure is claimed.
