Run 266 — Source/test durable-completion settlement-projection commitment / ledger-finalization interface boundary

You are continuing QBIND after Run 265.

Current accepted state:

Run 264 closed the source/test durable-completion consumer settlement-projection sink boundary.

Run 265 closed the release-binary evidence gap for Run 264. Run 265 proved, on real `target/release/qbind-node` plus a release-built helper, that the Run 264 settlement-projection sink boundary symbols are present and exercised in release mode. The helper remains dead code from production runtime. The fixture settlement-projection sink mutates only modeled in-memory `DurableCompletionConsumerSettlementProjectionLedger` state. Production/MainNet/external settlement-projection sinks remain reachable but unavailable/fail-closed.

Full C4 remains OPEN. C5 remains OPEN.

Exact objective:

Add the next source/test-only boundary above Run 264: a modeled durable-completion **settlement-projection commitment / ledger-finalization interface boundary**.

This boundary models the first post-settlement-projection commitment step that a future production settlement subsystem might use after a Run 264 `SettlementProjectionRecorded` outcome. It should convert a valid modeled settlement-projection state into a typed settlement-commitment intent and modeled in-memory settlement-commitment receipt.

This is a mockable, in-memory, source/test boundary only.

Strict scope:

* Source/test only.
* No release-binary evidence in this run.
* No production runtime enablement.
* No real settlement backend.
* No real settlement execution.
* No real settlement finality.
* No real audit-ledger acknowledgement.
* No real audit ledger write.
* No real external publication.
* No real external-publication confirmation.
* No real production attestation/finalization/completion-report/durable-consume/persistent-replay backend.
* No real governance execution engine.
* No real production mutation engine.
* No real on-chain governance proof verifier.
* No KMS/HSM/RemoteSigner backend.
* No RocksDB/file/schema/migration/storage-format change.
* No wire/schema/marker/sequence/trust-bundle change.
* No Run 070 call.
* No `LivePqcTrustState` mutation.
* No real trust swap.
* No session eviction.
* No sequence write.
* No marker write.
* No network I/O.
* No MainNet governance.
* No MainNet peer-driven apply.
* No validator-set rotation.
* No policy-change enablement.
* Keep Full C4 OPEN.
* Keep C5 OPEN.

Required new source module:

`crates/qbind-node/src/pqc_governance_durable_completion_settlement_commitment.rs`

Register it in:

`crates/qbind-node/src/lib.rs`

Required new test file:

`crates/qbind-node/tests/run_266_durable_completion_settlement_commitment_tests.rs`

Required evidence doc:

`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_266.md`

Required narrow doc updates:

* `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
* `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
* `docs/whitepaper/contradiction.md`

Required ordering:

Preserve the full prior modeled chain:

1. Run 246 pipeline success before Run 248 sink intent.
2. Run 248 `ConsumeReceiptRecorded` before Run 250 completion report.
3. Run 250 `CompletionReportRecorded` before Run 252 finalization.
4. Run 252 `DurableCompletionFinalized` before Run 254 attestation.
5. Run 254 `DurableCompletionAttested` before Run 256 backend request.
6. Run 256 `BackendSubmissionRecorded` before Run 258 audit/publication receipt.
7. Run 258 `AuditReceiptRecorded` before Run 260 acknowledgement / confirmation.
8. Run 260 `AcknowledgementRecorded` before Run 262 consumer request.
9. Run 262 `AcknowledgementConsumed` before Run 264 settlement-projection request.
10. Run 264 `SettlementProjectionRecorded` before any Run 266 settlement-commitment request.
11. Only Run 266 `SettlementCommitmentRecorded`, or an equivalently named success outcome, may authorize a new modeled settlement-commitment state.

Suggested type names:

* `DurableCompletionSettlementCommitmentKind`
* `DurableCompletionSettlementCommitmentPolicy`
* `DurableCompletionSettlementCommitmentIdentity`
* `DurableCompletionSettlementCommitmentInput`
* `DurableCompletionSettlementCommitmentExpectations`
* `DurableCompletionSettlementCommitmentRequest`
* `DurableCompletionSettlementCommitmentResponse`
* `DurableCompletionSettlementCommitmentRecord`
* `DurableCompletionSettlementCommitmentDigest`
* `DurableCompletionSettlementCommitmentTranscriptDigest`
* `DurableCompletionSettlementCommitmentOutcome`

Suggested sink trait:

`GovernanceDurableCompletionSettlementCommitmentSink`

Suggested implementations:

* `FixtureDurableCompletionSettlementCommitmentSink`
* `ProductionSettlementCommitmentSink`
* `MainNetSettlementCommitmentSink`
* `ExternalSettlementCommitmentSink`

Implementation rules:

* Fixture sink is DevNet/TestNet only.
* Fixture sink mutates only an in-memory `DurableCompletionSettlementCommitmentLedger`.
* Production/MainNet/external settlement-commitment sinks are reachable but unavailable/fail-closed.
* No real settlement, no real settlement finality, no external call, no network call, no file write, no RocksDB write, no marker write, no sequence write, no Run 070 call, no `LivePqcTrustState` mutation.

Required ledger:

* `DurableCompletionSettlementCommitmentLedger`
* `DurableCompletionSettlementCommitmentLedgerRecord`
* `DurableCompletionSettlementCommitmentLedgerSnapshot`
* `DurableCompletionSettlementCommitmentLedgerStatus`

Ledger semantics:

* One valid settlement-commitment record inserts exactly one in-memory record.
* Duplicate identical settlement-commitment record is idempotent.
* Same settlement-commitment record id with a different digest is equivocation and fails closed.
* Rollback restores the in-memory snapshot.
* Rollback failure never claims success.
* Fixture ledger never writes files, RocksDB, markers, sequences, external publications, confirmations, audit-ledger entries, real settlement records, or production durable state.

Suggested outcome enum:

Use exact names close to these:

* `LegacyBypassNoSettlementCommitment`
* `RejectedBeforeSettlementProjectionNoCommitment`
* `SettlementProjectionDidNotRecordNoCommitment`
* `SettlementCommitmentRecorded`
* `SettlementCommitmentDuplicateIdempotent`
* `SettlementCommitmentRejectedBeforeRecord`
* `SettlementCommitmentRecordFailedNoCommitment`
* `SettlementCommitmentRolledBackNoCommitment`
* `SettlementCommitmentRollbackFailedFatalNoCommitment`
* `SettlementCommitmentAmbiguousFailClosedNoCommitment`
* `ProductionSettlementCommitmentUnavailableNoCommitment`
* `MainNetSettlementCommitmentUnavailableNoCommitment`
* `ExternalSettlementCommitmentUnavailableNoCommitment`
* `MainNetPeerDrivenApplyRefusedNoCommitment`
* `ValidatorSetRotationUnsupportedNoCommitment`
* `PolicyChangeUnsupportedNoCommitment`

Only `SettlementCommitmentRecorded` may authorize a new modeled settlement-commitment state.

Required projection helpers:

* `project_settlement_projection_outcome_to_commitment_request(...)`
* `evaluate_durable_completion_settlement_commitment(...)`
* `recover_durable_completion_settlement_commitment_window(...)`
* `settlement_commitment_outcome_authorizes_record(...)`
* `settlement_commitment_outcome_projects_to_recorded(...)`

Projection semantics:

* Only Run 264 `SettlementProjectionRecorded` creates a settlement-commitment request.
* Run 264 duplicate-idempotent settlement-projection outcome may only match an already-recorded settlement-commitment record and must not create a new one by itself.
* Every other Run 264 outcome maps to no-commitment fail-closed.
* Run 262 `AcknowledgementConsumed` alone creates no settlement-commitment request.
* Run 260 `AcknowledgementRecorded` alone creates no settlement-commitment request.
* Run 258 `AuditReceiptRecorded` alone creates no settlement-commitment request.
* Run 256 `BackendSubmissionRecorded` alone creates no settlement-commitment request.
* All earlier stages alone create no settlement-commitment request.
* Binding mismatch leaves invocation count zero.
* Digest mismatch after invocation does not record.
* Failure/rollback/ambiguous/unavailable paths fail closed.

Required invariant helpers:

Add grep-verifiable helpers with names close to:

* `durable_completion_settlement_commitment_rejection_is_non_mutating`
* `durable_completion_settlement_commitment_never_calls_run_070`
* `durable_completion_settlement_commitment_never_mutates_live_pqc_trust_state`
* `durable_completion_settlement_commitment_never_writes_sequence_or_marker`
* `durable_completion_settlement_commitment_no_rocksdb_file_schema_migration_change`
* `durable_completion_settlement_commitment_no_external_publication`
* `durable_completion_settlement_commitment_no_real_audit_ledger`
* `durable_completion_settlement_commitment_no_real_settlement`
* `durable_completion_settlement_commitment_projection_required`
* `durable_completion_settlement_commitment_record_required_before_committed`
* `durable_completion_settlement_commitment_failed_record_never_records`
* `durable_completion_settlement_commitment_rollback_never_records`
* `durable_completion_settlement_commitment_ambiguous_window_fails_closed`
* `durable_completion_settlement_commitment_mainnet_peer_driven_apply_refused_first`
* `durable_completion_settlement_commitment_production_mainnet_unavailable`
* `durable_completion_settlement_commitment_external_unavailable`
* `durable_completion_settlement_commitment_validator_set_rotation_unsupported`
* `durable_completion_settlement_commitment_policy_change_unsupported`
* `durable_completion_settlement_commitment_local_operator_cannot_satisfy_mainnet_authority`
* `durable_completion_settlement_commitment_peer_majority_cannot_satisfy_mainnet_authority`

Required tests:

Accepted / compatible:

* Disabled settlement-commitment policy preserves legacy bypass and records nothing.
* Disabled settlement-projection / consumer / acknowledgement / receipt / backend / attestation / finalization / reporter / sink / pipeline policy never reaches settlement commitment.
* DevNet fixture chain records exactly one modeled in-memory settlement-commitment record only after the full Run 246 → 248 → 250 → 252 → 254 → 256 → 258 → 260 → 262 → 264 → 266 chain.
* TestNet fixture chain records exactly one modeled in-memory settlement-commitment record only after the same full chain.
* Add-root / retire-root / revoke-root / emergency-revoke-root / noop modeled actions preserve ordering and commit only after Run 264 `SettlementProjectionRecorded`.
* Duplicate identical settlement-commitment record is idempotent.
* Run 264 duplicate-idempotent settlement-projection outcome may only match an already-recorded settlement-commitment record and never creates a new commitment by itself.
* Production settlement-commitment path is reachable but unavailable/fail-closed and records no state.
* MainNet settlement-commitment path is reachable but unavailable/fail-closed and records no state.
* External settlement-commitment path is reachable but unavailable/fail-closed and performs no publication/network I/O.
* MainNet peer-driven apply is refused before pipeline progression, sink invocation, reporter invocation, finalizer invocation, attestor invocation, backend invocation, receipt invocation, acknowledgement invocation, consumer invocation, settlement-projection invocation, and settlement-commitment invocation.
* Validator-set rotation unsupported and records no state.
* Policy-change unsupported and records no state.

Rejected / fail-closed:

* Every rejection before Run 266 produces no settlement-commitment request and no record.
* Settlement-projection did-not-record / rejected-before-record / record failure / rollback / rollback-failed / ambiguous window produces no settlement-commitment invocation and no record.
* Commitment before Run 264 `SettlementProjectionRecorded` is rejected.
* Same settlement-commitment record id with different digest fails closed as equivocation.
* Wrong environment / chain / genesis / governance surface / mutation surface fails before sink invocation.
* Wrong commitment identity / policy / kind / request / response / record digest fails closed.
* Wrong settlement-projection / consumer / acknowledgement / receipt / backend / attestation / finalization / completion-report / sink / pipeline digest fails closed.
* Wrong proposal id / decision id / candidate digest / authority-domain sequence fails closed.
* Malformed request / response fails closed.
* Local operator key cannot satisfy MainNet authority.
* Peer majority cannot satisfy MainNet authority.
* Production/MainNet/external unavailable paths record no state and perform no publication.
* Rejected paths produce no Run 070 call, no `LivePqcTrustState` mutation, no trust swap, no session eviction, no sequence write, no marker write, no RocksDB/file/schema/migration/storage-format change, no external publication, no confirmation, no real audit-ledger write, and no real settlement write.

Recovery / crash-window:

* before-pipeline through after-settlement-projection-success-before-commitment-request windows fail closed unless explicit matching settlement-projection success exists.
* after-settlement-commitment-request-before-record fails closed.
* after-settlement-commitment-record-before-success fails closed unless explicit matching commitment record exists.
* after-settlement-commitment-success recovers as recorded.
* ambiguous, record-failed, rollback-completed, rollback-failed, unknown, production/MainNet/external windows fail closed.
* MainNet peer-driven apply refusal precedes recovery classification where applicable.

Projection:

* Only Run 264 `SettlementProjectionRecorded` creates a settlement-commitment request.
* Run 262 `AcknowledgementConsumed` alone creates no settlement-commitment request.
* Run 260 `AcknowledgementRecorded` alone creates no settlement-commitment request.
* Run 258 `AuditReceiptRecorded` alone creates no settlement-commitment request.
* Run 256 backend submission alone creates no settlement-commitment request.
* All earlier stage successes alone create no settlement-commitment request.
* Run 264 duplicate-idempotent settlement-projection outcome creates no new settlement-commitment record unless a matching commitment record already exists.
* Only `SettlementCommitmentRecorded` authorizes modeled commitment state.
* Every other outcome does not record.

Stage-ordering:

* MainNet peer-driven apply refusal happens before every later stage.
* Evaluator/call-site authorization precedes durable replay.
* Durable replay precedes mutation engine.
* Mutation engine precedes modeled applier.
* Modeled applier precedes sink intent.
* Sink receipt precedes completion report.
* Completion report precedes finalization.
* Finalization precedes attestation.
* Attestation precedes backend request.
* Backend submission precedes audit receipt request.
* Audit receipt precedes acknowledgement request.
* Acknowledgement record precedes consumer request.
* Consumer record precedes settlement-projection request.
* Settlement-projection record precedes settlement-commitment request.
* Settlement-commitment record happens only after settlement-projection success.
* Rejection before settlement-commitment stage leaves settlement-commitment invocation count zero.
* Settlement-commitment record failure does not invalidate settlement-projection state but does not authorize commitment state.
* Settlement-commitment rollback failure is fatal/fail-closed and does not authorize commitment state.

Non-mutation:

* no Run 070 call;
* no `LivePqcTrustState` mutation;
* no trust swap;
* no session eviction;
* no sequence write;
* no marker write;
* no RocksDB/file/schema/migration/storage-format write;
* no wire/schema/marker/sequence/trust-bundle change;
* no external network publication;
* no real settlement;
* no real external-publication confirmation;
* no real audit-ledger write;
* fixture commitment sink mutates only `DurableCompletionSettlementCommitmentLedger`;
* production/MainNet/external paths remain unavailable/fail-closed.

Validation commands:

Run at minimum:

* `cargo build -p qbind-node --lib`
* `cargo test -p qbind-node --test run_266_durable_completion_settlement_commitment_tests`
* `cargo test -p qbind-node --test run_264_durable_completion_consumer_settlement_projection_tests`
* `cargo test -p qbind-node --test run_262_durable_completion_acknowledgement_consumer_tests`
* `cargo test -p qbind-node --test run_260_durable_completion_audit_receipt_acknowledgement_tests`
* `cargo test -p qbind-node --test run_258_durable_completion_audit_publication_receipt_tests`
* `cargo test -p qbind-node --test run_256_durable_completion_attestation_backend_tests`
* `cargo test -p qbind-node --test run_254_modeled_durable_completion_attestation_projection_tests`
* `cargo test -p qbind-node --test run_252_modeled_durable_completion_finalization_projection_tests`
* `cargo test -p qbind-node --test run_250_modeled_durable_consume_completion_reporter_tests`
* `cargo test -p qbind-node --test run_248_modeled_durable_consume_projection_sink_tests`
* `cargo test -p qbind-node --test run_246_governance_modeled_end_to_end_pipeline_tests`
* `cargo test -p qbind-node --test run_244_modeled_governance_trust_mutation_applier_tests`
* `cargo test -p qbind-node --test run_242_governance_execution_mutation_engine_tests`
* `cargo test -p qbind-node --test run_240_governance_evaluator_replay_durable_runtime_integration_tests`
* `cargo test -p qbind-node --test run_238_governance_evaluator_replay_durable_backend_tests`
* `cargo test -p qbind-node --test run_236_governance_evaluator_replay_consume_runtime_integration_tests`
* `cargo test -p qbind-node --test run_234_governance_evaluator_replay_consume_boundary_tests`
* `cargo test -p qbind-node --test run_232_governance_evaluator_replay_runtime_integration_tests`
* `cargo test -p qbind-node --test run_230_governance_evaluator_replay_state_tests`
* `cargo test -p qbind-node --test run_228_peer_evaluator_context_representation_tests`
* `cargo test -p qbind-node --test run_226_governance_evaluator_runtime_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_224_governance_evaluator_runtime_integration_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

Security scanning:

* Run secret scanning and report result.
* Run CodeQL if available and report honestly.
* Because Run 266 changes source and tests, do not classify it as trivial unless the checker itself does so.
* If Rust CodeQL analysis is skipped because the database is too large, times out, or tooling is unavailable, state that explicitly and do not claim CodeQL coverage.

Documentation:

Create:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_266.md`

Update narrowly:

* `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
* `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
* `docs/whitepaper/contradiction.md`

Docs must state:

* Run 266 is source/test only.
* Run 266 adds a modeled durable-completion settlement-projection commitment / ledger-finalization interface boundary.
* The boundary consumes only Run 264 `SettlementProjectionRecorded`.
* Run 264 `SettlementProjectionRecorded` is required before any settlement-commitment request can exist.
* Only `SettlementCommitmentRecorded` authorizes modeled settlement-commitment state.
* Fixture commitment sink is DevNet/TestNet evidence-only and in-memory only.
* Production/MainNet/external commitment sinks remain reachable but unavailable/fail-closed.
* No real settlement is implemented.
* No real settlement finality is implemented.
* No real audit-ledger acknowledgement is implemented.
* No real external-publication confirmation is implemented.
* No real external publication is implemented.
* No real production backend is implemented.
* No RocksDB/file/schema/migration/storage-format change is implemented.
* No wire/schema/marker/sequence/trust-bundle change is implemented.
* No MainNet governance is enabled.
* No MainNet peer-driven apply is enabled.
* No validator-set rotation is implemented.
* No policy-change enablement is implemented.
* No Run 070 call.
* No `LivePqcTrustState` mutation.
* Rejected commitment paths are non-mutating.
* Full C4 remains OPEN.
* C5 remains OPEN.

Contradiction handling:

* Crosscheck Run 266 against Runs 050–265.
* If no contradiction is found, add a “Run 266 — no contradiction found” entry to `docs/whitepaper/contradiction.md`.
* If contradiction is found, record it explicitly with exact file/function/doc reference and recommend the smallest safe fix.

Expected verdict after completion:

Source/test positive only if:

* Run 266 tests pass.
* Full regression corpus passes.
* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_266.md` exists.
* Protocol/ops docs are updated.
* `contradiction.md` has a Run 266 entry.
* Secret scan result is reported.
* CodeQL result or honest skip/timeout reason is reported.
* C4/C5 remain OPEN.

Suggested next step after successful Run 266:

Run 267 — release-binary evidence for the Run 266 durable-completion settlement-projection commitment / ledger-finalization interface boundary.

Final response format:

1. Exact verdict.
2. Files changed.
3. Tests run and results.
4. Security invariants preserved.
5. Honest limitations.
6. C4/C5 status.
7. Suggested Run 267 next step.
