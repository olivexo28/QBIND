Continue QBIND from the current state.

Project:
- QBIND post-quantum L1
- security-first, production-grade, execution-first
- C4 is the main live operability tracker
- C5 tracks timeout-verification activation, forged-traffic rejection, and transport-root dependency
- no broad redesign
- no silent regressions
- no hype / no overclaiming

Current state:
- Runs 037–045 hardened PQC transport.
- Runs 046–049 completed live timeout-backoff evidence through the 800-tick cap.
- Runs 050–068 completed the signed trust-bundle lifecycle path:
  - structured PQC trust bundles
  - signed ML-DSA-44 bundle verification
  - chain_id crosscheck
  - sequence anti-rollback persistence
  - activation-height gating
  - minimum activation-height policy
  - root/leaf revocation
  - per-entry revocation activation_height
  - local revoked-leaf startup self-check
  - local revoked issuer-root startup self-check
  - DevNet/TestNet/MainNet evidence
  - N=4 MainNet signed-bundle peer-connection evidence
- Run 069 landed validation-only trust-bundle reload-check:
  - hidden --p2p-trust-bundle-reload-check <PATH>
  - validates candidate bundles using startup security pipeline
  - does not apply live trust changes
  - does not mutate active roots/revocations/sessions
  - does not persist sequence or burn sequence numbers
- Run 070 landed reload-apply contract and rollback semantics.
- Run 071 introduced LivePqcTrustState and live snapshot reads.
- Run 072 added production P2P session eviction.
- Run 073 landed ProductionLiveTrustApplyContext:
  - composed validation + live trust swap + session eviction + sequence commit
  - removed UnsupportedRuntimeContext from the local-file apply path
  - process-start reload-apply worked
- Run 074 strongest-positive:
  - landed long-running local operator-triggered live trust-bundle apply
  - new LiveReloadController
  - SIGHUP handler behind hidden opt-in flags:
    - --p2p-trust-bundle-live-reload-enabled
    - --p2p-trust-bundle-live-reload-path <PATH>
  - valid candidate applies while node remains running
  - invalid candidate does not mutate trust state
  - invalid candidate does not mutate sequence
  - invalid candidate does not evict sessions
  - concurrent trigger guarded by CAS as AlreadyInProgress
  - only Fatal arm signals shutdown
  - six new live-reload metrics
  - reload-check remains validation-only
  - process-start reload-apply remains correct
- C4 remains OPEN.
- C5 remains OPEN / narrowed.

Current remaining C4 trust/lifecycle blockers include:
- peer-supplied / gossiped trust-bundle acceptance
- activation_epoch runtime source
- in-binary / on-chain bundle-signing-key ratification
- external KMS/HSM custody integration
- production fast-sync / consensus-storage restore
- per-environment production trust-anchor operation
- selective session retention if ever wanted
- admin-API or filesystem-watcher trigger if ever desired

Task:
Run 075. Update the operator lifecycle runbook to document the hot-reload lifecycle from Runs 069–074.

Primary objective:
Update docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md so operators have accurate guidance for:
- validation-only reload-check from Run 069;
- process-start local reload-apply from Run 073;
- long-running local SIGHUP-triggered live apply from Run 074;
- sequence commit ordering;
- session eviction behavior;
- invalid-candidate no-mutation behavior;
- concurrent trigger behavior;
- metrics and evidence expectations;
- explicit boundary that peer/gossiped bundle acceptance is still not implemented.

Important framing:
This is a documentation and consistency task.
Do not change runtime code unless a contradiction is found.
Do not implement peer/gossiped bundle acceptance.
Do not implement KMS/HSM.
Do not implement activation_epoch.
Do not implement signing-key ratification.
Do not implement fast-sync restore.
Do not implement admin API or filesystem watcher.
Do not redesign trust bundles.
Do not redesign KEMTLS.
Do not modify consensus or timeout logic.
Do not claim full C4 or C5 closure.

Strict scope:
- Docs-first.
- Source changes are allowed only if the runbook reveals a real contradiction in current code or tests.
- If no contradiction is found, do not touch crates/**/src/**.
- Preserve all Run 050–074 claims accurately.
- Do not recommend --p2p-trusted-root fallback.
- Do not recommend DummySig / DummyKem / DummyAead.
- Do not recommend unsigned TestNet/MainNet bundles.
- Do not recommend peer/gossiped trust-bundle acceptance.
- Do not recommend unauthenticated remote admin mutation.
- Do not recommend key reuse between transport roots, bundle-signing keys, validator consensus keys, or leaf KEM material.
- Crosscheck all runbook updates against Run 069–074 evidence and contradiction.md.

Required investigation:

1. Inspect current operator runbook and hot-reload evidence
   Inspect:
   - docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md
   - docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_069.md
   - docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_070.md
   - docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_071.md
   - docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_072.md
   - docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_073.md
   - docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_074.md
   - docs/whitepaper/contradiction.md

   Inspect code only for reference:
   - crates/qbind-node/src/pqc_trust_reload.rs
   - crates/qbind-node/src/pqc_live_trust.rs
   - crates/qbind-node/src/p2p_session_eviction.rs
   - crates/qbind-node/src/pqc_live_trust_apply.rs
   - crates/qbind-node/src/pqc_live_trust_reload.rs
   - crates/qbind-node/src/main.rs
   - crates/qbind-node/src/cli.rs
   - crates/qbind-node/src/metrics.rs
   - crates/qbind-node/tests/run_069_pqc_trust_bundle_reload_check_tests.rs
   - crates/qbind-node/tests/run_070_pqc_trust_bundle_reload_apply_tests.rs
   - crates/qbind-node/tests/run_071_pqc_live_trust_tests.rs
   - crates/qbind-node/tests/run_072_p2p_session_eviction_tests.rs
   - crates/qbind-node/tests/run_073_pqc_trust_bundle_reload_apply_runtime_tests.rs
   - crates/qbind-node/tests/run_074_pqc_trust_bundle_live_reload_tests.rs

   Document investigation findings in Run 075 evidence doc.

2. Update runbook for reload-check
   Add or revise operator guidance:
   - --p2p-trust-bundle-reload-check <PATH> is validation-only.
   - It validates a candidate using the trust-bundle security pipeline.
   - It does not mutate LivePqcTrustState.
   - It does not evict sessions.
   - It does not commit sequence.
   - It is suitable for preflight validation before operator apply.
   - It is not peer/gossiped bundle acceptance.

3. Update runbook for process-start reload apply
   Add or revise operator guidance:
   - process-start local-file reload-apply from Run 073 uses ProductionLiveTrustApplyContext.
   - It validates, swaps live trust state, evicts sessions, and commits sequence.
   - It is local operator-controlled.
   - It is not long-running SIGHUP reload unless Run 074 flags are used.
   - It is not remote or peer-supplied.

4. Update runbook for long-running SIGHUP live reload
   Add or revise operator guidance:
   - --p2p-trust-bundle-live-reload-enabled must be explicitly enabled.
   - --p2p-trust-bundle-live-reload-path <PATH> must be configured.
   - SIGHUP triggers validation/apply of that local path.
   - Valid candidate applies while node remains running.
   - Invalid candidate returns VERDICT=invalid and does not mutate trust, sequence, or sessions.
   - Concurrent trigger returns AlreadyInProgress / equivalent and does not start a second apply.
   - Fatal rollback ambiguity may stop the node.
   - This is local operator-triggered only.
   - This is not peer/gossiped propagation.
   - This is not a remote admin API.

5. Document apply pipeline and ordering
   The runbook must state the safe order:
   - validate candidate
   - snapshot old LivePqcTrustState
   - swap to new LivePqcTrustState
   - evict sessions
   - commit sequence persistence
   - report success

   Failure behavior:
   - validation failure: no swap, no eviction, no sequence commit
   - swap failure: old state remains, no eviction, no sequence commit
   - eviction failure: rollback live state, no sequence commit
   - sequence commit failure: rollback live state; fatal if rollback fails
   - invalid candidate while running does not kill the node
   - fatal rollback ambiguity can stop the node

6. Document session eviction behavior
   Add guidance:
   - successful live reload evicts all existing P2P sessions conservatively.
   - reason: sessions may have authenticated under old roots/leaves.
   - reconnect behavior depends on existing dial/listener loops.
   - selective session retention is not implemented.
   - operators should expect short liveness disruption during reload.
   - do not treat reload as zero-disruption yet.

7. Document metrics and evidence expectations
   Add or revise checklist items:
   - reload-check candidate validation logs.
   - live reload trigger metrics.
   - live reload success/failure metrics.
   - session eviction counters from Run 072.
   - sequence file before/after apply.
   - old/new bundle fingerprint prefix.
   - old/new sequence.
   - no static-root fallback proof.
   - no Dummy crypto proof.
   - invalid candidate no-mutation proof.
   - concurrent trigger behavior if tested.

8. Update operator workflows
   Normal trust-bundle rotation:
   - preflight with reload-check.
   - copy signed candidate to configured local reload path.
   - trigger SIGHUP.
   - confirm apply success, sequence update, session eviction, and reconnect behavior.
   - archive evidence.

   Emergency revocation:
   - if using live reload, expect immediate session eviction.
   - confirm affected old sessions are closed.
   - confirm revoked cert/root no longer accepted after reconnect.
   - do not bypass with static roots.

   Incident checklist:
   - distinguish invalid candidate from fatal rollback ambiguity.
   - invalid candidate should not require node restart.
   - fatal reload state ambiguity should be treated as operational incident.

9. Update residual risks
   Ensure remaining open items are accurate:
   - peer/gossiped bundle acceptance remains open.
   - activation_epoch runtime source remains open.
   - KMS/HSM custody remains open.
   - signing-key ratification remains open.
   - fast-sync / consensus-storage restore remains open.
   - per-environment production trust-anchor operation remains open.
   - selective session retention remains open.
   - remote admin API / filesystem watcher trigger remains absent unless implemented.
   - full C4/C5 remain open.
   - Remove or update any text saying long-running local live reload is still open.

10. Update evidence mapping
   Add mapping:
   - Run 069: validation-only reload-check.
   - Run 070: apply contract and rollback semantics.
   - Run 071: mutable live trust snapshot.
   - Run 072: production session eviction.
   - Run 073: ProductionLiveTrustApplyContext process-start apply.
   - Run 074: long-running SIGHUP live apply.

Required deliverables:

1. Update:
   - docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md

2. Create:
   - docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_075.md

   Evidence doc must include:
   - exact objective
   - exact verdict
   - exact files changed
   - exact commands run
   - tests/checks run and pass/fail status
   - investigation findings with file/function references
   - runbook sections changed
   - how Runs 069–074 are represented
   - contradictions found or not found
   - remaining open items
   - exact immediate next action

3. Update if needed:
   - docs/whitepaper/contradiction.md

   Update rules:
   - If the runbook is updated to reflect Runs 069–074, append a Run 075 C4 note saying the operator playbook prose gap for hot-reload lifecycle is closed/narrowed.
   - Keep C4 open for:
     - peer/gossiped trust-bundle acceptance
     - activation_epoch runtime source
     - KMS/HSM custody
     - in-binary/on-chain bundle-signing-key ratification
     - production fast-sync / consensus-storage restore
     - per-environment production trust-anchor operation
     - selective session retention if still absent
     - remote admin API / filesystem watcher trigger if still absent
   - Do not close full C4.
   - Do not claim C5 closure.
   - If the runbook contradicts implemented behavior, fix the runbook and record the contradiction if material.

Required checks:
Because this is docs-only unless contradictions are found, run:
- grep or rg checks proving no unsafe guidance:
  - no recommendation to use --p2p-trusted-root as fallback
  - no recommendation to use DummySig / DummyKem / DummyAead
  - no recommendation to accept unsigned TestNet/MainNet bundles
  - no recommendation to accept peer/gossiped bundles
  - no recommendation to use unauthenticated remote admin trust mutation
  - no recommendation to reuse transport root key as bundle-signing key
- markdown/link sanity if project has a checker
- cargo check -p qbind-node --bin qbind-node

If source files are touched, additionally run all affected unit/integration tests.

Expected verdict options:
- Strongest positive:
  runbook updated accurately for Runs 069–074; evidence doc created; contradiction.md updated; unsafe guidance absent; cargo check passes; no source changes required.
- Partial positive:
  runbook updated but some evidence references or checks are incomplete. State boundary.
- Negative:
  runbook still contradicts implemented behavior, unsafe fallback/key reuse guidance remains, or checks fail.

Required final response:
1. Exact verdict
2. Exact files changed
3. Exact commands run
4. Tests/checks run and pass/fail status
5. What was fixed or documented
6. What was proven
7. What remains not solved
8. Whether contradiction.md was updated and why
9. Exact immediate next action recommended