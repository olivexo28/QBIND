Run 374 — Public DevNet stable operator identity-generation command and registration package for M6

Continue QBIND after accepted Run 373.

Current state:
Run 373 is accepted as PASS / public-DevNet M12 cross-process PqcStaticRoot strict-auth hardening positive.

M12 is Green for the public DevNet readiness track, scoped to abuse/DoS deployed live-socket controls and strict-auth/static-root hardening.

Public DevNet remains NOT launch-ready.

Current public DevNet readiness:
- M1 canonical DevNet genesis package: Green.
- M2 release binary provenance: Green.
- M3 same-host reproducibility / BuildID: Green.
- M5 operator onboarding: Green.
- M10 public P2P port posture: Green.
- M11 peer admission policy: Green.
- M12 abuse/DoS controls: Green.
- M16 incident response: Green.
- M17 run-a-node docs: Green.
- M18 user-facing disclaimers: Green.
- M19 network parameter publication: Green.
- M20 genesis hash publication: Green.
- M4 seed/bootnodes: Yellow / launch-blocking.
- M6 validator / node identity generation: Yellow/Partial.
- M7–M9 and M13–M15 remain unchanged unless the matrix already records otherwise.

Full C4 remains OPEN.
C5 remains OPEN.
MainNet authority rotation/revocation remains Red.
No TestNet/MainNet readiness is claimed.

Primary objective:
Close or materially narrow M6 by providing a stable operator-facing DevNet identity-generation and identity-verification package.

The package must let an external DevNet operator generate, inspect, verify, and safely handle the identity material needed for:
1. a full node identity;
2. a seed / bootnode identity;
3. a validator candidate identity, if supported;
4. PQC KEMTLS leaf material or documented dev-only equivalent;
5. published NodeId / peer identity values that can later be referenced by the M4 seed-list live entries.

Strict scope:
M6 identity generation / verification only.

No public DevNet launch.
No live seed-node deployment.
No M4 Green.
No faucet.
No RPC gateway.
No explorer.
No status page.
No TestNet readiness claim.
No MainNet readiness claim.
No C4 closure.
No C5 closure.
No authority-lifecycle runtime wiring.
No validator-set mutation.
No epoch transition.
No sequence/marker write.
No LivePqcTrustState mutation.
No trust-bundle live apply.
No P2P wire-format change.
No peer-admission weakening.
No new public network endpoint.
No committed private keys, generated leaf keys, generated trust bundles, data dirs, or logs.

Required investigation:
Inspect:
- existing identity / key / KEMTLS helper surfaces;
- `crates/qbind-node/src/cli.rs`;
- `crates/qbind-node/src/p2p_node_builder.rs`;
- `crates/qbind-node/src/p2p_tcp.rs`;
- `crates/qbind-node/src/pqc_root_config.rs`;
- `crates/qbind-node/examples/devnet_pqc_root_helper.rs`;
- `crates/qbind-node/examples/devnet_pqc_trust_bundle_helper.rs`;
- Run 356 genesis package;
- Run 357 seed-list schema;
- Run 358 operator `IDENTITY.md` and `QUICKSTART.md`;
- Run 371–373 KEMTLS/static-root helpers and evidence;
- `docs/release/QBIND_PUBLIC_DEVNET_READINESS_CRITERIA.md`;
- `docs/release/public-devnet/network/devnet-seed-list.schema.json`;
- `docs/release/public-devnet/p2p/PEER_ADMISSION_POLICY.md`;
- `docs/release/public-devnet/p2p/VERIFY.md`.

Decision gate:
Record one of:

Route A — Existing helper surfaces are sufficient.
Use existing helpers and publish an operator-facing identity package. No production source change.

Route B — A small dedicated DevNet identity helper is needed.
Add a release-built helper/example only, not production runtime behavior, unless the repo already has a CLI pattern for identity generation.

Route C — A stable operator-facing command cannot be provided without broader identity/custody design.
Keep M6 Yellow/Partial and document the exact blocker.

Preferred implementation:
Add a canonical public DevNet identity directory:

`docs/release/public-devnet/identity/`

Add:
1. `README.md`
2. `IDENTITY_GENERATION.md`
3. `IDENTITY_VERIFY.md`
4. `OPERATOR_IDENTITY_SCHEMA.json`
5. `EXAMPLE_PUBLIC_IDENTITY.json`
6. `SAFETY.md`
7. `VERIFY.md`

If a better canonical location exists, use it and explain why.

If a helper is needed, add:

`crates/qbind-node/examples/run_374_public_devnet_identity_generation_helper.rs`

If a harness is needed, add:

`scripts/devnet/run_374_public_devnet_identity_generation.sh`

Add canonical evidence:

`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_374.md`

Update narrowly:
- `docs/release/QBIND_PUBLIC_DEVNET_READINESS_CRITERIA.md`
- `docs/release/public-devnet/operator/IDENTITY.md`
- `docs/release/public-devnet/operator/QUICKSTART.md`
- `docs/release/public-devnet/network/README.md`
- `docs/release/public-devnet/network/devnet-seed-list.schema.json`, only if needed and backward-compatible;
- `docs/release/public-devnet/p2p/PEER_ADMISSION_POLICY.md`
- `docs/release/public-devnet/p2p/VERIFY.md`
- `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
- `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
- `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
- `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
- `docs/whitepaper/contradiction.md`

Required identity package content:
The operator-facing identity package must define:

1. Which identity types exist:
   - node identity;
   - peer identity;
   - validator address / validator signing identity, if present;
   - KEMTLS leaf identity;
   - trust-root / root authority identity;
   - seed-list identity fields.
2. Which material is safe to publish:
   - NodeId;
   - peer id;
   - validator address, if public;
   - leaf certificate;
   - root id / root public key;
   - fingerprints.
3. Which material must never be published:
   - KEM secret key;
   - ML-DSA signing secret;
   - root signing key;
   - validator signing key;
   - mnemonics;
   - private config tokens.
4. Exact operator command(s) to generate identity material.
5. Exact operator command(s) to print or verify public identity.
6. Exact operator command(s) to derive the NodeId / peer id from generated material.
7. Exact operator command(s) to validate identity JSON against schema.
8. How generated identity maps to Run 357 seed-list fields.
9. How generated identity maps to `--p2p-peer`, `--p2p-leaf-cert`, `--p2p-leaf-cert-key`, and `--p2p-trusted-root`.
10. How generated identity maps to validator candidate onboarding, if supported.
11. Explicit DevNet-only / no-value / resettable safety label.
12. Explicit statement that this does not create MainNet custody or C5 closure.
13. Explicit statement that helper-generated material is not production custody.
14. Rotation / revocation limitations.
15. What remains before M4 live seed publication.

Required evidence:
The run must prove:

1. identity-generation helper or command builds;
2. identity-generation command produces valid output in a temp directory;
3. private key material is written only to the operator-selected temp/output path and never committed;
4. public identity JSON validates against schema;
5. derived NodeId / peer id is deterministic from public cert/material;
6. public identity can be inserted into a seed-list candidate object without schema violation;
7. generated material can be used in a loopback strict-auth handshake, if feasible;
8. wrong or mismatched material fails closed, if feasible;
9. MainNet use is refused or explicitly unsupported;
10. no launch-ready claim;
11. no M4 Green claim;
12. no C4/C5 closure claim.

Required tests:
Run and record exact counts:
- `cargo build -p qbind-node --release`
- helper build if added;
- harness if added;
- identity schema validation command;
- seed-list schema validation command using the generated public identity;
- relevant KEMTLS/static-root helper regression, if feasible;
- `cargo test -p qbind-node --lib`

If source or helper tests are added, run them explicitly.

Security:
Run secret scanning over all changed files.

No generated private keys, mnemonics, credentials, private infrastructure, real endpoints, generated roots, generated leaf keys, generated data dirs, logs, or metrics dumps may be committed.

Generated identity material must be temporary, dev-only, and gitignored unless the file is explicitly public-only.

CodeQL:
If Rust example/source changes, invoke CodeQL if available. If skipped or database-too-large, record the exact result and do not call it clean.

Readiness matrix rules:
- M6 may move Yellow/Partial → Green only if there is a stable operator-facing identity generation and verification package with schema, commands, safety guidance, and evidence.
- M6 must remain Yellow/Partial if identity generation is helper-only and not stable enough for external operators.
- M4 must remain Yellow unless live seed/bootnode reachability evidence lands.
- M12 remains Green unless this run contradicts prior M12 evidence.
- Public DevNet remains NOT launch-ready unless all must-haves are Green.

Expected verdict:
PASS / public-DevNet operator identity-generation positive if the identity package, schema, commands, verification evidence, docs, tests, scan, and provenance all land.

Partial-positive if identity documentation improves but stable generation remains helper-only or incomplete.

Final response format:
1. Exact verdict.
2. Files changed.
3. Decision gate route.
4. Identity package contents.
5. Identity types covered.
6. Generated material summary.
7. Public vs private material.
8. Operator commands.
9. Schema / seed-list mapping.
10. KEMTLS / static-root verification evidence.
11. Rejection / fail-closed evidence.
12. Default compatibility / no runtime behavior change.
13. Readiness matrix delta for M6.
14. Current public DevNet readiness status.
15. Remaining public DevNet blockers.
16. Public TestNet blockers.
17. MainNet blockers.
18. C4/C5 status.
19. Tests run.
20. Security scans.
21. CodeQL.
22. Provenance.
23. Honest limitations.
24. Suggested Run 375 next step.