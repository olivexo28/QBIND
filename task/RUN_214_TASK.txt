Task: Run 214 — Release-Binary Governance Execution Payload Carrying Evidence

Current accepted state:

* Run 211: source/test governance execution policy boundary landed.
* Run 212: release-binary governance execution policy-boundary evidence fully closed.
* Run 213: source/test governance execution payload/carrying and production-context wiring landed:

  * `pqc_governance_execution_payload_carrying.rs`;
  * additive optional `governance_execution` JSON sibling on v2 ratification sidecar;
  * wire types converting into Run 211 internal governance execution input/decision material;
  * seven production-context routing helpers:

    1. reload-check;
    2. reload-apply;
    3. startup `--p2p-trust-bundle`;
    4. SIGHUP;
    5. local peer-candidate-check;
    6. live inbound `0x05`;
    7. peer-driven drain;
  * 61 tests covering A1–A16 / R1–R40;
  * legacy no-governance-execution payload compatibility;
  * production/on-chain/MainNet governance execution remains unavailable/fail-closed;
  * MainNet peer-driven apply remains refused.
* Run 213 preserves:

  * no real governance execution engine;
  * no real on-chain governance proof verifier;
  * no MainNet governance enablement;
  * no validator-set rotation;
  * no real KMS/HSM backend;
  * no real RemoteSigner backend;
  * no production signing key custody;
  * full C4 remains OPEN;
  * C5 remains OPEN.
* Release-binary governance-execution payload/carrying evidence is deferred to Run 214.

Objective:
Produce release-binary evidence that real `target/release/qbind-node` production payload/context paths can carry governance execution input/decision material and route it into the Run 211 governance execution evaluator through the Run 213 production-context helpers.

This run must prove:

1. legacy/no-governance-execution payloads remain compatible under default Disabled behavior;
2. fixture governance execution material reaches production-context paths in release mode where policy allows;
3. production/on-chain/MainNet governance execution material reaches the evaluator and fails closed as unavailable;
4. malformed/invalid governance execution material fails closed;
5. governance execution input/decision/transcript/policy digests remain deterministic and domain-bound through wire conversion;
6. carried governance decision authorizes lifecycle action only when action, candidate digest, and sequence match;
7. validator-set rotation remains unsupported;
8. rejected governance-execution-payload cases produce no mutation;
9. MainNet peer-driven apply remains refused even with fixture governance approval;
10. no real governance execution engine, on-chain proof verifier, KMS-HSM backend, RemoteSigner backend, or validator-set rotation is claimed.

Strict scope:

* Release-binary evidence only.
* Use real `target/release/qbind-node`.
* Use release-built helper(s) to mint governance-execution-carrying sidecars / peer-candidate material.
* No production source change unless a tiny harness-only fix is required.
* No real governance execution engine.
* No real on-chain governance proof verifier.
* No MainNet governance enablement.
* No MainNet peer-driven apply enablement.
* No validator-set rotation.
* No authority-set rotation beyond existing lifecycle boundary checks.
* No real KMS implementation.
* No real HSM implementation.
* No real RemoteSigner backend.
* No production signing key custody.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* No schema/wire change beyond Run 213’s additive optional governance-execution sibling.
* No authority-marker schema change.
* No sequence-file schema change.
* No trust-bundle core schema change.
* No authority lifecycle semantics change.
* Do not weaken Runs 070, 130–213.
* Do not claim full C4 or C5 closure.

Required deliverables:

1. Add release-binary helper:
   `crates/qbind-node/examples/run_214_governance_execution_payload_release_binary_helper.rs`

2. Add release-binary harness:
   `scripts/devnet/run_214_governance_execution_payload_release_binary.sh`

3. Add evidence archive:
   `docs/devnet/run_214_governance_execution_payload_release_binary/`

4. Add canonical evidence report:
   `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_214.md`

5. Update narrowly:

   * `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
   * `docs/whitepaper/contradiction.md`
   * `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
   * `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
   * `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

6. Crosscheck all new/modified code and docs against existing design/spec. If contradictions or inconsistencies are found, record them in `contradiction.md` before finalizing.

Required release-built helper matrix:
The helper must exercise the Run 213 governance-execution payload/call-site routing corpus in release mode.

Accepted / compatible cases:
A1. no-governance-execution payload remains compatible under default Disabled.

A2. DevNet fixture governance execution carried through reload-check context and accepted under explicit fixture policy.

A3. TestNet fixture governance execution carried through reload-check context and accepted under explicit fixture policy.

A4. DevNet fixture governance execution carried through reload-apply context and accepted under explicit fixture policy.

A5. governance execution input digest preserved through wire conversion.

A6. governance execution decision digest preserved through wire conversion.

A7. governance execution transcript digest preserved through wire conversion.

A8. governance policy digest preserved through wire conversion if implemented.

A9. fixture governance execution routes to Run 211 evaluator when material is present.

A10. lifecycle rotate action authorized only when carried decision authorizes rotate with matching candidate digest and sequence.

A11. lifecycle revoke action authorized only when carried decision authorizes revoke with matching candidate/revoked-key material and sequence.

A12. emergency revoke accepted only when carried decision explicitly authorizes emergency action under emergency fixture policy.

A13. combined lifecycle + governance proof + custody-attestation digest + fixture governance execution accepted for DevNet release-helper production-context path.

A14. GenesisBound / EmergencyCouncil / OnChainGovernance proof-carrier behavior remains unchanged when governance execution policy is Disabled.

A15. custody / RemoteSigner / KMS-HSM / custody-attestation paths remain compatible when governance execution policy is Disabled.

A16. production/on-chain/MainNet governance execution material reaches evaluator and returns typed unavailable outcome under production-required policy.

Required rejection cases:
R1. governance execution material absent where governance execution policy requires it rejected fail-closed.

R2. malformed governance execution input rejected.

R3. malformed governance execution decision rejected.

R4. malformed combined governance execution payload rejected.

R5. unsupported future governance execution schema version rejected.

R6. fixture governance rejected under ProductionGovernanceRequired.

R7. emergency fixture rejected under ProductionGovernanceRequired.

R8. fixture governance rejected under MainnetGovernanceRequired.

R9. production governance rejected as unavailable.

R10. on-chain governance rejected as unavailable.

R11. MainNet governance rejected as unavailable.

R12. unknown governance class rejected.

R13. wrong environment rejected.

R14. wrong chain rejected.

R15. wrong genesis rejected.

R16. wrong authority root rejected.

R17. wrong lifecycle action rejected.

R18. wrong candidate digest rejected.

R19. wrong authority-domain sequence rejected.

R20. wrong governance proof digest rejected.

R21. wrong on-chain proof digest rejected.

R22. wrong custody attestation digest rejected.

R23. wrong proposal id rejected.

R24. wrong decision id rejected.

R25. wrong effective epoch rejected.

R26. expired decision rejected.

R27. stale/replayed decision rejected.

R28. quorum threshold insufficient rejected.

R29. emergency action not authorized rejected.

R30. validator-set rotation unsupported rejected.

R31. policy-change action unsupported rejected.

R32. local operator cannot satisfy governance execution.

R33. peer majority cannot satisfy governance execution.

R34. governance valid but lifecycle action mismatch rejected.

R35. lifecycle valid but governance decision invalid rejected.

R36. lifecycle + governance proof + custody valid but production governance execution unavailable rejected.

R37. validation-only rejection writes no marker and no sequence.

R38. mutating rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

R39. invalid live `0x05` governance-execution candidate is not propagated, staged, or applied, if live surface is included.

R40. MainNet peer-driven apply remains refused even with fixture governance approval.

Required real binary checks:
Use real `target/release/qbind-node` to prove:

* MainNet peer-driven apply remains refused.
* Default behavior does not expose or enable governance execution.
* No `governance execution active` / `production governance active` / `MainNet governance enabled` claim is emitted.
* No real on-chain governance proof verifier active claim is emitted.
* Existing custody-attestation policy selector behavior from Run 210 remains compatible.
* Existing KMS/HSM backend behavior from Run 204 remains compatible.
* Existing RemoteSigner transport behavior from Run 202 remains compatible.
* Existing custody policy selector behavior from Run 193 remains compatible.
* Existing governance proof carrier paths remain compatible.
* No validator-set rotation claim is emitted.

Required source/release reachability proof:
The report must include source grep/call-site proof for:

* `pqc_governance_execution_payload_carrying`;
* governance execution wire class;
* governance execution action wire;
* governance execution input wire;
* governance execution decision wire;
* governance execution payload wire;
* governance execution load status;
* `governance_execution`;
* seven per-surface governance-execution routing helpers;
* Run 211 evaluator entry points.

Required mutation / no-mutation proof:
For rejected governance-execution-payload scenarios:

* no Run 070 apply call;
* no live trust swap;
* no session eviction;
* no sequence write;
* no marker write;
* marker bytes unchanged where present;
* sequence bytes unchanged where present;
* no `.tmp` residue;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead.

For accepted fixture governance mutating compatibility scenarios:

* governance-execution payload parse occurs before marker decision;
* governance execution evaluation occurs before apply/mutation;
* lifecycle validation occurs before apply/mutation;
* governance proof validation occurs before apply/mutation where applicable;
* custody validation occurs before apply/mutation where applicable;
* custody attestation validation occurs before apply/mutation where applicable;
* Run 055 sequence commit succeeds if a mutating path is exercised;
* v2 marker persists strictly after sequence commit.

Required denylist:
Across the run prove:

* no MainNet apply;
* no autonomous apply;
* no apply on receipt;
* no peer-majority authority;
* no real governance execution claim;
* no production governance active claim;
* no MainNet governance enabled claim;
* no real on-chain governance proof verifier claim;
* no real KMS backend claim;
* no real HSM backend claim;
* no real RemoteSigner backend claim;
* no custody attestation production-active claim;
* no validator-set rotation claim;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead;
* no schema/wire/metric drift beyond Run 213’s additive optional governance-execution sibling;
* no marker write before sequence commit;
* no sequence write on validation-only surfaces;
* no marker write on validation-only surfaces.

Required captured metadata:

* qbind-node SHA-256 and ELF Build ID;
* helper SHA-256 and ELF Build ID;
* git commit hash;
* rustc/cargo versions;
* exact commands;
* stdout/stderr logs;
* per-scenario exit codes;
* governance-execution fixture input/decision/payload paths and hashes;
* marker/sequence JSON and SHA before/after where applicable;
* data-dir inventories;
* denylist grep results.

Validation commands:
Run at minimum:

* `cargo build --release -p qbind-node --bin qbind-node`
* `cargo build --release -p qbind-node --example run_214_governance_execution_payload_release_binary_helper`
* `bash scripts/devnet/run_214_governance_execution_payload_release_binary.sh`
* `cargo test -p qbind-node --test run_213_governance_execution_payload_callsite_tests`
* `cargo test -p qbind-node --test run_211_governance_execution_policy_tests`
* `cargo test -p qbind-node --test run_209_custody_attestation_policy_selector_tests`
* `cargo test -p qbind-node --test run_207_custody_attestation_payload_callsite_tests`
* `cargo test -p qbind-node --test run_205_custody_attestation_verifier_tests`
* `cargo test -p qbind-node --test run_203_kms_hsm_backend_boundary_tests`
* `cargo test -p qbind-node --test run_201_remote_signer_transport_boundary_tests`
* `cargo test -p qbind-node --test run_198_remote_signer_policy_selector_tests`
* `cargo test -p qbind-node --test run_196_remote_signer_payload_callsite_tests`
* `cargo test -p qbind-node --test run_194_remote_authority_signer_boundary_tests`
* `cargo test -p qbind-node --test run_192_authority_custody_policy_selector_tests`
* `cargo test -p qbind-node --test run_190_authority_custody_payload_callsite_tests`
* `cargo test -p qbind-node --test run_188_authority_custody_boundary_tests`
* `cargo test -p qbind-node --test run_186_onchain_governance_production_verifier_boundary_tests`
* `cargo test -p qbind-node --test run_184_onchain_governance_payload_carrying_tests`
* `cargo test -p qbind-node --test run_182_onchain_governance_production_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_180_onchain_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_178_onchain_governance_proof_tests`
* `cargo test -p qbind-node --test run_176_live_0x05_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_173_validation_only_governance_required_policy_tests`
* `cargo test -p qbind-node --test run_171_governance_required_policy_selector_tests`
* `cargo test -p qbind-node --test run_169_governance_proof_loader_surface_integration_tests`
* `cargo test -p qbind-node --test run_167_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_165_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_163_governance_authority_verifier_tests`
* `cargo test -p qbind-node --test run_161_lifecycle_marker_integration_tests`
* `cargo test -p qbind-node --test run_159_authority_signing_key_lifecycle_tests`
* `cargo test -p qbind-node --test run_157_unified_testnet_fixture_universe_tests`
* `cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests`
* `cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests`
* `cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests`
* `cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests`
* `cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests`
* `cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Documentation requirements:
`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_214.md` must state:

* Run 214 is release-binary governance-execution payload/carrying evidence.
* No real governance execution engine is implemented.
* No real on-chain governance proof verifier is implemented.
* Fixture governance execution remains DevNet/TestNet evidence-only.
* Production/on-chain/MainNet governance execution remains unavailable/fail-closed.
* Governance-execution payload/carrying evidence does not enable MainNet peer-driven apply.
* Validator-set rotation remains unsupported.
* KMS/HSM/RemoteSigner/custody-attestation remain boundary-only.
* existing custody/KMS-HSM/RemoteSigner/custody-attestation/governance proof paths remain compatible.
* full C4 remains open.
* C5 remains open.

Acceptance criteria:
Run 214 is acceptable only if:

1. Release-built helper proves the Run 213 governance-execution payload/carrying corpus.
2. Real `target/release/qbind-node` confirms MainNet peer-driven apply remains refused.
3. Legacy no-governance-execution payloads remain compatible.
4. Fixture governance execution reaches production-context paths in release mode where policy allows.
5. Production/on-chain/MainNet governance execution reaches evaluator and fails closed as unavailable.
6. Malformed/invalid governance execution material fails closed.
7. Governance execution digest preservation through wire conversion is proven.
8. Lifecycle action authorization is bound to action, candidate digest, and sequence.
9. Validator-set rotation remains unsupported.
10. Rejected governance-execution-payload cases produce no mutation.
11. Existing custody/KMS-HSM/RemoteSigner/custody-attestation/governance paths remain compatible.
12. No real governance execution / on-chain proof verifier / KMS-HSM backend / RemoteSigner backend / validator-set rotation claim is made.
13. No full C4 or C5 closure is claimed.
