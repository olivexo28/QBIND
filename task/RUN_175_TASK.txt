Task: Run 175 — Release-Binary Local Peer-Candidate-Check Required-Policy Governance-Proof Evidence

Current accepted state:

* Run 171: hidden Required-policy selector landed:

  * CLI: `--p2p-trust-bundle-governance-proof-required`;
  * env: `QBIND_P2P_TRUST_BUNDLE_GOVERNANCE_PROOF_REQUIRED`;
  * default remains `GovernanceProofPolicy::NotRequired`.
* Run 172: release-binary Required-policy evidence landed for mutating reload-apply surfaces.
* Run 173: source/test validation-only Required-policy wiring landed for reload-check and local peer-candidate-check.
* Run 174: release-binary validation-only Required-policy evidence landed for `--p2p-trust-bundle-reload-check`.
* Run 174 explicitly deferred local peer-candidate-check release-binary cases:

  * A4/A5;
  * R15/R16.
* Run 174 also preserves the live inbound `0x05` limitation:

  * live peer-candidate envelopes still do not carry the governance-proof sibling.
* MainNet peer-driven apply remains refused.
* OnChainGovernance remains unsupported/fail-closed.
* governance execution remains unimplemented.
* KMS/HSM remains unimplemented.
* validator-set rotation remains open.
* full C4 remains open.
* C5 remains open.

Objective:
Produce release-binary evidence that the local `--p2p-trust-bundle-peer-candidate-check` validation-only surface enforces `GovernanceProofPolicy::RequiredForLifecycleSensitive` using the hidden Run 171 selector and the Run 173 validation-only wiring.

This run must prove on real `target/release/qbind-node`:

1. default local peer-candidate-check behavior remains `NotRequired`;
2. CLI selector activates Required policy for local peer-candidate-check;
3. env selector activates Required policy for local peer-candidate-check;
4. Required policy accepts valid proof-carrying peer-candidate sidecars where anti-rollback, lifecycle, and governance checks all pass;
5. Required policy rejects no-proof and invalid-proof peer-candidate sidecars fail-closed;
6. local peer-candidate-check remains strictly validation-only and non-mutating;
7. MainNet peer-driven apply remains refused.

Strict scope:

* Release-binary evidence only.
* Use real `target/release/qbind-node`.
* Use release-built helper(s) to mint proof-carrying peer-candidate fixtures.
* No production source change unless a tiny harness-only fix is required.
* No live inbound `0x05` proof-carrying schema work.
* No MainNet peer-driven apply enablement.
* No governance execution engine.
* No on-chain governance implementation.
* No KMS/HSM implementation.
* No validator-set rotation.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* No new proof schema.
* No marker schema change.
* No sequence-file schema change.
* No trust-bundle schema change.
* No peer-candidate envelope schema change.
* Do not weaken Runs 070, 130–174.
* Do not claim full C4 or C5 closure.

Required deliverables:

1. Add release-binary harness:
   `scripts/devnet/run_175_peer_candidate_check_governance_required_policy_release_binary.sh`

2. Add evidence archive:
   `docs/devnet/run_175_peer_candidate_check_governance_required_policy_release_binary/`

3. Add canonical evidence report:
   `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_175.md`

4. Update narrowly:

   * `docs/whitepaper/contradiction.md`
   * `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
   * `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
   * `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

5. Crosscheck all new/modified code and docs against existing design/spec. If contradictions or inconsistencies are found, record them in `contradiction.md` before finalizing.

Required production validation-only surface:
Exercise:

* local `--p2p-trust-bundle-peer-candidate-check`

Do not claim live inbound `0x05` proof-carrying evidence in this run unless the envelope can actually carry the governance-proof sibling without schema drift.

Required accepted scenarios:

A1. peer-candidate-check default NotRequired accepts old no-proof candidate

* No CLI flag and no env var.
* Expected:

  * real `qbind-node` exits success;
  * default policy is NotRequired;
  * validation-only semantics preserved;
  * no sequence write;
  * no marker write.

A2. peer-candidate-check CLI Required accepts valid proof-carrying Rotate candidate

* Use `--p2p-trust-bundle-governance-proof-required`.
* Expected:

  * hidden CLI selector observed;
  * proof sibling parsed through production loader;
  * `GovernanceProofLoadStatus::Available` or equivalent visible;
  * `GovernanceProofContext::Available` reaches marker decision;
  * governance verifier accepts;
  * lifecycle validation accepts;
  * marker decision accepts;
  * no sequence write;
  * no marker write;
  * no live trust mutation;
  * no session eviction.

A3. peer-candidate-check env Required accepts valid proof-carrying Rotate candidate

* Use `QBIND_P2P_TRUST_BUNDLE_GOVERNANCE_PROOF_REQUIRED=1`.
* Expected:

  * env selector observed;
  * Required policy active;
  * valid proof accepted;
  * validation-only non-mutation preserved.

A4. false/unset env preserves NotRequired

* Test unset, `0`, `false`, or equivalent non-truthy value.
* Expected:

  * Required policy is not activated accidentally;
  * old no-proof candidate remains compatible.

A5. idempotent proof-carrying peer-candidate accepted under Required policy, if fixture-supported

* same authority sequence / digest / proof;
* accepted as idempotent/no-op according to existing marker rules;
* no mutation.

Required rejection scenarios:

R1. peer-candidate-check CLI Required + no-proof candidate rejected

* Expected:

  * `GovernanceAuthorityRequiredButMissing` visible;
  * no sequence write;
  * no marker write.

R2. peer-candidate-check env Required + no-proof candidate rejected

* Expected:

  * `GovernanceAuthorityRequiredButMissing` visible;
  * no sequence write;
  * no marker write.

R3. peer-candidate-check Required + malformed proof rejected.

R4. peer-candidate-check Required + invalid issuer signature rejected.

R5. peer-candidate-check Required + wrong environment proof rejected.

R6. peer-candidate-check Required + wrong chain proof rejected.

R7. peer-candidate-check Required + wrong genesis proof rejected.

R8. peer-candidate-check Required + wrong authority root proof rejected.

R9. peer-candidate-check Required + wrong lifecycle action proof rejected.

R10. peer-candidate-check Required + wrong candidate digest proof rejected.

R11. peer-candidate-check Required + wrong authority-domain sequence proof rejected.

R12. peer-candidate-check Required + OnChainGovernance proof rejected as unsupported.

R13. peer-candidate-check Required + local operator config proof rejected.

R14. peer-candidate-check Required + peer-majority proof rejected.

R15. peer-candidate-check Required rejection writes no marker and no sequence.

R16. peer-candidate-check Required rejection performs no live trust swap, no session eviction, and no Run 070 call.

R17. selector cannot be accidentally enabled by unrelated flags.

R18. MainNet peer-driven apply remains refused even if peer-candidate-check Required proof check accepts.

Required selector proof:
The evidence report must include:

* CLI proof:

  * `--p2p-trust-bundle-governance-proof-required` is present but hidden;
  * enabling it activates Required policy for local peer-candidate-check.
* env proof:

  * `QBIND_P2P_TRUST_BUNDLE_GOVERNANCE_PROOF_REQUIRED=1` activates Required policy for local peer-candidate-check;
  * false / unset value preserves NotRequired.
* source grep/call-site proof for:

  * `governance_proof_policy_from_cli_or_env`;
  * `preflight_run_132_validation_only_v2_marker_check`;
  * local peer-candidate-check dispatch;
  * `preflight_v2_validation_only_marker_check_with_governance_proof_load`;
  * `preflight_v2_marker_decision_with_governance_proof_load`;
  * `load_versioned_ratification_with_governance_proof_from_path`;
  * `GovernanceProofContext::Available`.

Required production-surface reachability proof:
The report must include release-binary logs proving:

* local peer-candidate-check production surface exercised:

  * Required policy;
  * proof-carrying Available path;
  * proof-required missing path;
  * invalid/malformed proof path;
  * no-mutation behavior.

Required no-mutation proof:
For every peer-candidate-check scenario:

* no Run 070 apply call;
* no live trust swap;
* no session eviction;
* no sequence write;
* no marker write;
* marker bytes unchanged if present;
* sequence bytes unchanged if present;
* no `.tmp` residue;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead.

Required denylist:
Across the run prove:

* no MainNet apply;
* no autonomous apply;
* no apply on receipt;
* no peer-majority authority;
* no governance execution claim;
* no on-chain governance claim;
* no KMS/HSM claim;
* no validator-set rotation claim;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead;
* no schema/wire/metric drift;
* no sequence write on validation-only surfaces;
* no marker write on validation-only surfaces.

Required captured metadata:

* qbind-node SHA-256 and ELF Build ID;
* helper SHA-256 and ELF Build ID;
* git commit hash;
* rustc/cargo versions;
* exact commands;
* stdout/stderr logs;
* per-scenario exit codes;
* proof sidecar / candidate paths and hashes;
* marker/sequence JSON and SHA before/after;
* data-dir inventories;
* denylist grep results.

Validation commands:
Run at minimum:

* `cargo build --release -p qbind-node --bin qbind-node`
* `cargo build --release -p qbind-node --example <proof-carrying fixture helper>`
* `bash scripts/devnet/run_175_peer_candidate_check_governance_required_policy_release_binary.sh`
* `cargo test -p qbind-node --test run_173_validation_only_governance_required_policy_tests`
* `cargo test -p qbind-node --test run_171_governance_required_policy_selector_tests`
* `cargo test -p qbind-node --test run_169_governance_proof_loader_surface_integration_tests`
* `cargo test -p qbind-node --test run_167_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_165_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_163_governance_authority_verifier_tests`
* `cargo test -p qbind-node --test run_161_lifecycle_marker_integration_tests`
* `cargo test -p qbind-node --test run_159_authority_signing_key_lifecycle_tests`
* `cargo test -p qbind-node --test run_157_unified_testnet_fixture_universe_tests`
* `cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests`
* `cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests`
* `cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests`
* `cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests`
* `cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests`
* `cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Documentation requirements:
`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_175.md` must state:

* Run 175 is release-binary local peer-candidate-check Required-policy governance-proof evidence.
* Default remains NotRequired.
* Hidden CLI/env selector activates Required policy on local peer-candidate-check.
* Existing no-proof candidates remain compatible under NotRequired.
* Required policy fails closed when proof is absent.
* Valid proof-carrying candidates pass under Required policy through the real local peer-candidate-check production surface where lifecycle/anti-rollback checks pass.
* Invalid proof-carrying candidates fail closed.
* Local peer-candidate-check remains strictly non-mutating.
* Live inbound `0x05` proof-carrying remains open because the envelope cannot carry the governance-proof sibling.
* OnChainGovernance remains unsupported/fail-closed.
* No MainNet apply is enabled.
* governance execution/on-chain proof remains unimplemented.
* KMS/HSM remains unimplemented.
* validator-set rotation remains open.
* full C4 remains open unless separately closed by explicit criteria.
* C5 remains open.

Acceptance criteria:
Run 175 is acceptable only if:

1. Real release binaries exercise Required policy on local peer-candidate-check.
2. Default NotRequired compatibility remains proven.
3. CLI selector and env selector both activate Required policy for local peer-candidate-check.
4. Required/no-proof local peer-candidate-check cases fail closed.
5. Valid proof-carrying candidates reach and pass the governance gate on local peer-candidate-check.
6. Invalid proof-carrying candidates fail closed.
7. Local peer-candidate-check scenarios produce no mutation.
8. MainNet remains refused even with Required policy and valid governance proof.
9. No governance execution/KMS-HSM/validator-set rotation claim is made.
10. No full C4 or C5 closure is claimed.
