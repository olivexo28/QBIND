Continue QBIND from the current state.

Project:
- QBIND post-quantum L1
- security-first, production-grade, execution-first
- C4 is the main live operability tracker
- C5 tracks timeout-verification activation, forged-traffic rejection, and transport-root dependency
- no broad redesign
- no silent regressions
- no hype / no overclaiming

Current state:
- Runs 037–045 hardened PQC transport:
  - ML-DSA-44 delegation cert verification
  - ML-KEM-768 transport KEM
  - ChaCha20-Poly1305 transport AEAD
  - PQC cert metrics
  - cert validity-window enforcement
- Runs 046–049 completed live timeout-backoff evidence through the 800-tick cap.
- Runs 050–068 completed the signed trust-bundle lifecycle path:
  - structured PQC trust bundles
  - signed ML-DSA-44 bundle verification
  - chain_id crosscheck
  - sequence anti-rollback persistence
  - activation-height gating
  - minimum activation-height policy
  - root/leaf revocation
  - per-entry revocation activation_height
  - local revoked-leaf startup self-check
  - local revoked issuer-root startup self-check
  - DevNet/TestNet/MainNet evidence
  - N=4 MainNet signed-bundle peer-connection evidence
- Run 069 landed validation-only trust-bundle reload-check:
  - hidden --p2p-trust-bundle-reload-check <PATH>
  - validates candidate bundles using startup security pipeline
  - does not apply live trust changes
  - does not mutate active roots/revocations/sessions
  - does not persist sequence or burn sequence numbers
- Run 070 partial-positive:
  - landed live-apply contract and rollback semantics
  - proved callback ordering:
    - snapshot
    - swap
    - evict sessions
    - commit sequence
    - rollback on post-swap failure
  - preserved Run 069 validation-only behavior
  - production binary still returns UnsupportedRuntimeContext
  - reason: active trust state is currently embedded immutably in the P2P builder / handshake verifier, and there is no production session-eviction hook yet
- C4 remains OPEN.
- C5 remains OPEN / narrowed.

Current remaining C4 trust/lifecycle blockers:
- mutable live trust-context handle
- production session eviction
- actual live trust-bundle apply
- peer/gossiped bundle acceptance
- activation_epoch runtime source
- in-binary / on-chain bundle-signing-key ratification
- external KMS/HSM custody integration
- production fast-sync / consensus-storage restore
- per-environment production trust-anchor operation

Task:
Run 071. Introduce and prove a mutable live trust-context handle for PQC handshake verification, without enabling live reload apply yet.

Primary objective:
Replace the immutable startup-only PQC trust material used by handshake verification with a safe shared live trust context that initially contains the exact same trust state as startup, and prove that normal startup and handshake behavior remain unchanged.

Important framing:
This is NOT full hot reload.
This is NOT session eviction.
This is NOT live trust-bundle apply.
This is NOT peer/gossiped bundle acceptance.
This is NOT KMS/HSM.
This is NOT signing-key ratification.
This is NOT activation_epoch implementation.
This is NOT fast-sync restore.
Do not redesign KEMTLS.
Do not redesign consensus.
Do not weaken startup trust-bundle validation.
Do not weaken Run 069 reload-check.
Do not weaken Run 070 apply contract.
Do not silently fall back to --p2p-trusted-root.
Do not silently fall back to DummySig, DummyKem, or DummyAead.
Do not claim full C4 or C5 closure.

Strict scope:
- Introduce a live trust-state handle, for example:
  - LivePqcTrustState
  - Arc<RwLock<LoadedTrustBundleRuntimeState>>
  - or equivalent project style
- Initialize it from the already validated startup trust bundle.
- Make PQC handshake verification read roots/revocations from the live trust context.
- Do not mutate the live trust context after startup in Run 071.
- Do not connect it to reload apply yet.
- Do not evict sessions yet.
- Do not accept peer/gossiped bundles.
- Preserve startup behavior exactly:
  - signed-bundle verification
  - environment validation
  - chain_id validation
  - activation-height gating
  - minimum activation-height policy
  - sequence anti-rollback persistence
  - local leaf self-check
  - local issuer-root self-check
- Preserve Run 052/054/061/062/063 revocation behavior.
- Crosscheck all code against existing design/spec. If contradictions or inconsistencies are found, update docs/whitepaper/contradiction.md.

Required investigation:

1. Inspect current PQC trust material ownership
   Inspect:
   - crates/qbind-node/src/main.rs
   - crates/qbind-node/src/p2p_node_builder.rs
   - crates/qbind-node/src/pqc_trust_bundle.rs
   - crates/qbind-node/src/pqc_trust_reload.rs
   - crates/qbind-net handshake verification config/types
   - LeafCertRevocationList
   - cert verifier / delegation cert verification path
   - P2pMetrics trust-bundle and PQC metrics
   - Run 070 evidence doc
   - docs/whitepaper/contradiction.md

   Determine:
   - where active root public keys live after startup
   - where active revoked leaf fingerprints live after startup
   - where active revoked root IDs live after startup
   - how listener and dialer handshake verification receives trust material
   - whether qbind-net already supports an Arc-backed revocation list or similar pattern
   - whether root set currently gets cloned into immutable configs

   Document exact file/function references in the Run 071 evidence doc.

2. Define live trust context structure
   Required fields:
   - environment
   - chain_id
   - bundle fingerprint
   - sequence
   - active transport roots
   - active revoked root IDs
   - active revoked leaf fingerprints
   - active/pending revocation counts if needed for metrics
   - activation/minimum-margin status if already tracked
   - loaded flag

   Requirements:
   - public metadata only.
   - no private keys.
   - no KEM secrets.
   - no root signing secrets.
   - no bundle-signing secret.
   - cheap read path for handshakes.
   - write path exists only as internal future capability, not used by Run 071.
   - safe snapshot method for metrics/evidence.
   - deterministic clone/snapshot behavior.

3. Define concurrency and locking semantics
   Required:
   - handshake verification may take a read lock / snapshot.
   - no write lock is taken in Run 071 after startup initialization.
   - avoid holding locks across network I/O or expensive crypto if possible.
   - avoid deadlock with metrics formatting.
   - avoid partial reads:
     - roots and revocation sets must come from the same snapshot.
   - if using RwLock, document poisoning/error behavior.
   - if lock acquisition fails, fail closed for verification.

4. Define qbind-net layering
   Required:
   - qbind-net must not depend on qbind-node.
   - if qbind-net needs dynamic trust material, define a trait/callback/interface in qbind-net or pass a snapshot provider.
   - qbind-node can implement that provider.
   - avoid circular dependency.
   - keep existing static config path working where tests depend on it.

5. Define metrics behavior
   Metrics should continue to report:
   - root mode
   - roots configured
   - trust bundle loaded
   - environment
   - active roots
   - revoked roots / revoked leaves / pending counts
   - sequence highest
   - cert accepted/rejected counters

   Requirements:
   - metrics should read a consistent snapshot.
   - no duplicate metric families.
   - no fabricated values.
   - existing Run 043/044/050/062/065 metric expectations preserved.

Required implementation:

1. Add live trust context module or type
   Preferred location:
   - crates/qbind-node/src/pqc_live_trust.rs
   or, if cleaner:
   - crates/qbind-node/src/pqc_trust_bundle.rs

   Include:
   - LivePqcTrustState
   - LivePqcTrustSnapshot
   - initialize_from_loaded_bundle(...)
   - snapshot()
   - maybe future swap method, but not used by Run 071 unless needed for tests
   - strict errors if lock poisoned/unavailable

2. Wire startup
   In main.rs / P2pNodeBuilder setup:
   - after startup trust bundle has fully validated and sequence persisted
   - after local self-checks
   - create LivePqcTrustState from the loaded bundle
   - pass the live trust handle into P2pNodeBuilder / handshake verifier
   - preserve the exact same roots/revocations as before

3. Wire handshake verification
   Listener and dialer PQC cert verification must read from the live trust snapshot.
   Requirements:
   - cert verification still fails closed for:
     - untrusted root
     - active revoked root
     - active revoked leaf
     - cert validity-window failure
     - bad signature
   - pending revocations are not enforced.
   - accepted/rejected metrics unchanged.
   - no Dummy crypto fallback.

4. Preserve static tests/configs
   If tests currently construct static trust configs:
   - either keep the old static path as a convenience wrapper around LivePqcTrustState
   - or update tests mechanically without weakening assertions
   - avoid wide churn

5. Do not connect reload apply
   Run 070 apply contract may remain UnsupportedRuntimeContext.
   Or, if the live trust handle is enough to remove only that one blocker, do not enable apply unless session eviction support also exists.
   Recommended:
   - keep Run 070 production apply unsupported.
   - document that Run 071 only resolves the mutable trust-context blocker, leaving session eviction/apply for Run 072.

Required tests:

1. Unit tests
   - LivePqcTrustState initializes from loaded bundle.
   - snapshot contains active roots, revoked root IDs, revoked leaf fingerprints, sequence, fingerprint.
   - snapshot is consistent.
   - no private material present.
   - pending revocations are not included in active enforcement sets.
   - active revocations are included.
   - metrics snapshot matches loaded bundle.
   - lock failure/poisoning if testable fails closed.

2. Handshake tests
   - valid cert accepted using live trust context.
   - bad signature rejected.
   - unknown root rejected.
   - active revoked root rejected.
   - active revoked leaf rejected.
   - pending revoked root not rejected early.
   - pending revoked leaf not rejected early.
   - accepted/rejected metrics move exactly as before.

3. Regression tests
   - Run 052 revoked-peer handshake tests pass.
   - Run 061 local leaf self-check tests pass.
   - Run 062 revocation activation tests pass.
   - Run 063 issuer-root self-check tests pass.
   - Run 067/068 MainNet peer evidence assumptions remain valid through tests.
   - Run 069 reload-check remains non-mutating.
   - Run 070 apply contract tests still pass and still return UnsupportedRuntimeContext if session eviction is absent.

Required regression commands:
- cargo test -p qbind-node --lib pqc_live_trust
- cargo test -p qbind-node --lib pqc_trust_reload
- cargo test -p qbind-node --lib pqc_trust_bundle
- cargo test -p qbind-node --lib pqc_trust_sequence
- cargo test -p qbind-node --lib pqc_trust_activation
- new Run 071 tests
- cargo test -p qbind-node --test run_069_pqc_trust_bundle_reload_check_tests
- cargo test -p qbind-node --test run_070_pqc_trust_bundle_reload_apply_tests
- cargo test -p qbind-node --test run_050_pqc_trust_bundle_tests
- cargo test -p qbind-node --test run_051_pqc_trust_bundle_signing_tests
- cargo test -p qbind-node --test run_052_pqc_leaf_revocation_tests
- cargo test -p qbind-node --test run_055_pqc_trust_bundle_sequence_tests
- cargo test -p qbind-node --test run_057_pqc_trust_bundle_activation_tests
- cargo test -p qbind-node --test run_061_pqc_local_leaf_self_check_tests
- cargo test -p qbind-node --test run_062_pqc_revocation_activation_tests
- cargo test -p qbind-node --test run_063_pqc_local_issuer_root_self_check_tests
- cargo test -p qbind-net --test run_052_leaf_revocation_handshake_tests
- cargo test -p qbind-node --lib metrics
- cargo test -p qbind-node --lib p2p
- cargo test -p qbind-node --test run_037_pqc_static_root_mutual_auth_tests
- cargo test -p qbind-node --test run_040_pqc_static_root_real_aead_tests
- cargo test -p qbind-node --test run_044_pqc_cert_verify_metrics_adapter_tests
- cargo test -p qbind-node --lib
- cargo test -p qbind-net --lib
- cargo test -p qbind-crypto --lib
- cargo build --release -p qbind-node --bin qbind-node
- cargo build --release -p qbind-node --example devnet_pqc_trust_bundle_helper
- cargo build --release -p qbind-node --example devnet_pqc_root_helper

If source touches timeout verification or binary consensus loop, also run:
- cargo test -p qbind-node --lib binary_consensus
- cargo test -p qbind-node --lib forged_injection
- cargo test -p qbind-node --lib run030
- cargo test -p qbind-consensus --lib timeout

Release-binary evidence:

1. Positive N=2 startup and peer connection
   - signed trust bundle
   - live trust context enabled internally
   - two nodes connect
   - cert accepted counters move
   - rejected counters zero
   - metrics reflect same trust-bundle state as before
   - no fallback
   - no Dummy crypto

2. Negative active revoked peer leaf
   - signed bundle revokes peer leaf
   - live trust context is used for verification
   - peer rejected fail-closed
   - rejected_revoked counter moves
   - no fallback
   - no Dummy crypto

3. Positive pending revoked peer leaf
   - signed bundle has future/pending peer leaf revocation
   - peer connects
   - rejected_revoked remains zero
   - no fallback
   - no Dummy crypto

4. Optional MainNet short smoke
   - if cheap, rerun N=2 MainNet positive to prove no regression from Run 067.
   - do not block Run 071 on long N=4 evidence if tests + smaller smoke are sufficient.

Evidence document:
Create:
- docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_071.md

Evidence doc must include:
- exact objective
- exact verdict
- exact files changed
- exact commands run
- tests/evidence run and pass/fail status
- investigation findings with file/function references
- live trust context structure
- concurrency/locking semantics
- qbind-net layering decision
- handshake verification path
- metrics behavior
- proof startup behavior preserved
- proof Run 069 reload-check remains non-mutating
- proof Run 070 apply still unsupported or exact boundary if changed
- release-binary evidence
- proof no fallback to --p2p-trusted-root
- proof no DummySig/DummyKem/DummyAead fallback
- remaining open items
- exact immediate next action

contradiction.md update rules:
- If mutable live trust context lands and is proven, update C4 to record that hot reload is narrowed by replacing immutable startup trust configs with a live trust-state read handle.
- If Run 070 apply remains unsupported due session eviction, record that exact remaining blocker.
- Keep C4 open for:
  - actual live trust-bundle apply
  - production session eviction
  - peer/gossiped bundle acceptance
  - activation_epoch runtime source
  - in-binary/on-chain bundle-signing-key ratification
  - KMS/HSM custody
  - production fast-sync / consensus-storage restore
  - per-environment production trust-anchor operation
- Do not close full C4.
- Do not claim C5 closure.
- If live trust context changes startup behavior, record contradiction/regression.
- If pending revocations are enforced early, record contradiction/regression.
- If active revocations are ignored, record contradiction/regression.
- If fallback/Dummy crypto activates, record contradiction/regression.

Expected verdict options:
- Strongest positive:
  mutable live trust context lands; handshake verifier reads from consistent live snapshots; startup behavior and all revocation semantics preserved; Run 069 remains non-mutating; Run 070 apply remains honestly bounded until session eviction; tests and release evidence pass.
- Partial positive:
  live trust context type lands but handshake path or release-binary evidence remains static/partial. State exact boundary.
- Negative:
  startup behavior changes unexpectedly, revocation semantics regress, stale/static verifier remains in use, fallback/Dummy crypto activates, or regressions fail.

Required final response:
1. Exact verdict
2. Exact files changed
3. Exact commands run
4. Tests/evidence run and pass/fail status
5. What was fixed
6. What was proven
7. What remains not solved
8. Whether contradiction.md was updated and why
9. Exact immediate next action recommended