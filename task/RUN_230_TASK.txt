Task: Run 230 — Source/Test Governance Evaluator Replay and Freshness State Boundary

Current accepted state:

* Run 220: source/test long-running governance-execution runtime consumption wiring landed.
* Run 221: release-binary runtime-consumption evidence fully closed.
* Run 222: source/test typed production governance execution evaluator interface boundary landed.
* Run 223: release-binary evaluator-interface evidence fully closed.
* Run 224: source/test governance evaluator runtime integration layer landed.
* Run 225: release-binary governance evaluator runtime integration evidence fully closed.
* Run 226: source/test governance evaluator runtime call-site wiring landed.
* Run 227: release-binary governance evaluator runtime call-site wiring evidence fully closed.
* Run 228: source/test peer evaluator-context representation boundary landed.
* Run 229: release-binary peer evaluator-context representation evidence fully closed:

  * release-built helper exercises the Run 228 peer evaluator-context representation boundary;
  * carrier taxonomy is release-evidenced;
  * `WireSchemaUnavailable` is fail-closed and not approval;
  * invalid live inbound `0x05` candidates are not propagated, staged, or applied;
  * invalid peer-driven drain candidates are not applied;
  * MainNet peer-driven apply remains refused;
  * no wire/schema/marker/sequence/trust-bundle change is claimed;
  * production/on-chain/MainNet evaluators remain unavailable/fail-closed;
  * fixture/emergency fixture evaluators remain non-production;
  * validator-set rotation remains unsupported;
  * Full C4 remains OPEN;
  * C5 remains OPEN.
* Current remaining production-governance gap:
  evaluator requests and responses bind replay nonce, freshness, and expiry fields, but there is not yet a typed state boundary that determines whether an evaluator decision is fresh, expired, stale, replayed, superseded, or already consumed before any mutation can happen.

Objective:
Add a source/test-only governance evaluator replay and freshness state boundary.

This run must define and test the pure/fail-closed state boundary for evaluator decisions before they can authorize lifecycle mutation. It must not implement a real governance engine, real on-chain verifier, MainNet governance, validator-set rotation, or broad storage redesign.

Release-binary replay/freshness evidence is deferred to Run 231.

Strict scope:

* Source/test only.
* No release-binary harness in this run.
* No real governance execution engine.
* No real on-chain governance proof verifier.
* No MainNet governance enablement.
* No MainNet peer-driven apply enablement.
* No validator-set rotation.
* No KMS/HSM backend implementation.
* No RemoteSigner backend implementation.
* No schema/wire/marker/sequence/trust-bundle changes.
* No broad storage redesign.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* Do not weaken Runs 070, 130–229.
* Do not claim full C4 or C5 closure.

Required source behavior:

1. Add a focused pure module, preferably:
   `crates/qbind-node/src/pqc_governance_evaluator_replay_state.rs`

2. Define typed replay/freshness inputs:

   * evaluator source identity digest;
   * evaluator request digest;
   * evaluator response digest;
   * evaluator transcript digest;
   * governance execution decision digest;
   * proposal id;
   * decision id;
   * lifecycle action;
   * candidate digest;
   * authority-domain sequence;
   * effective epoch;
   * expiry epoch;
   * replay nonce;
   * environment;
   * chain id;
   * genesis hash;
   * validation surface;
   * current canonical epoch;
   * optional previously seen decision state.

3. Define replay/freshness state classification:

   * `Fresh`;
   * `FreshButNotYetEffective`;
   * `Expired`;
   * `Stale`;
   * `ReplayDetected`;
   * `AlreadyConsumed`;
   * `Superseded`;
   * `WrongEpoch`;
   * `WrongEnvironment`;
   * `WrongChain`;
   * `WrongGenesis`;
   * `WrongSurface`;
   * `MalformedState`;
   * `StateUnavailable`;
   * `ProductionStateUnavailable`;
   * `MainNetStateUnavailable`.

4. Define typed outcome:

   * `ProceedFresh`;
   * `ProceedDeferred`;
   * `FailClosedExpired`;
   * `FailClosedReplay`;
   * `FailClosedAlreadyConsumed`;
   * `FailClosedSuperseded`;
   * `FailClosedWrongBinding`;
   * `FailClosedStateUnavailable`;
   * `FailClosedProductionUnavailable`;
   * `FailClosedMainNetUnavailable`.

5. Deterministic digest helpers:

   * replay state key digest;
   * replay observation digest;
   * consumed decision digest;
   * freshness transcript digest.

6. Define a pure trait/interface:

   * `GovernanceEvaluatorReplayStateReader`
   * optionally `GovernanceEvaluatorReplayStateWriter` as a boundary only.

For this run:

* fixture in-memory reader/writer may exist only for DevNet/TestNet source tests;
* production reader/writer must be callable but return unavailable/fail-closed;
* MainNet reader/writer must be callable but return unavailable/fail-closed;
* no real RocksDB schema, file format, database migration, marker schema, or sequence schema changes.

7. Compose with Run 222 / Run 224 / Run 226 / Run 228 types where feasible:

   * evaluator request/response digests;
   * runtime integration outcome;
   * peer evaluator-context surface;
   * lifecycle action/candidate/sequence binding.

8. Preserve mutation safety:
   Replay/freshness rejection must happen before:

   * Run 070 call;
   * live trust swap;
   * session eviction;
   * sequence write;
   * marker write.

9. Preserve MainNet refusal:
   MainNet peer-driven apply remains refused even if replay/freshness state says fresh.

Required accepted scenarios:
A1. DevNet fixture replay state accepts first-seen fresh evaluator decision.

A2. TestNet fixture replay state accepts first-seen fresh evaluator decision.

A3. fresh but not-yet-effective decision returns `ProceedDeferred`, not approval for mutation.

A4. decision at effective epoch returns `ProceedFresh`.

A5. decision before expiry returns `ProceedFresh`.

A6. deterministic replay state key digest.

A7. deterministic replay observation digest.

A8. deterministic consumed decision digest.

A9. deterministic freshness transcript digest.

A10. replay state key binds environment, chain id, genesis hash, source identity digest, request digest, response digest, proposal id, decision id, lifecycle action, candidate digest, sequence, and replay nonce.

A11. fixture writer records consumed decision only after explicit consume call.

A12. read-only validation does not mark consumed.

A13. production replay state reader is callable and returns unavailable/fail-closed.

A14. MainNet replay state reader is callable and returns unavailable/fail-closed.

A15. Run 224 integration remains compatible when replay state policy is Disabled / not wired.

A16. Run 228 peer evaluator context remains compatible when replay state policy is Disabled / not wired.

Required rejection scenarios:
R1. expired decision rejected.

R2. stale decision rejected.

R3. replayed decision rejected.

R4. already-consumed decision rejected.

R5. superseded decision rejected.

R6. wrong effective epoch rejected.

R7. wrong expiry epoch rejected.

R8. wrong environment rejected.

R9. wrong chain rejected.

R10. wrong genesis rejected.

R11. wrong validation surface rejected.

R12. wrong source identity digest rejected.

R13. wrong request digest rejected.

R14. wrong response digest rejected.

R15. wrong transcript digest rejected.

R16. wrong proposal id rejected.

R17. wrong decision id rejected.

R18. wrong lifecycle action rejected.

R19. wrong candidate digest rejected.

R20. wrong authority-domain sequence rejected.

R21. wrong replay nonce rejected.

R22. malformed state rejected.

R23. state unavailable rejected.

R24. production state unavailable rejected.

R25. MainNet state unavailable rejected.

R26. local operator cannot satisfy replay state policy.

R27. peer majority cannot satisfy replay state policy.

R28. validator-set rotation unsupported rejected.

R29. policy-change action unsupported rejected.

R30. validation-only rejection writes no marker and no sequence.

R31. mutating rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

R32. MainNet peer-driven apply remains refused even when replay state is fresh.

Required tests:
Add focused tests, preferably:

`crates/qbind-node/tests/run_230_governance_evaluator_replay_state_tests.rs`

Minimum coverage:

* A1–A16.
* R1–R32.
* deterministic digest tests.
* first-seen / replay / already-consumed / superseded tests.
* effective epoch / expiry epoch tests.
* read-only validation does not consume tests.
* explicit consume marks consumed tests.
* production/MainNet unavailable tests.
* no-mutation tests.
* MainNet refusal tests.
* compatibility with Runs 228, 226, 224, 222.

Required documentation:
Add:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_230.md`

Update:

* `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
* `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
* `docs/whitepaper/contradiction.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

Documentation must state:

* Run 230 is source/test governance evaluator replay/freshness state boundary work.
* It defines a typed pure replay/freshness state boundary for evaluator decisions.
* Fixture replay state is DevNet/TestNet source-test only.
* Production/MainNet replay state remains unavailable/fail-closed.
* No real governance engine or on-chain proof verifier is implemented.
* No RocksDB/file/schema/migration/storage format change is implemented.
* MainNet peer-driven apply remains refused.
* Validator-set rotation remains unsupported.
* Release-binary replay/freshness evidence is deferred to Run 231.
* Full C4 remains OPEN.
* C5 remains OPEN.

Validation commands:
Run at minimum:

* `cargo build -p qbind-node --lib`
* `cargo test -p qbind-node --test run_230_governance_evaluator_replay_state_tests`
* `cargo test -p qbind-node --test run_228_peer_evaluator_context_representation_tests`
* `cargo test -p qbind-node --test run_226_governance_evaluator_runtime_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_224_governance_evaluator_runtime_integration_tests`
* `cargo test -p qbind-node --test run_222_governance_execution_evaluator_tests`
* `cargo test -p qbind-node --test run_220_governance_execution_runtime_consumption_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

Acceptance criteria:
Run 230 is acceptable only if:

1. A typed replay/freshness state boundary exists.
2. Fresh / expired / stale / replay / consumed / superseded outcomes are distinguished.
3. Production/MainNet state remains unavailable/fail-closed.
4. Read-only validation does not mark consumed.
5. Explicit consume marks consumed only in fixture source tests.
6. Rejections are non-mutating.
7. MainNet peer-driven apply remains refused.
8. Validator-set rotation remains unsupported.
9. No storage/schema/migration change is introduced.
10. Release-binary evidence is deferred to Run 231.
11. No full C4 or C5 closure is claimed.
