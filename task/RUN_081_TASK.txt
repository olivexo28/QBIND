Continue QBIND from the current state.

Project:
- QBIND post-quantum L1
- security-first, production-grade, execution-first
- C4 is the main live operability tracker
- no broad redesign
- no silent regressions
- no overclaiming

Current state:
- Run 078 added typed/versioned/bounded peer-candidate wire envelope for frame discriminator 0x05.
- Run 079 wired disabled-by-default live receive-loop dispatch for 0x05, validation-only.
- Run 080 landed disabled-by-default send-side publisher plumbing:
  - LivePeerCandidateWirePublisher
  - bounded wait-for-peer + publish-once behavior
  - raw-frame send channel in TcpKemTlsP2pService
  - hidden flags:
    - --p2p-trust-bundle-peer-candidate-wire-publish-enabled
    - --p2p-trust-bundle-peer-candidate-wire-publish-path
    - --p2p-trust-bundle-peer-candidate-wire-publish-once
  - send-side metrics:
    - qbind_p2p_pqc_trust_bundle_peer_candidate_sent_total
    - qbind_p2p_pqc_trust_bundle_peer_candidate_send_failure_total
    - qbind_p2p_pqc_trust_bundle_peer_candidate_send_no_peer_total
    - qbind_p2p_pqc_trust_bundle_peer_candidate_send_oversize_total
- Run 080 verdict is currently PARTIAL POSITIVE because the release-binary N=2 real 0x05 exchange artifact is missing.
- Run 080 evidence doc states the exact next action:
  run a two-process release-binary DevNet capture with sender publish-once + receiver wire-validation enabled, collect sender/receiver logs + /metrics + sequence file checks, and append artifacts to upgrade the verdict.

Task:
Run 081. Evidence-only completion for Run 080: produce release-binary N=2 real 0x05 peer-candidate frame-exchange artifacts.

Primary objective:
Prove that the release qbind-node binary can send a real peer-candidate 0x05 frame from one running validator to another over the live P2P transport, and the receiver validates it through the Run 079 receive path while preserving all validation-only invariants.

Important framing:
This is evidence-first.
Do not add new features.
Do not redesign the wire path.
Do not implement peer-driven live apply.
Do not implement rebroadcast/propagation.
Do not implement activation_epoch, KMS/HSM, signing-key ratification, or fast-sync.
Do not modify source unless a real bug blocks the evidence run.
Do not weaken validation, sequence, live trust, or session invariants.
Do not claim full C4 or C5 closure.

Required release-binary evidence:

1. Build and identify binaries
   - cargo build --release -p qbind-node --bin qbind-node
   - cargo build --release -p qbind-node --example devnet_pqc_trust_bundle_helper
   - cargo build --release -p qbind-node --example devnet_pqc_root_helper
   - record:
     - branch
     - commit
     - clean/dirty status
     - qbind-node sha256
     - qbind-node ELF BuildID
     - helper sha256 / BuildID

2. Generate N=2 trust material
   - signed trust bundle
   - two leaf certs
   - two KEM secret files
   - peer leaf mappings
   - candidate envelope file for 0x05 send
   - no private material in logs/artifacts

3. Baseline N=2 signed-bundle startup
   - start V0 and V1
   - real PQC mutual-auth
   - cert accepted counters move
   - cert rejected counters remain zero
   - sequence file written normally
   - no --p2p-trusted-root fallback
   - no DummySig / DummyKem / DummyAead

4. Sender enabled, receiver enabled: valid candidate
   - sender uses:
     - --p2p-trust-bundle-peer-candidate-wire-publish-enabled
     - --p2p-trust-bundle-peer-candidate-wire-publish-path <valid envelope>
     - --p2p-trust-bundle-peer-candidate-wire-publish-once
   - receiver uses:
     - Run 079 wire-validation enable flag
   - prove:
     - sender reports sent_total increment
     - receiver reports received_total increment
     - receiver reports validated_total increment
     - logs say validation-only / not-applied
     - sequence file unchanged after candidate validation
     - live trust state unchanged where observable
     - session remains alive
     - session eviction metrics unchanged
     - live reload apply metrics unchanged
     - no propagation/rebroadcast
     - no fallback
     - no Dummy crypto

5. Sender enabled, receiver disabled
   - receiver does not enable wire validation
   - sender sends valid 0x05 frame
   - prove receiver disabled/cheap-ignore behavior
   - connection remains healthy
   - no sequence/live-trust/session mutation

6. Invalid candidate
   - send tampered or wrong-chain candidate
   - receiver rejects
   - sequence unchanged
   - no live trust mutation
   - no session eviction

7. Duplicate or rate-limit case if feasible
   - send same candidate twice or burst
   - prove duplicate/rate-limit metric moves according to Run 076/079/080 behavior
   - no mutation

Required regression commands:
- cargo test -p qbind-node --test run_080_pqc_peer_candidate_wire_send_tests
- cargo test -p qbind-node --test run_079_pqc_peer_candidate_wire_live_dispatch_tests
- cargo test -p qbind-node --test run_078_pqc_peer_candidate_wire_tests
- cargo test -p qbind-node --test run_077_pqc_peer_candidate_binary_tests
- cargo test -p qbind-node --test run_076_pqc_peer_candidate_validation_tests
- cargo test -p qbind-node --lib pqc_peer_candidate_wire
- cargo test -p qbind-node --lib metrics::tests::peer_candidate_send_metrics
- cargo test -p qbind-node --lib
- cargo test -p qbind-net --lib
- cargo test -p qbind-crypto --lib

If source changes are made, rerun the full affected matrix and explain why source changes were necessary.

Evidence document:
Create:
- docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_081.md

Also update:
- docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_080.md if project convention permits appending a “Run 081 follow-up evidence” note.
- docs/whitepaper/contradiction.md

Evidence doc must include:
- exact objective
- exact verdict
- exact files changed
- exact commands run
- test/evidence pass/fail status
- binary/helper identities
- N=2 orchestration command lines
- valid 0x05 exchange logs
- sender metrics
- receiver metrics
- sequence file before/after hashes
- proof no live reload apply metrics moved
- proof no session eviction metrics moved
- proof sessions remained healthy
- proof no fallback to --p2p-trusted-root
- proof no DummySig/DummyKem/DummyAead
- exact remaining C4 boundaries
- immediate next action

contradiction.md update rules:
- If N=2 release-binary real 0x05 frame exchange succeeds, update C4 to record Run 080/081 narrowed to validation-only wire exchange proven on release binary.
- Keep C4 open for:
  - peer-driven live apply / propagation
  - activation_epoch runtime source
  - KMS/HSM custody
  - in-binary/on-chain signing-key ratification
  - production fast-sync / consensus-storage restore
  - per-environment production trust-anchor operation
- Do not close full C4.
- Do not claim C5 closure.
- If candidate mutates live trust, burns sequence, evicts sessions, triggers apply, or causes fallback/Dummy crypto, record contradiction/regression.

Expected verdict:
- Strongest positive:
  release qbind-node N=2 exchanges a real 0x05 frame over live P2P transport; receiver validates valid candidate; invalid candidate rejects; validation-only invariants hold; sequence/live trust/session/apply state unchanged; no fallback; no Dummy crypto.
- Partial positive:
  evidence still relies on harness or cannot prove receiver validation / non-mutation.
- Negative:
  candidate mutates state, burns sequence, evicts sessions, triggers apply, bypasses validation, or regressions fail.

Required final response:
1. Exact verdict
2. Exact files changed
3. Exact commands run
4. Tests/evidence run and pass/fail status
5. What was proven
6. What remains not solved
7. Whether contradiction.md was updated and why
8. Exact immediate next action recommended