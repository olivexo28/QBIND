Run 296 — Release-binary evidence for the production KMS/HSM/cloud-KMS/PKCS#11 custody backend

You are continuing QBIND after Run 295.

Current accepted state:

Run 292 is accepted as PASS / release-binary evidence positive for the production durable replay RocksDB backend.

Run 294 is accepted as PASS / release-binary evidence positive for the production RemoteSigner backend.

Run 295 is accepted as PASS / source-test production KMS/HSM/cloud-KMS/PKCS#11 custody backend implementation.

Run 295 added:

`crates/qbind-node/src/pqc_production_kms_hsm_custody_backend.rs`

The backend defines `ProductionKmsHsmCustodyBackend<T: KmsHsmCustodyProviderTransport>`, composes the existing Run 203 KMS/HSM verifier, builds deterministic request envelopes and request IDs, enforces request/response correlation and transcript/domain binding, keeps fixture KMS/HSM strictly DevNet/TestNet source-test only, keeps production cloud-KMS / PKCS#11 / generic KMS/HSM paths reachable but fail-closed without real provider config, defaults to Disabled, refuses MainNet absent production authority criteria, and never falls back to fixture, RemoteSigner, local signing, raw local keys, or in-memory signing under production policy.

Run 295 is source/test only. It is not release-binary evidence.

Current C4/C5 matrix:

* Production durable replay RocksDB backend — Green for narrow release-binary-evidenced backend behavior only.
* Real production RemoteSigner backend — Green for narrow release-binary-evidenced backend behavior only.
* Real KMS/HSM/cloud-KMS/PKCS#11 custody backend — Yellow / source-test implementation landed, release-binary evidence pending Run 296.
* Real custody attestation verifier — Red.
* Real on-chain governance proof verifier — Red.
* Governance execution engine — Red.
* Validator-set rotation / authority-set synchronization — Red.
* MainNet authority rotation/revocation under production custody — Red.
* Production signing audit trail / crypto-agility activation / incident response — Red.
* Full MainNet release-binary evidence under production custody — Red.

Full C4 remains OPEN. C5 remains OPEN.

Preflight documentation hygiene:

Before or during Run 296, verify that `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_295.md` records the exact CodeQL result directly. If it still says only “see final response,” replace that with the actual honest result: CodeQL was skipped because the database was too large, and no CodeQL coverage is claimed. This is documentation hygiene only; do not change Run 295’s verdict unless a real contradiction is found.

Objective:

Produce release-binary evidence for the Run 295 production KMS/HSM/cloud-KMS/PKCS#11 custody backend.

This run must prove, on real `target/release/qbind-node` plus a release-built helper, that the Run 295 production custody backend symbols are present and exercised in release mode, while the production binary remains default-Disabled/silent, no CLI flag is added, MainNet remains refused absent production authority criteria and custody attestation, and C4/C5 remain open.

This is release-binary evidence only.

Do not add another modeled layer.

Do not add a new backend.

Do not wire KMS/HSM custody into default production runtime behavior.

Do not add a public CLI flag.

Do not claim C4 closure.

Do not claim C5 closure.

Primary deliverables:

1. New release helper:

`crates/qbind-node/examples/run_296_production_kms_hsm_custody_backend_release_binary_helper.rs`

2. New release harness:

`scripts/devnet/run_296_production_kms_hsm_custody_backend_release_binary.sh`

The harness must be LF-clean and executable.

3. New ignored evidence archive directory:

`docs/devnet/run_296_production_kms_hsm_custody_backend_release_binary/`

Track only:

* `README.md`
* `summary.txt`
* `.gitignore`

Generated logs, raw helper outputs, temp files, stdout/stderr captures, and binary artifacts must be ignored unless the project’s evidence convention explicitly requires otherwise.

4. New canonical evidence report:

`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_296.md`

5. Narrow documentation updates:

* `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
* `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
* `docs/whitepaper/contradiction.md`

Strict scope:

Allowed:

* Build real `target/release/qbind-node`.
* Build a release helper that links against the real release-built qbind-node library symbols.
* Exercise the Run 295 production KMS/HSM custody backend in release mode.
* Use fixture KMS/HSM and mock transports inside the release helper only as release-mode evidence scaffolding.
* Prove provider taxonomy and policy behavior in release mode.
* Prove request/response/transcript/domain binding in release mode.
* Prove unavailable, misconfigured, malformed, replay, wrong-domain, wrong-provider, wrong-key, wrong-policy, wrong-protocol, missing-attestation, and MainNet refusal fail-closed behavior in release mode.
* Prove default production binary surfaces remain Disabled/silent.
* Prove no new CLI flag is exposed.
* Prove MainNet remains refused absent production authority criteria and custody attestation.
* Capture release hashes, helper verdict, scenario return codes, denylist results, reachability results, C4/C5 taxonomy greps, regression results, secret scan, and CodeQL result or honest skip/timeout.
* Update docs/evidence narrowly.

Not allowed:

* Do not add new production runtime wiring.
* Do not add a public CLI flag.
* Do not enable KMS/HSM custody by default.
* Do not make MainNet use KMS/HSM custody.
* Do not claim production custody closure.
* Do not claim C4 closure.
* Do not claim C5 closure.
* Do not claim MainNet readiness.
* Do not claim full production readiness.
* Do not implement real cloud-KMS credentials, real PKCS#11 sessions, real HSM module loading, or real provider account wiring in this run.
* Do not commit secrets, tokens, API keys, HSM PINs, cloud account identifiers, real provider endpoints, or real PKCS#11 module paths.
* Do not implement custody attestation verifier closure.
* Do not implement on-chain governance proof verification.
* Do not implement a governance execution engine.
* Do not implement validator-set rotation.
* Do not implement settlement, settlement finality, settlement receipt, settlement-receipt acknowledgement, settlement-finality projection, settlement-outcome report backend, or settlement-outcome publication.
* Do not implement external publication, external-publication confirmation, receipt, acknowledgement, audit finalization, audit completion, audit archive, audit seal, or audit anchor.
* Do not call Run 070.
* Do not mutate `LivePqcTrustState`.
* Do not swap trust state.
* Do not evict sessions.
* Do not write PQC trust-bundle sequence files.
* Do not write authority markers.
* Do not change trust-bundle wire format.
* Do not change peer-candidate wire format.
* Do not change consensus wire format.
* Do not weaken Run 292 durable replay RocksDB Green-for-scope status.
* Do not weaken Run 294 RemoteSigner Green-for-scope status.
* Do not silently fall back to fixture KMS/HSM, RemoteSigner, local signing, raw local keys, dummy signing, or in-memory signing under production custody policy.
* Do not mark unrelated Red rows Green.
* Do not mark Full C4 or C5 closed.

Required release helper behavior:

The release helper must link against the real release-built qbind-node library and exercise the actual Run 295 production KMS/HSM custody backend symbols.

At minimum, it must reference the actual implemented names, including where present:

* `pqc_production_kms_hsm_custody_backend`
* `ProductionKmsHsmCustodyBackend`
* `KmsHsmCustodyProviderTransport`
* `ProductionKmsHsmCustodyBackendPolicy`
* `ProductionCustodyProviderKind`
* `ProductionCustodyRequestKind`
* `ProductionCustodyRequest`
* `ProductionCustodyResponse`
* `ProductionCustodyError`
* `ProductionCustodyOutcome`
* `production_kms_hsm_custody_request_id`
* `production_kms_hsm_custody_transcript_digest`
* `FixtureKmsCustodyProvider`
* `FixtureHsmCustodyProvider`
* `ProductionCustodyProviderStub`
* `MockKmsHsmCustodyTransport`
* `verify_authority_custody_backend_response`

If actual names differ, use the real names and document substitutions in the evidence report.

The helper must produce machine-readable summary output with per-check PASS/FAIL counts and a final verdict.

Suggested helper sections:

A. Accepted / compatible release checks

* Disabled default policy is explicit and inert.
* DevNet fixture KMS accepts a valid request only under explicit fixture policy.
* DevNet fixture HSM accepts a valid request only under explicit fixture policy.
* TestNet fixture KMS/HSM accepts only under explicit allowed policy if existing policy permits.
* Fixture KMS/HSM acceptance is release-mode scaffolding only, not production authority.
* Production cloud-KMS request can be built without fixture fallback.
* Production PKCS#11 HSM request can be built without fixture fallback.
* Production generic KMS/HSM request can be built without fixture fallback.
* Production cloud-KMS path is reachable and fails closed as unavailable/misconfigured without real provider config.
* Production PKCS#11 HSM path is reachable and fails closed as unavailable/misconfigured without real module/session/key handle.
* Request id is deterministic across two helper invocations.
* Response digest is deterministic across two helper invocations if represented.
* Transcript digest is deterministic across two helper invocations.
* Two identical requests produce identical request/transcript digests.
* Valid fixture response authorizes exactly the matching request.
* Run 203 `verify_authority_custody_backend_response` composition is exercised.
* Run 293/294 RemoteSigner Green-for-scope behavior remains unchanged where representable.

B. Rejection / fail-closed release checks

* Default Disabled policy fails closed before provider invocation.
* MainNet identity refused before provider invocation.
* Fixture KMS material refused for MainNet.
* Fixture HSM material refused for MainNet.
* RemoteSigner material cannot satisfy KMS/HSM custody row.
* Local operator material alone refused for MainNet.
* Peer majority refused for MainNet.
* Wrong environment rejected.
* Wrong chain rejected.
* Wrong genesis/domain rejected.
* Wrong authority sequence/epoch rejected.
* Wrong provider kind rejected.
* Wrong provider identity rejected.
* Wrong key handle rejected.
* Wrong signer identity rejected.
* Wrong request id rejected.
* Wrong transcript digest rejected.
* Wrong candidate/proposal digest rejected.
* Wrong authorized action rejected.
* Wrong protocol version rejected.
* Missing signature/proof rejected.
* Missing attestation rejected where production policy requires attestation.
* Attestation unavailable remains fail-closed.
* Attestation invalid or unverified remains fail-closed if represented.
* Malformed response rejected.
* Oversized response rejected if response sizing exists.
* Response from prior request replay rejected.
* Provider unavailable rejected.
* Provider timeout rejected.
* Provider decode error rejected.
* Provider refused rejected.
* Provider policy rejected rejected.
* Unsupported provider rejected.
* Endpoint unavailable rejected.
* Unsupported protocol rejected.
* Ambiguous result rejected fail-closed.
* Unsupported request kinds fail closed before provider invocation:

  * validator-set rotation;
  * policy change;
  * on-chain governance proof verification.

C. MainNet / authority policy release checks

* MainNet cannot be satisfied by fixture KMS.
* MainNet cannot be satisfied by fixture HSM.
* MainNet cannot be satisfied by RemoteSigner-only evidence.
* MainNet cannot be satisfied by local operator material.
* MainNet cannot be satisfied by peer majority.
* MainNet cannot bypass missing custody attestation.
* MainNet cannot bypass missing governance proof verifier.
* MainNet cannot bypass validator-set rotation gate.
* MainNet production custody unavailable path records no mutation.
* MainNet peer-driven apply remains refused.

D. Replay / recovery / idempotency release checks

If Run 295 recovery helpers are represented:

* no prior request returns clean no-prior/no-op;
* byte-identical duplicate request is idempotent or safely classified as identical;
* same request id with different transcript fails closed;
* same request id with different key handle fails closed;
* same request id with different response commitment fails closed;
* different request ids are unrelated;
* idempotency is byte-identical only;
* recovery window is pure comparison and makes no persisted durable-acceptance claim.

If replay state is not represented beyond pure comparison, document that honestly and prove no durable replay acceptance is claimed.

E. Non-mutation / no-authority-extension release checks

Every rejected helper path must prove:

* no Run 070 call;
* no `LivePqcTrustState` mutation;
* no trust swap;
* no session eviction;
* no PQC trust-bundle sequence write;
* no authority marker write;
* no durable replay overwrite;
* no settlement;
* no external publication;
* no governance execution engine;
* no validator-set rotation;
* no RemoteSigner fallback;
* no KMS/HSM signing under wrong policy;
* no raw local production signing key load;
* no fallback to fixture/local signing under production policy;
* no MainNet enablement.

These may be proven through helper invariants, source-reachability greps, denylist checks, and real-binary scenario silence.

Required real-binary scenarios:

The harness must build and run real `target/release/qbind-node`.

At minimum:

S1. `target/release/qbind-node --help`

Expected:

* rc=0.
* no public Run 295/296 KMS/HSM custody backend flag appears.
* no KMS/HSM custody backend enablement claim.
* no custody attestation claim.
* no MainNet readiness claim.
* no C4/C5 closure claim.

S2. Default DevNet binary surface.

Use the smallest safe invocation already used by recent release evidence harnesses.

Expected:

* KMS/HSM custody backend is not constructed by default;
* no KMS/HSM backend enablement banner;
* no helper/evidence-only code path appears;
* no mutation.

S3. Default TestNet binary surface.

Expected:

* KMS/HSM custody backend is not constructed by default;
* no KMS/HSM backend enablement banner;
* no custody attestation / on-chain verifier / governance / validator-set-rotation claim;
* no mutation.

S4. Default MainNet binary surface.

Expected:

* KMS/HSM custody backend is not constructed by default;
* no KMS/HSM backend enablement banner;
* no MainNet enablement claim;
* any existing MainNet-required-config failure remains expected fail-closed behavior;
* no mutation.

S5. Existing governance/custody/RemoteSigner selectors, if any, remain compatible.

If an existing hidden selector related to governance execution, custody, RemoteSigner, or KMS/HSM exists, prove it still parses or fails closed exactly as before. If there is no existing selector, document that no new CLI selector was added.

S6. Invalid selector / invalid config fail-closed scenario.

Use the nearest existing invalid governance/custody/KMS/HSM selector if available. It must fail closed before mutation.

Required release artifact metadata:

Capture in `summary.txt`:

* git commit;
* branch name if available;
* dirty/clean status;
* rustc version;
* cargo version;
* OS/kernel summary if existing harness convention captures it;
* `target/release/qbind-node` SHA-256;
* release helper SHA-256;
* helper verdict;
* helper check counts by category;
* deterministic request/transcript digest samples;
* real-binary scenario return codes;
* denylist result;
* reachability result;
* C4/C5 taxonomy result;
* regression result;
* secret scan result;
* CodeQL result or honest limitation;
* C4/C5 status.

Required denylist:

The harness must assert zero matches in release binary stdout/stderr/help/helper logs for false claims such as:

* `C4 closed`
* `C5 closed`
* `MainNet ready`
* `production ready`
* `production custody active`
* `KMS active`
* `HSM active`
* `PKCS11 active`
* `cloud KMS active`
* `custody attestation active`
* `on-chain governance verifier active`
* `governance execution engine active`
* `validator-set rotation active`
* `peer-driven apply enabled`
* `Run 070 applied`
* `LivePqcTrustState mutated`
* `trust swap complete`
* `session eviction complete`
* `authority marker written`
* `trust-bundle sequence written`
* `settlement finalized`
* `settlement receipt recorded`
* `settlement outcome published`
* `external publication completed`
* `external publication confirmed`
* `fallback to fixture`
* `fallback to local signing`
* `fallback to RemoteSigner`
* `raw local production key`
* `DummySig active`
* `DummyKem active`
* `DummyAead active`

Adjust exact grep patterns to avoid false positives from explicit negative statements such as “not active” or evidence non-goal prose. The evidence must clearly distinguish absent enablement claims from documented non-goals.

Required reachability greps:

The harness must prove the release helper and/or source references the Run 295 production symbols, including:

* `ProductionKmsHsmCustodyBackend`
* `KmsHsmCustodyProviderTransport`
* `ProductionKmsHsmCustodyBackendPolicy`
* `ProductionCustodyProviderKind`
* `ProductionCustodyRequestKind`
* `ProductionCustodyError`
* `ProductionCustodyOutcome`
* `production_kms_hsm_custody_request_id`
* `production_kms_hsm_custody_transcript_digest`
* `FixtureKmsCustodyProvider`
* `FixtureHsmCustodyProvider`
* `ProductionCustodyProviderStub`
* `MockKmsHsmCustodyTransport`
* `verify_authority_custody_backend_response`

Required C4/C5 taxonomy verification:

The harness must grep `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md` and prove:

* Status as of Run 296 is present.
* Full C4 remains OPEN.
* C5 remains OPEN.
* Production durable replay RocksDB backend remains Green for release-binary-evidenced scope only.
* Real production RemoteSigner backend remains Green for release-binary-evidenced scope only.
* Real KMS/HSM/cloud-KMS/PKCS#11 custody backend row is updated correctly.
* If Run 296 evidence passes, this row may become Green only for narrow release-binary-evidenced custody-backend behavior.
* The KMS/HSM row wording must explicitly say: not wired by default, no public CLI flag, MainNet refused absent production authority criteria and custody attestation, does not close C4/C5.
* Real custody attestation verifier remains Red.
* Real on-chain governance proof verifier remains Red.
* Governance execution engine remains Red.
* Validator-set rotation / authority-set synchronization remains Red.
* MainNet authority rotation/revocation under production custody remains Red.
* Production signing audit trail / crypto-agility activation / incident response remains Red.
* Full MainNet release-binary evidence under production custody remains Red.
* Matrix taxonomy still separates boundary readiness from production readiness.
* Green-for-scope does not mean C4/C5 closure unless every closure gate is met.

Important matrix guidance:

If Run 296 proves release-binary behavior for the Run 295 KMS/HSM custody backend, it is acceptable to mark only the narrow **Real KMS/HSM/cloud-KMS/PKCS#11 custody backend** capability as Green or Green-for-scope, provided the row wording is precise:

“Green for release-binary-evidenced KMS/HSM custody backend behavior only; not wired by default into production runtime; no public CLI flag; MainNet refused absent production authority criteria and custody attestation; production cloud-KMS / PKCS#11 / generic provider paths remain fail-closed without real provider config; does not implement custody attestation verifier / on-chain verifier / governance engine / validator-set rotation; does not close C4/C5.”

Do not turn custody attestation Green.

Do not turn on-chain verifier Green.

Do not turn governance engine Green.

Do not turn validator-set rotation Green.

Do not claim C4/C5 closure.

Required tests / regression commands:

Run at minimum:

* `cargo build -p qbind-node --release --bin qbind-node`
* `cargo build -p qbind-node --release --example run_296_production_kms_hsm_custody_backend_release_binary_helper`
* `scripts/devnet/run_296_production_kms_hsm_custody_backend_release_binary.sh`
* `cargo test -p qbind-node --test run_295_production_kms_hsm_custody_backend_tests`
* `cargo test -p qbind-node --test run_293_production_remote_signer_backend_tests`
* `cargo test -p qbind-node --test run_291_production_durable_replay_rocksdb_tests`
* `cargo test -p qbind-node --test run_290_durable_completion_external_publication_audit_anchor_tests`
* `cargo test -p qbind-node --test run_288_durable_completion_external_publication_audit_seal_tests`
* `cargo test -p qbind-node --test run_286_durable_completion_external_publication_audit_archive_tests`
* `cargo test -p qbind-node --test run_284_durable_completion_external_publication_audit_completion_tests`
* `cargo test -p qbind-node --test run_282_durable_completion_external_publication_audit_finalization_tests`
* `cargo test -p qbind-node --test run_280_durable_completion_external_publication_acknowledgement_tests`
* `cargo test -p qbind-node --test run_278_durable_completion_external_publication_receipt_tests`
* `cargo test -p qbind-node --test run_276_durable_completion_external_publication_confirmation_tests`
* `cargo test -p qbind-node --test run_274_durable_completion_settlement_outcome_publication_tests`
* `cargo test -p qbind-node --test run_272_durable_completion_settlement_outcome_report_tests`
* `cargo test -p qbind-node --test run_270_durable_completion_settlement_receipt_acknowledgement_tests`
* `cargo test -p qbind-node --test run_268_durable_completion_settlement_finalization_tests`
* `cargo test -p qbind-node --test run_266_durable_completion_settlement_commitment_tests`
* `cargo test -p qbind-node --test run_264_durable_completion_consumer_settlement_projection_tests`
* `cargo test -p qbind-node --test run_262_durable_completion_acknowledgement_consumer_tests`
* `cargo test -p qbind-node --test run_260_durable_completion_audit_receipt_acknowledgement_tests`
* `cargo test -p qbind-node --test run_258_durable_completion_audit_publication_receipt_tests`
* `cargo test -p qbind-node --test run_256_durable_completion_attestation_backend_tests`
* `cargo test -p qbind-node --test run_254_modeled_durable_completion_attestation_projection_tests`
* `cargo test -p qbind-node --test run_252_modeled_durable_completion_finalization_projection_tests`
* `cargo test -p qbind-node --test run_250_modeled_durable_consume_completion_reporter_tests`
* `cargo test -p qbind-node --test run_248_modeled_durable_consume_projection_sink_tests`
* `cargo test -p qbind-node --test run_246_governance_modeled_end_to_end_pipeline_tests`
* `cargo test -p qbind-node --test run_244_modeled_governance_trust_mutation_applier_tests`
* `cargo test -p qbind-node --test run_242_governance_execution_mutation_engine_tests`
* `cargo test -p qbind-node --test run_240_governance_evaluator_replay_durable_runtime_integration_tests`
* `cargo test -p qbind-node --test run_238_governance_evaluator_replay_durable_backend_tests`
* `cargo test -p qbind-node --test run_236_governance_evaluator_replay_consume_runtime_integration_tests`
* `cargo test -p qbind-node --test run_234_governance_evaluator_replay_consume_boundary_tests`
* `cargo test -p qbind-node --test run_232_governance_evaluator_replay_runtime_integration_tests`
* `cargo test -p qbind-node --test run_230_governance_evaluator_replay_state_tests`
* `cargo test -p qbind-node --test run_228_peer_evaluator_context_representation_tests`
* `cargo test -p qbind-node --test run_226_governance_evaluator_runtime_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_224_governance_evaluator_runtime_integration_tests`
* `cargo test -p qbind-node --test run_203_kms_hsm_backend_tests` or the actual Run 203 test target
* `cargo test -p qbind-node --test run_201_remote_signer_transport_boundary_tests` or the actual Run 201 target
* `cargo test -p qbind-node --test run_194_remote_authority_signer_boundary_tests` or the actual Run 194 target
* `cargo test -p qbind-node --test run_188_authority_custody_boundary_tests` or the actual Run 188 target
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If actual test target names differ, use the real names and record substitutions in the evidence.

If running the full old corpus is too expensive, run the full required list unless there is a documented known pre-existing blocker. Do not silently skip older regression targets.

Security scanning:

* Run secret scanning over changed files and evidence scripts.
* Run CodeQL if available.
* Because Run 296 is release-binary evidence for a real custody backend, CodeQL or an honest skip/timeout reason is required.
* If CodeQL is skipped, times out, reports database too large, tooling is unavailable, or otherwise does not complete, report that explicitly and do not claim CodeQL coverage.
* The canonical evidence file must include the exact CodeQL result or exact honest limitation directly.

Required docs:

`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_296.md` must include:

1. Exact verdict.
2. Files changed.
3. Release artifacts and hashes.
4. Helper corpus results.
5. Real-binary scenarios.
6. Provider taxonomy / policy release evidence.
7. Request/response/transcript/domain binding release evidence.
8. Accepted release evidence.
9. Rejection/fail-closed release evidence.
10. MainNet refusal / authority policy release evidence.
11. Replay/recovery/idempotency release evidence.
12. Production-binary default-disabled/silent evidence.
13. C4/C5 matrix taxonomy verification.
14. Regression results.
15. Security scan results.
16. Honest limitations.
17. C4/C5 status.
18. Suggested Run 297 next step.

Docs must state clearly:

* Run 296 is release-binary evidence for the Run 295 production KMS/HSM/cloud-KMS/PKCS#11 custody backend.
* Run 296 does not add new production runtime wiring.
* Run 296 does not add a public CLI flag.
* Run 296 does not enable KMS/HSM custody by default.
* Run 296 does not enable MainNet.
* Run 296 does not implement real provider credentials / real cloud-KMS account integration / real PKCS#11 session integration.
* Run 296 does not implement custody attestation verifier closure.
* Run 296 does not implement on-chain governance proof verification.
* Run 296 does not implement governance execution engine.
* Run 296 does not implement validator-set rotation.
* Run 296 does not implement settlement or external publication.
* Run 296 does not call Run 070.
* Run 296 does not mutate `LivePqcTrustState`.
* Run 296 does not write trust-bundle sequence or authority marker files.
* Run 296 preserves Run 292 durable replay RocksDB Green-for-scope status.
* Run 296 preserves Run 294 RemoteSigner Green-for-scope status.
* Full C4 remains OPEN.
* C5 remains OPEN.

Contradiction handling:

Update `docs/whitepaper/contradiction.md`.

Add a “Run 296” entry.

If no contradiction is found, state that Run 296 release-evidences the Run 295 production KMS/HSM/cloud-KMS/PKCS#11 custody backend while preserving default Disabled/fail-closed runtime posture, no CLI flag, no MainNet enablement, no custody-attestation closure, no on-chain-verifier/governance-engine/validator-set-rotation implementation, no Run 070 call, no `LivePqcTrustState` mutation, no trust-bundle sequence or authority-marker writes, and Full C4 OPEN / C5 OPEN.

If contradiction is found, record the exact contradiction with file/function/doc reference and recommend the smallest safe fix.

Expected verdicts:

PASS / release-binary evidence positive only if:

* release `qbind-node` builds;
* release helper builds;
* harness exits 0;
* helper corpus passes;
* real-binary scenarios pass;
* release artifacts and hashes are captured;
* denylist is clean;
* reachability greps pass;
* C4/C5 taxonomy greps pass;
* Run 295 source tests still pass;
* required regression corpus passes;
* secret scan is clean;
* CodeQL result or honest skip/timeout is reported directly in the canonical evidence;
* docs/evidence/contradiction are updated;
* Full C4 remains OPEN;
* C5 remains OPEN.

Partial-positive only if:

* release helper proves the backend in release mode but one bounded, non-critical scenario is incomplete and documented honestly;
* no false closure claim is made.

Blocked only if:

* the Run 295 backend cannot be linked/exercised in release mode without redesign;
* document the exact blocker and smallest safe next step.

Unacceptable:

* claiming C4 closure;
* claiming C5 closure;
* claiming MainNet readiness;
* claiming runtime default enablement;
* adding hidden production behavior without evidence;
* adding a public CLI flag;
* silently falling back to fixture KMS/HSM, RemoteSigner, local signing, raw local keys, dummy signing, or in-memory signing;
* using raw local production signing keys;
* skipping provider/request/response/transcript/domain-binding release checks;
* skipping unavailable/misconfigured/malformed/replay/wrong-domain/wrong-key release checks;
* hiding CodeQL failure/timeout/skip;
* hiding regression failures.

Suggested Run 297 next step after successful Run 296:

Begin the next Red-row closure campaign.

Preferred next target:

Run 297 — source/test production custody attestation verifier implementation.

Reason: after Run 296, the KMS/HSM custody backend can be Green-for-scope, but C5 still requires production custody with attestation. The next highest-value Red row is therefore a real custody attestation verifier that can validate provider attestation evidence and reject unverified custody material.

Run 297 should be source/test only, default Disabled/fail-closed, MainNet refused unless production authority criteria are satisfied, release-binary evidence deferred to Run 298.

Required final response format:

1. Exact verdict.
2. Files changed.
3. Release artifacts and hashes.
4. Helper corpus results.
5. Real-binary scenarios.
6. Provider taxonomy / policy release evidence.
7. Request/response/transcript/domain binding release evidence.
8. Accepted release evidence.
9. Rejection/fail-closed release evidence.
10. MainNet refusal / authority policy release evidence.
11. Replay/recovery/idempotency release evidence.
12. Production-binary default-disabled/silent evidence.
13. Tests run and results.
14. C4/C5 matrix taxonomy status.
15. Security invariants preserved.
16. Honest limitations.
17. C4/C5 status.
18. Suggested Run 297 next step.
