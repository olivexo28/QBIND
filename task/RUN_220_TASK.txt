Task: Run 220 — Source/Test Long-Running Governance Execution Runtime Consumption Wiring

Current accepted state:

* Run 211: source/test governance execution policy boundary landed.
* Run 212: release-binary governance execution policy-boundary evidence fully closed.
* Run 213: source/test governance execution payload/carrying and production-context wiring landed.
* Run 214: release-binary governance execution payload/carrying evidence fully closed.
* Run 215: source/test hidden governance execution policy selector landed.
* Run 216: release-binary governance execution policy selector evidence fully closed.
* Run 217: source/test governance execution runtime arming carrier landed.
* Run 218: release-binary governance execution runtime-arming evidence fully closed.
* Run 219: governance execution runtime surface gap audit landed:

  * selectors are fully wired and real-binary evidenced;
  * runtime arming carrier is partially wired;
  * `GovernanceExecutionRuntimeArmingConfig` is built on live paths;
  * some call sites currently discard the arming outcome, e.g. `let _outcome = arming.arm_surface(...)`;
  * SIGHUP preflight has a similar non-consuming hook;
  * some paths force governance-execution load status to `Absent`;
  * reload-check, reload-apply, startup `--p2p-trust-bundle`, SIGHUP, and local peer-candidate-check are partially wired;
  * live inbound `0x05` and peer-driven drain remain helper-evidenced only for governance-execution runtime consumption;
  * rejection remains non-mutating;
  * MainNet peer-driven apply remains refused;
  * the gap is consumption, not safety.
* Run 219 selected:

  * Run 220: source/test long-running governance-execution runtime consumption wiring.
  * Run 221: release-binary long-running governance-execution runtime consumption evidence.
* Full C4 remains OPEN.
* C5 remains OPEN.

Objective:
Wire governance-execution runtime arming so long-running node preflight paths actually consume the selected `GovernanceExecutionPolicy` and governance-execution payload load status instead of discarding the outcome or forcing `Absent`.

This run must make selected governance-execution policy meaningful on production source call sites at source/test level while preserving default Disabled behavior, fail-closed explicit policy behavior, and MainNet refusal.

Release-binary runtime-consumption evidence is deferred to Run 221.

Strict scope:

* Source/test only.
* No release-binary harness in this run.
* No real governance execution engine.
* No real on-chain governance proof verifier.
* No MainNet governance enablement.
* No MainNet peer-driven apply enablement.
* No validator-set rotation.
* No authority-set rotation beyond existing lifecycle boundary checks.
* No real KMS implementation.
* No real HSM implementation.
* No real RemoteSigner backend.
* No production signing key custody.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* No schema/wire changes.
* No authority-marker schema change.
* No sequence-file schema change.
* No trust-bundle core schema change.
* No authority lifecycle semantics change.
* Do not weaken Runs 070, 130–219.
* Do not claim full C4 or C5 closure.

Required source behavior:

1. Replace discarded runtime-arming outcomes with consumed outcomes where safe.

Specifically inspect and address the Run 219 findings:

* `main.rs` call sites where `let _outcome = arming.arm_surface(...)` discards the result.
* `pqc_live_trust_reload.rs` / SIGHUP hook where the arming outcome is not consumed.
* any forced `GovernanceExecutionLoadStatus::Absent` in runtime paths that should use real loaded governance-execution material when present.

2. Route loaded governance-execution material into runtime preflight contexts.

For sidecar-based surfaces:

* reload-check;
* reload-apply;
* startup `--p2p-trust-bundle`;
* SIGHUP;
* local peer-candidate-check;

the runtime path must use the Run 213 combined loader / payload parser where representable, so optional `governance_execution` sidecar material can become:

* `GovernanceExecutionLoadStatus::Absent`;
* `GovernanceExecutionLoadStatus::Available`;
* `GovernanceExecutionLoadStatus::Malformed`.

Names may differ; use actual project types.

3. Consume selected policy and load status.

For each representable runtime surface:

* resolve policy from CLI/env using Run 215/217 helper;
* pass selected policy into Run 217 arming config;
* pass real governance-execution load status into Run 213 routing;
* consume returned outcome;
* if outcome rejects, fail closed before mutation.

4. Default compatibility.

Unset CLI/env must preserve existing behavior:

* `GovernanceExecutionPolicy::Disabled`;
* legacy no-governance-execution sidecars remain compatible;
* no new governance-execution enforcement by default.

5. Explicit policy behavior.

When an explicit governance-execution policy is selected:

* missing governance-execution material fails closed where policy requires it;
* malformed governance-execution material fails closed;
* valid fixture governance execution passes only on DevNet/TestNet where policy allows;
* emergency council fixture passes only under explicit emergency policy and explicit emergency action;
* production/on-chain/MainNet governance execution reaches the evaluator and fails closed as unavailable;
* invalid selector values fail closed before runtime mutation.

6. Runtime surface treatment.

For each of the seven surfaces, implement or document precisely:

A. reload-check:

* consume selected policy;
* consume real governance-execution sidecar status;
* validation-only rejection writes no marker and no sequence.

B. reload-apply:

* consume selected policy;
* consume real governance-execution sidecar status;
* mutating rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

C. startup `--p2p-trust-bundle`:

* consume selected policy;
* consume real governance-execution sidecar status where the startup path has sidecar material;
* reject fail-closed before mutation.

D. SIGHUP:

* consume selected policy from `LiveReloadConfig`;
* consume real governance-execution sidecar status where representable;
* reject fail-closed before mutation.

E. local peer-candidate-check:

* consume selected policy;
* consume real governance-execution material from the candidate/sidecar context where representable;
* validation-only rejection writes no marker and no sequence.

F. live inbound `0x05`:

* if production inbound envelope can carry governance-execution material, route and consume selected policy;
* if not currently representable, document exact limitation and keep invalid candidates non-propagated / non-staged / non-applied;
* do not invent a schema in Run 220 unless a strictly additive already-established carrier exists.

G. peer-driven drain:

* consume selected policy where the peer-driven drain has access to sidecar/candidate material;
* MainNet peer-driven apply remains refused unconditionally;
* if not representable, document exact limitation.

7. Preserve non-mutation and ordering.

Rejected governance-execution runtime-consumption outcomes must produce:

* no Run 070 apply call;
* no live trust swap;
* no session eviction;
* no sequence write;
* no marker write;
* no marker-before-sequence regression.

Accepted mutating compatibility path, if exercised, must preserve:

* selector resolution before governance-execution payload parse;
* runtime policy arming before marker decision;
* governance-execution payload parse before marker decision;
* governance execution evaluation before apply/mutation;
* lifecycle validation before apply/mutation;
* governance proof validation before apply/mutation where applicable;
* custody validation before apply/mutation where applicable;
* custody attestation validation before apply/mutation where applicable;
* Run 055 sequence commit before v2 marker persistence.

Required accepted scenarios:
A1. default CLI/env absent resolves to Disabled through runtime config and consumes a Disabled outcome without changing legacy behavior.

A2. reload-check consumes fixture policy and accepts DevNet governance-execution sidecar material.

A3. reload-check consumes fixture policy and accepts TestNet governance-execution sidecar material.

A4. reload-apply consumes fixture policy and accepts DevNet governance-execution sidecar material where lifecycle/governance/custody checks pass.

A5. startup `--p2p-trust-bundle` consumes selected fixture policy and governance-execution sidecar material where representable.

A6. SIGHUP consumes selected fixture policy and governance-execution sidecar material where representable.

A7. local peer-candidate-check consumes selected fixture policy and governance-execution material where representable.

A8. live inbound `0x05` consumes selected policy where representable; otherwise documents exact limitation and preserves no-propagation/no-staging/no-apply.

A9. peer-driven drain consumes selected policy where representable and remains MainNet-refused.

A10. CLI-over-env precedence is preserved through runtime consumption.

A11. invalid selector fails closed before runtime mutation.

A12. production-governance-required reaches production governance unavailable outcome and rejects.

A13. mainnet-governance-required reaches MainNet governance unavailable / MainNet refusal outcome and rejects.

A14. emergency-council-fixture-allowed only accepts explicit emergency action.

A15. default Disabled preserves Run 214 compatibility for no-governance-execution payloads.

A16. runtime path no longer discards `arm_surface` outcome on the surfaces that claim consumption.

A17. runtime path no longer forces `Absent` when a real governance-execution sidecar is present on representable surfaces.

Required rejection scenarios:
R1. invalid CLI selector rejected before mutation.

R2. invalid env selector rejected before mutation.

R3. unrelated env/CLI does not enable governance execution.

R4. missing governance-execution material rejected under FixtureGovernanceAllowed.

R5. missing governance-execution material rejected under ProductionGovernanceRequired.

R6. malformed governance-execution material rejected.

R7. fixture governance rejected under ProductionGovernanceRequired.

R8. emergency fixture rejected under ProductionGovernanceRequired.

R9. fixture governance rejected under MainnetGovernanceRequired.

R10. production governance rejected as unavailable.

R11. on-chain governance rejected as unavailable.

R12. MainNet governance rejected as unavailable.

R13. wrong lifecycle action rejected.

R14. wrong candidate digest rejected.

R15. wrong authority-domain sequence rejected.

R16. wrong governance proof digest rejected.

R17. expired decision rejected.

R18. stale/replayed decision rejected.

R19. quorum threshold insufficient rejected.

R20. emergency action not authorized rejected.

R21. validator-set rotation unsupported rejected.

R22. policy-change action unsupported rejected.

R23. local operator cannot satisfy governance execution.

R24. peer majority cannot satisfy governance execution.

R25. validation-only rejection writes no marker and no sequence.

R26. mutating rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

R27. invalid live inbound `0x05` governance-execution candidate is not propagated, staged, or applied where live surface is representable.

R28. MainNet peer-driven apply remains refused even with MainnetGovernanceRequired and fixture governance approval.

Required tests:
Add focused tests, preferably:

`crates/qbind-node/tests/run_220_governance_execution_runtime_consumption_tests.rs`

Minimum coverage:

* A1–A17 where representable.
* R1–R28.
* source reachability:

  * runtime config consumes selected policy;
  * runtime config consumes real governance-execution sidecar status where representable;
  * `arm_surface` outcome is consumed, not discarded;
  * Run 213 payload routing reaches Run 211 evaluator under selected policy.
* regression against Run 219 findings:

  * no `let _outcome = arming.arm_surface(...)` remains in production runtime call sites that claim consumption;
  * no forced `Absent` remains where real sidecar material should be loaded.
* selector precedence:

  * default;
  * CLI;
  * env;
  * CLI-over-env;
  * invalid fail-closed.
* no-mutation invariants:

  * validation-only non-mutation;
  * mutating rejection non-mutation.
* MainNet refusal invariants.
* compatibility with:

  * Run 214 governance-execution payload path;
  * Run 210 custody-attestation selector;
  * Run 199 RemoteSigner selector;
  * Run 193 custody selector.

Required documentation:
Add:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_220.md`

Update:

* `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
* `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
* `docs/whitepaper/contradiction.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

Documentation must state:

* Run 220 is source/test long-running governance-execution runtime consumption wiring.
* Runtime call sites now consume the selected governance-execution policy where representable.
* Runtime call sites now consume real governance-execution sidecar status where representable.
* Default remains `GovernanceExecutionPolicy::Disabled`.
* Fixture governance execution remains DevNet/TestNet source/test only.
* Emergency council fixture execution is explicit and non-production.
* Production/on-chain/MainNet governance execution remains unavailable/fail-closed.
* Live inbound `0x05` and peer-driven drain limitations must be described honestly if still not fully representable.
* MainNet peer-driven apply remains refused.
* No real governance execution engine is implemented.
* No real on-chain governance proof verifier is implemented.
* No real KMS/HSM backend is implemented.
* No real RemoteSigner backend is implemented.
* Validator-set rotation remains unsupported.
* Release-binary runtime-consumption evidence is deferred to Run 221.
* Full C4 remains OPEN.
* C5 remains OPEN.

Validation commands:
Run at minimum:

* `cargo build -p qbind-node --lib`
* `cargo build -p qbind-node --bin qbind-node`
* `cargo test -p qbind-node --test run_220_governance_execution_runtime_consumption_tests`
* `cargo test -p qbind-node --test run_217_governance_execution_runtime_arming_tests`
* `cargo test -p qbind-node --test run_215_governance_execution_policy_selector_tests`
* `cargo test -p qbind-node --test run_213_governance_execution_payload_callsite_tests`
* `cargo test -p qbind-node --test run_211_governance_execution_policy_tests`
* `cargo test -p qbind-node --test run_209_custody_attestation_policy_selector_tests`
* `cargo test -p qbind-node --test run_207_custody_attestation_payload_callsite_tests`
* `cargo test -p qbind-node --test run_205_custody_attestation_verifier_tests`
* `cargo test -p qbind-node --test run_203_kms_hsm_backend_boundary_tests`
* `cargo test -p qbind-node --test run_201_remote_signer_transport_boundary_tests`
* `cargo test -p qbind-node --test run_198_remote_signer_policy_selector_tests`
* `cargo test -p qbind-node --test run_196_remote_signer_payload_callsite_tests`
* `cargo test -p qbind-node --test run_194_remote_authority_signer_boundary_tests`
* `cargo test -p qbind-node --test run_192_authority_custody_policy_selector_tests`
* `cargo test -p qbind-node --test run_190_authority_custody_payload_callsite_tests`
* `cargo test -p qbind-node --test run_188_authority_custody_boundary_tests`
* `cargo test -p qbind-node --test run_186_onchain_governance_production_verifier_boundary_tests`
* `cargo test -p qbind-node --test run_184_onchain_governance_payload_carrying_tests`
* `cargo test -p qbind-node --test run_182_onchain_governance_production_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_180_onchain_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_178_onchain_governance_proof_tests`
* `cargo test -p qbind-node --test run_176_live_0x05_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_173_validation_only_governance_required_policy_tests`
* `cargo test -p qbind-node --test run_171_governance_required_policy_selector_tests`
* `cargo test -p qbind-node --test run_169_governance_proof_loader_surface_integration_tests`
* `cargo test -p qbind-node --test run_167_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_165_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_163_governance_authority_verifier_tests`
* `cargo test -p qbind-node --test run_161_lifecycle_marker_integration_tests`
* `cargo test -p qbind-node --test run_159_authority_signing_key_lifecycle_tests`
* `cargo test -p qbind-node --test run_157_unified_testnet_fixture_universe_tests`
* `cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests`
* `cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests`
* `cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests`
* `cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests`
* `cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests`
* `cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Acceptance criteria:
Run 220 is acceptable only if:

1. Runtime paths consume selected governance-execution policy where representable.
2. Runtime paths consume real governance-execution sidecar status where representable.
3. `arm_surface` outcomes are no longer discarded on surfaces that claim consumption.
4. Forced `Absent` is removed where real sidecar material should be loaded.
5. Default remains Disabled.
6. Invalid selector values fail closed before mutation.
7. Fixture governance execution passes only where selected runtime policy allows.
8. Emergency council fixture execution remains explicit and non-production.
9. Production/on-chain/MainNet governance execution remains fail-closed as unavailable.
10. Validation-only surfaces remain non-mutating.
11. Mutating rejection surfaces produce no mutation.
12. MainNet peer-driven apply remains refused.
13. Docs defer release-binary runtime-consumption evidence to Run 221.
14. No real governance execution / on-chain proof verifier / KMS-HSM backend / RemoteSigner backend / validator-set rotation claim is made.
15. No full C4 or C5 closure is claimed.
