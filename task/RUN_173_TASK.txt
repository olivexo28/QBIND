Task: Run 173 — Wire Required Governance-Proof Policy into Validation-Only Surfaces

Current accepted state:

* Run 167: additive governance-proof carrier landed.
* Run 169: governance-proof loader wired into production v2 marker-decision callers at source/test level.
* Run 171: hidden Required-policy selector landed:

  * CLI: `--p2p-trust-bundle-governance-proof-required`;
  * env: `QBIND_P2P_TRUST_BUNDLE_GOVERNANCE_PROOF_REQUIRED`;
  * default remains `GovernanceProofPolicy::NotRequired`.
* Run 172: release-binary Required-policy evidence landed for mutating surfaces:

  * default NotRequired compatibility proven;
  * Required + valid proof reload-apply accepted;
  * Required + no proof reload-apply rejected with `GovernanceAuthorityRequiredButMissing`;
  * invalid proof variants rejected;
  * MainNet peer-driven apply refused.
* Run 172 limitation:

  * validation-only `--p2p-trust-bundle-reload-check` does not call `governance_proof_policy_from_cli_or_env`;
  * therefore validation-only Required-policy enforcement is not yet release-binary proven;
  * threading Required policy through validation-only is deferred.
* MainNet peer-driven apply remains refused.
* OnChainGovernance remains unsupported/fail-closed.
* governance execution remains unimplemented.
* KMS/HSM remains unimplemented.
* validator-set rotation remains open.
* full C4 remains open.
* C5 remains open.

Objective:
Wire the hidden Required governance-proof policy selector into validation-only v2 surfaces so validation-only checks can enforce `GovernanceProofPolicy::RequiredForLifecycleSensitive` without mutation.

This is source/test integration only. Release-binary validation-only Required-policy evidence is deferred to Run 174.

Strict scope:

* Source/test only.
* No release-binary harness in this run.
* No MainNet peer-driven apply enablement.
* No governance execution engine.
* No on-chain governance implementation.
* No KMS/HSM implementation.
* No validator-set rotation.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* No new proof schema.
* No marker schema change.
* No sequence-file schema change.
* No trust-bundle schema change.
* No peer-candidate envelope schema change.
* Do not weaken Runs 070, 130–172.
* Do not claim full C4 or C5 closure.

Required validation-only surfaces:
Wire policy selection into every validation-only surface that consumes v2 sidecar / marker-decision context:

1. `--p2p-trust-bundle-reload-check`;
2. local peer-candidate-check validation-only, if present;
3. live inbound `0x05` validation-only path, if it has v2 sidecar context;
4. any shared validation-only helper used by those surfaces.

Required behavior:

* Default remains `GovernanceProofPolicy::NotRequired`.
* With no CLI/env selector:

  * old no-proof sidecars remain accepted where they were already accepted;
  * validation-only surfaces remain non-mutating.
* With CLI selector:

  * `--p2p-trust-bundle-governance-proof-required` activates Required policy for validation-only surfaces.
* With env selector:

  * `QBIND_P2P_TRUST_BUNDLE_GOVERNANCE_PROOF_REQUIRED=1|true|yes|on` activates Required policy for validation-only surfaces.
* Required policy:

  * valid proof-carrying sidecar accepted if anti-rollback, lifecycle, and governance checks pass;
  * no-proof sidecar rejected with `GovernanceAuthorityRequiredButMissing`;
  * malformed proof rejected fail-closed;
  * invalid proof rejected fail-closed;
  * no marker write;
  * no sequence write;
  * no live trust swap;
  * no session eviction;
  * no Run 070 call.
* MainNet peer-driven apply remains refused even if validation-only parsing accepts a valid proof.

Implementation target:
Use the existing selector helpers from Run 171:

* `governance_proof_required_env_selector_enabled`;
* `governance_proof_policy_from_selector`;
* `governance_proof_policy_from_cli_or_env`.

Do not create a second selector path.

Use the existing Run 169 governance-proof loader path:

* `load_versioned_ratification_with_governance_proof_from_path`;
* `load_v2_ratification_sidecar_with_governance_proof_from_path`;
* `preflight_v2_marker_decision_with_governance_proof_load`;
* `GovernanceProofLoadStatus`;
* `GovernanceProofContext::Available`.

Required tests:
Add focused tests, preferably:

`crates/qbind-node/tests/run_173_validation_only_governance_required_policy_tests.rs`

Minimum acceptance matrix:

A1. reload-check default NotRequired accepts old no-proof sidecar

* no sequence write;
* no marker write.

A2. reload-check CLI Required accepts valid proof-carrying Rotate sidecar

* proof context becomes Available;
* governance verifier accepts;
* lifecycle accepts;
* no sequence write;
* no marker write.

A3. reload-check env Required accepts valid proof-carrying Rotate sidecar

* same as A2, using env selector.

A4. local peer-candidate-check Required accepts valid proof-carrying sidecar, if surface exists

* validation-only;
* no mutation.

A5. live inbound `0x05` Required accepts valid proof-carrying candidate, if surface can carry proof context

* validation-only;
* no mutation.
* If live `0x05` cannot carry proof context yet, document exact boundary and defer to later run.

A6. false/unset env preserves NotRequired

* unset / false / 0 does not enable Required policy.

Required rejection matrix:

R1. reload-check CLI Required + no-proof sidecar rejected with `GovernanceAuthorityRequiredButMissing`.

R2. reload-check env Required + no-proof sidecar rejected with `GovernanceAuthorityRequiredButMissing`.

R3. reload-check Required + malformed proof rejected.

R4. reload-check Required + invalid issuer signature rejected.

R5. reload-check Required + wrong environment proof rejected.

R6. reload-check Required + wrong chain proof rejected.

R7. reload-check Required + wrong genesis proof rejected.

R8. reload-check Required + wrong authority root proof rejected.

R9. reload-check Required + wrong lifecycle action proof rejected.

R10. reload-check Required + wrong candidate digest proof rejected.

R11. reload-check Required + wrong authority-domain sequence proof rejected.

R12. reload-check Required + OnChainGovernance proof rejected as unsupported.

R13. reload-check Required + local operator config proof rejected.

R14. reload-check Required + peer-majority proof rejected.

R15. validation-only Required rejection writes no marker and no sequence.

R16. validation-only Required rejection performs no live trust swap, no session eviction, and no Run 070 call.

R17. selector cannot be accidentally enabled by unrelated flags.

R18. MainNet peer-driven apply remains refused even if validation-only Required proof check accepts.

Required source-reachability proof:
Tests and docs must show:

* reload-check now calls `governance_proof_policy_from_cli_or_env` or receives its result;
* Required policy reaches `preflight_v2_marker_decision_with_governance_proof_load`;
* no-proof sidecars under Required produce `GovernanceAuthorityRequiredButMissing`;
* valid proof-carrying sidecars under Required produce `GovernanceProofContext::Available`.

Required backwards compatibility:
All prior tests must remain green:

* Run 172 assumptions remain valid.
* Run 171 selector tests.
* Run 169 loader surface tests.
* Run 167 carrier tests.
* Run 165 governance marker tests.
* Run 163 governance verifier tests.
* Run 161 lifecycle marker tests.
* Run 159 lifecycle tests.
* Run 134/138/142/148/150/152 regression suites.

Required documentation:
Add:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_173.md`

Update:

* `docs/whitepaper/contradiction.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

Documentation must state:

* Run 173 is source/test validation-only Required-policy wiring.
* Validation-only reload-check now consumes the hidden Required-policy selector.
* Default remains NotRequired.
* Required policy can accept valid proof-carrying sidecars and reject missing/invalid proofs in validation-only mode.
* Validation-only surfaces remain non-mutating.
* Release-binary validation-only Required-policy evidence is deferred to Run 174.
* MainNet peer-driven apply remains refused.
* OnChainGovernance remains unsupported/fail-closed.
* governance execution remains unimplemented.
* KMS/HSM remains unimplemented.
* validator-set rotation remains open.
* full C4 remains open.
* C5 remains open.

Validation commands:
Run at minimum:

* `cargo build -p qbind-node --lib`
* `cargo test -p qbind-node --test run_173_validation_only_governance_required_policy_tests`
* `cargo test -p qbind-node --test run_171_governance_required_policy_selector_tests`
* `cargo test -p qbind-node --test run_169_governance_proof_loader_surface_integration_tests`
* `cargo test -p qbind-node --test run_167_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_165_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_163_governance_authority_verifier_tests`
* `cargo test -p qbind-node --test run_161_lifecycle_marker_integration_tests`
* `cargo test -p qbind-node --test run_159_authority_signing_key_lifecycle_tests`
* `cargo test -p qbind-node --test run_157_unified_testnet_fixture_universe_tests`
* `cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests`
* `cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests`
* `cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests`
* `cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests`
* `cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests`
* `cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Acceptance criteria:
Run 173 is acceptable only if:

1. Validation-only reload-check consumes the hidden Required-policy selector.
2. Default NotRequired compatibility remains intact.
3. Required validation-only checks accept valid proof-carrying sidecars.
4. Required validation-only checks reject no-proof and invalid-proof sidecars fail-closed.
5. Validation-only surfaces remain strictly non-mutating.
6. MainNet peer-driven apply remains refused.
7. Docs defer release-binary validation-only Required-policy evidence to Run 174.
8. No full C4 or C5 closure is claimed.
