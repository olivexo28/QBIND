Task: Run 209 — Source/Test Custody-Attestation Policy Selector and Production Preflight Integration

Current accepted state:

* Run 205: source/test production custody-attestation verifier skeleton landed.
* Run 206: release-binary custody-attestation verifier-boundary evidence fully closed.
* Run 207: source/test custody-attestation payload/carrying and production-context wiring landed.
* Run 208: release-binary custody-attestation payload/carrying evidence fully closed:

  * real `target/release/qbind-node` preserves default `CustodyAttestationPolicy::Disabled`;
  * legacy/no-attestation payloads remain compatible;
  * fixture attestation reaches production-context paths in release mode through helper/library paths;
  * production/cloud-KMS/PKCS#11/HSM/RemoteSigner attestation reaches verifier and fails closed as unavailable;
  * malformed/invalid custody-attestation material fails closed;
  * custody-attestation digest preservation through wire conversion is proven;
  * rejected custody-attestation-payload cases produce no mutation;
  * MainNet peer-driven apply remains refused;
  * no real KMS/HSM attestation verifier, RemoteSigner backend, governance execution, on-chain proof verifier, or validator-set rotation exists.
* Current limitation:
  custody-attestation payloads can be carried and validated through production-context helpers, but no hidden production preflight selector exists to choose `CustodyAttestationPolicy::{FixtureAttestationAllowed, RemoteSignerAttestationRequired, KmsAttestationRequired, HsmAttestationRequired, ProductionAttestationRequired, MainnetProductionAttestationRequired}` at binary/source call sites.
* Full C4 remains OPEN.
* C5 remains OPEN.

Objective:
Add a source/test-only hidden custody-attestation policy selector and wire it into production v2 marker-decision preflight contexts.

This run must allow DevNet/TestNet evidence surfaces to explicitly require fixture custody-attestation material, while preserving `CustodyAttestationPolicy::Disabled` by default and preserving fail-closed production/MainNet attestation behavior.

Release-binary custody-attestation policy selector evidence is deferred to Run 210.

Strict scope:

* Source/test only.
* No release-binary harness in this run.
* Hidden selector only.
* Disabled by default.
* No real cloud-KMS attestation verifier.
* No real PKCS#11 attestation verifier.
* No real HSM-vendor attestation verifier.
* No real KMS implementation.
* No real HSM implementation.
* No real RemoteSigner backend.
* No networked signer daemon.
* No production signing key custody.
* No MainNet peer-driven apply enablement.
* No governance execution engine.
* No real on-chain proof verifier.
* No validator-set rotation.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* No schema/wire changes.
* No authority-marker schema change.
* No sequence-file schema change.
* No trust-bundle core schema change.
* No authority lifecycle semantics change.
* Do not weaken Runs 070, 130–208.
* Do not claim full C4 or C5 closure.

Required selector design:
Add a hidden custody-attestation policy selector. Prefer one hidden CLI flag plus one env var.

CLI:

* `--p2p-trust-bundle-custody-attestation-policy <disabled|fixture-attestation-allowed|remote-signer-attestation-required|kms-attestation-required|hsm-attestation-required|production-attestation-required|mainnet-production-attestation-required>`
* hidden via clap.

Env:

* `QBIND_P2P_TRUST_BUNDLE_CUSTODY_ATTESTATION_POLICY=disabled|fixture-attestation-allowed|remote-signer-attestation-required|kms-attestation-required|hsm-attestation-required|production-attestation-required|mainnet-production-attestation-required`

Required default:

* unset CLI/env => `CustodyAttestationPolicy::Disabled`.

Required policy constraints:

* `FixtureAttestationAllowed`:

  * DevNet/TestNet evidence only.
  * must not satisfy MainNet production attestation.
* `RemoteSignerAttestationRequired`:

  * must fail closed because no real RemoteSigner attestation verifier exists.
* `KmsAttestationRequired`:

  * must fail closed because no real KMS attestation verifier exists.
* `HsmAttestationRequired`:

  * must fail closed because no real HSM attestation verifier exists.
* `ProductionAttestationRequired`:

  * must fail closed because no real production attestation verifier exists.
* `MainnetProductionAttestationRequired`:

  * must fail closed.
  * MainNet peer-driven apply remains refused regardless.
* invalid selector values:

  * fail closed or reject startup/preflight with typed parse error.
* unrelated flags/env vars must not enable custody-attestation policy.
* if both CLI and env exist, CLI must take precedence unless an existing QBIND selector convention dictates otherwise; document deterministic precedence.

Required source behavior:

1. Add selector helpers, preferably in or near:

   * `pqc_custody_attestation_payload_carrying.rs`;
   * or a new `pqc_custody_attestation_policy_surface.rs`.

2. Helpers must include grep-verifiable names similar to:

   * `custody_attestation_policy_from_selector`;
   * `custody_attestation_policy_from_cli_or_env`;
   * `custody_attestation_policy_env_selector`.

3. Wire selected custody-attestation policy into all seven production v2 marker-decision preflight contexts:

   * reload-check;
   * reload-apply;
   * startup `--p2p-trust-bundle`;
   * SIGHUP;
   * local peer-candidate-check;
   * live inbound `0x05`;
   * peer-driven drain.

4. The selected policy must reach the Run 207 custody-attestation payload/call-site routing layer.

5. Existing no-attestation payloads remain compatible under default Disabled.

6. Under explicit custody-attestation policy:

   * missing attestation material fails closed where attestation is required;
   * valid fixture attestation passes only where policy permits;
   * production/cloud-KMS/PKCS#11/HSM/RemoteSigner attestation material reaches verifier and fails closed as unavailable;
   * malformed material fails closed;
   * validation-only surfaces remain non-mutating;
   * mutating surfaces preserve sequence-before-marker ordering.

Required accepted scenarios:
A1. default selector absent => Disabled; old no-attestation payload accepted where it was accepted before.

A2. CLI `fixture-attestation-allowed` selects `FixtureAttestationAllowed`; DevNet fixture attestation accepted under source/test production-context path.

A3. env `fixture-attestation-allowed` selects `FixtureAttestationAllowed`; TestNet fixture attestation accepted under source/test production-context path.

A4. CLI `remote-signer-attestation-required` selects `RemoteSignerAttestationRequired`; RemoteSigner attestation material reaches verifier and fails closed as unavailable.

A5. CLI `kms-attestation-required` selects `KmsAttestationRequired`; KMS attestation material reaches verifier and fails closed as unavailable.

A6. CLI `hsm-attestation-required` selects `HsmAttestationRequired`; HSM attestation material reaches verifier and fails closed as unavailable.

A7. CLI `production-attestation-required` selects `ProductionAttestationRequired`; production attestation material reaches verifier and fails closed as unavailable.

A8. env `mainnet-production-attestation-required` selects `MainnetProductionAttestationRequired`; MainNet production attestation unavailable / MainNet refusal outcome reached.

A9. CLI-over-env precedence is deterministic:

* env sets `fixture-attestation-allowed`;
* CLI sets `disabled`;
* resolved policy is Disabled.

A10. no-attestation payload remains compatible under default Disabled.

A11. GenesisBound / EmergencyCouncil / OnChainGovernance proof behavior remains unchanged when custody-attestation policy is Disabled.

A12. Run 193 custody-policy selector behavior remains compatible.

A13. Run 199 RemoteSigner-policy selector behavior remains compatible.

A14. mutating DevNet fixture attestation under FixtureAttestationAllowed preflight accepts where lifecycle/governance/custody/attestation all pass and sequence-before-marker ordering remains governed by existing apply path.

A15. live inbound `0x05` receives selected policy if live config supports it; if not, document exact limitation.

Required rejection scenarios:
R1. invalid CLI selector value rejected with typed parse error.

R2. invalid env selector value rejected with typed parse error.

R3. unrelated CLI/env does not enable custody-attestation policy.

R4. no-attestation payload rejected under FixtureAttestationAllowed.

R5. no-attestation payload rejected under ProductionAttestationRequired.

R6. fixture attestation rejected under ProductionAttestationRequired.

R7. fixture attestation rejected under MainnetProductionAttestationRequired.

R8. RemoteSigner attestation rejected as unavailable.

R9. KMS attestation rejected as unavailable.

R10. HSM attestation rejected as unavailable.

R11. cloud-KMS attestation rejected as unavailable.

R12. PKCS#11 HSM attestation rejected as unavailable.

R13. production attestation rejected as unavailable.

R14. MainNet production attestation rejected as unavailable.

R15. malformed custody-attestation material rejected.

R16. wrong environment rejected.

R17. wrong chain rejected.

R18. wrong genesis rejected.

R19. wrong authority root rejected.

R20. wrong signing-key fingerprint rejected.

R21. wrong custody class rejected.

R22. wrong backend/provider/signer id rejected.

R23. wrong key id / key label rejected.

R24. wrong suite rejected.

R25. wrong lifecycle action rejected.

R26. wrong candidate digest rejected.

R27. wrong authority-domain sequence rejected.

R28. wrong governance proof digest rejected.

R29. wrong request digest rejected.

R30. wrong response digest rejected.

R31. wrong transcript digest rejected.

R32. stale/replayed attestation rejected.

R33. expired attestation rejected.

R34. invalid attestation commitment rejected.

R35. local operator cannot satisfy production attestation.

R36. peer majority / gossip count cannot satisfy production attestation.

R37. validation-only rejection writes no marker and no sequence.

R38. mutating rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

R39. invalid live inbound `0x05` custody-attestation candidate is not propagated, staged, or applied.

R40. MainNet peer-driven apply remains refused even with MainnetProductionAttestationRequired and fixture attestation.

Required tests:
Add focused tests, preferably:

`crates/qbind-node/tests/run_209_custody_attestation_policy_selector_tests.rs`

Minimum coverage:

* A1–A15 where representable.
* R1–R40.
* selector parsing and precedence:

  * default;
  * CLI;
  * env;
  * CLI-over-env;
  * invalid value fail-closed.
* source reachability:

  * selected policy reaches all seven production-context helpers where representable.
* no-mutation invariants:

  * validation-only non-mutation;
  * mutating rejection non-mutation.
* MainNet refusal invariants.

Required documentation:
Add:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_209.md`

Update:

* `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
* `docs/whitepaper/contradiction.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

Documentation must state:

* Run 209 is source/test hidden custody-attestation policy selector and production preflight integration.
* Default remains `CustodyAttestationPolicy::Disabled`.
* Fixture attestation remains DevNet/TestNet evidence-only.
* Fixture attestation cannot satisfy MainNet production attestation.
* Production/cloud-KMS/PKCS#11/HSM/RemoteSigner attestation remains unavailable/fail-closed.
* MainNet peer-driven apply remains refused.
* No real cloud-KMS attestation verifier is implemented.
* No real PKCS#11 attestation verifier is implemented.
* No real HSM-vendor attestation verifier is implemented.
* No real KMS/HSM backend is implemented.
* No real RemoteSigner backend is implemented.
* governance execution remains unimplemented.
* real on-chain proof verification remains unimplemented.
* validator-set rotation remains open.
* release-binary custody-attestation policy selector evidence is deferred to Run 210.
* full C4 remains open.
* C5 remains open.

Validation commands:
Run at minimum:

* `cargo build -p qbind-node --lib`
* `cargo test -p qbind-node --test run_209_custody_attestation_policy_selector_tests`
* `cargo test -p qbind-node --test run_207_custody_attestation_payload_callsite_tests`
* `cargo test -p qbind-node --test run_205_custody_attestation_verifier_tests`
* `cargo test -p qbind-node --test run_203_kms_hsm_backend_boundary_tests`
* `cargo test -p qbind-node --test run_201_remote_signer_transport_boundary_tests`
* `cargo test -p qbind-node --test run_198_remote_signer_policy_selector_tests`
* `cargo test -p qbind-node --test run_196_remote_signer_payload_callsite_tests`
* `cargo test -p qbind-node --test run_194_remote_authority_signer_boundary_tests`
* `cargo test -p qbind-node --test run_192_authority_custody_policy_selector_tests`
* `cargo test -p qbind-node --test run_190_authority_custody_payload_callsite_tests`
* `cargo test -p qbind-node --test run_188_authority_custody_boundary_tests`
* `cargo test -p qbind-node --test run_186_onchain_governance_production_verifier_boundary_tests`
* `cargo test -p qbind-node --test run_184_onchain_governance_payload_carrying_tests`
* `cargo test -p qbind-node --test run_182_onchain_governance_production_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_180_onchain_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_178_onchain_governance_proof_tests`
* `cargo test -p qbind-node --test run_176_live_0x05_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_173_validation_only_governance_required_policy_tests`
* `cargo test -p qbind-node --test run_171_governance_required_policy_selector_tests`
* `cargo test -p qbind-node --test run_169_governance_proof_loader_surface_integration_tests`
* `cargo test -p qbind-node --test run_167_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_165_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_163_governance_authority_verifier_tests`
* `cargo test -p qbind-node --test run_161_lifecycle_marker_integration_tests`
* `cargo test -p qbind-node --test run_159_authority_signing_key_lifecycle_tests`
* `cargo test -p qbind-node --test run_157_unified_testnet_fixture_universe_tests`
* `cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests`
* `cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests`
* `cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests`
* `cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests`
* `cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests`
* `cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Acceptance criteria:
Run 209 is acceptable only if:

1. A hidden custody-attestation policy selector exists.
2. Default remains Disabled.
3. Selector reaches production preflight contexts at source/test level.
4. Fixture attestation passes only where selected policy allows.
5. Missing/malformed/invalid custody-attestation material fails closed under explicit policy.
6. Production/cloud-KMS/PKCS#11/HSM/RemoteSigner attestation remains fail-closed as unavailable.
7. Validation-only surfaces remain non-mutating.
8. Mutating rejection surfaces produce no mutation.
9. MainNet peer-driven apply remains refused even with MainnetProductionAttestationRequired and fixture attestation.
10. Docs defer release-binary custody-attestation policy selector evidence to Run 210.
11. No real KMS/HSM attestation / RemoteSigner backend / governance execution / real on-chain proof / validator-set rotation claim is made.
12. No full C4 or C5 closure is claimed.
