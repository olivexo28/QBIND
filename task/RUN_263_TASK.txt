Run 263 — Release-binary durable-completion acknowledgement consumer / post-acknowledgement settlement interface evidence

You are continuing QBIND after Run 262.

Current accepted state:

Run 260 closed the source/test durable-completion audit-receipt acknowledgement / external-publication confirmation boundary.

Run 261 closed release-binary evidence for Run 260.

Run 262 closed the source/test durable-completion acknowledgement consumer / post-acknowledgement settlement interface boundary. Run 262 added:

* `crates/qbind-node/src/pqc_governance_durable_completion_acknowledgement_consumer.rs`
* `crates/qbind-node/tests/run_262_durable_completion_acknowledgement_consumer_tests.rs`
* `crates/qbind-node/src/lib.rs` registration
* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_262.md`
* narrow protocol/ops doc updates
* `docs/whitepaper/contradiction.md` no-contradiction entry

Run 262 is source/test only. It has no release-binary evidence yet.

Strict workflow:

* Execution-first.
* Narrow scope only.
* No broad redesign.
* No silent regressions.
* No hype or overclaiming.
* Release-binary evidence only.
* No new source/test feature boundary.
* No production runtime enablement.
* No real settlement backend.
* No real audit-ledger acknowledgement backend.
* No real external-publication confirmation backend.
* No real external publication.
* No real production attestation backend.
* No real finalization backend.
* No real completion-report backend.
* No real durable consume backend.
* No real persistent replay backend.
* No real governance execution engine.
* No real production mutation engine.
* No real on-chain governance proof verifier.
* No KMS/HSM/RemoteSigner backend.
* No RocksDB/file/schema/migration/storage-format change.
* No wire/schema/marker/sequence/trust-bundle change.
* No Run 070 call.
* No `LivePqcTrustState` mutation.
* No real trust swap.
* No session eviction.
* No sequence write.
* No marker write.
* No network I/O or external settlement.
* No MainNet governance.
* No MainNet peer-driven apply.
* No validator-set rotation.
* Keep Full C4 OPEN.
* Keep C5 OPEN.

Objective:

Produce release-binary evidence for the Run 262 durable-completion acknowledgement consumer / post-acknowledgement settlement interface boundary, mirroring the Run 261 → Run 260 release-binary evidence pattern.

Run 263 must prove on real `target/release/qbind-node` plus a release-built helper that the Run 262 consumer boundary symbols are present in release-built code, that the helper exercises the Run 262 production library symbols in release mode, and that the consumer / settlement path is evidenced only as a fixture/in-memory/dev-test boundary — not production-enabled.

Required deliverables:

1. Add release helper:

   `crates/qbind-node/examples/run_263_durable_completion_acknowledgement_consumer_release_binary_helper.rs`

2. Add release harness:

   `scripts/devnet/run_263_durable_completion_acknowledgement_consumer_release_binary.sh`

3. Add evidence archive directory:

   `docs/devnet/run_263_durable_completion_acknowledgement_consumer_release_binary/`

   Track only:

   * `README.md`
   * `summary.txt`
   * `.gitignore`

   Generated logs, helper tables, reachability files, provenance files, and test logs must be regenerated and ignored.

4. Add canonical evidence report:

   `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_263.md`

5. Update narrowly:

   * `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
   * `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
   * `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
   * `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
   * `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
   * `docs/whitepaper/contradiction.md`

Required existing ordering:

1. Run 246 pipeline success is required before any Run 248 sink intent can exist.
2. Run 248 `ConsumeReceiptRecorded` is required before any Run 250 completion-report intent can exist.
3. Run 250 `CompletionReportRecorded` is required before any Run 252 finalization intent can exist.
4. Run 252 `DurableCompletionFinalized` is required before any Run 254 attestation intent can exist.
5. Run 254 `DurableCompletionAttested` is required before any Run 256 backend request can exist.
6. Run 256 `BackendSubmissionRecorded` is required before any Run 258 audit/publication receipt request can exist.
7. Run 258 `AuditReceiptRecorded` is required before any Run 260 acknowledgement / confirmation request can exist.
8. Run 260 `AcknowledgementRecorded` is required before any Run 262 acknowledgement-consumer / settlement request can exist.
9. Only Run 262 `AcknowledgementConsumed` authorizes a new modeled consumer state.

Helper requirements:

The release helper must link against release-built production library symbols and exercise the actual Run 262 API, including the exact implemented names for:

* `pqc_governance_durable_completion_acknowledgement_consumer`
* `DurableCompletionAcknowledgementConsumerInput`
* `DurableCompletionAcknowledgementConsumerPolicy`
* `DurableCompletionAcknowledgementConsumerKind`
* `DurableCompletionAcknowledgementConsumerIdentity`
* `DurableCompletionAcknowledgementConsumerExpectations`
* `DurableCompletionAcknowledgementConsumerRequest`
* `DurableCompletionAcknowledgementConsumerResponse`
* `DurableCompletionAcknowledgementConsumerRecord`
* `DurableCompletionAcknowledgementConsumerLedger`
* `DurableCompletionAcknowledgementConsumerLedgerRecord`
* `DurableCompletionAcknowledgementConsumerDigest`
* `DurableCompletionAcknowledgementConsumerTranscriptDigest`
* `DurableCompletionAcknowledgementConsumerOutcome`
* `GovernanceDurableCompletionAcknowledgementConsumer`
* `FixtureDurableCompletionAcknowledgementConsumer`
* `ProductionDurableCompletionSettlementConsumer`
* `MainNetDurableCompletionSettlementConsumer`
* `ExternalDurableCompletionSettlementConsumer`
* `project_acknowledgement_outcome_to_consumer_request`
* `evaluate_durable_completion_acknowledgement_consumer`
* `recover_durable_completion_acknowledgement_consumer_window`
* all Run 262 grep-verifiable invariant helpers.

If exact symbol names differ, use the actual implemented Run 262 API names and record the substitutions honestly in `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_263.md`. Do not add compatibility shims unless strictly necessary for compilation, and do not change production behavior.

Helper architecture:

Copy the Run 261 release helper structure and adapt it to Run 262.

The helper must attach to the full real modeled chain:

* Run 256 backend submission
* Run 258 audit receipt
* Run 260 acknowledgement
* Run 262 consumer

Do not fabricate a standalone consumer request.

Add or copy attachment helpers from the Run 262 tests:

* attach Run 256 backend submission
* attach Run 258 receipt
* attach Run 260 acknowledgement
* create Run 262 consumer input from the actual Run 260 acknowledgement outcome and digest binding

Critical projection rule:

The Run 263 helper must drive projection from the Run 260 acknowledgement outcome:

* `input.acknowledgement_binding`
* `project_acknowledgement_outcome_to_consumer_request`

Do NOT project from Run 258 receipt or Run 256 backend outcome directly.

Required projection mapping:

* Run 260 `AcknowledgementRecorded` → creates consumer request.
* Run 260 `AcknowledgementDuplicateIdempotent` → idempotent-only, never creates a new consumer record by itself.
* Run 260 legacy/no-acknowledgement outcomes → legacy/no-consumer.
* Run 260 rejected-before-audit-receipt / acknowledgement-did-not-record / rejected-before-record / record failure / rollback / rollback-failed / ambiguous window outcomes → no-consumer fail-closed.
* Run 260 production/MainNet/external acknowledgement unavailable outcomes → corresponding no-consumer unavailable/fail-closed.
* Run 260 `MainNetPeerDrivenApplyRefusedNoAcknowledgement` → `MainNetPeerDrivenApplyRefusedNoConsumer`.
* Run 260 `ValidatorSetRotationUnsupportedNoAcknowledgement` → `ValidatorSetRotationUnsupportedNoConsumer`.
* Run 260 `PolicyChangeUnsupportedNoAcknowledgement` → `PolicyChangeUnsupportedNoConsumer`.
* Every other non-recorded acknowledgement outcome → no-consumer fail-closed.

Helper corpus:

The helper must include these tables:

* accepted / compatible
* rejection / fail-closed
* recovery / crash-window
* projection
* stage-ordering
* consumer-ledger
* non-mutation
* reachability

Accepted / compatible cases must prove at minimum:

* Disabled consumer policy preserves legacy no-consumer bypass.
* Disabled acknowledgement policy never invokes consumer.
* Disabled receipt policy never invokes acknowledgement or consumer.
* Disabled backend policy never invokes receipt, acknowledgement, or consumer.
* Disabled attestor/finalizer/reporter/sink/pipeline/evaluator-call-site policies never reach consumer.
* DevNet fixture chain records exactly one modeled in-memory consumer record only after the full Run 246 → 248 → 250 → 252 → 254 → 256 → 258 → 260 → 262 chain.
* TestNet fixture chain records exactly one modeled in-memory consumer record only after the same full chain.
* Add-root / retire-root / revoke-root / emergency-revoke-root / noop modeled action variants preserve ordering and consume only after acknowledgement recorded.
* Duplicate identical consumer record is idempotent.
* Run 260 duplicate-idempotent acknowledgement may only match an already-recorded consumer record and never creates a new consumer record by itself.
* Existing Run 262 source/test behavior remains compatible.
* Existing Run 260 acknowledgement behavior remains compatible.
* Existing Run 258 receipt behavior remains compatible.
* Existing Run 256 backend behavior remains compatible.
* Existing Run 254 attestation behavior remains compatible.
* Existing Run 252 finalization behavior remains compatible.
* Existing Run 250 completion-reporter behavior remains compatible.
* Existing Run 248 sink behavior remains compatible.
* Existing Run 246 pipeline behavior remains compatible.
* Existing Run 244 modeled applier behavior remains compatible.
* Existing Run 242 mutation-engine behavior remains compatible.
* Existing Run 240 durable completion behavior remains compatible.
* Production settlement path is reachable but unavailable/fail-closed and records no consumer state.
* MainNet settlement path is reachable but unavailable/fail-closed and records no consumer state.
* External settlement path is reachable but unavailable/fail-closed and performs no publication/network I/O.
* MainNet peer-driven apply is refused before pipeline progression, sink invocation, reporter invocation, finalizer invocation, attestor invocation, backend invocation, receipt invocation, acknowledgement invocation, and consumer invocation.
* Validator-set rotation unsupported and records no consumer state.
* Policy-change unsupported and records no consumer state.

Rejected / fail-closed cases must prove at minimum:

* Every rejection before Run 262 produces no consumer request and no consumer record.
* Acknowledgement did-not-record / rejected-before-record / record failure / rollback / rollback-failed / ambiguous window produces no consumer invocation and no consumer record.
* Consumer before Run 260 `AcknowledgementRecorded` is rejected.
* Consumer after modeled apply failure, sink failure, reporter failure, finalizer failure, attestor failure, backend failure, receipt failure, acknowledgement failure, acknowledgement rollback, acknowledgement rollback-failed, or acknowledgement ambiguous window is rejected.
* Same consumer record id with different digest fails closed as equivocation.
* Wrong environment / chain / genesis / governance surface / mutation surface fails before consumer invocation.
* Wrong consumer identity / policy / kind / request digest / response digest / record digest fails closed.
* Wrong acknowledgement identity / request / response / record / transcript digest fails closed.
* Wrong receipt identity / request / response / record / transcript digest fails closed.
* Wrong backend identity / request / response / receipt / transcript digest fails closed.
* Wrong attestation / finalization / completion-report / sink / reporter / pipeline digest fails closed.
* Wrong proposal id / decision id / candidate digest / authority-domain sequence fails closed.
* Malformed request / response fails closed.
* Local operator key cannot satisfy MainNet authority.
* Peer majority cannot satisfy MainNet authority.
* Production/MainNet/external settlement unavailable paths record no consumer state and perform no publication.
* Rejected paths produce no Run 070 call, no `LivePqcTrustState` mutation, no trust swap, no session eviction, no sequence write, no marker write, no RocksDB/file/schema/migration/storage-format change, no external publication, no confirmation, no real audit-ledger write, and no real settlement write.

Recovery / crash-window cases must prove at minimum:

* before-pipeline through after-acknowledgement-success-before-consumer-request windows fail closed unless explicit matching acknowledgement success exists.
* after-consumer-request-before-consumer-record fails closed.
* after-consumer-record-before-consumer-success fails closed unless explicit matching consumer record exists.
* after-consumer-success recovers as consumed.
* ambiguous, record-failed, rollback-completed, rollback-failed, unknown, production/MainNet/external settlement windows fail closed.
* MainNet peer-driven apply refusal precedes recovery classification where applicable.

Projection cases must prove at minimum:

* Only Run 260 `AcknowledgementRecorded` creates a consumer request.
* Run 246 pipeline success alone creates no consumer request.
* Run 248 sink receipt alone creates no consumer request.
* Run 250 completion report alone creates no consumer request.
* Run 252 finalization alone creates no consumer request.
* Run 254 attestation alone creates no consumer request.
* Run 256 backend submission alone creates no consumer request.
* Run 258 audit receipt alone creates no consumer request.
* Run 260 acknowledgement request alone creates no consumer request.
* Run 260 duplicate-idempotent acknowledgement creates no new consumer record unless a matching consumer record already exists.
* Only `AcknowledgementConsumed` authorizes modeled consumer state.
* Every other outcome does not record.

Stage-ordering cases must prove at minimum:

* MainNet peer-driven apply refusal happens before every later stage.
* Evaluator/call-site authorization precedes durable replay.
* Durable replay precedes mutation engine.
* Mutation engine precedes modeled applier.
* Modeled applier precedes sink intent.
* Sink receipt precedes completion report.
* Completion report precedes finalization.
* Finalization precedes attestation.
* Attestation precedes backend request.
* Backend submission precedes audit receipt request.
* Audit receipt precedes acknowledgement request.
* Acknowledgement record precedes consumer request.
* Consumer record happens only after acknowledgement recorded state.
* Rejection before consumer stage leaves consumer invocation count zero.
* Consumer record failure does not invalidate acknowledgement but does not authorize consumer state.
* Consumer rollback failure is fatal/fail-closed and does not authorize consumer state.

Consumer-ledger cases must prove at minimum:

* One valid consumer record inserts one in-memory record.
* Duplicate identical consumer record is idempotent and does not increase count.
* Same record id with different digest is equivocation and does not increase count.
* Wrong consumer / acknowledgement / receipt / backend / attestation / finalization / report / sink / pipeline / proposal / decision / candidate / sequence digest does not record.
* Malformed request / response does not record.
* Rollback restores in-memory ledger snapshot.
* Rollback failure never claims success.
* Fixture ledger never writes files, RocksDB, markers, sequences, external publications, confirmations, audit-ledger entries, settlement records, or production durable state.

Non-mutation cases must prove at minimum:

* no Run 070 call;
* no `LivePqcTrustState` mutation;
* no trust swap;
* no session eviction;
* no sequence write;
* no marker write;
* no RocksDB/file/schema/migration/storage-format write;
* no wire/schema/marker/sequence/trust-bundle change;
* no external network publication;
* no real external settlement;
* no real external-publication confirmation;
* no real audit-ledger write;
* fixture consumer mutates only `DurableCompletionAcknowledgementConsumerLedger`;
* production/MainNet/external paths remain unavailable/fail-closed.

Real release-binary scenarios:

The harness must run the real `target/release/qbind-node` for at least:

* S1 — `--help`: no consumer / settlement / external-publication / confirmation production enablement banner or public flag drift.
* S2 — default DevNet startup parse/smoke: no consumer/settlement/publication/confirmation enablement claim.
* S3 — default TestNet startup parse/smoke: no consumer/settlement/publication/confirmation enablement claim.
* S4 — default MainNet startup parse/smoke: no consumer/settlement/publication/confirmation enablement claim.
* S5 — hidden governance-execution selector or equivalent hidden selector still parses as before, without exposing the Run 262/263 helper-only path as public production enablement.
* S6 — invalid governance-execution selector or equivalent invalid selector fails closed before mutation if applicable, with no marker write, no sequence write, no live trust swap, no session eviction, no Run 070 call, no receipt sink invocation, no acknowledgement sink invocation, no consumer invocation, no audit-ledger write, no external-publication confirmation, no external publication, and no settlement.

Do not add any new public CLI surface for Run 263. If a hidden selector already exists from earlier runs, the harness may use it only as a parse/fail-closed smoke surface.

Denylist:

Prove captured real-binary logs and helper logs do not contain active/enabled claims for:

* real settlement backend enabled;
* durable-completion acknowledgement consumer production enabled;
* MainNet settlement enabled;
* external settlement enabled;
* real audit-ledger acknowledgement backend enabled;
* real external-publication confirmation backend enabled;
* real external publication backend enabled;
* real production attestation backend enabled;
* real finalization backend enabled;
* real completion-report backend enabled;
* real durable consume backend enabled;
* real persistent replay backend enabled;
* real production mutation engine enabled;
* MainNet mutation engine enabled;
* MainNet governance enabled;
* MainNet peer-driven apply enabled;
* real governance execution engine enabled;
* real on-chain governance proof verifier enabled;
* RocksDB replay backend enabled;
* file replay backend enabled;
* schema migration enabled;
* storage-format migration enabled;
* KMS/HSM backend enabled;
* RemoteSigner backend enabled;
* validator-set rotation enabled;
* policy-change action enabled;
* autonomous apply;
* apply-on-receipt;
* apply-on-acknowledgement;
* apply-on-consumer;
* apply-on-settlement;
* peer-majority authority;
* Run 070 apply from the consumer boundary;
* `LivePqcTrustState` mutation from the consumer boundary;
* real trust swap from the consumer boundary;
* session eviction from the consumer boundary;
* marker write from the consumer boundary;
* sequence write from the consumer boundary;
* RocksDB write from the consumer boundary;
* file write from the consumer boundary;
* external publication by the fixture consumer;
* external-publication confirmation by the fixture consumer;
* audit-ledger write by the fixture consumer;
* settlement write by the fixture consumer;
* production durable consume by the fixture consumer;
* production finalization by the fixture consumer;
* production attestation by the fixture consumer;
* production backend submission by the fixture consumer;
* production audit receipt by the fixture consumer;
* production acknowledgement by the fixture consumer;
* DummySig / DummyKem / DummyAead active on production path.

Source/release reachability proof:

The harness must grep production source for Run 262 symbols and prove the release helper exercises them in release mode.

Required greps include:

* `pqc_governance_durable_completion_acknowledgement_consumer`
* `DurableCompletionAcknowledgementConsumerInput`
* `DurableCompletionAcknowledgementConsumerPolicy`
* `DurableCompletionAcknowledgementConsumerKind`
* `DurableCompletionAcknowledgementConsumerIdentity`
* `DurableCompletionAcknowledgementConsumerExpectations`
* `DurableCompletionAcknowledgementConsumerRequest`
* `DurableCompletionAcknowledgementConsumerResponse`
* `DurableCompletionAcknowledgementConsumerRecord`
* `DurableCompletionAcknowledgementConsumerLedger`
* `DurableCompletionAcknowledgementConsumerLedgerRecord`
* `DurableCompletionAcknowledgementConsumerDigest`
* `DurableCompletionAcknowledgementConsumerTranscriptDigest`
* `DurableCompletionAcknowledgementConsumerOutcome`
* `GovernanceDurableCompletionAcknowledgementConsumer`
* `FixtureDurableCompletionAcknowledgementConsumer`
* `ProductionDurableCompletionSettlementConsumer`
* `MainNetDurableCompletionSettlementConsumer`
* `ExternalDurableCompletionSettlementConsumer`
* `project_acknowledgement_outcome_to_consumer_request`
* `evaluate_durable_completion_acknowledgement_consumer`
* `recover_durable_completion_acknowledgement_consumer_window`
* `AcknowledgementConsumed`
* `AcknowledgementConsumerDuplicateIdempotent`
* `AcknowledgementConsumerRejectedBeforeRecord`
* `AcknowledgementConsumerRecordFailedNoConsumer`
* `AcknowledgementConsumerRolledBackNoConsumer`
* `AcknowledgementConsumerRollbackFailedFatalNoConsumer`
* `AcknowledgementConsumerAmbiguousFailClosedNoConsumer`
* `ProductionSettlementUnavailableNoConsumer`
* `MainNetSettlementUnavailableNoConsumer`
* `ExternalSettlementUnavailableNoConsumer`
* `MainNetPeerDrivenApplyRefusedNoConsumer`
* `ValidatorSetRotationUnsupportedNoConsumer`
* `PolicyChangeUnsupportedNoConsumer`
* all Run 262 invariant helper names.

If exact names differ, use the exact implemented Run 262 names and record differences honestly in `QBIND_DEVNET_EVIDENCE_RUN_263.md`.

Validation commands:

Run at minimum:

* `cargo build -p qbind-node --release --bin qbind-node`
* `cargo build -p qbind-node --release --example run_263_durable_completion_acknowledgement_consumer_release_binary_helper`
* `bash scripts/devnet/run_263_durable_completion_acknowledgement_consumer_release_binary.sh`
* `cargo test -p qbind-node --test run_262_durable_completion_acknowledgement_consumer_tests`
* `cargo test -p qbind-node --test run_260_durable_completion_audit_receipt_acknowledgement_tests`
* `cargo test -p qbind-node --test run_258_durable_completion_audit_publication_receipt_tests`
* `cargo test -p qbind-node --test run_256_durable_completion_attestation_backend_tests`
* `cargo test -p qbind-node --test run_254_modeled_durable_completion_attestation_projection_tests`
* `cargo test -p qbind-node --test run_252_modeled_durable_completion_finalization_projection_tests`
* `cargo test -p qbind-node --test run_250_modeled_durable_consume_completion_reporter_tests`
* `cargo test -p qbind-node --test run_248_modeled_durable_consume_projection_sink_tests`
* `cargo test -p qbind-node --test run_246_governance_modeled_end_to_end_pipeline_tests`
* `cargo test -p qbind-node --test run_244_modeled_governance_trust_mutation_applier_tests`
* `cargo test -p qbind-node --test run_242_governance_execution_mutation_engine_tests`
* `cargo test -p qbind-node --test run_240_governance_evaluator_replay_durable_runtime_integration_tests`
* `cargo test -p qbind-node --test run_238_governance_evaluator_replay_durable_backend_tests`
* `cargo test -p qbind-node --test run_236_governance_evaluator_replay_consume_runtime_integration_tests`
* `cargo test -p qbind-node --test run_234_governance_evaluator_replay_consume_boundary_tests`
* `cargo test -p qbind-node --test run_232_governance_evaluator_replay_runtime_integration_tests`
* `cargo test -p qbind-node --test run_230_governance_evaluator_replay_state_tests`
* `cargo test -p qbind-node --test run_228_peer_evaluator_context_representation_tests`
* `cargo test -p qbind-node --test run_226_governance_evaluator_runtime_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_224_governance_evaluator_runtime_integration_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

Security scanning:

* Run secret scanning if available and report the result.
* Run CodeQL if available and report the result honestly.
* If Rust CodeQL analysis is skipped because the database is too large, or if the checker classifies the change set as trivial and skips analysis, say that explicitly and do not overclaim CodeQL coverage.

Documentation:

Create:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_263.md`
* `docs/devnet/run_263_durable_completion_acknowledgement_consumer_release_binary/README.md`
* `docs/devnet/run_263_durable_completion_acknowledgement_consumer_release_binary/summary.txt`
* `docs/devnet/run_263_durable_completion_acknowledgement_consumer_release_binary/.gitignore`

Update narrowly:

* `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
* `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
* `docs/whitepaper/contradiction.md`

Docs must state:

* Run 263 is release-binary evidence for Run 262.
* No production mutating behavior is enabled.
* The acknowledgement consumer / post-acknowledgement settlement boundary is release-evidenced, not production-enabled.
* The release helper exercises Run 262 production library symbols in release mode.
* The release helper remains dead code from production runtime.
* The fixture consumer mutates only modeled in-memory `DurableCompletionAcknowledgementConsumerLedger` state.
* Production settlement, MainNet settlement, and external settlement consumers remain reachable but unavailable/fail-closed.
* Run 260 `AcknowledgementRecorded` is required before any consumer / settlement request can exist.
* Only `AcknowledgementConsumed` authorizes modeled consumer state.
* Every non-recorded acknowledgement outcome produces no consumer request and no consumer record.
* Duplicate identical consumer record is idempotent.
* Same consumer record id with different digest fails closed as equivocation.
* Failed consumer record, rollback, rollback-failed, ambiguous windows, unavailable production/MainNet/external settlement paths, rejected replay states, and unsupported actions never record.
* MainNet peer-driven apply remains refused before pipeline progression, sink invocation, reporter invocation, finalizer invocation, attestor invocation, backend invocation, receipt invocation, acknowledgement invocation, and consumer invocation.
* Rejected consumer paths are non-mutating.
* No real settlement is implemented.
* No real audit-ledger acknowledgement is implemented.
* No real external-publication confirmation is implemented.
* No real external publication is implemented.
* No real production attestation backend is implemented.
* No real finalization backend is implemented.
* No real completion-report backend is implemented.
* No real durable consume backend is implemented.
* No real persistent replay backend is implemented.
* No real governance execution engine is implemented.
* No real production mutation engine is implemented.
* No real on-chain governance proof verifier is implemented.
* No RocksDB/file/schema/migration/storage-format change is implemented.
* No wire/schema/marker/sequence/trust-bundle change is implemented.
* No KMS/HSM/RemoteSigner backend is implemented.
* No MainNet governance enablement.
* No MainNet peer-driven apply enablement.
* No validator-set rotation.
* The consumer boundary does not call Run 070.
* The consumer boundary does not mutate `LivePqcTrustState`.
* The consumer boundary does not perform a real trust swap.
* The consumer boundary does not evict sessions.
* The consumer boundary does not write sequence files.
* The consumer boundary does not write authority markers.
* The consumer boundary does not perform external publication.
* The consumer boundary does not perform real external-publication confirmation.
* The consumer boundary does not write a real audit ledger.
* The consumer boundary does not perform real settlement.
* Full C4 remains OPEN.
* C5 remains OPEN.

Contradiction handling:

* Crosscheck Run 263 against Runs 050–262.
* If no contradiction is found, add a “no Run 263 contradiction” entry to `docs/whitepaper/contradiction.md`.
* If contradiction is found, record it explicitly with exact file/function/doc reference and recommend the smallest safe fix.

Expected verdict:

Positive release-binary evidence only if:

* release helper builds;
* release harness exits 0;
* S1–S6 pass;
* denylist empty;
* regression corpus passes;
* docs/evidence are updated;
* contradiction entry is present;
* scans are reported honestly.

Run 263 closes only the Run 262 release-binary evidence gap. It must not claim Full C4 or C5 closure.

Final response format:

1. Exact verdict.
2. Files changed.
3. Release artifacts and hashes.
4. Helper corpus results.
5. Real-binary scenarios.
6. Tests run and results.
7. Security invariants preserved.
8. Honest limitations.
9. C4/C5 status.
10. Suggested Run 264 next step.
