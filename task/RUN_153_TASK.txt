Task: Run 153 — Release-Binary End-to-End Peer-Driven Apply Evidence

Current accepted state:
- Run 144: peer-driven live apply safety gate specification landed.
- Run 145: non-applying staged peer-candidate queue scaffold landed.
- Run 146: staging queue wired into live inbound 0x05 path.
- Run 147: release-binary evidence for staged peer-candidate queue / arming behavior landed.
- Run 148: source/test peer-driven apply controller using existing Run 070 apply contract landed.
- Run 149: release-binary arming evidence for DevNet/TestNet peer-driven apply surface landed.
- Run 150: source/test explicit drain trigger from staged queue into Run 148 controller landed.
- Run 151: partial-positive release-binary trigger-surface arming landed via hidden `--p2p-trust-bundle-peer-candidate-drain-once`.
- Run 152: source/test binary-reachable drain invocation plumbing landed:
  - `ProductionDrainInvocationBuilder`;
  - `ProductionV2MarkerCoordinator`;
  - shared `Arc<parking_lot::Mutex<PeerCandidateStagingQueue>>`;
  - `try_drain_once_shared`;
  - release binary observably links the full pipeline;
  - release-binary end-to-end apply evidence explicitly deferred to Run 153.
- MainNet remains refused.
- Governance, KMS/HSM, signing-key rotation/revocation lifecycle, validator-set rotation, full C4, and C5 remain open.

Objective:
Produce release-binary evidence that a real `target/release/qbind-node` DevNet/TestNet process can receive a live peer-candidate `0x05` v2 trust-bundle candidate, validate it, stage it, explicitly drain it once, and apply it through:

  live inbound 0x05 candidate
    → validation-only v2 acceptance
    → staging queue
    → hidden explicit drain-once hook
    → ProductionDrainInvocationBuilder
    → ProductionV2MarkerCoordinator
    → Run 150 PeerDrivenApplyDrain::try_drain_once
    → Run 148 try_apply_staged_peer_candidate
    → Run 070 apply_validated_candidate_with_previous
    → LivePqcTrustState swap
    → session eviction
    → Run 055 sequence commit
    → v2 authority marker persist after commit

This is release-binary evidence. Source changes are allowed only if required to make the already-landed Run 152 binary-reachable plumbing actually callable by the Run 151 hidden hook. If any code change is needed, keep it minimal, hidden, disabled-by-default, DevNet/TestNet-only, and MainNet-refused.

Strict scope:
- Release-binary end-to-end evidence for DevNet/TestNet explicit peer-driven apply drain.
- No autonomous background drain.
- No automatic apply on receipt.
- No peer-majority authority.
- No MainNet enablement.
- No governance implementation.
- No KMS/HSM implementation.
- No signing-key rotation/revocation lifecycle.
- No validator-set rotation.
- No new wire format.
- No trust-bundle schema change.
- No ratification sidecar schema change.
- No authority-marker schema change.
- No sequence-file schema change.
- No peer-candidate envelope schema change.
- Do not weaken Runs 070, 142, 143, 145, 146, 147, 148, 149, 150, 151, or 152.
- Do not claim full C4 or C5 closure.

Deliverables:
1. Add release-binary harness:
   scripts/devnet/run_153_peer_driven_apply_end_to_end_release_binary.sh

2. Add evidence archive:
   docs/devnet/run_153_peer_driven_apply_end_to_end_release_binary/

3. Add canonical evidence report:
   docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_153.md

4. Update narrowly:
   - docs/whitepaper/contradiction.md
   - docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md
   - docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md
   - docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md

5. Crosscheck all new/modified code and docs against existing design/spec. If contradictions or inconsistencies are found, record them in contradiction.md before finalizing.

Required release-binary topology:
Use a real DevNet N=3 topology unless TestNet fixtures are already available:

- V0: publisher of live `0x05` v2 peer-candidate.
- V1: receiver with wire validation, staging, apply-enabled, and drain-once enabled.
- V2: observer / propagation invariant node.

Use:
- real `target/release/qbind-node`;
- real live P2P `0x05` exchange;
- real signed DevNet trust-bundle material;
- real v2 ratification sidecar;
- real ML-KEM-768 / ML-DSA-44 / ChaCha20-Poly1305 path;
- mutual-auth Required if existing harnesses use it;
- no active DummySig / DummyKem / DummyAead.

Required accepted scenarios:

A1. DevNet end-to-end peer-driven apply succeeds
- V0 publishes valid v2 candidate over live `0x05`.
- V1 validates, stages, and explicitly drains once.
- Expected:
  - live `0x05` path actually exercised;
  - v2 validation-only acceptance visible;
  - candidate staged before drain;
  - drain-once trigger accepted;
  - `ProductionDrainInvocationBuilder` invoked;
  - `ProductionV2MarkerCoordinator` invoked;
  - Run 150 drain invoked;
  - Run 148 controller invoked;
  - Run 070 apply contract invoked;
  - `LivePqcTrustState` swap occurs;
  - session eviction occurs according to Run 070/072 semantics;
  - Run 055 sequence commit succeeds;
  - v2 authority marker persists strictly after sequence commit;
  - Applied outcome visible;
  - no autonomous repeat drain.

A2. TestNet end-to-end peer-driven apply succeeds if feasible
- Same as A1 under TestNet.
- MainNet behavior must not be inferred.
- If TestNet fixture setup is infeasible, document exact blocker and keep DevNet strongest-positive / TestNet partial.

A3. empty queue drain returns NoCandidate
- Drain-once enabled but no candidate staged.
- Expected:
  - typed NoCandidate visible;
  - no live trust swap;
  - no session eviction;
  - no sequence write;
  - no marker write.

A4. disabled policy refuses drain
- Candidate may be valid/staged, but apply/drain policy disabled.
- Expected:
  - disabled/co-requisite refusal visible;
  - no mutation.

A5. MainNet refused
- Attempt to arm or run peer-driven apply drain under MainNet.
- Expected:
  - fail closed before mutation;
  - MainNetRefused / FATAL visible;
  - no P2P apply;
  - no sequence write;
  - no marker write;
  - no live trust swap.

A6. duplicate candidate cannot double-apply
- Same candidate is published twice or drain is triggered twice.
- Expected:
  - at most one Applied outcome;
  - second drain returns NoCandidate / AlreadyApplied / deduped;
  - no duplicate sequence write;
  - no duplicate marker write;
  - no duplicate session eviction.

A7. deterministic highest-sequence selection
- Stage multiple valid v2 candidates.
- Expected:
  - highest authority-domain sequence selected;
  - tie-break by lexicographically smallest fingerprint if equal sequence;
  - selected candidate applied;
  - lower candidate not accidentally applied first.

Required rejection/no-op scenarios:

R1. lower-sequence candidate cannot apply
- Expected:
  - rejected before Run 070 apply;
  - no live trust swap;
  - no session eviction;
  - no sequence write;
  - no marker write.

R2. same-sequence different-digest candidate cannot apply
- Expected:
  - equivocation/conflict reason visible;
  - no mutation.

R3. bad-signature candidate cannot apply
- Expected:
  - Run 130 verifier failure visible;
  - no mutation.

R4. wrong-domain candidate cannot apply
- wrong environment, wrong chain, or wrong genesis.
- Expected:
  - wrong-domain reason visible;
  - no mutation.

R5. ambiguous v1+v2 candidate cannot apply
- Expected:
  - ambiguity refusal visible;
  - no mutation.

R6. expired staged candidate cannot apply
- Expected:
  - CandidateExpired or equivalent visible;
  - no mutation.

R7. concurrent drain attempt returns AlreadyInProgress
- Trigger two drains close together if feasible.
- Expected:
  - one enters;
  - second returns AlreadyInProgress;
  - no double apply;
  - no duplicate sequence/marker/eviction.

R8. apply validation failure before swap, if feasible
- Expected:
  - no live trust swap;
  - no session eviction;
  - no sequence write;
  - no marker write.
- If release-binary-infeasible without fault injection, document and cite Run 152/150 source-test coverage.

R9. eviction failure / sequence commit failure / marker persist failure, if feasible
- Expected:
  - existing Run 070 rollback/fatal semantics preserved;
  - no marker persist unless sequence commit succeeded;
  - post-commit marker persist failure fatal/operator-actionable.
- If release-binary-infeasible without unsafe fault injection, document and cite Run 152/150/148 source-test coverage.

R10. propagation-only behavior unchanged
- With propagation enabled:
  - valid candidate may rebroadcast only after validation;
  - invalid candidate never rebroadcasts;
  - propagation does not imply apply.
- With propagation disabled:
  - no rebroadcast.

Required ordering proof:
For every accepted apply scenario, prove by ordered logs or deterministic archive artifacts:

1. P2P connection established.
2. live `0x05` candidate received.
3. v2 validation-only accepted.
4. candidate staged.
5. explicit drain-once triggered.
6. `ProductionDrainInvocationBuilder` built invocation.
7. `ProductionV2MarkerCoordinator` accepted pre-apply marker decision.
8. Run 150 drain invoked.
9. Run 148 controller invoked.
10. Run 070 ordering:
    validate → snapshot previous → swap → evict_sessions → commit_sequence.
11. sequence commit succeeds.
12. v2 marker persists after sequence commit.
13. Applied outcome emitted.

Required captured evidence:
- qbind-node SHA-256 and ELF Build ID;
- helper binary SHA-256 and ELF Build ID;
- git commit hash;
- rustc/cargo versions;
- exact commands;
- node PIDs;
- stdout/stderr logs;
- metrics before/after;
- sequence JSON/SHA before/after;
- authority marker JSON/SHA before/after;
- active trust root / live-trust fingerprint before/after if observable;
- session-eviction counters before/after;
- staging queue observations if logged;
- drain outcome logs;
- ordered line-number proof for apply ordering;
- denylist grep for out-of-scope behavior.

Required denylist / negative invariants:
Across all scenarios:
- no autonomous background drain;
- no apply on receipt without explicit drain;
- no peer-majority authority;
- no governance claim;
- no KMS/HSM claim;
- no signing-key rotation/revocation claim;
- no validator-set rotation claim;
- no MainNet apply;
- no fallback to `--p2p-trusted-root`;
- no active DummySig / DummyKem / DummyAead;
- no SIGHUP outcome;
- no reload-apply outcome;
- no startup mutation path accidentally selected;
- no snapshot/restore path accidentally selected;
- no schema/wire/metric drift.

Validation commands:
Run at minimum:
- cargo build --release -p qbind-node --bin qbind-node
- cargo build --release -p qbind-node --example <needed helper(s)>
- bash scripts/devnet/run_153_peer_driven_apply_end_to_end_release_binary.sh
- cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests
- cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests
- cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests
- cargo test -p qbind-node --test run_146_live_inbound_0x05_staging_hook_tests
- cargo test -p qbind-node --test run_145_peer_candidate_staging_tests
- cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests
- cargo test -p qbind-node --test run_088_pqc_peer_candidate_propagation_tests
- cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests
- cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests
- cargo test -p qbind-node --lib pqc_authority
- cargo test -p qbind-node --lib

If exact test target names differ, locate the nearest existing Run 145/146/148/150/152/142/088 targets and document exact commands/results.

Documentation requirements:
`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_153.md` must state:
- Run 153 is release-binary end-to-end peer-driven apply evidence.
- Whether any minimal source hook was needed to make Run 152 plumbing callable.
- No autonomous background apply exists.
- No automatic apply on receipt exists.
- MainNet remains refused.
- Governance remains unimplemented.
- KMS/HSM remains unimplemented.
- Signing-key rotation/revocation lifecycle remains open.
- Validator-set rotation remains open.
- TestNet evidence is present or explicitly deferred with reason.
- Full C4 remains open unless the team has separately defined C4 closure criteria and all are met.
- C5 remains open.

Acceptance criteria:
Run 153 is acceptable only if:
1. real `target/release/qbind-node` is used;
2. real live P2P `0x05` candidate is received and staged;
3. explicit local drain-once trigger applies at most one valid staged candidate;
4. accepted apply routes through ProductionDrainInvocationBuilder, ProductionV2MarkerCoordinator, Run 150 drain, Run 148 controller, and Run 070 contract;
5. Run 070 ordering is evidenced;
6. sequence commit happens before v2 marker persistence;
7. session eviction occurs according to Run 070/072 semantics;
8. reject/no-op scenarios produce no mutation;
9. MainNet is refused;
10. no autonomous peer-driven apply exists;
11. no peer-majority/governance/KMS-HSM claim is made;
12. evidence archive is complete;
13. docs are updated narrowly;
14. no C5 closure is claimed.