Task: Run 233 — Release-Binary Governance Evaluator Replay/Freshness Runtime Integration Evidence

Current accepted state:

* Run 230: source/test governance evaluator replay/freshness state boundary landed.
* Run 231: release-binary replay/freshness state evidence fully closed.
* Run 232: source/test replay/freshness runtime integration landed:

  * `pqc_governance_evaluator_replay_runtime_integration.rs`;
  * `integrate_governance_evaluator_replay_runtime`;
  * typed outcome `GovernanceEvaluatorReplayRuntimeOutcome`;
  * outcomes include:

    * `ProceedLegacyBypass`;
    * `ProceedDeferred`;
    * `ProceedFresh`;
    * `ReplayFreshnessFailClosed`;
    * `RuntimeIntegrationFailClosed`;
    * `MainNetPeerDrivenApplyRefused`;
  * replay/freshness is now a mandatory pre-mutation gate in the evaluator runtime integration path at source/test level;
  * `ProceedDeferred` is not mutation approval;
  * fresh is required before mutation authorization;
  * read-only validation does not consume;
  * explicit consume remains fixture-only;
  * production/MainNet replay state remains unavailable/fail-closed;
  * MainNet peer-driven apply remains refused;
  * no storage/schema/migration/RocksDB/file-format change;
  * release-binary evidence deferred to Run 233.
* Full C4 remains OPEN.
* C5 remains OPEN.

Objective:
Produce release-binary evidence for the Run 232 governance evaluator replay/freshness runtime integration.

This run must prove in release mode that replay/freshness validation is composed into the evaluator runtime integration path, that fresh is required before mutation authorization, that deferred is not approval, that expired/stale/replayed/consumed/superseded decisions fail closed before mutation, that read-only validation does not consume, and that explicit consume remains fixture-only.

Strict scope:

* Release-binary evidence only.
* Use release-built helper and real `target/release/qbind-node` where applicable.
* No production source behavior change unless a tiny harness-only fix is required.
* No real governance execution engine.
* No real on-chain governance proof verifier.
* No MainNet governance enablement.
* No MainNet peer-driven apply enablement.
* No validator-set rotation.
* No KMS/HSM backend implementation.
* No RemoteSigner backend implementation.
* No RocksDB schema change.
* No file-format change.
* No database migration.
* No wire/schema/marker/sequence/trust-bundle change.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* Do not weaken Runs 070, 130–232.
* Do not claim full C4 or C5 closure.

Required deliverables:

1. Add release-binary helper:
   `crates/qbind-node/examples/run_233_governance_evaluator_replay_runtime_integration_release_binary_helper.rs`

2. Add release-binary harness:
   `scripts/devnet/run_233_governance_evaluator_replay_runtime_integration_release_binary.sh`

3. Add evidence archive:
   `docs/devnet/run_233_governance_evaluator_replay_runtime_integration_release_binary/`

4. Add canonical evidence report:
   `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_233.md`

5. Update narrowly:

   * `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
   * `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
   * `docs/whitepaper/contradiction.md`
   * `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
   * `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
   * `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

6. Crosscheck all new/modified code and docs against existing design/spec. If contradictions are found, record them in `contradiction.md`.

Required release-helper matrix:
The release-built helper must exercise Run 232 replay/freshness runtime integration symbols through production library code.

Accepted / compatible cases:
A1. Disabled policy + absent carrier preserves `ProceedLegacyBypass`.

A2. DevNet fixture evaluator decision with fresh replay state reaches `ProceedFresh`.

A3. TestNet fixture evaluator decision with fresh replay state reaches `ProceedFresh`.

A4. not-yet-effective decision reaches `ProceedDeferred`, not mutation authorization.

A5. fresh decision at effective epoch authorizes only after evaluator and replay state both agree.

A6. explicit consume marks consumed only after successful fixture authorization.

A7. read-only validation does not mark consumed.

A8. production replay reader is reached and fails closed unavailable.

A9. MainNet replay reader is reached and fails closed unavailable/refused.

A10. MainNet peer-driven apply remains refused even when replay state is fresh.

A11. `ProceedFresh` is the only replay/freshness outcome that can authorize mutation.

A12. `ProceedDeferred` is release-evidenced as not approval.

A13. Run 230 replay/freshness boundary release behavior remains compatible.

A14. Run 231 replay/freshness standalone release behavior remains compatible.

A15. Run 228 peer evaluator context behavior remains compatible.

A16. Run 226 call-site integration behavior remains compatible.

A17. Run 224 evaluator runtime integration behavior remains compatible.

Required rejection cases:
R1. expired decision rejected before mutation.

R2. stale decision rejected before mutation.

R3. replayed decision rejected before mutation.

R4. already-consumed decision rejected before mutation.

R5. superseded decision rejected before mutation.

R6. wrong environment rejected.

R7. wrong chain rejected.

R8. wrong genesis rejected.

R9. wrong validation surface rejected.

R10. wrong source identity digest rejected.

R11. wrong request digest rejected.

R12. wrong response digest rejected.

R13. wrong transcript digest rejected.

R14. wrong proposal id rejected.

R15. wrong decision id rejected.

R16. wrong lifecycle action rejected.

R17. wrong candidate digest rejected.

R18. wrong authority-domain sequence rejected.

R19. wrong replay nonce rejected.

R20. malformed replay state rejected.

R21. replay state unavailable rejected.

R22. production replay state unavailable rejected.

R23. MainNet replay state unavailable/refused rejected.

R24. validator-set rotation unsupported rejected.

R25. policy-change action unsupported rejected.

R26. validation-only rejection writes no marker and no sequence.

R27. mutating rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

Required real release checks:
Use release artifacts to prove:

* helper binary SHA-256 and ELF Build ID captured;
* `qbind-node` release binary SHA-256 and ELF Build ID captured if built;
* Run 232 replay/freshness runtime integration helper corpus passes;
* fresh/deferred/expired/stale/replay/consumed/superseded outcomes are distinguished through runtime integration;
* fresh is required before mutation authorization;
* deferred is not approval;
* read-only validation does not consume;
* explicit consume marks consumed only in fixture evidence;
* production/MainNet replay state unavailable is fail-closed;
* no storage/schema/migration/RocksDB/file-format drift;
* MainNet peer-driven apply remains refused;
* no real governance engine active claim;
* no real on-chain proof verifier active claim;
* no MainNet governance enabled claim;
* no validator-set rotation claim;
* no KMS/HSM backend active claim;
* no RemoteSigner backend active claim;
* no peer-majority authority claim;
* no automatic apply / apply-on-receipt claim.

Required source/release reachability proof:
The report must include grep/call-site proof for:

* `pqc_governance_evaluator_replay_runtime_integration`;
* `integrate_governance_evaluator_replay_runtime`;
* `GovernanceEvaluatorReplayRuntimeOutcome`;
* `ProceedLegacyBypass`;
* `ProceedDeferred`;
* `ProceedFresh`;
* `ReplayFreshnessFailClosed`;
* `RuntimeIntegrationFailClosed`;
* `MainNetPeerDrivenApplyRefused`;
* Run 224 evaluator runtime integration usage;
* Run 226 call-site integration usage;
* Run 228 peer evaluator context compatibility;
* Run 230 replay/freshness boundary usage;
* read-only validation path;
* explicit consume path;
* production/MainNet unavailable fail-closed path;
* MainNet peer-driven refusal guard.

Required mutation / no-mutation proof:
For every rejected replay/freshness runtime integration case:

* no Run 070 apply call;
* no live trust swap;
* no session eviction;
* no sequence write;
* no marker write;
* no `.tmp` residue;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead;
* validation-only rejection writes no marker and no sequence;
* mutating rejection produces no mutation.

Required denylist:
Across the run prove:

* no MainNet apply;
* no autonomous apply;
* no apply on receipt;
* no peer-majority authority;
* no real governance execution engine claim;
* no production governance active claim;
* no MainNet governance enabled claim;
* no real on-chain governance proof verifier claim;
* no real KMS backend claim;
* no real HSM backend claim;
* no real RemoteSigner backend claim;
* no custody attestation production-active claim;
* no validator-set rotation claim;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead;
* no RocksDB schema change;
* no file format change;
* no database migration;
* no network wire schema change;
* no trust-bundle schema change;
* no marker schema change;
* no sequence schema change;
* no marker write before sequence commit;
* no sequence write on validation-only surfaces;
* no marker write on validation-only surfaces.

Required captured metadata:

* helper SHA-256 and ELF Build ID;
* qbind-node SHA-256 and ELF Build ID if built;
* git commit hash;
* rustc/cargo versions;
* exact commands;
* stdout/stderr logs;
* per-scenario exit codes;
* replay state key digests;
* replay observation digests;
* consumed decision digests;
* freshness transcript digests;
* replay/freshness classification values;
* replay/freshness runtime integration outcome values;
* before/after fixture replay-store state snapshots;
* marker/sequence before/after hashes where applicable;
* data-dir inventories;
* denylist grep results.

Validation commands:
Run at minimum:

* `cargo build --release -p qbind-node --bin qbind-node`
* `cargo build --release -p qbind-node --example run_233_governance_evaluator_replay_runtime_integration_release_binary_helper`
* `bash scripts/devnet/run_233_governance_evaluator_replay_runtime_integration_release_binary.sh`
* `cargo test -p qbind-node --test run_232_governance_evaluator_replay_runtime_integration_tests`
* `cargo test -p qbind-node --test run_230_governance_evaluator_replay_state_tests`
* `cargo test -p qbind-node --test run_228_peer_evaluator_context_representation_tests`
* `cargo test -p qbind-node --test run_226_governance_evaluator_runtime_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_224_governance_evaluator_runtime_integration_tests`
* `cargo test -p qbind-node --test run_222_governance_execution_evaluator_tests`
* `cargo test -p qbind-node --test run_220_governance_execution_runtime_consumption_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Documentation requirements:
`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_233.md` must state:

* Run 233 is release-binary governance evaluator replay/freshness runtime integration evidence.
* Replay/freshness is composed into the evaluator runtime integration path in release evidence.
* Fresh is required before mutation authorization.
* Deferred is not mutation approval.
* Expired/stale/replayed/consumed/superseded decisions fail closed before mutation.
* Read-only validation does not mark consumed.
* Explicit consume remains fixture-only.
* Production/MainNet replay state remains unavailable/fail-closed.
* No RocksDB/file/schema/migration/storage-format change is implemented.
* MainNet peer-driven apply remains refused.
* Validator-set rotation remains unsupported.
* No real governance engine or on-chain proof verifier is implemented.
* Full C4 remains OPEN.
* C5 remains OPEN.

Acceptance criteria:
Run 233 is acceptable only if:

1. Release-built helper exercises the Run 232 replay/freshness runtime integration.
2. Fresh is required before mutation authorization in release evidence.
3. Deferred is not approval in release evidence.
4. Expired/stale/replayed/consumed/superseded decisions fail closed before mutation.
5. Read-only validation does not consume.
6. Explicit consume remains fixture-only.
7. Production/MainNet replay state remains unavailable/fail-closed.
8. Rejections are non-mutating.
9. MainNet peer-driven apply remains refused.
10. No storage/schema/migration/RocksDB/file-format change is claimed.
11. Existing Run 231, Run 229, Run 227, Run 225, and Run 223 release behavior remains compatible.
12. No full C4 or C5 closure is claimed.
