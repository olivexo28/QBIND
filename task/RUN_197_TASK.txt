Task: Run 197 — Release-Binary RemoteSigner Payload Carrying Evidence

Current accepted state:

* Run 194: source/test RemoteSigner production-custody interface boundary landed:

  * `pqc_remote_authority_signer.rs`;
  * `RemoteSignerPolicy::{Disabled, FixtureLoopbackAllowed, ProductionRemoteSignerRequired, MainnetProductionRemoteSignerRequired}`;
  * `RemoteSignerIdentity`;
  * `RemoteSignerRequest`;
  * `RemoteSignerResponse`;
  * deterministic domain-separated SHA3-256 canonical digest;
  * `RemoteAuthoritySigner` trait;
  * DevNet/TestNet-only `FixtureLoopbackRemoteSigner`;
  * fail-closed `ProductionRemoteSigner`;
  * `validate_remote_signer`;
  * `validate_remote_signer_for_custody_class`;
  * `validate_lifecycle_governance_custody_and_remote_signer`.
* Run 195: release-binary RemoteSigner boundary evidence fully closed:

  * fixture loopback RemoteSigner remains DevNet/TestNet evidence-only;
  * production RemoteSigner remains unavailable/fail-closed;
  * local operator keys and peer-majority cannot satisfy RemoteSigner policy;
  * MainNet peer-driven apply remains refused.
* Run 196: source/test RemoteSigner payload/carrying and production-context wiring landed:

  * `pqc_remote_signer_payload_carrying.rs`;
  * `RemoteSignerIdentityWire`;
  * `RemoteSignerRequestWire`;
  * `RemoteSignerResponseWire`;
  * `RemoteSignerAttestationWire`;
  * `RemoteSignerLoadStatus::{Absent, Available, Malformed}`;
  * optional `remote_signer_attestation` JSON sibling extraction before strict v2 parse;
  * combined v2-sidecar loader;
  * seven per-surface production-context routing helpers;
  * custody-class routing;
  * `validate_loaded_remote_signer`;
  * 58 tests covering A1–A10 / R1–R34;
  * full lib suite 1316 passed.
* Run 196 preserves:

  * no real RemoteSigner backend;
  * no networked signer service;
  * no real KMS/HSM/cloud-KMS/PKCS#11;
  * no MainNet peer-driven apply enablement;
  * no governance execution;
  * no real on-chain proof verification;
  * no validator-set rotation;
  * full C4 open;
  * C5 open.
* Release-binary RemoteSigner payload/carrying evidence is deferred to Run 197.

Objective:
Produce release-binary evidence that real `target/release/qbind-node` production payload/context paths can carry RemoteSigner identity/request/response attestation material and route it into the Run 194 RemoteSigner boundary through the Run 196 production-context helpers.

This run must prove:

1. legacy/no-RemoteSigner payloads remain compatible under default Disabled behavior;
2. fixture loopback RemoteSigner material reaches production-context paths in release mode where policy allows;
3. production RemoteSigner material reaches the boundary and fails closed as unavailable;
4. malformed/invalid RemoteSigner material fails closed;
5. RemoteSigner request/response digest binding is deterministic and domain-bound;
6. rejected cases produce no mutation;
7. MainNet peer-driven apply remains refused even with fixture loopback RemoteSigner material;
8. no real RemoteSigner/KMS/HSM/governance execution/validator-set rotation is claimed.

Strict scope:

* Release-binary evidence only.
* Use real `target/release/qbind-node`.
* Use release-built helper(s) to mint RemoteSigner-carrying sidecars / peer-candidate material.
* No production source change unless a tiny harness-only fix is required.
* No real RemoteSigner backend.
* No networked signer service.
* No real KMS implementation.
* No real HSM implementation.
* No cloud KMS integration.
* No PKCS#11 integration.
* No MainNet peer-driven apply enablement.
* No governance execution engine.
* No real on-chain proof verifier.
* No validator-set rotation.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* No schema/wire change beyond Run 196’s additive optional RemoteSigner sibling.
* No authority-marker schema change.
* No sequence-file schema change.
* No trust-bundle core schema change.
* Do not weaken Runs 070, 130–196.
* Do not claim full C4 or C5 closure.

Required deliverables:

1. Add release-binary helper:
   `crates/qbind-node/examples/run_197_remote_signer_payload_release_binary_helper.rs`

2. Add release-binary harness:
   `scripts/devnet/run_197_remote_signer_payload_release_binary.sh`

3. Add evidence archive:
   `docs/devnet/run_197_remote_signer_payload_release_binary/`

4. Add canonical evidence report:
   `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_197.md`

5. Update narrowly:

   * `docs/whitepaper/contradiction.md`
   * `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
   * `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
   * `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

6. Crosscheck all new/modified code and docs against existing design/spec. If contradictions or inconsistencies are found, record them in `contradiction.md` before finalizing.

Required release-built helper matrix:
The helper must exercise the Run 196 RemoteSigner payload/call-site routing in release mode.

Accepted / compatible cases:
A1. no-RemoteSigner payload remains compatible under default Disabled.

A2. DevNet fixture loopback RemoteSigner material carried through reload-check context and accepted under explicit fixture policy.

A3. TestNet fixture loopback RemoteSigner material carried through reload-check context and accepted under explicit fixture policy.

A4. DevNet fixture loopback RemoteSigner material carried through reload-apply context and accepted under explicit fixture policy.

A5. RemoteSigner request/response canonical digest preserved through wire conversion.

A6. custody class `RemoteSigner` routes to RemoteSigner boundary when RemoteSigner material is present.

A7. combined lifecycle + governance + custody + fixture RemoteSigner accepted for DevNet release-helper production-context path.

A8. GenesisBound / EmergencyCouncil / OnChainGovernance proof behavior remains unchanged when RemoteSigner policy is Disabled.

A9. KMS/HSM/fixture/local custody paths remain compatible with RemoteSigner payload absent.

A10. production RemoteSigner material reaches boundary and returns typed unavailable outcome under production-required policy.

Required rejection cases:
R1. RemoteSigner material absent where RemoteSigner policy requires it rejected fail-closed.

R2. malformed RemoteSigner identity rejected.

R3. malformed RemoteSigner request rejected.

R4. malformed RemoteSigner response rejected.

R5. malformed combined RemoteSigner attestation rejected.

R6. fixture loopback rejected under ProductionRemoteSignerRequired.

R7. fixture loopback rejected under MainnetProductionRemoteSignerRequired.

R8. production RemoteSigner rejected as unavailable.

R9. MainNet production RemoteSigner rejected as unavailable.

R10. wrong environment rejected.

R11. wrong chain rejected.

R12. wrong genesis rejected.

R13. wrong authority root rejected.

R14. wrong custody key id rejected.

R15. wrong signing-key fingerprint rejected.

R16. wrong lifecycle action rejected.

R17. wrong candidate digest rejected.

R18. wrong authority-domain sequence rejected.

R19. wrong request digest rejected.

R20. stale/replayed request rejected.

R21. stale/replayed response rejected.

R22. expired attestation rejected.

R23. expired response rejected.

R24. unsupported suite rejected.

R25. invalid signature / placeholder signature rejected.

R26. local operator key cannot satisfy RemoteSigner policy.

R27. peer majority / gossip count cannot satisfy RemoteSigner policy.

R28. RemoteSigner valid but custody metadata invalid rejected.

R29. custody valid but RemoteSigner response invalid rejected.

R30. lifecycle valid + governance valid + custody valid + production RemoteSigner unavailable rejected.

R31. validation-only rejection writes no marker and no sequence.

R32. mutating rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

R33. invalid live `0x05` RemoteSigner material candidate is not propagated, staged, or applied, if live surface is included.

R34. MainNet peer-driven apply remains refused even with fixture loopback RemoteSigner material.

Required real binary checks:
Use real `target/release/qbind-node` to prove:

* MainNet peer-driven apply remains refused.
* Default behavior does not expose or enable production RemoteSigner.
* No `remote signer production active` / `RemoteSigner enabled` / `RemoteSigner backend connected` claim is emitted.
* No KMS/HSM active claim is emitted.
* Existing custody policy selector behavior from Run 193 remains compatible.
* Existing governance fixture proof paths remain compatible.
* No governance execution claim is emitted.
* No validator-set rotation claim is emitted.

Required source/release reachability proof:
The report must include source grep/call-site proof for:

* `pqc_remote_signer_payload_carrying`;
* `RemoteSignerIdentityWire`;
* `RemoteSignerRequestWire`;
* `RemoteSignerResponseWire`;
* `RemoteSignerAttestationWire`;
* `RemoteSignerLoadStatus`;
* `remote_signer_attestation`;
* seven per-surface RemoteSigner routing helpers;
* `validate_loaded_remote_signer`;
* `validate_remote_signer`;
* `validate_remote_signer_for_custody_class`;
* `validate_lifecycle_governance_custody_and_remote_signer`.

Required mutation / no-mutation proof:
For rejected RemoteSigner-payload scenarios:

* no Run 070 apply call;
* no live trust swap;
* no session eviction;
* no sequence write;
* no marker write;
* marker bytes unchanged where present;
* sequence bytes unchanged where present;
* no `.tmp` residue;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead.

For any accepted fixture RemoteSigner mutating compatibility scenario:

* RemoteSigner payload parse occurs before marker decision;
* RemoteSigner request validation occurs before apply/mutation;
* RemoteSigner response validation occurs before apply/mutation;
* custody validation occurs before apply/mutation;
* lifecycle validation occurs before apply/mutation;
* governance validation occurs before apply/mutation where applicable;
* Run 055 sequence commit succeeds if a mutating path is exercised;
* v2 marker persists strictly after sequence commit.

Required denylist:
Across the run prove:

* no MainNet apply;
* no autonomous apply;
* no apply on receipt;
* no peer-majority authority;
* no real RemoteSigner backend claim;
* no RemoteSigner production active claim;
* no real governance execution claim;
* no real on-chain governance proof claim;
* no KMS/HSM enabled or active claim;
* no validator-set rotation claim;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead;
* no schema/wire/metric drift beyond Run 196’s additive optional RemoteSigner sibling;
* no marker write before sequence commit;
* no sequence write on validation-only surfaces;
* no marker write on validation-only surfaces.

Required captured metadata:

* qbind-node SHA-256 and ELF Build ID;
* helper SHA-256 and ELF Build ID;
* git commit hash;
* rustc/cargo versions;
* exact commands;
* stdout/stderr logs;
* per-scenario exit codes;
* RemoteSigner fixture request/response paths and hashes;
* marker/sequence JSON and SHA before/after where applicable;
* data-dir inventories;
* denylist grep results.

Validation commands:
Run at minimum:

* `cargo build --release -p qbind-node --bin qbind-node`
* `cargo build --release -p qbind-node --example run_197_remote_signer_payload_release_binary_helper`
* `bash scripts/devnet/run_197_remote_signer_payload_release_binary.sh`
* `cargo test -p qbind-node --test run_196_remote_signer_payload_callsite_tests`
* `cargo test -p qbind-node --test run_194_remote_authority_signer_boundary_tests`
* `cargo test -p qbind-node --test run_192_authority_custody_policy_selector_tests`
* `cargo test -p qbind-node --test run_190_authority_custody_payload_callsite_tests`
* `cargo test -p qbind-node --test run_188_authority_custody_boundary_tests`
* `cargo test -p qbind-node --test run_186_onchain_governance_production_verifier_boundary_tests`
* `cargo test -p qbind-node --test run_184_onchain_governance_payload_carrying_tests`
* `cargo test -p qbind-node --test run_182_onchain_governance_production_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_180_onchain_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_178_onchain_governance_proof_tests`
* `cargo test -p qbind-node --test run_176_live_0x05_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_173_validation_only_governance_required_policy_tests`
* `cargo test -p qbind-node --test run_171_governance_required_policy_selector_tests`
* `cargo test -p qbind-node --test run_169_governance_proof_loader_surface_integration_tests`
* `cargo test -p qbind-node --test run_167_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_165_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_163_governance_authority_verifier_tests`
* `cargo test -p qbind-node --test run_161_lifecycle_marker_integration_tests`
* `cargo test -p qbind-node --test run_159_authority_signing_key_lifecycle_tests`
* `cargo test -p qbind-node --test run_157_unified_testnet_fixture_universe_tests`
* `cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests`
* `cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests`
* `cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests`
* `cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests`
* `cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests`
* `cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Documentation requirements:
`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_197.md` must state:

* Run 197 is release-binary RemoteSigner payload/carrying evidence.
* No real RemoteSigner backend is implemented.
* Fixture loopback RemoteSigner is DevNet/TestNet evidence-only.
* Production RemoteSigner remains unavailable/fail-closed.
* RemoteSigner payload/carrying evidence does not enable MainNet peer-driven apply.
* KMS/HSM remain unimplemented.
* governance execution remains unimplemented.
* real on-chain proof verification remains unimplemented.
* validator-set rotation remains open.
* existing custody/governance proof paths remain compatible.
* full C4 remains open.
* C5 remains open.

Acceptance criteria:
Run 197 is acceptable only if:

1. Release-built helper proves the Run 196 RemoteSigner payload/carrying corpus.
2. Real `target/release/qbind-node` confirms MainNet peer-driven apply remains refused.
3. Legacy no-RemoteSigner payloads remain compatible.
4. Fixture loopback RemoteSigner material reaches production-context paths in release mode where policy allows.
5. Production RemoteSigner material reaches boundary and fails closed as unavailable.
6. Malformed/invalid RemoteSigner material fails closed.
7. RemoteSigner request/response binding remains deterministic and domain-bound.
8. Rejected RemoteSigner-payload cases produce no mutation.
9. Existing custody/governance paths remain compatible.
10. No real RemoteSigner / KMS / HSM / governance execution / validator-set rotation claim is made.
11. No full C4 or C5 closure is claimed.
