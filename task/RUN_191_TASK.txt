Task: Complete Run 191 Release-Binary Evidence Closure

Current state:
Run 191 helper, harness, canonical report, archive scaffold, and doc updates are committed and pushed.

However, the uploaded archive summary still contains placeholder text:

`Run 191 — release-binary authority-custody metadata carrying evidence`
`(placeholder summary; the harness rewrites this file on every run.)`
`verdict: placeholder (regenerated by harness)`

Therefore Run 191 is not release-binary-closed yet.

Objective:
Execute the Run 191 release-binary harness in a build environment, overwrite the placeholder `summary.txt` with real build provenance and verdicts, and update the canonical report only if the actual run differs from the drafted claims.

Strict scope:

* Execution/evidence closure only.
* Do not add production source.
* Do not change CLI behavior.
* Do not change schema/wire/metric behavior.
* Do not enable MainNet peer-driven apply.
* Do not implement real KMS.
* Do not implement real HSM.
* Do not implement cloud KMS.
* Do not implement PKCS#11.
* Do not implement remote signer production integration.
* Do not implement governance execution.
* Do not implement real on-chain proof verification.
* Do not implement validator-set rotation.
* Do not claim full C4 or C5 closure.

Required commands:
Run at minimum:

```bash
cargo build --release -p qbind-node --bin qbind-node
cargo build --release -p qbind-node --example run_191_authority_custody_payload_release_binary_helper
bash scripts/devnet/run_191_authority_custody_payload_release_binary.sh
```

Then confirm the harness ran the required regression matrix, or run any missing regression commands manually and record them.

Required evidence updates:

1. Ensure:
   `docs/devnet/run_191_authority_custody_payload_release_binary/summary.txt`
   no longer contains:

   * `placeholder summary`
   * `the harness rewrites this file on every run`
   * `verdict: placeholder`

2. The real summary must include:

   * git commit;
   * qbind-node SHA-256;
   * qbind-node ELF Build ID;
   * helper SHA-256;
   * helper ELF Build ID;
   * rustc/cargo versions;
   * real binary scenario verdicts;
   * helper A1–A10 / R1–R32 verdicts;
   * regression verdicts;
   * denylist result;
   * final verdict.

3. Confirm:
   `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_191.md`
   matches the actual harness result.

4. If any scenario fails or is skipped, update the report honestly:

   * mark partial-positive if needed;
   * identify exact blocker;
   * do not claim release-binary closure if the real binary did not prove it.

Required acceptance checks:
Run 191 is acceptable only if:

1. the real harness was executed;
2. `summary.txt` contains a real verdict, not the placeholder;
3. release-built helper proves the Run 190 custody metadata carrying/call-site corpus;
4. real `target/release/qbind-node` confirms MainNet peer-driven apply remains refused;
5. legacy no-custody payloads remain compatible;
6. valid DevNet/TestNet fixture/local custody metadata reaches production context paths in release mode where policy allows;
7. KMS/HSM/RemoteSigner placeholders reach custody validator and fail closed as unavailable;
8. invalid custody metadata fails closed;
9. rejected custody-metadata cases produce no mutation;
10. existing governance proof paths remain compatible;
11. no real KMS/HSM / governance execution / real on-chain proof / validator-set rotation claim is made;
12. full C4 and C5 remain open.
