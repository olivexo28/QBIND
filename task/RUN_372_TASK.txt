Run 372 — Strict-mutual-auth + multi-peer concurrent M12 hardening evidence

Continue QBIND after accepted Run 371.

Current state:
Run 371 is accepted as POSITIVE / public-DevNet abuse/DoS M12 KEMTLS live-socket release-binary positive.

Run 371 moved M12 Yellow/Partial → Green for the public DevNet readiness track, scoped only to the two abuse/DoS deployed live-socket controls:
1. connection-rate limiting over deployed live sockets;
2. per-peer message-rate limiting over a KEMTLS-admitted deployed live peer socket.

Public DevNet remains NOT launch-ready.
M4 remains Yellow/launch-blocking.
M6 remains Yellow/Partial.
M7–M9 and M13–M15 remain unchanged unless the readiness matrix already records them Green elsewhere.
Full C4 remains OPEN.
C5 remains OPEN.
MainNet authority rotation/revocation remains Red.

Primary objective:
Produce hardening evidence that the Run 371 M12 Green result still holds under stricter admission and multi-peer concurrent flood conditions.

This run should prove:
1. strict mutual-auth admission can be used for the two-node flood harness;
2. production-grade or pqc-static-root KEMTLS material can be used instead of default deterministic test-grade material, where supported;
3. multiple simultaneous KEMTLS-admitted peers are bucket-isolated;
4. one abusive peer’s message flood does not consume another peer’s budget;
5. connection-rate and per-peer message-rate controls remain independent;
6. M12 remains Green for scope, without broadening to public DevNet launch readiness.

Strict scope:
Release-binary hardening evidence for M12 only.

No public DevNet launch.
No live external seed/bootnode deployment.
No M4 Green.
No M6 Green.
No faucet.
No RPC gateway.
No explorer.
No status page.
No TestNet readiness claim.
No MainNet readiness claim.
No C4 closure.
No C5 closure.
No authority-lifecycle runtime wiring.
No validator-set mutation.
No epoch transition.
No sequence/marker write.
No LivePqcTrustState mutation.
No P2P wire-format change.
No peer-admission weakening.
No trust-bundle weakening.
No new public CLI flags.
No committed private keys or trust material.

Required investigation:
Inspect:
- Run 371 helper/harness/evidence.
- Existing KEMTLS and pqc-static-root helper surfaces.
- `crates/qbind-node/examples/devnet_pqc_root_helper.rs`.
- `crates/qbind-node/examples/devnet_pqc_trust_bundle_helper.rs`.
- P2P/KEMTLS release harnesses from Runs 037–041 and 084–089.
- `crates/qbind-node/src/p2p_tcp.rs`.
- `crates/qbind-node/src/p2p_node_builder.rs`.
- `crates/qbind-node/src/deployed_inbound_per_peer_limiter.rs`.
- `crates/qbind-node/src/peer_rate_limiter.rs`.
- `crates/qbind-node/src/metrics.rs`.
- `crates/qbind-node/src/metrics_http.rs`.
- `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_371.md`.
- `docs/release/QBIND_PUBLIC_DEVNET_READINESS_CRITERIA.md`.

Expected artifacts:
Add release helper:
`crates/qbind-node/examples/run_372_public_devnet_m12_strict_auth_multi_peer_flood_helper.rs`

Add release harness:
`scripts/devnet/run_372_public_devnet_m12_strict_auth_multi_peer_flood_release_binary.sh`

Add archive:
`docs/devnet/run_372_public_devnet_m12_strict_auth_multi_peer_flood_release_binary/{README.md,summary.txt,.gitignore}`

Add canonical evidence:
`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_372.md`

Update narrowly:
- `docs/release/QBIND_PUBLIC_DEVNET_READINESS_CRITERIA.md`
- `docs/release/public-devnet/p2p/ABUSE_DOS_POSTURE.md`
- `docs/release/public-devnet/p2p/VERIFY.md`
- `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
- `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
- `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
- `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
- `docs/whitepaper/contradiction.md`

Required scenarios:
1. `default_preserves_behavior`
2. `strict_mutual_auth_admission`
3. `pqc_static_root_or_production_grade_material_path`
4. `single_peer_under_budget`
5. `single_peer_over_budget`
6. `multi_peer_bucket_isolation`
7. `abusive_peer_does_not_consume_honest_peer_budget`
8. `connection_rate_regression`
9. `combined_limiter_independence`
10. `invalid_configs_fail_closed`
11. `mainnet_refused`
12. `hidden_cli_surface_checked`
13. `non_mutation_guards`

Required evidence:
- release `qbind-node` builds and SHA-256 captured;
- helper builds and SHA-256 captured;
- BuildIDs captured where available;
- toolchain captured;
- strict mutual-auth admission succeeds or any unsupported strict-auth blocker is recorded honestly;
- multiple admitted peers are observed;
- under-budget messages produce no per-peer drops;
- over-budget peer floods produce per-peer drops on live `/metrics`;
- honest peer remains under budget while abusive peer drops;
- connection-rate metric stays independent;
- hidden abuse/DoS flags remain hidden;
- invalid configs fail closed;
- MainNet refused;
- no launch/TestNet/MainNet/C4/C5 claim.

Readiness rules:
- M12 should remain Green if strict-auth and multi-peer hardening evidence passes.
- If strict-auth or multi-peer evidence cannot be produced, do not downgrade M12 automatically; record the limitation as hardening outstanding unless it contradicts Run 371.
- Do not alter M4 unless live seed/bootnode reachability lands.
- Do not alter M6 unless stable identity generation lands.
- Do not alter M7–M9/M13–M15 unless genuinely closed.
- Public DevNet remains NOT launch-ready.

Required tests:
Always run:
- `cargo build -p qbind-node --release`
- `cargo build -p qbind-node --release --example run_372_public_devnet_m12_strict_auth_multi_peer_flood_helper`
- Run the Run 372 harness.
- `cargo test -p qbind-node --test run_370_public_devnet_deployed_live_socket_m12_tests`
- `cargo test -p qbind-node --test run_369_public_devnet_deployed_per_peer_limiter_wiring_tests`
- `cargo test -p qbind-node --test run_365_public_devnet_deployed_peer_rate_threading_tests`
- `cargo test -p qbind-node --test run_363_public_devnet_per_peer_message_rate_runtime_tests`
- `cargo test -p qbind-node --test run_362_public_devnet_abuse_dos_runtime_tests`
- `cargo test -p qbind-node --test run_361_public_devnet_abuse_dos_hardening_tests`
- `cargo test -p qbind-node --lib`

Security:
Run secret scanning over all changed files.
Generated KEMTLS material must remain temporary, dev-only, and gitignored.
No private keys, mnemonics, credentials, tokens, private infrastructure, real production hostnames, or live endpoints may be committed.

CodeQL:
If any Rust example/source changes, invoke CodeQL if available. If skipped or database-too-large, record the exact result and do not call it clean.

Expected verdict:
PASS / public-DevNet M12 strict-auth multi-peer hardening positive if strict mutual-auth and multi-peer bucket-isolation evidence lands while preserving all Run 371 guarantees.

Partial-positive if the helper/harness improves evidence but strict-auth or multi-peer flood cannot be completed.

Final response format:
1. Exact verdict.
2. Files changed.
3. Release artifacts and hashes.
4. Decision gate route.
5. Strict mutual-auth evidence.
6. PQC/static-root material evidence.
7. Multi-peer flood evidence.
8. Per-peer bucket isolation.
9. Connection-rate regression.
10. Combined limiter independence.
11. CLI/config surface.
12. Default compatibility.
13. Rejection/fail-closed evidence.
14. Non-mutation evidence.
15. Readiness matrix delta.
16. Current public DevNet readiness status.
17. Remaining public DevNet blockers.
18. Public TestNet blockers.
19. MainNet blockers.
20. C4/C5 status.
21. Tests run.
22. Security scans.
23. CodeQL.
24. Provenance.
25. Honest limitations.
26. Suggested Run 373 next step.