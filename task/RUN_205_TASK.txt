Task: Run 205 — Source/Test Production Custody Attestation Verifier Skeleton

Current accepted state:

* Run 188: source/test authority-custody boundary landed.
* Run 189: release-binary authority custody-boundary evidence fully closed.
* Run 190: source/test custody metadata carrying and production-context routing landed.
* Run 191: release-binary custody metadata carrying evidence fully closed.
* Run 192: source/test hidden authority-custody policy selector landed.
* Run 193: release-binary custody policy selector evidence fully closed.
* Run 194: source/test RemoteSigner production-custody interface boundary landed.
* Run 195: release-binary RemoteSigner boundary evidence fully closed.
* Run 196: source/test RemoteSigner payload/carrying and production-context wiring landed.
* Run 197: release-binary RemoteSigner payload/carrying evidence fully closed.
* Run 198: source/test RemoteSigner policy selector landed.
* Run 199: release-binary RemoteSigner policy selector evidence fully closed.
* Run 200: C4/C5 closure criteria and remaining-work consolidation landed.
* Run 201: source/test RemoteSigner transport boundary landed.
* Run 202: release-binary RemoteSigner transport-boundary evidence fully closed.
* Run 203: source/test KMS-HSM backend abstraction boundary landed.
* Run 204: release-binary KMS-HSM backend-boundary evidence fully closed:

  * release-built helper proves the Run 203 A1–A15 / R1–R41 corpus;
  * fixture KMS/HSM remain DevNet/TestNet evidence-only;
  * production KMS/HSM remain unavailable/fail-closed;
  * cloud KMS and PKCS#11 HSM remain unavailable/fail-closed;
  * RemoteSigner path remains separate and unchanged;
  * MainNet peer-driven apply remains refused;
  * no real KMS/HSM/cloud-KMS/PKCS#11/RemoteSigner backend exists;
  * no governance execution;
  * no real on-chain proof verification;
  * no validator-set rotation;
  * full C4 remains OPEN;
  * C5 remains OPEN.
* Current limitation:
  Production custody attestation is still only a placeholder digest/commitment. There is no typed verifier skeleton for validating a production custody attestation chain, attestation evidence class, freshness, replay protection, provider identity, or device/module identity.

Objective:
Add a source/test-only production custody attestation verifier skeleton.

This run must define typed custody attestation evidence, verifier policy, provider/device identity binding, freshness/replay checks, and fail-closed production behavior. It must not implement real cloud KMS attestation, PKCS#11 attestation, HSM vendor attestation, or remote signer attestation verification.

Release-binary custody-attestation verifier-boundary evidence is deferred to Run 206.

Strict scope:

* Source/test only.
* No release-binary harness in this run.
* No real KMS implementation.
* No real HSM implementation.
* No real cloud KMS attestation verifier.
* No real PKCS#11 attestation verifier.
* No real HSM vendor attestation verifier.
* No real RemoteSigner backend.
* No networked signer daemon.
* No production signing key custody.
* No MainNet peer-driven apply enablement.
* No governance execution engine.
* No real on-chain proof verifier.
* No validator-set rotation.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* No marker schema change.
* No sequence-file schema change.
* No trust-bundle core schema change.
* No authority lifecycle semantics change.
* No wire/schema change unless strictly additive and source/test-only.
* Do not weaken Runs 070, 130–204.
* Do not claim full C4 or C5 closure.

Required design:
Add a typed custody attestation verifier module, preferably:

`crates/qbind-node/src/pqc_custody_attestation_verifier.rs`

The module must define:

1. Attestation class:

* `Disabled`;
* `FixtureAttestation`;
* `RemoteSignerAttestation`;
* `KmsAttestation`;
* `HsmAttestation`;
* `CloudKmsAttestationUnavailable`;
* `Pkcs11HsmAttestationUnavailable`;
* `ProductionAttestationUnavailable`;
* `Unknown`.

2. Attestation policy:

* `Disabled` default;
* `FixtureAttestationAllowed`;
* `RemoteSignerAttestationRequired`;
* `KmsAttestationRequired`;
* `HsmAttestationRequired`;
* `ProductionAttestationRequired`;
* `MainnetProductionAttestationRequired`.

3. Attestation evidence:
   Must bind at minimum:

* environment;
* chain_id;
* genesis_hash;
* authority root fingerprint;
* bundle-signing key fingerprint;
* custody class;
* custody backend kind where applicable;
* backend id / provider id / signer id;
* custody key id / key label;
* suite id;
* lifecycle action;
* candidate digest;
* authority-domain sequence;
* governance proof digest where applicable;
* request digest where applicable;
* response digest where applicable;
* transcript digest where applicable;
* attestation nonce;
* issuance timestamp or epoch;
* expiry / freshness window;
* evidence digest / certificate digest / placeholder commitment.

4. Attestation verifier input:
   Must include:

* expected environment;
* expected chain_id;
* expected genesis_hash;
* expected authority root fingerprint;
* expected bundle-signing key fingerprint;
* expected custody class;
* expected backend/provider/signer id;
* expected custody key id / key label;
* expected lifecycle action;
* expected candidate digest;
* expected authority-domain sequence;
* freshness/replay window;
* current timestamp or logical epoch where represented.

5. Deterministic digest helpers:

* attestation evidence digest;
* attestation input digest;
* attestation transcript digest;
* optional provider identity digest.

6. Verifier trait/abstraction:
   Define a pure/mockable trait such as:

* `CustodyAttestationVerifier`;
* `verify_custody_attestation`.

For this run:

* fixture attestation may exist only for DevNet/TestNet source tests;
* production attestation verifiers must be callable but fail closed as unavailable;
* no real cloud/HSM/vendor/PKCS#11 verification is allowed.

7. Typed outcomes:
   Must distinguish:

* fixture attestation accepted;
* disabled;
* fixture attestation rejected under production policy;
* RemoteSigner attestation unavailable;
* KMS attestation unavailable;
* HSM attestation unavailable;
* cloud KMS attestation unavailable;
* PKCS#11 HSM attestation unavailable;
* production attestation unavailable;
* MainNet production attestation unavailable;
* unknown attestation class rejected;
* wrong environment;
* wrong chain;
* wrong genesis;
* wrong authority root;
* wrong signing-key fingerprint;
* wrong custody class;
* wrong backend/provider/signer id;
* wrong key id / key label;
* wrong suite;
* wrong lifecycle action;
* wrong candidate digest;
* wrong authority-domain sequence;
* wrong governance proof digest;
* wrong request digest;
* wrong response digest;
* wrong transcript digest;
* stale/replayed attestation;
* expired attestation;
* malformed attestation evidence;
* unsupported attestation version;
* invalid attestation commitment;
* local operator cannot satisfy production attestation;
* peer majority cannot satisfy production attestation.

Required source behavior:

1. Attestation verifier must compose with Run 188 custody metadata.
2. Attestation verifier must compose with Run 203 KMS/HSM backend identity/request/response where feasible.
3. Attestation verifier must compose with Run 201 RemoteSigner transport identity/request/response where feasible.
4. Production attestation paths must return unavailable/fail-closed.
5. Fixture attestation must be DevNet/TestNet source/test only.
6. No marker write, sequence write, live trust swap, session eviction, or Run 070 call.
7. MainNet peer-driven apply remains refused even if fixture attestation verifies successfully.
8. RemoteSigner and KMS/HSM paths must remain separate custody options.

Required accepted scenarios:
A1. fixture attestation accepted under explicit fixture policy on DevNet.

A2. fixture attestation accepted under explicit fixture policy on TestNet.

A3. attestation evidence digest deterministic.

A4. attestation input digest deterministic.

A5. attestation transcript digest deterministic.

A6. attestation evidence binds environment, chain, genesis, authority root, signing-key fingerprint, custody class, backend/signer id, key id, lifecycle action, candidate digest, authority-domain sequence.

A7. fixture attestation composes with Run 188 custody metadata.

A8. fixture attestation composes with Run 203 fixture KMS backend evidence.

A9. fixture attestation composes with Run 203 fixture HSM backend evidence.

A10. fixture attestation composes with Run 201 fixture RemoteSigner transport evidence.

A11. production attestation boundary callable and returns typed unavailable outcome.

A12. cloud KMS attestation boundary callable and returns typed unavailable outcome.

A13. PKCS#11 HSM attestation boundary callable and returns typed unavailable outcome.

A14. GenesisBound / EmergencyCouncil / OnChainGovernance proof behavior remains unchanged when attestation policy is Disabled.

Required rejection scenarios:
R1. attestation rejected under Disabled policy.

R2. fixture attestation rejected under ProductionAttestationRequired.

R3. fixture attestation rejected under MainnetProductionAttestationRequired.

R4. RemoteSigner attestation rejected as unavailable.

R5. KMS attestation rejected as unavailable.

R6. HSM attestation rejected as unavailable.

R7. cloud KMS attestation rejected as unavailable.

R8. PKCS#11 HSM attestation rejected as unavailable.

R9. production attestation rejected as unavailable.

R10. MainNet production attestation rejected as unavailable.

R11. unknown attestation class rejected.

R12. wrong environment rejected.

R13. wrong chain rejected.

R14. wrong genesis rejected.

R15. wrong authority root rejected.

R16. wrong signing-key fingerprint rejected.

R17. wrong custody class rejected.

R18. wrong backend/provider/signer id rejected.

R19. wrong key id / key label rejected.

R20. wrong suite rejected.

R21. wrong lifecycle action rejected.

R22. wrong candidate digest rejected.

R23. wrong authority-domain sequence rejected.

R24. wrong governance proof digest rejected.

R25. wrong request digest rejected.

R26. wrong response digest rejected.

R27. wrong transcript digest rejected.

R28. stale/replayed attestation rejected.

R29. expired attestation rejected.

R30. malformed attestation evidence rejected.

R31. unsupported attestation version rejected.

R32. invalid attestation commitment rejected.

R33. local operator cannot satisfy production attestation.

R34. peer majority cannot satisfy production attestation.

R35. attestation valid but custody metadata invalid rejected.

R36. custody valid but attestation invalid rejected.

R37. lifecycle + governance + custody valid but production attestation unavailable rejected.

R38. validation-only rejection remains non-mutating.

R39. mutating preflight rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

R40. MainNet peer-driven apply remains refused even with fixture attestation.

Required tests:
Add focused tests, preferably:

`crates/qbind-node/tests/run_205_custody_attestation_verifier_tests.rs`

Minimum coverage:

* A1–A14.
* R1–R40.
* deterministic digest tests.
* evidence/input/transcript binding tests.
* fixture-vs-production attestation separation.
* cloud KMS / PKCS#11 / production attestation unavailable tests.
* malformed evidence fail-closed tests.
* no-I/O guarantee for production attestation path.
* no-mutation guarantee.
* MainNet refusal invariant.
* compatibility with Run 188 custody, Run 201 RemoteSigner transport, and Run 203 KMS/HSM backend paths.

Required documentation:
Add:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_205.md`

Update:

* `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
* `docs/whitepaper/contradiction.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

Documentation must state:

* Run 205 is source/test production custody attestation verifier skeleton work.
* No real cloud KMS attestation verifier is implemented.
* No real PKCS#11 attestation verifier is implemented.
* No real HSM vendor attestation verifier is implemented.
* Fixture attestation is DevNet/TestNet source/test only.
* Production attestation remains unavailable/fail-closed.
* Custody attestation verifier does not enable MainNet peer-driven apply.
* RemoteSigner and KMS/HSM remain backend-boundary only.
* governance execution remains unimplemented.
* real on-chain proof verification remains unimplemented.
* validator-set rotation remains open.
* release-binary custody-attestation verifier-boundary evidence is deferred to Run 206.
* full C4 remains open.
* C5 remains open.

Validation commands:
Run at minimum:

* `cargo build -p qbind-node --lib`
* `cargo test -p qbind-node --test run_205_custody_attestation_verifier_tests`
* `cargo test -p qbind-node --test run_203_kms_hsm_backend_boundary_tests`
* `cargo test -p qbind-node --test run_201_remote_signer_transport_boundary_tests`
* `cargo test -p qbind-node --test run_198_remote_signer_policy_selector_tests`
* `cargo test -p qbind-node --test run_196_remote_signer_payload_callsite_tests`
* `cargo test -p qbind-node --test run_194_remote_authority_signer_boundary_tests`
* `cargo test -p qbind-node --test run_192_authority_custody_policy_selector_tests`
* `cargo test -p qbind-node --test run_190_authority_custody_payload_callsite_tests`
* `cargo test -p qbind-node --test run_188_authority_custody_boundary_tests`
* `cargo test -p qbind-node --test run_186_onchain_governance_production_verifier_boundary_tests`
* `cargo test -p qbind-node --test run_184_onchain_governance_payload_carrying_tests`
* `cargo test -p qbind-node --test run_182_onchain_governance_production_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_180_onchain_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_178_onchain_governance_proof_tests`
* `cargo test -p qbind-node --test run_176_live_0x05_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_173_validation_only_governance_required_policy_tests`
* `cargo test -p qbind-node --test run_171_governance_required_policy_selector_tests`
* `cargo test -p qbind-node --test run_169_governance_proof_loader_surface_integration_tests`
* `cargo test -p qbind-node --test run_167_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_165_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_163_governance_authority_verifier_tests`
* `cargo test -p qbind-node --test run_161_lifecycle_marker_integration_tests`
* `cargo test -p qbind-node --test run_159_authority_signing_key_lifecycle_tests`
* `cargo test -p qbind-node --test run_157_unified_testnet_fixture_universe_tests`
* `cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests`
* `cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests`
* `cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests`
* `cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests`
* `cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests`
* `cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Acceptance criteria:
Run 205 is acceptable only if:

1. A typed custody attestation verifier skeleton exists.
2. Fixture attestation is DevNet/TestNet source-test only.
3. Production/cloud/PKCS#11/HSM vendor attestation remains unavailable/fail-closed.
4. Evidence/input/transcript digests are deterministic and domain-bound.
5. Attestation verifier composes with Run 188 custody metadata.
6. Attestation verifier composes with Run 201 RemoteSigner transport and Run 203 KMS/HSM backend boundaries where feasible.
7. Validation-only surfaces remain non-mutating.
8. Mutating rejection paths produce no mutation.
9. MainNet peer-driven apply remains refused.
10. Docs defer release-binary custody-attestation verifier-boundary evidence to Run 206.
11. No real KMS/HSM attestation / RemoteSigner backend / governance execution / validator-set rotation claim is made.
12. No full C4 or C5 closure is claimed.
