Task: Run 216 — Release-Binary Governance Execution Policy Selector Evidence

Current accepted state:

* Run 211: source/test governance execution policy boundary landed.
* Run 212: release-binary governance execution policy-boundary evidence fully closed.
* Run 213: source/test governance execution payload/carrying and production-context wiring landed.
* Run 214: release-binary governance execution payload/carrying evidence fully closed.
* Run 215: source/test hidden governance execution policy selector landed:

  * module `pqc_governance_execution_policy_surface.rs`;
  * hidden CLI flag:
    `--p2p-trust-bundle-governance-execution-policy <POLICY>`;
  * env var:
    `QBIND_P2P_TRUST_BUNDLE_GOVERNANCE_EXECUTION_POLICY=<POLICY>`;
  * supported values:
    `disabled`;
    `fixture-governance-allowed`;
    `emergency-council-fixture-allowed`;
    `production-governance-required`;
    `mainnet-governance-required`;
  * CLI-over-env precedence;
  * invalid values fail closed with typed parse errors;
  * default remains `GovernanceExecutionPolicy::Disabled`;
  * seven preflight wrappers route selected policy into all seven Run 213 production v2 marker-decision contexts;
  * 55 tests passed;
  * release-binary evidence deferred to Run 216.
* No real governance execution engine exists.
* No real on-chain governance proof verifier exists.
* No MainNet governance enablement exists.
* Validator-set rotation remains unsupported.
* No real KMS/HSM backend exists.
* No real RemoteSigner backend exists.
* MainNet peer-driven apply remains refused.
* Full C4 remains OPEN.
* C5 remains OPEN.

Objective:
Produce release-binary evidence that real `target/release/qbind-node` surfaces exercise the hidden governance execution policy selector from Run 215 and route the selected policy into production preflight contexts.

This run must prove:

1. default behavior remains `GovernanceExecutionPolicy::Disabled`;
2. hidden CLI selector works;
3. env selector works;
4. CLI-over-env precedence is deterministic;
5. invalid selector values fail closed;
6. selected policy reaches production preflight contexts;
7. fixture governance execution passes only where policy allows;
8. emergency council fixture execution is explicit and non-production;
9. production/on-chain/MainNet governance execution remains fail-closed as unavailable;
10. MainNet peer-driven apply remains refused even with `mainnet-governance-required`;
11. no real governance execution engine, on-chain proof verifier, KMS-HSM backend, RemoteSigner backend, or validator-set rotation is claimed.

Strict scope:

* Release-binary evidence only.
* Use real `target/release/qbind-node`.
* Use release-built helper(s) to mint governance-execution-carrying sidecars / peer-candidate material.
* No production source change unless a tiny harness-only fix is required.
* No real governance execution engine.
* No real on-chain governance proof verifier.
* No MainNet governance enablement.
* No MainNet peer-driven apply enablement.
* No validator-set rotation.
* No authority-set rotation beyond existing lifecycle boundary checks.
* No real KMS implementation.
* No real HSM implementation.
* No real RemoteSigner backend.
* No production signing key custody.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* No schema/wire change.
* No authority-marker schema change.
* No sequence-file schema change.
* No trust-bundle core schema change.
* No authority lifecycle semantics change.
* Do not weaken Runs 070, 130–215.
* Do not claim full C4 or C5 closure.

Required deliverables:

1. Add release-binary helper:
   `crates/qbind-node/examples/run_216_governance_execution_policy_release_binary_helper.rs`

2. Add release-binary harness:
   `scripts/devnet/run_216_governance_execution_policy_release_binary.sh`

3. Add evidence archive:
   `docs/devnet/run_216_governance_execution_policy_release_binary/`

4. Add canonical evidence report:
   `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_216.md`

5. Update narrowly:

   * `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
   * `docs/whitepaper/contradiction.md`
   * `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
   * `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
   * `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

6. Crosscheck all new/modified code and docs against existing design/spec. If contradictions or inconsistencies are found, record them in `contradiction.md` before finalizing.

Required release-built helper matrix:
The helper must exercise the Run 215 governance execution policy selector and Run 213 governance-execution payload/call-site routing in release mode.

Accepted / compatible selector cases:
A1. unset CLI/env resolves to `GovernanceExecutionPolicy::Disabled`.

A2. CLI `disabled` resolves to Disabled.

A3. env `disabled` resolves to Disabled.

A4. CLI `fixture-governance-allowed` resolves to FixtureGovernanceAllowed and accepts DevNet fixture governance execution.

A5. env `fixture-governance-allowed` resolves to FixtureGovernanceAllowed and accepts TestNet fixture governance execution.

A6. CLI `emergency-council-fixture-allowed` resolves to EmergencyCouncilFixtureAllowed and accepts DevNet emergency council fixture execution only for explicit emergency action.

A7. CLI `production-governance-required` resolves to ProductionGovernanceRequired and reaches production governance unavailable outcome.

A8. env `mainnet-governance-required` resolves to MainnetGovernanceRequired and reaches MainNet governance unavailable / MainNet refusal outcome.

A9. CLI-over-env precedence is deterministic:

* env sets `fixture-governance-allowed`;
* CLI sets `disabled`;
* resolved policy is Disabled.

A10. no-governance-execution payload remains compatible under default Disabled.

A11. GenesisBound / EmergencyCouncil / OnChainGovernance proof-carrier behavior remains unchanged when governance execution policy is Disabled.

A12. Run 193 custody-policy selector behavior remains compatible.

A13. Run 199 RemoteSigner-policy selector behavior remains compatible.

A14. Run 210 custody-attestation-policy selector behavior remains compatible.

A15. lifecycle rotate action is authorized only when selected policy and carried decision authorize rotate with matching candidate digest and sequence.

A16. lifecycle revoke action is authorized only when selected policy and carried decision authorize revoke with matching candidate/revoked-key material and sequence.

Required rejection cases:
R1. invalid CLI selector value rejected with typed parse error.

R2. invalid env selector value rejected with typed parse error.

R3. unrelated CLI/env does not enable governance execution policy.

R4. no-governance-execution payload rejected under FixtureGovernanceAllowed.

R5. no-governance-execution payload rejected under ProductionGovernanceRequired.

R6. fixture governance rejected under ProductionGovernanceRequired.

R7. emergency fixture rejected under ProductionGovernanceRequired.

R8. fixture governance rejected under MainnetGovernanceRequired.

R9. production governance rejected as unavailable.

R10. on-chain governance rejected as unavailable.

R11. MainNet governance rejected as unavailable.

R12. unknown governance class rejected.

R13. malformed governance execution material rejected.

R14. wrong environment rejected.

R15. wrong chain rejected.

R16. wrong genesis rejected.

R17. wrong authority root rejected.

R18. wrong lifecycle action rejected.

R19. wrong candidate digest rejected.

R20. wrong authority-domain sequence rejected.

R21. wrong governance proof digest rejected.

R22. wrong on-chain proof digest rejected.

R23. wrong custody attestation digest rejected.

R24. wrong proposal id rejected.

R25. wrong decision id rejected.

R26. wrong effective epoch rejected.

R27. expired decision rejected.

R28. stale/replayed decision rejected.

R29. quorum threshold insufficient rejected.

R30. emergency action not authorized rejected.

R31. validator-set rotation unsupported rejected.

R32. policy-change action unsupported rejected.

R33. local operator cannot satisfy governance execution.

R34. peer majority / gossip count cannot satisfy governance execution.

R35. governance valid but lifecycle action mismatch rejected.

R36. lifecycle valid but governance decision invalid rejected.

R37. validation-only rejection writes no marker and no sequence.

R38. mutating rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

R39. invalid live inbound `0x05` governance-execution candidate is not propagated, staged, or applied, if live surface is included.

R40. MainNet peer-driven apply remains refused even with MainnetGovernanceRequired and fixture governance approval.

Required real binary checks:
Use real `target/release/qbind-node` to prove:

* hidden CLI flag is present but hidden from normal help, if feasible to check.
* env selector activates governance execution policy.
* CLI selector activates governance execution policy.
* CLI-over-env precedence works.
* invalid selector value fails closed.
* MainNet peer-driven apply remains refused.
* default behavior does not expose or enable governance execution.
* no `governance execution active` / `production governance active` / `MainNet governance enabled` claim is emitted.
* no real on-chain governance proof verifier active claim is emitted.
* existing custody-attestation policy selector behavior from Run 210 remains compatible.
* existing KMS/HSM backend behavior from Run 204 remains compatible.
* existing RemoteSigner transport behavior from Run 202 remains compatible.
* existing custody policy selector behavior from Run 193 remains compatible.
* existing governance proof carrier paths remain compatible.
* no validator-set rotation claim is emitted.

Required source/release reachability proof:
The report must include source grep/call-site proof for:

* `pqc_governance_execution_policy_surface`;
* `QBIND_P2P_TRUST_BUNDLE_GOVERNANCE_EXECUTION_POLICY`;
* `p2p_trust_bundle_governance_execution_policy`;
* `governance_execution_policy_from_selector`;
* `governance_execution_policy_from_cli_or_env`;
* seven per-surface governance-execution-policy preflight wrappers;
* `GovernanceExecutionPolicy::{Disabled, FixtureGovernanceAllowed, EmergencyCouncilFixtureAllowed, ProductionGovernanceRequired, MainnetGovernanceRequired}`;
* Run 213 governance-execution payload routing helpers;
* production/on-chain/MainNet governance unavailable variants;
* MainNet governance refusal helper.

Required mutation / no-mutation proof:
For rejected governance-execution-policy scenarios:

* no Run 070 apply call;
* no live trust swap;
* no session eviction;
* no sequence write;
* no marker write;
* marker bytes unchanged where present;
* sequence bytes unchanged where present;
* no `.tmp` residue;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead.

For any accepted fixture governance mutating compatibility scenario:

* selector resolution occurs before governance-execution material parse;
* governance-execution material parse occurs before marker decision;
* governance execution evaluation occurs before apply/mutation;
* lifecycle validation occurs before apply/mutation;
* governance proof validation occurs before apply/mutation where applicable;
* custody validation occurs before apply/mutation where applicable;
* custody attestation validation occurs before apply/mutation where applicable;
* Run 055 sequence commit succeeds if a mutating path is exercised;
* v2 marker persists strictly after sequence commit.

Required denylist:
Across the run prove:

* no MainNet apply;
* no autonomous apply;
* no apply on receipt;
* no peer-majority authority;
* no real governance execution claim;
* no production governance active claim;
* no MainNet governance enabled claim;
* no real on-chain governance proof verifier claim;
* no real KMS backend claim;
* no real HSM backend claim;
* no real RemoteSigner backend claim;
* no custody attestation production-active claim;
* no validator-set rotation claim;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead;
* no schema/wire/metric drift;
* no marker write before sequence commit;
* no sequence write on validation-only surfaces;
* no marker write on validation-only surfaces.

Required captured metadata:

* qbind-node SHA-256 and ELF Build ID;
* helper SHA-256 and ELF Build ID;
* git commit hash;
* rustc/cargo versions;
* exact commands;
* stdout/stderr logs;
* per-scenario exit codes;
* governance execution policy values;
* governance-execution fixture input/decision/payload paths and hashes;
* marker/sequence JSON and SHA before/after where applicable;
* data-dir inventories;
* denylist grep results.

Validation commands:
Run at minimum:

* `cargo build --release -p qbind-node --bin qbind-node`
* `cargo build --release -p qbind-node --example run_216_governance_execution_policy_release_binary_helper`
* `bash scripts/devnet/run_216_governance_execution_policy_release_binary.sh`
* `cargo test -p qbind-node --test run_215_governance_execution_policy_selector_tests`
* `cargo test -p qbind-node --test run_213_governance_execution_payload_callsite_tests`
* `cargo test -p qbind-node --test run_211_governance_execution_policy_tests`
* `cargo test -p qbind-node --test run_209_custody_attestation_policy_selector_tests`
* `cargo test -p qbind-node --test run_207_custody_attestation_payload_callsite_tests`
* `cargo test -p qbind-node --test run_205_custody_attestation_verifier_tests`
* `cargo test -p qbind-node --test run_203_kms_hsm_backend_boundary_tests`
* `cargo test -p qbind-node --test run_201_remote_signer_transport_boundary_tests`
* `cargo test -p qbind-node --test run_198_remote_signer_policy_selector_tests`
* `cargo test -p qbind-node --test run_196_remote_signer_payload_callsite_tests`
* `cargo test -p qbind-node --test run_194_remote_authority_signer_boundary_tests`
* `cargo test -p qbind-node --test run_192_authority_custody_policy_selector_tests`
* `cargo test -p qbind-node --test run_190_authority_custody_payload_callsite_tests`
* `cargo test -p qbind-node --test run_188_authority_custody_boundary_tests`
* `cargo test -p qbind-node --test run_186_onchain_governance_production_verifier_boundary_tests`
* `cargo test -p qbind-node --test run_184_onchain_governance_payload_carrying_tests`
* `cargo test -p qbind-node --test run_182_onchain_governance_production_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_180_onchain_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_178_onchain_governance_proof_tests`
* `cargo test -p qbind-node --test run_176_live_0x05_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_173_validation_only_governance_required_policy_tests`
* `cargo test -p qbind-node --test run_171_governance_required_policy_selector_tests`
* `cargo test -p qbind-node --test run_169_governance_proof_loader_surface_integration_tests`
* `cargo test -p qbind-node --test run_167_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_165_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_163_governance_authority_verifier_tests`
* `cargo test -p qbind-node --test run_161_lifecycle_marker_integration_tests`
* `cargo test -p qbind-node --test run_159_authority_signing_key_lifecycle_tests`
* `cargo test -p qbind-node --test run_157_unified_testnet_fixture_universe_tests`
* `cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests`
* `cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests`
* `cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests`
* `cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests`
* `cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests`
* `cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Documentation requirements:
`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_216.md` must state:

* Run 216 is release-binary governance execution policy selector evidence.
* Default remains `GovernanceExecutionPolicy::Disabled`.
* Hidden CLI/env selectors activate governance execution policies.
* CLI-over-env precedence is deterministic.
* Invalid values fail closed.
* Fixture governance execution remains DevNet/TestNet evidence-only.
* Emergency council fixture execution is explicit and non-production.
* Fixture governance cannot satisfy MainNet production governance execution.
* Production/on-chain/MainNet governance execution remains unavailable/fail-closed.
* MainNet peer-driven apply remains refused.
* No real governance execution engine is implemented.
* No real on-chain governance proof verifier is implemented.
* No real KMS/HSM backend is implemented.
* No real RemoteSigner backend is implemented.
* validator-set rotation remains unsupported.
* existing custody/KMS-HSM/RemoteSigner/custody-attestation/governance proof paths remain compatible.
* full C4 remains open.
* C5 remains open.

Acceptance criteria:
Run 216 is acceptable only if:

1. Real release binaries exercise the hidden governance execution policy selector.
2. Default Disabled remains proven.
3. CLI selector and env selector both work.
4. CLI-over-env precedence is proven.
5. Invalid selector values fail closed.
6. Fixture governance execution passes only where selected policy allows.
7. Emergency council fixture execution remains explicit and non-production.
8. Production/on-chain/MainNet governance execution remains fail-closed as unavailable.
9. MainNet peer-driven apply remains refused even with MainnetGovernanceRequired and fixture governance approval.
10. Rejected governance-execution-policy cases produce no mutation.
11. Existing custody/KMS-HSM/RemoteSigner/custody-attestation/governance proof paths remain compatible.
12. No real governance execution / on-chain proof verifier / KMS-HSM backend / RemoteSigner backend / validator-set rotation claim is made.
13. No full C4 or C5 closure is claimed.
