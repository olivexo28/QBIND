Task: Run 192 — Source/Test Authority Custody Policy Selector and Production Preflight Integration

Current accepted state:

* Run 188: source/test authority-custody boundary landed:

  * `pqc_authority_custody.rs`;
  * typed custody classes;
  * typed custody policies;
  * `AuthorityCustodyAttestation`;
  * pure custody validator;
  * pure lifecycle + governance + custody helper.
* Run 189: release-binary custody-boundary evidence fully closed.
* Run 190: source/test authority-custody metadata carrying and production-context routing landed:

  * `pqc_authority_custody_payload_carrying.rs`;
  * `AuthorityCustodyAttestationWire`;
  * `AuthorityCustodyLoadStatus`;
  * sibling extraction;
  * combined Run 167 + Run 184 + Run 190 sidecar loader;
  * seven per-surface routing helpers;
  * 55 integration tests + 10 in-module tests.
* Run 191: release-binary authority-custody metadata carrying evidence fully closed:

  * real `target/release/qbind-node` preserves default `AuthorityCustodyPolicy::Disabled`;
  * no custody selector is exposed at the binary surface;
  * release-built helper proves Run 190 A1–A10 / R1–R32 corpus;
  * KMS/HSM/RemoteSigner placeholders fail closed;
  * fixture/local custody cannot satisfy MainNet production custody;
  * MainNet peer-driven apply remains refused;
  * no real KMS/HSM/cloud-KMS/PKCS#11/remote-signer backend;
  * no governance execution;
  * no real on-chain proof verification;
  * no validator-set rotation;
  * full C4 remains open;
  * C5 remains open.
* Current limitation:
  custody metadata can be carried and validated through helper/production library paths, but no hidden production preflight selector exists to choose `AuthorityCustodyPolicy::{FixtureOnly, DevnetLocalAllowed, TestnetLocalAllowed, ProductionCustodyRequired, MainnetProductionCustodyRequired}` at binary/source call sites.
* Release-binary custody-policy selector evidence is deferred to Run 193.

Objective:
Add a source/test-only hidden authority-custody policy selector and wire it into production v2 marker-decision preflight contexts.

This run must allow DevNet/TestNet evidence surfaces to explicitly require fixture/local custody metadata, while preserving `AuthorityCustodyPolicy::Disabled` by default and preserving fail-closed MainNet production-custody behavior.

Strict scope:

* Source/test only.
* No release-binary harness in this run.
* Hidden selector only.
* Disabled by default.
* No real KMS implementation.
* No real HSM implementation.
* No cloud KMS integration.
* No PKCS#11 integration.
* No remote signer production integration.
* No MainNet peer-driven apply enablement.
* No governance execution engine.
* No real on-chain proof verifier.
* No validator-set rotation.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* No schema/wire changes.
* No authority-marker schema change.
* No sequence-file schema change.
* No trust-bundle core schema change.
* Do not weaken Runs 070, 130–191.
* Do not claim full C4 or C5 closure.

Required selector design:
Add a hidden authority-custody policy selector. Prefer one hidden CLI flag plus one env var, or env-only if keeping CLI surface minimal.

Acceptable shape:

CLI:

* `--p2p-trust-bundle-authority-custody-policy <disabled|fixture-only|devnet-local-allowed|testnet-local-allowed|production-custody-required|mainnet-production-custody-required>`
* hidden via clap.

Env:

* `QBIND_P2P_TRUST_BUNDLE_AUTHORITY_CUSTODY_POLICY=disabled|fixture-only|devnet-local-allowed|testnet-local-allowed|production-custody-required|mainnet-production-custody-required`

Required default:

* unset CLI/env => `AuthorityCustodyPolicy::Disabled`.

Required policy constraints:

* `FixtureOnly`:

  * DevNet/TestNet evidence only.
  * MainNet must reject fixture custody before it can satisfy production custody.
* `DevnetLocalAllowed`:

  * DevNet only.
  * Must reject on TestNet/MainNet.
* `TestnetLocalAllowed`:

  * TestNet only.
  * Must reject on DevNet/MainNet unless explicitly documented otherwise.
* `ProductionCustodyRequired`:

  * must fail closed because KMS/HSM/RemoteSigner production backends are unavailable.
* `MainnetProductionCustodyRequired`:

  * must fail closed.
  * MainNet peer-driven apply remains refused regardless.
* invalid selector values:

  * fail closed or reject startup/preflight with typed parse error.
* unrelated flags/env vars must not enable custody policy.

Required source behavior:

1. Add selector helpers, preferably in or near:

   * `pqc_authority_custody_payload_carrying.rs`;
   * or a new `pqc_authority_custody_policy_surface.rs`.

2. Helpers must include:

   * `authority_custody_policy_from_selector`;
   * `authority_custody_policy_from_cli_or_env`;
   * `authority_custody_policy_env_selector`;
   * names can differ, but must be grep-verifiable.

3. Wire selected custody policy into all seven production v2 marker-decision preflight contexts:

   * reload-check;
   * reload-apply;
   * startup `--p2p-trust-bundle`;
   * SIGHUP;
   * local peer-candidate-check;
   * live inbound `0x05`;
   * peer-driven drain.

4. The selected policy must reach the Run 190 custody payload/call-site routing layer.

5. Existing no-custody payloads remain compatible under default Disabled.

6. Under explicit custody policy:

   * missing custody metadata fails closed where custody is required;
   * valid fixture/local metadata passes only where policy permits;
   * KMS/HSM/RemoteSigner metadata reaches validator and fails closed as unavailable;
   * malformed metadata fails closed;
   * validation-only surfaces remain non-mutating;
   * mutating surfaces preserve sequence-before-marker ordering.

Required accepted scenarios:
A1. default selector absent => Disabled.

* old no-custody payload accepted where it was accepted before.

A2. CLI `fixture-only` selects `FixtureOnly`.

* DevNet fixture custody metadata accepted under source/test production-context path.

A3. env `fixture-only` selects `FixtureOnly`.

* TestNet fixture custody metadata accepted under source/test production-context path.

A4. CLI `devnet-local-allowed` selects `DevnetLocalAllowed`.

* DevNet local-operator custody metadata accepted.

A5. env `testnet-local-allowed` selects `TestnetLocalAllowed`.

* TestNet local-operator custody metadata accepted.

A6. `production-custody-required` selector reaches validator and fails closed with production custody unavailable for placeholders.

* This is an accepted selector parse / expected fail-closed validation outcome.

A7. `mainnet-production-custody-required` selector reaches validator and fails closed with MainNet production custody unavailable / MainNet refusal.

* This is an accepted selector parse / expected fail-closed validation outcome.

A8. custody selector wiring does not change GenesisBound / EmergencyCouncil / OnChainGovernance proof behavior when custody policy is Disabled.

A9. mutating DevNet fixture custody under FixtureOnly preflight accepts where lifecycle/governance/custody all pass and sequence-before-marker ordering remains governed by existing apply path.

A10. live inbound `0x05` receives selected policy if live config supports it; if not, document exact limitation.

Required rejection scenarios:
R1. invalid CLI selector value rejected.

R2. invalid env selector value rejected.

R3. unrelated CLI/env does not enable custody policy.

R4. no-custody payload rejected under FixtureOnly.

R5. no-custody payload rejected under DevnetLocalAllowed.

R6. fixture custody rejected under ProductionCustodyRequired.

R7. local operator custody rejected under ProductionCustodyRequired.

R8. fixture custody rejected on MainNet.

R9. local custody rejected on MainNet.

R10. DevNet local custody rejected under TestnetLocalAllowed.

R11. TestNet local custody rejected under DevnetLocalAllowed.

R12. KMS placeholder rejected as unavailable under ProductionCustodyRequired.

R13. HSM placeholder rejected as unavailable under ProductionCustodyRequired.

R14. RemoteSigner placeholder rejected as unavailable under ProductionCustodyRequired.

R15. malformed custody metadata rejected.

R16. wrong environment rejected.

R17. wrong chain rejected.

R18. wrong genesis rejected.

R19. wrong authority root rejected.

R20. wrong signing-key fingerprint rejected.

R21. wrong candidate digest rejected.

R22. wrong authority-domain sequence rejected.

R23. expired attestation rejected.

R24. custody key id mismatch rejected.

R25. unsupported custody suite rejected.

R26. validation-only rejection writes no marker and no sequence.

R27. mutating rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

R28. live inbound invalid custody metadata candidate is not propagated, staged, or applied.

R29. MainNet peer-driven apply remains refused even with MainnetProductionCustodyRequired and metadata claiming KMS/HSM.

Required tests:
Add focused tests, preferably:

`crates/qbind-node/tests/run_192_authority_custody_policy_selector_tests.rs`

Minimum coverage:

* A1–A10 where representable.
* R1–R29.
* selector parsing and precedence:

  * default;
  * CLI;
  * env;
  * CLI-over-env if both exist, or documented deterministic precedence;
  * invalid value fail-closed.
* source reachability:

  * selected policy reaches all seven production-context helpers where representable.
* no-mutation invariants:

  * validation-only non-mutation;
  * mutating rejection non-mutation.
* MainNet refusal invariants.

Required documentation:
Add:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_192.md`

Update:

* `docs/whitepaper/contradiction.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

Documentation must state:

* Run 192 is source/test hidden authority-custody policy selector and production preflight integration.
* Default remains `AuthorityCustodyPolicy::Disabled`.
* Fixture/local custody remains DevNet/TestNet evidence-only.
* Fixture/local custody cannot satisfy MainNet production custody.
* KMS/HSM/RemoteSigner placeholders remain fail-closed.
* MainNet peer-driven apply remains refused.
* No real KMS/HSM/cloud-KMS/PKCS#11/remote-signer backend is implemented.
* governance execution remains unimplemented.
* real on-chain proof verification remains unimplemented.
* validator-set rotation remains open.
* release-binary custody-policy selector evidence is deferred to Run 193.
* full C4 remains open.
* C5 remains open.

Validation commands:
Run at minimum:

* `cargo build -p qbind-node --lib`
* `cargo test -p qbind-node --test run_192_authority_custody_policy_selector_tests`
* `cargo test -p qbind-node --test run_190_authority_custody_payload_callsite_tests`
* `cargo test -p qbind-node --test run_188_authority_custody_boundary_tests`
* `cargo test -p qbind-node --test run_186_onchain_governance_production_verifier_boundary_tests`
* `cargo test -p qbind-node --test run_184_onchain_governance_payload_carrying_tests`
* `cargo test -p qbind-node --test run_182_onchain_governance_production_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_180_onchain_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_178_onchain_governance_proof_tests`
* `cargo test -p qbind-node --test run_176_live_0x05_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_173_validation_only_governance_required_policy_tests`
* `cargo test -p qbind-node --test run_171_governance_required_policy_selector_tests`
* `cargo test -p qbind-node --test run_169_governance_proof_loader_surface_integration_tests`
* `cargo test -p qbind-node --test run_167_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_165_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_163_governance_authority_verifier_tests`
* `cargo test -p qbind-node --test run_161_lifecycle_marker_integration_tests`
* `cargo test -p qbind-node --test run_159_authority_signing_key_lifecycle_tests`
* `cargo test -p qbind-node --test run_157_unified_testnet_fixture_universe_tests`
* `cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests`
* `cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests`
* `cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests`
* `cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests`
* `cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests`
* `cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Acceptance criteria:
Run 192 is acceptable only if:

1. A hidden authority-custody policy selector exists.
2. Default remains Disabled.
3. Selector reaches production preflight contexts at source/test level.
4. Valid DevNet/TestNet fixture/local custody metadata passes only where policy allows.
5. Missing/malformed/invalid custody metadata fails closed under explicit policy.
6. KMS/HSM/RemoteSigner placeholders remain fail-closed.
7. Validation-only surfaces remain non-mutating.
8. Mutating rejection surfaces produce no mutation.
9. MainNet peer-driven apply remains refused even with custody metadata claiming KMS/HSM.
10. Docs defer release-binary custody-policy selector evidence to Run 193.
11. No real KMS/HSM / governance execution / real on-chain proof / validator-set rotation claim is made.
12. No full C4 or C5 closure is claimed.
