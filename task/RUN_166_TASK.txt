Task: Run 166 — Release-Binary Governance Gate Enforcement Evidence for v2 Marker Decisions

Current accepted state:

* Run 163: source/test pure governance authority verifier landed.
* Run 164: partial-positive release-binary boundary showed the verifier worked through a release-built helper but had no production-surface callers.
* Run 165: source/test integration wired governance authority verification into the v2 lifecycle / marker-decision path:

  * `GovernanceProofPolicy`;
  * `GovernanceProofContext`;
  * `GovernanceMarkerGate`;
  * `evaluate_governance_marker_gate`;
  * `decide_v2_marker_acceptance_with_lifecycle_and_governance`;
  * typed errors `GovernanceAuthorityRejected` and `GovernanceAuthorityRequiredButMissing`;
  * all four mutating surfaces route through the helper with behavior-preserving `NotRequired` / `Unavailable` wiring.
* Run 165 deliberately did not change wire/schema formats.
* Existing v2 wire material carries no governance-proof fields.
* Therefore production surfaces can enforce the governance gate and fail closed when a proof is required but unavailable, but cannot yet carry a valid governance proof through `qbind-node` production surfaces without a future schema/carrying run.
* MainNet peer-driven apply remains refused.
* Governance execution / on-chain proof remains unimplemented.
* KMS/HSM remains unimplemented.
* validator-set rotation remains open.
* full C4 remains open.
* C5 remains open.

Objective:
Produce release-binary evidence that the Run 165 governance gate is reachable and enforced through real `target/release/qbind-node` v2 marker-decision surfaces.

This run must honestly prove:

1. governance-gate source reachability from production surfaces;
2. behavior-preserving `NotRequired` compatibility;
3. fail-closed `GovernanceAuthorityRequiredButMissing` behavior when a production surface requires governance proof but the existing wire cannot carry one;
4. no mutation on governance-gate rejection;
5. release-built helper evidence for the full Run 163 governance proof corpus remains green.

This run must not silently invent a governance-proof wire/schema path.

Strict scope:

* Release-binary evidence / boundary only.
* Use real `target/release/qbind-node`.
* Use release-built helper(s).
* No MainNet peer-driven apply enablement.
* No governance execution engine.
* No on-chain governance implementation.
* No KMS/HSM implementation.
* No validator-set rotation.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* No wire-format change.
* No marker schema change.
* No sequence-file schema change.
* No trust-bundle schema change.
* Do not weaken Runs 070, 130–165.
* Do not claim full C4 or C5 closure.

Required deliverables:

1. Add release-binary harness:
   scripts/devnet/run_166_governance_gate_release_binary_enforcement.sh

2. Add evidence archive:
   docs/devnet/run_166_governance_gate_release_binary_enforcement/

3. Add canonical evidence report:
   docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_166.md

4. Update narrowly:

   * docs/whitepaper/contradiction.md
   * docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md
   * docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md
   * docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md

5. Crosscheck all new/modified code and docs against existing design/spec. If contradictions or inconsistencies are found, record them in contradiction.md before finalizing.

Required investigation:
Before running scenarios, document all production surfaces and their governance gate wiring:

1. startup `--p2p-trust-bundle`;
2. reload-check validation-only;
3. local peer-candidate-check validation-only;
4. process-start reload-apply;
5. SIGHUP live reload;
6. live inbound `0x05`;
7. peer-driven drain / `ProductionV2MarkerCoordinator`.

For each surface, document:

* whether it routes through `decide_v2_marker_acceptance_with_lifecycle_and_governance`;
* whether policy is currently `NotRequired`, `Required`, or `Unavailable`;
* whether the surface can carry a real `GovernanceAuthorityProof`;
* expected behavior when proof is required but unavailable.

Required release-binary scenario matrix:

A1. reload-check with governance policy NotRequired remains compatible

* Use valid v2 lifecycle candidate that was accepted before Run 165.
* Expected:

  * release binary exits success;
  * marker decision path reached;
  * governance gate is NotRequired / compatibility path;
  * no sequence write;
  * no marker write;
  * validation-only semantics preserved.

A2. reload-apply with governance policy NotRequired remains compatible

* Use valid v2 lifecycle candidate on mutating reload-apply surface.
* Expected:

  * release binary accepts and applies according to existing Run 134/162 semantics;
  * Run 070 / Run 055 ordering preserved;
  * marker persists only after sequence commit;
  * governance gate does not weaken existing lifecycle/anti-rollback checks.

A3. reload-check with governance proof required but unavailable fails closed

* Configure or fixture the marker decision policy to require governance proof while the production wire remains unable to carry one.
* Expected:

  * release binary exits non-zero;
  * error contains `GovernanceAuthorityRequiredButMissing` or equivalent typed text;
  * no sequence write;
  * no marker write;
  * validation-only non-mutation preserved.

A4. reload-apply with governance proof required but unavailable fails closed

* Same candidate on mutating surface.
* Expected:

  * release binary exits non-zero;
  * governance-required-missing reason visible;
  * Run 070 apply is not called;
  * no live trust mutation;
  * no session eviction;
  * no sequence write;
  * no marker write;
  * existing marker bytes unchanged.

A5. startup `--p2p-trust-bundle` with governance proof required but unavailable fails closed, if the policy can be reached without adding schema

* Expected:

  * startup refuses before P2P/network startup;
  * no marker write;
  * no sequence write;
  * no trusted-root fallback.

A6. peer-driven drain with governance proof required but unavailable fails closed, if feasible on release binary without schema drift

* Expected:

  * candidate may stage;
  * drain refuses before Run 070 apply;
  * no live trust swap;
  * no session eviction;
  * no sequence write;
  * no marker write.
* If infeasible without changing production schema or harnessing complex live setup, document and cite Run 165 source/test coverage.

A7. release-built governance helper still proves full proof corpus

* Build and run `run_164_governance_authority_fixture_helper` or an updated helper.
* Expected:

  * A1–A5 / R1–R16 typed governance proof corpus passes;
  * this is explicitly classified as helper evidence, not production-surface proof-carrying evidence.

Required rejection matrix:
At minimum through helper evidence, and through production surface where representable:

* wrong environment rejected;
* wrong chain rejected;
* wrong genesis rejected;
* wrong authority root rejected;
* wrong lifecycle action rejected;
* wrong candidate digest rejected;
* wrong authority-domain sequence rejected;
* invalid issuer signature rejected;
* unsupported issuer suite rejected;
* non-PQC suite rejected;
* threshold not met rejected if representable;
* malformed proof rejected;
* stale/replayed proof rejected;
* OnChainGovernance unsupported/fail-closed;
* local operator config alone rejected;
* peer majority / gossip count rejected.

Required source-reachability proof:
The evidence report must include:

* grep/call-site proof that `evaluate_governance_marker_gate`, `GovernanceAuthorityRequiredButMissing`, or `validate_lifecycle_with_governance_authority` is reachable from production `src` marker-decision paths after Run 165;
* comparison against Run 164’s “zero production-surface caller” boundary;
* release-binary log proof that the governance gate is exercised, at minimum through RequiredButMissing.

Required negative invariants:
Across all production-surface scenarios:

* no MainNet apply;
* no autonomous apply;
* no apply on receipt;
* no peer-majority authority;
* no governance execution claim;
* no on-chain governance claim;
* no KMS/HSM claim;
* no validator-set rotation claim;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead;
* no schema/wire/metric drift;
* no marker write before sequence commit;
* no sequence write on validation-only surfaces;
* no marker write on validation-only surfaces;
* rejected governance-gate scenarios produce no mutation.

Required captured metadata:

* qbind-node SHA-256 and ELF Build ID;
* helper SHA-256 and ELF Build ID;
* git commit hash;
* rustc/cargo versions;
* exact commands;
* stdout/stderr logs;
* per-scenario exit codes;
* marker/sequence JSON and SHA before/after;
* data-dir inventories;
* denylist grep results.

Validation commands:
Run at minimum:

* cargo build --release -p qbind-node --bin qbind-node
* cargo build --release -p qbind-node --example run_164_governance_authority_fixture_helper
* cargo build --release -p qbind-node --example <needed v2/lifecycle fixture helper>
* bash scripts/devnet/run_166_governance_gate_release_binary_enforcement.sh
* cargo test -p qbind-node --test run_165_governance_marker_integration_tests
* cargo test -p qbind-node --test run_163_governance_authority_verifier_tests
* cargo test -p qbind-node --test run_161_lifecycle_marker_integration_tests
* cargo test -p qbind-node --test run_159_authority_signing_key_lifecycle_tests
* cargo test -p qbind-node --test run_157_unified_testnet_fixture_universe_tests
* cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests
* cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests
* cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests
* cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests
* cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests
* cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests
* cargo test -p qbind-node --lib pqc_authority
* cargo test -p qbind-node --lib

If exact test names differ, locate nearest existing targets and document exact commands/results.

Documentation requirements:
`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_166.md` must state:

* Run 166 is release-binary governance gate enforcement / boundary evidence.
* Governance verification is production-source reachable after Run 165.
* Existing production v2 wire material cannot yet carry real governance proofs.
* RequiredButMissing is fail-closed on release binaries where governance proof is required.
* The full governance proof corpus remains helper-evidenced, not production-surface proof-carrying evidenced.
* OnChainGovernance remains unsupported/fail-closed.
* No MainNet apply is enabled.
* governance execution/on-chain proof remains unimplemented.
* KMS/HSM remains unimplemented.
* validator-set rotation remains open.
* full C4 remains open.
* C5 remains open.
* Next required run is the governance-proof carrying/schema design or implementation run, likely Run 167, if production-surface accepted-governance-proof evidence is desired.

Acceptance criteria:
Run 166 is acceptable only if:

1. Real release binaries exercise the governance gate through production marker-decision surfaces.
2. RequiredButMissing or equivalent fail-closed behavior is visible on release binaries.
3. NotRequired compatibility remains proven for existing DevNet/TestNet flows.
4. Rejected governance-gate scenarios produce no mutation.
5. The helper corpus still proves the pure governance verifier.
6. The report does not overclaim accepted governance-proof enforcement through production surfaces unless a real proof-carrying path exists.
7. MainNet remains refused.
8. KMS-HSM/governance execution/validator-set rotation remain open.
9. The next required schema/carrying run is precisely identified if needed.
10. No full C4 or C5 closure is claimed.
