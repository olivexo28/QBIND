Task: Run 227 — Release-Binary Governance Evaluator Runtime Call-Site Wiring Evidence

Current accepted state:

* Run 220: source/test long-running governance-execution runtime consumption wiring landed.
* Run 221: release-binary runtime-consumption evidence fully closed.
* Run 222: source/test typed production governance execution evaluator interface boundary landed.
* Run 223: release-binary evaluator-interface evidence fully closed.
* Run 224: source/test governance evaluator runtime integration layer landed.
* Run 225: release-binary governance evaluator runtime integration evidence fully closed.
* Run 226: source/test governance evaluator runtime call-site wiring landed:

  * `wire_governance_evaluator_runtime_callsite`;
  * `wire_governance_evaluator_runtime_callsite_without_evaluator_context`;
  * typed `GovernanceEvaluatorRuntimeCallsiteFailClosed`;
  * `consume_run_220_governance_execution_runtime_outcome` in `main.rs` now routes reload-apply, startup `--p2p-trust-bundle`, reload-check, and local peer-candidate-check through the integration layer;
  * `consume_run_220_sighup_governance_execution_marker_decision` in `pqc_live_trust_reload.rs` routes SIGHUP through the integration layer;
  * default Disabled `ProceedLegacyBypass` preserved;
  * any present carrier without evaluator context fails closed;
  * live `0x05` and peer-driven drain full positive binding remain not-yet-representable from the binary and are deferred;
  * MainNet peer-driven apply remains refused;
  * 59 Run 226 tests passed;
  * release-binary call-site wiring evidence deferred to Run 227.
* Full C4 remains OPEN.
* C5 remains OPEN.

Objective:
Produce release-binary evidence that real `target/release/qbind-node` call-site paths route through the Run 224 governance evaluator runtime integration layer via the Run 226 call-site wiring.

This run must prove in release mode that:

1. representable runtime call sites consume `GovernanceEvaluatorRuntimeIntegrationOutcome`;
2. default Disabled legacy bypass remains preserved;
3. present governance-execution carrier without evaluator context fails closed;
4. production/on-chain/MainNet evaluators remain unavailable/fail-closed;
5. fixture/emergency fixture behavior remains non-production;
6. rejected call-site integration outcomes produce no mutation;
7. MainNet peer-driven apply remains refused;
8. live `0x05` and peer-driven drain limitations are documented honestly if still not fully representable.

Strict scope:

* Release-binary evidence only.
* Use real `target/release/qbind-node`.
* Use release-built helper(s) to mint fixture/evaluator/payload material where needed.
* No production source behavior change unless a tiny harness-only fix is required.
* No real governance execution engine.
* No real on-chain governance proof verifier.
* No MainNet governance enablement.
* No MainNet peer-driven apply enablement.
* No validator-set rotation.
* No KMS/HSM backend implementation.
* No RemoteSigner backend implementation.
* No schema/wire/marker/sequence/trust-bundle changes.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* Do not weaken Runs 070, 130–226.
* Do not claim full C4 or C5 closure.

Required deliverables:

1. Add release-binary helper:
   `crates/qbind-node/examples/run_227_governance_evaluator_runtime_callsite_wiring_release_binary_helper.rs`

2. Add release-binary harness:
   `scripts/devnet/run_227_governance_evaluator_runtime_callsite_wiring_release_binary.sh`

3. Add evidence archive:
   `docs/devnet/run_227_governance_evaluator_runtime_callsite_wiring_release_binary/`

4. Add canonical evidence report:
   `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_227.md`

5. Update narrowly:

   * `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
   * `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
   * `docs/whitepaper/contradiction.md`
   * `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
   * `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
   * `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

6. Crosscheck all new/modified code and docs against existing design/spec. If contradictions are found, record them in `contradiction.md`.

Required release-helper matrix:
The release-built helper must exercise the Run 226 call-site wiring symbols through production library code.

Accepted / compatible cases:
A1. reload-check default Disabled + absent carrier produces `ProceedLegacyBypass`.

A2. reload-apply default Disabled + absent carrier produces `ProceedLegacyBypass`.

A3. startup `--p2p-trust-bundle` default Disabled + absent carrier produces `ProceedLegacyBypass`.

A4. SIGHUP default Disabled + absent carrier produces `ProceedLegacyBypass`.

A5. local peer-candidate-check default Disabled + absent carrier produces `ProceedLegacyBypass`.

A6. reload-check with DevNet fixture policy and valid sidecar routes through integration and accepts only when runtime consumption and evaluator both agree.

A7. reload-apply with DevNet fixture policy and valid sidecar routes through integration and accepts only when runtime consumption and evaluator both agree.

A8. SIGHUP with DevNet fixture policy and valid sidecar routes through integration where representable.

A9. local peer-candidate-check with TestNet fixture policy and valid sidecar routes through integration where representable.

A10. explicit emergency fixture policy accepts only explicit emergency action.

A11. production evaluator path is reached from call-site integration and fails closed as unavailable.

A12. on-chain evaluator path is reached from call-site integration and fails closed as unavailable.

A13. MainNet evaluator path is reached from call-site integration and fails closed/refused.

A14. CLI-over-env precedence is preserved through call-site integration.

A15. invalid selector fails closed before mutation.

A16. live inbound `0x05` limitation is evidenced honestly if not representable; invalid candidates are not propagated/staged/applied where tested.

A17. peer-driven drain limitation is evidenced honestly if not representable; MainNet peer-driven apply remains refused.

A18. `GovernanceEvaluatorRuntimeIntegrationOutcome` is consumed, not discarded.

A19. `ProceedMutate` is the only mutation-authorizing integration outcome.

A20. `RuntimeConsumptionFailClosed`, `EvaluatorRejected`, and `MainNetPeerDrivenApplyRefused` fail closed before mutation.

A21. existing Run 221 runtime-consumption release behavior remains compatible.

A22. existing Run 223 evaluator-interface release behavior remains compatible.

A23. existing Run 225 integration-layer release behavior remains compatible.

Required rejection cases:
R1. missing governance-execution material rejected when explicit evaluator policy requires it.

R2. malformed governance-execution material rejected.

R3. wrong evaluator source rejected.

R4. wrong environment rejected.

R5. wrong chain rejected.

R6. wrong genesis rejected.

R7. wrong authority root rejected.

R8. wrong governance proof digest rejected.

R9. wrong on-chain proof digest rejected.

R10. wrong custody attestation digest rejected.

R11. wrong proposal id rejected.

R12. wrong decision id rejected.

R13. wrong lifecycle action rejected.

R14. wrong candidate digest rejected.

R15. wrong authority-domain sequence rejected.

R16. expired evaluator request rejected.

R17. stale/replayed evaluator request rejected.

R18. quorum/threshold insufficient rejected.

R19. emergency action not authorized rejected.

R20. validator-set rotation unsupported rejected.

R21. policy-change action unsupported rejected.

R22. production evaluator unavailable rejected.

R23. on-chain evaluator unavailable rejected.

R24. MainNet evaluator unavailable/refused rejected.

R25. local operator cannot satisfy evaluator policy.

R26. peer majority cannot satisfy evaluator policy.

R27. evaluator valid but governance execution decision invalid rejected.

R28. governance execution decision valid but evaluator response invalid rejected.

R29. validation-only rejection writes no marker and no sequence.

R30. mutating rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

R31. MainNet peer-driven apply remains refused even with fixture evaluator approval.

Required real binary checks:
Use real `target/release/qbind-node` to prove:

* hidden governance-execution selector still parses;
* invalid selector fails closed before mutation;
* default Disabled remains legacy-compatible;
* reload-check call-site wiring reaches integration where representable;
* reload-apply call-site wiring reaches integration where representable;
* startup `--p2p-trust-bundle` call-site wiring reaches integration where representable;
* SIGHUP call-site wiring reaches integration where feasible in harness;
* local peer-candidate-check call-site wiring reaches integration where representable;
* present carrier without evaluator context fails closed;
* MainNet peer-driven apply remains refused;
* no real governance engine active claim;
* no real on-chain proof verifier active claim;
* no MainNet governance enabled claim;
* no validator-set rotation claim;
* no KMS/HSM backend active claim;
* no RemoteSigner backend active claim;
* no peer-majority authority claim;
* no automatic apply / apply-on-receipt claim.

Required source/release reachability proof:
The report must include grep/call-site proof for:

* `wire_governance_evaluator_runtime_callsite`;
* `wire_governance_evaluator_runtime_callsite_without_evaluator_context`;
* `GovernanceEvaluatorRuntimeCallsiteFailClosed`;
* `consume_run_220_governance_execution_runtime_outcome`;
* `consume_run_220_sighup_governance_execution_marker_decision`;
* `GovernanceEvaluatorRuntimeIntegrationOutcome`;
* `ProceedLegacyBypass`;
* `ProceedMutate`;
* `RuntimeConsumptionFailClosed`;
* `EvaluatorRejected`;
* `MainNetPeerDrivenApplyRefused`;
* Run 220 runtime-consumption calls;
* Run 224 integration calls;
* Run 222 evaluator calls;
* Run 211 governance execution validation calls;
* Run 213 payload material usage;
* MainNet peer-driven guard;
* no mutation before integration success.

Required mutation / no-mutation proof:
For every rejected call-site integration case:

* no Run 070 apply call;
* no live trust swap;
* no session eviction;
* no sequence write;
* no marker write;
* no `.tmp` residue;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead.

Required denylist:
Across the run prove:

* no MainNet apply;
* no autonomous apply;
* no apply on receipt;
* no peer-majority authority;
* no real governance execution engine claim;
* no production governance active claim;
* no MainNet governance enabled claim;
* no real on-chain governance proof verifier claim;
* no real KMS backend claim;
* no real HSM backend claim;
* no real RemoteSigner backend claim;
* no custody attestation production-active claim;
* no validator-set rotation claim;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead;
* no schema/wire/metric drift;
* no marker write before sequence commit;
* no sequence write on validation-only surfaces;
* no marker write on validation-only surfaces.

Required captured metadata:

* helper SHA-256 and ELF Build ID;
* qbind-node SHA-256 and ELF Build ID;
* git commit hash;
* rustc/cargo versions;
* exact commands;
* stdout/stderr logs;
* per-scenario exit codes;
* evaluator source/request/response/transcript digests where used;
* governance execution payload digests;
* call-site integration outcome values;
* marker/sequence before/after hashes where applicable;
* data-dir inventories;
* denylist grep results.

Validation commands:
Run at minimum:

* `cargo build --release -p qbind-node --bin qbind-node`
* `cargo build --release -p qbind-node --example run_227_governance_evaluator_runtime_callsite_wiring_release_binary_helper`
* `bash scripts/devnet/run_227_governance_evaluator_runtime_callsite_wiring_release_binary.sh`
* `cargo test -p qbind-node --test run_226_governance_evaluator_runtime_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_224_governance_evaluator_runtime_integration_tests`
* `cargo test -p qbind-node --test run_222_governance_execution_evaluator_tests`
* `cargo test -p qbind-node --test run_220_governance_execution_runtime_consumption_tests`
* `cargo test -p qbind-node --test run_217_governance_execution_runtime_arming_tests`
* `cargo test -p qbind-node --test run_215_governance_execution_policy_selector_tests`
* `cargo test -p qbind-node --test run_213_governance_execution_payload_callsite_tests`
* `cargo test -p qbind-node --test run_211_governance_execution_policy_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Documentation requirements:
`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_227.md` must state:

* Run 227 is release-binary governance evaluator runtime call-site wiring evidence.
* Representable runtime call sites route through the integration layer in release evidence.
* Default Disabled legacy bypass is preserved.
* Present carrier without evaluator context fails closed.
* Production/on-chain/MainNet evaluators remain unavailable/fail-closed.
* Fixture evaluator remains DevNet/TestNet evidence-only.
* Emergency fixture evaluator is explicit and non-production.
* Live inbound `0x05` and peer-driven drain limitations are documented honestly if still not fully representable.
* MainNet peer-driven apply remains refused.
* Validator-set rotation remains unsupported.
* No real governance engine or on-chain proof verifier is implemented.
* Full C4 remains OPEN.
* C5 remains OPEN.

Acceptance criteria:
Run 227 is acceptable only if:

1. Release-built helper exercises the Run 226 call-site wiring.
2. Real release binary evidence proves representable call-site paths route through the integration layer.
3. Integration outcomes are consumed, not discarded.
4. Default Disabled legacy bypass remains preserved.
5. Present carrier without evaluator context fails closed.
6. Production/on-chain/MainNet evaluators remain unavailable/fail-closed.
7. Fixture/emergency fixture evaluators remain non-production.
8. Rejections are non-mutating.
9. MainNet peer-driven apply remains refused.
10. Validator-set rotation remains unsupported.
11. Existing Run 221, Run 223, and Run 225 release behavior remains compatible.
12. No full C4 or C5 closure is claimed.
