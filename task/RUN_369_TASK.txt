Run 369 — Wire per-peer message-rate limiting onto deployed TcpKemTls inbound path

Continue QBIND after accepted Run 368.

Current state:
Run 368 is accepted as PARTIAL-POSITIVE / public-DevNet-abuse-DoS-M12 per-peer live-socket release-binary positive at the AsyncPeerManagerImpl layer.

Run 368 proved:
- per-peer message-rate limiting over a real admitted-peer loopback socket at the AsyncPeerManagerImpl layer;
- 5/0 bucket accepted 5 under-budget frames with 0 drops;
- 80-frame over-budget flood dropped about 75 frames;
- qbind_net_per_peer_drops_total{...,reason="rate_limit"} / total_rate_limit_drops() incremented;
- Run 367 connection-rate live-socket proof was re-run successfully:
  10 inbound TCP connections, max 3 → 3 accepted / 7 refused,
  qbind_p2p_connection_rate_drop_total = 7;
- helper 9/9 scenarios passed;
- no production/library source changed;
- no new CLI flag;
- no P2P wire-format change;
- no peer-admission/trust-bundle weakening;
- no validator-set / epoch / sequence / LivePqcTrustState mutation;
- no TestNet/MainNet readiness;
- no C4/C5 closure.

Run 368 did NOT move M12 Green because the deployed qbind-node inbound path:
TcpKemTlsP2pService::subscribe → P2pInboundDemuxer → handlers
does not consult PeerRateLimiter, and AsyncPeerManagerImpl is never spawned by main.rs.

Current public DevNet readiness:
- Green: M1, M2, M3 same-host scope, M5, M10, M11, M16, M17, M18, M19, M20.
- Yellow/Partial: M4, M6, M7, M8, M9, M12, M13, M14, M15.
- M4 remains launch-blocking.
- Public DevNet remains NOT launch-ready.
- Full C4 OPEN.
- C5 OPEN.
- MainNet authority rotation/revocation Red.

Primary objective:
Wire the existing per-peer PeerRateLimiter onto the deployed TcpKemTls inbound receive path so deployed inbound peer frames are rate-limited before handler dispatch.

This is a source/test run only. Release-binary live-socket evidence is deferred to Run 370.

Strict scope:
- Per-peer message-rate limiter deployment wiring only.
- Preserve the existing hidden/devnet-only CLI/config surface from Runs 362/363.
- Preserve default behavior for normal traffic:
  - default per-peer limiter posture: 1000 msg/s + 100 burst;
  - connection-rate limiter remains disabled unless explicitly enabled.
- Do not add public CLI flags.
- Do not change P2P wire format.
- Do not weaken KEMTLS, trust-bundle admission, peer-admission, genesis binding, or mutual-auth policy.
- Do not alter connection-rate limiter semantics except as needed for composition tests.
- Do not launch public DevNet.
- Do not mark M12 Green in this run.
- Do not mark M4 or M6 Green.
- Do not claim TestNet/MainNet readiness.
- Do not close C4 or C5.
- Do not wire authority lifecycle.
- Do not mutate validator set, epoch, sequence, marker, LivePqcTrustState, or execution sink.
- Do not add fake live seed/bootnode endpoints.

Required investigation:
Inspect before implementation:

- crates/qbind-node/src/p2p_tcp.rs
- crates/qbind-node/src/p2p_inbound.rs
- crates/qbind-node/src/p2p_node_builder.rs
- crates/qbind-node/src/async_peer_manager.rs
- crates/qbind-node/src/peer_rate_limiter.rs
- crates/qbind-node/src/public_devnet_abuse_dos_runtime.rs
- crates/qbind-node/src/public_devnet_abuse_dos_config.rs
- crates/qbind-node/src/metrics.rs
- crates/qbind-node/src/metrics_http.rs
- crates/qbind-node/src/main.rs
- crates/qbind-node/src/cli.rs
- Run 361/362/363/365/368 tests and helpers
- docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_368.md
- docs/release/QBIND_PUBLIC_DEVNET_READINESS_CRITERIA.md
- docs/release/public-devnet/p2p/ABUSE_DOS_POSTURE.md
- docs/release/public-devnet/p2p/VERIFY.md

Implementation requirements:
Choose the smallest safe implementation path after investigation.

Preferred design:
- Add a deployed inbound per-peer limiter adapter used by the TcpKemTls inbound path.
- It should consume the same validated peer-rate configuration already derived from CliArgs / PublicDevnetAbuseDosRuntimeConfig / P2pNodeBuilder.
- It should default to the documented 1000 msg/s + 100 burst posture.
- It should evaluate rate limit before dispatching an inbound peer message to handlers.
- On over-budget message:
  - drop the message;
  - increment the existing per-peer rate-limit metric/counter path if available;
  - do not increment qbind_p2p_connection_rate_drop_total;
  - do not tear down the connection solely because of the per-peer limiter unless existing semantics already do so.
- On under-budget message:
  - dispatch exactly as before.
- If no stable peer identity is available at the demuxer boundary, define a narrow deterministic temporary peer-keying strategy suitable for rate limiting, document it honestly, and ensure it does not become an identity/authentication claim.
- If wiring through AsyncPeerManagerImpl is simpler and safer than duplicating checks in P2pInboundDemuxer, route deployed inbound frames through that existing receive path only if it does not change wire format, admission, or handler semantics.

Required source/test target:
Add:

crates/qbind-node/tests/run_369_public_devnet_deployed_per_peer_limiter_wiring_tests.rs

Test coverage must include:

Accepted / default compatibility:
1. default config derives 1000 msg/s + 100 burst for deployed inbound path;
2. no-flag normal under-budget messages dispatch as before;
3. custom --p2p-max-messages-per-second reaches deployed inbound limiter;
4. custom --p2p-burst-allowance reaches deployed inbound limiter;
5. under-budget deployed inbound messages are accepted/dispatched;
6. over-budget deployed inbound messages are dropped;
7. per-peer drop counter increments on over-budget deployed inbound message;
8. connection-rate metric does not increment on per-peer message drops;
9. connection-rate limiter remains independent and unchanged;
10. no new public CLI flags appear.

Rejection / fail-closed:
11. zero per-peer max rejected;
12. unsafe/unbounded per-peer max rejected;
13. unsafe/unbounded burst rejected;
14. invalid connection-rate config still rejected;
15. inconsistent per-address config still rejected;
16. MainNet abuse/DoS enablement refused;
17. invented CLI flags rejected.

Non-mutation / safety:
18. no P2P wire-format change;
19. no peer-admission weakening;
20. no trust-bundle behavior change;
21. no LivePqcTrustState mutation;
22. no sequence/marker write;
23. no validator-set mutation;
24. no epoch transition;
25. no Run 070 path;
26. no authority-lifecycle wiring;
27. no public DevNet launch claim;
28. no TestNet/MainNet readiness claim;
29. no C4/C5 closure claim.

Regression:
30. Run 368 AsyncPeerManagerImpl-layer helper semantics remain compatible;
31. Run 367 connection-rate behavior remains compatible at the source/test level;
32. Run 365 deployed builder config derivation remains compatible;
33. defaults are stable across direct PeerRateLimiter and deployed inbound adapter where applicable.

Required docs/evidence:
Add:

docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_369.md

Update narrowly:

- docs/release/QBIND_PUBLIC_DEVNET_READINESS_CRITERIA.md
- docs/release/public-devnet/p2p/ABUSE_DOS_POSTURE.md
- docs/release/public-devnet/p2p/VERIFY.md
- docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md
- docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md
- docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md
- docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md
- docs/whitepaper/contradiction.md

Evidence document must record:
1. exact verdict;
2. files changed;
3. deployed inbound wiring summary;
4. exact call path before and after Run 369;
5. peer identity/keying used for limiter buckets;
6. accepted tests;
7. rejection/fail-closed tests;
8. non-mutation tests;
9. default compatibility;
10. CLI/config surface;
11. metric/counter behavior;
12. readiness matrix delta for M12;
13. public DevNet readiness status;
14. remaining DevNet blockers;
15. TestNet blockers;
16. MainNet blockers;
17. C4/C5 status;
18. tests run with exact counts;
19. security scan result;
20. CodeQL result;
21. provenance;
22. honest limitations;
23. suggested Run 370.

Readiness matrix rules:
- M12 stays Yellow/Partial after Run 369 unless release-binary live-socket deployed-path evidence also lands.
- Run 369 may update M12 to “Yellow/Partial strengthened — deployed TcpKemTls receive-path source/test wiring landed.”
- M12 may move Green only in a later release-binary run if deployed live socket evidence proves both:
  1. connection-rate limiting over live sockets;
  2. per-peer message-rate limiting over the deployed TcpKemTls receive path.
- Do not alter M4 unless live seed/bootnode reachability evidence lands.
- Do not alter M6 unless stable identity-generation support lands.
- Public DevNet remains NOT launch-ready unless all must-haves are Green.

Required tests:
Run and record exact counts:

- cargo build -p qbind-node --lib
- cargo test -p qbind-node --test run_369_public_devnet_deployed_per_peer_limiter_wiring_tests
- cargo test -p qbind-node --test run_365_public_devnet_deployed_peer_rate_threading_tests
- cargo test -p qbind-node --test run_363_public_devnet_per_peer_message_rate_runtime_tests
- cargo test -p qbind-node --test run_362_public_devnet_abuse_dos_runtime_tests
- cargo test -p qbind-node --test run_361_public_devnet_abuse_dos_hardening_tests
- cargo test -p qbind-node --lib

Security:
Run secret scanning over all changed files.

No private keys, mnemonics, seed phrases, credentials, API keys, tokens, private infrastructure, real production hostnames, or unapproved live endpoints may be introduced.

Use localhost, RFC 5737, RFC 3849, and temporary test fixtures only.

CodeQL:
Because production Rust source is expected to change, invoke CodeQL if available.

If CodeQL is skipped, unavailable, timed out, database-too-large, or not meaningful, record the exact result and do not call it clean.

Expected verdict:
PASS / public-DevNet deployed per-peer limiter source-test positive if the deployed TcpKemTls inbound path now consults the per-peer limiter at source/test level with defaults preserved, metrics correct, and all regression/non-mutation tests passing.

Partial-positive if the wiring boundary is clarified but not completed.

Unacceptable:
- marking M12 Green in Run 369 without release-binary live-socket deployed-path evidence;
- changing P2P wire format;
- weakening peer admission;
- weakening KEMTLS or trust-bundle validation;
- exposing hidden abuse/DoS flags publicly;
- silently changing defaults;
- incrementing connection-rate metric for per-peer message drops;
- mutating LivePqcTrustState, sequence, validator set, epoch, marker, or authority state;
- claiming public DevNet launch-ready;
- claiming TestNet/MainNet readiness;
- closing C4 or C5;
- hiding skipped CodeQL;
- publishing secrets or private infrastructure.

Final response format:
1. Exact verdict.
2. Files changed.
3. Deployed inbound wiring summary.
4. Before/after call path.
5. Per-peer bucket identity/keying.
6. Default compatibility.
7. CLI/config surface.
8. Accepted tests.
9. Rejection/fail-closed tests.
10. Metric/counter behavior.
11. Connection-rate independence.
12. Non-mutation evidence.
13. Readiness matrix delta for M12.
14. Current public DevNet readiness status.
15. Remaining public DevNet blockers.
16. Public TestNet blockers.
17. MainNet blockers.
18. C4/C5 status.
19. Tests run.
20. Security scans.
21. CodeQL.
22. Provenance.
23. Honest limitations.
24. Suggested Run 370 next step.