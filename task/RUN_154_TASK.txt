Task: Run 154 — Source/Test TestNet Fixture Tooling for Peer-Driven Apply Evidence

Current accepted state:

* Run 153 landed release-binary end-to-end DevNet peer-driven apply evidence through the hidden drain-once hook.
* Run 153 made the full pipeline callable from a real release binary:
  live inbound 0x05 → validation-only v2 acceptance → staging queue → drain-once hook → ProductionDrainInvocationBuilder → ProductionV2MarkerCoordinator → Run 150 drain → Run 148 controller → Run 070 apply → LivePqcTrustState swap → session eviction → sequence commit → v2 marker persist after commit.
* Run 153 explicitly deferred TestNet A2 because TestNet fixture setup is not yet available.
* MainNet remains refused.
* Governance remains unimplemented.
* KMS/HSM remains unimplemented.
* signing-key rotation/revocation lifecycle remains open.
* validator-set rotation remains open.
* full C4 remains open.
* C5 remains open.

Objective:
Add the smallest TestNet fixture tooling required to produce signed TestNet trust-bundle material, v2 ratification sidecars, transport credentials, and peer-candidate 0x05 fixtures suitable for a future Run 155 release-binary TestNet peer-driven apply evidence harness.

This is source/test fixture tooling only. Do not run or claim release-binary TestNet end-to-end apply closure in this run.

Strict scope:

* Source/test fixture tooling only.
* No production MainNet enablement.
* No governance implementation.
* No KMS/HSM implementation.
* No signing-key rotation/revocation lifecycle.
* No validator-set rotation.
* No autonomous background drain.
* No automatic apply on receipt.
* No peer-majority authority.
* No new wire format.
* No trust-bundle schema change.
* No ratification sidecar schema change.
* No authority-marker schema change.
* No sequence-file schema change.
* No peer-candidate envelope schema change.
* Do not weaken Runs 070, 142, 143, 145–153.
* Do not claim full C4 or C5 closure.

Required deliverables:

1. Add or extend a fixture helper capable of minting TestNet material, preferably by extending the existing Run 133 v2 fixture helper rather than creating a parallel system.

2. The helper must generate:

   * TestNet genesis / runtime-domain metadata;
   * signed TestNet trust bundle;
   * ML-DSA-44 bundle-signing public key material;
   * v2 ratification sidecar bound to TestNet environment;
   * transport root / leaf credentials suitable for mutual-auth TestNet evidence;
   * valid v2 peer-candidate 0x05 fixture;
   * invalid negative fixtures:

     * lower sequence;
     * same-sequence different digest;
     * bad signature;
     * wrong environment;
     * wrong chain;
     * wrong genesis;
     * ambiguous v1+v2 material;
     * duplicate candidate;
     * expired candidate if the staging layer supports expiry testing.

3. Ensure every generated TestNet artifact is explicitly domain-bound:

   * environment = TestNet;
   * TestNet chain_id;
   * TestNet genesis hash;
   * correct authority root fingerprint;
   * correct v2 authority-domain sequence.

4. Ensure DevNet helper behavior remains unchanged.

5. Ensure MainNet helper behavior does not accidentally produce production-authoritative material. If the helper has a MainNet mode, it must remain clearly fixture-only and refused by production paths unless already allowed by prior evidence rules.

6. Add tests proving:

   * TestNet valid bundle verifies under TestNet context;
   * TestNet v2 ratification verifies under TestNet context;
   * TestNet peer-candidate validates under TestNet context;
   * same artifact fails under DevNet context;
   * same artifact fails under MainNet context unless explicitly fixture-only validation is intended;
   * wrong-chain / wrong-genesis variants fail;
   * bad-signature variant fails;
   * lower-sequence and same-sequence different-digest variants fail through v2 marker comparison;
   * helper output is deterministic enough for evidence harnesses or records non-deterministic fields explicitly;
   * no production source-code anchor is introduced;
   * no fallback root or fallback signing key is introduced.

Suggested test file:
crates/qbind-node/tests/run_154_testnet_peer_apply_fixture_tests.rs

Required documentation:
Add:

* docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_154.md

Update:

* docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md
* docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md
* docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md
* docs/whitepaper/contradiction.md

Documentation must state:

* Run 154 is source/test fixture tooling only.
* It closes the fixture-tooling blocker that caused Run 153 A2 TestNet evidence to be deferred.
* Release-binary TestNet end-to-end peer-driven apply evidence remains deferred to Run 155.
* MainNet remains refused.
* Governance remains unimplemented.
* KMS/HSM remains unimplemented.
* signing-key rotation/revocation lifecycle remains open.
* validator-set rotation remains open.
* full C4 remains open.
* C5 remains open.

Validation commands:
Run at minimum:

* cargo build -p qbind-node --lib
* cargo build -p qbind-node --example run_133_v2_validation_only_fixture_helper
* cargo test -p qbind-node --test run_154_testnet_peer_apply_fixture_tests
* cargo test -p qbind-node --test run_153_peer_driven_apply_end_to_end_release_binary_tests
  or the closest Run 153 / Run 152 test target if no Run 153 test target exists
* cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests
* cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests
* cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests
* cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests
* cargo test -p qbind-node --lib pqc_authority
* cargo test -p qbind-node --lib

Acceptance criteria:
Run 154 is acceptable only if:

1. TestNet fixture tooling exists for the Run 153 A2 evidence gap.
2. Valid TestNet v2 peer-candidate material can be generated.
3. Invalid TestNet negative fixtures can be generated.
4. TestNet artifacts are bound to TestNet environment, chain_id, and genesis hash.
5. DevNet fixture behavior remains unchanged.
6. MainNet remains refused.
7. No production authority anchor, fallback signing key, or schema drift is introduced.
8. Docs defer release-binary TestNet evidence to Run 155.
9. No full C4 or C5 closure is claimed.
