Run 275 — Release-binary durable-completion settlement-outcome report consumer / settlement-outcome publication boundary evidence

You are continuing QBIND after Run 274.

Current accepted state:

Run 274 is accepted as PASS / source-test positive.

Run 274 added the modeled durable-completion settlement-outcome report consumer / settlement-outcome publication interface boundary:

* `crates/qbind-node/src/pqc_governance_durable_completion_settlement_outcome_publication.rs`
* module registration in `crates/qbind-node/src/lib.rs`
* `crates/qbind-node/tests/run_274_durable_completion_settlement_outcome_publication_tests.rs`
* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_274.md`
* narrow protocol/ops doc updates
* `docs/whitepaper/contradiction.md` Run 274 entry

Run 274 proves:

* only Run 272 `SettlementOutcomeReportRecorded` creates a settlement-outcome publication request;
* Run 272 `SettlementOutcomeReportDuplicateIdempotent` may only match an already-recorded settlement-outcome publication record and never creates a new publication by itself;
* only `SettlementOutcomePublicationRecorded` authorizes modeled settlement-outcome publication state;
* duplicate identical settlement-outcome publication records are idempotent;
* same settlement-outcome publication record id with a different digest fails closed as equivocation;
* fixture settlement-outcome publication sink is DevNet/TestNet evidence-only and mutates only the in-memory `DurableCompletionSettlementOutcomePublicationLedger`;
* production/MainNet/external settlement-outcome publication sinks remain reachable but unavailable/fail-closed;
* rejected paths are non-mutating;
* no real settlement, settlement finality, settlement receipt, settlement-receipt acknowledgement, settlement-finality projection, settlement-outcome report backend, settlement-outcome publication, audit-ledger acknowledgement, external-publication confirmation, external publication, production backend, governance execution engine, on-chain proof verifier, KMS/HSM/RemoteSigner backend, MainNet governance, peer-driven apply, validator-set rotation, or policy-change enablement exists.

The full Run 274 validation corpus passed:

* `cargo build -p qbind-node --lib` — rc=0
* Run 274 test — 63 passed / 0 failed
* regression corpus Run 272 → Run 224 — all rc=0
* `cargo test -p qbind-node --lib pqc_authority` — 164 passed / 0 failed
* `cargo test -p qbind-node --lib` — 1365 passed / 0 failed

Full C4 remains OPEN. C5 remains OPEN.

Objective:

Produce release-binary evidence for the Run 274 durable-completion settlement-outcome report consumer / settlement-outcome publication interface boundary.

Run 275 must prove on real `target/release/qbind-node` plus a release-built helper that the Run 274 settlement-outcome publication production library symbols are present and exercised in release mode, while the helper remains dead code from production runtime and the modeled settlement-outcome publication path remains fixture/in-memory/dev-test only.

Strict scope:

* Release-binary evidence only.
* No new source/test feature boundary.
* No production runtime enablement.
* No real settlement backend.
* No real settlement execution.
* No real settlement finality.
* No real settlement receipt.
* No real settlement-receipt acknowledgement.
* No real settlement-finality projection.
* No real settlement-outcome report backend.
* No real settlement-outcome publication.
* No real settlement-outcome publication backend.
* No real settlement-outcome confirmation.
* No real audit-ledger acknowledgement.
* No real audit-ledger write.
* No real external publication.
* No real external-publication confirmation.
* No real production attestation/finalization/completion-report/durable-consume/persistent-replay backend.
* No real governance execution engine.
* No real production mutation engine.
* No real on-chain governance proof verifier.
* No KMS/HSM/RemoteSigner backend.
* No RocksDB/file/schema/migration/storage-format change.
* No wire/schema/marker/sequence/trust-bundle change.
* No Run 070 call.
* No `LivePqcTrustState` mutation.
* No real trust swap.
* No session eviction.
* No sequence write.
* No marker write.
* No network I/O.
* No MainNet governance.
* No MainNet peer-driven apply.
* No validator-set rotation.
* No policy-change enablement.
* Keep Full C4 OPEN.
* Keep C5 OPEN.

Required deliverables:

1. Add release helper:

   `crates/qbind-node/examples/run_275_durable_completion_settlement_outcome_publication_release_binary_helper.rs`

2. Add release harness:

   `scripts/devnet/run_275_durable_completion_settlement_outcome_publication_release_binary.sh`

3. Add evidence archive directory:

   `docs/devnet/run_275_durable_completion_settlement_outcome_publication_release_binary/`

   Track only:

   * `README.md`
   * `summary.txt`
   * `.gitignore`

   Generated logs, helper tables, reachability files, provenance files, and test logs must be regenerated and ignored.

4. Add canonical evidence report:

   `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_275.md`

5. Update narrowly:

   * `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
   * `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
   * `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
   * `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
   * `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
   * `docs/whitepaper/contradiction.md`

Required existing ordering:

1. Run 246 pipeline success before Run 248 sink intent.
2. Run 248 `ConsumeReceiptRecorded` before Run 250 completion report.
3. Run 250 `CompletionReportRecorded` before Run 252 finalization.
4. Run 252 `DurableCompletionFinalized` before Run 254 attestation.
5. Run 254 `DurableCompletionAttested` before Run 256 backend request.
6. Run 256 `BackendSubmissionRecorded` before Run 258 audit/publication receipt.
7. Run 258 `AuditReceiptRecorded` before Run 260 acknowledgement / confirmation.
8. Run 260 `AcknowledgementRecorded` before Run 262 acknowledgement-consumer request.
9. Run 262 `AcknowledgementConsumed` before Run 264 settlement-projection request.
10. Run 264 `SettlementProjectionRecorded` before Run 266 settlement-commitment request.
11. Run 266 `SettlementCommitmentRecorded` before Run 268 settlement-finalization request.
12. Run 268 `SettlementFinalizationRecorded` before Run 270 settlement-receipt acknowledgement request.
13. Run 270 `SettlementReceiptAcknowledgementRecorded` before Run 272 settlement-outcome report request.
14. Run 272 `SettlementOutcomeReportRecorded` before Run 274 settlement-outcome publication request.
15. Only Run 274 `SettlementOutcomePublicationRecorded` authorizes a new modeled settlement-outcome publication state.

Helper requirements:

The release helper must link against release-built production library symbols and exercise the actual Run 274 API, including the exact implemented names for:

* `pqc_governance_durable_completion_settlement_outcome_publication`
* `DurableCompletionSettlementOutcomePublicationKind`
* `DurableCompletionSettlementOutcomePublicationPolicy`
* `DurableCompletionSettlementOutcomePublicationIdentity`
* `DurableCompletionSettlementOutcomePublicationInput`
* `DurableCompletionSettlementOutcomePublicationExpectations`
* `DurableCompletionSettlementOutcomePublicationRequest`
* `DurableCompletionSettlementOutcomePublicationResponse`
* `DurableCompletionSettlementOutcomePublicationRecord`
* `DurableCompletionSettlementOutcomePublicationDigest`
* `DurableCompletionSettlementOutcomePublicationTranscriptDigest`
* `DurableCompletionSettlementOutcomePublicationOutcome`
* `GovernanceDurableCompletionSettlementOutcomePublicationSink`
* `FixtureDurableCompletionSettlementOutcomePublicationSink`
* `ProductionSettlementOutcomePublicationSink`
* `MainNetSettlementOutcomePublicationSink`
* `ExternalSettlementOutcomePublicationSink`
* `DurableCompletionSettlementOutcomePublicationLedger`
* `DurableCompletionSettlementOutcomePublicationLedgerRecord`
* `DurableCompletionSettlementOutcomePublicationLedgerSnapshot`
* `DurableCompletionSettlementOutcomePublicationLedgerStatus`
* `project_settlement_outcome_report_outcome_to_outcome_publication_request`
* `evaluate_durable_completion_settlement_outcome_publication`
* `recover_durable_completion_settlement_outcome_publication_window`
* `settlement_outcome_publication_outcome_authorizes_record`
* `settlement_outcome_publication_outcome_projects_to_recorded`
* all Run 274 grep-verifiable invariant helpers.

If exact symbol names differ, use the actual implemented Run 274 API names and record substitutions honestly in `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_275.md`. Do not add compatibility shims unless strictly necessary for compilation, and do not change production behavior.

Helper architecture:

Copy the Run 273 release helper structure and adapt it to Run 274.

The helper must attach to the full real modeled chain:

* Run 256 backend submission
* Run 258 audit receipt
* Run 260 acknowledgement
* Run 262 acknowledgement consumer
* Run 264 settlement projection
* Run 266 settlement commitment
* Run 268 settlement finalization
* Run 270 settlement-receipt acknowledgement
* Run 272 settlement-outcome report
* Run 274 settlement-outcome publication

Do not fabricate a standalone settlement-outcome publication request.

Critical projection rule:

The Run 275 helper must drive projection from the Run 272 settlement-outcome report outcome:

* `input.settlement_outcome_report_binding`
* `project_settlement_outcome_report_outcome_to_outcome_publication_request`

Do NOT project from Run 270 settlement-receipt acknowledgement, Run 268 settlement finalization, Run 266 settlement commitment, Run 264 settlement projection, Run 262 consumer, Run 260 acknowledgement, Run 258 receipt, or Run 256 backend outcome directly.

Required projection mapping:

* Run 272 `SettlementOutcomeReportRecorded` → creates settlement-outcome publication request.
* Run 272 `SettlementOutcomeReportDuplicateIdempotent` → idempotent-only, never creates a new settlement-outcome publication record by itself.
* Run 272 legacy/no-outcome-report outcomes → legacy/no-publication.
* Run 272 rejected-before-report / report-did-not-record / record-failure / rollback / rollback-failed / ambiguous-window / production / MainNet / external outcomes → corresponding no-publication fail-closed / unavailable outcomes.
* Run 272 `MainNetPeerDrivenApplyRefusedNoOutcomeReport` → `MainNetPeerDrivenApplyRefusedNoPublication`.
* Run 272 `ValidatorSetRotationUnsupportedNoOutcomeReport` → `ValidatorSetRotationUnsupportedNoPublication`.
* Run 272 `PolicyChangeUnsupportedNoOutcomeReport` → `PolicyChangeUnsupportedNoPublication`.
* Every other non-recorded settlement-outcome report outcome → no-publication fail-closed.

Helper corpus:

The helper must include these tables:

* accepted / compatible
* rejection / fail-closed
* recovery / crash-window
* projection
* stage-ordering
* settlement-outcome-publication ledger
* non-mutation
* reachability

Accepted / compatible cases must prove at minimum:

* Disabled settlement-outcome publication policy preserves legacy no-publication bypass.
* Disabled settlement-outcome report policy never invokes settlement-outcome publication.
* Disabled settlement-receipt acknowledgement policy never invokes settlement-outcome report or settlement-outcome publication.
* Disabled settlement-finalization policy never invokes settlement-receipt acknowledgement, settlement-outcome report, or settlement-outcome publication.
* Disabled settlement-commitment policy never invokes settlement finalization, settlement-receipt acknowledgement, settlement-outcome report, or settlement-outcome publication.
* Disabled settlement-projection policy never invokes settlement commitment, settlement finalization, settlement-receipt acknowledgement, settlement-outcome report, or settlement-outcome publication.
* Disabled consumer / acknowledgement / receipt / backend / attestor / finalizer / reporter / sink / pipeline policies never reach settlement-outcome publication.
* DevNet fixture chain records exactly one modeled in-memory settlement-outcome publication record only after the full Run 246 → 248 → 250 → 252 → 254 → 256 → 258 → 260 → 262 → 264 → 266 → 268 → 270 → 272 → 274 chain.
* TestNet fixture chain records exactly one modeled in-memory settlement-outcome publication record only after the same full chain.
* Add-root / retire-root / revoke-root / emergency-revoke-root / noop modeled action variants preserve ordering and publish only after Run 272 `SettlementOutcomeReportRecorded`.
* Duplicate identical settlement-outcome publication record is idempotent.
* Run 272 duplicate-idempotent settlement-outcome report outcome may only match an already-recorded settlement-outcome publication record and never creates a new publication by itself.
* Production settlement-outcome publication path is reachable but unavailable/fail-closed and records no state.
* MainNet settlement-outcome publication path is reachable but unavailable/fail-closed and records no state.
* External settlement-outcome publication path is reachable but unavailable/fail-closed and performs no publication/network I/O.
* MainNet peer-driven apply is refused before pipeline progression, sink invocation, reporter invocation, finalizer invocation, attestor invocation, backend invocation, receipt invocation, acknowledgement invocation, consumer invocation, settlement-projection invocation, settlement-commitment invocation, settlement-finalization invocation, settlement-receipt acknowledgement invocation, settlement-outcome report invocation, and settlement-outcome publication invocation.
* Validator-set rotation unsupported and records no state.
* Policy-change unsupported and records no state.

Rejected / fail-closed cases must prove at minimum:

* Every rejection before Run 274 produces no settlement-outcome publication request and no record.
* Settlement-outcome report did-not-record / rejected-before-record / record failure / rollback / rollback-failed / ambiguous window produces no settlement-outcome publication invocation and no record.
* Settlement-outcome publication before Run 272 `SettlementOutcomeReportRecorded` is rejected.
* Same settlement-outcome publication record id with different digest fails closed as equivocation.
* Wrong environment / chain / genesis / governance surface / mutation surface fails before settlement-outcome publication invocation.
* Wrong publication identity / policy / kind / request digest / response digest / record digest fails closed.
* Wrong settlement-outcome report identity / request / response / record / transcript digest fails closed.
* Wrong settlement-receipt acknowledgement identity / request / response / record / transcript digest fails closed.
* Wrong settlement-finalization identity / request / response / record / transcript digest fails closed.
* Wrong settlement-commitment / settlement-projection / consumer / acknowledgement / receipt / backend / attestation / finalization / completion-report / sink / pipeline digest fails closed.
* Wrong proposal id / decision id / candidate digest / authority-domain sequence fails closed.
* Malformed request / response fails closed.
* Local operator key cannot satisfy MainNet authority.
* Peer majority cannot satisfy MainNet authority.
* Production/MainNet/external settlement-outcome publication unavailable paths record no state and perform no publication.
* Rejected paths produce no Run 070 call, no `LivePqcTrustState` mutation, no trust swap, no session eviction, no sequence write, no marker write, no RocksDB/file/schema/migration/storage-format change, no external publication, no confirmation, no real audit-ledger write, no real settlement write, no real settlement-finality write, no real settlement-receipt write, no real settlement-receipt acknowledgement, no real settlement-finality projection, no real settlement-outcome report backend, and no real settlement-outcome publication.

Recovery / crash-window cases must prove at minimum:

* before-pipeline through after-settlement-outcome-report-success-before-publication-request windows fail closed unless explicit matching settlement-outcome report success exists.
* after-settlement-outcome-publication-request-before-record fails closed.
* after-settlement-outcome-publication-record-before-success fails closed unless explicit matching settlement-outcome publication record exists.
* after-settlement-outcome-publication-success recovers as recorded.
* ambiguous, record-failed, rollback-completed, rollback-failed, unknown, production/MainNet/external windows fail closed.
* MainNet peer-driven apply refusal precedes recovery classification where applicable.

Projection cases must prove at minimum:

* Only Run 272 `SettlementOutcomeReportRecorded` creates a settlement-outcome publication request.
* Run 270 `SettlementReceiptAcknowledgementRecorded` alone creates no settlement-outcome publication request.
* Run 268 `SettlementFinalizationRecorded` alone creates no settlement-outcome publication request.
* Run 266 `SettlementCommitmentRecorded` alone creates no settlement-outcome publication request.
* Run 264 `SettlementProjectionRecorded` alone creates no settlement-outcome publication request.
* Run 262 `AcknowledgementConsumed` alone creates no settlement-outcome publication request.
* Run 260 `AcknowledgementRecorded` alone creates no settlement-outcome publication request.
* Run 258 `AuditReceiptRecorded` alone creates no settlement-outcome publication request.
* Run 256 backend submission alone creates no settlement-outcome publication request.
* All earlier stage successes alone create no settlement-outcome publication request.
* Run 272 duplicate-idempotent settlement-outcome report outcome creates no new settlement-outcome publication record unless a matching publication record already exists.
* Only `SettlementOutcomePublicationRecorded` authorizes modeled settlement-outcome publication state.
* Every other outcome does not record.

Stage-ordering cases must prove at minimum:

* MainNet peer-driven apply refusal happens before every later stage.
* Evaluator/call-site authorization precedes durable replay.
* Durable replay precedes mutation engine.
* Mutation engine precedes modeled applier.
* Modeled applier precedes sink intent.
* Sink receipt precedes completion report.
* Completion report precedes finalization.
* Finalization precedes attestation.
* Attestation precedes backend request.
* Backend submission precedes audit receipt request.
* Audit receipt precedes acknowledgement request.
* Acknowledgement record precedes consumer request.
* Consumer record precedes settlement-projection request.
* Settlement-projection record precedes settlement-commitment request.
* Settlement-commitment record precedes settlement-finalization request.
* Settlement-finalization record precedes settlement-receipt acknowledgement request.
* Settlement-receipt acknowledgement record precedes settlement-outcome report request.
* Settlement-outcome report record precedes settlement-outcome publication request.
* Settlement-outcome publication record happens only after settlement-outcome report success.
* Rejection before settlement-outcome publication stage leaves settlement-outcome publication invocation count zero.
* Settlement-outcome publication record failure does not invalidate settlement-outcome report state but does not authorize settlement-outcome publication state.
* Settlement-outcome publication rollback failure is fatal/fail-closed and does not authorize settlement-outcome publication state.

Settlement-outcome publication ledger cases must prove at minimum:

* One valid settlement-outcome publication record inserts one in-memory record.
* Duplicate identical settlement-outcome publication record is idempotent and does not increase count.
* Same record id with different digest is equivocation and does not increase count.
* Wrong publication / outcome-report / receipt-acknowledgement / finalization / commitment / projection / consumer / acknowledgement / receipt / backend / attestation / finalization / report / sink / pipeline / proposal / decision / candidate / sequence digest does not record.
* Malformed request / response does not record.
* Rollback restores in-memory ledger snapshot.
* Rollback failure never claims success.
* Fixture ledger never writes files, RocksDB, markers, sequences, external publications, confirmations, audit-ledger entries, settlement records, settlement-finality records, settlement-receipt records, settlement-receipt acknowledgement records, settlement-finality projection records, settlement-outcome report records, real settlement-outcome publication records, or production durable state.

Non-mutation cases must prove at minimum:

* no Run 070 call;
* no `LivePqcTrustState` mutation;
* no trust swap;
* no session eviction;
* no sequence write;
* no marker write;
* no RocksDB/file/schema/migration/storage-format write;
* no wire/schema/marker/sequence/trust-bundle change;
* no external network publication;
* no real settlement;
* no real settlement finality;
* no real settlement receipt;
* no real settlement-receipt acknowledgement;
* no real settlement-finality projection;
* no real settlement-outcome report backend;
* no real settlement-outcome publication;
* no real external-publication confirmation;
* no real audit-ledger write;
* fixture settlement-outcome publication sink mutates only modeled in-memory `DurableCompletionSettlementOutcomePublicationLedger`;
* production/MainNet/external paths remain unavailable/fail-closed.

Real release-binary scenarios:

The harness must run the real `target/release/qbind-node` for at least:

* S1 — `--help`: no settlement-outcome publication / settlement-outcome report backend / settlement-outcome report / settlement-receipt acknowledgement / settlement-finality projection / settlement-finalization / settlement-receipt / settlement-finality / settlement / external-publication / confirmation production enablement banner or public flag drift.
* S2 — default DevNet startup parse/smoke: no settlement-outcome publication / settlement-outcome report backend / settlement-receipt acknowledgement / settlement-finality projection / settlement-finalization / settlement-receipt / settlement-finality / settlement / publication / confirmation enablement claim.
* S3 — default TestNet startup parse/smoke: same no-enablement claim.
* S4 — default MainNet startup parse/smoke: same no-enablement claim.
* S5 — hidden governance-execution selector or equivalent hidden selector still parses as before, without exposing the Run 274/275 helper-only path as public production enablement.
* S6 — invalid governance-execution selector or equivalent invalid selector fails closed before mutation if applicable, with no marker write, no sequence write, no live trust swap, no session eviction, no Run 070 call, no receipt sink invocation, no acknowledgement sink invocation, no consumer invocation, no settlement-projection invocation, no settlement-commitment invocation, no settlement-finalization invocation, no settlement-receipt acknowledgement invocation, no settlement-outcome report invocation, no settlement-outcome publication invocation, no audit-ledger write, no external-publication confirmation, no external publication, no settlement, no settlement receipt, no settlement-receipt acknowledgement, no settlement-finality projection, no settlement-outcome report backend, no settlement-outcome publication, and no settlement finality.

Do not add any new public CLI surface for Run 275. If a hidden selector already exists from earlier runs, the harness may use it only as a parse/fail-closed smoke surface.

Denylist:

Prove captured real-binary logs and helper logs do not contain active/enabled claims for:

* real settlement backend enabled;
* real settlement outcome publication enabled;
* real settlement-outcome publication backend enabled;
* real settlement outcome report backend enabled;
* real settlement outcome report enabled;
* real settlement receipt enabled;
* real settlement-receipt acknowledgement enabled;
* real settlement-finality projection enabled;
* real settlement finality enabled;
* durable-completion settlement-outcome publication production enabled;
* MainNet settlement-outcome publication enabled;
* external settlement-outcome publication enabled;
* durable-completion settlement-outcome report production enabled;
* durable-completion settlement-receipt acknowledgement production enabled;
* durable-completion settlement-finalization production enabled;
* durable-completion settlement-commitment production enabled;
* durable-completion settlement-projection production enabled;
* real audit-ledger acknowledgement backend enabled;
* real external-publication confirmation backend enabled;
* real external publication backend enabled;
* real production attestation backend enabled;
* real finalization backend enabled;
* real completion-report backend enabled;
* real durable consume backend enabled;
* real persistent replay backend enabled;
* real production mutation engine enabled;
* MainNet mutation engine enabled;
* MainNet governance enabled;
* MainNet peer-driven apply enabled;
* real governance execution engine enabled;
* real on-chain governance proof verifier enabled;
* RocksDB replay backend enabled;
* file replay backend enabled;
* schema migration enabled;
* storage-format migration enabled;
* KMS/HSM backend enabled;
* RemoteSigner backend enabled;
* validator-set rotation enabled;
* policy-change action enabled;
* autonomous apply;
* apply-on-receipt;
* apply-on-acknowledgement;
* apply-on-consumer;
* apply-on-settlement-projection;
* apply-on-settlement-commitment;
* apply-on-settlement-finalization;
* apply-on-settlement-receipt-acknowledgement;
* apply-on-settlement-outcome-report;
* apply-on-settlement-outcome-publication;
* apply-on-settlement;
* peer-majority authority;
* Run 070 apply from the settlement-outcome publication boundary;
* `LivePqcTrustState` mutation from the settlement-outcome publication boundary;
* real trust swap from the settlement-outcome publication boundary;
* session eviction from the settlement-outcome publication boundary;
* marker write from the settlement-outcome publication boundary;
* sequence write from the settlement-outcome publication boundary;
* RocksDB write from the settlement-outcome publication boundary;
* file write from the settlement-outcome publication boundary;
* external publication by the fixture settlement-outcome publication sink;
* external-publication confirmation by the fixture settlement-outcome publication sink;
* audit-ledger write by the fixture settlement-outcome publication sink;
* settlement write by the fixture settlement-outcome publication sink;
* settlement receipt write by the fixture settlement-outcome publication sink;
* settlement-receipt acknowledgement write by the fixture settlement-outcome publication sink;
* settlement-finality projection write by the fixture settlement-outcome publication sink;
* settlement-outcome report write by the fixture settlement-outcome publication sink;
* settlement-outcome publication write by the fixture settlement-outcome publication sink;
* settlement finality write by the fixture settlement-outcome publication sink;
* production durable consume by the fixture settlement-outcome publication sink;
* production finalization by the fixture settlement-outcome publication sink;
* production attestation by the fixture settlement-outcome publication sink;
* production backend submission by the fixture settlement-outcome publication sink;
* production audit receipt by the fixture settlement-outcome publication sink;
* production acknowledgement by the fixture settlement-outcome publication sink;
* production consumer by the fixture settlement-outcome publication sink;
* production settlement projection by the fixture settlement-outcome publication sink;
* production settlement commitment by the fixture settlement-outcome publication sink;
* production settlement finalization by the fixture settlement-outcome publication sink;
* production settlement-receipt acknowledgement by the fixture settlement-outcome publication sink;
* production settlement-outcome report by the fixture settlement-outcome publication sink;
* DummySig / DummyKem / DummyAead active on production path.

Source/release reachability proof:

The harness must grep production source for Run 274 symbols and prove the release helper exercises them in release mode.

Required greps include:

* `pqc_governance_durable_completion_settlement_outcome_publication`
* `DurableCompletionSettlementOutcomePublicationKind`
* `DurableCompletionSettlementOutcomePublicationPolicy`
* `DurableCompletionSettlementOutcomePublicationIdentity`
* `DurableCompletionSettlementOutcomePublicationInput`
* `DurableCompletionSettlementOutcomePublicationExpectations`
* `DurableCompletionSettlementOutcomePublicationRequest`
* `DurableCompletionSettlementOutcomePublicationResponse`
* `DurableCompletionSettlementOutcomePublicationRecord`
* `DurableCompletionSettlementOutcomePublicationDigest`
* `DurableCompletionSettlementOutcomePublicationTranscriptDigest`
* `DurableCompletionSettlementOutcomePublicationOutcome`
* `GovernanceDurableCompletionSettlementOutcomePublicationSink`
* `FixtureDurableCompletionSettlementOutcomePublicationSink`
* `ProductionSettlementOutcomePublicationSink`
* `MainNetSettlementOutcomePublicationSink`
* `ExternalSettlementOutcomePublicationSink`
* `DurableCompletionSettlementOutcomePublicationLedger`
* `DurableCompletionSettlementOutcomePublicationLedgerRecord`
* `DurableCompletionSettlementOutcomePublicationLedgerSnapshot`
* `DurableCompletionSettlementOutcomePublicationLedgerStatus`
* `project_settlement_outcome_report_outcome_to_outcome_publication_request`
* `evaluate_durable_completion_settlement_outcome_publication`
* `recover_durable_completion_settlement_outcome_publication_window`
* `settlement_outcome_publication_outcome_authorizes_record`
* `settlement_outcome_publication_outcome_projects_to_recorded`
* `SettlementOutcomePublicationRecorded`
* `SettlementOutcomePublicationDuplicateIdempotent`
* `SettlementOutcomePublicationRejectedBeforeRecord`
* `SettlementOutcomePublicationRecordFailedNoPublication`
* `SettlementOutcomePublicationRolledBackNoPublication`
* `SettlementOutcomePublicationRollbackFailedFatalNoPublication`
* `SettlementOutcomePublicationAmbiguousFailClosedNoPublication`
* `ProductionSettlementOutcomePublicationUnavailableNoPublication`
* `MainNetSettlementOutcomePublicationUnavailableNoPublication`
* `ExternalSettlementOutcomePublicationUnavailableNoPublication`
* `MainNetPeerDrivenApplyRefusedNoPublication`
* `ValidatorSetRotationUnsupportedNoPublication`
* `PolicyChangeUnsupportedNoPublication`
* all `durable_completion_settlement_outcome_publication_*` invariant helper names.

If exact names differ, use the exact implemented Run 274 names and record differences honestly in `QBIND_DEVNET_EVIDENCE_RUN_275.md`.

Validation commands:

Run at minimum:

* `cargo build -p qbind-node --release --bin qbind-node`
* `cargo build -p qbind-node --release --example run_275_durable_completion_settlement_outcome_publication_release_binary_helper`
* `bash scripts/devnet/run_275_durable_completion_settlement_outcome_publication_release_binary.sh`
* `cargo test -p qbind-node --test run_274_durable_completion_settlement_outcome_publication_tests`
* `cargo test -p qbind-node --test run_272_durable_completion_settlement_outcome_report_tests`
* `cargo test -p qbind-node --test run_270_durable_completion_settlement_receipt_acknowledgement_tests`
* `cargo test -p qbind-node --test run_268_durable_completion_settlement_finalization_tests`
* `cargo test -p qbind-node --test run_266_durable_completion_settlement_commitment_tests`
* `cargo test -p qbind-node --test run_264_durable_completion_consumer_settlement_projection_tests`
* `cargo test -p qbind-node --test run_262_durable_completion_acknowledgement_consumer_tests`
* `cargo test -p qbind-node --test run_260_durable_completion_audit_receipt_acknowledgement_tests`
* `cargo test -p qbind-node --test run_258_durable_completion_audit_publication_receipt_tests`
* `cargo test -p qbind-node --test run_256_durable_completion_attestation_backend_tests`
* `cargo test -p qbind-node --test run_254_modeled_durable_completion_attestation_projection_tests`
* `cargo test -p qbind-node --test run_252_modeled_durable_completion_finalization_projection_tests`
* `cargo test -p qbind-node --test run_250_modeled_durable_consume_completion_reporter_tests`
* `cargo test -p qbind-node --test run_248_modeled_durable_consume_projection_sink_tests`
* `cargo test -p qbind-node --test run_246_governance_modeled_end_to_end_pipeline_tests`
* `cargo test -p qbind-node --test run_244_modeled_governance_trust_mutation_applier_tests`
* `cargo test -p qbind-node --test run_242_governance_execution_mutation_engine_tests`
* `cargo test -p qbind-node --test run_240_governance_evaluator_replay_durable_runtime_integration_tests`
* `cargo test -p qbind-node --test run_238_governance_evaluator_replay_durable_backend_tests`
* `cargo test -p qbind-node --test run_236_governance_evaluator_replay_consume_runtime_integration_tests`
* `cargo test -p qbind-node --test run_234_governance_evaluator_replay_consume_boundary_tests`
* `cargo test -p qbind-node --test run_232_governance_evaluator_replay_runtime_integration_tests`
* `cargo test -p qbind-node --test run_230_governance_evaluator_replay_state_tests`
* `cargo test -p qbind-node --test run_228_peer_evaluator_context_representation_tests`
* `cargo test -p qbind-node --test run_226_governance_evaluator_runtime_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_224_governance_evaluator_runtime_integration_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

Security scanning:

* Run secret scanning if available and report the result.
* Run CodeQL if available and report the result honestly.
* If Rust CodeQL analysis is skipped because the database is too large, times out, tooling is unavailable, no changes are detected, or the checker classifies the change set as trivial, say that explicitly and do not claim CodeQL coverage.

Documentation:

Create:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_275.md`
* `docs/devnet/run_275_durable_completion_settlement_outcome_publication_release_binary/README.md`
* `docs/devnet/run_275_durable_completion_settlement_outcome_publication_release_binary/summary.txt`
* `docs/devnet/run_275_durable_completion_settlement_outcome_publication_release_binary/.gitignore`

Update narrowly:

* `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
* `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
* `docs/whitepaper/contradiction.md`

Docs must state:

* Run 275 is release-binary evidence for Run 274.
* No production mutating behavior is enabled.
* The settlement-outcome publication boundary is release-evidenced, not production-enabled.
* The release helper exercises Run 274 production library symbols in release mode.
* The release helper remains dead code from production runtime.
* The fixture settlement-outcome publication sink mutates only modeled in-memory `DurableCompletionSettlementOutcomePublicationLedger` state.
* Production settlement-outcome publication, MainNet settlement-outcome publication, and external settlement-outcome publication sinks remain reachable but unavailable/fail-closed.
* Run 272 `SettlementOutcomeReportRecorded` is required before any settlement-outcome publication request can exist.
* Only `SettlementOutcomePublicationRecorded` authorizes modeled settlement-outcome publication state.
* Every non-recorded settlement-outcome report outcome produces no settlement-outcome publication request and no settlement-outcome publication record.
* Duplicate identical settlement-outcome publication record is idempotent.
* Same settlement-outcome publication record id with different digest fails closed as equivocation.
* Failed settlement-outcome publication record, rollback, rollback-failed, ambiguous windows, unavailable production/MainNet/external paths, rejected replay states, and unsupported actions never record.
* MainNet peer-driven apply remains refused before all modeled pipeline stages and settlement-outcome publication invocation.
* Rejected settlement-outcome publication paths are non-mutating.
* No real settlement is implemented.
* No real settlement finality is implemented.
* No real settlement receipt is implemented.
* No real settlement-receipt acknowledgement is implemented.
* No real settlement-finality projection is implemented.
* No real settlement-outcome report backend is implemented.
* No real settlement-outcome publication is implemented.
* No real audit-ledger acknowledgement is implemented.
* No real external-publication confirmation is implemented.
* No real external publication is implemented.
* No real production backend is implemented.
* No RocksDB/file/schema/migration/storage-format change is implemented.
* No wire/schema/marker/sequence/trust-bundle change is implemented.
* No KMS/HSM/RemoteSigner backend is implemented.
* No MainNet governance enablement.
* No MainNet peer-driven apply enablement.
* No validator-set rotation.
* No policy-change enablement.
* The settlement-outcome publication boundary does not call Run 070.
* The settlement-outcome publication boundary does not mutate `LivePqcTrustState`.
* The settlement-outcome publication boundary does not perform a real trust swap.
* The settlement-outcome publication boundary does not evict sessions.
* The settlement-outcome publication boundary does not write sequence files.
* The settlement-outcome publication boundary does not write authority markers.
* The settlement-outcome publication boundary does not perform external publication.
* The settlement-outcome publication boundary does not perform real external-publication confirmation.
* The settlement-outcome publication boundary does not write a real audit ledger.
* The settlement-outcome publication boundary does not perform real settlement.
* The settlement-outcome publication boundary does not implement real settlement finality.
* The settlement-outcome publication boundary does not write or produce a real settlement receipt.
* The settlement-outcome publication boundary does not implement real settlement-receipt acknowledgement.
* The settlement-outcome publication boundary does not implement real settlement-finality projection.
* The settlement-outcome publication boundary does not implement real settlement-outcome report backend.
* The settlement-outcome publication boundary does not implement real settlement-outcome publication.
* Full C4 remains OPEN.
* C5 remains OPEN.

Contradiction handling:

* Crosscheck Run 275 against Runs 050–274.
* If no contradiction is found, add a “Run 275 — no contradiction found” entry to `docs/whitepaper/contradiction.md`.
* If contradiction is found, record it explicitly with exact file/function/doc reference and recommend the smallest safe fix.

Expected verdict:

Positive release-binary evidence only if:

* release helper builds;
* release harness exits 0;
* S1–S6 pass;
* denylist is empty;
* source/helper reachability greps pass;
* regression corpus passes;
* docs/evidence are updated;
* contradiction entry is present;
* scans are reported honestly.

Run 275 closes only the Run 274 release-binary evidence gap. It must not claim Full C4 or C5 closure.

Final response format:

1. Exact verdict.
2. Files changed.
3. Release artifacts and hashes.
4. Helper corpus results.
5. Real-binary scenarios.
6. Tests run and results.
7. Security invariants preserved.
8. Honest limitations.
9. C4/C5 status.
10. Suggested Run 276 next step.
