Continue QBIND from the current state.

Project:
- QBIND post-quantum L1
- security-first, production-grade, execution-first
- C4 is the main live operability tracker
- C5 tracks timeout-verification activation, forged-traffic rejection, and transport-root dependency
- no broad redesign
- no silent regressions
- no overclaiming

Current state:
- Run 078 added typed/versioned/bounded peer-candidate wire envelope for frame discriminator 0x05.
- Run 079 wired disabled-by-default live receive-loop dispatch for 0x05, validation-only.
- Run 080 added disabled-by-default send-side publisher plumbing for 0x05.
- Run 081 produced release-binary N=2 real 0x05 exchange evidence:
  - valid send/validate: PASS
  - receiver-disabled cheap-ignore: PASS
  - invalid candidate reject: PASS
  - duplicate suppression: PASS
  - receiver sequence file hash unchanged
  - live reload apply metrics unchanged
  - session eviction metrics unchanged
  - sessions remained healthy
- Run 081 verdict is PARTIAL POSITIVE because strict “no DummySig” closure was not achieved.
- The remaining blocker is an existing Run 033 probe/log line referencing TrustedClientRoots/DummySig.

Task:
Run 082. Resolve or isolate the remaining TrustedClientRoots/DummySig boundary, then rerun the Run 081 release-binary N=2 real 0x05 exchange matrix.

Primary objective:
Determine whether the observed TrustedClientRoots/DummySig reference is:
A. stale/test-only/log-only/probe-only and not reachable from the production-honest PQC static-root path, or
B. an actual production-path DummySig registration/leak that must be removed or made fail-closed.

Important framing:
This is a focused cryptographic-honesty and evidence task.
Do not add new peer-candidate features.
Do not implement propagation.
Do not implement peer-driven live apply.
Do not implement activation_epoch.
Do not implement KMS/HSM.
Do not implement signing-key ratification.
Do not implement fast-sync restore.
Do not redesign KEMTLS.
Do not redesign consensus.
Do not weaken the Run 081 0x05 validation-only path.
Do not claim full C4 or C5 closure.

Required investigation:

1. Locate every DummySig / TrustedClientRoots reference
   Search the entire repo for:
   - DummySig
   - DummyKem
   - DummyAead
   - TrustedClientRoots
   - Run033
   - Run 033
   - dummy_sig_registered
   - dummy_kem_registered
   - dummy_aead_registered

   Classify each reference as:
   - test-only
   - doc/evidence-only
   - devnet-helper-only
   - probe/log-only
   - production binary reachable
   - production runtime active

2. Trace the release-binary N=2 Run 081 path
   For the exact release-binary command shape used in Run 081, trace:
   - transport root mode
   - trust bundle load path
   - signature verifier selected
   - KEM backend selected
   - AEAD backend selected
   - cert verification path
   - whether DummySig can be registered
   - whether DummySig can be selected
   - whether TrustedClientRoots is a stale probe label or active verifier

3. Decide remediation
   If the reference is stale/log/probe-only:
   - remove or rename the misleading probe/log if safe; or
   - update evidence to isolate it as non-active and add a deterministic proof that production path uses real ML-DSA-44.
   - Do not hide an actual active DummySig path.

   If the reference is production-active:
   - remove DummySig registration from the production-honest pqc-static-root signed-bundle path.
   - ensure MainNet/TestNet cannot register or select DummySig.
   - preserve any legitimate unit-test-only DummySig behind test/dev-only gates.
   - fail closed if production config would require DummySig.

4. Required proof
   Rerun the Run 081 release-binary matrix after remediation/isolation:
   - baseline N=2 startup
   - valid 0x05 send/validate
   - receiver-disabled cheap-ignore
   - invalid candidate reject
   - duplicate suppression

   Prove:
   - no DummySig active in production-honest path
   - no DummyKem
   - no DummyAead
   - real ML-DSA-44 cert verification path active
   - real ML-KEM-768 active
   - real ChaCha20-Poly1305 active
   - sequence file unchanged by peer-candidate validation
   - live reload apply metrics unchanged
   - session eviction metrics unchanged
   - sessions remain healthy
   - no static-root fallback
   - no peer candidate apply or propagation

Required tests:
- cargo test -p qbind-node --test run_080_pqc_peer_candidate_wire_send_tests
- cargo test -p qbind-node --test run_079_pqc_peer_candidate_wire_live_dispatch_tests
- cargo test -p qbind-node --test run_078_pqc_peer_candidate_wire_tests
- cargo test -p qbind-node --test run_077_binary_peer_candidate_check_tests
- cargo test -p qbind-node --test run_076_pqc_peer_candidate_validation_tests
- cargo test -p qbind-node --lib pqc_peer_candidate_wire
- cargo test -p qbind-node --lib metrics::tests::peer_candidate_send_metrics
- cargo test -p qbind-node --lib
- cargo test -p qbind-net --lib
- cargo test -p qbind-crypto --lib
- cargo build --release -p qbind-node --bin qbind-node
- cargo build --release -p qbind-node --example devnet_pqc_trust_bundle_helper
- cargo build --release -p qbind-node --example devnet_pqc_root_helper

If source changes are made:
- rerun all affected unit/integration tests.
- explain exactly why the source change was necessary.

Evidence document:
Create:
- docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_082.md

Also update:
- docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_081.md if project convention permits appending a follow-up note.
- docs/whitepaper/contradiction.md

Evidence doc must include:
- exact objective
- exact verdict
- exact files changed
- exact commands run
- tests/evidence pass/fail status
- full DummySig / DummyKem / DummyAead reference inventory
- classification of each reference
- production-path trace for the Run 081 command shape
- remediation or isolation decision
- release-binary N=2 matrix rerun
- proof no DummySig active on production-honest path
- proof no DummyKem / DummyAead
- proof real ML-DSA-44 / ML-KEM-768 / ChaCha20-Poly1305
- proof 0x05 validation-only non-mutation invariants still hold
- exact remaining C4 boundaries
- immediate next action

contradiction.md update rules:
- If the DummySig reference is proven stale/test-only and no production-active DummySig exists, record the C4 narrowing and exact proof.
- If production-active DummySig is found and removed, record the fix and evidence.
- If DummySig remains active, record a contradiction/regression and do not claim closure.
- Keep C4 open for:
  - peer-driven live apply / propagation
  - activation_epoch runtime source
  - KMS/HSM custody
  - in-binary/on-chain signing-key ratification
  - production fast-sync / consensus-storage restore
  - per-environment production trust-anchor operation
- Do not close full C4.
- Do not claim C5 closure unless specifically justified.

Expected verdict:
- Strongest positive:
  DummySig boundary resolved or isolated; Run 081 N=2 real 0x05 matrix rerun passes; no active DummySig/DummyKem/DummyAead in production-honest path; validation-only invariants preserved.
- Partial positive:
  inventory completed but release-binary matrix or active-path proof remains incomplete.
- Negative:
  production-active DummySig remains reachable or validation-only invariants regress.

Required final response:
1. Exact verdict
2. Exact files changed
3. Exact commands run
4. Tests/evidence run and pass/fail status
5. What was found
6. What was fixed or isolated
7. What was proven
8. What remains not solved
9. Whether contradiction.md was updated and why
10. Exact immediate next action recommended