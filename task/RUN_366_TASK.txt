Run 366 — Release-binary end-to-end evidence for public DevNet abuse/DoS M12

Continue QBIND after accepted Run 365.

Current state:
Run 365 is accepted as PASS / public-DevNet-per-peer deployed-node threading source-test positive.

Run 365 landed the source/test threading that Run 364 identified as missing:
- `P2pNodeBuilder::deployed_peer_rate_limiter_config`
- `P2pNodeBuilder::deployed_async_peer_manager_config`
- `P2pNodeBuilder::build_deployed_peer_manager`
- `P2pNodeContext.peer_rate_limiter_config`
- source/test proof that the CLI-derived per-peer `peer_rate_limiter_config` reaches the deployed builder path and the live `AsyncPeerManagerImpl` construction path.

Current M12 state:
- Run 362: connection-rate limiter wired into live `p2p_tcp` accept loop, metric added, release-binary evidence landed.
- Run 363: per-peer message-rate runtime override wired into live peer-manager construction path at source/test level.
- Run 364: release-binary evidence for both controls landed, but M12 stayed Yellow/Partial because deployed `qbind-node` did not yet thread the per-peer override into the deployed builder path.
- Run 365: deployed-builder threading landed at source/test level, but no release-binary evidence.
- M12 remains Yellow/Partial strengthened.
- M12 may move Green only if Run 366 proves end-to-end release-binary behavior over the deployed-node path.

Current public DevNet readiness:
- Green: M1, M2, M3 same-host scope, M5, M10, M11, M16, M17, M18, M19, M20.
- Yellow/Partial: M4, M6, M7, M8, M9, M12, M13, M14, M15.
- M4 remains launch-blocking.
- Public DevNet remains NOT launch-ready.
- Full C4 OPEN.
- C5 OPEN.
- MainNet authority rotation/revocation Red.

Primary objective:
Produce release-binary end-to-end evidence that the real deployed `qbind-node` path honors both public DevNet abuse/DoS controls:

1. operator-configurable inbound connection-rate limiting from Run 362;
2. operator-configurable per-peer message-rate limiting threaded through the deployed builder path from Run 365.

If the evidence genuinely proves both controls end-to-end on the release binary, with defaults preserved and all non-claims intact, then M12 may move Yellow/Partial → Green for the public DevNet readiness track.

Strict scope:
Release-binary evidence for M12 only.

No public DevNet launch.
No live seed-node deployment.
No bootnode deployment.
No faucet.
No RPC gateway.
No explorer.
No status page.
No MainNet enablement.
No TestNet readiness claim.
No C4 closure.
No C5 closure.
No authority-lifecycle runtime wiring.
No validator-set mutation.
No production epoch transition.
No execution-sink write.
No P2P wire-format change.
No peer-admission weakening.
No trust-bundle behavior weakening.
No default-open public port.
No M4 Green.
No M6 Green.
No public DevNet launch-ready claim unless every must-have is Green, which is not expected.

Required investigation:
Inspect:

- `crates/qbind-node/src/main.rs`
- `crates/qbind-node/src/p2p_node_builder.rs`
- `crates/qbind-node/src/p2p_tcp.rs`
- `crates/qbind-node/src/async_peer_manager.rs`
- `crates/qbind-node/src/public_devnet_abuse_dos_runtime.rs`
- `crates/qbind-node/src/public_devnet_abuse_dos_config.rs`
- `crates/qbind-node/src/peer_rate_limiter.rs`
- `crates/qbind-node/src/metrics.rs`
- `crates/qbind-node/src/metrics_http.rs`
- `crates/qbind-node/src/cli.rs`
- Run 362/363/364/365 tests, evidence, helper, and harness
- `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_365.md`
- `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_364.md`
- `docs/release/QBIND_PUBLIC_DEVNET_READINESS_CRITERIA.md`
- `docs/release/public-devnet/p2p/ABUSE_DOS_POSTURE.md`
- `docs/release/public-devnet/p2p/VERIFY.md`

Required implementation:
Prefer evidence/helper/harness/docs only.

Add release-binary helper:

`crates/qbind-node/examples/run_366_public_devnet_abuse_dos_m12_end_to_end_release_helper.rs`

Add release harness:

`scripts/devnet/run_366_public_devnet_abuse_dos_m12_end_to_end_release_binary.sh`

Add evidence archive:

`docs/devnet/run_366_public_devnet_abuse_dos_m12_end_to_end_release_binary/README.md`
`docs/devnet/run_366_public_devnet_abuse_dos_m12_end_to_end_release_binary/summary.txt`
`docs/devnet/run_366_public_devnet_abuse_dos_m12_end_to_end_release_binary/.gitignore`

Only those three archive files should be tracked. Raw logs, temp directories, helper JSON, process logs, and generated artifacts must be gitignored.

Add canonical evidence:

`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_366.md`

Update narrowly:

- `docs/release/QBIND_PUBLIC_DEVNET_READINESS_CRITERIA.md`
- `docs/release/public-devnet/p2p/ABUSE_DOS_POSTURE.md`
- `docs/release/public-devnet/p2p/VERIFY.md`
- `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
- `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
- `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
- `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
- `docs/whitepaper/contradiction.md`

Do not modify production source unless a tiny evidence-only visibility/test seam is absolutely required. If any production source changes, document it explicitly and keep M12 Green blocked unless release evidence covers the changed deployed path.

Required release-binary evidence:
The Run 366 helper/harness must prove on real release artifacts:

1. `target/release/qbind-node` builds successfully.
2. release helper builds successfully.
3. `qbind-node` SHA-256 captured.
4. helper SHA-256 captured.
5. BuildIDs captured where available.
6. toolchain versions captured.
7. helper links real Run 361/362/363/365 symbols.
8. default no-flag behavior preserves current behavior.
9. no-flag connection limiter disabled.
10. no-flag per-peer limiter remains `1000` msg/s + `100` burst.
11. connection-rate limiter admits under-budget inbound attempts.
12. connection-rate limiter refuses over-budget inbound attempts.
13. `qbind_p2p_connection_rate_drop_total` increments on connection-rate refusal.
14. connection-rate metric does not increment on allowed inbound attempts.
15. deployed-builder path installs custom per-peer message-rate config.
16. running/deployed peer manager honors custom per-peer max messages/sec.
17. running/deployed peer manager honors custom burst allowance.
18. custom per-peer limiter allows under-budget messages.
19. custom per-peer limiter drops over-budget messages.
20. connection-rate limiting and per-peer message-rate limiting are independent.
21. per-peer message drop does not increment the connection-rate metric.
22. connection-rate refusal does not admit a peer.
23. hidden/devnet-only flags remain absent from `--help`.
24. real hidden flags parse.
25. invented flags are rejected by clap.
26. invalid zero connection-rate config fails closed.
27. invalid zero per-peer message-rate config fails closed.
28. unsafe/unbounded values fail closed.
29. inconsistent per-address config fails closed.
30. MainNet abuse/DoS enablement refused.
31. no public DevNet launch claim.
32. no M4 Green claim.
33. no M6 Green claim.
34. no TestNet readiness claim.
35. no MainNet readiness claim.
36. no C4/C5 closure claim.
37. no P2P wire-format change.
38. no peer-admission weakening.
39. no trust-bundle behavior weakening.
40. no `LivePqcTrustState` mutation.
41. no sequence/marker write.
42. no validator-set mutation.
43. no epoch transition.
44. no Run 070 path.

Preferred runtime evidence shape:
Use a bounded loopback release-binary harness.

Acceptable approaches, in priority order:

A. Start one or more real `target/release/qbind-node` processes on loopback with:
- `--env devnet`;
- explicit temporary `--data-dir`;
- `--enable-p2p`;
- loopback listen address;
- explicit genesis hash pin where required;
- hidden abuse/DoS flags;
- timeout-supervised process lifetime;
- metrics endpoint if available;
- scripted inbound attempts and peer/message path stimulus.

B. If the full-node process blocks in LocalMesh / consensus loops, keep it bounded with timeouts and prove the relevant deployed P2P path is live through logs, metrics, loopback sockets, and the release helper.

C. If a full process cannot safely drive message traffic without broader infrastructure, the helper may instantiate the deployed builder and live peer manager from the same release-built symbols, but then the verdict must be Partial-positive and M12 must remain Yellow/Partial unless the evidence truly exercises the running deployed binary path.

Do not fake a live seed or external endpoint. Use only localhost / RFC 5737 / RFC 3849 addresses.

Required helper scenarios:
At minimum:

1. `default_preserves_behavior`
   - no abuse/DoS flags/config;
   - connection limiter disabled;
   - per-peer defaults `1000` / `100`;
   - no new public CLI flags;
   - metric remains zero.

2. `connection_rate_end_to_end`
   - configure small connection-rate bucket;
   - prove first N inbound attempts allowed;
   - prove N+1 refused;
   - prove metric increments exactly once;
   - prove no peer admitted on refusal.

3. `per_peer_message_rate_end_to_end`
   - configure small per-peer bucket;
   - prove deployed builder installs the custom config;
   - prove admitted peer/message path uses custom values;
   - prove first N messages allowed;
   - prove N+1 dropped;
   - prove connection-rate metric untouched.

4. `combined_independence`
   - configure both controls;
   - prove connection refusal and message drop are independent;
   - prove counters distinct.

5. `invalid_configs_fail_closed`
   - zero connection-rate window;
   - zero connection-rate max;
   - zero per-peer message max;
   - unsafe/unbounded values;
   - inconsistent per-address config.

6. `mainnet_refused`
   - enabled abuse/DoS config under MainNet refused.

7. `cli_surface_hidden_and_parse_checked`
   - hidden flags absent from `--help`;
   - real hidden flags parse;
   - invented flags rejected.

Required production-binary scenarios:
Run and record:

1. `qbind-node --help` hides all 8 hidden Run 362/363 abuse/DoS flags.
2. Real hidden flags parse successfully on DevNet.
3. Invalid abuse/DoS config exits non-zero or is proven fail-closed through the exact production validation path.
4. MainNet abuse/DoS enablement exits non-zero or is proven fail-closed through the exact production validation path.
5. Invented abuse/DoS flag exits with clap error.
6. Default DevNet/TestNet/MainNet surfaces remain unchanged.
7. No accidental public CLI flag exposure.

Required tests:
Run and record exact counts:

- `cargo build -p qbind-node --release`
- `cargo build -p qbind-node --release --example run_366_public_devnet_abuse_dos_m12_end_to_end_release_helper`
- Run the Run 366 harness.
- `cargo test -p qbind-node --test run_365_public_devnet_deployed_peer_rate_threading_tests`
- `cargo test -p qbind-node --test run_363_public_devnet_per_peer_message_rate_runtime_tests`
- `cargo test -p qbind-node --test run_362_public_devnet_abuse_dos_runtime_tests`
- `cargo test -p qbind-node --test run_361_public_devnet_abuse_dos_hardening_tests`
- `cargo test -p qbind-node --lib`

Preserve the reconciled Run 361 count as 30 unless the actual target output changes.

Security:
Run secret scanning over all changed files.

No private keys, mnemonics, seed phrases, credentials, API keys, tokens, private infrastructure, real production hostnames, or unapproved live endpoints may be introduced.

Use localhost, RFC 5737, RFC 3849, and temporary generated non-secret test material only.

CodeQL:
If no production/library source changes occur and this is helper/harness/docs-only, record whether CodeQL is meaningful for the diff.

If any Rust source or Rust example is added, invoke CodeQL if available.

If CodeQL is skipped, unavailable, timed out, database-too-large, or not meaningful, record the exact result and do not call it clean.

Provenance:
Record in `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_366.md`:

- git commit;
- branch;
- clean/dirty state;
- files changed;
- helper path;
- harness path;
- archive path;
- release binary SHA-256;
- helper SHA-256;
- BuildIDs where available;
- toolchain versions;
- exact helper scenarios;
- exact production-binary scenarios;
- exact runtime evidence shape used;
- test counts;
- secret scan result;
- CodeQL result;
- readiness matrix delta for M12;
- exact reason M12 is or is not Green.

Readiness matrix rules:
- M12 may move Yellow/Partial → Green only if release-binary evidence proves the deployed node honors both connection-rate and per-peer message-rate runtime config end-to-end, with defaults preserved.
- If evidence is construction-path only, keep M12 Yellow/Partial.
- If a real running process cannot be driven over the inbound/message path, keep M12 Yellow/Partial and document the blocker.
- Do not alter M4 unless live seed/bootnode reachability evidence lands.
- Do not alter M6 unless stable identity-generation support lands.
- Do not alter M7–M9/M13–M15 unless genuinely closed.
- Public DevNet remains NOT launch-ready unless all must-haves are Green.

Expected verdict:
PASS / public-DevNet-abuse-DoS-M12 end-to-end release-binary positive if helper, harness, production-binary scenarios, running/deployed-path evidence, tests, docs, security scan, CodeQL provenance, and readiness update land, and M12 Green is justified.

Partial-positive if release evidence lands but any part remains construction-path-only, bounded-helper-only, or not genuinely end-to-end on the deployed node.

Unacceptable:
- claiming public DevNet launch-ready;
- marking M12 Green without end-to-end release-binary evidence for both controls;
- marking M4 Green without live seed/bootnode reachability evidence;
- marking M6 Green without identity-generation support;
- exposing hidden/devnet-only abuse/DoS flags in public help accidentally;
- silently changing defaults;
- weakening peer admission;
- changing P2P wire format;
- publishing fake live seed addresses;
- publishing secrets or private infrastructure;
- claiming TestNet readiness;
- claiming MainNet readiness;
- closing C4 or C5;
- adding authority-lifecycle runtime wiring;
- deploying seed nodes, faucet, RPC, explorer, or status page in this run;
- hiding skipped CodeQL;
- using marketing language.

Final response format:
1. Exact verdict.
2. Files changed.
3. Release artifacts and hashes.
4. Runtime evidence shape.
5. Helper/harness corpus.
6. Production-binary scenarios.
7. Connection-rate end-to-end evidence.
8. Per-peer message-rate end-to-end evidence.
9. Combined limiter independence evidence.
10. Rejection/fail-closed evidence.
11. Default compatibility.
12. CLI/config surface.
13. Metric evidence.
14. Non-mutation evidence.
15. Readiness matrix delta for M12.
16. Current public DevNet readiness status.
17. Remaining public DevNet blockers.
18. Public TestNet blockers.
19. MainNet blockers.
20. C4/C5 status.
21. Tests run.
22. Security scans.
23. CodeQL.
24. Provenance.
25. Honest limitations.
26. Suggested Run 367 next step.