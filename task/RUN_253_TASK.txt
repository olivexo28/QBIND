Run 253 — Release-binary modeled durable-completion finalization-projection evidence

You are continuing QBIND after Run 252.

Run 240 is fully closed at source/test level.
Run 241 is fully closed as release-binary evidence for Run 240.
Run 242 is fully closed as source/test governance execution mutation-engine boundary work.
Run 243 is fully closed as release-binary evidence for Run 242.
Run 244 is fully closed as source/test modeled governance trust-state mutation applier boundary work.
Run 245 is fully closed as release-binary evidence for Run 244.
Run 246 is fully closed as source/test governance modeled end-to-end pipeline boundary work.
Run 247 is fully closed as release-binary evidence for Run 246.
Run 248 is fully closed as source/test modeled durable-consume projection sink boundary work.
Run 249 is fully closed as release-binary evidence for Run 248.
Run 250 is fully closed as source/test modeled durable-consume receipt-acknowledgement / completion reporter boundary work.
Run 251 is fully closed as release-binary evidence for Run 250.
Run 252 is fully closed as source/test modeled durable-completion finalization-projection boundary work.

Strict workflow:

* Execution-first.
* Narrow scope only.
* No broad redesign.
* No silent regressions.
* No hype or overclaiming.
* Keep Full C4 OPEN and C5 OPEN unless real production evidence closes them.
* MainNet peer-driven apply remains refused.
* Fixture/local/loopback material remains DevNet/TestNet evidence-only.
* Rejected paths must remain non-mutating.
* Do not weaken Runs 070, 130–252.
* Release-binary evidence only.
* No production source module modification unless required for a compile-only fix, and any such fix must be explicitly justified.

Current state:

* Run 246 models the end-to-end ordering from evaluator/call-site authorization through durable replay, mutation-engine authorization, modeled applier success, and durable consume authorization.
* Run 248 models the durable-consume projection sink: only a Run 246 `ModeledApplierAppliedAndDurableConsumeAuthorized` outcome can create a sink intent, and only `ConsumeReceiptRecorded` records a modeled in-memory receipt.
* Run 249 release-evidenced the Run 248 sink boundary.
* Run 250 models the durable-consume completion reporter: only Run 248 `ConsumeReceiptRecorded` can create a completion-report intent, and only `CompletionReportRecorded` authorizes a new modeled completion-reported state.
* Run 251 release-evidenced the Run 250 completion-reporter boundary.
* Run 252 models durable-completion finalization projection: only Run 250 `CompletionReportRecorded` can create a finalization intent, and only `DurableCompletionFinalized` authorizes a new modeled durable-completion-finalized state.
* Run 252 has no release-binary evidence yet.
* No real persistent replay backend is implemented.
* No real durable consume backend is implemented.
* No real completion-report backend is implemented.
* No real finalization backend is implemented.
* No real governance execution engine is implemented.
* No real production mutation engine is implemented.
* No real on-chain governance proof verifier is implemented.
* No RocksDB/file/schema/migration/storage-format change is implemented.
* No KMS/HSM/RemoteSigner backend is implemented.
* No MainNet governance enablement.
* No MainNet peer-driven apply enablement.
* Validator-set rotation remains unsupported.
* Full C4 remains OPEN.
* C5 remains OPEN.

Objective:

Produce release-binary evidence for the Run 252 modeled durable-completion finalization-projection boundary, mirroring the Run 241 / 243 / 245 / 247 / 249 / 251 release-evidence pattern.

Run 253 must prove on real `target/release/qbind-node` plus a release-built helper that the Run 252 finalization-projection boundary is present in release-built code, that the helper exercises the Run 246 pipeline → Run 248 consume-sink projection → Run 250 completion-report projection → Run 252 finalization projection → fixture finalization-recording path through production library symbols, and that all fail-closed / non-mutating / no-finalization invariants remain true in release mode.

Run 253 must not implement a real persistent replay backend, must not implement a real durable consume backend, must not implement a real completion-report backend, must not implement a real finalization backend, must not wire production runtime mutation, must not call Run 070, must not mutate `LivePqcTrustState`, must not perform a real trust swap, must not evict sessions, must not write sequence files, must not write authority markers, must not touch RocksDB, must not change wire/schema/marker/sequence/trust-bundle formats, and must not enable MainNet governance or MainNet peer-driven apply.

Required deliverables:

1. Add a release helper:

   `crates/qbind-node/examples/run_253_modeled_durable_completion_finalization_projection_release_binary_helper.rs`

   The helper must link against release-built production library symbols and exercise:

   * `pqc_governance_modeled_durable_completion_finalization_projection`
   * `GovernanceModeledDurableCompletionFinalizationInput`
   * `GovernanceModeledDurableCompletionFinalizationExpectations`
   * `GovernanceModeledDurableCompletionFinalizationPolicy`
   * `GovernanceModeledDurableCompletionFinalization`
   * `GovernanceModeledDurableCompletionFinalizationSurface`
   * `GovernanceModeledDurableCompletionFinalizationEnvironmentBinding`
   * `GovernanceModeledDurableCompletionFinalizationRuntimeBinding`
   * `GovernanceModeledDurableCompletionFinalizationReplayBinding`
   * `GovernanceModeledDurableCompletionFinalizationPipelineBinding`
   * `GovernanceModeledDurableCompletionFinalizationSinkBinding`
   * `GovernanceModeledDurableCompletionFinalizationReporterBinding`
   * `ModeledDurableCompletionFinalizationLedger`
   * `ModeledDurableCompletionFinalizationRecord`
   * `ModeledDurableCompletionFinalizationSnapshot`
   * `ModeledDurableCompletionFinalizationStatus`
   * `ModeledDurableCompletionFinalizationDigest`
   * `GovernanceModeledDurableCompletionFinalizationOutcome`
   * `GovernanceModeledDurableCompletionFinalizer`
   * `FixtureModeledDurableCompletionFinalizer`
   * `ProductionModeledDurableCompletionFinalizer`
   * `MainNetModeledDurableCompletionFinalizer`
   * `project_completion_reporter_outcome_to_finalization_intent`
   * `evaluate_modeled_durable_completion_finalization`
   * `recover_modeled_durable_completion_finalization_window`
   * `finalization_outcome_authorizes_modeled_finalization`
   * `finalization_outcome_projects_to_durable_completion_finalized`
   * all Run 252 grep-verifiable invariant helpers

   If any exact symbol name differs from Run 252, use the exact implemented name and record the substitution in `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_253.md`. Do not invent compatibility shims unless needed for compilation, and do not change production behavior.

2. Add a release harness:

   `scripts/devnet/run_253_modeled_durable_completion_finalization_projection_release_binary.sh`

   The harness must:

   * build `target/release/qbind-node`;
   * build the Run 253 release helper;
   * capture git commit, rustc version, cargo version;
   * capture SHA-256 and ELF Build ID for `target/release/qbind-node`;
   * capture SHA-256 and ELF Build ID for the Run 253 helper;
   * run real-binary surface scenarios;
   * run the helper corpus in release mode;
   * run source-reachability greps for the Run 252 symbols;
   * run helper-output reachability checks proving the helper exercised the Run 252 symbols;
   * run a denylist proving no active production/MainNet enablement claims;
   * run the required regression test corpus;
   * write generated evidence into an ignored evidence directory.

3. Add evidence archive directory:

   `docs/devnet/run_253_modeled_durable_completion_finalization_projection_release_binary/`

   Track only:

   * `README.md`
   * `summary.txt`
   * `.gitignore`

   Generated artifacts must be ignored, following the Run 241 / 243 / 245 / 247 / 249 / 251 convention.

4. Add canonical evidence report:

   `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_253.md`

Release-helper corpus:

The helper must include accepted/compatible, rejection, recovery, projection, stage-ordering, finalization-ledger, non-mutation, and reachability tables.

Accepted / compatible cases must prove at minimum:

* Disabled finalizer policy preserves legacy bypass and performs no finalization, no finalization record, and no mutation.
* Disabled reporter policy preserves legacy bypass and never invokes the finalizer.
* Disabled sink policy preserves legacy bypass and never invokes reporter or finalizer.
* Disabled pipeline policy preserves legacy bypass and never invokes sink, reporter, or finalizer.
* Disabled evaluator/call-site policy preserves legacy bypass and never invokes sink, reporter, or finalizer.
* DevNet fixture pipeline success + sink receipt recorded + completion report recorded + finalizer record success records exactly one modeled in-memory finalization.
* TestNet fixture pipeline success + sink receipt recorded + completion report recorded + finalizer record success records exactly one modeled in-memory finalization.
* Modeled add-root pipeline success records finalization only after modeled applier success, sink receipt record, and completion report record.
* Modeled retire-root pipeline success records finalization only after modeled applier success, sink receipt record, and completion report record.
* Modeled revoke-root pipeline success records finalization only after modeled applier success, sink receipt record, and completion report record.
* Modeled emergency-revoke-root pipeline success records finalization only after modeled applier success, sink receipt record, and completion report record.
* Modeled noop pipeline success records finalization only under explicit success semantics, no trust-state drift, a recorded receipt, and a recorded completion report.
* Duplicate identical finalization is idempotent under an explicit typed outcome and creates no second finalization.
* `CompletionReportDuplicateIdempotent` may only match an already-finalized completion and never creates a new finalization by itself.
* Existing Run 250 completion-reporter behavior remains compatible.
* Existing Run 248 sink behavior remains compatible.
* Existing Run 246 pipeline behavior remains compatible.
* Existing Run 244 modeled applier behavior remains compatible.
* Existing Run 242 mutation-engine behavior remains compatible.
* Existing Run 240 durable completion behavior remains compatible.
* Production finalizer path is reachable but unavailable/fail-closed and records no finalization.
* MainNet finalizer path is reachable but unavailable/fail-closed and records no finalization.
* MainNet peer-driven apply is refused before pipeline progression, before sink invocation, before reporter invocation, and before finalizer invocation.
* Validator-set rotation unsupported and records no finalization.
* Policy-change unsupported and records no finalization.

Rejected / fail-closed cases must prove at minimum:

* evaluator rejection produces no sink intent, no receipt, no reporter intent, no completion report, no finalizer intent, and no finalization;
* call-site rejection produces no sink intent, no receipt, no reporter intent, no completion report, no finalizer intent, and no finalization;
* durable replay stale produces no finalizer intent and no finalization;
* durable replay expired produces no finalizer intent and no finalization;
* durable replay consumed produces no finalizer intent and no finalization;
* durable replay superseded produces no finalizer intent and no finalization;
* durable replay backend unavailable produces no finalizer intent and no finalization;
* mutation-engine rejection produces no finalizer intent and no finalization;
* modeled applier rejected-before-snapshot produces no finalizer intent and no finalization;
* modeled applier rejected-before-apply produces no finalizer intent and no finalization;
* modeled apply failure produces no finalizer intent and no finalization;
* modeled rollback produces no finalizer intent and no finalization;
* modeled rollback-failed produces no finalizer intent and no finalization;
* modeled ambiguous window produces no finalizer intent and no finalization;
* sink did not record receipt produces no reporter/finalizer invocation and no finalization;
* sink rejected-before-record produces no reporter/finalizer invocation and no finalization;
* sink record failure produces no reporter/finalizer invocation and no finalization;
* sink rollback produces no reporter/finalizer invocation and no finalization;
* sink rollback-failed produces no reporter/finalizer invocation and no finalization;
* sink ambiguous window produces no reporter/finalizer invocation and no finalization;
* reporter did not record completion produces no finalizer invocation and no finalization;
* reporter rejected-before-record produces no finalizer invocation and no finalization;
* reporter record failure produces no finalizer invocation and no finalization;
* reporter rollback produces no finalizer invocation and no finalization;
* reporter rollback-failed produces no finalizer invocation and no finalization;
* reporter ambiguous window produces no finalizer invocation and no finalization;
* finalization before completion-report record is rejected;
* finalization after modeled apply failure is rejected;
* finalization after sink record failure is rejected;
* finalization after reporter record failure is rejected;
* finalization after reporter rollback is rejected;
* finalization after reporter rollback-failed is rejected;
* finalization after reporter ambiguous window is rejected;
* same finalization id with different digest is rejected as equivocation and records no second finalization;
* wrong environment rejected before finalizer invocation;
* wrong chain rejected before finalizer invocation;
* wrong genesis rejected before finalizer invocation;
* wrong governance surface rejected before finalizer invocation;
* wrong mutation surface rejected before finalizer invocation;
* wrong finalization digest rejected before finalization record;
* wrong completion-report digest rejected before finalization record;
* wrong receipt digest rejected before finalization record;
* wrong sink decision digest rejected before finalization record;
* wrong reporter decision digest rejected before finalization record;
* wrong pipeline decision digest rejected before finalization record;
* wrong proposal id rejected before finalization record;
* wrong decision id rejected before finalization record;
* wrong candidate digest rejected before finalization record;
* wrong authority-domain sequence rejected before finalization record;
* malformed finalization rejected before record;
* local operator key cannot satisfy MainNet authority;
* peer majority cannot satisfy MainNet authority;
* production finalizer unavailable records no finalization;
* MainNet finalizer unavailable records no finalization;
* validator-set rotation attempt unsupported and records no finalization;
* policy-change attempt unsupported and records no finalization;
* every rejected path produces no Run 070 call, no `LivePqcTrustState` mutation, no real trust swap, no session eviction, no sequence write, no marker write, no RocksDB/file/schema/migration/storage-format change, and no finalizer invocation where rejection happens before the finalizer stage.

Recovery / crash-window cases must prove at minimum:

* before-pipeline window fails closed / no finalization;
* after-pipeline-success-before-sink-intent window fails closed / no finalization;
* after-sink-intent-before-receipt-record window fails closed / no finalization;
* after-receipt-record-before-report-intent window fails closed / no finalization;
* after-report-intent-before-report-record window fails closed / no finalization;
* after-report-record-before-finalization-intent window fails closed / no finalization;
* after-finalization-intent-before-finalization-record window fails closed / no durable completion finality;
* after-finalization-record-before-finalization-success window fails closed unless an explicit matching finalization success exists;
* after-finalization-success recovers as durable-completion finalized;
* after-finalization-ambiguous fails closed / no finalization;
* finalization record-failed window fails closed / no finalization;
* finalization rollback-completed window fails closed / no finalization;
* finalization rollback-failed window is fatal/fail-closed / no finalization;
* unknown window fails closed / no finalization;
* production/MainNet recovery classification unavailable/fail-closed;
* MainNet peer-driven apply refusal precedes recovery classification where applicable.

Projection cases must prove at minimum:

* only Run 250 `CompletionReportRecorded` creates a finalization intent;
* Run 246 pipeline success alone creates no finalization intent;
* Run 248 sink receipt record alone creates no finalization intent;
* Run 250 completion-report intent alone creates no finalization intent;
* `CompletionReportDuplicateIdempotent` creates no new finalization unless a matching finalization already exists;
* evaluator success alone creates no finalization intent;
* durable replay freshness alone creates no finalization intent;
* mutation-engine authorization alone creates no finalization intent;
* modeled applier success without sink receipt and completion report creates no finalization intent;
* only `DurableCompletionFinalized` authorizes modeled durable-completion-finalized state;
* `LegacyBypassNoFinalization` does not finalize;
* `RejectedBeforeReporterNoFinalization` does not finalize;
* `ReporterDidNotRecordCompletionNoFinalization` does not finalize;
* `DurableCompletionDuplicateIdempotent` does not create a second finalization and must not be counted as a new finalization;
* `DurableCompletionRejectedBeforeRecord` does not finalize;
* `DurableCompletionRecordFailedNoFinalization` does not finalize;
* `DurableCompletionRolledBackNoFinalization` does not finalize;
* `DurableCompletionRollbackFailedFatalNoFinalization` does not finalize;
* `DurableCompletionAmbiguousFailClosedNoFinalization` does not finalize;
* `ProductionFinalizerUnavailableNoFinalization` does not finalize;
* `MainNetFinalizerUnavailableNoFinalization` does not finalize;
* `MainNetPeerDrivenApplyRefusedNoFinalization` does not finalize;
* `ValidatorSetRotationUnsupportedNoFinalization` does not finalize;
* `PolicyChangeUnsupportedNoFinalization` does not finalize.

Stage-ordering cases must prove at minimum:

* MainNet peer-driven apply refusal happens before pipeline progression, sink invocation, reporter invocation, and finalizer invocation.
* Evaluator/call-site authorization happens before durable replay freshness.
* Durable replay freshness happens before mutation-engine authorization.
* Mutation-engine authorization happens before modeled applier invocation.
* Modeled applier success happens before sink intent.
* Sink receipt recording happens after pipeline consume authorization.
* Completion report recording happens after sink receipt-recorded state.
* Finalization intent happens only after completion-report recorded state.
* Durable-completion finalization recording happens after completion-report recorded state.
* A rejection before the finalizer stage leaves the finalizer invocation count at zero.
* A finalization record failure does not retroactively invalidate the completion-report record but does not authorize durable-completion finality.
* A finalization rollback failure is fatal/fail-closed and does not authorize durable-completion finality.
* The finalizer itself performs no persistent durable completion beyond modeled in-memory fixture state.

Finalization-ledger cases must prove at minimum:

* one valid finalization inserts exactly one modeled in-memory ledger record;
* duplicate identical finalization is idempotent and does not increase record count;
* same finalization id with different digest is equivocation and does not increase record count;
* wrong finalization digest does not record;
* wrong completion-report digest does not record;
* wrong receipt digest does not record;
* wrong sink decision digest does not record;
* wrong reporter decision digest does not record;
* wrong pipeline decision digest does not record;
* wrong proposal id does not record;
* wrong decision id does not record;
* wrong candidate digest does not record;
* wrong authority-domain sequence does not record;
* malformed finalization does not record;
* rollback restores modeled finalization ledger snapshot;
* rollback failure never claims success;
* fixture finalization ledger never writes files, RocksDB, markers, sequences, or production durable state.

Non-mutation cases must prove at minimum:

* no Run 070 call on any helper case;
* no `LivePqcTrustState` mutation on any helper case;
* no real trust swap on any helper case;
* no session eviction on any helper case;
* no sequence write on any helper case;
* no marker write on any helper case;
* no RocksDB/file/schema/migration/storage-format write or format change;
* no wire/schema/marker/sequence/trust-bundle format change;
* fixture finalizer mutates only `ModeledDurableCompletionFinalizationLedger`;
* production/MainNet paths remain unavailable/fail-closed;
* MainNet peer-driven apply remains refused first.

Real release-binary surface scenarios:

The harness must run the real `target/release/qbind-node` for at least:

* S1 — `--help` / default help surface: no durable-completion finalization enablement banner or visible public flag drift.
* S2 — default DevNet startup parse/smoke surface: no finalization enablement claim.
* S3 — default TestNet startup parse/smoke surface: no finalization enablement claim.
* S4 — default MainNet startup parse/smoke surface: no finalization enablement claim.
* S5 — hidden governance-execution selector or equivalent hidden selector still parses as before, without exposing the Run 252/253 helper-only path as a public production enablement surface.
* S6 — invalid governance-execution selector or equivalent invalid selector fails closed before mutation if applicable, with no marker write, no sequence write, no live trust swap, no session eviction, no Run 070 call, and no finalizer invocation.

Do not add any new public CLI surface for Run 253. If a hidden selector already exists from earlier runs, the harness may use it only as a parse/fail-closed smoke surface.

Denylist:

Prove the captured real-binary logs and helper logs do not contain active/enabled claims for:

* real finalization backend enabled;
* modeled finalization production enabled;
* MainNet modeled finalization enabled;
* real completion-report backend enabled;
* modeled completion-reporter production enabled;
* MainNet modeled completion-reporter enabled;
* real durable consume backend enabled;
* real persistent replay backend enabled;
* modeled durable-consume sink production enabled;
* MainNet modeled durable-consume sink enabled;
* real production mutation engine enabled;
* MainNet mutation engine enabled;
* MainNet governance enabled;
* MainNet peer-driven apply enabled;
* real governance execution engine enabled;
* real on-chain governance proof verifier enabled;
* RocksDB replay backend enabled;
* file replay backend enabled;
* schema migration enabled;
* storage-format migration enabled;
* KMS/HSM backend enabled;
* RemoteSigner backend enabled;
* validator-set rotation enabled;
* policy-change action enabled;
* autonomous apply;
* apply-on-receipt;
* peer-majority authority;
* Run 070 apply from the modeled finalizer;
* LivePqcTrustState mutation from the modeled finalizer;
* real trust swap from the modeled finalizer;
* session eviction from the modeled finalizer;
* marker write from the modeled finalizer;
* sequence write from the modeled finalizer;
* RocksDB write from the modeled finalizer;
* file write from the modeled finalizer;
* production durable consume by the modeled finalizer itself;
* production finalization by the modeled finalizer itself;
* DummySig / DummyKem / DummyAead active on production path.

Source/release reachability proof:

The harness must grep production source for the Run 252 symbols and prove the release helper exercises them in release mode.

Required greps include:

* `pqc_governance_modeled_durable_completion_finalization_projection`
* `GovernanceModeledDurableCompletionFinalizationInput`
* `GovernanceModeledDurableCompletionFinalizationExpectations`
* `GovernanceModeledDurableCompletionFinalizationPolicy`
* `GovernanceModeledDurableCompletionFinalization`
* `GovernanceModeledDurableCompletionFinalizationSurface`
* `GovernanceModeledDurableCompletionFinalizationEnvironmentBinding`
* `GovernanceModeledDurableCompletionFinalizationRuntimeBinding`
* `GovernanceModeledDurableCompletionFinalizationReplayBinding`
* `GovernanceModeledDurableCompletionFinalizationPipelineBinding`
* `GovernanceModeledDurableCompletionFinalizationSinkBinding`
* `GovernanceModeledDurableCompletionFinalizationReporterBinding`
* `ModeledDurableCompletionFinalizationLedger`
* `ModeledDurableCompletionFinalizationRecord`
* `ModeledDurableCompletionFinalizationSnapshot`
* `ModeledDurableCompletionFinalizationStatus`
* `ModeledDurableCompletionFinalizationDigest`
* `GovernanceModeledDurableCompletionFinalizationOutcome`
* `GovernanceModeledDurableCompletionFinalizer`
* `FixtureModeledDurableCompletionFinalizer`
* `ProductionModeledDurableCompletionFinalizer`
* `MainNetModeledDurableCompletionFinalizer`
* `project_completion_reporter_outcome_to_finalization_intent`
* `evaluate_modeled_durable_completion_finalization`
* `recover_modeled_durable_completion_finalization_window`
* `finalization_outcome_authorizes_modeled_finalization`
* `finalization_outcome_projects_to_durable_completion_finalized`
* `DurableCompletionFinalized`
* `DurableCompletionDuplicateIdempotent`
* `DurableCompletionRejectedBeforeRecord`
* `DurableCompletionRecordFailedNoFinalization`
* `DurableCompletionRolledBackNoFinalization`
* `DurableCompletionRollbackFailedFatalNoFinalization`
* `DurableCompletionAmbiguousFailClosedNoFinalization`
* `ProductionFinalizerUnavailableNoFinalization`
* `MainNetFinalizerUnavailableNoFinalization`
* `MainNetPeerDrivenApplyRefusedNoFinalization`
* `ValidatorSetRotationUnsupportedNoFinalization`
* `PolicyChangeUnsupportedNoFinalization`
* `modeled_finalization_rejection_is_non_mutating`
* `modeled_finalization_never_calls_run_070`
* `modeled_finalization_never_mutates_live_pqc_trust_state`
* `modeled_finalization_never_writes_sequence_or_marker`
* `modeled_finalization_no_rocksdb_file_schema_migration_change`
* `modeled_finalization_pipeline_success_required_before_finalization`
* `modeled_finalization_sink_receipt_required_before_finalization`
* `modeled_finalization_completion_report_required_before_finalization`
* `modeled_finalization_record_required_before_durable_completion`
* `modeled_finalization_failed_record_never_finalizes`
* `modeled_finalization_rollback_never_finalizes`
* `modeled_finalization_ambiguous_window_fails_closed`
* `modeled_finalization_mainnet_peer_driven_apply_refused_first`
* `modeled_finalization_production_mainnet_unavailable`
* `modeled_finalization_validator_set_rotation_unsupported`
* `modeled_finalization_policy_change_unsupported`
* `modeled_finalization_local_operator_cannot_satisfy_mainnet_authority`
* `modeled_finalization_peer_majority_cannot_satisfy_mainnet_authority`

If exact symbol names differ, use the exact implemented Run 252 names and record the differences honestly in `QBIND_DEVNET_EVIDENCE_RUN_253.md`.

Validation commands:

Run at minimum:

* `cargo build -p qbind-node --release --bin qbind-node`
* `cargo build -p qbind-node --release --example run_253_modeled_durable_completion_finalization_projection_release_binary_helper`
* `bash scripts/devnet/run_253_modeled_durable_completion_finalization_projection_release_binary.sh`
* `cargo test -p qbind-node --test run_252_modeled_durable_completion_finalization_projection_tests`
* `cargo test -p qbind-node --test run_250_modeled_durable_consume_completion_reporter_tests`
* `cargo test -p qbind-node --test run_248_modeled_durable_consume_projection_sink_tests`
* `cargo test -p qbind-node --test run_246_governance_modeled_end_to_end_pipeline_tests`
* `cargo test -p qbind-node --test run_244_modeled_governance_trust_mutation_applier_tests`
* `cargo test -p qbind-node --test run_242_governance_execution_mutation_engine_tests`
* `cargo test -p qbind-node --test run_240_governance_evaluator_replay_durable_runtime_integration_tests`
* `cargo test -p qbind-node --test run_238_governance_evaluator_replay_durable_backend_tests`
* `cargo test -p qbind-node --test run_236_governance_evaluator_replay_consume_runtime_integration_tests`
* `cargo test -p qbind-node --test run_234_governance_evaluator_replay_consume_boundary_tests`
* `cargo test -p qbind-node --test run_232_governance_evaluator_replay_runtime_integration_tests`
* `cargo test -p qbind-node --test run_230_governance_evaluator_replay_state_tests`
* `cargo test -p qbind-node --test run_228_peer_evaluator_context_representation_tests`
* `cargo test -p qbind-node --test run_226_governance_evaluator_runtime_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_224_governance_evaluator_runtime_integration_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

Security scanning:

* Run secret scanning if available and report the result.
* Run CodeQL if available and report the result honestly.
* If Rust CodeQL analysis is skipped because the database is too large, say that explicitly and do not overclaim CodeQL coverage.

Documentation:

Create:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_253.md`
* `docs/devnet/run_253_modeled_durable_completion_finalization_projection_release_binary/README.md`
* `docs/devnet/run_253_modeled_durable_completion_finalization_projection_release_binary/summary.txt`
* `docs/devnet/run_253_modeled_durable_completion_finalization_projection_release_binary/.gitignore`

Update narrowly:

* `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
* `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
* `docs/whitepaper/contradiction.md`

Documentation must state:

* Run 253 is release-binary evidence for Run 252.
* No production mutating behavior is enabled.
* The modeled durable-completion finalization-projection boundary is release-evidenced, not production-enabled.
* The release helper exercises the Run 252 production library symbols in release mode.
* The release helper remains dead code from the production runtime.
* The fixture finalizer mutates only modeled in-memory `ModeledDurableCompletionFinalizationLedger` state.
* Run 246 pipeline success is required before any sink intent can exist.
* Run 248 `ConsumeReceiptRecorded` is required before any completion-report intent can exist.
* Run 250 `CompletionReportRecorded` is required before any finalization intent can exist.
* Only `DurableCompletionFinalized` authorizes a new modeled durable-completion-finalized state.
* `CompletionReportDuplicateIdempotent` may only match an already-finalized completion and never creates one by itself.
* Duplicate identical finalization is idempotent and creates no second finalization.
* Same finalization id with different digest fails closed as equivocation.
* Every non-recording reporter outcome produces no finalizer invocation and no finalization.
* Failed finalization record, rollback, rollback-failed, ambiguous windows, unavailable production/MainNet finalizer paths, rejected replay states, and unsupported actions never finalize.
* MainNet peer-driven apply remains refused before pipeline progression, sink invocation, reporter invocation, and finalizer invocation.
* Rejected finalizer paths are non-mutating.
* No real finalization backend is implemented.
* No real completion-report backend is implemented.
* No real durable consume backend is implemented.
* No real persistent replay backend is implemented.
* No real production mutation engine is implemented.
* No real governance execution engine is implemented.
* No real on-chain governance proof verifier is implemented.
* No RocksDB/file/schema/migration/storage-format change is implemented.
* No wire/schema/marker/sequence/trust-bundle change is implemented.
* No KMS/HSM/RemoteSigner backend is implemented.
* No MainNet governance enablement.
* No MainNet peer-driven apply enablement.
* No validator-set rotation.
* The finalizer does not call Run 070.
* The finalizer does not mutate `LivePqcTrustState`.
* The finalizer does not perform a real trust swap.
* The finalizer does not evict sessions.
* The finalizer does not write sequence files.
* The finalizer does not write authority markers.
* Full C4 remains OPEN.
* C5 remains OPEN.

Contradiction handling:

* Crosscheck Run 253 against Runs 050–252.
* If no contradiction is found, add a “no Run 253 contradiction” entry to `docs/whitepaper/contradiction.md`.
* If any contradiction is found, do not hide it. Record it explicitly with the exact file/function/doc reference and recommend the smallest safe fix.

Strict non-goals:

* Do not implement a real persistent replay backend.
* Do not implement a real durable consume backend.
* Do not implement a real completion-report backend.
* Do not implement a real finalization backend.
* Do not implement a real production mutation engine.
* Do not wire production runtime mutation.
* Do not call Run 070.
* Do not mutate `LivePqcTrustState`.
* Do not perform a real trust swap.
* Do not evict sessions.
* Do not write sequence files.
* Do not write authority markers.
* Do not implement a real governance execution engine.
* Do not implement a real on-chain governance proof verifier.
* Do not implement validator-set rotation.
* Do not add RocksDB/file/schema/migration/storage-format changes.
* Do not change wire/schema/marker/sequence/trust-bundle formats.
* Do not enable MainNet governance.
* Do not enable MainNet peer-driven apply.
* Do not add autonomous apply or apply-on-receipt.
* Do not let peer majority satisfy authority.
* Do not weaken any fail-closed behavior from Runs 070 or 130–252.

Expected verdict:

Positive for release-binary evidence, if the release helper/harness and regression corpus pass.

Run 253 should close the Run 252 release-binary evidence gap only. It must not claim Full C4 or C5 closure.

Final response format:

1. Exact verdict.
2. Files changed.
3. Release artifacts and hashes.
4. Helper corpus results.
5. Real-binary scenarios.
6. Tests run and results.
7. Security invariants preserved.
8. Honest limitations.
9. C4/C5 status.
10. Suggested Run 254 next step.
