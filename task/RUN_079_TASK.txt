Continue QBIND from the current state.

Project:
- QBIND post-quantum L1
- security-first, production-grade, execution-first
- C4 is the main live operability tracker
- C5 tracks timeout-verification activation, forged-traffic rejection, and transport-root dependency
- no broad redesign
- no silent regressions
- no hype / no overclaiming

Current state:
- Runs 037–045 hardened PQC transport.
- Runs 046–049 completed live timeout-backoff evidence through the 800-tick cap.
- Runs 050–068 completed the signed trust-bundle lifecycle path:
  - structured PQC trust bundles
  - signed ML-DSA-44 bundle verification
  - chain_id crosscheck
  - sequence anti-rollback persistence
  - activation-height gating
  - minimum activation-height policy
  - root/leaf revocation
  - per-entry revocation activation_height
  - local revoked-leaf startup self-check
  - local revoked issuer-root startup self-check
  - DevNet/TestNet/MainNet evidence
  - N=4 MainNet signed-bundle peer-connection evidence
- Run 069 landed validation-only local reload-check.
- Run 070 landed reload-apply contract and rollback semantics.
- Run 071 introduced LivePqcTrustState and live snapshot reads.
- Run 072 added production P2P session eviction.
- Run 073 landed ProductionLiveTrustApplyContext for process-start local reload-apply.
- Run 074 landed long-running SIGHUP local live reload-apply.
- Run 075 updated the operator runbook for Runs 069–074.
- Run 076 added library-level disabled-by-default peer-candidate validation:
  - PeerCandidateValidator
  - PeerCandidateEnvelope
  - 256 KiB cap
  - duplicate LRU
  - fixed-window rate limiter
  - valid candidates validate but are not applied
  - invalid candidates fail closed
  - no LivePqcTrustState handle
  - no session evictor
  - no sequence mutation
  - no _applied_total metric
- Run 077 added production-binary-facing local peer-candidate check mode:
  - hidden required-together flags:
    - --p2p-trust-bundle-peer-candidate-validation-enabled
    - --p2p-trust-bundle-peer-candidate-check <ENVELOPE_PATH>
  - valid local fixture validates but is not applied
  - invalid fixtures fail closed
  - sequence/live trust/session state remains unchanged
  - no node start in this mode
- Run 078 positive-narrow:
  - added typed/versioned/bounded P2P peer-candidate wire envelope
  - PeerCandidateWireEnvelopeV1
  - domain tag QBIND:PQC_TRUST_BUNDLE_PEER_CANDIDATE_WIRE:v1
  - version 1
  - reserved frame discriminator 0x05
  - no collision with p2p_tcp.rs frame types 0x01/0x02/0x03
  - strict fail-closed codec
  - MAX_PEER_CANDIDATE_WIRE_FRAME_BYTES enforced on declared payload_len before allocation
  - disabled-by-default PeerCandidateWireReceiver
  - wraps Run 076 → Run 069 validation pipeline
  - reuses seven Run 076 peer-candidate metrics
  - no _applied_total
  - no peer_candidate_wire_* metrics
  - startup banner hook only
  - no production listener/gossip subscription/admin API/filesystem watcher
  - no live receive-loop integration yet
- C4 remains OPEN.
- C5 remains OPEN / narrowed.

Current remaining C4 trust/lifecycle blockers include:
- live P2P receive-loop integration for peer-candidate validation
- peer-driven live apply / propagation
- activation_epoch runtime source
- in-binary / on-chain bundle-signing-key ratification
- external KMS/HSM custody integration
- production fast-sync / consensus-storage restore
- per-environment production trust-anchor operation
- selective per-peer session retention if ever wanted
- admin-API or filesystem-watcher triggers if ever desired

Task:
Run 079. Wire the disabled-by-default peer-candidate wire receiver into the live P2P frame dispatch path, while preserving validation-only semantics.

Primary objective:
A running qbind-node should be able to receive a bounded peer-candidate wire frame over the live P2P transport and validate it through the Run 078 PeerCandidateWireReceiver, without applying it, propagating it, persisting sequence, mutating LivePqcTrustState, or evicting sessions.

Important framing:
This is live P2P receive integration only.
This is validation-only.
This is NOT peer-driven live apply.
This is NOT automatic gossip propagation.
This is NOT trust-bundle synchronization.
This is NOT consensus ratification.
This is NOT activation_epoch.
This is NOT KMS/HSM.
This is NOT signing-key ratification.
This is NOT fast-sync restore.
Do not redesign KEMTLS.
Do not redesign consensus.
Do not weaken local operator reload-check.
Do not weaken local SIGHUP live reload.
Do not silently fall back to --p2p-trusted-root.
Do not silently fall back to DummySig, DummyKem, or DummyAead.
Do not claim full C4 or C5 closure.

Strict scope:
- Add live receive-loop dispatch for the reserved peer-candidate frame discriminator 0x05.
- Gate the handler behind the existing hidden Run 078 wire-validation flag.
- Default behavior unchanged when the flag is absent.
- Decode and size-bound the frame before expensive validation.
- Call PeerCandidateWireReceiver / PeerCandidateValidator only after cheap pre-checks.
- Do not call ProductionLiveTrustApplyContext.
- Do not call LiveReloadController.
- Do not call LivePqcTrustState::swap_snapshot.
- Do not call P2pSessionEvictor.
- Do not call check_and_update_sequence.
- Do not rebroadcast or forward peer candidate frames.
- Do not disconnect honest peers for invalid candidates unless the existing frame policy already requires it.
- Crosscheck all code against existing design/spec. If contradictions or inconsistencies are found, update docs/whitepaper/contradiction.md.

Required investigation:

1. Inspect live P2P receive loop
   Inspect:
   - crates/qbind-node/src/p2p_tcp.rs
   - crates/qbind-node/src/p2p_node_builder.rs
   - qbind-net frame/message types
   - existing frame discriminator handling for 0x01 / 0x02 / 0x03
   - read-loop error behavior
   - peer registry / connection handling
   - metrics hooks
   - crates/qbind-node/src/pqc_peer_candidate_wire.rs
   - crates/qbind-node/src/pqc_trust_peer_candidate.rs
   - docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_076.md
   - docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_077.md
   - docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_078.md
   - docs/whitepaper/contradiction.md

   Determine:
   - exact live read-loop insertion point.
   - how frame type 0x05 can be recognized without disturbing 0x01/0x02/0x03.
   - how to pass peer identity/source metadata into the validator if available.
   - whether invalid peer-candidate frames should be ignored, counted, or treated as protocol violations.
   - whether frame size is already bounded before read allocation.
   - whether a second pre-allocation bound is needed.

2. Define live receive semantics
   Required:
   - When wire-validation flag is disabled:
     - frame 0x05 is ignored or rejected cheaply.
     - no expensive validation.
     - disabled counter increments if the frame reaches the handler.
   - When enabled:
     - valid frame validates and logs validation-only/not-applied.
     - invalid frame rejects and counts rejection.
     - oversize frame drops before expensive validation.
     - duplicate frame suppresses repeated validation.
     - rate-limited frame drops/rejects before expensive validation.
   - All outcomes:
     - no LivePqcTrustState mutation.
     - no sequence mutation.
     - no session eviction.
     - no propagation.
     - no live apply metrics.
     - no reload-check metrics.
     - no Dummy/static fallback.

3. Define safety and DoS behavior
   Required:
   - max size enforced before full allocation or before expensive decode where feasible.
   - no unbounded task spawn per frame.
   - no unbounded buffering.
   - rate limit applies per peer if peer ID is available; otherwise global fixed-window limit.
   - duplicate suppression applies by candidate fingerprint/sequence or envelope fingerprint.
   - malformed frames do not panic.
   - malformed frames do not poison the read loop for honest traffic unless existing protocol policy says malformed frames close connection.

4. Define metrics/logging
   Reuse Run 076 counters:
   - qbind_p2p_pqc_trust_bundle_peer_candidate_received_total
   - qbind_p2p_pqc_trust_bundle_peer_candidate_validated_total
   - qbind_p2p_pqc_trust_bundle_peer_candidate_rejected_total
   - qbind_p2p_pqc_trust_bundle_peer_candidate_dropped_oversize_total
   - qbind_p2p_pqc_trust_bundle_peer_candidate_rate_limited_total
   - qbind_p2p_pqc_trust_bundle_peer_candidate_duplicate_total
   - qbind_p2p_pqc_trust_bundle_peer_candidate_disabled_total

   Requirements:
   - no _applied_total family.
   - no new unnecessary peer_candidate_wire_* metric family.
   - local reload-check metrics must not move.
   - live-reload apply metrics must not move.
   - session eviction metrics must not move.
   - logs must include:
     - peer/source if available
     - frame type
     - candidate sequence/fingerprint prefix if decoded
     - outcome
     - validation-only/not-applied
   - no private material.
   - no bundle bytes dumped.

Required implementation:

1. Wire live P2P frame dispatch
   In the live read loop:
   - recognize frame discriminator 0x05.
   - if disabled, call disabled/cheap path and continue according to policy.
   - if enabled, bound/decode through PeerCandidateWireEnvelopeV1.
   - call PeerCandidateWireReceiver.
   - record outcome metrics.
   - do not propagate.
   - do not apply.

2. Add flag plumbing
   Ensure the Run 078 wire-validation flag reaches the live P2P service / receive loop.
   If current flag only logs a banner, wire it into the service configuration.

3. Preserve existing frame behavior
   Existing 0x01/0x02/0x03 frames must behave exactly as before.
   Existing tests for PQC static-root mutual auth and KEMTLS real AEAD must still pass.

4. Preserve Run 077 local check
   Local peer-candidate check mode remains:
   - non-mutating
   - no node start
   - deterministic exit code

5. Preserve local reload paths
   Ensure:
   - Run 069 reload-check unchanged.
   - Run 073 process-start apply unchanged.
   - Run 074 SIGHUP live reload unchanged.
   - peer candidate receive cannot trigger any of them.

Required tests:

1. Unit tests
   - disabled live handler does not validate.
   - enabled valid frame validates and is not applied.
   - invalid frame rejects.
   - oversize frame drops before validation.
   - duplicate frame suppresses.
   - rate limit triggers.
   - unknown version rejects.
   - malformed frame rejects without panic.
   - 0x01/0x02/0x03 frame handling unaffected if testable.
   - no sequence mutation.
   - no LivePqcTrustState mutation.
   - no session eviction.

2. Integration tests
   - N=2 P2P nodes connect with wire validation disabled.
   - sending/injecting frame 0x05 while disabled does not validate and does not break honest connection.
   - N=2 P2P nodes connect with wire validation enabled.
   - send valid candidate frame over live P2P path; candidate validates, not applied.
   - send invalid candidate frame; candidate rejects, no mutation.
   - send oversize frame; dropped before expensive validation.
   - duplicate frame suppressed.
   - session remains alive after candidate validation/rejection.
   - sequence file unchanged.
   - live trust snapshot unchanged.
   - session eviction counters unchanged.
   - live reload apply counters unchanged.
   - local check mode from Run 077 still works.
   - SIGHUP live reload from Run 074 still works.

Required regression commands:
- cargo test -p qbind-node --lib pqc_peer_candidate_wire
- cargo test -p qbind-node --lib pqc_trust_peer_candidate
- cargo test -p qbind-node --lib pqc_peer_candidate_binary
- new Run 079 live wire tests
- cargo test -p qbind-node --test run_076_pqc_peer_candidate_validation_tests
- cargo test -p qbind-node --test run_077_pqc_peer_candidate_binary_tests
- cargo test -p qbind-node --test run_078_pqc_peer_candidate_wire_tests
- cargo test -p qbind-node --test run_069_pqc_trust_bundle_reload_check_tests
- cargo test -p qbind-node --test run_073_pqc_trust_bundle_reload_apply_runtime_tests
- cargo test -p qbind-node --test run_074_pqc_trust_bundle_live_reload_tests
- cargo test -p qbind-node --lib metrics
- cargo test -p qbind-node --lib p2p
- cargo test -p qbind-node --lib
- cargo test -p qbind-net --lib
- cargo test -p qbind-crypto --lib
- cargo build --release -p qbind-node --bin qbind-node
- cargo build --release -p qbind-node --example devnet_pqc_trust_bundle_helper
- cargo build --release -p qbind-node --example devnet_pqc_root_helper

Because this touches P2P wire/message handling, also run:
- cargo test -p qbind-node --test run_037_pqc_static_root_mutual_auth_tests
- cargo test -p qbind-node --test run_040_pqc_static_root_real_aead_tests
- cargo test -p qbind-net --test run_052_leaf_revocation_handshake_tests

If source touches timeout verification or binary consensus loop, also run:
- cargo test -p qbind-node --lib binary_consensus
- cargo test -p qbind-node --lib forged_injection
- cargo test -p qbind-node --lib run030
- cargo test -p qbind-consensus --lib timeout

Release-binary evidence:

1. Disabled-by-default N=2 baseline
   - start N=2 signed-bundle nodes without wire-validation flag.
   - prove normal peer connection works.
   - send/inject 0x05 candidate frame if possible.
   - prove disabled/cheap path or no validation.
   - prove connection remains healthy.
   - prove no sequence mutation.
   - prove no session eviction.
   - no fallback.
   - no Dummy crypto.

2. Enabled valid peer-candidate frame
   - start N=2 with wire-validation flag enabled.
   - send valid 0x05 candidate frame over live P2P transport.
   - prove candidate validates.
   - prove logs/metrics say validation-only/not-applied.
   - prove sequence file unchanged.
   - prove LivePqcTrustState unchanged.
   - prove sessions remain alive.
   - prove no session eviction metrics.
   - prove no live reload apply metrics.

3. Invalid peer-candidate frame
   - send tampered or wrong-chain 0x05 frame.
   - prove rejected.
   - prove no sequence/live-trust/session mutation.

4. Oversize frame
   - send oversize 0x05 frame.
   - prove dropped before expensive validation.
   - prove oversize metric increments.
   - prove no crash.

5. Duplicate/rate-limit frame
   - send duplicate or burst.
   - prove duplicate/rate-limit metric moves.

Evidence document:
Create:
- docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_079.md

Evidence document must include:
- exact objective
- exact verdict
- exact files changed
- exact commands run
- tests/evidence run and pass/fail status
- investigation findings with file/function references
- live receive-loop insertion point
- enablement flag plumbing
- frame semantics
- validation-only semantics
- DoS controls
- metrics/logging
- proof local check mode unchanged
- proof local reload paths unchanged
- release-binary evidence
- proof valid peer candidate is not applied
- proof invalid peer candidate does not mutate state
- proof no sequence burn
- proof no session eviction
- proof no fallback to --p2p-trusted-root
- proof no DummySig/DummyKem/DummyAead fallback
- exact boundaries:
  - no peer-driven live apply
  - no propagation/rebroadcast
  - no consensus ratification
  - activation_epoch still absent
  - KMS/HSM still absent
  - signing-key ratification still absent
- exact immediate next action

contradiction.md update rules:
- If disabled-by-default live P2P receive-loop integration lands, update C4 to record peer/gossiped trust-bundle acceptance narrowed to validation-only live receive.
- If release-binary evidence cannot send a real frame and only harness injection is possible, state exact boundary.
- Keep C4 open for:
  - peer-driven live apply / propagation
  - activation_epoch runtime source
  - in-binary/on-chain bundle-signing-key ratification
  - KMS/HSM custody
  - production fast-sync / consensus-storage restore
  - per-environment production trust-anchor operation
- Do not close full C4.
- Do not claim C5 closure.
- If peer candidate mutates live trust state or burns sequence, record contradiction/regression.
- If invalid peer candidate evicts sessions, record contradiction/regression.
- If peer candidate bypasses signed-bundle validation, record contradiction/regression.
- If failure falls back to static roots or Dummy crypto, record contradiction/regression.

Expected verdict options:
- Strongest positive:
  disabled-by-default live P2P receive-loop integration lands; release binary receives real 0x05 frame over live P2P transport; valid peer candidate validates but is not applied; invalid/oversize/duplicate/rate-limited candidates fail closed; sequence, live trust, sessions, and apply metrics remain unchanged; no propagation; no fallback; no Dummy crypto.
- Partial positive:
  live dispatch path lands but release-binary evidence uses harness injection or cannot prove real frame delivery. State exact boundary.
- Negative:
  peer candidate mutates state, burns sequence, bypasses validation, evicts sessions, triggers apply, causes fallback/Dummy crypto, or regressions fail.

Required final response:
1. Exact verdict
2. Exact files changed
3. Exact commands run
4. Tests/evidence run and pass/fail status
5. What was fixed
6. What was proven
7. What remains not solved
8. Whether contradiction.md was updated and why
9. Exact immediate next action recommended