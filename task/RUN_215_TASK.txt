Task: Run 215 — Source/Test Governance Execution Policy Selector and Production Preflight Integration

Current accepted state:

* Run 211: source/test governance execution policy boundary landed.
* Run 212: release-binary governance execution policy-boundary evidence fully closed.
* Run 213: source/test governance execution payload/carrying and production-context wiring landed.
* Run 214: release-binary governance execution payload/carrying evidence fully closed:

  * real `target/release/qbind-node` preserves default governance-execution-disabled behavior;
  * legacy no-governance-execution payloads remain compatible;
  * fixture governance execution reaches production-context paths in release mode through helper/library paths;
  * production/on-chain/MainNet governance execution reaches evaluator and fails closed as unavailable;
  * malformed/invalid governance execution material fails closed;
  * governance execution digest preservation through wire conversion is proven;
  * lifecycle action authorization is bound to action, candidate digest, and sequence;
  * validator-set rotation remains unsupported;
  * rejected governance-execution-payload cases produce no mutation;
  * MainNet peer-driven apply remains refused;
  * no real governance execution engine, real on-chain proof verifier, KMS-HSM backend, RemoteSigner backend, or validator-set rotation exists.
* Current limitation:
  governance-execution payloads can be carried and evaluated through production-context helpers, but no hidden production preflight selector exists to choose `GovernanceExecutionPolicy::{FixtureGovernanceAllowed, EmergencyCouncilFixtureAllowed, ProductionGovernanceRequired, MainnetGovernanceRequired}` at binary/source call sites.
* Full C4 remains OPEN.
* C5 remains OPEN.

Objective:
Add a source/test-only hidden governance execution policy selector and wire it into production v2 marker-decision preflight contexts.

This run must allow DevNet/TestNet evidence surfaces to explicitly require fixture governance execution material, while preserving `GovernanceExecutionPolicy::Disabled` by default and preserving fail-closed production/MainNet governance behavior.

Release-binary governance execution policy selector evidence is deferred to Run 216.

Strict scope:

* Source/test only.
* No release-binary harness in this run.
* Hidden selector only.
* Disabled by default.
* No real governance execution engine.
* No real on-chain governance proof verifier.
* No MainNet governance enablement.
* No MainNet peer-driven apply enablement.
* No validator-set rotation.
* No authority-set rotation beyond existing lifecycle boundary checks.
* No real KMS implementation.
* No real HSM implementation.
* No real RemoteSigner backend.
* No production signing key custody.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* No schema/wire changes.
* No authority-marker schema change.
* No sequence-file schema change.
* No trust-bundle core schema change.
* No authority lifecycle semantics change.
* Do not weaken Runs 070, 130–214.
* Do not claim full C4 or C5 closure.

Required selector design:
Add a hidden governance-execution policy selector. Prefer one hidden CLI flag plus one env var.

CLI:

* `--p2p-trust-bundle-governance-execution-policy <disabled|fixture-governance-allowed|emergency-council-fixture-allowed|production-governance-required|mainnet-governance-required>`
* hidden via clap.

Env:

* `QBIND_P2P_TRUST_BUNDLE_GOVERNANCE_EXECUTION_POLICY=disabled|fixture-governance-allowed|emergency-council-fixture-allowed|production-governance-required|mainnet-governance-required`

Required default:

* unset CLI/env => `GovernanceExecutionPolicy::Disabled`.

Required policy constraints:

* `FixtureGovernanceAllowed`:

  * DevNet/TestNet evidence only.
  * must not satisfy MainNet production governance execution.
* `EmergencyCouncilFixtureAllowed`:

  * DevNet/TestNet evidence only.
  * emergency actions must remain explicit and separate.
  * must not satisfy MainNet production governance execution.
* `ProductionGovernanceRequired`:

  * must fail closed because no real governance execution engine exists.
* `MainnetGovernanceRequired`:

  * must fail closed.
  * MainNet peer-driven apply remains refused regardless.
* invalid selector values:

  * fail closed or reject startup/preflight with typed parse error.
* unrelated flags/env vars must not enable governance execution policy.
* if both CLI and env exist, CLI must take precedence unless an existing QBIND selector convention dictates otherwise; document deterministic precedence.

Required source behavior:

1. Add selector helpers, preferably in or near:

   * `pqc_governance_execution_payload_carrying.rs`;
   * or a new `pqc_governance_execution_policy_surface.rs`.

2. Helpers must include grep-verifiable names similar to:

   * `governance_execution_policy_from_selector`;
   * `governance_execution_policy_from_cli_or_env`;
   * `governance_execution_policy_env_selector`.

3. Wire selected governance execution policy into all seven production v2 marker-decision preflight contexts:

   * reload-check;
   * reload-apply;
   * startup `--p2p-trust-bundle`;
   * SIGHUP;
   * local peer-candidate-check;
   * live inbound `0x05`;
   * peer-driven drain.

4. The selected policy must reach the Run 213 governance-execution payload/call-site routing layer.

5. Existing no-governance-execution payloads remain compatible under default Disabled.

6. Under explicit governance execution policy:

   * missing governance execution material fails closed where governance execution is required;
   * valid fixture governance execution passes only where policy permits;
   * emergency governance execution passes only under explicit emergency fixture policy;
   * production/on-chain/MainNet governance execution material reaches evaluator and fails closed as unavailable;
   * malformed material fails closed;
   * validation-only surfaces remain non-mutating;
   * mutating surfaces preserve sequence-before-marker ordering.

Required accepted scenarios:
A1. default selector absent => Disabled; old no-governance-execution payload accepted where it was accepted before.

A2. CLI `fixture-governance-allowed` selects `FixtureGovernanceAllowed`; DevNet fixture governance execution accepted under source/test production-context path.

A3. env `fixture-governance-allowed` selects `FixtureGovernanceAllowed`; TestNet fixture governance execution accepted under source/test production-context path.

A4. CLI `emergency-council-fixture-allowed` selects `EmergencyCouncilFixtureAllowed`; DevNet emergency council fixture execution accepted only for explicit emergency action.

A5. CLI `production-governance-required` selects `ProductionGovernanceRequired`; production governance execution material reaches evaluator and fails closed as unavailable.

A6. env `mainnet-governance-required` selects `MainnetGovernanceRequired`; MainNet governance unavailable / MainNet refusal outcome reached.

A7. CLI-over-env precedence is deterministic:

* env sets `fixture-governance-allowed`;
* CLI sets `disabled`;
* resolved policy is Disabled.

A8. no-governance-execution payload remains compatible under default Disabled.

A9. GenesisBound / EmergencyCouncil / OnChainGovernance proof-carrier behavior remains unchanged when governance execution policy is Disabled.

A10. Run 193 custody-policy selector behavior remains compatible.

A11. Run 199 RemoteSigner-policy selector behavior remains compatible.

A12. Run 210 custody-attestation-policy selector behavior remains compatible.

A13. mutating DevNet fixture governance under FixtureGovernanceAllowed preflight accepts where lifecycle/governance proof/custody/governance execution all pass and sequence-before-marker ordering remains governed by existing apply path.

A14. lifecycle rotate action is authorized only when selected policy and carried decision authorize rotate with matching candidate digest and sequence.

A15. lifecycle revoke action is authorized only when selected policy and carried decision authorize revoke with matching candidate/revoked-key material and sequence.

A16. live inbound `0x05` receives selected policy if live config supports it; if not, document exact limitation.

Required rejection scenarios:
R1. invalid CLI selector value rejected with typed parse error.

R2. invalid env selector value rejected with typed parse error.

R3. unrelated CLI/env does not enable governance execution policy.

R4. no-governance-execution payload rejected under FixtureGovernanceAllowed.

R5. no-governance-execution payload rejected under ProductionGovernanceRequired.

R6. fixture governance rejected under ProductionGovernanceRequired.

R7. emergency fixture rejected under ProductionGovernanceRequired.

R8. fixture governance rejected under MainnetGovernanceRequired.

R9. production governance rejected as unavailable.

R10. on-chain governance rejected as unavailable.

R11. MainNet governance rejected as unavailable.

R12. unknown governance class rejected.

R13. malformed governance execution material rejected.

R14. wrong environment rejected.

R15. wrong chain rejected.

R16. wrong genesis rejected.

R17. wrong authority root rejected.

R18. wrong lifecycle action rejected.

R19. wrong candidate digest rejected.

R20. wrong authority-domain sequence rejected.

R21. wrong governance proof digest rejected.

R22. wrong on-chain proof digest rejected.

R23. wrong custody attestation digest rejected.

R24. wrong proposal id rejected.

R25. wrong decision id rejected.

R26. wrong effective epoch rejected.

R27. expired decision rejected.

R28. stale/replayed decision rejected.

R29. quorum threshold insufficient rejected.

R30. emergency action not authorized rejected.

R31. validator-set rotation unsupported rejected.

R32. policy-change action unsupported rejected.

R33. local operator cannot satisfy governance execution.

R34. peer majority / gossip count cannot satisfy governance execution.

R35. governance valid but lifecycle action mismatch rejected.

R36. lifecycle valid but governance decision invalid rejected.

R37. validation-only rejection writes no marker and no sequence.

R38. mutating rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

R39. invalid live inbound `0x05` governance-execution candidate is not propagated, staged, or applied.

R40. MainNet peer-driven apply remains refused even with MainnetGovernanceRequired and fixture governance approval.

Required tests:
Add focused tests, preferably:

`crates/qbind-node/tests/run_215_governance_execution_policy_selector_tests.rs`

Minimum coverage:

* A1–A16 where representable.
* R1–R40.
* selector parsing and precedence:

  * default;
  * CLI;
  * env;
  * CLI-over-env;
  * invalid value fail-closed.
* source reachability:

  * selected policy reaches all seven production-context helpers where representable.
* action authorization:

  * rotate;
  * revoke;
  * emergency revoke;
  * wrong action fail-closed.
* no-mutation invariants:

  * validation-only non-mutation;
  * mutating rejection non-mutation.
* MainNet refusal invariants.

Required documentation:
Add:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_215.md`

Update:

* `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
* `docs/whitepaper/contradiction.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

Documentation must state:

* Run 215 is source/test hidden governance execution policy selector and production preflight integration.
* Default remains `GovernanceExecutionPolicy::Disabled`.
* Fixture governance execution remains DevNet/TestNet evidence-only.
* Emergency council fixture execution is explicit and non-production.
* Fixture governance cannot satisfy MainNet production governance execution.
* Production/on-chain/MainNet governance execution remains unavailable/fail-closed.
* MainNet peer-driven apply remains refused.
* No real governance execution engine is implemented.
* No real on-chain governance proof verifier is implemented.
* No real KMS/HSM backend is implemented.
* No real RemoteSigner backend is implemented.
* validator-set rotation remains unsupported.
* release-binary governance execution policy selector evidence is deferred to Run 216.
* full C4 remains open.
* C5 remains open.

Validation commands:
Run at minimum:

* `cargo build -p qbind-node --lib`
* `cargo test -p qbind-node --test run_215_governance_execution_policy_selector_tests`
* `cargo test -p qbind-node --test run_213_governance_execution_payload_callsite_tests`
* `cargo test -p qbind-node --test run_211_governance_execution_policy_tests`
* `cargo test -p qbind-node --test run_209_custody_attestation_policy_selector_tests`
* `cargo test -p qbind-node --test run_207_custody_attestation_payload_callsite_tests`
* `cargo test -p qbind-node --test run_205_custody_attestation_verifier_tests`
* `cargo test -p qbind-node --test run_203_kms_hsm_backend_boundary_tests`
* `cargo test -p qbind-node --test run_201_remote_signer_transport_boundary_tests`
* `cargo test -p qbind-node --test run_198_remote_signer_policy_selector_tests`
* `cargo test -p qbind-node --test run_196_remote_signer_payload_callsite_tests`
* `cargo test -p qbind-node --test run_194_remote_authority_signer_boundary_tests`
* `cargo test -p qbind-node --test run_192_authority_custody_policy_selector_tests`
* `cargo test -p qbind-node --test run_190_authority_custody_payload_callsite_tests`
* `cargo test -p qbind-node --test run_188_authority_custody_boundary_tests`
* `cargo test -p qbind-node --test run_186_onchain_governance_production_verifier_boundary_tests`
* `cargo test -p qbind-node --test run_184_onchain_governance_payload_carrying_tests`
* `cargo test -p qbind-node --test run_182_onchain_governance_production_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_180_onchain_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_178_onchain_governance_proof_tests`
* `cargo test -p qbind-node --test run_176_live_0x05_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_173_validation_only_governance_required_policy_tests`
* `cargo test -p qbind-node --test run_171_governance_required_policy_selector_tests`
* `cargo test -p qbind-node --test run_169_governance_proof_loader_surface_integration_tests`
* `cargo test -p qbind-node --test run_167_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_165_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_163_governance_authority_verifier_tests`
* `cargo test -p qbind-node --test run_161_lifecycle_marker_integration_tests`
* `cargo test -p qbind-node --test run_159_authority_signing_key_lifecycle_tests`
* `cargo test -p qbind-node --test run_157_unified_testnet_fixture_universe_tests`
* `cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests`
* `cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests`
* `cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests`
* `cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests`
* `cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests`
* `cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Acceptance criteria:
Run 215 is acceptable only if:

1. A hidden governance execution policy selector exists.
2. Default remains Disabled.
3. Selector reaches production preflight contexts at source/test level.
4. Fixture governance execution passes only where selected policy allows.
5. Emergency council fixture execution is explicit and non-production.
6. Missing/malformed/invalid governance execution material fails closed under explicit policy.
7. Production/on-chain/MainNet governance execution remains fail-closed as unavailable.
8. Governance execution authorizes lifecycle action only when action, candidate digest, and sequence match.
9. Validation-only surfaces remain non-mutating.
10. Mutating rejection surfaces produce no mutation.
11. MainNet peer-driven apply remains refused even with MainnetGovernanceRequired and fixture governance approval.
12. Docs defer release-binary governance execution policy selector evidence to Run 216.
13. No real governance execution / on-chain proof verifier / KMS-HSM backend / RemoteSigner backend / validator-set rotation claim is made.
14. No full C4 or C5 closure is claimed.
