Task: Run 241 — Release-Binary Durable Governance Evaluator Replay Backend Runtime Integration Evidence

Current accepted state:

* Run 238: source/test durable replay state backend boundary landed.
* Run 239: release-binary durable replay state backend boundary evidence fully closed.
* Run 240: source/test durable replay backend runtime integration landed:

  * `crates/qbind-node/src/pqc_governance_evaluator_replay_durable_runtime_integration.rs`;
  * `DurableReplayRuntimeIntegrationInput`;
  * `DurableReplayRuntimeOutcome`;
  * `integrate_durable_replay_runtime`;
  * `recover_durable_replay_runtime_crash_window`;
  * `wire_durable_replay_runtime_callsite`;
  * durable read/observe happens before mutation authorization;
  * Run 230/232 freshness gate is used;
  * mutation authorization happens only on fresh / known-fresh;
  * compare-and-mark-consumed happens only after `AppliedSuccessfully`;
  * crash-window ambiguity is typed and fail-closed;
  * fixture restart snapshot remains source/test-only durability modeling;
  * production/MainNet durable backend remains unavailable/fail-closed;
  * MainNet peer-driven apply remains refused;
  * no real persistent replay backend;
  * no RocksDB/file/schema/migration/storage-format change;
  * no wire/schema/marker/sequence/trust-bundle change;
  * release-binary durable-runtime integration evidence deferred to Run 241.
* Full C4 remains OPEN.
* C5 remains OPEN.

Objective:
Produce release-binary evidence for the Run 240 durable governance evaluator replay backend runtime integration.

This run must prove in release mode that the durable backend is exercised as the runtime state provider through release-built code:

1. durable read/observe occurs before mutation authorization;
2. replay/freshness gate is applied using durable state;
3. mutation is authorized only on fresh / known-fresh;
4. compare-and-mark-consumed occurs only after successful mutation completion;
5. crash-window ambiguity is typed and fail-closed;
6. fixture restart snapshot durability remains evidence-only;
7. production/MainNet durable backend remains unavailable/fail-closed;
8. no mutation side effects occur from the evidence boundary.

Strict scope:

* Release-binary evidence only.
* Use release-built helper and real `target/release/qbind-node` where applicable.
* No production source behavior change unless a tiny harness-only fix is required.
* No real persistent replay backend.
* No RocksDB schema.
* No file format.
* No database migration.
* No real governance execution engine.
* No real mutation engine.
* No real on-chain governance proof verifier.
* No MainNet governance enablement.
* No MainNet peer-driven apply enablement.
* No validator-set rotation.
* No KMS/HSM backend implementation.
* No RemoteSigner backend implementation.
* No wire/schema/marker/sequence/trust-bundle change.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* Do not weaken Runs 070, 130–240.
* Do not claim full C4 or C5 closure.

Required deliverables:

1. Add release-binary helper:
   `crates/qbind-node/examples/run_241_governance_evaluator_replay_durable_runtime_integration_release_binary_helper.rs`

2. Add release-binary harness:
   `scripts/devnet/run_241_governance_evaluator_replay_durable_runtime_integration_release_binary.sh`

3. Add evidence archive:
   `docs/devnet/run_241_governance_evaluator_replay_durable_runtime_integration_release_binary/`

4. Add canonical evidence report:
   `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_241.md`

5. Update narrowly:

   * `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
   * `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
   * `docs/whitepaper/contradiction.md`
   * `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
   * `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
   * `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

6. Crosscheck all new/modified code and docs against existing design/spec. If contradictions are found, record them in `contradiction.md`.

Required release-helper matrix:
The release-built helper must exercise Run 240 durable runtime integration symbols through production library code.

Accepted / compatible cases:
A1. default Disabled / legacy bypass performs no durable write.

A2. first-seen DevNet fixture decision is observed as fresh.

A3. first-seen TestNet fixture decision is observed as fresh.

A4. known fresh decision proceeds as known fresh.

A5. deferred decision is observed but does not authorize mutation.

A6. fresh observed decision authorizes mutation only after replay/runtime agreement.

A7. `AppliedSuccessfully` performs compare-and-mark-consumed in DevNet fixture.

A8. `AppliedSuccessfully` performs compare-and-mark-consumed in TestNet fixture.

A9. same decision after durable consume reads consumed / fail-closed.

A10. read-only validation observes/reads but does not consume.

A11. apply failed after observe does not consume.

A12. rollback after observe does not consume.

A13. after-mutation-before-consume crash window is typed and fails closed.

A14. after-consume crash window reads consumed / fail-closed.

A15. fixture restart snapshot preserves observed state through runtime integration.

A16. fixture restart snapshot preserves consumed state through runtime integration.

A17. production durable backend path is reached and fails closed unavailable.

A18. MainNet durable backend path is reached and fails closed unavailable/refused.

A19. MainNet peer-driven apply remains refused even if durable state is fresh.

A20. Run 236 replay consume runtime integration remains compatible.

A21. Run 238 durable backend boundary remains compatible.

A22. Run 239 release durable-backend behavior remains compatible.

A23. Run 237 release consume-runtime behavior remains compatible.

A24. Run 235 release consume-boundary behavior remains compatible.

A25. `integrate_durable_replay_runtime` proves durable read/observe occurs before mutation authorization.

A26. compare-and-mark-consumed is reachable only after `AppliedSuccessfully`.

A27. crash-window recovery helper returns typed fail-closed outcomes for ambiguous windows.

Required rejection cases:
R1. expired durable state rejected before mutation.

R2. stale durable state rejected before mutation.

R3. replay detected rejected before mutation.

R4. consumed decision rejected before mutation.

R5. superseded decision rejected before mutation.

R6. malformed durable record rejected before mutation.

R7. backend unavailable rejected.

R8. production durable backend unavailable rejected.

R9. MainNet durable backend unavailable/refused rejected.

R10. wrong environment rejected.

R11. wrong chain rejected.

R12. wrong genesis rejected.

R13. wrong validation surface rejected.

R14. wrong mutation surface rejected.

R15. wrong replay state key digest rejected.

R16. wrong source identity digest rejected.

R17. wrong request digest rejected.

R18. wrong response digest rejected.

R19. wrong transcript digest rejected.

R20. wrong decision digest rejected.

R21. wrong proposal id rejected.

R22. wrong decision id rejected.

R23. wrong lifecycle action rejected.

R24. wrong candidate digest rejected.

R25. wrong authority-domain sequence rejected.

R26. wrong replay nonce rejected.

R27. compare-and-mark-consumed wrong expected state rejected.

R28. consume attempted before observe rejected.

R29. consume attempted before successful mutation rejected.

R30. consume attempted after failed apply rejected.

R31. consume attempted after rollback rejected.

R32. ambiguous crash window rejected.

R33. local operator cannot satisfy durable replay backend policy.

R34. peer majority cannot satisfy durable replay backend policy.

R35. validator-set rotation unsupported rejected.

R36. policy-change action unsupported rejected.

R37. rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

R38. MainNet peer-driven apply remains refused even when durable backend says fresh.

Required real release checks:
Use release artifacts to prove:

* helper binary SHA-256 and ELF Build ID captured;
* `qbind-node` release binary SHA-256 and ELF Build ID captured if built;
* Run 240 helper corpus passes;
* durable read/observe happens before mutation authorization;
* replay/freshness gate uses durable state;
* fresh / known-fresh is required before mutation authorization;
* compare-and-mark-consumed is after-success-only;
* fixture restart snapshot preserves observed state;
* fixture restart snapshot preserves consumed state;
* crash-window ambiguity is typed and fail-closed;
* production/MainNet durable backend unavailable is fail-closed;
* no real persistent backend active claim;
* no RocksDB schema drift;
* no file format drift;
* no database migration;
* no marker/sequence write from runtime integration layer;
* no Run 070 call from runtime integration layer;
* MainNet peer-driven apply remains refused;
* no real governance engine active claim;
* no real mutation engine active claim;
* no real on-chain proof verifier active claim;
* no MainNet governance enabled claim;
* no validator-set rotation claim;
* no KMS/HSM backend active claim;
* no RemoteSigner backend active claim;
* no peer-majority authority claim;
* no automatic apply / apply-on-receipt claim.

Required source/release reachability proof:
The report must include grep/call-site proof for:

* `pqc_governance_evaluator_replay_durable_runtime_integration`;
* `DurableReplayRuntimeIntegrationInput`;
* `DurableReplayRuntimeOutcome`;
* `integrate_durable_replay_runtime`;
* `recover_durable_replay_runtime_crash_window`;
* `wire_durable_replay_runtime_callsite`;
* `ProceedLegacyBypassNoDurableWrite`;
* `ProceedDeferredObserved`;
* `ProceedFreshObserved`;
* `ProceedKnownFresh`;
* `ProceedMutationAuthorized`;
* `ConsumeDurableAfterMutationSuccess`;
* `DoNotConsumeBeforeApply`;
* `DoNotConsumeApplyFailed`;
* `DoNotConsumeRolledBack`;
* `CrashWindowFailClosed`;
* `DurableReplayFailClosed`;
* `ReplayRuntimeFailClosed`;
* `ConsumeRuntimeFailClosed`;
* `ProductionDurableUnavailable`;
* `MainNetDurableUnavailable`;
* `MainNetPeerDrivenApplyRefused`;
* Run 238 durable backend usage;
* Run 236 consume runtime usage;
* Run 232 replay/freshness runtime usage;
* Run 230 replay/freshness state usage;
* production unavailable path;
* MainNet unavailable/refused path;
* MainNet peer-driven refusal guard.

Required mutation / no-mutation proof:
For every rejected durable-runtime-integration case:

* no Run 070 apply call;
* no live trust swap;
* no session eviction;
* no sequence write;
* no marker write;
* no `.tmp` residue;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead;
* read-only validation does not consume;
* rollback does not consume;
* failed apply does not consume;
* MainNet refused does not consume.

Required denylist:
Across the run prove:

* no MainNet apply;
* no autonomous apply;
* no apply on receipt;
* no peer-majority authority;
* no real governance execution engine claim;
* no real mutation engine claim;
* no production governance active claim;
* no MainNet governance enabled claim;
* no real on-chain governance proof verifier claim;
* no real persistent replay backend claim;
* no real KMS backend claim;
* no real HSM backend claim;
* no real RemoteSigner backend claim;
* no custody attestation production-active claim;
* no validator-set rotation claim;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead;
* no RocksDB schema change;
* no file format change;
* no database migration;
* no network wire schema change;
* no trust-bundle schema change;
* no marker schema change;
* no sequence schema change;
* no marker write before sequence commit;
* no sequence write on validation-only surfaces;
* no marker write on validation-only surfaces.

Required captured metadata:

* helper SHA-256 and ELF Build ID;
* qbind-node SHA-256 and ELF Build ID if built;
* git commit hash;
* rustc/cargo versions;
* exact commands;
* stdout/stderr logs;
* per-scenario exit codes;
* durable runtime outcome values;
* durable backend key digests;
* durable record digests;
* durable operation transcript digests;
* crash-window transcript digests;
* durable record state values;
* durable backend outcome values;
* durable consume outcome values;
* crash-window values;
* before/after fixture durable backend snapshots;
* marker/sequence before/after hashes where applicable;
* data-dir inventories;
* denylist grep results.

Validation commands:
Run at minimum:

* `cargo build --release -p qbind-node --bin qbind-node`
* `cargo build --release -p qbind-node --example run_241_governance_evaluator_replay_durable_runtime_integration_release_binary_helper`
* `bash scripts/devnet/run_241_governance_evaluator_replay_durable_runtime_integration_release_binary.sh`
* `cargo test -p qbind-node --test run_240_governance_evaluator_replay_durable_runtime_integration_tests`
* `cargo test -p qbind-node --test run_238_governance_evaluator_replay_durable_backend_tests`
* `cargo test -p qbind-node --test run_236_governance_evaluator_replay_consume_runtime_integration_tests`
* `cargo test -p qbind-node --test run_234_governance_evaluator_replay_consume_boundary_tests`
* `cargo test -p qbind-node --test run_232_governance_evaluator_replay_runtime_integration_tests`
* `cargo test -p qbind-node --test run_230_governance_evaluator_replay_state_tests`
* `cargo test -p qbind-node --test run_228_peer_evaluator_context_representation_tests`
* `cargo test -p qbind-node --test run_226_governance_evaluator_runtime_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_224_governance_evaluator_runtime_integration_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Documentation requirements:
`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_241.md` must state:

* Run 241 is release-binary durable replay backend runtime integration evidence.
* Durable replay backend is release-evidenced as the typed runtime state provider.
* Durable read/observe happens before mutation authorization.
* Compare-and-mark-consumed happens only after successful mutation completion.
* Crash-window ambiguity is typed and fail-closed.
* Fixture durable backend remains DevNet/TestNet evidence-only.
* Fixture restart snapshot models durability only for release evidence.
* Production/MainNet durable backend remains unavailable/fail-closed.
* No real persistent replay backend is implemented.
* No RocksDB/file/schema/migration/storage-format change is implemented.
* No real governance engine, mutation engine, or on-chain proof verifier is implemented.
* MainNet peer-driven apply remains refused.
* Validator-set rotation remains unsupported.
* Full C4 remains OPEN.
* C5 remains OPEN.

Acceptance criteria:
Run 241 is acceptable only if:

1. Release-built helper exercises the Run 240 durable runtime integration.
2. Durable read/observe-before-mutation ordering is release-evidenced.
3. Compare-and-mark-consumed-after-success-only ordering is release-evidenced.
4. Crash-window ambiguity is typed and fail-closed.
5. Fixture restart snapshot durability is release-evidenced without real file format.
6. Production/MainNet durable backend remains unavailable/fail-closed.
7. Rejections are non-mutating.
8. MainNet peer-driven apply remains refused.
9. No RocksDB/file/schema/migration/storage-format change is claimed.
10. Existing Run 239, Run 237, Run 235, Run 233, and Run 231 release behavior remains compatible.
11. No full C4 or C5 closure is claimed.
