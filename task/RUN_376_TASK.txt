Run 376 — Public DevNet M4/M6 live identity registration and seed-candidate admission boundary

Continue QBIND after accepted Run 375.

Current state:
Run 375 is accepted as PASS / public-DevNet first-class identity command positive.

Run 375 delivered:
- first-class `qbind-node identity generate|verify|print-public|seed-candidate`;
- DevNet-only generation for `full-node`, `seed`, and `validator-candidate`;
- schema-compatible `public-identity.json`;
- deterministic NodeId derivation from public KEMTLS leaf cert;
- `seed-candidate` output mapped to the Run 357 seed-list schema;
- loopback strict-auth static-root boot using generated material;
- MainNet/TestNet generation refusal;
- mismatched validator-id fail-closed;
- no default runtime behavior change;
- no P2P wire/admission/trust change;
- no C4/C5 closure.

Current readiness:
- M12 Green.
- M6 generation + verification half Green-for-scope.
- M6 overall Yellow/Partial because live registration/admission is absent.
- M4 Yellow/launch-blocking because no live seed/bootnode reachability exists.
- Public DevNet NOT launch-ready.
- C4 OPEN.
- C5 OPEN.
- MainNet authority rotation/revocation Red.

Primary objective:
Add a bounded, non-launching public DevNet live identity registration/admission boundary that takes a `qbind-node identity seed-candidate` / `public-identity.json` output and verifies whether it is admissible for a future live seed-list entry.

This run should close the remaining M6 registration half if possible, without moving M4 Green unless real live seed reachability evidence also lands.

Strict scope:
Registration/admission validation boundary only.

No public DevNet launch.
No real externally reachable seed publication unless explicitly proven and in scope.
No M4 Green unless real live seed/bootnode reachability evidence lands.
No faucet.
No RPC gateway.
No explorer.
No status page.
No TestNet readiness claim.
No MainNet readiness claim.
No C4 closure.
No C5 closure.
No authority-lifecycle runtime wiring.
No validator-set mutation.
No epoch transition.
No sequence/marker write.
No LivePqcTrustState mutation.
No trust-bundle live apply.
No peer-driven apply.
No P2P wire-format change.
No peer-admission weakening.
No committed private keys, generated roots, generated leaf keys, data dirs, logs, or metrics dumps.

Required investigation:
Inspect:
- `crates/qbind-node/src/identity_cli.rs`;
- Run 375 tests and harness;
- `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_375.md`;
- `docs/release/public-devnet/identity/*`;
- `docs/release/public-devnet/network/devnet-seed-list.schema.json`;
- `docs/release/public-devnet/network/README.md`;
- `docs/release/public-devnet/operator/IDENTITY.md`;
- `docs/release/public-devnet/operator/QUICKSTART.md`;
- `docs/release/public-devnet/p2p/PEER_ADMISSION_POLICY.md`;
- `docs/release/QBIND_PUBLIC_DEVNET_READINESS_CRITERIA.md`;
- `p2p_node_builder.rs`;
- `p2p_tcp.rs`;
- `pqc_root_config.rs`;
- any existing admission/static-peer verification surfaces.

Decision gate:
Record one of:

Route A — Existing `qbind-node identity` and seed-list validation surfaces are enough.
Add docs/harness/evidence only for registration/admission verification.

Route B — A small non-mutating `qbind-node identity register-check` or `seed-admission-check` subcommand is needed.
Add the minimal first-class command and tests.

Route C — Live registration requires real M4 seed infrastructure.
Keep M6 Yellow/Partial and document the exact blocker.

Preferred implementation:
Add a non-mutating first-class command if needed:

`qbind-node identity register-check <public-identity.json> --seed-list <path> [--role seed|full-node|validator-candidate]`

or:

`qbind-node identity seed-admission-check <identity_dir> --seed-list <path>`

The command must:
1. read public identity material only;
2. validate `public-identity.json` against `OPERATOR_IDENTITY_SCHEMA.json`;
3. validate the candidate against `devnet-seed-list.schema.json`;
4. require `status=planned` unless real reachability evidence is supplied;
5. reject `status=live` without `last_reachability_evidence`;
6. reject private material embedded in public JSON;
7. reject TestNet/MainNet identity material;
8. reject mismatched genesis hash or missing expected genesis placeholder, depending on schema rules;
9. reject malformed NodeId/peer_id/fingerprint fields;
10. verify NodeId deterministically from `leaf.cert.bin` if a cert path is supplied;
11. never open a socket unless an explicit loopback-only verification mode is added;
12. never mutate runtime state.

Optional bounded verification:
If feasible, add loopback-only admission verification:
- use generated identity material;
- boot a local `qbind-node` under strict-auth static-root;
- confirm the generated identity is admitted under the expected NodeId;
- confirm mismatched material fails closed.
This must remain loopback-only and must not claim M4 reachability.

Required tests:
Add:

`crates/qbind-node/tests/run_376_public_devnet_identity_registration_tests.rs`

Cover at minimum:
1. Run 375 generated full-node identity passes register-check as planned.
2. seed identity passes register-check as planned.
3. validator-candidate identity passes register-check as planned.
4. schema-valid public identity maps into seed-list candidate.
5. `status=live` without reachability evidence is rejected.
6. placeholder/planned with reachability evidence is rejected if schema forbids it.
7. private key contents embedded in public JSON are rejected.
8. malformed NodeId rejected.
9. malformed peer_id rejected.
10. malformed trusted_root_spec rejected.
11. wrong environment rejected.
12. MainNet/TestNet refused.
13. mismatched leaf cert vs public identity rejected.
14. mismatched validator index rejected.
15. unknown role rejected.
16. no socket opened by default.
17. no sequence/marker write.
18. no LivePqcTrustState mutation.
19. no validator-set mutation.
20. no C4/C5 closure claim.
21. no M4 Green claim.
22. M12 unchanged.

Required release evidence:
Add harness:

`scripts/devnet/run_376_public_devnet_identity_registration.sh`

Add canonical evidence:

`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_376.md`

If useful, add archive:

`docs/devnet/run_376_public_devnet_identity_registration/{README.md,summary.txt,.gitignore}`

Harness must prove:
1. `cargo build -p qbind-node --release --bin qbind-node`.
2. `qbind-node identity generate devnet seed ...`.
3. `qbind-node identity seed-candidate ...`.
4. `qbind-node identity register-check` or equivalent accepts planned candidate.
5. seed-list schema validation succeeds.
6. live-without-reachability fails closed.
7. private-material-in-public-json fails closed.
8. mismatched cert/identity fails closed.
9. MainNet/TestNet refused.
10. no private material committed.
11. release binary SHA-256 captured.
12. BuildID captured.
13. toolchain captured.

Docs to update:
- `docs/release/public-devnet/identity/README.md`
- `docs/release/public-devnet/identity/IDENTITY_GENERATION.md`
- `docs/release/public-devnet/identity/IDENTITY_VERIFY.md`
- `docs/release/public-devnet/identity/SAFETY.md`
- `docs/release/public-devnet/identity/VERIFY.md`
- `docs/release/public-devnet/operator/IDENTITY.md`
- `docs/release/public-devnet/operator/QUICKSTART.md`
- `docs/release/public-devnet/network/README.md`
- `docs/release/public-devnet/p2p/PEER_ADMISSION_POLICY.md`
- `docs/release/public-devnet/p2p/VERIFY.md`
- `docs/release/QBIND_PUBLIC_DEVNET_READINESS_CRITERIA.md`
- `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
- `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
- `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
- `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
- `docs/whitepaper/contradiction.md`

Readiness matrix rules:
- M6 may move Yellow/Partial → Green only if the command provides a stable first-class generation, verification, and registration/admission-check workflow sufficient for external DevNet operators, even before M4 live reachability.
- If live network registration is inseparable from M4 live seeds, keep M6 Yellow/Partial and record exactly why.
- M4 must remain Yellow unless live seed/bootnode reachability evidence lands.
- M12 remains Green unless contradicted.
- Public DevNet remains NOT launch-ready unless all must-haves are Green.
- C4 and C5 remain OPEN.

Security:
Run secret scanning over all changed files.
No generated private keys, root signing keys, KEM secret keys, data dirs, logs, metrics dumps, credentials, tokens, private infrastructure, or live endpoints may be committed.

CodeQL:
If Rust source changes, invoke CodeQL if available.
If skipped, database-too-large, timed out, unavailable, or not meaningful, record the exact result and do not call it clean.

Expected verdict:
PASS / public-DevNet identity registration/admission-check positive if the non-mutating registration/admission command, tests, release harness, docs, schema checks, security scan, and provenance all land.

Partial-positive if only docs/harness improve but M6 cannot move Green.

Final response format:
1. Exact verdict.
2. Files changed.
3. Decision gate route.
4. Registration/admission-check surface.
5. Identity package updates.
6. Identity types covered.
7. Register-check input/output.
8. Public vs private material enforcement.
9. Operator commands.
10. Schema / seed-list mapping.
11. KEMTLS / static-root verification evidence, if any.
12. Rejection / fail-closed evidence.
13. Default compatibility / no runtime behavior change.
14. Readiness matrix delta for M6.
15. Current public DevNet readiness status.
16. Remaining public DevNet blockers.
17. Public TestNet blockers.
18. MainNet blockers.
19. C4/C5 status.
20. Tests run.
21. Security scans.
22. CodeQL.
23. Provenance.
24. Honest limitations.
25. Suggested Run 377 next step.