Continue QBIND from the current state.

Project:
- QBIND post-quantum L1
- security-first, production-grade, execution-first
- C4 is the main live operability tracker
- C5 tracks timeout-verification activation, forged-traffic rejection, and transport-root dependency
- no broad redesign
- no silent regressions
- no hype / no overclaiming

Current state:
- Runs 037–045 hardened the PQC transport path:
  - ML-DSA-44 transport delegation cert verification
  - ML-KEM-768 transport KEM
  - ChaCha20-Poly1305 transport AEAD
  - PQC cert metrics
  - cert validity-window enforcement
- Runs 046–049 completed live timeout-backoff evidence through the 800-tick cap.
- Run 050 landed structured PQC trust bundles.
- Run 051 landed signed ML-DSA-44 trust-bundle verification.
- Run 052 landed leaf-level certificate revocation enforcement.
- Run 053 landed trust-bundle chain_id crosscheck.
- Run 054 produced release-binary revoked-peer leaf evidence.
- Run 055 landed sequence-number monotonicity persistence.
- Run 056 produced release-binary anti-rollback evidence.
- Run 057 implemented activation-height gating.
- Run 058 produced release-binary activation-height evidence.
- Run 059 produced MainNet signed-bundle release-binary startup/load evidence.
- Run 060 landed the production PQC trust lifecycle operator playbook.
- Run 061 implemented local revoked-leaf startup self-check.
- Run 062 implemented per-entry revocation activation_height active/pending behavior.
- Run 063 implemented local issuer-root revocation startup self-check.
- Run 064 updated the operator runbook for Runs 061–063.
- Run 065 implemented deterministic per-environment minimum activation-height policy:
  - DevNet = 0 blocks
  - TestNet = 8 blocks
  - MainNet = 32 blocks
  - too-soon TestNet/MainNet activation fails closed before sequence update, root merge, or P2P start
  - emergency immediate revocation remains available when revocation activation_height is None
- Run 066 updated the operator runbook for Run 065.
- Current remaining C4 trust/lifecycle items include:
  - multi-validator MainNet peer-connection evidence
  - activation_epoch runtime source
  - minimum-margin enforcement on future gossiped / peer-supplied trust-bundle path
  - on-the-fly trust-bundle hot reload
  - in-binary / on-chain bundle-signing-key ratification
  - KMS/HSM custody integration
  - production fast-sync / consensus-storage restore
  - per-environment production trust-anchor operation
- C4 remains OPEN.
- C5 remains OPEN / narrowed.

Task:
Run 067. Produce multi-validator MainNet signed-bundle peer-connection evidence on the release binary.

Primary objective:
Prove that multiple qbind-node validators running in MainNet mode can establish real PQC mutual-auth peer connections using the signed trust-bundle path, not static-root fallback or test-grade crypto.

Important framing:
This is an evidence-first run.
Do not implement hot reload.
Do not implement activation_epoch.
Do not implement gossiped-bundle minimum-margin enforcement.
Do not implement KMS/HSM.
Do not implement bundle-signing-key ratification.
Do not implement fast-sync restore.
Do not redesign trust bundles.
Do not redesign KEMTLS.
Do not modify consensus or timeout logic.
Do not modify source unless a real bug is found.
Do not weaken signed-bundle verification.
Do not weaken chain_id enforcement.
Do not weaken sequence anti-rollback persistence.
Do not weaken activation-height or minimum-margin policy.
Do not silently fall back to --p2p-trusted-root.
Do not silently fall back to DummySig, DummyKem, or DummyAead.
Do not claim full C4 or C5 closure.

Required active stack:
- release qbind-node binary
- --env mainnet
- --p2p-mutual-auth required
- --p2p-pqc-root-mode pqc-static-root
- --p2p-trust-bundle <signed-mainnet-bundle>
- --p2p-trust-bundle-signing-key KEYID:100:PK
- explicit --data-dir for each validator
- real ML-DSA-44 transport delegation certs
- real ML-KEM-768 leaf KEM material
- real ChaCha20-Poly1305 AEAD
- signed MainNet trust bundle with:
  - environment = MainNet
  - chain_id matching runtime MainNet NodeConfig.chain_id()
  - sequence valid for each node data dir
  - activation_height satisfying MainNet minimum-margin policy or absent if allowed
  - valid active root(s)
  - no local/peer leaf revocation that would block the intended positive connection
- metrics enabled
- no --p2p-trusted-root
- no DummySig
- no DummyKem
- no DummyAead

Strict scope:
- Prefer N=2 release-binary MainNet peer-connection smoke as the smallest evidence shape.
- If N=2 is insufficient due current node topology requirements, use N=4 but keep the run short.
- Do not require a full long B14 recovery run unless the binary naturally reaches it and it is cheap.
- Existing Run 050–066 behavior must be preserved.
- Crosscheck all evidence against existing design/spec. If contradictions or inconsistencies are found, update docs/whitepaper/contradiction.md.

Required evidence cases:

1. Build and identify binaries
   - Build release qbind-node.
   - Build release devnet_pqc_trust_bundle_helper.
   - Build release devnet_pqc_root_helper if needed.
   - Record:
     - branch
     - commit
     - dirty/clean status
     - qbind-node sha256
     - qbind-node ELF BuildID
     - helper sha256
     - helper ELF BuildID
   - Confirm --devnet-forged-inject remains hidden from normal --help.
   - Confirm relevant flags:
     - --env
     - --p2p-trust-bundle
     - --p2p-trust-bundle-signing-key
     - --p2p-pqc-root-mode
     - --p2p-leaf-cert
     - --p2p-leaf-cert-key
     - --p2p-peer-leaf-cert
     - --p2p-mutual-auth
     - --data-dir

2. Generate MainNet trust material
   Create material for at least two validators:
   - one signed MainNet trust bundle
   - one ML-DSA-44 transport root
   - one trust-bundle signing key
   - validator leaf delegation certs
   - ML-KEM-768 leaf secret files
   - all peer leaf cert mappings required by qbind-node
   - no private key material in logs

   Required:
   - bundle chain_id must match runtime MainNet chain_id.
   - bundle signature must verify.
   - sequence persistence must be valid for each fresh data dir.
   - activation policy must not block the positive smoke.
   - if activation_height is included, it must respect MainNet minimum margin and Run 057 current-height behavior.
   - if activation_height is omitted, document why that is valid under current policy.

3. Positive N=2 MainNet peer-connection smoke
   Run V0 and V1 with:
   - --env mainnet
   - signed MainNet trust bundle
   - signing key flag
   - required pqc-static-root mutual auth
   - real PQC leaf cert/KEM material
   - metrics endpoints

   Prove:
   - both nodes start.
   - signed bundle verifies on both nodes.
   - MainNet environment is accepted only because bundle environment is MainNet.
   - chain_id check passes.
   - sequence persistence record is written for each node.
   - activation/minimum-margin policy does not reject the bundle.
   - P2P mutual-auth connection succeeds.
   - newly_connected_peers or equivalent peer-connected signal appears.
   - cert verify accepted counter moves.
   - cert rejected counters remain zero.
   - qbind_p2p_pqc_root_mode = pqc-static-root / expected numeric value.
   - qbind_p2p_pqc_roots_configured > 0.
   - qbind_p2p_pqc_trust_bundle_loaded = 1.
   - qbind_p2p_pqc_trust_bundle_signature_verified_total = 1 per node, if metrics expose it.
   - no --p2p-trusted-root was supplied.
   - no DummySig / DummyKem / DummyAead fallback.

4. Negative MainNet unsigned bundle peer-connection smoke
   If low-cost:
   - attempt the same N=2 setup with unsigned MainNet bundle.
   - startup must fail closed before peer connection.
   - no sequence persistence file created/updated.
   - no fallback.
   - no Dummy crypto.

   If this was already strongly covered by Run 059 and not rerun, state boundary honestly.

5. Negative MainNet revoked-peer smoke
   If low-cost:
   - signed MainNet bundle revokes V1 leaf fingerprint or issuer root.
   - V0/V1 connection must fail closed.
   - rejected_revoked counter moves where observable.
   - no fallback.
   - no Dummy crypto.

   Do not block the primary positive peer-connection proof on this if it becomes expensive; Run 054/061/063 already cover DevNet revocation paths.

6. Optional N=4 MainNet smoke
   If N=2 passes and N=4 is cheap:
   - run a short N=4 MainNet signed-bundle cluster.
   - prove all expected peers connect.
   - prove cert accepted counters move.
   - prove no rejection counters under honest traffic.
   - no need for long B14 unless naturally available.

Required tests to rerun:
- cargo test -p qbind-node --lib pqc_trust_bundle
- cargo test -p qbind-node --lib pqc_trust_sequence
- cargo test -p qbind-node --lib pqc_trust_activation
- cargo test -p qbind-node --test run_050_pqc_trust_bundle_tests
- cargo test -p qbind-node --test run_051_pqc_trust_bundle_signing_tests
- cargo test -p qbind-node --test run_052_pqc_leaf_revocation_tests
- cargo test -p qbind-node --test run_055_pqc_trust_bundle_sequence_tests
- cargo test -p qbind-node --test run_057_pqc_trust_bundle_activation_tests
- cargo test -p qbind-node --test run_061_pqc_local_leaf_self_check_tests
- cargo test -p qbind-node --test run_062_pqc_revocation_activation_tests
- cargo test -p qbind-node --test run_063_pqc_local_issuer_root_self_check_tests
- cargo test -p qbind-net --test run_052_leaf_revocation_handshake_tests
- cargo test -p qbind-node --lib metrics
- cargo test -p qbind-node --lib p2p
- cargo test -p qbind-node --test run_037_pqc_static_root_mutual_auth_tests
- cargo test -p qbind-node --test run_040_pqc_static_root_real_aead_tests
- cargo test -p qbind-node --test run_044_pqc_cert_verify_metrics_adapter_tests
- cargo test -p qbind-node --lib
- cargo test -p qbind-net --lib
- cargo test -p qbind-crypto --lib
- cargo build --release -p qbind-node --bin qbind-node
- cargo build --release -p qbind-node --example devnet_pqc_trust_bundle_helper
- cargo build --release -p qbind-node --example devnet_pqc_root_helper

If source touches timeout verification or binary consensus loop, also run:
- cargo test -p qbind-node --lib binary_consensus
- cargo test -p qbind-node --lib forged_injection
- cargo test -p qbind-node --lib run030
- cargo test -p qbind-consensus --lib timeout

Evidence document:
Create:
- docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_067.md

Evidence document must include:
- exact objective
- exact verdict
- exact files changed
- exact commands run
- tests/evidence run and pass/fail status
- binary identity
- helper identities
- MainNet trust-bundle material procedure
- MainNet chain_id expected/used
- signed-bundle verification proof
- sequence persistence record contents for each node
- activation/minimum-margin status for bundle used
- positive N=2 MainNet peer-connection smoke
- optional negative unsigned/revoked MainNet smokes if run
- optional N=4 smoke if run
- /metrics excerpts from both nodes
- proof no --p2p-trusted-root supplied
- proof no DummySig/DummyKem/DummyAead fallback
- exact boundary if full MainNet cluster or N=4 is not run
- remaining open items
- exact immediate next action

contradiction.md update rules:
- If MainNet N=2 signed-bundle peer connection succeeds, update C4 to record multi-validator MainNet peer-connection evidence narrowed/closed for scoped N=2.
- If N=4 is also run successfully, record that separately.
- If only startup/load passes but peer connection fails for unrelated config, state the exact boundary.
- Keep C4 open for:
  - activation_epoch runtime source
  - minimum-margin enforcement on future gossiped / peer-supplied trust-bundle path
  - hot reload if still absent
  - in-binary/on-chain bundle-signing-key ratification if still absent
  - external KMS/HSM custody integration if still absent
  - production fast-sync / consensus-storage restore
  - per-environment production trust-anchor operation if not fully solved
- Do not close full C4.
- Do not claim C5 closure unless explicitly justified.
- If unsigned MainNet bundle loads, record as contradiction/regression.
- If peer connection uses static-root fallback or Dummy crypto, record as contradiction/regression.
- If MainNet behaves like DevNet unsigned mode, record as contradiction/regression.

Expected verdict options:
- Strongest positive:
  N=2 MainNet release-binary signed-bundle peer connection succeeds with real PQC transport, signed bundle verification, chain_id enforcement, sequence persistence, no static-root fallback, no Dummy crypto, and required regressions pass.
- Partial positive:
  MainNet signed-bundle startup/load succeeds but peer connection is blocked by unrelated config. State boundary.
- Negative:
  unsigned/tampered/wrong-chain MainNet bundle is accepted, peer connection falls back to static roots/Dummy crypto, or regressions fail.

Required final response:
1. Exact verdict
2. Exact files changed
3. Exact commands run
4. Tests/evidence run and pass/fail status
5. What was proven
6. What remains not solved
7. Whether contradiction.md was updated and why
8. Exact immediate next action recommended