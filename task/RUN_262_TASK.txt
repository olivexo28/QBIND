Run 262 continuation — Finish regression, evidence docs, contradiction entry, and scans

You are continuing QBIND Run 262 after the core implementation landed.

Current status:

Run 262 is partially complete.

Already committed and pushed on `copilot/run-262-task`:

* `crates/qbind-node/src/pqc_governance_durable_completion_acknowledgement_consumer.rs`
* `crates/qbind-node/tests/run_262_durable_completion_acknowledgement_consumer_tests.rs`
* `crates/qbind-node/src/lib.rs`

Current verified results:

* `cargo build -p qbind-node --lib` — PASS.
* `cargo test -p qbind-node --test run_262_durable_completion_acknowledgement_consumer_tests` — 57 passed, 0 failed.

Important correction:

Run 262 is not closed yet.

Do not move to Run 263.
Do not create release-binary evidence.
Do not write a Run 263 helper or harness.
Do not add a new feature boundary.
Do not change production behavior.

Remaining Run 262 work only:

1. Run the full regression corpus.
2. Create the canonical Run 262 evidence doc.
3. Update the narrow protocol/ops docs.
4. Add the `contradiction.md` no-contradiction entry or record any contradiction found.
5. Run secret scanning.
6. Run CodeQL or report honestly if skipped.
7. Produce the final 7-part Run 262 verdict.

Strict scope:

* Source/test only.
* No release-binary evidence.
* No production runtime enablement.
* No real settlement backend.
* No real audit-ledger acknowledgement backend.
* No real external-publication confirmation backend.
* No real external publication.
* No real network I/O.
* No real persistent storage.
* No RocksDB/file/schema/migration/storage-format change.
* No wire/schema/marker/sequence/trust-bundle change.
* No Run 070 call.
* No `LivePqcTrustState` mutation.
* No trust swap.
* No session eviction.
* No sequence write.
* No marker write.
* No MainNet governance.
* No MainNet peer-driven apply.
* No validator-set rotation.
* Keep Full C4 OPEN.
* Keep C5 OPEN.

Do not rewrite the Run 262 module unless regressions expose a real issue.

Do not rewrite the Run 262 tests unless regressions expose a real issue.

Required regression commands:

Run at minimum:

* `cargo build -p qbind-node --lib`
* `cargo test -p qbind-node --test run_262_durable_completion_acknowledgement_consumer_tests`
* `cargo test -p qbind-node --test run_260_durable_completion_audit_receipt_acknowledgement_tests`
* `cargo test -p qbind-node --test run_258_durable_completion_audit_publication_receipt_tests`
* `cargo test -p qbind-node --test run_256_durable_completion_attestation_backend_tests`
* `cargo test -p qbind-node --test run_254_modeled_durable_completion_attestation_projection_tests`
* `cargo test -p qbind-node --test run_252_modeled_durable_completion_finalization_projection_tests`
* `cargo test -p qbind-node --test run_250_modeled_durable_consume_completion_reporter_tests`
* `cargo test -p qbind-node --test run_248_modeled_durable_consume_projection_sink_tests`
* `cargo test -p qbind-node --test run_246_governance_modeled_end_to_end_pipeline_tests`
* `cargo test -p qbind-node --test run_244_modeled_governance_trust_mutation_applier_tests`
* `cargo test -p qbind-node --test run_242_governance_execution_mutation_engine_tests`
* `cargo test -p qbind-node --test run_240_governance_evaluator_replay_durable_runtime_integration_tests`
* `cargo test -p qbind-node --test run_238_governance_evaluator_replay_durable_backend_tests`
* `cargo test -p qbind-node --test run_236_governance_evaluator_replay_consume_runtime_integration_tests`
* `cargo test -p qbind-node --test run_234_governance_evaluator_replay_consume_boundary_tests`
* `cargo test -p qbind-node --test run_232_governance_evaluator_replay_runtime_integration_tests`
* `cargo test -p qbind-node --test run_230_governance_evaluator_replay_state_tests`
* `cargo test -p qbind-node --test run_228_peer_evaluator_context_representation_tests`
* `cargo test -p qbind-node --test run_226_governance_evaluator_runtime_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_224_governance_evaluator_runtime_integration_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

Important target correction:

Use:

`run_232_governance_evaluator_replay_runtime_integration_tests`

Do not use the nonexistent `run_232_governance_evaluator_replay_state_tests`.

Run 230 is the replay-state target.

Required documentation:

Create:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_262.md`

Update narrowly:

* `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
* `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
* `docs/whitepaper/contradiction.md`

`QBIND_DEVNET_EVIDENCE_RUN_262.md` must record:

* exact verdict;
* files changed;
* source/test scope only;
* Run 262 module path;
* Run 262 test path;
* test counts;
* regression corpus results;
* secret scan result;
* CodeQL result or honest skip reason;
* security invariants preserved;
* honest limitations;
* C4/C5 status;
* suggested Run 263 next step.

The Run 262 evidence doc must state:

* Run 262 is source/test only.
* Run 262 adds a modeled durable-completion acknowledgement consumer / post-acknowledgement settlement interface boundary.
* The boundary consumes only Run 260 `AcknowledgementRecorded`.
* Run 260 `AcknowledgementRecorded` is required before any consumer / settlement request can exist.
* Only `AcknowledgementConsumed` authorizes modeled consumer state.
* Every non-recorded acknowledgement outcome produces no consumer request and no consumer record.
* The fixture consumer is DevNet/TestNet evidence-only and in-memory only.
* The fixture consumer mutates only `DurableCompletionAcknowledgementConsumerLedger`.
* Production settlement, MainNet settlement, and external settlement consumers are reachable but unavailable/fail-closed.
* No real settlement is implemented.
* No real audit-ledger acknowledgement is implemented.
* No real external-publication confirmation is implemented.
* No real external publication is implemented.
* No real production attestation backend is implemented.
* No real finalization backend is implemented.
* No real completion-report backend is implemented.
* No real durable consume backend is implemented.
* No real persistent replay backend is implemented.
* No real governance execution engine is implemented.
* No real production mutation engine is implemented.
* No real on-chain governance proof verifier is implemented.
* No KMS/HSM/RemoteSigner backend is implemented.
* No RocksDB/file/schema/migration/storage-format change is implemented.
* No wire/schema/marker/sequence/trust-bundle change is implemented.
* No MainNet governance is enabled.
* No MainNet peer-driven apply is enabled.
* No validator-set rotation is implemented.
* No Run 070 call.
* No `LivePqcTrustState` mutation.
* No real trust swap.
* No session eviction.
* No sequence write.
* No marker write.
* Rejected consumer paths are non-mutating.
* Full C4 remains OPEN.
* C5 remains OPEN.

Protocol/ops docs:

Append a narrow Run 262 entry after the Run 261 entry in each updated document.

Do not rewrite old run history.

Do not change prior verdicts.

Contradiction handling:

Crosscheck Run 262 against Runs 050–261.

If no contradiction is found, add a “Run 262 — no contradiction found” entry to `docs/whitepaper/contradiction.md`.

If a contradiction is found, record it explicitly with exact file/function/doc reference and recommend the smallest safe fix.

Security scanning:

Run secret scanning and report the result.

Run CodeQL if available.

Because Run 262 changes source and tests, do not call this trivial unless the checker itself classifies it that way.

If CodeQL is skipped because the Rust database is too large or tooling is unavailable, state that honestly and do not claim CodeQL coverage.

Expected final verdict:

Source/test positive only if:

* Run 262 tests pass.
* Full regression corpus passes.
* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_262.md` exists.
* The five protocol/ops docs are updated.
* `contradiction.md` has a Run 262 entry.
* Secret scan result is reported.
* CodeQL result or honest skip reason is reported.
* C4/C5 remain OPEN.

Suggested next step only after Run 262 closes:

Run 263 — release-binary evidence for the Run 262 durable-completion acknowledgement consumer / post-acknowledgement settlement interface boundary.

Final response format:

1. Exact verdict.
2. Files changed.
3. Tests run and results.
4. Security invariants preserved.
5. Honest limitations.
6. C4/C5 status.
7. Suggested Run 263 next step.
