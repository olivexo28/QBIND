Continue QBIND from the current state.

Project:
- QBIND post-quantum L1
- security-first, production-grade, execution-first
- C4 is the main live operability tracker
- C5 tracks timeout-verification activation, forged-traffic rejection, and transport-root dependency
- no broad redesign
- no silent regressions
- no hype / no overclaiming

Current state:
- Runs 037–045 hardened PQC transport:
  - ML-DSA-44 delegation cert verification
  - ML-KEM-768 transport KEM
  - ChaCha20-Poly1305 transport AEAD
  - PQC cert metrics
  - cert validity-window enforcement
- Runs 046–049 completed live timeout-backoff evidence through the 800-tick cap.
- Runs 050–068 completed the signed trust-bundle lifecycle path:
  - structured PQC trust bundles
  - signed ML-DSA-44 bundle verification
  - chain_id crosscheck
  - sequence anti-rollback persistence
  - activation-height gating
  - minimum activation-height policy
  - root/leaf revocation
  - per-entry revocation activation_height
  - local revoked-leaf startup self-check
  - local revoked issuer-root startup self-check
  - DevNet/TestNet/MainNet evidence
  - N=4 MainNet signed-bundle peer-connection evidence
- Run 069 landed validation-only trust-bundle reload-check:
  - hidden --p2p-trust-bundle-reload-check <PATH>
  - validates candidate bundles using startup security pipeline
  - does not apply live trust changes
  - does not mutate active roots/revocations/sessions
  - does not persist sequence or burn sequence numbers
- Run 070 partial-positive:
  - landed live-apply contract and rollback semantics
  - proved callback ordering:
    - snapshot
    - swap
    - evict sessions
    - commit sequence
    - rollback on post-swap failure
  - production binary still returned UnsupportedRuntimeContext because runtime adapter and session eviction were missing
- Run 071 positive-narrow:
  - introduced LivePqcTrustState = Arc<RwLock<Arc<LivePqcTrustSnapshot>>>
  - production-honest PQC handshake root resolver and leaf-revocation closure now read through live snapshots
  - startup behavior preserved
  - active/pending revocation semantics preserved
- Run 072 strongest-positive:
  - added production-honest P2P session eviction hook
  - TcpKemTlsP2pService implements P2pSessionEvictor
  - evict_all_sessions drains peer registry, drops outbound mpsc senders, aborts read/write JoinHandles
  - truthful EvictionReport invariant: attempted == evicted + failed
  - eviction metrics added
  - trust state and sequence state remain unchanged by eviction
  - Run 070 evict_sessions callback can now be satisfied
- C4 remains OPEN.
- C5 remains OPEN / narrowed.

Current remaining C4 trust/lifecycle blockers:
- actual live trust-bundle apply
- peer/gossiped bundle acceptance
- activation_epoch runtime source
- in-binary / on-chain bundle-signing-key ratification
- external KMS/HSM custody integration
- production fast-sync / consensus-storage restore
- per-environment production trust-anchor operation

Task:
Run 073. Wire the LiveTrustApplyContext runtime adapter and prove full local operator-triggered live trust-bundle apply.

Primary objective:
Compose Run 069 validation, Run 070 apply contract, Run 071 LivePqcTrustState, Run 072 P2pSessionEvictor, and pqc_trust_sequence::commit_sequence into a real production binary local-operator live-apply path.

The target pipeline is:

validate candidate
→ snapshot old live trust state
→ swap LivePqcTrustState to candidate trust state
→ evict existing P2P sessions
→ commit sequence persistence
→ report success

Important framing:
This is local operator-triggered live apply only.
This is NOT peer-supplied/gossiped bundle acceptance.
This is NOT automatic filesystem watcher hot reload.
This is NOT activation_epoch implementation.
This is NOT KMS/HSM.
This is NOT signing-key ratification.
This is NOT fast-sync restore.
Do not redesign KEMTLS.
Do not redesign consensus.
Do not weaken startup trust-bundle validation.
Do not weaken Run 069 reload-check.
Do not weaken Run 070 rollback contract.
Do not weaken Run 071 live trust snapshot semantics.
Do not weaken Run 072 session eviction semantics.
Do not silently fall back to --p2p-trusted-root.
Do not silently fall back to DummySig, DummyKem, or DummyAead.
Do not claim full C4 or C5 closure.

Strict scope:
- Enable local operator-triggered live apply from a local file only.
- Candidate must pass the same checks as Run 069:
  - parse trust bundle
  - verify ML-DSA-44 bundle signature
  - validate environment
  - validate chain_id
  - validate root status/windows
  - validate bundle activation_height
  - validate Run 065 minimum activation-height policy
  - validate per-entry revocation activation_height
  - validate sequence monotonicity against persisted accepted sequence
  - check local leaf fingerprint revocation
  - check local issuer-root revocation
- Only after successful validation:
  - atomically swap LivePqcTrustState
  - evict P2P sessions using Run 072 hook
  - commit sequence persistence
- If validation fails:
  - do not swap
  - do not evict
  - do not commit sequence
- If swap fails:
  - old live trust state remains active
  - do not evict
  - do not commit sequence
- If eviction fails after swap:
  - roll back LivePqcTrustState to old snapshot
  - do not commit sequence
  - report reload failure
- If sequence commit fails after swap and eviction:
  - roll back LivePqcTrustState to old snapshot
  - report reload failure
  - if rollback fails, fail closed with a fatal reload error
- Existing startup behavior must remain unchanged.
- Run 069 reload-check must remain validation-only and non-mutating.
- No peer/gossip input.
- No unsigned TestNet/MainNet reload.
- No static-root fallback.

Required investigation:

1. Inspect existing pieces
   Inspect:
   - crates/qbind-node/src/pqc_trust_reload.rs
   - crates/qbind-node/src/pqc_live_trust.rs
   - crates/qbind-node/src/p2p_session_eviction.rs
   - crates/qbind-node/src/pqc_trust_sequence.rs
   - crates/qbind-node/src/pqc_trust_bundle.rs
   - crates/qbind-node/src/pqc_trust_activation.rs
   - crates/qbind-node/src/main.rs
   - crates/qbind-node/src/cli.rs
   - crates/qbind-node/src/p2p_tcp.rs
   - crates/qbind-node/src/p2p_node_builder.rs
   - crates/qbind-node/src/metrics.rs
   - Run 069 / Run 070 / Run 071 / Run 072 tests and evidence docs
   - docs/whitepaper/contradiction.md

   Document exact file/function references in the evidence document.

2. Define runtime adapter
   Implement a concrete LiveTrustApplyContext adapter that provides the callbacks required by the Run 070 apply contract:
   - snapshot live trust state
   - swap live trust state
   - rollback live trust state
   - evict sessions
   - commit sequence

   Required:
   - use LivePqcTrustState::snapshot and swap_snapshot
   - use P2pSessionEvictor::evict_all_sessions
   - use pqc_trust_sequence commit path
   - do not duplicate sequence logic
   - do not bypass validation
   - do not mutate state before validation succeeds

3. Define live apply trigger
   Use the existing hidden opt-in apply flag from Run 070 if available:
   - --p2p-trust-bundle-reload-apply-enabled
   - and a local candidate path flag if already present

   If the flag exists but currently returns UnsupportedRuntimeContext, wire it to the runtime adapter.

   Requirements:
   - disabled by default
   - local file only
   - no remote unauthenticated endpoint
   - no peer-supplied bundle
   - logs must clearly state operator-triggered local reload apply
   - validation-only --p2p-trust-bundle-reload-check remains unchanged

4. Define session behavior
   Required v0 policy:
   - evict all current P2P sessions on successful trust-state swap
   - allow reconnect under the new trust context if existing dial/listener loops do so
   - if reconnect is not automatic, document boundary
   - do not selectively retain sessions
   - do not keep sessions authenticated under old trust after successful reload

5. Define metrics/logs
   Add or wire metrics if not already complete:
   - reload apply attempt total
   - reload apply success total
   - reload apply failure total
   - reload apply rollback total
   - reload apply sequence commit failure total
   - reload apply active sequence
   - reload apply session evictions total
   - existing Run 072 session eviction counters must move truthfully

   Requirements:
   - no fabricated metrics
   - no duplicate families
   - validation-only reload-check must not increment apply-success metrics
   - logs must include:
     - old fingerprint prefix
     - new fingerprint prefix
     - old sequence
     - new sequence
     - eviction attempted/evicted/failed
     - sequence commit success/failure
     - rollback success/failure
   - no private material

Required implementation:

1. Wire LiveTrustApplyContext adapter
   Add narrow runtime adapter in qbind-node, likely:
   - pqc_trust_reload.rs
   - or a small new module if cleaner

   It should compose:
   - LivePqcTrustState
   - P2pSessionEvictor
   - sequence persistence path
   - metrics sink if available

2. Remove UnsupportedRuntimeContext from the live production path
   Only for the explicit local operator-triggered apply path.
   Keep UnsupportedRuntimeContext for unsupported modes if appropriate.

3. Preserve reload-check
   Existing:
   - --p2p-trust-bundle-reload-check <PATH>
   must remain:
   - non-mutating
   - no sequence persistence
   - no live trust update
   - no session eviction
   - exits 0/1 as before

4. Preserve startup path
   Normal startup with signed trust bundle must behave exactly as before.
   Startup must still persist sequence normally.
   Startup must not invoke reload-apply metrics or session eviction.

5. Tests
   Add tests proving:
   - valid candidate apply swaps LivePqcTrustState.
   - sequence commits only after successful swap and eviction.
   - validation failure does not swap/evict/commit.
   - swap failure does not evict/commit.
   - eviction failure rolls back and does not commit.
   - sequence commit failure rolls back.
   - rollback failure after commit failure reports fatal/critical variant.
   - reload-check remains non-mutating.
   - startup path unchanged.
   - Run 072 eviction report is consumed truthfully.
   - metrics count attempts/success/failure/rollback truthfully.
   - invalid rollback/tampered/wrong-chain/local-revoked candidates do not alter state.

Required regression tests:
- cargo test -p qbind-node --lib pqc_trust_reload
- cargo test -p qbind-node --lib pqc_live_trust
- cargo test -p qbind-node --lib p2p_session_eviction
- cargo test -p qbind-node --lib pqc_trust_bundle
- cargo test -p qbind-node --lib pqc_trust_sequence
- cargo test -p qbind-node --lib pqc_trust_activation
- new Run 073 reload-apply runtime tests
- cargo test -p qbind-node --test run_069_pqc_trust_bundle_reload_check_tests
- cargo test -p qbind-node --test run_070_pqc_trust_bundle_reload_apply_tests
- cargo test -p qbind-node --test run_071_pqc_live_trust_tests
- cargo test -p qbind-node --test run_072_p2p_session_eviction_tests
- cargo test -p qbind-node --test run_050_pqc_trust_bundle_tests
- cargo test -p qbind-node --test run_051_pqc_trust_bundle_signing_tests
- cargo test -p qbind-node --test run_052_pqc_leaf_revocation_tests
- cargo test -p qbind-node --test run_055_pqc_trust_bundle_sequence_tests
- cargo test -p qbind-node --test run_057_pqc_trust_bundle_activation_tests
- cargo test -p qbind-node --test run_061_pqc_local_leaf_self_check_tests
- cargo test -p qbind-node --test run_062_pqc_revocation_activation_tests
- cargo test -p qbind-node --test run_063_pqc_local_issuer_root_self_check_tests
- cargo test -p qbind-net --test run_052_leaf_revocation_handshake_tests
- cargo test -p qbind-node --lib metrics
- cargo test -p qbind-node --lib p2p
- cargo test -p qbind-node --test run_037_pqc_static_root_mutual_auth_tests
- cargo test -p qbind-node --test run_040_pqc_static_root_real_aead_tests
- cargo test -p qbind-node --test run_044_pqc_cert_verify_metrics_adapter_tests
- cargo test -p qbind-node --lib
- cargo test -p qbind-net --lib
- cargo test -p qbind-crypto --lib
- cargo build --release -p qbind-node --bin qbind-node
- cargo build --release -p qbind-node --example devnet_pqc_trust_bundle_helper
- cargo build --release -p qbind-node --example devnet_pqc_root_helper

If source touches timeout verification or binary consensus loop, also run:
- cargo test -p qbind-node --lib binary_consensus
- cargo test -p qbind-node --lib forged_injection
- cargo test -p qbind-node --lib run030
- cargo test -p qbind-consensus --lib timeout

Release-binary evidence:

1. Positive baseline N=2 startup
   - start two nodes with signed trust bundle sequence 1
   - prove peer connection works
   - prove cert accepted counters move
   - prove sequence=1 persisted
   - no fallback
   - no Dummy crypto

2. Positive live apply sequence 2
   - operator-trigger local reload apply using valid candidate sequence 2
   - prove candidate validates
   - prove LivePqcTrustState changes to sequence 2 / fingerprint 2
   - prove sessions are evicted
   - prove sequence persistence updates to 2 after apply
   - prove reload apply success metric increments
   - prove Run 072 eviction counters move
   - no fallback
   - no Dummy crypto

3. Reconnect behavior
   - if reconnect automatic:
     - prove peers reconnect under new trust context
     - cert accepted counters may move again; document exact behavior
   - if reconnect not automatic:
     - prove old sessions close and node remains healthy
     - document boundary honestly

4. Negative rollback apply candidate
   - attempt sequence 1 after sequence 2 accepted
   - prove rejected
   - prove live trust state remains sequence 2
   - prove sequence persistence remains 2
   - prove no session eviction
   - no fallback

5. Negative tampered/wrong-chain candidate
   - prove rejected
   - prove no live state mutation
   - prove sequence unchanged
   - prove no eviction

6. Negative local-revoked candidate
   - prove rejected
   - prove no live state mutation
   - prove sequence unchanged
   - prove no eviction

7. Proof reload-check remains validation-only
   - run --p2p-trust-bundle-reload-check valid seq 3 candidate
   - prove sequence file remains at 2
   - prove no live apply metrics move
   - prove no eviction

Evidence document:
Create:
- docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_073.md

Evidence document must include:
- exact objective
- exact verdict
- exact files changed
- exact commands run
- tests/evidence run and pass/fail status
- investigation findings with file/function references
- runtime adapter design
- apply trigger
- validation pipeline reuse
- atomic state swap semantics
- sequence commit ordering
- rollback behavior
- session eviction behavior
- reconnect behavior
- metrics/logging behavior
- proof reload-check remains validation-only
- proof startup behavior preserved
- release-binary evidence
- proof invalid candidates do not alter state
- proof no fallback to --p2p-trusted-root
- proof no DummySig/DummyKem/DummyAead fallback
- exact boundaries:
  - peer/gossiped bundles still absent
  - activation_epoch still absent
  - KMS/HSM still absent
  - signing-key ratification still absent
- exact immediate next action

contradiction.md update rules:
- If live apply lands and is proven, update C4 to record hot reload narrowed from validation-only/session-ready to local operator-triggered live apply.
- If reload apply works only in tests but not production binary, record partial positive and exact boundary.
- Keep C4 open for:
  - peer/gossiped bundle acceptance
  - activation_epoch runtime source
  - in-binary/on-chain bundle-signing-key ratification
  - KMS/HSM custody
  - production fast-sync / consensus-storage restore
  - per-environment production trust-anchor operation
- Do not close full C4.
- Do not claim C5 closure.
- If apply burns sequence before successful state swap + eviction, record contradiction/regression.
- If invalid candidate mutates live trust state, record contradiction/regression.
- If session eviction is skipped while live trust changes, record contradiction/regression.
- If failure falls back to static roots or Dummy crypto, record contradiction/regression.

Expected verdict options:
- Strongest positive:
  local operator-triggered live apply lands in production binary; valid candidate validates, swaps live trust state, evicts sessions, commits sequence after success; invalid candidates do not mutate state or sequence; reload-check remains non-mutating; no fallback; no Dummy crypto; tests and release evidence pass.
- Partial positive:
  adapter/tests land but production live evidence or reconnect behavior is incomplete. State exact boundary.
- Negative:
  apply burns sequence before swap/eviction, invalid candidate mutates state, session eviction is skipped, fallback/Dummy crypto activates, or regressions fail.

Required final response:
1. Exact verdict
2. Exact files changed
3. Exact commands run
4. Tests/evidence run and pass/fail status
5. What was fixed
6. What was proven
7. What remains not solved
8. Whether contradiction.md was updated and why
9. Exact immediate next action recommended