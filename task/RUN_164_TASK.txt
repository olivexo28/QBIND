Task: Run 164 — Release-Binary Governance Authority Verifier Evidence / Boundary

Current accepted state:

* Run 159: source/test v2 signing-key lifecycle validator landed.
* Run 161: lifecycle validation wired into shared v2 marker-decision helper.
* Run 162: release-binary lifecycle enforcement evidence landed through validation-only and mutating v2 marker-decision surfaces.
* Run 163: source/test governance ratification authority verifier landed:

  * `pqc_governance_authority.rs`;
  * `GovernanceAuthorityProof`;
  * `GovernanceAuthorityClass::{GenesisBound, EmergencyCouncil, OnChainGovernance}`;
  * OnChainGovernance deliberately fail-closed / unsupported;
  * typed `GovernanceAuthorityVerificationOutcome`;
  * `GovernanceIssuerSignatureVerifier` hook;
  * deterministic `FixtureIssuerSignatureVerifier`;
  * optional `GovernanceThreshold`;
  * pure `verify_governance_authority_proof`;
  * pure `validate_lifecycle_with_governance_authority`;
  * 32 tests covering A1–A5, R1–R16, no-I/O guarantee, combined-helper composition, and “acceptance does not enable MainNet apply.”
* Run 163 did not wire governance verification into mutating apply surfaces.
* No release-binary governance verifier evidence exists yet.
* MainNet peer-driven apply remains refused.
* Governance execution / on-chain proof remains unimplemented.
* KMS/HSM remains unimplemented.
* validator-set rotation remains open.
* full C4 remains open.
* C5 remains open.

Objective:
Produce the strongest honest release-binary evidence currently possible for the Run 163 governance authority verifier, and clearly determine whether the verifier is reachable from any production binary surface today.

This is a release-binary evidence / boundary run. It must not enable MainNet apply or mutate live trust based on governance proofs.

Strict scope:

* Release-binary evidence / boundary only.
* Use real `target/release/qbind-node` and release-built helper(s).
* No MainNet peer-driven apply enablement.
* No governance execution engine.
* No on-chain governance integration.
* No KMS/HSM implementation.
* No validator-set rotation.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* No wire-format change unless a hard reachability blocker is discovered; if so, stop and document the blocker.
* No marker schema change.
* No sequence-file schema change.
* No trust-bundle schema change.
* Do not weaken Runs 070, 130–163.
* Do not claim full C4 or C5 closure.

Investigation requirement:
First determine all possible release-binary surfaces that could honestly exercise Run 163 governance authority verification:

1. startup `--p2p-trust-bundle` v2 path;
2. reload-check validation-only path;
3. local peer-candidate-check validation-only path;
4. process-start reload-apply path;
5. SIGHUP live-reload path;
6. live inbound `0x05` validation-only path;
7. peer-driven staged queue / drain-once path;
8. lifecycle marker-decision path from Run 161/162;
9. fixture helper / example binary path.

For each surface, document:

* whether it currently calls `verify_governance_authority_proof`;
* whether it currently calls `validate_lifecycle_with_governance_authority`;
* whether it can observe `GovernanceAuthorityProof`;
* whether it can carry `GenesisBound`, `EmergencyCouncil`, or `OnChainGovernance` proof classes;
* whether evidence would require a schema/wire change;
* whether evidence can be captured without production behavior drift.

Required deliverables:

1. Add release-binary evidence harness:
   scripts/devnet/run_164_governance_authority_release_binary.sh

2. Add evidence archive:
   docs/devnet/run_164_governance_authority_release_binary/

3. Add canonical evidence report:
   docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_164.md

4. Update narrowly:

   * docs/whitepaper/contradiction.md
   * docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md
   * docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md
   * docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md

5. Crosscheck all new/modified code and docs against existing design/spec. If contradictions or inconsistencies are found, record them in contradiction.md before finalizing.

Preferred evidence path:
If an existing release-binary path can exercise the governance verifier without schema drift, use it.

Preferred order:
A. release-built helper emits governance authority proof fixtures;
B. real `target/release/qbind-node` validates them through an existing v2 validation or lifecycle surface;
C. logs prove governance accept/reject outcomes;
D. no mutation occurs unless the chosen surface is already a mutating surface and existing ordering rules are preserved.

Required governance evidence matrix if release-binary reachable:

Accepted cases:
A1. GenesisBound Rotate proof accepted.

* valid proof;
* correct environment;
* correct chain;
* correct genesis;
* correct authority root;
* correct lifecycle action;
* correct candidate digest;
* correct authority-domain sequence;
* PQC issuer suite;
* issuer signature verifies.

A2. GenesisBound Revoke proof accepted.

A3. GenesisBound EmergencyRevoke proof accepted if representable.

A4. EmergencyCouncil EmergencyRevoke proof accepted if representable.

A5. Idempotent same governance proof accepted / classified replay-safe if supported.

Rejected cases:
R1. wrong environment rejected.

R2. wrong chain rejected.

R3. wrong genesis rejected.

R4. wrong authority root rejected.

R5. wrong lifecycle action rejected.

R6. wrong candidate digest rejected.

R7. wrong authority-domain sequence rejected.

R8. invalid issuer signature rejected.

R9. unsupported issuer suite rejected.

R10. non-PQC suite rejected.

R11. threshold not met rejected, if threshold metadata is representable.

R12. malformed proof rejected.

R13. stale/replayed lower authority proof rejected.

R14. OnChainGovernance rejected as unsupported if no proof format exists.

R15. local operator config alone rejected as MainNet authority proof.

R16. peer majority / gossip count rejected as authority proof.

Required evidence if verifier is not release-binary reachable:
If no existing release-binary surface calls `verify_governance_authority_proof` or `validate_lifecycle_with_governance_authority`, the harness/report must still prove:

* release-built helper can mint the governance proof fixture corpus;
* real `target/release/qbind-node` surfaces remain unchanged and do not silently claim governance enforcement;
* existing DevNet/TestNet peer-driven apply evidence from Runs 153/158 remains valid;
* existing lifecycle release-binary enforcement from Run 162 remains valid;
* no MainNet enablement;
* no KMS/HSM;
* no validator-set rotation;
* exact source-level call graph showing Run 163 verifier is not yet composed into marker decision / lifecycle / apply helpers;
* exact proposed next integration run, likely Run 165.

In that case, verdict must be:

`partial-positive: release-binary fixture/evidence boundary captured; governance authority verifier not yet production-surface reachable`.

Do not call it strongest-positive.

Required negative invariants:
Across all scenarios:

* no MainNet apply;
* no autonomous apply;
* no apply on receipt;
* no peer-majority authority;
* no governance execution claim;
* no on-chain governance claim unless truly implemented;
* no KMS/HSM claim;
* no validator-set rotation claim;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead;
* no schema/wire/metric drift;
* no marker write before sequence commit on any mutating surface;
* no sequence write on validation-only surfaces;
* no marker write on validation-only surfaces.

Validation commands:
Run at minimum:

* cargo build --release -p qbind-node --bin qbind-node
* cargo build --release -p qbind-node --example <governance fixture helper if added>
* bash scripts/devnet/run_164_governance_authority_release_binary.sh
* cargo test -p qbind-node --test run_163_governance_authority_verifier_tests
* cargo test -p qbind-node --test run_161_lifecycle_marker_integration_tests
* cargo test -p qbind-node --test run_159_authority_signing_key_lifecycle_tests
* cargo test -p qbind-node --test run_157_unified_testnet_fixture_universe_tests
* cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests
* cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests
* cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests
* cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests
* cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests
* cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests
* cargo test -p qbind-node --lib pqc_authority
* cargo test -p qbind-node --lib

If exact test names differ, locate nearest existing targets and document exact commands/results.

Documentation requirements:
`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_164.md` must state:

* whether Run 163 governance authority verification is release-binary reachable today;
* which authority proof classes were actually evidenced on release binaries;
* which proof classes remain source/test-only and why;
* whether OnChainGovernance remains unsupported/fail-closed;
* no MainNet apply is enabled;
* governance execution/on-chain proof remains unimplemented unless actually added;
* KMS/HSM remains unimplemented;
* validator-set rotation remains open;
* full C4 remains open;
* C5 remains open.

Acceptance criteria:
Run 164 is acceptable only if:

1. It honestly determines release-binary reachability of Run 163 governance authority verification.
2. It captures release-binary governance verifier evidence for every feasible existing proof class.
3. It documents infeasible proof classes without fabricating evidence.
4. It preserves all validation-only and mutating-surface invariants.
5. It does not introduce schema/wire drift silently.
6. MainNet remains refused.
7. KMS-HSM/governance execution/validator-set rotation remain open.
8. The next required integration run is precisely identified if release-binary reachability is incomplete.
9. No full C4 or C5 closure is claimed.
