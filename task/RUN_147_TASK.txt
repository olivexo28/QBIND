Task: Run 147 — Release-Binary Evidence for Live 0x05 Peer-Candidate Staging Hook

Current accepted state:
- Run 142/143: live inbound 0x05 v2 validation-only path is source/test wired and release-binary evidenced.
- Run 144: peer-driven live apply safety specification landed.
- Run 145: non-applying PeerCandidateStagingQueue scaffold landed as library-level source/test.
- Run 146: PeerCandidateStagingQueue hook is wired into the live inbound 0x05 dispatcher behind an optional staging_queue handle. Staging is invoked only after Run 142 v2 and Run 123 v1 marker conflict checks, before propagation, and only for Validated(_) outcomes.
- No CLI flag was added in Run 146.
- Release-binary default behavior remains unchanged.
- Peer-driven live apply remains unimplemented.

Objective:
Produce release-binary evidence that the real target/release/qbind-node live inbound 0x05 path can stage already-validated peer candidates through the Run 146 staging hook while preserving the non-applying guarantees.

This run is evidence-only unless a minimal binary arming surface is genuinely required. If no existing release-binary path can arm the staging queue, stop and report a Run 147 blocker or return partial-positive. Do not silently add a broad implementation.

Strict scope:
- Evidence-only by default.
- Use real target/release/qbind-node.
- Use real live P2P 0x05 frame exchange.
- Use real DevNet fixture material.
- No peer-driven live apply.
- No Run 070 apply invocation.
- No LivePqcTrustState mutation.
- No sequence write.
- No authority-marker write.
- No session eviction.
- No SIGHUP/reload-apply invocation.
- No trust-bundle schema change.
- No ratification sidecar schema change.
- No marker schema change.
- No wire-format change.
- No KMS/HSM.
- No governance implementation.
- No signing-key rotation/revocation lifecycle.
- No MainNet staging enablement.
- Do not weaken validation-only or propagation-only behavior.
- Do not claim full C4 or C5 closure.

Important feasibility gate:
Before building the harness, inspect the Run 146 code and answer:

Can a real target/release/qbind-node binary arm LivePeerCandidateWireDispatcher::staging_queue through an existing runtime config path?

If yes:
- proceed with release-binary evidence.

If no:
- do not fabricate evidence;
- either:
  1. return partial-positive evidence documenting that Run 146 source/test hook exists but no release-binary arming path exists, or
  2. if explicitly allowed by maintainers, add the smallest hidden disabled-by-default DevNet/TestNet-only arming flag and treat that as a source/test scope change, not pure evidence.

Preferred path if a flag is necessary:
Add a hidden disabled-by-default flag:

--p2p-trust-bundle-peer-candidate-staging-enabled

Requirements for this flag:
- hidden from help if existing peer-candidate flags are hidden;
- default false;
- refused on MainNet unconditionally;
- requires existing live inbound 0x05 validation enablement;
- does not imply propagation;
- does not imply apply;
- constructs a bounded PeerCandidateStagingQueue using conservative defaults;
- exposes only enough log/metrics-free evidence to prove staging happened;
- no new metric family unless absolutely unavoidable;
- no peer-driven apply.

If this flag is added, update the verdict honestly:
- Run 147 becomes “source/test + release-binary evidence for hidden opt-in staging arming,” not evidence-only.

Required deliverables:
1. Add release-binary evidence harness:
   scripts/devnet/run_147_live_0x05_peer_candidate_staging_release_binary.sh

2. Add evidence archive:
   docs/devnet/run_147_live_0x05_peer_candidate_staging_release_binary/

3. Add canonical evidence report:
   docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_147.md

4. Update:
   - docs/whitepaper/contradiction.md
   - docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md
   - docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md
   - docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md

5. Crosscheck all new/modified docs against existing design/spec. If contradictions or inconsistencies are found, record them in contradiction.md before finalizing.

Harness topology:
Use an N=3 DevNet topology, matching Run 143 where possible:
- V0 publisher: sends live 0x05 candidates.
- V1 receiver: live inbound 0x05 validation enabled and staging armed if supported.
- V2 observer: optional propagation observer.

Required evidence capture:
- qbind-node SHA-256;
- qbind-node ELF Build ID if available;
- helper SHA-256 and Build ID if available;
- git commit hash;
- rustc/cargo versions;
- exact command lines;
- node PIDs;
- node stdout/stderr logs;
- metrics scrape if existing metrics already expose relevant peer-candidate outcomes;
- per-node pqc_trust_bundle_sequence.json SHA-256 pre/post;
- per-node pqc_authority_state.json SHA-256 pre/post if present;
- per-node data-dir inventory;
- staging evidence:
  - stable log line proving staging hook armed;
  - stable log line or debug archive proving candidate staged;
  - queue count / staged candidate fingerprint / staged sequence if available without adding new metrics;
- denylist grep for apply, sequence write, marker write, session eviction, SIGHUP, reload-apply, trusted-root fallback, dummy crypto.

Required scenario matrix:

A1. valid v2 live 0x05 candidate stages
- V0 sends valid v2 candidate.
- V1 validates candidate over real live 0x05.
- V1 stages candidate after validation.
- Expected:
  - Run 142 v2 path selected;
  - candidate validation accepted;
  - staging hook armed;
  - candidate staged;
  - no sequence write;
  - no authority-marker write;
  - no LivePqcTrustState mutation;
  - no session eviction;
  - no apply.

A2. idempotent v2 candidate dedupes
- Send same valid v2 candidate twice.
- Expected:
  - first stages;
  - second is deduped/already-staged;
  - queue does not grow unbounded;
  - no mutation.

A3. higher-sequence v2 candidate stages
- V1 has local v2 marker seq=N.
- Candidate seq=N+1.
- Expected:
  - validation accepts;
  - candidate stages;
  - local marker bytes unchanged;
  - sequence bytes unchanged.

A4. v2-after-v1 migration candidate stages
- V1 has local v1 marker.
- Candidate valid v2 migration candidate.
- Expected:
  - validation accepts;
  - candidate stages;
  - v1 marker bytes preserved;
  - no v2 marker write.

R1. staging disabled preserves Run 143 behavior
- Receiver validation enabled but staging not armed.
- Valid v2 candidate is validated but not staged.
- Existing Run 143 validation-only behavior unchanged.

R2. MainNet refusal, if flag/config exists
- Attempt to start with staging armed under MainNet.
- Expected:
  - startup refuses or staging config refuses;
  - no staging;
  - no P2P startup if startup refusal is the chosen behavior.
- If no binary flag/config exists, document as not applicable.

R3. lower-sequence v2 candidate does not stage
- Candidate rejected by validation.
- Expected:
  - no staging;
  - no mutation;
  - no invalid propagation.

R4. same-sequence different-digest candidate does not stage
- Expected:
  - no staging;
  - no mutation.

R5. bad-signature v2 candidate does not stage
- Expected:
  - Run 130 verifier failure;
  - no staging;
  - no mutation.

R6. wrong-domain v2 candidate does not stage
- Wrong chain/environment/genesis.
- Expected:
  - validation rejects;
  - no staging.

R7. ambiguous v1+v2 candidate does not stage
- Expected:
  - ambiguity guard fires;
  - no staging;
  - no propagation.

R8. propagation disabled + staging enabled
- Valid candidate stages.
- No rebroadcast.

R9. propagation enabled + staging disabled
- Valid candidate may propagate under existing Run 088/143 rules.
- No staging.

R10. propagation enabled + staging enabled
- Valid candidate may both stage and propagate.
- Invalid candidate neither stages nor propagates.
- No apply.

R11. queue bound behavior on release binary, if observable
- Send more candidates than per-peer/global cap.
- Expected:
  - reject-new or bounded behavior as specified by Run 145;
  - no unbounded growth;
  - no mutation.
- If not feasible with available fixture catalogue, document exact limitation and rely on Run 145/146 tests.

R12. v1 live inbound regression
- Existing v1 candidate behavior unchanged.
- No accidental v2 staging unless explicitly supported by the policy and tests.
- Still no apply.

R13. legacy/no-sidecar regression
- Existing behavior unchanged.
- No staging unless validation outcome and policy explicitly allow it.

Required negative invariants across all scenarios:
- no Run 070 apply call;
- no LivePqcTrustState swap;
- no pqc_trust_bundle_sequence.json byte change due to staging;
- no pqc_authority_state.json byte change due to staging;
- no session eviction;
- no SIGHUP outcome;
- no reload-apply outcome;
- no peer-driven apply metric/log;
- no trusted-root fallback;
- no active DummySig / DummyKem / DummyAead;
- invalid candidates never stage;
- invalid candidates never propagate.

Required report content:
- State whether Run 147 was pure evidence-only or needed a minimal hidden flag.
- If a hidden flag was added, explicitly state the source delta and why Run 147 was not purely evidence-only.
- Include:
  - feasibility gate result;
  - binary identity;
  - helper identity;
  - scenario table;
  - staging proof;
  - non-mutation proof;
  - v1/legacy regression proof;
  - propagation interaction proof if covered;
  - out-of-scope denylist proof.
- State:
  - peer-driven live apply remains unimplemented;
  - staging is non-authoritative;
  - MainNet staging remains refused;
  - release-binary staging is evidenced only if the staging hook is actually armed in the real binary;
  - full C4 remains open;
  - C5 remains open.

Validation commands:
Run at minimum:
- cargo build --release -p qbind-node --bin qbind-node
- cargo build --release -p qbind-node --example <needed helpers>
- bash scripts/devnet/run_147_live_0x05_peer_candidate_staging_release_binary.sh
- cargo test -p qbind-node --test run_146_live_inbound_0x05_staging_hook_tests
- cargo test -p qbind-node --test run_145_peer_candidate_staging_tests
- cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests
- cargo test -p qbind-node --test run_088_pqc_peer_candidate_propagation_tests
- cargo test -p qbind-node --test run_079_peer_candidate_wire_tests
- cargo test -p qbind-node --test run_109_peer_candidate_wire_ratification_tests
- cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests
- cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests
- cargo test -p qbind-node --lib pqc_authority
- cargo test -p qbind-node --lib

Acceptance criteria:
Run 147 is acceptable only if:
1. the feasibility gate is answered honestly;
2. if evidence is claimed, real target/release/qbind-node arms the staging hook;
3. real live 0x05 candidate receipt drives staging after validation acceptance;
4. invalid candidates do not stage;
5. staging remains bounded, deduped, and non-authoritative;
6. no apply, live trust mutation, sequence write, marker write, or session eviction occurs;
7. validation-only and propagation-only behavior remain unchanged;
8. MainNet staging remains refused;
9. evidence artifacts are archived under docs/devnet/run_147_live_0x05_peer_candidate_staging_release_binary/;
10. contradiction.md and operator/protocol docs are narrowly updated;
11. no full C4 or C5 closure is claimed.