Run 251 — Release-binary modeled durable-consume completion-reporter evidence

You are continuing QBIND after Run 250.

Run 240 is fully closed at source/test level.
Run 241 is fully closed as release-binary evidence for Run 240.
Run 242 is fully closed as source/test governance execution mutation-engine boundary work.
Run 243 is fully closed as release-binary evidence for Run 242.
Run 244 is fully closed as source/test modeled governance trust-state mutation applier boundary work.
Run 245 is fully closed as release-binary evidence for Run 244.
Run 246 is fully closed as source/test governance modeled end-to-end pipeline boundary work.
Run 247 is fully closed as release-binary evidence for Run 246.
Run 248 is fully closed as source/test modeled durable-consume projection sink boundary work.
Run 249 is fully closed as release-binary evidence for Run 248.
Run 250 is fully closed as source/test modeled durable-consume receipt-acknowledgement / completion reporter boundary work.

Strict workflow:

* Execution-first.
* Narrow scope only.
* No broad redesign.
* No silent regressions.
* No hype or overclaiming.
* Keep Full C4 OPEN and C5 OPEN unless real production evidence closes them.
* MainNet peer-driven apply remains refused.
* Fixture/local/loopback material remains DevNet/TestNet evidence-only.
* Rejected paths must remain non-mutating.
* Do not weaken Runs 070, 130–250.
* Release-binary evidence only.
* No production source module modification unless required for a compile-only fix, and any such fix must be explicitly justified.

Current state:

* Run 224 introduced source/test governance evaluator runtime integration.
* Run 225 release-evidenced the Run 224 evaluator-runtime integration.
* Run 226 wired representable runtime call sites through the evaluator-runtime integration at source/test level.
* Run 227 release-evidenced the Run 226 call-site wiring.
* Run 230 / 232 / 234 / 236 / 238 / 240 built the replay/freshness, consume-after-success, durable replay-state backend, and durable runtime integration path.
* Run 241 release-evidenced the Run 240 durable runtime integration.
* Run 242 added the source/test governance execution mutation-engine boundary.
* Run 243 release-evidenced the Run 242 mutation-engine boundary.
* Run 244 added the source/test modeled governance trust-state mutation applier boundary.
* Run 245 release-evidenced the Run 244 modeled applier boundary.
* Run 246 added the source/test governance modeled end-to-end pipeline.
* Run 247 release-evidenced the Run 246 modeled end-to-end pipeline boundary.
* Run 248 added the source/test modeled durable-consume projection sink.
* Run 249 release-evidenced the Run 248 modeled durable-consume projection sink boundary.
* Run 250 added the source/test modeled durable-consume receipt-acknowledgement / completion reporter in `crates/qbind-node/src/pqc_governance_modeled_durable_consume_completion_reporter.rs`.
* Run 250 proves source/test-only that Run 246 pipeline success is required before a sink intent can exist, Run 248 `ConsumeReceiptRecorded` is required before any completion report can be recorded, and only `CompletionReportRecorded` authorizes a new modeled completion-reported state.
* Run 250 has no release-binary evidence yet.
* No real persistent replay backend is implemented.
* No real durable consume backend is implemented.
* No real completion-report backend is implemented.
* No real governance execution engine is implemented.
* No real production mutation engine is implemented.
* No real on-chain governance proof verifier is implemented.
* No RocksDB/file/schema/migration/storage-format change is implemented.
* No KMS/HSM/RemoteSigner backend is implemented.
* No MainNet governance enablement.
* No MainNet peer-driven apply enablement.
* Validator-set rotation remains unsupported.
* Full C4 remains OPEN.
* C5 remains OPEN.

Objective:

Produce release-binary evidence for the Run 250 modeled durable-consume completion-reporter boundary, mirroring the Run 241 / 243 / 245 / 247 / 249 release-evidence pattern.

Run 251 must prove on real `target/release/qbind-node` plus a release-built helper that the Run 250 completion-reporter boundary is present in release-built code, that the helper exercises the Run 246 pipeline → Run 248 consume-sink projection → Run 250 completion-report projection → fixture report-recording path through production library symbols, and that all fail-closed / non-mutating / no-completion invariants remain true in release mode.

Run 251 must not implement a real persistent replay backend, must not implement a real durable consume backend, must not implement a real completion-report backend, must not wire production runtime mutation, must not call Run 070, must not mutate `LivePqcTrustState`, must not perform a real trust swap, must not evict sessions, must not write sequence files, must not write authority markers, must not touch RocksDB, must not change wire/schema/marker/sequence/trust-bundle formats, and must not enable MainNet governance or MainNet peer-driven apply.

Preflight docs hygiene:

* In `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`, if the Run 249 and Run 250 changelog bullets are joined like:

  * `**Full C4 remains OPEN; C5 remains OPEN.*** **Run 250**`
* fix it narrowly by inserting a newline so Run 250 renders as a separate bullet.
* This is docs-only hygiene and must not be treated as a behavior change.

Required deliverables:

1. Add a release helper:

   `crates/qbind-node/examples/run_251_modeled_durable_consume_completion_reporter_release_binary_helper.rs`

   The helper must link against release-built production library symbols and exercise:

   * `pqc_governance_modeled_durable_consume_completion_reporter`
   * `GovernanceModeledDurableConsumeCompletionReporterInput`
   * `GovernanceModeledDurableConsumeCompletionReporterExpectations`
   * `GovernanceModeledDurableConsumeCompletionReporterPolicy`
   * `GovernanceModeledDurableConsumeCompletionReporterSurface`
   * `GovernanceModeledDurableConsumeCompletionReporterEnvironmentBinding`
   * `GovernanceModeledDurableConsumeCompletionReporterRuntimeBinding`
   * `GovernanceModeledDurableConsumeCompletionReporterReplayBinding`
   * `GovernanceModeledDurableConsumeCompletionReporterPipelineBinding`
   * `GovernanceModeledDurableConsumeCompletionReporterSinkBinding`
   * `GovernanceModeledDurableConsumeCompletionReport`
   * `ModeledDurableConsumeCompletionReportLedger`
   * `ModeledDurableConsumeCompletionReportRecord`
   * `ModeledDurableConsumeCompletionReportSnapshot`
   * `ModeledDurableConsumeCompletionReportStatus`
   * `ModeledDurableConsumeCompletionReportDigest`
   * `GovernanceModeledDurableConsumeCompletionReporterOutcome`
   * `CompletionReportIntent`
   * `GovernanceModeledDurableConsumeCompletionReporter`
   * `FixtureModeledDurableConsumeCompletionReporter`
   * `ProductionModeledDurableConsumeCompletionReporter`
   * `MainNetModeledDurableConsumeCompletionReporter`
   * `project_sink_outcome_to_completion_report_intent`
   * `evaluate_modeled_durable_consume_completion_reporter`
   * `recover_modeled_durable_consume_completion_reporter_window`
   * `completion_reporter_outcome_authorizes_modeled_completion`
   * `completion_reporter_outcome_projects_to_durable_completion`
   * all Run 250 grep-verifiable invariant helpers

   If any exact symbol name differs from Run 250, use the exact implemented name and record the substitution in `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_251.md`. Do not invent compatibility shims unless needed for compilation, and do not change production behavior.

2. Add a release harness:

   `scripts/devnet/run_251_modeled_durable_consume_completion_reporter_release_binary.sh`

   The harness must:

   * build `target/release/qbind-node`;
   * build the Run 251 release helper;
   * capture git commit, rustc version, cargo version;
   * capture SHA-256 and ELF Build ID for `target/release/qbind-node`;
   * capture SHA-256 and ELF Build ID for the Run 251 helper;
   * run real-binary surface scenarios;
   * run the helper corpus in release mode;
   * run source-reachability greps for the Run 250 symbols;
   * run helper-output reachability checks proving the helper exercised the Run 250 symbols;
   * run a denylist proving no active production/MainNet enablement claims;
   * run the required regression test corpus;
   * write generated evidence into an ignored evidence directory.

3. Add evidence archive directory:

   `docs/devnet/run_251_modeled_durable_consume_completion_reporter_release_binary/`

   Track only:

   * `README.md`
   * `summary.txt`
   * `.gitignore`

   Generated artifacts must be ignored, following the Run 241 / 243 / 245 / 247 / 249 convention.

4. Add canonical evidence report:

   `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_251.md`

Release-helper corpus:

The helper must include accepted/compatible, rejection, recovery, projection, stage-ordering, completion-report-ledger, non-mutation, and reachability tables.

Accepted / compatible cases must prove at minimum:

* Disabled reporter policy preserves legacy bypass and performs no acknowledgement, no completion report, and no mutation.
* Disabled sink policy preserves legacy bypass and never invokes the reporter.
* Disabled pipeline policy preserves legacy bypass and never invokes the reporter.
* Disabled evaluator/call-site policy preserves legacy bypass and never invokes the reporter.
* DevNet fixture pipeline success + sink receipt recorded + reporter record success records one modeled in-memory completion report.
* TestNet fixture pipeline success + sink receipt recorded + reporter record success records one modeled in-memory completion report.
* Modeled add-root pipeline success records a completion report only after modeled applier success and sink receipt record.
* Modeled retire-root pipeline success records a completion report only after modeled applier success and sink receipt record.
* Modeled revoke-root pipeline success records a completion report only after modeled applier success and sink receipt record.
* Modeled emergency-revoke-root pipeline success records a completion report only after modeled applier success and sink receipt record.
* Modeled noop pipeline success records a completion report only under explicit success semantics, no trust-state drift, and a recorded receipt.
* Duplicate identical completion report is idempotent under an explicit typed outcome and creates no second report.
* `ConsumeReceiptDuplicateIdempotent` only matches an already-recorded completion report and never creates a new one by itself.
* Existing Run 248 sink behavior remains compatible.
* Existing Run 246 pipeline behavior remains compatible.
* Existing Run 244 modeled applier behavior remains compatible.
* Existing Run 242 mutation-engine behavior remains compatible.
* Existing Run 240 durable completion behavior remains compatible.
* Production reporter path is reachable but unavailable/fail-closed and records no completion report.
* MainNet reporter path is reachable but unavailable/fail-closed and records no completion report.
* MainNet peer-driven apply refused before pipeline progression, before sink invocation, and before reporter invocation.
* Validator-set rotation unsupported and records no completion report.
* Policy-change unsupported and records no completion report.

Rejected / fail-closed cases must prove at minimum:

* evaluator rejection produces no sink intent, no receipt, no reporter intent, and no completion report;
* call-site rejection produces no sink intent, no receipt, no reporter intent, and no completion report;
* durable replay stale produces no reporter intent and no completion report;
* durable replay expired produces no reporter intent and no completion report;
* durable replay consumed produces no reporter intent and no completion report;
* durable replay superseded produces no reporter intent and no completion report;
* durable replay backend unavailable produces no reporter intent and no completion report;
* mutation-engine rejection produces no reporter intent and no completion report;
* modeled applier rejected-before-snapshot produces no reporter intent and no completion report;
* modeled applier rejected-before-apply produces no reporter intent and no completion report;
* modeled apply failure produces no reporter intent and no completion report;
* modeled rollback produces no reporter intent and no completion report;
* modeled rollback-failed produces no reporter intent and no completion report;
* modeled ambiguous window produces no reporter intent and no completion report;
* sink did not record receipt produces no reporter invocation and no completion report;
* sink rejected-before-record produces no reporter invocation and no completion report;
* sink record failure produces no reporter invocation and no completion report;
* sink rollback produces no reporter invocation and no completion report;
* sink rollback-failed produces no reporter invocation and no completion report;
* sink ambiguous window produces no reporter invocation and no completion report;
* completion before sink receipt record is rejected;
* completion after modeled apply failure is rejected;
* completion after sink record failure is rejected;
* completion after sink rollback is rejected;
* completion after sink rollback-failed is rejected;
* completion after sink ambiguous window is rejected;
* same completion report id with different digest is rejected as equivocation and records no second report;
* wrong environment rejected before reporter invocation;
* wrong chain rejected before reporter invocation;
* wrong genesis rejected before reporter invocation;
* wrong governance surface rejected before reporter invocation;
* wrong mutation surface rejected before reporter invocation;
* wrong report digest rejected before report record;
* wrong receipt digest rejected before report record;
* wrong sink decision digest rejected before report record;
* wrong pipeline decision digest rejected before report record;
* wrong proposal id rejected before report record;
* wrong decision id rejected before report record;
* wrong candidate digest rejected before report record;
* wrong authority-domain sequence rejected before report record;
* malformed completion report rejected before record;
* local operator key cannot satisfy MainNet authority;
* peer majority cannot satisfy MainNet authority;
* production reporter unavailable records no completion report;
* MainNet reporter unavailable records no completion report;
* validator-set rotation attempt unsupported and records no completion report;
* policy-change attempt unsupported and records no completion report;
* every rejected path produces no Run 070 call, no `LivePqcTrustState` mutation, no real trust swap, no session eviction, no sequence write, no marker write, no RocksDB/file/schema/migration/storage-format change, and no reporter invocation where rejection happens before the reporter stage.

Recovery / crash-window cases must prove at minimum:

* before-pipeline window fails closed / no report;
* after-pipeline-success-before-sink-intent window fails closed / no report;
* after-sink-intent-before-receipt-record window fails closed / no report;
* after-receipt-record-before-report-intent window fails closed / no report;
* after-report-intent-before-report-record window fails closed / no completion;
* after-report-record-before-report-success window fails closed unless an explicit matching completion report success exists;
* after-report-success recovers as completion reported;
* after-report-ambiguous fails closed / no completion;
* reporter record-failed window fails closed / no completion;
* reporter rollback-completed window fails closed / no completion;
* reporter rollback-failed window is fatal/fail-closed / no completion;
* unknown window fails closed / no completion;
* production/MainNet recovery classification unavailable/fail-closed;
* MainNet peer-driven apply refusal precedes recovery classification where applicable.

Projection cases must prove at minimum:

* only Run 248 `ConsumeReceiptRecorded` creates a completion-report intent;
* Run 246 pipeline success alone creates no completion-report intent;
* sink intent alone creates no completion-report intent;
* `ConsumeReceiptDuplicateIdempotent` creates no new completion report unless a matching completion report already exists;
* evaluator success alone creates no completion-report intent;
* durable replay freshness alone creates no completion-report intent;
* mutation-engine authorization alone creates no completion-report intent;
* modeled applier success without sink receipt record creates no completion-report intent;
* only `CompletionReportRecorded` authorizes modeled completion-reported state;
* `LegacyBypassNoCompletionReport` does not complete;
* `RejectedBeforeSinkNoCompletionReport` does not complete;
* `SinkDidNotRecordReceiptNoCompletionReport` does not complete;
* `CompletionReportDuplicateIdempotent` does not create a second completion report and must not be counted as a new completion;
* `CompletionReportRejectedBeforeRecord` does not complete;
* `CompletionReportRecordFailedNoCompletion` does not complete;
* `CompletionReportRolledBackNoCompletion` does not complete;
* `CompletionReportRollbackFailedFatalNoCompletion` does not complete;
* `CompletionReportAmbiguousFailClosedNoCompletion` does not complete;
* `ProductionReporterUnavailableNoCompletion` does not complete;
* `MainNetReporterUnavailableNoCompletion` does not complete;
* `MainNetPeerDrivenApplyRefusedNoCompletion` does not complete;
* `ValidatorSetRotationUnsupportedNoCompletion` does not complete;
* `PolicyChangeUnsupportedNoCompletion` does not complete.

Stage-ordering cases must prove at minimum:

* MainNet peer-driven apply refusal happens before pipeline progression, sink invocation, and reporter invocation.
* Evaluator/call-site authorization happens before durable replay freshness.
* Durable replay freshness happens before mutation-engine authorization.
* Mutation-engine authorization happens before modeled applier invocation.
* Modeled applier success happens before sink intent.
* Sink receipt recording happens after pipeline consume authorization.
* Completion report intent happens only after sink receipt recording.
* Completion report recording happens after receipt-recorded state.
* A rejection before the reporter stage leaves the reporter invocation count at zero.
* A reporter record failure does not retroactively invalidate the sink-recorded receipt but does not authorize completion.
* A reporter rollback failure is fatal/fail-closed and does not authorize completion.
* The reporter itself performs no persistent durable consume beyond modeled in-memory fixture state.

Completion-report-ledger cases must prove at minimum:

* one valid completion report inserts exactly one modeled in-memory ledger record;
* duplicate identical completion report is idempotent and does not increase record count;
* same completion report id with different digest is equivocation and does not increase record count;
* wrong report digest does not record;
* wrong receipt digest does not record;
* wrong sink decision digest does not record;
* wrong pipeline decision digest does not record;
* wrong proposal id does not record;
* wrong decision id does not record;
* wrong candidate digest does not record;
* wrong authority-domain sequence does not record;
* malformed completion report does not record;
* rollback restores modeled completion-report ledger snapshot;
* rollback failure never claims success;
* fixture completion ledger never writes files, RocksDB, markers, sequences, or production durable state.

Non-mutation cases must prove at minimum:

* no Run 070 call on any helper case;
* no `LivePqcTrustState` mutation on any helper case;
* no real trust swap on any helper case;
* no session eviction on any helper case;
* no sequence write on any helper case;
* no marker write on any helper case;
* no RocksDB/file/schema/migration/storage-format write or format change;
* no wire/schema/marker/sequence/trust-bundle format change;
* fixture reporter mutates only `ModeledDurableConsumeCompletionReportLedger`;
* production/MainNet paths remain unavailable/fail-closed;
* MainNet peer-driven apply remains refused first.

Real release-binary surface scenarios:

The harness must run the real `target/release/qbind-node` for at least:

* S1 — `--help` / default help surface: no durable-consume completion-reporter enablement banner or visible public flag drift.
* S2 — default DevNet startup parse/smoke surface: no completion-reporter enablement claim.
* S3 — default TestNet startup parse/smoke surface: no completion-reporter enablement claim.
* S4 — default MainNet startup parse/smoke surface: no completion-reporter enablement claim.
* S5 — hidden governance-execution selector or equivalent hidden selector still parses as before, without exposing the Run 250/251 helper-only path as a public production enablement surface.
* S6 — invalid governance-execution selector or equivalent invalid selector fails closed before mutation if applicable, with no marker write, no sequence write, no live trust swap, no session eviction, no Run 070 call, and no completion-reporter invocation.

Do not add any new public CLI surface for Run 251. If a hidden selector already exists from earlier runs, the harness may use it only as a parse/fail-closed smoke surface.

Denylist:

Prove the captured real-binary logs and helper logs do not contain active/enabled claims for:

* real completion-report backend enabled;
* modeled completion-reporter production enabled;
* MainNet modeled completion-reporter enabled;
* real durable consume backend enabled;
* real persistent replay backend enabled;
* modeled durable-consume sink production enabled;
* MainNet modeled durable-consume sink enabled;
* real production mutation engine enabled;
* MainNet mutation engine enabled;
* MainNet governance enabled;
* MainNet peer-driven apply enabled;
* real governance execution engine enabled;
* real on-chain governance proof verifier enabled;
* RocksDB replay backend enabled;
* file replay backend enabled;
* schema migration enabled;
* storage-format migration enabled;
* KMS/HSM backend enabled;
* RemoteSigner backend enabled;
* validator-set rotation enabled;
* policy-change action enabled;
* autonomous apply;
* apply-on-receipt;
* peer-majority authority;
* Run 070 apply from the modeled completion reporter;
* LivePqcTrustState mutation from the modeled completion reporter;
* real trust swap from the modeled completion reporter;
* session eviction from the modeled completion reporter;
* marker write from the modeled completion reporter;
* sequence write from the modeled completion reporter;
* RocksDB write from the modeled completion reporter;
* file write from the modeled completion reporter;
* production durable consume by the modeled completion reporter itself;
* production completion reporting by the modeled reporter itself;
* DummySig / DummyKem / DummyAead active on production path.

Source/release reachability proof:

The harness must grep production source for the Run 250 symbols and prove the release helper exercises them in release mode.

Required greps include:

* `pqc_governance_modeled_durable_consume_completion_reporter`
* `GovernanceModeledDurableConsumeCompletionReporterInput`
* `GovernanceModeledDurableConsumeCompletionReporterExpectations`
* `GovernanceModeledDurableConsumeCompletionReporterPolicy`
* `GovernanceModeledDurableConsumeCompletionReport`
* `ModeledDurableConsumeCompletionReportLedger`
* `ModeledDurableConsumeCompletionReportRecord`
* `ModeledDurableConsumeCompletionReportSnapshot`
* `ModeledDurableConsumeCompletionReportStatus`
* `ModeledDurableConsumeCompletionReportDigest`
* `GovernanceModeledDurableConsumeCompletionReporterOutcome`
* `CompletionReportIntent`
* `GovernanceModeledDurableConsumeCompletionReporter`
* `FixtureModeledDurableConsumeCompletionReporter`
* `ProductionModeledDurableConsumeCompletionReporter`
* `MainNetModeledDurableConsumeCompletionReporter`
* `project_sink_outcome_to_completion_report_intent`
* `evaluate_modeled_durable_consume_completion_reporter`
* `recover_modeled_durable_consume_completion_reporter_window`
* `completion_reporter_outcome_authorizes_modeled_completion`
* `completion_reporter_outcome_projects_to_durable_completion`
* `CompletionReportRecorded`
* `CompletionReportDuplicateIdempotent`
* `CompletionReportRejectedBeforeRecord`
* `CompletionReportRecordFailedNoCompletion`
* `CompletionReportRolledBackNoCompletion`
* `CompletionReportRollbackFailedFatalNoCompletion`
* `CompletionReportAmbiguousFailClosedNoCompletion`
* `ProductionReporterUnavailableNoCompletion`
* `MainNetReporterUnavailableNoCompletion`
* `MainNetPeerDrivenApplyRefusedNoCompletion`
* `ValidatorSetRotationUnsupportedNoCompletion`
* `PolicyChangeUnsupportedNoCompletion`
* `modeled_completion_reporter_rejection_is_non_mutating`
* `modeled_completion_reporter_never_calls_run_070`
* `modeled_completion_reporter_never_mutates_live_pqc_trust_state`
* `modeled_completion_reporter_never_writes_sequence_or_marker`
* `modeled_completion_reporter_no_rocksdb_file_schema_migration_change`
* `modeled_completion_reporter_pipeline_success_required_before_report`
* `modeled_completion_reporter_sink_receipt_required_before_report`
* `modeled_completion_reporter_report_record_required_before_completion`
* `modeled_completion_reporter_failed_record_never_completes`
* `modeled_completion_reporter_rollback_never_completes`
* `modeled_completion_reporter_ambiguous_window_fails_closed`
* `modeled_completion_reporter_mainnet_peer_driven_apply_refused_first`
* `modeled_completion_reporter_production_mainnet_unavailable`
* `modeled_completion_reporter_validator_set_rotation_unsupported`
* `modeled_completion_reporter_policy_change_unsupported`
* `modeled_completion_reporter_local_operator_cannot_satisfy_mainnet_authority`
* `modeled_completion_reporter_peer_majority_cannot_satisfy_mainnet_authority`

If exact symbol names differ, use the exact implemented Run 250 names and record the differences honestly in `QBIND_DEVNET_EVIDENCE_RUN_251.md`.

Validation commands:

Run at minimum:

* `cargo build -p qbind-node --release --bin qbind-node`
* `cargo build -p qbind-node --release --example run_251_modeled_durable_consume_completion_reporter_release_binary_helper`
* `bash scripts/devnet/run_251_modeled_durable_consume_completion_reporter_release_binary.sh`
* `cargo test -p qbind-node --test run_250_modeled_durable_consume_completion_reporter_tests`
* `cargo test -p qbind-node --test run_248_modeled_durable_consume_projection_sink_tests`
* `cargo test -p qbind-node --test run_246_governance_modeled_end_to_end_pipeline_tests`
* `cargo test -p qbind-node --test run_244_modeled_governance_trust_mutation_applier_tests`
* `cargo test -p qbind-node --test run_242_governance_execution_mutation_engine_tests`
* `cargo test -p qbind-node --test run_240_governance_evaluator_replay_durable_runtime_integration_tests`
* `cargo test -p qbind-node --test run_238_governance_evaluator_replay_durable_backend_tests`
* `cargo test -p qbind-node --test run_236_governance_evaluator_replay_consume_runtime_integration_tests`
* `cargo test -p qbind-node --test run_234_governance_evaluator_replay_consume_boundary_tests`
* `cargo test -p qbind-node --test run_232_governance_evaluator_replay_runtime_integration_tests`
* `cargo test -p qbind-node --test run_230_governance_evaluator_replay_state_tests`
* `cargo test -p qbind-node --test run_228_peer_evaluator_context_representation_tests`
* `cargo test -p qbind-node --test run_226_governance_evaluator_runtime_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_224_governance_evaluator_runtime_integration_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

Security scanning:

* Run secret scanning if available and report the result.
* Run CodeQL if available and report the result honestly.
* If Rust CodeQL analysis is skipped because the database is too large, say that explicitly and do not overclaim CodeQL coverage.

Documentation:

Create:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_251.md`
* `docs/devnet/run_251_modeled_durable_consume_completion_reporter_release_binary/README.md`
* `docs/devnet/run_251_modeled_durable_consume_completion_reporter_release_binary/summary.txt`
* `docs/devnet/run_251_modeled_durable_consume_completion_reporter_release_binary/.gitignore`

Update narrowly:

* `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
* `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
* `docs/whitepaper/contradiction.md`

Documentation must state:

* Run 251 is release-binary evidence for Run 250.
* No production mutating behavior is enabled.
* The modeled completion-reporter boundary is release-evidenced, not production-enabled.
* The release helper exercises the Run 250 production library symbols in release mode.
* The release helper remains dead code from the production runtime.
* The fixture reporter mutates only modeled in-memory `ModeledDurableConsumeCompletionReportLedger` state.
* Run 246 pipeline success is required before any sink intent can exist.
* Run 248 `ConsumeReceiptRecorded` is required before any completion-report intent can exist.
* Only `CompletionReportRecorded` authorizes a new modeled completion-reported state.
* `ConsumeReceiptDuplicateIdempotent` may only match an already-recorded completion report and never creates one by itself.
* Duplicate identical completion report is idempotent and creates no second report.
* Same completion report id with different digest fails closed as equivocation.
* Every non-recording sink outcome produces no reporter invocation and no completion report.
* Failed report record, rollback, rollback-failed, ambiguous windows, unavailable production/MainNet reporter paths, rejected replay states, and unsupported actions never complete.
* MainNet peer-driven apply remains refused before pipeline progression, sink invocation, and reporter invocation.
* Rejected reporter paths are non-mutating.
* No real completion-report backend is implemented.
* No real durable consume backend is implemented.
* No real persistent replay backend is implemented.
* No real production mutation engine is implemented.
* No real governance execution engine is implemented.
* No real on-chain governance proof verifier is implemented.
* No RocksDB/file/schema/migration/storage-format change is implemented.
* No wire/schema/marker/sequence/trust-bundle change is implemented.
* No KMS/HSM/RemoteSigner backend is implemented.
* No MainNet governance enablement.
* No MainNet peer-driven apply enablement.
* No validator-set rotation.
* The reporter does not call Run 070.
* The reporter does not mutate `LivePqcTrustState`.
* The reporter does not perform a real trust swap.
* The reporter does not evict sessions.
* The reporter does not write sequence files.
* The reporter does not write authority markers.
* Full C4 remains OPEN.
* C5 remains OPEN.

Contradiction handling:

* Crosscheck Run 251 against Runs 050–250.
* If no contradiction is found, add a “no Run 251 contradiction” entry to `docs/whitepaper/contradiction.md`.
* If any contradiction is found, do not hide it. Record it explicitly with the exact file/function/doc reference and recommend the smallest safe fix.

Strict non-goals:

* Do not implement a real persistent replay backend.
* Do not implement a real durable consume backend.
* Do not implement a real completion-report backend.
* Do not implement a real production mutation engine.
* Do not wire production runtime mutation.
* Do not call Run 070.
* Do not mutate `LivePqcTrustState`.
* Do not perform a real trust swap.
* Do not evict sessions.
* Do not write sequence files.
* Do not write authority markers.
* Do not implement a real governance execution engine.
* Do not implement a real on-chain governance proof verifier.
* Do not implement validator-set rotation.
* Do not add RocksDB/file/schema/migration/storage-format changes.
* Do not change wire/schema/marker/sequence/trust-bundle formats.
* Do not enable MainNet governance.
* Do not enable MainNet peer-driven apply.
* Do not add autonomous apply or apply-on-receipt.
* Do not let peer majority satisfy authority.
* Do not weaken any fail-closed behavior from Runs 070 or 130–250.

Expected verdict:

Positive for release-binary evidence, if the release helper/harness and regression corpus pass.

Run 251 should close the Run 250 release-binary evidence gap only. It must not claim Full C4 or C5 closure.

Final response format:

1. Exact verdict.
2. Files changed.
3. Release artifacts and hashes.
4. Helper corpus results.
5. Real-binary scenarios.
6. Tests run and results.
7. Security invariants preserved.
8. Honest limitations.
9. C4/C5 status.
10. Suggested Run 252 next step.
