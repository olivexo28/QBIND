# RUN 133 — Release-Binary Evidence for v2 Validation-Only Surfaces

## Current state

QBIND is a post-quantum-secure L1 blockchain. Security, correctness, production safety, and evidence discipline take priority over speed or convenience.

Recent state:

- Runs 050–068 completed signed PQC trust-bundle lifecycle.
- Runs 069–075 completed local operator reload lifecycle.
- Runs 076–086 completed peer-candidate `0x05` validation-only lifecycle.
- Run 087 added peer-driven propagation/apply safety specification.
- Run 088 implemented disabled-by-default validation-before-rebroadcast propagation prototype.
- Run 089 produced strongest-positive N=3 DevNet release-binary propagation evidence.
- Run 090 updated the operator runbook for propagation-only behavior.
- Runs 091–099 established and release-evidenced canonical `activation_epoch` runtime-source behavior while preserving fail-closed behavior when no committed epoch exists.
- Run 100 added the formal production trust-anchor authority and bundle-signing-key ratification model.
- Run 101 added additive genesis authority fields and canonical genesis verification API.
- Run 102 wired canonical genesis verification into release-binary startup and fixed `--print-genesis-hash`.
- Run 103 implemented the minimal bundle-signing-key ratification verifier primitive.
- Run 104 added genesis-bound full ML-DSA-44 authority public key material.
- Runs 105–115 wired and release-evidenced v1 ratification enforcement across startup, reload-check, peer-candidate check, live inbound `0x05`, process-start reload-apply, and SIGHUP live reload.
- Run 116 produced the spec-first authority anti-rollback persistence model.
- Run 117 implemented the authority marker storage/snapshot primitive.
- Run 118 added authority-marker derivation and compare-before-accept helpers.
- Run 119 wired authority marker compare/persist into process-start reload-apply.
- Run 120 wired authority marker compare/persist into startup `--p2p-trust-bundle`.
- Run 121 wired authority marker compare/persist into SIGHUP live reload.
- Run 122 produced strongest-positive release-binary evidence for authority marker behavior on mutating surfaces.
- Run 123 wired validation-only authority marker conflict checks for reload-check, local peer-candidate check, and live inbound `0x05`.
- Run 124 wired snapshot/restore authority marker conflict enforcement.
- Run 125 produced strongest-positive release-binary evidence for snapshot/restore authority marker conflict enforcement.
- Run 126 produced the spec-first authority-state reset/recovery procedure.
- Run 127 implemented the offline authority-state reset CLI skeleton with typed refusal cases.
- Run 128 produced strongest-positive release-binary evidence for the offline authority-state reset CLI.
- Run 129 produced the spec-first ratification v2 design:
  - selected monotonic model: per-authority-domain monotonic sequence `authority_domain_sequence`;
  - v2 domain tag: `QBIND:BUNDLE-SIGNING-RATIFICATION:v2`;
  - lifecycle action binding: `ratify`, `rotate`, `revoke`;
  - v2-after-v1 is the explicit migration path;
  - v1-after-v2 is fail-closed;
  - lower sequence is rejected;
  - same sequence with different digest is rejected.
- Run 130 implemented the v2 ratification primitive:
  - v2 schema/types;
  - v2 canonical preimage/digest;
  - v2 typed verifier failures;
  - `verify_bundle_signing_key_ratification_v2`;
  - no production v2 enforcement wiring.
- Run 131 implemented the v2 authority marker primitive:
  - `PersistentAuthorityStateRecordVersioned::{V1,V2}`;
  - `PersistentAuthorityStateRecordV2`;
  - `derive_authority_state_v2_from_ratification(...)`;
  - `compare_authority_marker_v2(...)`;
  - `migrate_authority_marker_v1_to_v2(...)`;
  - `prepare_v2_marker_for_acceptance(...)`;
  - no production v2 surface wiring.
- Run 132 wired v2 ratification and v2 marker compatibility into validation-only surfaces:
  - versioned sidecar dispatch via `VersionedRatificationSidecar::{V1,V2}` or repo equivalent;
  - unknown/malformed schema fails closed;
  - existing v1 loader and behavior remain unchanged;
  - `reload-check` supports v1/v2 validation-only checks;
  - local `peer-candidate-check` supports v1/v2 validation-only checks;
  - v2 uses the Run 130 verifier;
  - v2 marker comparison uses Run 131 helpers;
  - v1-after-v2 rejects;
  - v2-after-v1 is accepted only as a migration candidate;
  - lower v2 sequence rejects;
  - same-sequence/different-digest rejects;
  - same-sequence/same-digest is idempotent;
  - higher sequence is validation-only compatible;
  - validation-only surfaces never persist marker state;
  - live inbound `0x05` v2 wiring was deferred;
  - mutating-surface v2 wiring was deferred;
  - release-binary v2 evidence remains open.

Run 132 evidence was strongest-positive for validation-only v2 source/test scope, but its three tracking docs were not updated by the GitHub AI agent.

Run 133 must first fix the Run 132 documentation sync gap.

Run 133 is evidence-only after the documentation sync fix.

---

# Exact objective

Produce release-binary evidence proving that the Run 132 v2 validation-only surfaces behave correctly on real `target/release/qbind-node` binaries.

The objective is to prove:

1. `--p2p-trust-bundle-reload-check` accepts valid v2 sidecars under the documented validation-only compatibility policy.
2. `--p2p-trust-bundle-peer-candidate-check` accepts valid v2 sidecars under the documented validation-only compatibility policy.
3. v1 behavior remains unchanged on both surfaces.
4. v1-after-v2 downgrade is refused.
5. lower v2 sequence is refused.
6. same-sequence/different-digest v2 is refused.
7. same-sequence/same-digest v2 is idempotent.
8. v2-after-v1 is accepted only as an explicit migration candidate and does not persist marker state.
9. malformed/unknown sidecar schema fails closed.
10. no validation-only scenario writes or mutates the authority marker file.
11. no sequence/root/live-trust mutation occurs.
12. no mutating-surface v2 wiring is added.
13. no live inbound `0x05` v2 wiring is added unless it was already safely implemented; if not, it remains deferred.

Run 133 should make this statement true:

> Real release binaries prove that v2 validation-only ratification checks accept only compatible v2 candidates, reject downgrade/replay/equivocation cases, preserve v1 behavior, and never persist marker state from validation-only paths.

---

# First checkpoint: Run 132 doc-sync fix

Before release-binary evidence work, update these three docs:

- `docs/whitepaper/contradiction.md`
- `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
- `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`

They must explicitly state:

- Run 132 wired v2 validation-only support for `reload-check` and local `peer-candidate-check`.
- Run 132 added versioned sidecar dispatch.
- Run 132 preserved existing v1 behavior.
- Run 132 uses Run 130 v2 verifier and Run 131 v2 marker comparison.
- v1-after-v2 rejects.
- lower v2 sequence rejects.
- same-sequence/different-digest rejects.
- v2-after-v1 is accepted only as a migration candidate.
- validation-only surfaces never persist marker state.
- live inbound `0x05` v2 wiring remains deferred.
- mutating-surface v2 wiring remains deferred.
- release-binary v2 evidence remains open until Run 133.
- rotation/revocation lifecycle remains future work.
- KMS/HSM, MainNet governance artifact support, peer-driven live apply, full C4, and C5 remain open.

Document this correction in the Run 133 evidence file.

---

# Strict scope

Allowed:

## A. Release-binary v2 validation-only evidence harness

Create or update a script such as:

```text
scripts/devnet/run_133_v2_validation_only_release_binary.sh
```

The harness should:

- build or use `target/release/qbind-node`;
- generate or reuse genesis-bound authority material;
- generate v1 ratification sidecars;
- generate v2 ratification sidecars for valid/invalid cases;
- seed absent/v1/v2 authority marker states as needed;
- run release-binary reload-check scenarios;
- run release-binary local peer-candidate-check scenarios;
- capture stdout/stderr/exit codes;
- capture marker bytes before/after;
- assert no marker writes in all validation-only scenarios;
- assert no normal node startup markers;
- assert no sequence/root/live-trust mutation;
- archive all evidence deterministically.

## B. Evidence helper updates

Allowed only if needed:

- helper for generating v2 sidecars;
- helper for v2 same-sequence/same-digest fixture;
- helper for v2 same-sequence/different-digest fixture;
- helper for lower-sequence fixture;
- helper for v1-after-v2 marker fixture;
- helper for marker byte comparison;
- helper for malformed/unknown sidecar schema fixture.

Helpers must remain evidence-only and must not become production bypasses.

## C. Narrow bug fixes

If release-binary evidence reveals a real Run 132 defect, fix it narrowly.

Any bug fix must:

- preserve v1 behavior;
- preserve no-persistence on validation-only surfaces;
- preserve v1-after-v2 refusal;
- preserve lower-sequence refusal;
- preserve same-sequence/different-digest refusal;
- not wire mutating surfaces;
- not wire live `0x05` unless explicitly still narrow and tested;
- add focused tests;
- be documented honestly.

## D. Documentation/evidence updates

Required docs/evidence updates are listed below.

---

# Strict non-goals

Run 133 MUST NOT:

- wire v2 into startup trust-bundle acceptance;
- wire v2 into process-start reload-apply;
- wire v2 into SIGHUP live reload;
- persist v2 marker state from validation-only surfaces;
- auto-migrate marker files during validation-only checks;
- implement live inbound `0x05` v2 wiring unless separately proven narrow and still non-mutating;
- implement signing-key rotation lifecycle;
- implement signing-key revocation lifecycle;
- implement MainNet governance artifact support;
- implement KMS/HSM custody;
- implement peer-driven live apply;
- implement peer-driven trust synchronization;
- change trust-bundle wire format;
- change peer-candidate wire format;
- change reset CLI behavior;
- redesign consensus;
- redesign fast-sync;
- redesign snapshots;
- weaken v1 ratification verification;
- weaken Run 117–132 anti-rollback/reset/v2 behavior;
- add production static source-code anchors;
- add fallback roots;
- add fallback signing keys;
- allow transport roots to authorize bundle-signing keys;
- allow local config alone to act as MainNet bundle-signing authority;
- claim rotation/revocation implemented;
- claim full C4 closure;
- claim C5 closure.

---

# Required investigation before evidence

Before running release evidence, investigate and document:

## 1. Release-binary command shapes

Identify exact command shapes for:

```text
--p2p-trust-bundle-reload-check
--p2p-trust-bundle-peer-candidate-check
--p2p-trust-bundle-ratification <PATH>
--data-dir <PATH>
--genesis-path <PATH>
--expected-genesis-hash <HASH>
--environment <devnet|testnet|mainnet>
```

or repo-current equivalents.

## 2. v2 fixture generation

Confirm how to generate:

- valid v2 no-marker fixture;
- v2-after-v1 migration candidate fixture;
- v1-after-v2 downgrade fixture;
- lower v2 sequence fixture;
- same-sequence/same-digest fixture;
- same-sequence/different-digest fixture;
- malformed/unknown sidecar fixture.

## 3. Marker non-persistence checks

Define exactly how the harness proves:

- marker file absent remains absent;
- existing marker bytes remain byte-identical;
- validation-only checks do not rewrite or create marker;
- sequence/root/live-trust mutation does not occur.

## 4. Evidence markers

Identify log/exit markers for:

- v2 accepted;
- v2 rejected for v1-after-v2;
- lower sequence rejected;
- same-sequence/different-digest rejected;
- malformed/unknown schema rejected;
- no normal node startup.

Avoid relying only on generic exit codes where possible.

---

# Required release-binary evidence scenarios

Produce logs, exit codes, marker before/after hashes, and a summary.

## Scenario group A — reload-check

### A1 — valid v2, no local marker

Prove:

- reload-check succeeds;
- marker remains absent;
- no sequence/root/live-trust mutation occurs.

### A2 — v2-after-v1 migration candidate

Seed v1 marker, run valid v2 candidate.

Prove:

- reload-check succeeds as validation-only migration candidate;
- v1 marker bytes remain unchanged;
- no marker persistence occurs.

### A3 — v1-after-v2 downgrade refused

Seed v2 marker, run v1 sidecar/candidate.

Prove:

- reload-check rejects;
- v2 marker bytes remain unchanged.

### A4 — lower v2 sequence refused

Seed v2 marker with higher sequence, run lower sequence v2 candidate.

Prove:

- reload-check rejects;
- marker bytes remain unchanged.

### A5 — same-sequence/same-digest idempotent

Seed v2 marker, run same v2 candidate.

Prove:

- reload-check succeeds;
- marker bytes remain unchanged.

### A6 — same-sequence/different-digest refused

Seed v2 marker, run same sequence but different digest.

Prove:

- reload-check rejects;
- marker bytes remain unchanged.

### A7 — malformed/unknown schema refused

Prove fail-closed behavior and no marker write.

## Scenario group B — local peer-candidate-check

Run the same core matrix as group A, at minimum:

### B1 — valid v2, no local marker

### B2 — v2-after-v1 migration candidate

### B3 — v1-after-v2 downgrade refused

### B4 — lower v2 sequence refused

### B5 — same-sequence/different-digest refused

### B6 — malformed/unknown schema refused

If full B matrix is too heavy, prioritize B1/B3/B4/B5/B6 and document B2 if omitted.

## Scenario group C — v1 regression

At least one v1 reload-check and one v1 peer-candidate-check should pass under the existing v1 path.

Prove v1 behavior still works.

---

# Required tests

Run targeted tests.

Add tests only if evidence reveals a gap or bug.

Minimum tests to run and report:

- Run 132 validation-only v2 tests;
- Run 131 v2 marker tests;
- Run 130 v2 verifier tests;
- Run 123 validation-only marker tests;
- Run 107 peer-candidate tests;
- Run 103 v1 ratification tests;
- Run 104 authority key material tests;
- qbind-ledger --lib;
- qbind-node --lib;
- qbind-crypto --lib if helper generation touched.

If any pre-existing test target has known compile/format issues, identify it clearly and prove it predates Run 133.

---

# Docs and evidence deliverables

Required:

- `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_133.md`;
- release-binary evidence archive under a deterministic directory, for example:
  - `docs/devnet/run_133_v2_validation_only_release_binary/`;
- release-binary harness under `scripts/devnet/` if added;
- update `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`;
- update `docs/whitepaper/contradiction.md`;
- update `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`.

Optional narrow docs:

- v2 validation-only release matrix;
- v1/v2 sidecar schema dispatch evidence note.

Do not perform broad documentation rewrite.

---

# contradiction.md update rules

Update `docs/whitepaper/contradiction.md` honestly.

Run 133 may strengthen evidence for:

- v2 validation-only release-binary behavior;
- v1/v2 compatibility policy;
- no-persistence behavior on validation-only paths;
- downgrade/replay refusal.

Run 133 may mark release-binary evidence for v2 validation-only surfaces narrowed/resolved only if:

- reload-check evidence covers v2 accepted/rejected cases;
- peer-candidate-check evidence covers v2 accepted/rejected cases;
- v1 behavior is release-evidenced;
- no marker persistence is proven;
- docs are synchronized.

Run 133 MUST NOT mark resolved:

- live inbound `0x05` v2 wiring unless actually implemented and evidenced;
- mutating-surface v2 enforcement;
- signing-key rotation/revocation lifecycle;
- KMS/HSM custody;
- MainNet governance artifact support;
- peer-driven live apply;
- validator-set rotation;
- full C4;
- C5.

The update must explicitly state:

- Run 133 does not wire v2 into mutating surfaces;
- Run 133 does not persist v2 marker state from validation-only checks;
- Run 133 does not implement signing-key rotation/revocation;
- Run 133 does not implement KMS/HSM custody;
- Run 133 does not implement peer-driven live apply;
- Run 133 does not implement MainNet governance artifacts;
- static production source-code anchors remain rejected;
- local config alone is still not enough for MainNet bundle-signing authority.

---

# Expected verdicts

Acceptable verdicts:

- strongest-positive;
- positive;
- partial-positive;
- negative.

Use **strongest-positive** only if:

- release-binary evidence covers reload-check and local peer-candidate-check core matrices;
- v1 regression is release-evidenced;
- no marker persistence is proven;
- docs are synchronized;
- no production runtime code changes were needed, or narrow bug fixes are fully proven.

Use **positive** if:

- release-binary evidence strongly covers both surfaces but one secondary scenario is omitted honestly;
- no-persistence proof is strong.

Use **partial-positive** if:

- only one surface has release-binary evidence;
- v2 rejection matrix is incomplete;
- no-persistence evidence is incomplete.

Use **negative** if:

- validation-only path persists marker;
- v1-after-v2 is accepted;
- lower sequence is accepted;
- same-sequence/different-digest is accepted;
- v1 behavior regresses;
- mutating surfaces are changed out of scope;
- local config alone can authorize MainNet authority.

---

# Required final response format

Your final response MUST contain:

## 1. Exact verdict

One of:

- strongest-positive;
- positive;
- partial-positive;
- negative.

## 2. What changed

Include:

- Run 132 doc-sync fix;
- scripts changed;
- helpers changed;
- docs changed;
- release-binary archive path;
- any runtime bug fixes, if any;
- explicit statement if no production runtime code changed.

## 3. What was proven

Separate:

- release-binary/process evidence;
- source-level evidence;
- test evidence.

For each scenario, state pass/fail/blocked.

## 4. What was not changed

Explicitly confirm:

- no v2 mutating-surface wiring;
- no v2 marker persistence from validation-only checks;
- no live `0x05` v2 wiring unless actually landed;
- no trust-bundle wire change;
- no peer-candidate wire change;
- no peer-driven apply;
- no KMS/HSM;
- no signing-key rotation/revocation;
- no MainNet governance artifact support;
- no full C4 closure;
- no C5 closure.

## 5. Contradictions or inconsistencies

Cross-check new evidence against:

- Run 100 authority model;
- Run 101 genesis authority implementation;
- Run 102 boot verification;
- Run 103 v1 verifier;
- Run 104 key material registry;
- Run 105–128 enforcement/evidence/reset lineage;
- Run 129 v2 schema spec;
- Run 130 v2 verifier implementation;
- Run 131 v2 marker implementation;
- Run 132 validation-only v2 wiring;
- contradiction.md;
- runbook;
- protocol docs.

If contradictions or inconsistencies are found:

- document them in `docs/whitepaper/contradiction.md`;
- explain them in final response.

Do not silently ignore contradictions.

## 6. Evidence references

List:

- evidence document;
- release-binary archive directory;
- summary file;
- per-scenario logs;
- marker before/after hashes;
- tests run;
- exact command/scenario summaries.

## 7. Residual risks and next recommended run

List remaining risks honestly.

Recommend the next run only after stating what Run 133 actually proved.

No hype.
No broad redesign.
No silent regressions.
No overclaiming.

