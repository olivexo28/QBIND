Task: Run 188 — Source/Test KMS-HSM Custody Boundary for Governance and Bundle-Signing Authority

Current accepted state:

* Run 185: release-binary OnChainGovernance fixture payload accepted-proof evidence landed.
* Run 186: source/test production OnChainGovernance verifier boundary landed:

  * fixture proofs remain DevNet/TestNet evidence-only;
  * production-class proof verification is unavailable/fail-closed;
  * fixture proof cannot satisfy MainNet production governance authority.
* Run 187: release-binary production OnChainGovernance verifier-boundary evidence landed:

  * release-built helper proves the Run 186 verifier-boundary corpus;
  * real `target/release/qbind-node` confirms MainNet peer-driven apply remains refused;
  * production-class OnChainGovernance proof verification is release-binary evidenced as unavailable/fail-closed;
  * fixture proof cannot satisfy MainNet production governance authority;
  * existing Run 185 fixture paths remain compatible.
* MainNet peer-driven apply remains refused.
* governance execution remains unimplemented.
* real on-chain proof verification remains unimplemented.
* KMS/HSM remains unimplemented.
* validator-set rotation remains open.
* full C4 remains open.
* C5 remains open.

Objective:
Add a source/test-only KMS/HSM custody boundary for bundle-signing authority and governance authority operations.

This run must define typed custody interfaces, policies, and fail-closed behavior for future production signing-key custody, without implementing a real KMS/HSM backend and without enabling MainNet apply.

The goal is to prevent local raw-key fixtures, local operator config, or helper-generated signing material from being mistaken for production MainNet custody.

Release-binary KMS/HSM boundary evidence is deferred to Run 189.

Strict scope:

* Source/test only.
* No release-binary harness in this run.
* No real KMS implementation.
* No real HSM implementation.
* No cloud KMS integration.
* No PKCS#11 integration.
* No remote signer production integration.
* No MainNet peer-driven apply enablement.
* No real governance execution engine.
* No real on-chain proof verifier.
* No validator-set rotation.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* No marker schema change.
* No sequence-file schema change.
* No trust-bundle core schema change unless a hard blocker is found and documented.
* No wire/schema change unless strictly additive and required for typed custody metadata.
* Do not weaken Runs 070, 130–187.
* Do not claim full C4 or C5 closure.

Required design:
Add a typed custody boundary module, preferably:

`crates/qbind-node/src/pqc_authority_custody.rs`

or similar.

The module must distinguish at least:

1. `FixtureLocalKey`

* DevNet/TestNet source/test only.
* May be accepted only under explicit fixture policy.
* Must not satisfy MainNet production custody.

2. `LocalOperatorKey`

* explicit local operator key material.
* DevNet/TestNet may use it where already allowed.
* Must not satisfy MainNet production custody.

3. `RemoteSigner`

* placeholder / future boundary.
* Must be fail-closed unless a real authenticated remote signer protocol is implemented.
* Must include identity binding / attestation placeholder fields.

4. `Kms`

* placeholder / future boundary.
* Must be fail-closed.
* Must include provider/key-id/attestation placeholder fields.

5. `Hsm`

* placeholder / future boundary.
* Must be fail-closed.
* Must include module-id/key-label/attestation placeholder fields.

6. `Unknown` / unsupported custody class

* fail-closed.

Required typed policy:
Define a custody policy enum such as:

* `FixtureOnly`
* `DevnetLocalAllowed`
* `TestnetLocalAllowed`
* `ProductionCustodyRequired`
* `MainnetProductionCustodyRequired`

or equivalent.

Policy behavior:

* DevNet:

  * fixture/local custody may be allowed only under explicit test/dev policy.
* TestNet:

  * fixture/local custody may be allowed only under explicit test policy.
* MainNet:

  * fixture/local/operator keys must be rejected as production custody.
  * KMS/HSM/remote signer placeholders must remain unavailable/fail-closed until real implementations land.
  * MainNet peer-driven apply remains refused regardless of custody outcome.

Required custody validation:
Add a pure validator such as:

`validate_authority_custody_attestation(...)`

or equivalent.

It must bind custody metadata to:

* environment;
* chain_id;
* genesis_hash;
* authority_root_fingerprint;
* bundle-signing key fingerprint;
* governance authority class;
* lifecycle action;
* candidate digest;
* authority-domain sequence;
* custody class;
* custody key identifier;
* custody attestation digest or placeholder commitment;
* freshness / expiry if represented.

Required outcome surface:
Typed outcomes must distinguish at least:

* fixture custody accepted;
* local operator custody accepted under DevNet/TestNet policy;
* production custody unavailable;
* KMS unavailable;
* HSM unavailable;
* remote signer unavailable;
* unknown custody class rejected;
* wrong environment;
* wrong chain;
* wrong genesis;
* wrong authority root;
* wrong signing-key fingerprint;
* wrong candidate digest;
* wrong authority-domain sequence;
* wrong lifecycle action;
* custody attestation missing;
* custody attestation malformed;
* custody attestation expired;
* custody key id mismatch;
* unsupported custody suite;
* fixture custody rejected for MainNet;
* local custody rejected for MainNet;
* MainNet production custody unavailable.

Names can differ, but outcomes must be typed and testable.

Required integration boundary:
Do not wire custody validation into mutating apply surfaces yet unless doing so remains pure/preflight and source/test only.

Add a pure composition helper such as:

`validate_governance_authority_with_custody(...)`

or:

`validate_lifecycle_governance_and_custody(...)`

This helper must:

* call existing lifecycle validation where appropriate;
* call governance authority / OnChainGovernance verifier where appropriate;
* call custody validator;
* return a typed combined decision;
* perform no I/O;
* write no marker;
* write no sequence;
* mutate no live trust;
* evict no sessions;
* call no Run 070 apply.

Required accepted scenarios:
A1. DevNet fixture custody accepted under explicit fixture policy.

A2. TestNet fixture custody accepted under explicit fixture policy.

A3. DevNet local-operator custody accepted under explicit DevNet local policy.

A4. TestNet local-operator custody accepted under explicit TestNet local policy.

A5. existing GenesisBound / EmergencyCouncil governance proof behavior remains unchanged when custody validation is not required.

A6. combined lifecycle + governance + fixture custody accepted for DevNet source/test.

A7. combined lifecycle + governance + local custody accepted for TestNet source/test, if policy allows.

A8. production custody boundary callable and returns typed unavailable outcome for KMS/HSM/remote signer placeholders.

Required rejection scenarios:
R1. fixture custody rejected under production custody policy.

R2. local operator custody rejected under production custody policy.

R3. fixture custody rejected for MainNet.

R4. local operator custody rejected for MainNet.

R5. KMS placeholder rejected as unavailable.

R6. HSM placeholder rejected as unavailable.

R7. remote signer placeholder rejected as unavailable.

R8. unknown custody class rejected.

R9. wrong environment rejected.

R10. wrong chain rejected.

R11. wrong genesis rejected.

R12. wrong authority root rejected.

R13. wrong signing-key fingerprint rejected.

R14. wrong candidate digest rejected.

R15. wrong authority-domain sequence rejected.

R16. wrong lifecycle action rejected.

R17. missing custody attestation rejected.

R18. malformed custody attestation rejected.

R19. expired custody attestation rejected.

R20. custody key id mismatch rejected.

R21. unsupported custody suite rejected.

R22. custody valid but governance proof invalid rejected.

R23. governance proof valid but custody invalid rejected.

R24. lifecycle valid + governance valid + custody placeholder unavailable rejected.

R25. MainNet peer-driven apply remains refused even if custody metadata claims KMS/HSM.

R26. local operator config alone cannot satisfy MainNet production custody.

R27. peer majority / gossip count cannot satisfy custody.

R28. validation-only rejection remains non-mutating.

R29. mutating preflight rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

Required tests:
Add focused tests, preferably:

`crates/qbind-node/tests/run_188_authority_custody_boundary_tests.rs`

Minimum coverage:

* A1–A8.
* R1–R29.
* pure no-I/O guarantee.
* fixture-vs-production custody separation.
* MainNet fixture/local custody refusal.
* KMS/HSM/remote signer placeholders fail-closed.
* combined lifecycle + governance + custody helper remains pure.
* existing OnChainGovernance fixture paths remain compatible.
* GenesisBound and EmergencyCouncil behavior unchanged.
* MainNet peer-driven apply remains refused.

Required documentation:
Add:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_188.md`

Update:

* `docs/whitepaper/contradiction.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

Documentation must state:

* Run 188 is source/test KMS-HSM custody-boundary work.
* No real KMS/HSM backend is implemented.
* KMS/HSM/remote signer placeholders are fail-closed.
* Fixture/local custody remains DevNet/TestNet evidence-only.
* Fixture/local custody cannot satisfy MainNet production custody.
* MainNet peer-driven apply remains refused.
* governance execution remains unimplemented.
* real on-chain proof verification remains unimplemented.
* validator-set rotation remains open.
* release-binary custody-boundary evidence is deferred to Run 189.
* full C4 remains open.
* C5 remains open.

Validation commands:
Run at minimum:

* `cargo build -p qbind-node --lib`
* `cargo test -p qbind-node --test run_188_authority_custody_boundary_tests`
* `cargo test -p qbind-node --test run_186_onchain_governance_production_verifier_boundary_tests`
* `cargo test -p qbind-node --test run_184_onchain_governance_payload_carrying_tests`
* `cargo test -p qbind-node --test run_182_onchain_governance_production_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_180_onchain_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_178_onchain_governance_proof_tests`
* `cargo test -p qbind-node --test run_176_live_0x05_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_173_validation_only_governance_required_policy_tests`
* `cargo test -p qbind-node --test run_171_governance_required_policy_selector_tests`
* `cargo test -p qbind-node --test run_169_governance_proof_loader_surface_integration_tests`
* `cargo test -p qbind-node --test run_167_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_165_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_163_governance_authority_verifier_tests`
* `cargo test -p qbind-node --test run_161_lifecycle_marker_integration_tests`
* `cargo test -p qbind-node --test run_159_authority_signing_key_lifecycle_tests`
* `cargo test -p qbind-node --test run_157_unified_testnet_fixture_universe_tests`
* `cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests`
* `cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests`
* `cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests`
* `cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests`
* `cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests`
* `cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Acceptance criteria:
Run 188 is acceptable only if:

1. A typed authority custody boundary exists.
2. Fixture/local custody is explicitly DevNet/TestNet evidence-only.
3. Fixture/local custody cannot satisfy MainNet production custody.
4. KMS/HSM/remote signer placeholders fail closed as unavailable.
5. Combined lifecycle + governance + custody decision remains pure and non-mutating.
6. Existing governance proof paths remain compatible.
7. MainNet peer-driven apply remains refused.
8. Docs defer release-binary custody-boundary evidence to Run 189.
9. No real KMS/HSM / governance execution / real on-chain proof / validator-set rotation claim is made.
10. No full C4 or C5 closure is claimed.
