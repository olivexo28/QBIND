Continue QBIND from the current state.

Project:
- QBIND post-quantum L1
- security-first, production-grade, execution-first
- C4 is the main live operability tracker
- C5 tracks timeout-verification activation, forged-traffic rejection, and transport-root dependency
- no broad redesign
- no silent regressions
- no overclaiming

Current state:
- Run 078 added typed/versioned/bounded peer-candidate wire envelope for frame discriminator 0x05.
- Run 079 wired disabled-by-default live receive-loop dispatch for 0x05, validation-only.
- Run 080 added disabled-by-default send-side publisher plumbing for 0x05.
- Run 081 produced release-binary N=2 real 0x05 exchange evidence:
  - baseline N=2 startup: PASS
  - valid send/validate: PASS
  - receiver-disabled cheap-ignore: PASS
  - invalid candidate reject: PASS
  - duplicate suppression: PASS
  - receiver sequence file hash unchanged
  - live reload apply metrics unchanged
  - session eviction metrics unchanged
  - sessions remained healthy
- Run 081 verdict remained PARTIAL POSITIVE because strict “no DummySig” closure was not achieved.
- Run 082 investigated the TrustedClientRoots/DummySig boundary:
  - classified the reference as probe/log-only stale narrative text inside TimeoutVerificationDisabledReason::ProductionPiecesUnavailable
  - found no active production DummySig registration on the pqc-static-root signed-bundle command shape
  - traced the production-honest path to real ML-DSA-44, real ML-KEM-768, and ChaCha20-Poly1305
  - did NOT rerun the full Run 081 release-binary N=2 0x05 matrix
- Run 082 verdict remained PARTIAL POSITIVE because live evidence rerun is still required.

Task:
Run 083. Evidence-only rerun of the Run 081 release-binary N=2 real 0x05 matrix, explicitly capturing the Run 033 probe line and proving the no-Dummy boundary.

Primary objective:
Upgrade the Run 081/082 boundary by rerunning the same release-binary N=2 0x05 matrix and proving:
- valid 0x05 send/validate still passes
- receiver-disabled cheap-ignore still passes
- invalid candidate still rejects
- duplicate suppression still works
- sequence/apply/eviction invariants remain unchanged
- no active DummySig / DummyKem / DummyAead exists on the production-honest path
- the Run 033 probe line does not indicate active DummySig usage

Important framing:
This is evidence-only unless a real bug appears.
Do not add new peer-candidate features.
Do not implement propagation.
Do not implement peer-driven live apply.
Do not implement activation_epoch.
Do not implement KMS/HSM.
Do not implement signing-key ratification.
Do not implement fast-sync restore.
Do not redesign KEMTLS.
Do not redesign consensus.
Do not weaken Run 081 validation-only 0x05 behavior.
Do not hide an actual active DummySig path.
Do not claim full C4 or C5 closure.

Required investigation before rerun:

1. Confirm current Dummy boundary from Run 082
   Reconfirm with repo search:
   - DummySig
   - DummyKem
   - DummyAead
   - TrustedClientRoots
   - Run033
   - Run 033
   - dummy_sig_registered
   - dummy_kem_registered
   - dummy_aead_registered
   - ProductionPiecesUnavailable

   Required classification:
   - test-only
   - doc/evidence-only
   - devnet-helper-only
   - probe/log-only
   - production binary reachable
   - production runtime active

2. Confirm production-honest Run 081 command shape
   For the exact release-binary N=2 commands used in Run 081, verify:
   - --p2p-pqc-root-mode pqc-static-root
   - --p2p-trust-bundle present
   - --p2p-trust-bundle-signing-key present
   - real leaf certs and ML-KEM material present
   - no --p2p-trusted-root fallback
   - no test crypto provider selected
   - make_test_crypto_provider path unreachable
   - ML-DSA-44 cert verifier selected
   - ML-KEM-768 selected
   - ChaCha20-Poly1305 selected

3. Capture required probe lines
   In the release-binary logs, capture:
   - [Run040] line showing:
     - transport signature suite / KEM / AEAD are real
     - dummy_kem_registered=false
     - dummy_aead_registered=false
   - [Run033] timeout-verification probe line showing:
     - the actual disabled reason for this command shape
     - reason should be SignerPresentKeyProviderUnavailable or equivalent non-Dummy reason
     - reason should NOT be ProductionPiecesUnavailable / TrustedClientRoots/DummySig on this path

   If the [Run033] line still contains TrustedClientRoots/DummySig:
   - do not claim strongest positive
   - record exact line and explain whether it is stale probe text or active path
   - do not hide it

Required release-binary matrix:

1. Build and identity capture
   Run:
   - cargo build --release -p qbind-node --bin qbind-node
   - cargo build --release -p qbind-node --example devnet_pqc_trust_bundle_helper
   - cargo build --release -p qbind-node --example devnet_pqc_root_helper

   Record:
   - branch
   - commit
   - clean/dirty status
   - qbind-node sha256
   - qbind-node ELF BuildID
   - helper sha256 / BuildID

2. Generate N=2 material
   Generate:
   - signed trust bundle
   - two leaf certs
   - two ML-KEM secret files
   - peer leaf mappings
   - valid candidate envelope
   - invalid/tampered or wrong-chain candidate envelope
   - duplicate candidate case

   Required:
   - no private material in logs/artifacts
   - no --p2p-trusted-root fallback

3. Baseline N=2 startup
   Prove:
   - both nodes start
   - real PQC mutual-auth works
   - cert accepted counters move
   - cert rejected counters remain zero
   - sequence file written normally
   - Run040 no DummyKem/DummyAead
   - Run033 no active DummySig boundary
   - no fallback

4. Valid 0x05 send/validate
   Sender:
   - --p2p-trust-bundle-peer-candidate-wire-publish-enabled
   - --p2p-trust-bundle-peer-candidate-wire-publish-path <valid envelope>
   - --p2p-trust-bundle-peer-candidate-wire-publish-once

   Receiver:
   - --p2p-trust-bundle-peer-candidate-wire-validation-enabled

   Prove:
   - sender sent_total increments
   - receiver received_total increments
   - receiver validated_total increments
   - logs say validation-only / not-applied
   - sequence file hash unchanged after candidate validation
   - live reload apply metrics remain zero
   - session eviction metrics remain zero
   - sessions remain healthy
   - no propagation/rebroadcast
   - no DummySig / DummyKem / DummyAead active
   - no fallback

5. Receiver-disabled cheap-ignore
   Receiver does not enable wire validation.
   Prove:
   - sender sends valid 0x05 frame
   - receiver does not validate
   - disabled/cheap-ignore behavior captured
   - connection remains healthy
   - no sequence/live-trust/session mutation
   - no fallback
   - no Dummy crypto active

6. Invalid candidate reject
   Send tampered or wrong-chain candidate.
   Prove:
   - receiver rejected_total increments
   - sequence unchanged
   - live reload apply metrics remain zero
   - session eviction metrics remain zero
   - sessions remain healthy
   - no fallback
   - no Dummy crypto active

7. Duplicate suppression
   Send same candidate twice or reproduce the Run 081 duplicate case.
   Prove:
   - duplicate_total increments
   - duplicate does not repeat expensive validation if that is the design
   - sequence unchanged
   - live reload apply metrics remain zero
   - session eviction metrics remain zero

Required regression commands:
- cargo test -p qbind-node --test run_080_pqc_peer_candidate_wire_send_tests
- cargo test -p qbind-node --test run_079_pqc_peer_candidate_wire_live_dispatch_tests
- cargo test -p qbind-node --test run_078_pqc_peer_candidate_wire_tests
- cargo test -p qbind-node --test run_077_binary_peer_candidate_check_tests
- cargo test -p qbind-node --test run_076_pqc_peer_candidate_validation_tests
- cargo test -p qbind-node --lib pqc_peer_candidate_wire
- cargo test -p qbind-node --lib metrics::tests::peer_candidate_send_metrics
- cargo test -p qbind-node --lib
- cargo test -p qbind-net --lib
- cargo test -p qbind-crypto --lib

If source changes are made:
- rerun all affected unit/integration tests
- explain exactly why the source change was necessary

Evidence document:
Create:
- docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_083.md

Also update:
- docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_081.md with a Run 083 follow-up note, if project convention permits
- docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_082.md with a Run 083 follow-up note, if project convention permits
- docs/whitepaper/contradiction.md

Evidence doc must include:
- exact objective
- exact verdict
- exact files changed
- exact commands run
- tests/evidence pass/fail status
- binary/helper identities
- DummySig / DummyKem / DummyAead reference inventory summary
- production-path trace for the Run 081/083 command shape
- captured Run040 line
- captured Run033 line
- baseline N=2 startup evidence
- valid 0x05 send/validate evidence
- receiver-disabled evidence
- invalid candidate evidence
- duplicate evidence
- sequence file before/after hashes
- proof live reload apply metrics unchanged
- proof session eviction metrics unchanged
- proof sessions remained healthy
- proof no active DummySig / DummyKem / DummyAead
- proof no --p2p-trusted-root fallback
- exact remaining C4 boundaries
- immediate next action

contradiction.md update rules:
- If the Run 083 matrix passes and the Run033/Run040 logs prove no active DummySig/DummyKem/DummyAead on the production-honest path, update C4 to record strongest-positive closure of the Run 081/082 no-Dummy boundary for the 0x05 validation-only release-binary evidence path.
- If the matrix passes but Run033 still contains TrustedClientRoots/DummySig, keep partial-positive and state exact boundary.
- If production-active DummySig is found, record contradiction/regression and do not claim closure.
- Keep C4 open for:
  - peer-driven live apply / propagation
  - activation_epoch runtime source
  - KMS/HSM custody
  - in-binary/on-chain signing-key ratification
  - production fast-sync / consensus-storage restore
  - per-environment production trust-anchor operation
- Do not close full C4.
- Do not claim C5 closure unless specifically justified.

Expected verdict:
- Strongest positive:
  Run 081 N=2 real 0x05 matrix rerun passes; captured Run033/Run040 logs prove no active DummySig/DummyKem/DummyAead; validation-only invariants preserved.
- Partial positive:
  matrix passes but no-Dummy proof remains incomplete.
- Negative:
  production-active DummySig remains reachable, validation-only invariants regress, or tests fail.

Required final response:
1. Exact verdict
2. Exact files changed
3. Exact commands run
4. Tests/evidence run and pass/fail status
5. What was found
6. What was proven
7. What remains not solved
8. Whether contradiction.md was updated and why
9. Exact immediate next action recommended