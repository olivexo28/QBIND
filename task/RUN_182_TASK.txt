Task: Run 182 — Source/Test Wire OnChainGovernance Wrappers into Production v2 Marker-Decision Call Sites

Current accepted state:

* Run 178: source/test typed OnChainGovernance proof verifier landed.
* Run 179: release-binary boundary evidence showed the OnChainGovernance verifier worked through a release-built helper but had no production callers.
* Run 180: source/test OnChainGovernance surface module landed:

  * `pqc_onchain_governance_proof_surface.rs`;
  * hidden CLI flag `--p2p-trust-bundle-onchain-governance-fixture-allowed`;
  * env var `QBIND_P2P_TRUST_BUNDLE_ONCHAIN_GOVERNANCE_FIXTURE_ALLOWED`;
  * default remains `OnChainGovernanceProofPolicy::Disabled`;
  * seven per-surface wrappers for reload-check, reload-apply, startup, SIGHUP, peer-candidate-check, live 0x05, and peer-driven drain.
* Run 181: release-binary selector/boundary evidence landed:

  * real `target/release/qbind-node` proves default Disabled remains silent/fail-closed;
  * CLI selector and env selector arm `AllowFixtureSourceTest`;
  * release-built helper exercises the Run 180 wrappers and Run 178/180 corpus;
  * MainNet refusal remains intact.
* Run 181 limitation:

  * the real production call sites are not yet wired to invoke the Run 180 wrappers;
  * wrapper exercise remains helper/in-process evidence;
  * production-surface accepted OnChainGovernance proof evidence remains open.
* MainNet peer-driven apply remains refused.
* governance execution remains unimplemented.
* real on-chain proof verification remains unimplemented.
* KMS/HSM remains unimplemented.
* validator-set rotation remains open.
* full C4 remains open.
* C5 remains open.

Objective:
Wire the Run 180 OnChainGovernance per-surface wrappers into the real production v2 marker-decision call sites at source/test level.

This run must make OnChainGovernance fixture proof verification production-source reachable through actual production call sites, while preserving Disabled as the default and preserving MainNet refusal.

Release-binary production-surface evidence is deferred to Run 183.

Strict scope:

* Source/test only.
* No release-binary harness in this run.
* Hidden DevNet/TestNet fixture policy only.
* Default remains `OnChainGovernanceProofPolicy::Disabled`.
* No MainNet peer-driven apply enablement.
* No real governance execution engine.
* No real on-chain proof verifier.
* No bridge/light-client integration.
* No KMS/HSM implementation.
* No validator-set rotation.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* No marker schema change.
* No sequence-file schema change.
* No trust-bundle core schema change.
* No additional wire/schema change unless a hard blocker is found and documented.
* Do not weaken Runs 070, 130–181.
* Do not claim full C4 or C5 closure.

Required production call-site wiring:
Wire the Run 180 wrappers into the actual production call paths where governance proof context already flows after Runs 169–177.

At minimum cover:

1. reload-check validation-only;
2. reload-apply mutating preflight;
3. startup `--p2p-trust-bundle` preflight;
4. SIGHUP live-reload preflight;
5. local `--p2p-trust-bundle-peer-candidate-check`;
6. live inbound `0x05` validation-only;
7. peer-driven drain / `ProductionV2MarkerCoordinator`.

If any call site cannot carry OnChainGovernance proof context without new schema/wire work, document the exact blocker and keep it fail-closed.

Required selector policy:
Use the Run 180 selector only:

* CLI: `--p2p-trust-bundle-onchain-governance-fixture-allowed`;
* env: `QBIND_P2P_TRUST_BUNDLE_ONCHAIN_GOVERNANCE_FIXTURE_ALLOWED`;
* truthy values as already defined.

Do not create a second selector path.

Required behavior:

* Default `Disabled`:

  * OnChainGovernance proof remains rejected/fail-closed.
  * Existing GenesisBound and EmergencyCouncil behavior remains unchanged.
* `AllowFixtureSourceTest`:

  * DevNet/TestNet fixture OnChainGovernance proof may pass only if lifecycle, anti-rollback, domain, proposal, quorum/threshold, freshness, replay, and digest checks all pass.
  * Invalid fixture proofs fail closed.
  * This remains fixture-only and must not be described as real on-chain governance.
* MainNet:

  * peer-driven apply remains refused even with valid OnChainGovernance fixture proof and selector enabled.
  * production MainNet OnChainGovernance remains unsupported/fail-closed.

Required tests:
Add focused tests, preferably:

`crates/qbind-node/tests/run_182_onchain_governance_production_callsite_wiring_tests.rs`

Minimum acceptance matrix:

A1. reload-check default Disabled rejects OnChainGovernance proof

* validation-only;
* no marker write;
* no sequence write.

A2. reload-check AllowFixtureSourceTest accepts valid DevNet OnChainGovernance Rotate proof

* validation-only;
* no marker write;
* no sequence write.

A3. reload-apply AllowFixtureSourceTest accepts valid DevNet OnChainGovernance Rotate proof

* preflight accepts;
* if full apply is exercised, sequence-before-marker ordering is preserved.

A4. startup preflight AllowFixtureSourceTest accepts valid fixture proof.

A5. SIGHUP preflight AllowFixtureSourceTest accepts valid fixture proof.

A6. local peer-candidate-check AllowFixtureSourceTest accepts valid fixture proof.

A7. live inbound 0x05 AllowFixtureSourceTest accepts valid fixture proof if the proof context is available from Run 176.

A8. peer-driven drain coordinator AllowFixtureSourceTest accepts valid fixture proof if proof context is available.

A9. GenesisBound and EmergencyCouncil proof behavior remains unchanged.

Required rejection matrix:

R1. OnChainGovernance proof rejected under default Disabled policy.

R2. selector unset / false preserves Disabled.

R3. MainNet peer-driven apply refused even with AllowFixtureSourceTest and valid fixture proof.

R4. wrong environment rejected.

R5. wrong chain rejected.

R6. wrong genesis rejected.

R7. wrong authority root rejected.

R8. wrong governance domain rejected.

R9. wrong proposal digest rejected.

R10. wrong proposal outcome rejected.

R11. wrong lifecycle action rejected.

R12. wrong candidate digest rejected.

R13. wrong authority-domain sequence rejected.

R14. expired governance proof rejected.

R15. replayed governance decision rejected.

R16. quorum not met rejected.

R17. threshold not met rejected.

R18. invalid proof bytes rejected.

R19. unsupported proof suite rejected.

R20. malformed proof rejected.

R21. local operator config rejected.

R22. peer-majority / gossip count rejected.

R23. OnChainGovernance proof valid but lifecycle invalid rejected.

R24. lifecycle valid but OnChainGovernance proof invalid rejected.

R25. validation-only rejection remains non-mutating.

R26. mutating preflight rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

R27. invalid live 0x05 OnChainGovernance proof candidate is not propagated, staged, or applied.

Required source-reachability proof:
Tests and docs must show production `src` call sites outside helper/example code for:

* `verify_onchain_governance_proof` or `validate_lifecycle_with_onchain_governance_proof`;
* `OnChainGovernanceProofPolicy::AllowFixtureSourceTest`;
* Run 180 per-surface wrappers;
* selector helpers;
* real production call-site integration.

Required backwards compatibility:
All prior tests must remain green:

* Run 180 tests.
* Run 178 tests.
* Run 176 live 0x05 governance proof carrier tests.
* Run 173 validation-only Required-policy tests.
* Run 171 selector tests.
* Run 169 loader surface tests.
* Run 167 proof carrier tests.
* Run 165 governance marker tests.
* Run 163 governance verifier tests.
* Run 161 lifecycle marker tests.
* Run 159 lifecycle tests.
* Run 134/138/142/148/150/152 regression suites.

Required documentation:
Add:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_182.md`

Update:

* `docs/whitepaper/contradiction.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

Documentation must state:

* Run 182 is source/test production call-site wiring for OnChainGovernance fixture proofs.
* Default remains `OnChainGovernanceProofPolicy::Disabled`.
* `AllowFixtureSourceTest` is hidden, explicit, and DevNet/TestNet fixture-only.
* Real on-chain governance proof verification remains unimplemented.
* Governance execution remains unimplemented.
* Production MainNet OnChainGovernance remains unsupported/fail-closed.
* MainNet peer-driven apply remains refused.
* KMS/HSM remains unimplemented.
* validator-set rotation remains open.
* release-binary production-surface OnChainGovernance evidence is deferred to Run 183.
* full C4 remains open.
* C5 remains open.

Validation commands:
Run at minimum:

* `cargo build -p qbind-node --lib`
* `cargo test -p qbind-node --test run_182_onchain_governance_production_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_180_onchain_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_178_onchain_governance_proof_tests`
* `cargo test -p qbind-node --test run_176_live_0x05_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_173_validation_only_governance_required_policy_tests`
* `cargo test -p qbind-node --test run_171_governance_required_policy_selector_tests`
* `cargo test -p qbind-node --test run_169_governance_proof_loader_surface_integration_tests`
* `cargo test -p qbind-node --test run_167_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_165_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_163_governance_authority_verifier_tests`
* `cargo test -p qbind-node --test run_161_lifecycle_marker_integration_tests`
* `cargo test -p qbind-node --test run_159_authority_signing_key_lifecycle_tests`
* `cargo test -p qbind-node --test run_157_unified_testnet_fixture_universe_tests`
* `cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests`
* `cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests`
* `cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests`
* `cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests`
* `cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests`
* `cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Acceptance criteria:
Run 182 is acceptable only if:

1. OnChainGovernance verifier is reachable from production source call sites outside helpers/examples.
2. Default policy remains Disabled and fail-closed.
3. Hidden AllowFixtureSourceTest policy reaches production marker-decision call sites.
4. DevNet/TestNet fixture OnChainGovernance proofs can pass at source/test marker-decision level through production call-site wiring.
5. Invalid fixture proofs fail closed.
6. GenesisBound and EmergencyCouncil behavior remains unchanged.
7. Validation-only surfaces remain non-mutating.
8. Mutating surfaces preserve sequence-before-marker ordering.
9. MainNet peer-driven apply remains refused even with valid fixture OnChainGovernance proof.
10. Docs defer release-binary production-surface evidence to Run 183.
11. No full C4 or C5 closure is claimed.
