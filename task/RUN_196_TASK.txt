Task: Run 196 — Source/Test RemoteSigner Attestation Payload Carrying and Custody Composition Wiring

Current accepted state:

* Run 188: source/test authority-custody boundary landed.
* Run 190: source/test custody metadata carrying and production-context routing landed.
* Run 192: source/test hidden authority-custody policy selector landed.
* Run 193: release-binary authority-custody policy selector evidence fully closed.
* Run 194: source/test RemoteSigner production-custody interface boundary landed:

  * `pqc_remote_authority_signer.rs`;
  * `RemoteSignerPolicy::{Disabled, FixtureLoopbackAllowed, ProductionRemoteSignerRequired, MainnetProductionRemoteSignerRequired}`;
  * `RemoteSignerIdentity`;
  * `RemoteSignerRequest`;
  * `RemoteSignerResponse`;
  * deterministic domain-separated SHA3-256 canonical digest;
  * `RemoteAuthoritySigner` trait;
  * DevNet/TestNet-only `FixtureLoopbackRemoteSigner`;
  * fail-closed `ProductionRemoteSigner`;
  * `validate_remote_signer`;
  * `validate_remote_signer_for_custody_class`;
  * `validate_lifecycle_governance_custody_and_remote_signer`;
  * named MainNet/local-operator/peer-majority refusal helpers.
* Run 195: release-binary RemoteSigner boundary evidence fully closed:

  * real `target/release/qbind-node` exposes no production RemoteSigner;
  * no RemoteSigner/KMS/HSM enablement banner;
  * helper proves Run 194 A1–A7 / R1–R31 corpus;
  * RemoteSigner request/response binding is deterministic and domain-bound;
  * fixture loopback RemoteSigner remains DevNet/TestNet evidence-only;
  * production RemoteSigner remains unavailable/fail-closed;
  * local operator keys and peer-majority/gossip cannot satisfy RemoteSigner policy;
  * MainNet peer-driven apply remains refused.
* Current limitation:
  RemoteSigner request/response material is not yet carried through production payload/context paths and is not yet routed into the Run 190/192 custody preflight composition.
* No real RemoteSigner backend exists.
* No real KMS/HSM/cloud-KMS/PKCS#11 exists.
* governance execution remains unimplemented.
* real on-chain proof verification remains unimplemented.
* validator-set rotation remains open.
* full C4 remains open.
* C5 remains open.

Objective:
Add source/test support for carrying RemoteSigner identity/request/response attestation material through production payload/context paths and routing it into lifecycle + governance + custody preflight composition.

This run must make RemoteSigner material available to production source call-site contexts at source/test level, while preserving disabled/fail-closed defaults and MainNet refusal.

Release-binary RemoteSigner payload/carrying evidence is deferred to Run 197.

Strict scope:

* Source/test only.
* No release-binary harness in this run.
* No real RemoteSigner backend.
* No networked signer service.
* No real KMS implementation.
* No real HSM implementation.
* No cloud KMS integration.
* No PKCS#11 integration.
* No MainNet peer-driven apply enablement.
* No governance execution engine.
* No real on-chain proof verifier.
* No validator-set rotation.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* Prefer additive optional payload/context fields.
* Preserve legacy/no-RemoteSigner payload compatibility.
* No authority-marker schema change.
* No sequence-file schema change.
* No trust-bundle core schema change unless a hard blocker is found and documented.
* Do not weaken Runs 070, 130–195.
* Do not claim full C4 or C5 closure.

Required payload/carrying design:
Add additive, optional RemoteSigner material to the production payload/context layer used by v2 authority marker-decision surfaces.

Preferred shape:

* Add optional JSON sibling(s) beside v2 ratification sidecar material, such as:

  * `remote_signer_identity`
  * `remote_signer_request`
  * `remote_signer_response`
* Or one combined optional sibling:

  * `remote_signer_attestation`
* Follow the Run 167 / Run 184 / Run 190 sibling-extraction pattern:

  * extract optional RemoteSigner sibling before strict v2 sidecar parse;
  * old no-RemoteSigner sidecars remain compatible;
  * malformed RemoteSigner sibling fails closed;
  * absent RemoteSigner sibling maps to typed absent/unavailable status.

Required wire/context representation:
Define wire types such as:

* `RemoteSignerIdentityWire`
* `RemoteSignerRequestWire`
* `RemoteSignerResponseWire`
* `RemoteSignerAttestationWire`
* `RemoteSignerLoadStatus::{Absent, Available, Malformed}`

Names may differ, but the representation must cover:

* signer identity;
* signer public identity;
* custody key id;
* authority root fingerprint;
* bundle-signing key fingerprint;
* environment;
* chain_id;
* genesis_hash;
* supported suite id;
* supported lifecycle actions;
* attestation digest / placeholder commitment;
* freshness / expiry;
* request digest or request body;
* response digest / response nonce;
* signature suite;
* placeholder signature commitment;
* expiry / freshness.

Required source behavior:

1. Convert wire RemoteSigner material into internal Run 194 types:

   * `RemoteSignerIdentity`;
   * `RemoteSignerRequest`;
   * `RemoteSignerResponse`;
   * expectations / policy context.

2. Route RemoteSigner material into:

   * `validate_remote_signer`;
   * `validate_remote_signer_for_custody_class`;
   * `validate_lifecycle_governance_custody_and_remote_signer`.

3. Integrate with Run 190 / Run 192 custody policy context:

   * if custody class is `RemoteSigner`, RemoteSigner material must be required under RemoteSigner policy;
   * absent RemoteSigner material fails closed when RemoteSigner is required;
   * malformed material fails closed before mutation.

4. Preserve compatibility:

   * no RemoteSigner payload remains accepted under default Disabled where previously accepted;
   * existing fixture/local custody paths remain compatible;
   * KMS/HSM placeholder paths remain fail-closed.

5. Surface coverage at source/test level:
   Route RemoteSigner payload context through the seven production-context helpers where representable:

   * reload-check;
   * reload-apply;
   * startup `--p2p-trust-bundle`;
   * SIGHUP;
   * local peer-candidate-check;
   * live inbound `0x05`;
   * peer-driven drain.

6. No mutation:

   * no marker write;
   * no sequence write;
   * no live trust swap;
   * no session eviction;
   * no Run 070 call.

Required accepted scenarios:
A1. no-RemoteSigner payload remains compatible under default Disabled.

A2. DevNet fixture loopback RemoteSigner material carried through reload-check context and accepted under explicit fixture policy.

A3. TestNet fixture loopback RemoteSigner material carried through reload-check context and accepted under explicit fixture policy.

A4. DevNet fixture loopback RemoteSigner material carried through reload-apply preflight and accepted under explicit fixture policy.

A5. RemoteSigner request/response canonical digest is preserved through wire conversion.

A6. custody class `RemoteSigner` routes to RemoteSigner boundary when RemoteSigner material is present.

A7. combined lifecycle + governance + custody + fixture RemoteSigner accepted for DevNet source/test production-context path.

A8. GenesisBound / EmergencyCouncil / OnChainGovernance proof behavior remains unchanged when RemoteSigner policy is Disabled.

A9. KMS/HSM/fixture/local custody paths remain compatible with RemoteSigner payload absent.

A10. production RemoteSigner material reaches boundary and returns typed unavailable outcome under production-required policy.

Required rejection scenarios:
R1. RemoteSigner material absent where RemoteSigner policy requires it rejected fail-closed.

R2. malformed RemoteSigner identity rejected.

R3. malformed RemoteSigner request rejected.

R4. malformed RemoteSigner response rejected.

R5. malformed combined RemoteSigner attestation rejected.

R6. fixture loopback rejected under ProductionRemoteSignerRequired.

R7. fixture loopback rejected under MainnetProductionRemoteSignerRequired.

R8. production RemoteSigner rejected as unavailable.

R9. MainNet production RemoteSigner rejected as unavailable.

R10. wrong environment rejected.

R11. wrong chain rejected.

R12. wrong genesis rejected.

R13. wrong authority root rejected.

R14. wrong custody key id rejected.

R15. wrong signing-key fingerprint rejected.

R16. wrong lifecycle action rejected.

R17. wrong candidate digest rejected.

R18. wrong authority-domain sequence rejected.

R19. wrong request digest rejected.

R20. stale/replayed request rejected.

R21. stale/replayed response rejected.

R22. expired attestation rejected.

R23. expired response rejected.

R24. unsupported suite rejected.

R25. invalid signature / placeholder signature rejected.

R26. local operator key cannot satisfy RemoteSigner policy.

R27. peer majority / gossip count cannot satisfy RemoteSigner policy.

R28. RemoteSigner valid but custody metadata invalid rejected.

R29. custody valid but RemoteSigner response invalid rejected.

R30. lifecycle valid + governance valid + custody valid + production RemoteSigner unavailable rejected.

R31. validation-only rejection writes no marker and no sequence.

R32. mutating rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

R33. invalid live `0x05` RemoteSigner material candidate is not propagated, staged, or applied.

R34. MainNet peer-driven apply remains refused even with fixture loopback RemoteSigner material.

Required tests:
Add focused tests, preferably:

`crates/qbind-node/tests/run_196_remote_signer_payload_callsite_tests.rs`

Minimum coverage:

* A1–A10 where representable.
* R1–R34.
* serde/parse compatibility:

  * old no-RemoteSigner payload parses;
  * RemoteSigner-carrying payload parses;
  * malformed identity/request/response fails closed;
  * unsupported future RemoteSigner schema version fails closed.
* canonical digest determinism through wire conversion.
* source reachability:

  * RemoteSigner material reaches production call-site context;
  * `validate_remote_signer` reached outside helper/example modules;
  * `validate_lifecycle_governance_custody_and_remote_signer` reached outside helper/example modules.
* no-mutation invariants.
* MainNet refusal invariants.

Required documentation:
Add:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_196.md`

Update:

* `docs/whitepaper/contradiction.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

Documentation must state:

* Run 196 is source/test RemoteSigner payload/carrying and production-context wiring.
* No real RemoteSigner backend is implemented.
* Fixture loopback RemoteSigner remains DevNet/TestNet source/test only.
* Production RemoteSigner remains unavailable/fail-closed.
* RemoteSigner payload/carrying evidence is source/test only.
* MainNet peer-driven apply remains refused.
* KMS/HSM remain unimplemented.
* governance execution remains unimplemented.
* real on-chain proof verification remains unimplemented.
* validator-set rotation remains open.
* release-binary RemoteSigner payload/carrying evidence is deferred to Run 197.
* full C4 remains open.
* C5 remains open.

Validation commands:
Run at minimum:

* `cargo build -p qbind-node --lib`
* `cargo test -p qbind-node --test run_196_remote_signer_payload_callsite_tests`
* `cargo test -p qbind-node --test run_194_remote_authority_signer_boundary_tests`
* `cargo test -p qbind-node --test run_192_authority_custody_policy_selector_tests`
* `cargo test -p qbind-node --test run_190_authority_custody_payload_callsite_tests`
* `cargo test -p qbind-node --test run_188_authority_custody_boundary_tests`
* `cargo test -p qbind-node --test run_186_onchain_governance_production_verifier_boundary_tests`
* `cargo test -p qbind-node --test run_184_onchain_governance_payload_carrying_tests`
* `cargo test -p qbind-node --test run_182_onchain_governance_production_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_180_onchain_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_178_onchain_governance_proof_tests`
* `cargo test -p qbind-node --test run_176_live_0x05_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_173_validation_only_governance_required_policy_tests`
* `cargo test -p qbind-node --test run_171_governance_required_policy_selector_tests`
* `cargo test -p qbind-node --test run_169_governance_proof_loader_surface_integration_tests`
* `cargo test -p qbind-node --test run_167_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_165_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_163_governance_authority_verifier_tests`
* `cargo test -p qbind-node --test run_161_lifecycle_marker_integration_tests`
* `cargo test -p qbind-node --test run_159_authority_signing_key_lifecycle_tests`
* `cargo test -p qbind-node --test run_157_unified_testnet_fixture_universe_tests`
* `cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests`
* `cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests`
* `cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests`
* `cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests`
* `cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests`
* `cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Acceptance criteria:
Run 196 is acceptable only if:

1. RemoteSigner material can be carried through production payload/context paths at source/test level.
2. Legacy no-RemoteSigner payloads remain compatible.
3. Fixture loopback RemoteSigner material reaches and passes production-context path where policy allows.
4. Production RemoteSigner material reaches boundary and fails closed as unavailable.
5. Malformed/invalid RemoteSigner material fails closed.
6. RemoteSigner integrates with custody composition at source/test level.
7. Validation-only surfaces remain non-mutating.
8. Mutating rejection paths produce no mutation.
9. MainNet peer-driven apply remains refused even with fixture loopback RemoteSigner material.
10. Docs defer release-binary RemoteSigner payload/carrying evidence to Run 197.
11. No real RemoteSigner / KMS / HSM / governance execution / validator-set rotation claim is made.
12. No full C4 or C5 closure is claimed.
