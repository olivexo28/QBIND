Task: Run 174 — Release-Binary Validation-Only Required-Policy Governance-Proof Evidence

Current accepted state:

* Run 171: hidden Required-policy selector landed:

  * CLI: `--p2p-trust-bundle-governance-proof-required`;
  * env: `QBIND_P2P_TRUST_BUNDLE_GOVERNANCE_PROOF_REQUIRED`;
  * default remains `GovernanceProofPolicy::NotRequired`.
* Run 172: release-binary Required-policy evidence landed for mutating surfaces:

  * default NotRequired compatibility proven;
  * Required + valid proof reload-apply accepted;
  * Required + no-proof reload-apply rejected with `GovernanceAuthorityRequiredButMissing`;
  * invalid proof variants rejected;
  * MainNet peer-driven apply refused.
* Run 173: source/test validation-only Required-policy wiring landed:

  * `preflight_v2_validation_only_marker_check_with_governance_proof_load`;
  * `preflight_run_132_validation_only_v2_marker_check` now resolves policy through `governance_proof_policy_from_cli_or_env`;
  * `--p2p-trust-bundle-reload-check` and local `--p2p-trust-bundle-peer-candidate-check` consume Required policy by construction;
  * validation-only invariants preserved.
* Run 173 limitation:

  * live inbound `0x05` peer-candidate envelopes still do not carry the governance-proof sibling;
  * live `0x05` proof-carrying remains out of scope.
* MainNet peer-driven apply remains refused.
* OnChainGovernance remains unsupported/fail-closed.
* governance execution remains unimplemented.
* KMS/HSM remains unimplemented.
* validator-set rotation remains open.
* full C4 remains open.
* C5 remains open.

Objective:
Produce release-binary evidence that validation-only production surfaces enforce `GovernanceProofPolicy::RequiredForLifecycleSensitive` using the hidden Run 171 selector and the Run 173 validation-only wiring.

This run must prove on real `target/release/qbind-node`:

1. default validation-only behavior remains `NotRequired`;
2. CLI selector activates Required policy on validation-only reload-check;
3. env selector activates Required policy on validation-only reload-check;
4. Required policy accepts valid proof-carrying sidecars where anti-rollback, lifecycle, and governance checks all pass;
5. Required policy rejects no-proof, malformed-proof, and invalid-proof sidecars fail-closed;
6. validation-only surfaces remain strictly non-mutating;
7. MainNet peer-driven apply remains refused.

Strict scope:

* Release-binary evidence only.
* Use real `target/release/qbind-node`.
* Use release-built helper(s) to mint proof-carrying sidecars.
* No production source change unless a tiny harness-only fix is required.
* No MainNet peer-driven apply enablement.
* No governance execution engine.
* No on-chain governance implementation.
* No KMS/HSM implementation.
* No validator-set rotation.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* No new proof schema.
* No marker schema change.
* No sequence-file schema change.
* No trust-bundle schema change.
* No peer-candidate envelope schema change.
* Do not weaken Runs 070, 130–173.
* Do not claim full C4 or C5 closure.

Required deliverables:

1. Add release-binary harness:
   `scripts/devnet/run_174_validation_only_governance_required_policy_release_binary.sh`

2. Add evidence archive:
   `docs/devnet/run_174_validation_only_governance_required_policy_release_binary/`

3. Add canonical evidence report:
   `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_174.md`

4. Update narrowly:

   * `docs/whitepaper/contradiction.md`
   * `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
   * `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
   * `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

5. Crosscheck all new/modified code and docs against existing design/spec. If contradictions or inconsistencies are found, record them in `contradiction.md` before finalizing.

Required production validation-only surfaces:
Exercise at minimum:

1. `--p2p-trust-bundle-reload-check`
2. local `--p2p-trust-bundle-peer-candidate-check`, if release-binary harness support exists

Do not include live inbound `0x05` proof-carrying claims unless the envelope can actually carry the governance-proof sibling without schema drift. If not feasible, document that live `0x05` proof-carrying remains open.

Required accepted scenarios:

A1. reload-check default NotRequired accepts old no-proof sidecar

* No CLI flag and no env var.
* Expected:

  * real `qbind-node` exits success;
  * default policy is NotRequired;
  * validation-only semantics preserved;
  * no sequence write;
  * no marker write.

A2. reload-check CLI Required accepts valid proof-carrying Rotate sidecar

* Use `--p2p-trust-bundle-governance-proof-required`.
* Expected:

  * hidden CLI selector observed;
  * proof sibling parsed through production loader;
  * `GovernanceProofLoadStatus::Available` or equivalent visible;
  * `GovernanceProofContext::Available` reaches marker decision;
  * governance verifier accepts;
  * lifecycle validation accepts;
  * marker decision accepts;
  * no sequence write;
  * no marker write;
  * no live trust mutation;
  * no session eviction.

A3. reload-check env Required accepts valid proof-carrying Rotate sidecar

* Use `QBIND_P2P_TRUST_BUNDLE_GOVERNANCE_PROOF_REQUIRED=1`.
* Expected:

  * env selector observed;
  * Required policy active;
  * valid proof accepted;
  * validation-only non-mutation preserved.

A4. local peer-candidate-check default NotRequired accepts old no-proof sidecar, if supported

* Expected:

  * existing validation-only behavior preserved;
  * no sequence write;
  * no marker write.

A5. local peer-candidate-check CLI/env Required accepts valid proof-carrying Rotate sidecar, if supported

* Expected:

  * proof context reaches validation-only marker decision;
  * valid proof accepted;
  * no mutation.

A6. false/unset env preserves NotRequired

* Test unset, `0`, `false`, or equivalent non-truthy value.
* Expected:

  * Required policy is not activated accidentally;
  * old no-proof sidecar remains compatible.

Required rejection scenarios:

R1. reload-check CLI Required + no-proof sidecar rejected

* Expected:

  * `GovernanceAuthorityRequiredButMissing` visible;
  * no sequence write;
  * no marker write.

R2. reload-check env Required + no-proof sidecar rejected

* Expected:

  * `GovernanceAuthorityRequiredButMissing` visible;
  * no sequence write;
  * no marker write.

R3. reload-check Required + malformed proof rejected.

R4. reload-check Required + invalid issuer signature rejected.

R5. reload-check Required + wrong environment proof rejected.

R6. reload-check Required + wrong chain proof rejected.

R7. reload-check Required + wrong genesis proof rejected.

R8. reload-check Required + wrong authority root proof rejected.

R9. reload-check Required + wrong lifecycle action proof rejected.

R10. reload-check Required + wrong candidate digest proof rejected.

R11. reload-check Required + wrong authority-domain sequence proof rejected.

R12. reload-check Required + OnChainGovernance proof rejected as unsupported.

R13. reload-check Required + local operator config proof rejected.

R14. reload-check Required + peer-majority proof rejected.

R15. local peer-candidate-check Required + no-proof sidecar rejected, if supported.

R16. local peer-candidate-check Required + invalid proof rejected, if supported.

R17. validation-only Required rejection writes no marker and no sequence.

R18. validation-only Required rejection performs no live trust swap, no session eviction, and no Run 070 call.

R19. selector cannot be accidentally enabled by unrelated flags.

R20. MainNet peer-driven apply remains refused even if validation-only Required proof check accepts.

Required selector proof:
The evidence report must include:

* CLI proof:

  * `--p2p-trust-bundle-governance-proof-required` is present but hidden;
  * enabling it activates Required policy for reload-check.
* env proof:

  * `QBIND_P2P_TRUST_BUNDLE_GOVERNANCE_PROOF_REQUIRED=1` activates Required policy for reload-check;
  * false / unset value preserves NotRequired.
* source grep/call-site proof for:

  * `governance_proof_policy_from_cli_or_env`;
  * `preflight_run_132_validation_only_v2_marker_check`;
  * `preflight_v2_validation_only_marker_check_with_governance_proof_load`;
  * `preflight_v2_marker_decision_with_governance_proof_load`;
  * `load_versioned_ratification_with_governance_proof_from_path`;
  * `GovernanceProofContext::Available`.

Required production-surface reachability proof:
The report must include release-binary logs proving:

* validation-only production surfaces exercised:

  * Required policy;
  * proof-carrying Available path;
  * proof-required missing path;
  * invalid/malformed proof path;
  * no-mutation behavior.

Required no-mutation proof:
For every validation-only scenario:

* no Run 070 apply call;
* no live trust swap;
* no session eviction;
* no sequence write;
* no marker write;
* marker bytes unchanged if present;
* sequence bytes unchanged if present;
* no `.tmp` residue;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead.

Required denylist:
Across the run prove:

* no MainNet apply;
* no autonomous apply;
* no apply on receipt;
* no peer-majority authority;
* no governance execution claim;
* no on-chain governance claim;
* no KMS/HSM claim;
* no validator-set rotation claim;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead;
* no schema/wire/metric drift;
* no sequence write on validation-only surfaces;
* no marker write on validation-only surfaces.

Required captured metadata:

* qbind-node SHA-256 and ELF Build ID;
* helper SHA-256 and ELF Build ID;
* git commit hash;
* rustc/cargo versions;
* exact commands;
* stdout/stderr logs;
* per-scenario exit codes;
* proof sidecar paths and hashes;
* marker/sequence JSON and SHA before/after;
* data-dir inventories;
* denylist grep results.

Validation commands:
Run at minimum:

* `cargo build --release -p qbind-node --bin qbind-node`
* `cargo build --release -p qbind-node --example <proof-carrying fixture helper>`
* `bash scripts/devnet/run_174_validation_only_governance_required_policy_release_binary.sh`
* `cargo test -p qbind-node --test run_173_validation_only_governance_required_policy_tests`
* `cargo test -p qbind-node --test run_171_governance_required_policy_selector_tests`
* `cargo test -p qbind-node --test run_169_governance_proof_loader_surface_integration_tests`
* `cargo test -p qbind-node --test run_167_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_165_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_163_governance_authority_verifier_tests`
* `cargo test -p qbind-node --test run_161_lifecycle_marker_integration_tests`
* `cargo test -p qbind-node --test run_159_authority_signing_key_lifecycle_tests`
* `cargo test -p qbind-node --test run_157_unified_testnet_fixture_universe_tests`
* `cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests`
* `cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests`
* `cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests`
* `cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests`
* `cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests`
* `cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Documentation requirements:
`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_174.md` must state:

* Run 174 is release-binary validation-only Required-policy governance-proof evidence.
* Default remains NotRequired.
* Hidden CLI/env selector activates Required policy on validation-only reload-check.
* Existing no-proof sidecars remain compatible under NotRequired.
* Required policy fails closed when proof is absent.
* Valid proof-carrying sidecars pass under Required policy through real validation-only production surfaces where lifecycle/anti-rollback checks pass.
* Invalid proof-carrying sidecars fail closed.
* Validation-only surfaces remain strictly non-mutating.
* Live inbound `0x05` proof-carrying remains open if envelope cannot carry the proof sibling.
* OnChainGovernance remains unsupported/fail-closed.
* No MainNet apply is enabled.
* governance execution/on-chain proof remains unimplemented.
* KMS/HSM remains unimplemented.
* validator-set rotation remains open.
* full C4 remains open unless separately closed by explicit criteria.
* C5 remains open.

Acceptance criteria:
Run 174 is acceptable only if:

1. Real release binaries exercise Required policy on validation-only reload-check.
2. Default NotRequired compatibility remains proven.
3. CLI selector and env selector both activate Required policy for validation-only reload-check.
4. Required/no-proof validation-only cases fail closed.
5. Valid proof-carrying sidecars reach and pass the governance gate on validation-only production surfaces.
6. Invalid proof-carrying sidecars fail closed.
7. Validation-only scenarios produce no mutation.
8. MainNet remains refused even with Required policy and valid governance proof.
9. No governance execution/KMS-HSM/validator-set rotation claim is made.
10. No full C4 or C5 closure is claimed.
