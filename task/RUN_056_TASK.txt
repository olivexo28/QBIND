Continue QBIND from the current state.

Project:
- QBIND post-quantum L1
- security-first, production-grade, execution-first
- C4 is the main live operability tracker
- C5 tracks timeout-verification activation, forged-traffic rejection, and transport-root dependency
- no broad redesign
- no silent regressions
- no hype / no overclaiming

Current state:
- Runs 037–045 hardened the PQC transport path:
  - ML-DSA-44 transport delegation cert verification
  - ML-KEM-768 transport KEM
  - ChaCha20-Poly1305 transport AEAD
  - PQC cert metrics
  - cert validity-window enforcement
- Runs 046–049 completed live timeout-backoff evidence through the 800-tick cap.
- Run 050 landed structured trust bundles:
  - --p2p-trust-bundle <PATH>
  - environment binding
  - bundle/root validity windows
  - root status active | retired | revoked
  - root-level revocation
  - canonical fingerprint
  - fail-closed validation
- Run 051 landed signed ML-DSA-44 trust-bundle verification:
  - --p2p-trust-bundle-signing-key KEYID:SUITE:PK
  - signed DevNet/TestNet live startup proven
  - unsigned TestNet/MainNet fail closed
  - tampered/wrong-key/malformed/unsupported-suite/key-root-collision fail closed
  - no fallback to --p2p-trusted-root
  - no DummySig/DummyKem/DummyAead fallback
- Run 052 landed leaf-level certificate revocation enforcement.
- Run 053 landed trust-bundle chain_id crosscheck.
- Run 054 produced release-binary leaf-revocation evidence artifacts.
- Run 055 landed trust-bundle sequence-number monotonicity persistence:
  - new pqc_trust_sequence.rs
  - persistent record under data dir
  - highest accepted sequence tracked per environment + chain_id trust domain
  - enforcement happens after signed-bundle verification and chain_id validation, but before roots are merged into the live PQC trust set
  - first load accepted and persisted
  - higher sequence accepted and persisted
  - lower sequence rejected as rollback
  - equal sequence + same fingerprint accepted as no-op restart
  - equal sequence + different fingerprint rejected as equivocation
  - corrupt/malformed/wrong-env/wrong-chain persistence fails closed
  - persistence write failure fails closed
  - four new metrics:
    - qbind_p2p_pqc_trust_bundle_sequence_highest
    - qbind_p2p_pqc_trust_bundle_sequence_rollback_rejected_total
    - qbind_p2p_pqc_trust_bundle_sequence_equal_fingerprint_mismatch_total
    - qbind_p2p_pqc_trust_bundle_sequence_persist_failures_total
  - qbind-node --lib 882/882 passed
  - Code Review clean
  - CodeQL clean
- Run 055 boundary:
  - release-binary anti-rollback smoke artifacts were NOT produced.
- C4 remains OPEN.
- C5 remains OPEN / narrowed.

Task:
Run 056. Produce release-binary live evidence artifacts for Run 055 trust-bundle sequence-number monotonicity persistence.

Primary objective:
Prove on the real release qbind-node binary that the signed trust-bundle sequence anti-rollback persistence works across restarts:
- sequence 1 first-load accepted and persisted;
- sequence 2 upgrade accepted and persisted;
- sequence 1 rollback after sequence 2 fails closed;
- equal sequence with same fingerprint behaves according to Run 055 policy;
- equal sequence with different fingerprint fails closed;
- corrupted persistence file fails closed;
- no fallback to --p2p-trusted-root or Dummy crypto.

Important framing:
This is evidence-first.
Do not implement activation epoch/height gating.
Do not implement CA/rotation playbook.
Do not implement MainNet live smoke unless trivial and non-distracting.
Do not redesign trust bundles.
Do not redesign KEMTLS.
Do not modify consensus or timeout logic.
Do not modify source unless the evidence run exposes a real bug.
Do not weaken signed-bundle verification.
Do not silently fall back to --p2p-trusted-root.
Do not silently fall back to DummySig, DummyKem, or DummyAead.
Do not claim full C4 or C5 closure.

Required active stack:
- release qbind-node binary
- --p2p-mutual-auth required
- --p2p-pqc-root-mode pqc-static-root
- --p2p-trust-bundle <signed-devnet-bundle>
- --p2p-trust-bundle-signing-key KEYID:100:PK
- explicit --data-dir
- real ML-DSA-44 transport delegation certs
- real ML-KEM-768 leaf KEM material
- real ChaCha20-Poly1305 AEAD
- metrics enabled where process lives long enough
- no DummySig
- no DummyKem
- no DummyAead

Strict scope:
- Evidence-only unless a real bug is found.
- Existing Run 055 implementation should be treated as already landed.
- Existing Run 050/051/052/053/054 behavior must be preserved.
- Use the existing persistence file path and format from Run 055.
- Do not add bypass flags.
- Do not add --ignore-trust-bundle-sequence.
- Do not silently delete or reset corrupted sequence state.
- Crosscheck all evidence against existing design/spec. If contradictions or inconsistencies are found, update docs/whitepaper/contradiction.md.

Required evidence cases:

1. Build and identify binaries
   - Build release qbind-node.
   - Build release devnet_pqc_trust_bundle_helper.
   - Build release devnet_pqc_root_helper if needed.
   - Record:
     - branch
     - commit
     - dirty/clean status
     - qbind-node sha256
     - qbind-node ELF BuildID
     - helper sha256
     - helper ELF BuildID
   - Confirm --devnet-forged-inject remains hidden from normal --help.
   - Confirm relevant flags:
     - --p2p-trust-bundle
     - --p2p-trust-bundle-signing-key
     - --p2p-pqc-root-mode
     - --p2p-leaf-cert
     - --p2p-leaf-cert-key
     - --p2p-peer-leaf-cert
     - --p2p-mutual-auth
     - --data-dir

2. Positive first-load sequence 1 smoke
   Create a valid signed DevNet trust bundle with:
   - environment = DevNet
   - matching or null chain_id according to Run 053 policy
   - sequence = 1
   - valid signing key
   - valid active root
   - no revoked local/peer leaf that would block startup

   Run qbind-node with a fresh data dir.

   Prove:
   - node starts.
   - signed bundle verifies.
   - trust bundle loads.
   - sequence record is written under the data dir.
   - record contains expected environment, chain_id, highest_sequence=1, and bundle fingerprint.
   - qbind_p2p_pqc_trust_bundle_sequence_highest = 1 if metrics are reachable.
   - no fallback to --p2p-trusted-root.
   - no DummySig/DummyKem/DummyAead fallback.
   - no private key material in logs.

3. Positive upgrade sequence 2 smoke
   Create a valid signed DevNet trust bundle for the same trust domain with:
   - sequence = 2
   - valid signing key
   - valid active root
   - different canonical fingerprint from sequence 1 if naturally different

   Restart qbind-node using the same data dir.

   Prove:
   - node starts.
   - signed bundle verifies.
   - sequence 2 is accepted.
   - persisted highest_sequence updates from 1 to 2.
   - qbind_p2p_pqc_trust_bundle_sequence_highest = 2 if metrics are reachable.
   - no fallback.
   - no Dummy crypto.

4. Negative rollback sequence 1 after sequence 2
   Restart qbind-node using the same data dir but with the old sequence 1 signed bundle.

   Expected:
   - startup fails closed before roots are merged.
   - precise FATAL/log reason mentions rollback/lower sequence.
   - persisted sequence record remains at 2.
   - no fallback to --p2p-trusted-root.
   - no DummySig/DummyKem/DummyAead.
   - if /metrics is unavailable due fail-closed startup, document boundary honestly and rely on Run 055 tests for rollback counter movement.

5. Positive equal-sequence same-fingerprint smoke
   Restart qbind-node using the same sequence 2 bundle and same data dir.

   Expected under Run 055 policy:
   - equal sequence + same fingerprint is accepted as no-op restart.
   - persisted record remains unchanged.
   - sequence_highest remains 2.
   - no fallback.
   - no Dummy crypto.

6. Negative equal-sequence different-fingerprint smoke
   Create a different validly signed bundle with:
   - same trust domain
   - same sequence = 2
   - different canonical fingerprint

   Restart qbind-node using the same data dir.

   Expected:
   - startup fails closed as equal-sequence fingerprint mismatch / equivocation.
   - persisted record remains unchanged.
   - no fallback.
   - no Dummy crypto.
   - if metrics unavailable, document boundary and rely on Run 055 tests for metric movement.

7. Negative corrupted sequence-file smoke
   Corrupt the persisted sequence file in the same data dir.

   Restart qbind-node with otherwise valid sequence 3 or sequence 2 bundle.

   Expected:
   - startup fails closed.
   - precise FATAL/log reason mentions malformed/corrupt sequence persistence.
   - corrupted state is not silently deleted, reset, or overwritten.
   - no fallback.
   - no Dummy crypto.

8. Optional wrong-domain persistence smoke
   If low cost:
   - manually alter persisted record environment or chain_id.
   - restart with otherwise valid bundle.
   - prove fail closed.
   - do not block Run 056 on this if unit/integration tests already prove it.

Required tests to rerun:
- cargo test -p qbind-node --lib pqc_trust_sequence
- cargo test -p qbind-node --lib pqc_trust_bundle
- cargo test -p qbind-node --test run_055_pqc_trust_bundle_sequence_tests
- cargo test -p qbind-node --test run_050_pqc_trust_bundle_tests
- cargo test -p qbind-node --test run_051_pqc_trust_bundle_signing_tests
- cargo test -p qbind-node --test run_052_pqc_leaf_revocation_tests
- cargo test -p qbind-net --test run_052_leaf_revocation_handshake_tests
- cargo test -p qbind-node --lib metrics
- cargo test -p qbind-node --lib p2p
- cargo test -p qbind-node --test run_037_pqc_static_root_mutual_auth_tests
- cargo test -p qbind-node --test run_040_pqc_static_root_real_aead_tests
- cargo test -p qbind-node --test run_044_pqc_cert_verify_metrics_adapter_tests
- cargo test -p qbind-node --lib
- cargo test -p qbind-net --lib
- cargo test -p qbind-crypto --lib
- cargo build --release -p qbind-node --bin qbind-node
- cargo build --release -p qbind-node --example devnet_pqc_trust_bundle_helper
- cargo build --release -p qbind-node --example devnet_pqc_root_helper

If source touches timeout verification or binary consensus loop, also run:
- cargo test -p qbind-node --lib binary_consensus
- cargo test -p qbind-node --lib forged_injection
- cargo test -p qbind-node --lib run030
- cargo test -p qbind-consensus --lib timeout

Evidence document:
Create:
- docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_056.md

Evidence document must include:
- exact objective
- exact verdict
- exact files changed
- exact commands run
- tests run and pass/fail status
- binary identity
- helper identities
- trust-bundle material procedure
- signed-bundle verification proof
- chain_id compatibility/matching status for bundles used
- sequence persistence file path
- sequence record contents after first load
- sequence record contents after upgrade
- positive first-load sequence 1 smoke
- positive upgrade sequence 2 smoke
- negative rollback sequence 1 after sequence 2 smoke
- positive equal-sequence same-fingerprint smoke
- negative equal-sequence different-fingerprint smoke
- negative corrupted sequence-file smoke
- /metrics excerpts where available
- exact boundary if fail-closed startup prevents metric scraping
- proof no fallback to --p2p-trusted-root
- proof no DummySig/DummyKem/DummyAead fallback
- remaining open items
- exact immediate next action

contradiction.md update rules:
- If release-binary sequence anti-rollback evidence passes, update C4 to record Run 055’s live-binary artifact boundary as closed/narrowed by Run 056.
- Keep C4 open for:
  - activation epoch/height gating
  - MainNet live signed-bundle smoke if still absent
  - production CA / cert rotation / signing-key rotation operator playbook
  - production fast-sync / consensus-storage restore
  - per-environment production trust-anchor operation if not fully solved
- Do not close full C4.
- Do not claim C5 closure unless explicitly justified.
- If rollback is accepted, record as contradiction/regression.
- If corrupt persistence is ignored/reset silently, record as contradiction/regression.
- If failure falls back to static roots or Dummy crypto, record as contradiction/regression.
- If metrics are unobservable due to fail-closed startup, document boundary honestly rather than fabricating.

Expected verdict options:
- Strongest positive:
  Release-binary evidence proves first-load sequence 1 accepted/persisted, sequence 2 upgrade accepted/persisted, rollback to sequence 1 rejected after restart, equal-sequence same-fingerprint accepted, equal-sequence different-fingerprint rejected, corrupted persistence fails closed, no fallback, all regressions pass.
- Partial positive:
  Release-binary evidence proves some anti-rollback behavior but a negative smoke or metric boundary remains incomplete. State exact boundary.
- Negative:
  rollback accepted, sequence file corruption ignored, equal-sequence ambiguity accepted unsafely, fallback/Dummy crypto activates, or regressions fail.

Required final response:
1. Exact verdict
2. Exact files changed
3. Exact commands run
4. Exact tests/evidence run and pass/fail status
5. What was proven
6. What remains not solved
7. Whether contradiction.md was updated and why
8. Exact immediate next action recommended