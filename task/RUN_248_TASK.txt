Run 248 — Source/test modeled durable-consume projection sink boundary

You are continuing QBIND after Run 247.

Run 240 is fully closed at source/test level.
Run 241 is fully closed as release-binary evidence for Run 240.
Run 242 is fully closed as source/test governance execution mutation-engine boundary work.
Run 243 is fully closed as release-binary evidence for Run 242.
Run 244 is fully closed as source/test modeled governance trust-state mutation applier boundary work.
Run 245 is fully closed as release-binary evidence for Run 244.
Run 246 is fully closed as source/test governance modeled end-to-end pipeline boundary work.
Run 247 is fully closed as release-binary evidence for Run 246.

Strict workflow:

* Execution-first.
* Narrow scope only.
* No broad redesign.
* No silent regressions.
* No hype or overclaiming.
* Keep Full C4 OPEN and C5 OPEN unless real production evidence closes them.
* MainNet peer-driven apply remains refused.
* Fixture/local/loopback material remains DevNet/TestNet evidence-only.
* Rejected paths must remain non-mutating.
* Do not weaken Runs 070, 130–247.
* Source/test only.
* No release-binary evidence in this run.

Current state:

* Run 224 introduced source/test governance evaluator runtime integration.
* Run 225 release-evidenced the Run 224 evaluator-runtime integration.
* Run 226 wired representable runtime call sites through the evaluator-runtime integration at source/test level.
* Run 227 release-evidenced the Run 226 call-site wiring.
* Run 230 / 232 / 234 / 236 / 238 / 240 built the replay/freshness, consume-after-success, durable replay-state backend, and durable runtime integration path.
* Run 241 release-evidenced the Run 240 durable runtime integration.
* Run 242 added the source/test governance execution mutation-engine boundary.
* Run 243 release-evidenced the Run 242 mutation-engine boundary.
* Run 244 added the source/test modeled governance trust-state mutation applier boundary.
* Run 245 release-evidenced the Run 244 modeled applier boundary.
* Run 246 added the source/test governance modeled end-to-end pipeline in `crates/qbind-node/src/pqc_governance_modeled_end_to_end_pipeline.rs`.
* Run 247 release-evidenced the Run 246 modeled end-to-end pipeline boundary on real `target/release/qbind-node` plus a release-built helper.
* The current Run 246/247 pipeline proves durable consume is authorized only after evaluator/call-site authorization, durable replay freshness, mutation-engine authorization, and modeled applier success all agree.
* What remains missing is a typed source/test boundary that models how a future production call site would record the after-success-only durable consume receipt once the Run 246 pipeline has authorized consume.
* No real governance execution engine is implemented.
* No real production mutation engine is implemented.
* No real on-chain governance proof verifier is implemented.
* No real persistent replay backend is implemented.
* No RocksDB/file/schema/migration/storage-format change is implemented.
* No KMS/HSM/RemoteSigner backend is implemented.
* No MainNet governance enablement.
* No MainNet peer-driven apply enablement.
* Validator-set rotation remains unsupported.
* Full C4 remains OPEN.
* C5 remains OPEN.

Objective:

Implement the smallest source/test-only typed modeled durable-consume projection sink boundary.

This run should extend the Run 246 modeled end-to-end pipeline with a mockable, in-memory consume-receipt sink that represents how a future production call site would record an after-success-only durable consume receipt through the Run 240 durable completion semantics.

The sink must be a model only. It must not implement a real persistent backend. It must not write RocksDB, files, schemas, migrations, storage formats, wire formats, authority markers, trust-bundle sequence files, or any production durable state. It must not call Run 070, mutate `LivePqcTrustState`, perform a real trust swap, evict sessions, or enable MainNet governance / MainNet peer-driven apply.

The key invariant is:

* Run 246 pipeline success authorizes a modeled consume receipt.
* The Run 248 sink may record that receipt only in modeled in-memory fixture state.
* Every non-success pipeline outcome must produce no sink invocation, no receipt, no consume, and no mutation.
* A sink failure, rollback, rollback failure, or ambiguous receipt window must never retroactively claim durable consume success.

Suggested implementation:

1. Add a new source/test-only module:

   `crates/qbind-node/src/pqc_governance_modeled_durable_consume_projection_sink.rs`

2. Register it in:

   `crates/qbind-node/src/lib.rs`

3. Define typed sink inputs and bindings, for example:

   * `GovernanceModeledDurableConsumeSinkInput`
   * `GovernanceModeledDurableConsumeSinkExpectations`
   * `GovernanceModeledDurableConsumeSinkPolicy`
   * `GovernanceModeledDurableConsumeSinkSurface`
   * `GovernanceModeledDurableConsumeSinkEnvironmentBinding`
   * `GovernanceModeledDurableConsumeSinkRuntimeBinding`
   * `GovernanceModeledDurableConsumeSinkReplayBinding`
   * `GovernanceModeledDurableConsumeSinkPipelineBinding`
   * `GovernanceModeledDurableConsumeSinkReceipt`

4. Define modeled in-memory receipt state, for example:

   * `ModeledDurableConsumeReceiptLedger`
   * `ModeledDurableConsumeReceiptRecord`
   * `ModeledDurableConsumeReceiptSnapshot`
   * `ModeledDurableConsumeReceiptStatus`
   * `ModeledDurableConsumeReceiptDigest`

5. Define an explicit sink outcome enum, for example:

   * `LegacyBypassNoReceipt`
   * `RejectedBeforePipelineNoReceipt`
   * `PipelineDidNotAuthorizeConsumeNoReceipt`
   * `ConsumeReceiptRecorded`
   * `ConsumeReceiptDuplicateIdempotent`
   * `ConsumeReceiptRejectedBeforeRecord`
   * `ConsumeReceiptRecordFailedNoConsume`
   * `ConsumeReceiptRolledBackNoConsume`
   * `ConsumeReceiptRollbackFailedFatalNoConsume`
   * `ConsumeReceiptAmbiguousFailClosedNoConsume`
   * `ProductionSinkUnavailableNoConsume`
   * `MainNetSinkUnavailableNoConsume`
   * `MainNetPeerDrivenApplyRefusedNoConsume`
   * `ValidatorSetRotationUnsupportedNoConsume`
   * `PolicyChangeUnsupportedNoConsume`

6. Define a pure/mockable sink trait, for example:

   * `GovernanceModeledDurableConsumeProjectionSink`
   * `record_modeled_consume_receipt(...)`
   * `recover_modeled_consume_receipt_window(...)`

7. Provide source/test-only sink implementations:

   * `FixtureModeledDurableConsumeProjectionSink`
   * `ProductionModeledDurableConsumeProjectionSink`
   * `MainNetModeledDurableConsumeProjectionSink`

   Requirements:

   * DevNet/TestNet fixture sink may mutate only `ModeledDurableConsumeReceiptLedger`.
   * Production/MainNet sinks must be reachable but unavailable/fail-closed.
   * MainNet peer-driven apply must be refused before pipeline progression and before sink invocation.
   * The fixture sink must expose an invocation counter so tests prove non-success paths never invoke it.
   * The fixture ledger must be in-memory only and must never touch RocksDB, files, markers, sequence files, or production durable state.

8. Add composition helpers:

   * `project_pipeline_outcome_to_consume_sink_intent(...)`
   * `evaluate_modeled_durable_consume_projection_sink(...)`
   * `recover_modeled_durable_consume_projection_sink_window(...)`
   * `sink_outcome_authorizes_modeled_consume_receipt(...)`
   * `sink_outcome_projects_to_durable_completion(...)`

9. Required composition semantics:

   * `GovernanceModeledEndToEndPipelineOutcome::ModeledApplierAppliedAndDurableConsumeAuthorized` is the only pipeline outcome that may create a sink intent.
   * Every other Run 246 pipeline outcome maps to `PipelineDidNotAuthorizeConsumeNoReceipt` or a more specific no-receipt fail-closed outcome.
   * `ConsumeReceiptRecorded` is the only sink outcome that may project to a consume-recorded state.
   * Duplicate receipt may be idempotent only if the receipt digest, proposal id, decision id, candidate digest, authority-domain sequence, and modeled pipeline decision digest all match exactly.
   * Same receipt id with different digest must fail closed as equivocation and must not consume.
   * Sink record failure must not consume.
   * Sink rollback success must not consume.
   * Sink rollback failure must be fatal/fail-closed and must not consume.
   * Ambiguous receipt windows must fail closed and must not consume.
   * Production/MainNet sink unavailable outcomes must not consume.
   * Validator-set rotation and policy-change remain unsupported and must not consume.

10. Add grep-verifiable invariant helpers, for example:

* `modeled_consume_sink_rejection_is_non_mutating`
* `modeled_consume_sink_never_calls_run_070`
* `modeled_consume_sink_never_mutates_live_pqc_trust_state`
* `modeled_consume_sink_never_writes_sequence_or_marker`
* `modeled_consume_sink_no_rocksdb_file_schema_migration_change`
* `modeled_consume_sink_pipeline_success_required_before_receipt`
* `modeled_consume_sink_receipt_record_required_before_consume`
* `modeled_consume_sink_failed_record_never_consumes`
* `modeled_consume_sink_rollback_never_consumes`
* `modeled_consume_sink_ambiguous_window_fails_closed`
* `modeled_consume_sink_mainnet_peer_driven_apply_refused_first`
* `modeled_consume_sink_production_mainnet_unavailable`
* `modeled_consume_sink_validator_set_rotation_unsupported`
* `modeled_consume_sink_policy_change_unsupported`
* `modeled_consume_sink_local_operator_cannot_satisfy_mainnet_authority`
* `modeled_consume_sink_peer_majority_cannot_satisfy_mainnet_authority`

Tests:

Add:

`crates/qbind-node/tests/run_248_modeled_durable_consume_projection_sink_tests.rs`

Required test matrix:

Accepted / compatible:

* Disabled sink policy preserves legacy bypass and performs no receipt and no mutation.
* Disabled pipeline policy preserves legacy bypass and never invokes the sink.
* Disabled evaluator/call-site policy preserves legacy bypass and never invokes the sink.
* DevNet fixture pipeline success + sink record success records one modeled in-memory receipt.
* TestNet fixture pipeline success + sink record success records one modeled in-memory receipt.
* Modeled add-root pipeline success records a receipt only after modeled applier success.
* Modeled retire-root pipeline success records a receipt only after modeled applier success.
* Modeled revoke-root pipeline success records a receipt only after modeled applier success.
* Modeled emergency-revoke-root pipeline success records a receipt only after modeled applier success.
* Modeled noop pipeline success records a receipt only under explicit success semantics and no trust-state drift.
* Duplicate identical receipt is idempotent under an explicit typed outcome.
* Existing Run 246 pipeline behavior remains compatible.
* Existing Run 244 modeled applier behavior remains compatible.
* Existing Run 242 mutation-engine behavior remains compatible.
* Existing Run 240 durable completion behavior remains compatible.
* Production sink path is reachable but unavailable/fail-closed and records no receipt.
* MainNet sink path is reachable but unavailable/fail-closed and records no receipt.
* MainNet peer-driven apply refused before pipeline progression and before sink invocation.
* Validator-set rotation unsupported and records no receipt.
* Policy-change unsupported and records no receipt.

Rejected / fail-closed:

* evaluator rejection produces no sink intent and no receipt;
* call-site rejection produces no sink intent and no receipt;
* durable replay stale produces no sink intent and no receipt;
* durable replay expired produces no sink intent and no receipt;
* durable replay consumed produces no sink intent and no receipt;
* durable replay superseded produces no sink intent and no receipt;
* durable replay backend unavailable produces no sink intent and no receipt;
* mutation-engine rejection produces no sink intent and no receipt;
* modeled applier rejected-before-snapshot produces no sink intent and no receipt;
* modeled applier rejected-before-apply produces no sink intent and no receipt;
* modeled apply failure produces no sink intent and no receipt;
* modeled rollback produces no sink intent and no receipt;
* modeled rollback-failed produces no sink intent and no receipt;
* modeled ambiguous window produces no sink intent and no receipt;
* consume before pipeline success is rejected;
* consume after modeled apply failure is rejected;
* consume after modeled rollback is rejected;
* consume after rollback-failed is rejected;
* consume after ambiguous window is rejected;
* same receipt id with different digest is rejected as equivocation and records no second receipt;
* wrong environment rejected before sink invocation;
* wrong chain rejected before sink invocation;
* wrong genesis rejected before sink invocation;
* wrong governance surface rejected before sink invocation;
* wrong mutation surface rejected before sink invocation;
* wrong receipt digest rejected before record;
* wrong pipeline decision digest rejected before record;
* wrong proposal id rejected before record;
* wrong decision id rejected before record;
* wrong candidate digest rejected before record;
* wrong authority-domain sequence rejected before record;
* malformed receipt rejected before record;
* local operator key cannot satisfy MainNet authority;
* peer majority cannot satisfy MainNet authority;
* production sink unavailable records no receipt;
* MainNet sink unavailable records no receipt;
* validator-set rotation attempt unsupported and records no receipt;
* policy-change attempt unsupported and records no receipt;
* every rejected path produces no Run 070 call, no `LivePqcTrustState` mutation, no real trust swap, no session eviction, no sequence write, no marker write, no RocksDB/file/schema/migration/storage-format change, and no sink invocation where rejection happens before the sink stage.

Recovery / crash-window cases:

* before-pipeline window fails closed / no receipt;
* after-pipeline-success-before-sink-intent window fails closed / no receipt;
* after-sink-intent-before-record window fails closed / no receipt;
* after-record-before-report window fails closed unless an explicit matching receipt report exists;
* after-report-success recovers as receipt recorded;
* after-report-ambiguous fails closed / no consume;
* record-failed window fails closed / no consume;
* rollback-completed window fails closed / no consume;
* rollback-failed window is fatal/fail-closed / no consume;
* unknown window fails closed / no consume;
* production/MainNet recovery classification unavailable/fail-closed;
* MainNet peer-driven apply refusal precedes recovery classification where applicable.

Projection cases:

* only Run 246 `ModeledApplierAppliedAndDurableConsumeAuthorized` creates a sink intent;
* evaluator success alone creates no sink intent;
* durable replay freshness alone creates no sink intent;
* mutation-engine authorization alone creates no sink intent;
* modeled applier authorization without applied success creates no sink intent;
* only `ConsumeReceiptRecorded` authorizes modeled receipt-recorded state;
* `LegacyBypassNoReceipt` does not consume;
* `PipelineDidNotAuthorizeConsumeNoReceipt` does not consume;
* `ConsumeReceiptRejectedBeforeRecord` does not consume;
* `ConsumeReceiptRecordFailedNoConsume` does not consume;
* `ConsumeReceiptRolledBackNoConsume` does not consume;
* `ConsumeReceiptRollbackFailedFatalNoConsume` does not consume;
* `ConsumeReceiptAmbiguousFailClosedNoConsume` does not consume;
* `ProductionSinkUnavailableNoConsume` does not consume;
* `MainNetSinkUnavailableNoConsume` does not consume;
* `MainNetPeerDrivenApplyRefusedNoConsume` does not consume;
* `ValidatorSetRotationUnsupportedNoConsume` does not consume;
* `PolicyChangeUnsupportedNoConsume` does not consume.

Stage-ordering cases:

* MainNet peer-driven apply refusal happens before pipeline progression and sink invocation.
* Evaluator/call-site authorization happens before durable replay freshness.
* Durable replay freshness happens before mutation-engine authorization.
* Mutation-engine authorization happens before modeled applier invocation.
* Modeled applier success happens before sink intent.
* Sink receipt recording happens after pipeline consume authorization.
* A rejection before the sink stage leaves the sink invocation count at zero.
* A sink record failure does not retroactively invalidate modeled applier success but does not authorize consume.
* A sink rollback failure is fatal/fail-closed and does not authorize consume.
* The sink itself performs no persistent durable consume beyond modeled in-memory fixture state.

Validation commands:

Run at minimum:

* `cargo build -p qbind-node --lib`
* `cargo test -p qbind-node --test run_248_modeled_durable_consume_projection_sink_tests`
* `cargo test -p qbind-node --test run_246_governance_modeled_end_to_end_pipeline_tests`
* `cargo test -p qbind-node --test run_244_modeled_governance_trust_mutation_applier_tests`
* `cargo test -p qbind-node --test run_242_governance_execution_mutation_engine_tests`
* `cargo test -p qbind-node --test run_240_governance_evaluator_replay_durable_runtime_integration_tests`
* `cargo test -p qbind-node --test run_238_governance_evaluator_replay_durable_backend_tests`
* `cargo test -p qbind-node --test run_236_governance_evaluator_replay_consume_runtime_integration_tests`
* `cargo test -p qbind-node --test run_234_governance_evaluator_replay_consume_boundary_tests`
* `cargo test -p qbind-node --test run_232_governance_evaluator_replay_runtime_integration_tests`
* `cargo test -p qbind-node --test run_230_governance_evaluator_replay_state_tests`
* `cargo test -p qbind-node --test run_228_peer_evaluator_context_representation_tests`
* `cargo test -p qbind-node --test run_226_governance_evaluator_runtime_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_224_governance_evaluator_runtime_integration_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

Security scanning:

* Run secret scanning if available and report the result.
* Run CodeQL if available and report the result honestly.
* If Rust CodeQL analysis is skipped because the database is too large, say that explicitly and do not overclaim CodeQL coverage.

Documentation:

Create:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_248.md`

Update narrowly:

* `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
* `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
* `docs/whitepaper/contradiction.md`

Documentation must state:

* Run 248 is source/test only.
* It adds a modeled durable-consume projection sink boundary.
* It models how a future production call site would record an after-success-only consume receipt.
* It does not implement a real persistent replay backend.
* It does not implement a real durable consume backend.
* It does not add RocksDB/file/schema/migration/storage-format changes.
* It does not add wire/schema/marker/sequence/trust-bundle changes.
* It does not write authority markers.
* It does not write trust-bundle sequence files.
* It does not call Run 070.
* It does not mutate `LivePqcTrustState`.
* It does not perform a real trust swap.
* It does not evict sessions.
* It does not implement a real production mutation engine.
* It does not implement a real governance execution engine.
* It does not implement a real on-chain governance proof verifier.
* It does not add KMS/HSM/RemoteSigner backend.
* It does not enable MainNet governance.
* It does not enable MainNet peer-driven apply.
* It does not implement validator-set rotation.
* The fixture sink mutates only modeled in-memory receipt state.
* Run 246 pipeline success is required before any sink receipt can be recorded.
* `ConsumeReceiptRecorded` is required before any modeled receipt-recorded state.
* Every non-success pipeline outcome produces no sink invocation and no receipt.
* Failed receipt record, rollback, rollback-failed, ambiguous windows, unavailable production/MainNet paths, rejected replay states, and unsupported actions never consume.
* Rejected sink paths are non-mutating.
* Full C4 remains OPEN.
* C5 remains OPEN.

Contradiction handling:

* Crosscheck Run 248 against Runs 050–247.
* If no contradiction is found, add a “no Run 248 contradiction” entry to `docs/whitepaper/contradiction.md`.
* If any contradiction is found, do not hide it. Record it explicitly with the exact file/function/doc reference and recommend the smallest safe fix.

Strict non-goals:

* Do not implement a real persistent replay backend.
* Do not implement a real durable consume backend.
* Do not implement a real production mutation engine.
* Do not wire production runtime mutation.
* Do not call Run 070.
* Do not mutate `LivePqcTrustState`.
* Do not perform a real trust swap.
* Do not evict sessions.
* Do not write sequence files.
* Do not write authority markers.
* Do not implement a real governance execution engine.
* Do not implement a real on-chain governance proof verifier.
* Do not implement validator-set rotation.
* Do not add RocksDB/file/schema/migration/storage-format changes.
* Do not change wire/schema/marker/sequence/trust-bundle formats.
* Do not enable MainNet governance.
* Do not enable MainNet peer-driven apply.
* Do not add autonomous apply or apply-on-receipt.
* Do not let peer majority satisfy authority.
* Do not weaken any fail-closed behavior from Runs 070 or 130–247.

Expected verdict:

Partial-positive / source-test positive.

Run 248 should close the source/test modeled durable-consume projection sink gap only. It must not claim Full C4 or C5 closure.

Final response format:

1. Exact verdict.
2. Files changed.
3. Tests run and results.
4. Security invariants preserved.
5. Honest limitations.
6. C4/C5 status.
7. Suggested Run 249 next step.
