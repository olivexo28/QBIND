Run 363 — Public DevNet abuse/DoS per-peer message-rate runtime override source/test boundary

Continue QBIND after accepted Run 362.

Current state:
Run 362 is accepted as PARTIAL-POSITIVE / public-DevNet-abuse-DoS-runtime positive.

Run 362 delivered:
- runtime-owned connection-rate limiter state;
- live `p2p_tcp` accept-loop integration;
- `qbind_p2p_connection_rate_drop_total`;
- hidden/devnet-only connection-rate CLI/config surface;
- release-binary helper/harness evidence;
- default behavior preserved;
- M12 kept Yellow/Partial because per-peer message-rate runtime override is not wired.

Current public DevNet readiness:
- Green: M1, M2, M3 same-host scope, M5, M10, M11, M16–M20.
- Yellow/Partial: M4, M6, M7–M9, M12, M13–M15.
- M4 remains launch-blocking.
- Public DevNet remains NOT launch-ready.
- Full C4 OPEN.
- C5 OPEN.
- MainNet authority rotation/revocation Red.

Primary objective:
Close the remaining M12 source/runtime gap at source/test level by wiring operator-configurable per-peer message-rate thresholds into the live per-peer `PeerRateLimiter` path.

Run 363 should make the already-validated Run 362 hidden/devnet-only flags:

- `--p2p-max-messages-per-second`
- `--p2p-burst-allowance`

actually affect the live per-peer inbound message-rate limiter used by `AsyncPeerManager` / the current peer-message path.

Run 363 is source/test only. Release-binary evidence and any M12 Green decision are deferred to Run 364.

Strict scope:
Source/test runtime-plumbing only.

No public DevNet launch.
No live seed-node deployment.
No bootnode deployment.
No faucet.
No RPC gateway.
No explorer.
No status page.
No MainNet enablement.
No TestNet readiness claim.
No C4 closure.
No C5 closure.
No authority-lifecycle runtime wiring.
No validator-set mutation.
No production epoch transition.
No execution-sink write.
No P2P wire-format change.
No peer-admission weakening.
No trust-bundle behavior weakening.
No default-open public port.
No M4 Green.
No M6 Green.
No M12 Green in Run 363 unless release-binary evidence is also produced, which is not the target.

Required investigation:
Inspect:

- `crates/qbind-node/src/public_devnet_abuse_dos_config.rs`
- `crates/qbind-node/src/public_devnet_abuse_dos_runtime.rs`
- `crates/qbind-node/src/peer_rate_limiter.rs`
- `crates/qbind-node/src/async_peer_manager.rs`
- `crates/qbind-node/src/p2p_node_builder.rs`
- `crates/qbind-node/src/p2p_tcp.rs`
- `crates/qbind-node/src/cli.rs`
- `crates/qbind-node/src/main.rs`
- `crates/qbind-node/src/metrics.rs`
- Run 361 and Run 362 tests/evidence
- `docs/release/public-devnet/p2p/ABUSE_DOS_POSTURE.md`
- `docs/release/public-devnet/p2p/VERIFY.md`
- `docs/release/QBIND_PUBLIC_DEVNET_READINESS_CRITERIA.md`

Required implementation:
1. Add or extend runtime config plumbing so the live `PeerRateLimiter` can be constructed from validated per-peer thresholds.

2. Preserve exact default behavior:
   - no flags → `PeerRateLimiter::with_defaults()`;
   - default max messages/sec remains `1000`;
   - default burst remains `100`;
   - no behavior change unless operator explicitly configures values.

3. Wire the validated per-peer message-rate values from the Run 362 abuse/DoS runtime config into the live peer-manager path.

4. Ensure invalid values fail closed before runtime starts:
   - zero max messages/sec rejected;
   - unsafe/unbounded thresholds rejected;
   - malformed config rejected;
   - MainNet refused absent production policy;
   - wrong environment rejected where represented.

5. Keep the connection-rate limiter from Run 362 unchanged except for any necessary composition with the same runtime config object.

6. Do not add new public CLI flags unless unavoidable. Prefer reusing the existing hidden/devnet-only flags from Run 362.

7. Add tests proving the previously inert per-peer flags now affect the live limiter construction path.

Required tests:
Add:

`crates/qbind-node/tests/run_363_public_devnet_per_peer_message_rate_runtime_tests.rs`

Cover at minimum:

1. default config uses `PeerRateLimiter::with_defaults()`;
2. default values remain `1000` msg/s + `100` burst;
3. explicit custom `--p2p-max-messages-per-second` accepted;
4. explicit custom `--p2p-burst-allowance` accepted;
5. custom values reach the live peer-manager limiter construction path;
6. under-budget messages are allowed;
7. over-budget messages are dropped;
8. refill behavior remains deterministic;
9. burst behavior remains deterministic;
10. connection-rate limiter remains independent;
11. connection-rate drop metric still increments only for connection-rate refusal, not message-rate drops unless a separate metric already exists;
12. invalid zero max messages/sec rejected;
13. unsafe/unbounded message-rate rejected;
14. MainNet refused;
15. defaults unchanged when no flags are present;
16. hidden flags absent from `--help`;
17. real hidden flags parse;
18. invented flags rejected;
19. no P2P wire-format change;
20. no peer-admission weakening;
21. no trust-bundle behavior change;
22. no `LivePqcTrustState` mutation;
23. no sequence/marker write;
24. no validator-set mutation;
25. no epoch transition;
26. no Run 070 path;
27. public DevNet not launch-ready;
28. M4 remains Yellow;
29. M12 remains Yellow/Partial pending Run 364 release-binary evidence;
30. all existing Run 361 and Run 362 tests remain green.

Required docs:
Add:

`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_363.md`

Update narrowly:

- `docs/release/QBIND_PUBLIC_DEVNET_READINESS_CRITERIA.md`
- `docs/release/public-devnet/p2p/ABUSE_DOS_POSTURE.md`
- `docs/release/public-devnet/p2p/VERIFY.md`
- `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
- `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
- `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
- `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
- `docs/whitepaper/contradiction.md`

Docs must say:
- Run 363 wires per-peer message-rate runtime override at source/test level.
- Release-binary evidence is deferred to Run 364.
- M12 is not Green until Run 364 release-binary evidence proves both connection-rate and per-peer message-rate runtime configurability.
- M4 remains Yellow/launch-blocking.
- Public DevNet remains NOT launch-ready.
- Full C4 remains OPEN.
- C5 remains OPEN.

Required commands:
Run and record:

- `cargo build -p qbind-node --lib`
- `cargo test -p qbind-node --test run_363_public_devnet_per_peer_message_rate_runtime_tests`
- `cargo test -p qbind-node --test run_362_public_devnet_abuse_dos_runtime_tests`
- `cargo test -p qbind-node --test run_361_public_devnet_abuse_dos_hardening_tests`
- `cargo test -p qbind-node --lib`

If any target differs, record the exact substitution.

Security:
Run secret scanning over all changed files.

CodeQL:
Because this run changes Rust source, invoke CodeQL if available. If skipped, unavailable, timed out, or database-too-large, record the exact result and do not call it clean.

Readiness matrix rules:
- M12 may move Yellow/Partial → Yellow/stronger if source/test runtime override lands.
- M12 must not move Green until release-binary evidence lands in Run 364.
- Do not alter M4 unless live seed/bootnode reachability evidence lands.
- Do not alter M6 unless stable identity-generation support lands.
- Do not alter M7–M9/M13–M15 unless genuinely closed.

Expected verdict:
PASS / public-DevNet-per-peer-message-rate runtime source-test positive if the per-peer runtime override wiring, tests, docs, security scan, CodeQL provenance, and matrix updates land.

Partial-positive if the API/model lands but live peer-manager construction still cannot consume the configured thresholds.

Final response format:
1. Exact verdict.
2. Files changed.
3. Per-peer message-rate runtime wiring summary.
4. Abuse/DoS config integration.
5. PeerRateLimiter construction behavior.
6. Default compatibility.
7. CLI/config surface.
8. Accepted tests.
9. Rejection/fail-closed tests.
10. Runtime/non-mutation evidence.
11. Readiness matrix delta for M12.
12. Current public DevNet readiness status.
13. Remaining public DevNet blockers.
14. Public TestNet blockers.
15. MainNet blockers.
16. C4/C5 status.
17. Tests run.
18. Security scans.
19. CodeQL.
20. Provenance.
21. Honest limitations.
22. Suggested Run 364 next step.