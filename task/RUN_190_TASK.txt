Task: Run 190 — Source/Test Authority Custody Metadata Carrying and Production Call-Site Wiring

Current accepted state:

* Run 188: source/test KMS-HSM authority-custody boundary landed:

  * `pqc_authority_custody.rs`;
  * `AuthorityCustodyClass::{FixtureLocalKey, LocalOperatorKey, RemoteSigner, Kms, Hsm, Unknown}`;
  * `AuthorityCustodyPolicy::{Disabled, FixtureOnly, DevnetLocalAllowed, TestnetLocalAllowed, ProductionCustodyRequired, MainnetProductionCustodyRequired}`;
  * `AuthorityCustodyAttestation`;
  * typed `AuthorityCustodyValidationOutcome`;
  * pure `validate_authority_custody_attestation`;
  * pure `validate_lifecycle_governance_and_custody`;
  * named MainNet / peer-majority / local-operator refusal helpers;
  * 48 source tests covering A1–A8 / R1–R29.
* Run 189: release-binary custody-boundary evidence is now fully closed:

  * real `target/release/qbind-node` preserves default `AuthorityCustodyPolicy::Disabled`;
  * no custody selector is exposed at the binary surface;
  * release-built helper exercises the full Run 188 A1–A8 / R1–R29 corpus through production library symbols;
  * fixture/local custody remains DevNet/TestNet evidence-only;
  * fixture/local custody cannot satisfy MainNet production custody;
  * RemoteSigner / KMS / HSM placeholders fail closed as unavailable;
  * MainNet peer-driven apply remains refused;
  * no real KMS/HSM/cloud-KMS/PKCS#11/remote-signer backend exists;
  * no governance execution;
  * no real on-chain proof verification;
  * no validator-set rotation;
  * full C4 remains open;
  * C5 remains open.
* Current limitation:
  Authority custody metadata is not yet carried through production v2 sidecar / peer-candidate / marker-decision contexts, and no production call site routes custody validation into the lifecycle + governance preflight path.

Objective:
Add source/test support for carrying typed authority-custody attestation metadata through production payload/context paths and routing it into production v2 marker-decision preflight composition.

This run must make custody metadata available to production source call-site contexts at source/test level, while preserving default disabled/fail-closed behavior and MainNet refusal.

Release-binary custody-metadata evidence is deferred to Run 191.

Strict scope:

* Source/test only.
* No release-binary harness in this run.
* No real KMS implementation.
* No real HSM implementation.
* No cloud KMS integration.
* No PKCS#11 integration.
* No remote signer production integration.
* No MainNet peer-driven apply enablement.
* No governance execution engine.
* No real on-chain proof verifier.
* No validator-set rotation.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* Prefer additive optional custody metadata carrier.
* Preserve legacy/no-custody payload compatibility.
* No authority-marker schema change.
* No sequence-file schema change.
* No trust-bundle core schema change unless a hard blocker is found and documented.
* Do not weaken Runs 070, 130–189.
* Do not claim full C4 or C5 closure.

Required payload/carrying design:
Add an additive, optional custody metadata carrier to the production payload/context layer used by v2 authority marker-decision surfaces.

Preferred shape:

* Add optional JSON sibling field beside v2 ratification sidecar material, such as:
  `authority_custody_attestation`
* Or use an equivalent additive field that does not break strict v2 parsing.
* Follow the Run 167 / Run 184 sibling-extraction pattern:

  * extract the optional custody sibling before strict v2 sidecar parse;
  * old no-custody sidecars remain byte/parse compatible;
  * malformed custody sibling fails closed;
  * absent custody sibling maps to a typed absent/unavailable status.

Required source behavior:

1. Define a wire/context representation for custody metadata, preferably:

   * `AuthorityCustodyAttestationWire`
   * `AuthorityCustodyLoadStatus::{Absent, Available, Malformed}`
   * or equivalent.
2. Convert wire custody metadata into internal `AuthorityCustodyAttestation`.
3. Route parsed custody metadata into the Run 188 custody validator.
4. Add a source/test policy selection path for custody validation:

   * default `AuthorityCustodyPolicy::Disabled`;
   * fixture/local policies available only in tests/internal contexts;
   * production-required policies fail closed for KMS/HSM/RemoteSigner placeholders.
5. Wire custody validation into the production preflight composition used after Runs 182/184/185:

   * lifecycle validation;
   * governance proof validation;
   * OnChainGovernance proof validation where applicable;
   * custody validation.
6. Ensure malformed custody metadata fails closed before mutation.
7. Ensure no-custody payloads remain compatible under default Disabled.
8. Ensure validation-only surfaces remain non-mutating.
9. Ensure mutating surfaces still persist marker only after successful sequence commit.
10. Ensure MainNet peer-driven apply remains refused even with valid fixture/local custody metadata.

Required production/context surfaces:
At source/test level, cover custody metadata across:

1. reload-check validation-only;
2. reload-apply mutating preflight;
3. startup `--p2p-trust-bundle` preflight;
4. SIGHUP preflight;
5. local peer-candidate-check validation-only;
6. live inbound `0x05`, if existing envelope context can carry custody metadata additively;
7. peer-driven drain coordinator.

If any surface cannot carry custody metadata without a new wire schema beyond an additive optional field, document the exact blocker and keep it fail-closed.

Required accepted scenarios:
A1. no-custody payload remains compatible under default Disabled.

* Existing behavior preserved.

A2. DevNet fixture custody metadata carried through reload-check and accepted under explicit fixture policy.

* validation-only;
* no marker write;
* no sequence write.

A3. TestNet fixture custody metadata carried through reload-check and accepted under explicit fixture policy.

A4. DevNet local-operator custody metadata carried through reload-check and accepted under explicit DevNet local policy.

A5. TestNet local-operator custody metadata carried through reload-check and accepted under explicit TestNet local policy.

A6. DevNet fixture custody metadata carried through reload-apply preflight and accepted under explicit fixture policy.

* if full apply is exercised, sequence-before-marker ordering preserved.

A7. combined lifecycle + governance + fixture custody accepted for DevNet source/test production-context path.

A8. combined lifecycle + governance + local custody accepted for TestNet source/test production-context path where policy allows.

A9. GenesisBound / EmergencyCouncil / OnChainGovernance proof paths remain compatible when custody validation is Disabled.

A10. KMS/HSM/RemoteSigner placeholder metadata reaches custody validator and returns typed unavailable outcome.

Required rejection scenarios:
R1. custody metadata absent where custody policy requires custody rejected fail-closed.

R2. malformed custody metadata payload rejected.

R3. fixture custody rejected under production custody policy.

R4. local operator custody rejected under production custody policy.

R5. fixture custody rejected for MainNet.

R6. local operator custody rejected for MainNet.

R7. KMS placeholder rejected as unavailable.

R8. HSM placeholder rejected as unavailable.

R9. remote signer placeholder rejected as unavailable.

R10. unknown custody class rejected.

R11. wrong environment rejected.

R12. wrong chain rejected.

R13. wrong genesis rejected.

R14. wrong authority root rejected.

R15. wrong signing-key fingerprint rejected.

R16. wrong candidate digest rejected.

R17. wrong authority-domain sequence rejected.

R18. wrong lifecycle action rejected.

R19. missing custody attestation rejected.

R20. malformed custody attestation rejected.

R21. expired custody attestation rejected.

R22. custody key id mismatch rejected.

R23. unsupported custody suite rejected.

R24. custody valid but governance proof invalid rejected.

R25. governance proof valid but custody invalid rejected.

R26. lifecycle valid + governance valid + custody placeholder unavailable rejected.

R27. local operator config alone cannot satisfy MainNet production custody.

R28. peer majority / gossip count cannot satisfy custody.

R29. validation-only rejection produces no marker write, no sequence write, no live trust swap, no session eviction, and no Run 070 call.

R30. mutating rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

R31. invalid live `0x05` custody metadata candidate is not propagated, staged, or applied, if live surface is included.

R32. MainNet peer-driven apply remains refused even with custody metadata claiming KMS/HSM.

Required tests:
Add focused tests, preferably:

`crates/qbind-node/tests/run_190_authority_custody_payload_callsite_tests.rs`

Minimum coverage:

* A1–A10 where representable.
* R1–R32.
* serde/parse compatibility:

  * old no-custody payload parses;
  * custody-carrying payload parses;
  * malformed custody payload fails closed;
  * unsupported future custody schema version fails closed.
* source reachability:

  * custody metadata reaches production call-site context;
  * custody policy reaches marker-decision preflight;
  * `validate_authority_custody_attestation` reached outside helper/example modules;
  * `validate_lifecycle_governance_and_custody` reached outside helper/example modules.
* no-mutation invariants for validation-only and rejected mutating preflights.
* MainNet refusal invariants.

Required documentation:
Add:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_190.md`

Update:

* `docs/whitepaper/contradiction.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

Documentation must state:

* Run 190 is source/test authority-custody metadata carrying and production call-site wiring.
* No real KMS/HSM backend is implemented.
* KMS/HSM/RemoteSigner placeholders remain fail-closed.
* Fixture/local custody remains DevNet/TestNet evidence-only.
* Fixture/local custody cannot satisfy MainNet production custody.
* Existing no-custody payloads remain compatible under default Disabled.
* Release-binary custody-metadata evidence is deferred to Run 191.
* MainNet peer-driven apply remains refused.
* governance execution remains unimplemented.
* real on-chain proof verification remains unimplemented.
* validator-set rotation remains open.
* full C4 remains open.
* C5 remains open.

Validation commands:
Run at minimum:

* `cargo build -p qbind-node --lib`
* `cargo test -p qbind-node --test run_190_authority_custody_payload_callsite_tests`
* `cargo test -p qbind-node --test run_188_authority_custody_boundary_tests`
* `cargo test -p qbind-node --test run_186_onchain_governance_production_verifier_boundary_tests`
* `cargo test -p qbind-node --test run_184_onchain_governance_payload_carrying_tests`
* `cargo test -p qbind-node --test run_182_onchain_governance_production_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_180_onchain_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_178_onchain_governance_proof_tests`
* `cargo test -p qbind-node --test run_176_live_0x05_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_173_validation_only_governance_required_policy_tests`
* `cargo test -p qbind-node --test run_171_governance_required_policy_selector_tests`
* `cargo test -p qbind-node --test run_169_governance_proof_loader_surface_integration_tests`
* `cargo test -p qbind-node --test run_167_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_165_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_163_governance_authority_verifier_tests`
* `cargo test -p qbind-node --test run_161_lifecycle_marker_integration_tests`
* `cargo test -p qbind-node --test run_159_authority_signing_key_lifecycle_tests`
* `cargo test -p qbind-node --test run_157_unified_testnet_fixture_universe_tests`
* `cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests`
* `cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests`
* `cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests`
* `cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests`
* `cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests`
* `cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Acceptance criteria:
Run 190 is acceptable only if:

1. Authority custody metadata can be carried through production payload/context paths at source/test level.
2. Legacy no-custody payloads remain compatible.
3. Valid DevNet/TestNet fixture/local custody metadata reaches and passes production call-site context at source/test level where policy allows.
4. KMS/HSM/RemoteSigner placeholders reach the validator and fail closed as unavailable.
5. Invalid custody metadata fails closed.
6. Validation-only surfaces remain non-mutating.
7. Mutating surfaces preserve sequence-before-marker ordering.
8. MainNet peer-driven apply remains refused even with custody metadata claiming KMS/HSM.
9. Docs defer release-binary custody-metadata evidence to Run 191.
10. No real KMS/HSM / governance execution / real on-chain proof / validator-set rotation claim is made.
11. No full C4 or C5 closure is claimed.
