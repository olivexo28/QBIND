Task: Run 151 — Release-Binary Evidence for Explicit DevNet/TestNet Peer-Driven Apply Drain Trigger

Current accepted state:
- Run 144: peer-driven live apply safety gate specification landed.
- Run 145: non-applying staged peer-candidate queue scaffold landed.
- Run 146: staging queue wired into live inbound 0x05 path.
- Run 147: release-binary evidence for staged peer-candidate queue / arming behavior landed.
- Run 148: source/test peer-driven apply controller using existing Run 070 apply contract landed.
- Run 149: release-binary arming evidence for DevNet/TestNet peer-driven apply surface landed.
- Run 150: source/test explicit drain trigger from staged queue into Run 148 controller landed.
- Run 150 did not add release-binary trigger evidence and did not modify main.rs / cli.rs.
- Peer-driven apply remains DevNet/TestNet-only, explicit, disabled by default, and MainNet-refused.
- Full C4 remains open.
- C5 remains open.

Objective:
Produce release-binary evidence that a real target/release/qbind-node DevNet/TestNet peer-driven staged candidate can be explicitly drained into the Run 148 controller and applied through the existing Run 070 apply contract.

This run must prove the full release-binary path:

  live inbound 0x05 peer candidate
      → validation-only v2 acceptance
      → staged queue
      → explicit local drain trigger
      → Run 148 peer-driven apply controller
      → Run 070 apply contract
      → LivePqcTrustState swap
      → session eviction
      → Run 055 sequence commit
      → v2 authority marker persist after sequence commit

This is release-binary evidence. Do not change production runtime behavior unless the existing Run 150 source/test drain trigger is not reachable from the binary at all. If a tiny hidden DevNet/TestNet-only binary hook is required, implement the smallest possible operator-local hook and document it explicitly as Run 151 scope. MainNet must remain refused.

Strict scope:
- Release-binary evidence for DevNet/TestNet explicit drain only.
- No autonomous background drain.
- No automatic apply on receipt.
- No peer-majority authority.
- No MainNet enablement.
- No governance implementation.
- No KMS/HSM implementation.
- No signing-key rotation/revocation lifecycle.
- No wire-format change.
- No trust-bundle schema change.
- No ratification sidecar schema change.
- No marker schema change.
- No sequence-file schema change.
- No peer-candidate envelope schema change.
- Do not weaken Runs 070, 142, 143, 145, 146, 147, 148, 149, or 150.
- Do not claim full C4 or C5 closure.

Deliverables:
1. Add release-binary harness:
   scripts/devnet/run_151_peer_driven_apply_drain_release_binary.sh

2. Add evidence archive:
   docs/devnet/run_151_peer_driven_apply_drain_release_binary/

3. Add canonical evidence report:
   docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_151.md

4. Update narrowly:
   - docs/whitepaper/contradiction.md
   - docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md
   - docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md
   - docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md

5. Crosscheck all new/modified code and docs against existing design/spec. If contradictions or inconsistencies are found, record them in contradiction.md before finalizing.

Binary trigger rule:
Prefer using an existing Run 150 binary-reachable trigger if one exists.

If none exists, add the narrowest hidden, disabled-by-default DevNet/TestNet-only local operator trigger, for example:
- process-start one-shot drain flag, or
- local signal distinct from SIGHUP, or
- hidden test/evidence-only flag that drains once after staging is observed.

The trigger must:
- be disabled by default;
- be refused on MainNet;
- drain at most one candidate per trigger;
- be explicit/local/operator controlled;
- never run automatically on 0x05 receipt;
- be concurrency guarded;
- route through Run 150 drain and Run 148 controller;
- never call Run 070 directly from main.rs.

Required release-binary topology:
Use a real DevNet or TestNet multi-node setup, preferably N=3:

- V0: publisher of live 0x05 v2 candidate.
- V1: receiver with staging + explicit drain enabled.
- V2: observer / propagation check node if useful.

Use the same real PQC transport assumptions as previous evidence:
- real target/release/qbind-node;
- real P2P exchange;
- mutual-auth Required if existing harnesses use it;
- signed trust bundle;
- real ML-KEM-768 / ML-DSA-44 / ChaCha20-Poly1305 path;
- no active DummySig / DummyKem / DummyAead.

Captured evidence:
- qbind-node SHA-256 and ELF Build ID;
- helper SHA-256 and ELF Build ID;
- git commit hash;
- rustc/cargo versions;
- exact commands;
- node PIDs;
- stdout/stderr logs;
- metrics scrapes before/after;
- sequence-file JSON and SHA-256 before/after;
- authority-marker JSON and SHA-256 before/after;
- data-dir inventories;
- live trust fingerprint or active-root evidence before/after if exposed;
- session-eviction counters before/after;
- drain outcome logs;
- Run 070 ordering proof;
- v2 marker-after-sequence-commit ordering proof;
- denylist grep for out-of-scope behavior.

Required scenario matrix:

A1. DevNet explicit drain applies one valid staged v2 candidate
- V0 sends valid v2 candidate over live 0x05.
- V1 validates and stages candidate.
- Explicit local drain trigger fires.
- Expected:
  - candidate was staged before drain;
  - drain selected exactly one candidate;
  - Run 148 controller invoked;
  - Run 070 apply contract invoked;
  - LivePqcTrustState swapped;
  - sessions evicted according to existing Run 072/070 semantics;
  - Run 055 sequence commit succeeds;
  - v2 authority marker persists only after sequence commit;
  - outcome is Applied;
  - node remains running unless existing successful-drain semantics intentionally exit.

A2. TestNet explicit drain applies one valid staged v2 candidate, if TestNet fixtures are feasible
- Same as A1 but TestNet.
- MainNet behavior must not be inferred.
- If TestNet fixture setup is infeasible in this run, document exact blocker and keep DevNet strongest-positive / TestNet partial.

A3. empty queue explicit drain returns NoCandidate
- Trigger drain without staged candidate.
- Expected:
  - no mutation;
  - no sequence write;
  - no marker write;
  - no session eviction;
  - typed NoCandidate outcome visible.

A4. disabled policy refuses drain
- Candidate may be staged or inbound may be valid.
- Drain policy disabled.
- Expected:
  - drain refused;
  - no mutation;
  - no sequence write;
  - no marker write;
  - no eviction.

A5. MainNet refusal
- Attempt to arm or run drain on MainNet.
- Expected:
  - binary refuses before P2P/apply or drain returns MainNetRefused;
  - no mutation;
  - no sequence write;
  - no marker write;
  - no peer-driven apply.

A6. duplicate candidate cannot double-apply
- Stage same candidate twice or trigger drain twice.
- Expected:
  - one apply maximum;
  - second trigger returns NoCandidate / AlreadyApplied / deduped result;
  - no duplicate sequence write;
  - no duplicate marker write;
  - no double session eviction.

A7. deterministic highest-sequence selection
- Stage multiple valid candidates.
- Expected:
  - highest authority-domain sequence selected;
  - tie-break by lexicographically smallest fingerprint_hex if equal sequence;
  - selected candidate is applied;
  - lower candidate remains staged or is superseded according to Run 150 policy.

R1. lower-sequence candidate cannot drain
- Candidate is lower than local persisted authority marker.
- Expected:
  - rejected before apply;
  - no LivePqcTrustState swap;
  - no session eviction;
  - no sequence write;
  - no marker write.

R2. same-sequence different-digest candidate cannot drain
- Expected:
  - equivocation/conflict reason visible;
  - no mutation.

R3. bad-signature candidate cannot drain
- Expected:
  - Run 130 verifier failure visible;
  - no mutation.

R4. wrong-domain candidate cannot drain
- wrong environment, wrong chain, or wrong genesis.
- Expected:
  - wrong-domain reason visible;
  - no mutation.

R5. ambiguous v1+v2 candidate cannot drain
- Expected:
  - ambiguity refusal visible;
  - no mutation.

R6. expired staged candidate cannot drain
- Candidate expires before drain.
- Expected:
  - CandidateExpired or equivalent visible;
  - no mutation.

R7. concurrency guard prevents double drain
- Attempt two drain triggers close together.
- Expected:
  - one enters drain;
  - second returns AlreadyInProgress or equivalent;
  - no double apply;
  - no double sequence write;
  - no double marker write;
  - no double eviction.

R8. forced apply validation failure before swap, if feasible
- Candidate reaches drain but fails controller/apply validation before swap.
- Expected:
  - no live trust swap;
  - no session eviction;
  - no sequence write;
  - no marker write.
- If infeasible on release binary without source fault injection, document and cite Run 150 source/test coverage.

R9. forced eviction failure / sequence commit failure / marker persist failure, if feasible
- Exercise only if honest release-binary fault setup is possible.
- Expected:
  - existing Run 070 rollback/fatal semantics preserved;
  - no marker persist unless sequence commit succeeded;
  - fatal/operator-actionable outcome on marker persist failure after commit.
- If infeasible on release binary, document as release-binary-infeasible and cite Run 150/148 source-test coverage.

R10. propagation-only behavior unchanged
- With propagation enabled:
  - valid candidate may rebroadcast only after validation if existing policy allows;
  - invalid candidate never rebroadcasts;
  - propagation does not imply apply.
- With propagation disabled:
  - no rebroadcast.

Required ordering proof for accepted apply:
For every accepted apply scenario, prove by ordered logs or deterministic evidence:

1. live 0x05 candidate received;
2. v2 validation-only accepted;
3. candidate staged;
4. explicit drain triggered;
5. Run 148 controller invoked;
6. Run 070 apply ordering:
   validate → snapshot previous → swap → evict_sessions → commit_sequence
7. sequence commit succeeds;
8. v2 marker persists after sequence commit;
9. applied outcome emitted.

Required negative invariants:
For every reject/no-op scenario:
- no LivePqcTrustState swap;
- no sequence write;
- no authority-marker write;
- no session eviction;
- no Run 070 apply call unless intentionally testing pre-swap failure;
- no SIGHUP outcome;
- no reload-apply outcome;
- no startup mutation path accidentally selected;
- no snapshot/restore path selected;
- no peer-majority authority;
- no governance claim;
- no KMS/HSM claim;
- no MainNet apply;
- no fallback to --p2p-trusted-root;
- no active DummySig / DummyKem / DummyAead.

Required validation commands:
Run at minimum:
- cargo build --release -p qbind-node --bin qbind-node
- cargo build --release -p qbind-node --example <needed helper(s)>
- bash scripts/devnet/run_151_peer_driven_apply_drain_release_binary.sh
- cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests
- cargo test -p qbind-node --test run_148_peer_driven_apply_controller_tests
- cargo test -p qbind-node --test run_146_live_inbound_0x05_staging_tests
- cargo test -p qbind-node --test run_145_peer_candidate_staging_tests
- cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests
- cargo test -p qbind-node --test run_088_pqc_peer_candidate_propagation_tests
- cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests
- cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests
- cargo test -p qbind-node --lib pqc_authority
- cargo test -p qbind-node --lib

If exact target names differ, locate the nearest existing Run 145/146/148/150/142/088 targets and document exact commands/results.

Documentation requirements:
docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_151.md must state:
- Run 151 is release-binary evidence for the explicit drain surface.
- Whether any tiny hidden binary hook was added, and why.
- No autonomous background apply exists.
- No automatic apply on receipt exists.
- MainNet remains refused.
- Governance remains unimplemented.
- KMS/HSM remains unimplemented.
- Signing-key rotation/revocation lifecycle remains open.
- TestNet evidence is present or explicitly deferred with reason.
- Full C4 remains open.
- C5 remains open.

Acceptance criteria:
Run 151 is acceptable only if:
1. real target/release/qbind-node is used;
2. real live 0x05 candidate staging precedes drain;
3. explicit local drain trigger applies at most one valid staged candidate;
4. accepted apply routes through Run 150 drain, Run 148 controller, and Run 070 apply contract;
5. sequence commit happens before v2 marker persistence;
6. session eviction occurs according to Run 070/072 semantics;
7. reject/no-op scenarios produce no mutation;
8. MainNet is refused;
9. no autonomous peer-driven apply exists;
10. no peer-majority/governance/KMS-HSM claim is made;
11. evidence archive is complete;
12. docs are updated narrowly;
13. no full C4 or C5 closure is claimed.