RUN 422 — GENESIS-BOUND CONSENSUS AUTHORITY AND STANDALONE-BINARY ACTIVATION

Repository: `olivexo28/QBIND`
Expected baseline: `main` at `5c2f9381`; resolve and record the complete SHA.
Run type: production implementation and source/test evidence for explicit DevNet authority activation.
Successor: Run 423, configured-authority standalone release-binary adversarial evidence.

This is the complete Run 422 coding-agent task. Execute it in the actual QBIND repository. The baseline below comes from the completed Run 421 workflow; inspect current source before choosing implementation details.

Work on the task branch assigned by the execution environment. Commit and push only that branch. Do not change `main`, open a PR, amend commits, force-push, rewrite history, or merge unrelated work. If the environment assigns a different branch during continuation, record the actual branch, starting commit, and ancestry accurately. Do not claim that an environment-created branch is the original branch. Do not manually copy completed agent output onto `main`.

## 1. Canonical starting state

Run 420 implemented configured-context Proposal/Vote signing, signature verification, and suite checks, then corrected the `verification_ctx=None` bypass. `ConsensusVerificationPolicy::Required` is the production default. `LocalFixtureUnsigned` is restricted to fixture/test construction and must remain unavailable through the production CLI.

Run 421 established partial-positive standalone release-binary evidence for unavailable-authority rejection. Its runtime/tooling commit was `85bb3a73e6fe2686c074cee5c0f15ff3cf1d2103`; its evidence commit was `451241828c8a411b04219beb7801545b0f934f7d`. The final `main` baseline `5c2f9381` additionally restored the six inadvertently regenerated historical Run 419 archive files. Resolve every abbreviated SHA rather than inventing its full form.

Preserve Run 421’s classification:

```text
RESULT=PARTIAL-POSITIVE-FOR-F3-F4-F8-UNAVAILABLE-AUTHORITY-FAIL-CLOSED-RELEASE-BINARY-EVIDENCE
CONFIGURED_AUTHORITY_RELEASE_BINARY_EVIDENCE=NOT-CAPTURED
SECURITY_POSTURE=RS1-OPEN / PUBLIC-DEVNET-NO-GO
```

S2–S6 established inbound unavailable-authority rejection in the tested topology. S7/S8 did not directly establish outbound suppression on the standalone binary; that evidence remains partial. Run 420 code/tests remain the direct evidence for those suppression helpers.

F3/F4/F8 are not fully closed in production. Verify their exact canonical definitions in the Run 417 audit and reconciliation; do not silently rename F8 or equate suite enforcement with the entirety of its finding. F1/F2/F5/F7 remain unresolved. F6 remains code/test plus partial runtime evidence from Run 419.

RS1 stays OPEN/launch-blocking; C4/C5 stay OPEN; M4 stays Yellow/launch-blocking; M6 stays Yellow/Partial; S5/S7 stay Yellow; public DevNet remains NO-GO. No readiness item moves Green. No TestNet/MainNet readiness claim. No `devnet-seeds.live.json`. Candidate seed status and reachability evidence stay unchanged.

## 2. Exact objective and completion boundary

Enable the normal standalone `qbind-node` production code path to construct an immutable, validated consensus verification/signing context from explicit, genesis-bound DevNet authority and an operator-supplied local signing key.

The context must contain the canonical validator membership, one authorized public key and suite per validator, a policy-approved cryptographic backend registry, the correct chain/configuration identity, and a matching local signer. Only after all components validate may Proposal/Vote processing use an active context.

Required outcomes:

* Valid explicit DevNet authority activates the production constructor and allows configured Proposal/Vote signing and verification in source/integration tests.
* Absent optional DevNet activation retains `Required` plus unavailable authority, with the Run 421 rejection/suppression behavior.
* Explicitly requested but invalid activation exits startup nonzero; it must not silently downgrade to `None` or unsigned operation.
* Strict TestNet/MainNet configurations reject missing or invalid mandatory authority. This task supplies no real TestNet/MainNet custody material and authorizes no external deployment.
* Production Proposal/Vote authenticity remains distinct from transport identity, governance authority, full consensus safety, and readiness.

Passing only a fixture constructor, creating an unused module, or constructing `Some` in a test while `main` still bypasses it is insufficient for a positive activation verdict. Source tests must exercise the same validated constructor used by `main`. Release compilation and parser/startup smoke checks do not constitute configured-authority adversarial release-binary evidence; that is Run 423.

## 3. Trust model and authority separation

Maintain three separate roles:

* KEMTLS roots/leaves authorize transport sessions.
* Trust-bundle ratification roots authorize their existing governance/trust operations.
* Consensus validator keys authorize the exact canonical consensus messages allowed for that role.

Neither a transport certificate, NodeId, peer address, validator index, shared suite number, nor successful KEMTLS session may establish consensus signing authority. Do not reinterpret the existing bundle-signing key registry as consensus authority. Reuse parsing or crypto code only with explicit role separation.

Trust assumptions must be stated: the operator accepts a specific network/genesis identity through the existing trusted bootstrap procedure; all validators use the same accepted authority commitment; the OS and signer custody boundary protect private keys. Hashing an arbitrary supplied genesis does not independently establish that it is the intended network’s genesis.

Bind authority to the accepted genesis identity and existing data-directory network identity. If the current bootstrap has an expected hash/pin, enforce it. Reject a supplied genesis or authority inconsistent with initialized storage. Do not silently reinitialize storage, reinterpret an existing chain, or invent a trusted hash from attacker-controlled input.

Private keys stay in operator custody. They must never be included in genesis, public authority files, snapshots, source control, evidence, command-line values, logs, or metrics. Runtime activation must not generate or auto-enroll keys. Synthetic material is allowed only for explicitly isolated tests and temporary DevNet verification.

## 4. Required preflight and source investigation

Record actual branch, full HEAD, worktree status, fetched `origin/main`, merge base, and both relevant ancestry counts. Distinguish the Run 421 baseline from any subsequent task-file commit. Do not assume equivalence from commit subjects or manufacture ancestry in a shallow clone.

Read applicable `AGENTS.md` and the complete relevant documents:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_417.md` through `RUN_421.md` and their evidence archives;
* `docs/protocol/QBIND_FOUNDATIONAL_RUNTIME_SECURITY_RECONCILIATION.md`;
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`, if that is its current path; resolve the actual path;
* `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`;
* `docs/release/QBIND_PUBLIC_DEVNET_READINESS_CRITERIA.md`;
* `docs/release/public-devnet/BLOCKER_REGISTER.md` and `LAUNCH_GO_NO_GO.md`;
* `docs/whitepaper/contradiction.md`;
* current genesis, consensus signing, suite policy, keystore, and operator specifications.

Trace actual source, including:

* genesis schema/versioning, canonical encoding/hash, environment identity, and persistence checks;
* validator membership, voting weights, ID/index conversions, and genesis-to-runtime mapping;
* any existing consensus public-key registry and its commitment;
* authority registry introduced for bundle ratification, without conflating roles;
* `TimeoutVerificationContext` and every `Some`/`None` constructor and consumer;
* Run 420 Proposal/Vote preimages, verifiers, policy, signers, and outbound/self-injection paths;
* local signer-keystore configuration and loading abstractions;
* suite/backend registration, governance policy, and key-provider lookup;
* `main.rs` and `binary_consensus_loop.rs` production construction;
* Timeout/NewView signing and verification affected by context availability;
* replay, snapshot restore, persisted genesis identity, and reconfiguration;
* all dummy, fixture, implicit authority, and compatibility paths.

Create a source trace with stable symbols and repository-relative paths. State which facts are observed in source, demonstrated by tests, or still unverified.

## 5. Critical shared-context check: Timeout/NewView

Before wiring `Some` into `TimeoutVerificationContext`, enumerate every consumer whose behavior changes when that Option becomes `Some`. Earlier code shows that context availability can control Timeout/NewView behavior as well as Proposal/Vote behavior. Do not assume an activation limited to Proposal/Vote just because the new loader was written for those messages.

Keep F5/F7 remediation outside this run. Reuse immutable cryptographic components where appropriate, but separate activation policy/capabilities if sharing the Option would silently activate, disable, or reconfigure another message family. A narrowly scoped separation of context construction is permitted when necessary for this boundary; a consensus redesign is not.

Requirements:

* Preserve every previously enforced Timeout/NewView check.
* Do not disable existing verified modes to make new Proposal/Vote tests pass.
* Test and document which message classes gain signing capability and which retain their prior policy.
* Do not infer F5 closure from a shared context becoming available.
* If safely isolating the change requires a broader protocol change, complete the safe provider/loader work, retain production activation unavailable, and report a precise PARTIAL result and dependency.

## 6. Implementation route

**Route A:** reuse an existing canonical consensus-authority source if it already binds the complete validator key set to accepted genesis. Extend and wire that boundary rather than create competing registries or formats.

**Route B:** only if Route A is unavailable, add a versioned consensus-authority registry committed by canonical genesis.

For either route:

* Exactly one authorized consensus key and suite per active validator in the supported configuration.
* Exact agreement with canonical membership and its ID/index mapping; CLI validator count or peer lists cannot override it.
* Reject duplicate IDs, duplicate/ambiguous signing identities, missing validators, extra validators, invalid ranges, and truncating conversions.
* Validate key encoding, length, suite, backend availability, and environment/protocol suite policy.
* Canonically order entries and bind each role, ID, suite, public key, and applicable configuration field.
* Bound input sizes and entry counts before allocation/cryptographic work. Reject ambiguous or duplicate serialized fields using the format’s strict parsing rules.
* Include authority in canonical genesis hashing and explain the schema/version/domain behavior.
* Preserve old-format semantics explicitly; old genesis without authority must never be reinterpreted as an activated authority set.
* A sidecar may carry bytes only if the accepted genesis cryptographically commits its complete contents/meaning. An uncommitted local override cannot define network authority.

Do not claim network isolation solely because the registry contains a chain label. Trace what Proposal/Vote signing preimages actually bind. Test mismatched chain/genesis/configuration identities. If different accepted genesis authorities can share the same signed domain and authorize replay, resolve it through an existing safe binding or document the exact versioned protocol change required and keep activation partial. Do not claim a changed preimage has been implemented when only the loader hash changed.

## 7. Local signer loading and custody

Reuse the existing signer/keystore format and suite interfaces where suitable. Add a safe path option only if no existing configuration field is sufficient. This task permits necessary configuration wiring, not arbitrary signing APIs.

Validate in this order where practical:

1. Authorized environment, chain/genesis, validator record, and suite policy.
2. Explicit local signer path and supported file/container format.
3. File type, ownership/permission policy, and bounded size.
4. Backend-supported private-key decoding.
5. Local identity/suite/public-key correspondence with accepted authority.
6. Signer self-test where required.

On Unix accept owner-readable private files with no group/other access, such as `0400` or `0600`, and reject special permission bits and unsafe file types. Validate the opened descriptor to avoid check-then-open races. Use existing no-follow/symlink protections where available. Document platform and parent-directory trust limits accurately; do not claim custody hardening unsupported by code.

Prove the loaded secret matches the canonical public key. If the backend cannot derive the public key, verify a fixed-purpose, domain-separated startup challenge signed by the loaded secret against the canonical public key. A companion public-key file is not proof by itself. The self-test must not sign arbitrary user input and must not be reusable as a Proposal/Vote/Timeout authorization.

Reject wrong identity, wrong suite, malformed keys, backend failure, or self-test failure. Do not clone secrets unnecessarily; use existing zeroization/secret wrappers and scrub temporary decoded buffers where supported. Error/Debug paths must not expose secret bytes or private paths. Public-key comparison need not be described as constant-time secret processing.

No auto-generation, raw private-key CLI/env values, network key downloads, arbitrary per-peer authority overrides, dummy signer, classical fallback, or unsigned fallback.

## 8. Startup policy and immutable construction

DevNet activation requires explicit accepted genesis/authority and explicit matching local signer. No activation input may preserve the Run 421 unavailable-authority mode, with `Required` still selected and a bounded truthful diagnostic. Partial/conflicting activation inputs must fail startup rather than downgrade silently.

TestNet/MainNet must enforce existing strict genesis and authority rules, reject known fixture modes and DevNet configuration, and fail on missing mandatory authority. This run supplies no genuine TestNet/MainNet operator material or launch permission. A mathematically valid key does not reveal whether it was generated for a test: enforce provenance through trusted environment/genesis selection and explicit mode separation; do not claim a magic test-key detector.

Build one validated immutable authority snapshot, then the signer and context. Publish it only after membership, chain/genesis identity, suite policy, backend availability, signer correspondence, and shared-context scope all validate. Validator activation cannot expose a half-initialized `Some` or silently take a verify-only role when a signer was requested.

Run authority validation before starting consensus/P2P tasks capable of processing traffic. Failures must leave no partially active services. Do not re-read mutable authority files for each message or trust changing filesystem contents after validation.

Absent/invalid authority must never select `LocalFixtureUnsigned`. Keep fixture selection unavailable from the production CLI and production constructors.

## 9. Signing, verification, and suite enforcement

Reuse Run 420 canonical chain-aware Proposal/Vote signing and verification APIs. Trace that the exact content being broadcast/self-injected is the content verified. The provider must bind signer ID, authorized key, suite, membership, and configuration consistently.

Before outbound Proposal/Vote emission, check local signer correspondence and selected suite policy. Signing failure suppresses emission/self-injection; it does not become an emitted-success or engine-acceptance event.

For authenticated ingress preserve F6 before consensus signature verification. Correct F6 identity with invalid consensus signature rejects. A valid signature with wrong authenticated sender rejects at F6. Verification success is distinct from engine acceptance and commitment.

A registered backend alone does not make a suite authorized. Enforce both active policy and validator key-record suite, without default coercion, cross-suite retry, or classical fallback. If the existing validator-key interface lacks policy information, extend the smallest safe boundary rather than equate registration with approval.

Retain resource limits and bounded queues. CPU-heavy signature work must not introduce unbounded tasks, acceptance-before-verification, unsafe caching, or speculative mutation.

## 10. Genesis-static lifecycle and restart boundaries

Constrain this run to the authority lifetime safely supported by current source. Do not implement key rotation, revocation, or membership redesign.

For a genesis-static registry, define its valid configuration and enforce that it cannot authorize a changed validator set/epoch with stale keys. Use an existing validated configuration/epoch source only. If no safe transition is supported, fail closed before activating the changed configuration and report the limitation; an operational note alone is not a stale-key guard.

Restart/snapshot/replay must verify compatible chain/genesis and authority identity before signing or ingesting authenticated messages. Do not transplant keys onto incompatible persistent state or regenerate identity when a check fails. Preserve existing crash-recovery and equivocation protections; do not claim protection against double-signing across clones/restarts that the implementation does not provide.

Do not consume `current_epoch` for trust-bundle activation. Preserve fail-closed `CurrentEpochUnavailable`; invent no synthetic epoch. Do not enable peer-driven trust apply/propagation, trust swaps, session eviction, or governance mutation as a side effect. C4/C5 remain OPEN.

## 11. Observability and typed errors

Use bounded typed outcomes for authority missing, incomplete membership, unknown/duplicate validator, ambiguous key, malformed key, unsupported/disallowed suite, backend unavailable, signer unavailable, insecure permissions, signer identity/suite/key mismatch, self-test failure, genesis/data identity mismatch, and context construction failure.

Reuse suitable existing errors instead of duplicating taxonomies. Separate startup activation rejection from inbound message rejection and signer failure. Distinguish context active from signature accepted, engine accepted, and committed.

Use bounded messages and fixed labels. Never log secret material, complete keys/signatures, payloads, or private paths. Avoid repeated rejection logs becoming an unbounded synchronous logging bottleneck. Record any remaining rate-limit/DoS limitation honestly.

## 12. Required test matrix

Tests must exercise behavior and shared production constructors, not just source-string presence. Record exact target names and assertions.

### A. Authority commitment and parsing

* Deterministic canonical encoding/hash regardless of permitted input ordering.
* Every security-relevant registry field changes the commitment when changed.
* Complete membership; correct ID/index mapping; duplicate/missing/extra/overflow IDs reject.
* Malformed, oversized, noncanonical, ambiguous fields reject as applicable.
* Duplicate or ambiguous signing-key assignments reject.
* Wrong chain/genesis/environment; incompatible persisted genesis reject.
* Unsupported/disallowed suite and missing backend reject.
* Bundle/transport key records cannot substitute for consensus authority.

### B. Signer loading

* Valid matching signer loads and signs a startup challenge/message through the real backend.
* Missing, unreadable, empty, truncated, oversized, malformed, wrong-suite keys reject.
* Wrong owner/permissions/type/symlink cases reject where supported; limits explicit.
* Wrong validator/public key; invalid companion/self-test reject.
* No secret/path leakage, implicit generation, fallback, or private-key CLI argument.

### C. Activation and environment behavior

* Valid explicit DevNet material reaches the same constructor invoked by `main` and yields an active immutable context.
* Absent DevNet activation remains `None` plus `Required`.
* Every explicit invalid/partial activation fails nonzero before P2P/consensus service start.
* TestNet/MainNet missing authority, wrong-environment genesis, and fixture policy reject.
* No partially initialized context, wrong membership, or CLI override escapes.
* Shared Timeout/NewView consumers preserve the documented scope; prior checks are not weakened.

### D. Proposal/Vote authenticity

* Real supported post-quantum backend positive tests; accepting mocks alone are insufficient.
* Outbound Proposal and Vote use the configured authorized signer and round-trip through the inbound verifier.
* Missing/empty/truncated/bit-flipped signature; altered content, signer, chain, view, block/payload/justify commitment reject as applicable.
* Wrong validator key, unknown suite, policy-disallowed registered suite, key/suite mismatch reject without fallback.
* F6 mismatch rejects before crypto; correct F6 plus invalid signature rejects at crypto.
* Signing backend failure, mismatched signer, or unavailable signer prevents queueing, broadcast, and self-injection.
* All affected delivered/engine/QC/aggregation/reconfiguration/outbound state remains unchanged on rejection. Where an internal state is not exposed, instrument test state rather than overclaim from unrelated counters.

### E. Lifetime and robustness

* Context snapshot does not change if source files change after validation.
* Parallel verification cannot mix keys, suites, membership, or results.
* Restart/restore with mismatched authority identity fails closed.
* Changed configuration cannot keep using genesis-static authority without an authorized transition.
* Malformed input cannot panic; signer failure does not create a busy loop; bounded queues remain bounded.
* Fixture bypass is unreachable through all current production constructors and parser routes.

## 13. Required validation and historical evidence preservation

Run focused tests above, affected crate checks/tests, affected release builds, and existing Run 420 verifier/loop/reachability regressions. Include relevant genesis, crypto, wire, serde, hash, node, consensus, authority, storage, and runtime tests. Run workspace tests if feasible and record actual completion/exit status.

Run the Run 421 unavailable-authority harness without activation inputs; retain its partial verdict. Run the Run 419 F6 harness and Run 418 harness with their original security expectations. Run 417 audit and Runs 404/405/410–415 for reconciliation and integrity. Do not edit harness expectations to hide a security regression caused by new activation behavior.

Find exact current script paths; use `bash` for scripts without executable bits. For a tracked CRLF shell script, create a unique temporary LF copy under the same `scripts/devnet` directory, run it through `bash`, record its exit, and remove it. Do not mass-normalize tracked files or use a `/tmp` copy that breaks `BASH_SOURCE`-relative paths.

Before old harnesses run, record a clean baseline of their tracked archives. Preserve their new regression outputs separately as Run 422 evidence where needed. Restore only known harness-generated historical changes, using the recorded pre-run versions; never discard pre-existing operator work. At completion prove zero unintended diff to the historical Run 417–421 archives, particularly the canonical Run 419 archive at the accepted baseline. Hash-valid regenerated history is still unintended history replacement.

Also record:

* rustfmt for new/changed Rust; repo-wide format drift separately, with exact scope;
* clippy for affected crates; no claim of clean analysis when warnings/errors remain;
* ordinary and CRLF-aware diff checks, with exit codes;
* changed JSON schema/semantic validation;
* evidence checksums and package-integrity anchor verification;
* secret/private-material scan, with actual tool/command and result;
* production CLI denylist for arbitrary signing/injection, fixture, authority override, and verification-disable controls;
* CodeQL/security analysis if available.

Missing execute permission or CRLF is an invocation issue to resolve using the established method, not a reason to label the test passed. Capture pipeline command exit statuses correctly; a successful `tee`/`grep` is not a successful test.

CodeQL status must be completed with findings, completed with zero findings, or skipped/incomplete with a reason. Database-too-large, timeout, or unexecuted analysis provides no zero-alert conclusion. Separate analysis of a final docs-only commit from analysis of the production code changes. No claim that a future CI run already passed.

## 14. Performance and operations

Measure startup registry validation, signer load/self-test, context construction, provider memory, and Proposal/Vote verification where practical. Separate debug test wall time from microbenchmarks and release throughput. State validator count, message sizes, concurrency, build profile, and measurement limits.

Analyze synchronous file/crypto work, async runtime blocking, signer locks, invalid-signature floods, rejection-log amplification, and queue backpressure. Do not assert O(1) total startup validation, zero allocation, or negligible overhead without supporting scope and evidence. Preserve correctness over throughput optimizations. No public load test or deployment is authorized.

## 15. Strict non-goals

No external deployment, durable seeds, firewall changes, public endpoints, or launch enablement. No real TestNet/MainNet authority provisioning. No transport-root rotation/revocation, governance activation, trust-bundle mutation, F6 redesign, or F1/F2/F5/F7 remediation. No consensus/QC/pacemaker/leader/snapshot/fast-sync redesign. No classical cryptographic fallback. No arbitrary signing or forged-message interface. No unrelated refactor or dependency churn.

Production source/configuration changes necessary for this DevNet authority boundary are authorized. They do not authorize weakening other gates or enabling modeled governance mutators. If a hard dependency requires broader protocol work, finish safe bounded work and report a partial verdict with the exact remaining dependency.

## 16. Deliverables and documentation

Create `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_422.md` and the archive:

```text
docs/devnet/run_422_consensus_authority_activation/
```

Archive files:

* `README.md`
* `summary.txt`
* `source_trace.txt`
* `authority_model.txt`
* `configuration_matrix.txt`
* `adversarial_matrix.txt`
* `verification_context_trace.txt`
* `shared_context_impact.txt`
* `commands.txt`
* `test_results.txt`
* `performance.txt`
* `SHA256SUMS.txt`
* `.gitignore`

`SHA256SUMS` must cover every intended publish-safe archive file except itself and `.gitignore`. Finalize content before calculating hashes. New artifacts use LF. Preserve existing line-ending conventions for narrow edits; do not change historical bytes merely to make checksum tools pass.

Update the actual current protocol/genesis/operator documents, foundational runtime-security reconciliation, readiness criteria, blocker register, artifact index, operator verification map, and `docs/whitepaper/contradiction.md`. Update package-integrity manifest hashes/byte sizes only for legitimately changed anchors, then run its verifiers.

Record exact authority source and operator trust assumption, accepted genesis pin/data identity behavior, suite policy, key custody, activation matrix, shared-context impact, configuration lifetime, failure modes, and remaining blockers. A narrow successor note may link earlier canonical reports to Run 422; historical verdicts and archives remain intact.

Contradiction ledger entries must distinguish source/test-positive activation from configured-authority adversarial runtime evidence still absent. Correct newly stale present-tense authority-unavailable prose without rewriting historical Run 420/421 results. Do not claim no contradiction until comparing current prose with source and actual tests.

Do not commit private keys, custody material, secrets, credentials, raw unbounded logs/metrics, temp data, or private paths. Synthetic public test vectors may be committed only when necessary and clearly identified. Prefer generating fresh private test material during tests over embedding usable private-key bytes. Archive `.gitignore` must exclude private/generated material.

## 17. Verdict rules

Positive source/test activation requires all of:

* accepted genesis genuinely commits the canonical authority under the stated bootstrap trust model;
* exact membership and suite policy are enforced;
* local signer matching is enforced;
* `main` invokes the validated constructor and can obtain `Some` from explicit valid DevNet material;
* invalid activation fails closed before network/consensus activity;
* Proposal/Vote positive/adversarial tests use the real supported crypto backend;
* TestNet/MainNet strict-negative and production-bypass tests pass;
* shared context has no undocumented out-of-scope activation;
* stale/incompatible authority cannot continue across unsupported configuration changes.

Successful source/test labels:

```text
RESULT=POSITIVE-FOR-DEVNET-CONSENSUS-AUTHORITY-ACTIVATION-CODE-TEST
F3_STATUS=CONFIGURED-AUTHORITY-PRODUCTION-PATH-CODE-TEST-POSITIVE
F4_STATUS=CONFIGURED-AUTHORITY-PRODUCTION-PATH-CODE-TEST-POSITIVE
F8_STATUS=CONFIGURED-AUTHORITY-PRODUCTION-PATH-CODE-TEST-POSITIVE
CONFIGURED_AUTHORITY_RELEASE_BINARY_EVIDENCE=NOT-YET-CAPTURED
AUTHORITY_SCOPE=GENESIS-BOUND-DEVNET
SECURITY_POSTURE=RS1-OPEN / PUBLIC-DEVNET-NO-GO
```

Apply finding labels only after reconciling their complete canonical scope. If a finding remains partly uncovered, mark it partial rather than claiming all of F3/F4/F8 from a subset.

Use `PARTIAL-POSITIVE` when safe validation/providers exist but production activation, canonical binding, or another required dependency remains unavailable. Missing required validation must be identified separately and cannot be silently included in a PASS.

Use `NEGATIVE` if an uncommitted override authorizes keys, signer mismatch activates, fixture/dummy authority escapes, unavailable context becomes permissive, transport/governance authority substitutes for consensus keys, invalid activation falls back unsigned, or a strict environment accepts a prohibited fixture mode.

No verdict closes RS1, C4, or C5; moves readiness items Green; establishes public DevNet GO; claims configured-authority release-binary adversarial evidence; or claims unimplemented rotation, revocation, replay, double-signing, or full consensus-safety protection.

## 18. Commit, ancestry, and cleanup discipline

Prefer focused normal commits for reproducing tests/source trace, authority commitment, signer loading, context wiring, adversarial/regression tests, and evidence. If implementation changes after validation, rerun affected checks. Avoid making every logical substep a separate commit merely to satisfy a count.

Report the full starting/base/implementation/final SHAs from Git. Do not invent an abbreviated SHA expansion or assume original task branches are ancestors after a squashed/manual import. For divergent history compare relevant trees and record evidence; do not claim ancestry from matching content.

The final evidence commit cannot embed its own hash without changing that hash. Record the tested implementation SHA in artifacts and the actual final documentation SHA in the final report, or use a later explicitly identified report commit. Do not fabricate self-referential provenance.

Before final completion remove temporary services, LF copies, generated secret/data/log material; restore historical evidence overwritten by harnesses; confirm `task/warning.txt` unchanged; delete `task/RUN_422_TASK.txt` if present and completed, otherwise accurately report its absence. Preserve unrelated task files. Commit and push intended work to the actual task branch; record a clean worktree and exact pushed tip. Open no PR.

## 19. Required final report

Provide:

1. Exact verdict and labels; completion, validation, and runtime-evidence axes separately.
2. Actual branch, full base/tested/final SHAs, ancestry, commit list, and deviations.
3. Exact changed-file list/diff statistics, distinguishing full Run 422 from later corrections.
4. Route selected; canonical authority source, hash/schema/version/reset impact.
5. Bootstrap trust assumption, expected genesis/data identity checks, membership mapping.
6. Authority-domain separation; signer custody/load/self-test and suite/backend policy.
7. Main-to-validated-context construction trace and environment activation matrix.
8. Timeout/NewView/shared-context impact with tests and unchanged open findings.
9. Genesis-static configuration/restart limits and stale-authority rejection.
10. Proposal/Vote positive/adversarial/non-mutation/bypass results and test counts without double-counting overlapping test filters.
11. Startup/crypto/DoS performance observations and unmeasured limits.
12. Every validation command, tested commit, exit code, failures/skips/timeouts, CodeQL status.
13. Formatting/line-ending checks; archive and package integrity; privacy scan.
14. Historical archive preservation, cleanup/task-file results, clean/pushed status.
15. F3/F4/F8 scope and unchanged F1/F2/F5/F6/F7, RS1/C4/C5, M4/M6/S5/S7 and NO-GO posture.
16. Exact outstanding dependencies and confirmation no PR was opened.

Recommend, but do not implement, Run 423 for configured-authority standalone release-binary adversarial evidence only if activation is code/test-positive and reviewed. If activation remains partial, recommend the smallest missing implementation step first. Do not start the successor or claim public DevNet launch readiness.
