Task: Run 185 — Release-Binary OnChainGovernance Payload-Carrying Accepted-Proof Evidence

Current accepted state:

* Run 178: source/test typed OnChainGovernance proof verifier landed.
* Run 180: source/test OnChainGovernance surface module and hidden fixture selector landed.
* Run 181: release-binary selector/boundary evidence landed, but wrappers were helper-exercised.
* Run 182: source/test wired all seven production v2 marker-decision call sites to the Run 180 wrappers.
* Run 183: release-binary call-site boundary evidence landed, but production payloads still reached call sites with `proof: None`.
* Run 184: source/test OnChainGovernance proof payload/carrying landed:

  * `pqc_onchain_governance_payload_carrying`;
  * additive optional `onchain_governance_proof` sibling on v2 sidecar JSON;
  * sibling extracted before strict v2 parse, following the Run 167 pattern;
  * parsed into `OnChainGovernanceProofWire`;
  * routed into the proof slot of all seven Run 182 named call-site entries;
  * malformed-payload fail-closed shim;
  * 44 tests covering A1–A7 / R1–R26;
  * default Disabled preserved;
  * MainNet peer-driven apply remains refused.
* Run 184 is source/test only.
* Release-binary accepted-proof evidence is deferred to Run 185.
* governance execution remains unimplemented.
* real on-chain proof verification remains unimplemented.
* KMS/HSM remains unimplemented.
* validator-set rotation remains open.
* full C4 remains open.
* C5 remains open.

Objective:
Produce release-binary evidence that real `target/release/qbind-node` production payload/context paths can carry typed OnChainGovernance fixture proof material into the Run 182 production call-site wrappers and accept valid fixture proofs under the hidden `AllowFixtureSourceTest` selector.

This run must prove:

1. legacy/no-proof payloads remain compatible under default Disabled behavior;
2. hidden CLI/env selector enables `AllowFixtureSourceTest`;
3. valid DevNet/TestNet fixture OnChainGovernance proof reaches production call-site verification through real payload/context paths;
4. valid proof is accepted on at least one validation-only and one mutating release-binary production surface;
5. invalid proof payloads fail closed;
6. accepted mutating cases preserve sequence-before-marker ordering;
7. rejected cases produce no mutation;
8. MainNet peer-driven apply remains refused even with valid fixture proof and selector enabled;
9. no real governance execution or real on-chain proof verification is claimed.

Strict scope:

* Release-binary evidence only.
* Use real `target/release/qbind-node`.
* Use release-built helper(s) to mint OnChainGovernance proof-carrying payloads.
* No production source change unless a tiny harness-only fix is required.
* No MainNet peer-driven apply enablement.
* No real governance execution engine.
* No real on-chain proof verifier.
* No bridge/light-client integration.
* No KMS/HSM implementation.
* No validator-set rotation.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* No schema/wire changes beyond Run 184’s additive optional sibling.
* No authority-marker schema change.
* No sequence-file schema change.
* No trust-bundle core schema change.
* Do not weaken Runs 070, 130–184.
* Do not claim full C4 or C5 closure.

Required deliverables:

1. Add release-binary harness:
   `scripts/devnet/run_185_onchain_governance_payload_release_binary.sh`

2. Add evidence archive:
   `docs/devnet/run_185_onchain_governance_payload_release_binary/`

3. Add canonical evidence report:
   `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_185.md`

4. Update narrowly:

   * `docs/whitepaper/contradiction.md`
   * `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
   * `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
   * `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

5. Crosscheck all new/modified code and docs against existing design/spec. If contradictions or inconsistencies are found, record them in `contradiction.md` before finalizing.

Required production surfaces:
Exercise at minimum:

A. Validation-only:

* preferred: `--p2p-trust-bundle-reload-check`.

B. Mutating:

* preferred: process-start `--p2p-trust-bundle-reload-apply-path`.

If feasible, also include:

* local `--p2p-trust-bundle-peer-candidate-check`;
* live inbound `0x05`;
* startup `--p2p-trust-bundle`;
* SIGHUP;
* peer-driven drain coordinator.

Do not substitute helper-only evidence for the central accepted-proof claim.

Required accepted scenarios:

A1. default Disabled preserves legacy/no-proof payload compatibility where previously accepted

* No CLI flag and no env var.
* Old no-proof v2 sidecar.
* Expected:

  * real `qbind-node` preserves existing behavior;
  * no unexpected OnChainGovernance acceptance;
  * validation-only non-mutation preserved where applicable.

A2. CLI selector enables `AllowFixtureSourceTest` on reload-check and accepts valid DevNet OnChainGovernance Rotate proof payload

* Use `--p2p-trust-bundle-onchain-governance-fixture-allowed`.
* Expected:

  * hidden CLI selector observed;
  * `onchain_governance_proof` sibling loaded from payload;
  * `OnChainGovernanceProofWire` parsed;
  * proof routed into production call-site context;
  * `verify_onchain_governance_proof` reached through real reload-check path;
  * lifecycle validation accepts;
  * marker decision accepts;
  * no marker write;
  * no sequence write.

A3. env selector enables `AllowFixtureSourceTest` on reload-check and accepts valid DevNet OnChainGovernance Rotate proof payload

* Use `QBIND_P2P_TRUST_BUNDLE_ONCHAIN_GOVERNANCE_FIXTURE_ALLOWED=1`.
* Expected:

  * env selector observed;
  * valid proof accepted;
  * validation-only non-mutation preserved.

A4. CLI selector enables `AllowFixtureSourceTest` on reload-apply and accepts valid DevNet OnChainGovernance Rotate proof payload

* Expected:

  * proof payload parsed before marker decision;
  * OnChainGovernance verifier accepts before apply/mutation;
  * lifecycle validation accepts before apply/mutation;
  * mutating apply succeeds;
  * Run 055 sequence commit succeeds;
  * v2 marker persists strictly after sequence commit.

A5. env selector enables `AllowFixtureSourceTest` on reload-apply and accepts valid DevNet OnChainGovernance Rotate proof payload

* Expected:

  * same as A4 using env selector;
  * sequence-before-marker ordering preserved.

A6. TestNet fixture OnChainGovernance Rotate proof accepted through at least one validation-only production surface, if fixture-supported.

A7. local peer-candidate-check accepts valid OnChainGovernance proof payload under fixture selector, if feasible.

A8. live inbound `0x05` accepts valid OnChainGovernance proof payload under fixture selector, if feasible after Run 184 payload support.

* If not feasible in release harness, document exact boundary and cite Run 184 source/test coverage.

A9. GenesisBound and EmergencyCouncil behavior remains unchanged.

Required rejection scenarios:

R1. selector unset / false keeps Disabled and rejects OnChainGovernance fixture proof where proof is evaluated.

R2. malformed OnChainGovernance proof payload rejected.

R3. wrong environment rejected.

R4. wrong chain rejected.

R5. wrong genesis rejected.

R6. wrong authority root rejected.

R7. wrong governance domain rejected.

R8. wrong proposal digest rejected.

R9. wrong proposal outcome rejected.

R10. wrong lifecycle action rejected.

R11. wrong candidate digest rejected.

R12. wrong authority-domain sequence rejected.

R13. expired proof rejected.

R14. replayed governance decision rejected.

R15. quorum not met rejected.

R16. threshold not met rejected.

R17. invalid proof bytes rejected.

R18. unsupported proof suite rejected.

R19. local operator config rejected.

R20. peer-majority / gossip-count proof rejected.

R21. valid proof but lifecycle invalid rejected.

R22. lifecycle valid but proof invalid rejected.

R23. validation-only rejection produces no marker write, no sequence write, no live trust swap, no session eviction, and no Run 070 call.

R24. mutating rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

R25. invalid live `0x05` OnChainGovernance proof candidate is not propagated, staged, or applied, if live surface is included.

R26. MainNet peer-driven apply remains refused even with valid proof and selector enabled.

Required source/release reachability proof:
The report must include:

* source grep/call-site proof for:

  * `pqc_onchain_governance_payload_carrying`;
  * `onchain_governance_proof`;
  * `OnChainGovernanceProofWire`;
  * Run 182 named call-site entries;
  * Run 180 wrappers;
  * `verify_onchain_governance_proof`;
  * hidden CLI/env selector helpers.
* release-binary logs proving:

  * payload sibling is loaded;
  * proof reaches production call-site context;
  * selector disabled by default;
  * CLI selector enables fixture policy;
  * env selector enables fixture policy;
  * at least one validation-only and one mutating production surface accept valid fixture proof;
  * invalid proof payloads fail closed.

Required mutation proof for accepted mutating scenarios:
For every accepted mutating OnChainGovernance fixture payload scenario:

* selector activation occurs before payload parse;
* proof payload parse occurs before marker decision;
* OnChainGovernance fixture verification occurs before apply/mutation;
* lifecycle validation occurs before apply/mutation;
* Run 070 / relevant apply ordering preserved;
* Run 055 sequence commit succeeds;
* v2 marker persists strictly after sequence commit;
* marker JSON/SHA before/after captured;
* sequence JSON/SHA before/after captured.

Required no-mutation proof for rejected scenarios:
For every rejected scenario:

* binary exits non-zero or typed refusal according to surface;
* no Run 070 apply call;
* no live trust swap;
* no session eviction;
* no sequence write;
* no marker write;
* marker bytes unchanged;
* sequence bytes unchanged;
* no `.tmp` residue;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead.

Required denylist:
Across the run prove:

* no MainNet apply;
* no autonomous apply;
* no apply on receipt;
* no peer-majority authority;
* no real governance execution claim;
* no real on-chain governance proof claim;
* no KMS/HSM claim;
* no validator-set rotation claim;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead;
* no schema/wire/metric drift beyond Run 184 optional sibling;
* no marker write before sequence commit;
* no sequence write on validation-only surfaces;
* no marker write on validation-only surfaces.

Required captured metadata:

* qbind-node SHA-256 and ELF Build ID;
* helper SHA-256 and ELF Build ID;
* git commit hash;
* rustc/cargo versions;
* exact commands;
* stdout/stderr logs;
* per-scenario exit codes;
* fixture proof payload paths and hashes;
* marker/sequence JSON and SHA before/after where applicable;
* data-dir inventories;
* denylist grep results.

Validation commands:
Run at minimum:

* `cargo build --release -p qbind-node --bin qbind-node`
* `cargo build --release -p qbind-node --example <onchain-governance-payload-fixture-helper>`
* `bash scripts/devnet/run_185_onchain_governance_payload_release_binary.sh`
* `cargo test -p qbind-node --test run_184_onchain_governance_payload_carrying_tests`
* `cargo test -p qbind-node --test run_182_onchain_governance_production_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_180_onchain_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_178_onchain_governance_proof_tests`
* `cargo test -p qbind-node --test run_176_live_0x05_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_173_validation_only_governance_required_policy_tests`
* `cargo test -p qbind-node --test run_171_governance_required_policy_selector_tests`
* `cargo test -p qbind-node --test run_169_governance_proof_loader_surface_integration_tests`
* `cargo test -p qbind-node --test run_167_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_165_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_163_governance_authority_verifier_tests`
* `cargo test -p qbind-node --test run_161_lifecycle_marker_integration_tests`
* `cargo test -p qbind-node --test run_159_authority_signing_key_lifecycle_tests`
* `cargo test -p qbind-node --test run_157_unified_testnet_fixture_universe_tests`
* `cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests`
* `cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests`
* `cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests`
* `cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests`
* `cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests`
* `cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Documentation requirements:
`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_185.md` must state:

* Run 185 is release-binary OnChainGovernance proof payload accepted-proof evidence.
* Default remains `OnChainGovernanceProofPolicy::Disabled`.
* Hidden CLI/env selector enables `AllowFixtureSourceTest`.
* DevNet/TestNet fixture OnChainGovernance proof payloads can pass through real production payload/context paths where lifecycle/anti-rollback checks pass.
* Invalid fixture proof payloads fail closed.
* Production MainNet OnChainGovernance remains unsupported/fail-closed.
* MainNet peer-driven apply remains refused.
* governance execution remains unimplemented.
* real on-chain proof verification remains unimplemented.
* KMS/HSM remains unimplemented.
* validator-set rotation remains open.
* full C4 remains open.
* C5 remains open.

Acceptance criteria:
Run 185 is acceptable only if:

1. Real release binaries load OnChainGovernance proof payloads.
2. Default Disabled behavior remains fail-closed.
3. CLI selector and env selector both activate fixture policy.
4. Valid DevNet/TestNet fixture OnChainGovernance proof reaches and passes production call-site verification through real payload/context at least through one validation-only and one mutating surface.
5. Invalid proof payloads fail closed.
6. Rejected scenarios produce no mutation.
7. Accepted mutating scenarios preserve sequence-before-marker ordering.
8. MainNet peer-driven apply remains refused even with a valid fixture proof.
9. No real governance execution / real on-chain proof / KMS-HSM / validator-set rotation claim is made.
10. No full C4 or C5 closure is claimed.
