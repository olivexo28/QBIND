Task: Complete Run 185 Release-Binary Execution Closure

Current state:
Run 185 scaffolding is committed and pushed, but the canonical evidence archive still contains the placeholder summary.

Delivered so far:

* `crates/qbind-node/examples/run_185_onchain_governance_payload_release_binary_helper.rs`
* `scripts/devnet/run_185_onchain_governance_payload_release_binary.sh`
* `docs/devnet/run_185_onchain_governance_payload_release_binary/`
* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_185.md`
* Run 185 doc entries in:

  * `docs/whitepaper/contradiction.md`
  * `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
  * `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
  * `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

Blocking issue:
`docs/devnet/run_185_onchain_governance_payload_release_binary/summary.txt` still records:

`verdict: NOT-YET-RUN`

Therefore Run 185 is not release-binary-closed yet.

Objective:
Execute the Run 185 release-binary harness in a build environment, overwrite the placeholder summary with real verdicts, and update the canonical report only if the actual run differs from the current drafted claims.

Strict scope:

* Execution/evidence closure only.
* Do not add new production source.
* Do not change schema/wire/metric behavior.
* Do not add MainNet enablement.
* Do not implement governance execution.
* Do not implement real on-chain proof verification.
* Do not implement KMS/HSM.
* Do not implement validator-set rotation.
* Do not claim full C4 or C5 closure.

Required commands:
Run at minimum:

```bash
cargo build --release -p qbind-node --bin qbind-node
cargo build --release -p qbind-node --example run_179_onchain_governance_proof_release_binary_helper
cargo build --release -p qbind-node --example run_185_onchain_governance_payload_release_binary_helper
bash scripts/devnet/run_185_onchain_governance_payload_release_binary.sh
```

Then run the required regression slice recorded in `QBIND_DEVNET_EVIDENCE_RUN_185.md`, or if the harness already runs it, confirm the harness summary includes it.

Required evidence updates:

1. Ensure:
   `docs/devnet/run_185_onchain_governance_payload_release_binary/summary.txt`
   no longer says `NOT-YET-RUN`.

2. The summary must include:

   * real git commit;
   * qbind-node SHA-256;
   * qbind-node ELF Build ID;
   * helper SHA-256 / Build ID;
   * rustc/cargo versions;
   * real scenario pass/fail matrix;
   * final verdict.

3. Confirm the canonical report:
   `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_185.md`
   matches the actual harness result.

4. If any scenario fails or is skipped, update the report honestly:

   * mark partial-positive if needed;
   * identify the exact blocker;
   * do not claim accepted-proof closure if the real binary did not prove it.

Required acceptance checks:
Run 185 is acceptable only if:

1. the real harness was executed;
2. `summary.txt` contains a real verdict, not the placeholder;
3. real release binaries load OnChainGovernance proof payloads;
4. at least one validation-only and one mutating production surface are evidenced, unless a precise blocker is documented;
5. invalid proof payloads fail closed;
6. rejected cases produce no mutation;
7. accepted mutating cases preserve sequence-before-marker ordering;
8. MainNet peer-driven apply remains refused;
9. no real governance execution / real on-chain proof / KMS-HSM / validator-set rotation claim is made;
10. full C4 and C5 remain open.
