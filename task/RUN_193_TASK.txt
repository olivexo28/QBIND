Task: Complete Run 193 Release-Binary Evidence Closure

Current state:
Run 193 helper, harness, canonical report, archive scaffold, and doc updates are committed and pushed.

However, the uploaded archive summary still contains placeholder text:

`Run 193 — release-binary authority-custody policy selector evidence (placeholder)`

and says:

`This file is overwritten by scripts/devnet/run_193_authority_custody_policy_release_binary.sh on every successful run.`

Therefore Run 193 is not release-binary-closed yet.

Objective:
Execute the Run 193 release-binary harness in a build environment, overwrite the placeholder `summary.txt` with real build provenance and verdicts, and update the canonical report only if the actual run differs from the drafted claims.

Strict scope:

* Execution/evidence closure only.
* Do not add production source.
* Do not change CLI behavior.
* Do not change schema/wire/metric behavior.
* Do not enable MainNet peer-driven apply.
* Do not implement real KMS.
* Do not implement real HSM.
* Do not implement cloud KMS.
* Do not implement PKCS#11.
* Do not implement remote signer production integration.
* Do not implement governance execution.
* Do not implement real on-chain proof verification.
* Do not implement validator-set rotation.
* Do not claim full C4 or C5 closure.

Required commands:
Run at minimum:

```bash
cargo build --release -p qbind-node --bin qbind-node
cargo build --release -p qbind-node --example run_193_authority_custody_policy_release_binary_helper
bash scripts/devnet/run_193_authority_custody_policy_release_binary.sh
```

Then confirm the harness ran the required regression matrix, or run any missing regression commands manually and record them.

Required evidence updates:

1. Ensure:
   `docs/devnet/run_193_authority_custody_policy_release_binary/summary.txt`
   no longer contains:

   * `(placeholder)`
   * `This file is overwritten`
   * `evidence directory has a stable tracked file`

2. The real summary must include:

   * git commit;
   * qbind-node SHA-256;
   * qbind-node ELF Build ID;
   * helper SHA-256;
   * helper ELF Build ID;
   * rustc/cargo versions;
   * real S1–S8 binary scenario verdicts;
   * helper A1–A12 / R1–R29 verdicts;
   * regression verdicts;
   * denylist result;
   * final verdict.

3. Confirm:
   `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_193.md`
   matches the actual harness result.

4. If any scenario fails or is skipped, update the report honestly:

   * mark partial-positive if needed;
   * identify exact blocker;
   * do not claim release-binary closure if the real binary did not prove it.

Required acceptance checks:
Run 193 is acceptable only if:

1. the real harness was executed;
2. `summary.txt` contains a real verdict, not the placeholder;
3. real release binaries exercise the hidden authority-custody policy selector;
4. default Disabled remains proven;
5. CLI selector and env selector both work;
6. CLI-over-env precedence is proven;
7. invalid selector values fail closed;
8. valid DevNet/TestNet fixture/local custody metadata passes only where selected policy allows;
9. KMS/HSM/RemoteSigner placeholders remain fail-closed;
10. MainNet peer-driven apply remains refused even with MainnetProductionCustodyRequired and metadata claiming KMS/HSM;
11. rejected custody-policy cases produce no mutation;
12. existing governance proof paths remain compatible;
13. no real KMS/HSM / governance execution / real on-chain proof / validator-set rotation claim is made;
14. full C4 and C5 remain open.
