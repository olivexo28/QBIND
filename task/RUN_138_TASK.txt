Task: Run 138 — Wire v2 ratification + v2 authority marker support into SIGHUP live-reload mutating surface

Current accepted state:
- Run 133: strongest-positive release-binary evidence for v2 validation-only reload-check and local peer-candidate-check.
- Run 134: strongest-positive source/test wiring for v2 process-start reload-apply.
- Run 135: strongest-positive release-binary evidence for v2 process-start reload-apply.
- Run 136: strongest-positive source/test wiring for startup --p2p-trust-bundle v2 marker support.
- Run 137: strongest-positive release-binary evidence for startup --p2p-trust-bundle v2 marker support.

Objective:
Wire v2 bundle-signing-key ratification and v2 authority-marker acceptance/persistence into the existing SIGHUP live-reload mutating surface.

This must mirror the existing v1 SIGHUP marker path and the v2 reload-apply/startup patterns from Runs 134 and 136.

Strict scope:
- Source/test wiring only.
- No release-binary evidence harness in this run.
- No live inbound 0x05 v2 apply.
- No peer-driven live apply.
- No snapshot/restore v2 marker wiring.
- No signing-key rotation/revocation lifecycle.
- No KMS/HSM implementation.
- No MainNet governance implementation.
- No CLI flag changes unless absolutely required by existing SIGHUP surface compatibility.
- No metric changes unless existing SIGHUP counters already cover the path.
- No trust-bundle wire/schema changes.
- No ratification sidecar schema changes.
- No marker schema changes.
- Do not weaken v1 SIGHUP behavior.
- Do not claim full C4 or C5 closure.

Implementation target:
The existing Run 074 SIGHUP live-reload path currently supports the v1 ratification/marker discipline. Add v2 dispatch so that when the configured SIGHUP reload candidate is accompanied by a v2 ratification sidecar:

1. The v2 path is selected explicitly.
2. The v1-only SIGHUP ratification gate is skipped only for v2 sidecars.
3. The Run 130 v2 verifier is invoked before any live mutation.
4. The Run 134/Run 136 v2 marker decision helper is invoked before live mutation.
5. The marker decision must use:
   AuthorityStateUpdateSource::SighupLiveReload
   or the exact existing enum variant used by the v1 SIGHUP marker path.
6. The existing Run 070 ordering remains strict:
   validate → snapshot previous → swap live trust → evict sessions → commit_sequence
7. v2 marker persistence must occur only after the existing sequence commit boundary succeeds.
8. If validation, v2 verifier, marker decision, swap, eviction, or sequence commit fails, the marker must not be written.
9. If marker persistence fails after successful sequence commit, the outcome must be fatal or operator-actionable in the same crash-window discipline used by Run 119/121/131/134/136.
10. The running node must not continue silently with an inconsistent marker state.

Required source behavior:
- Add a narrow helper analogous to:
  preflight_run_136_v2_marker_decision_for_startup(...)
  but scoped to SIGHUP live reload.
- The helper must perform zero disk writes.
- The helper must compose:
  - Run 130 v2 ratification verifier.
  - Run 134/136 v2 marker decision helper.
  - SIGHUP-specific AuthorityStateUpdateSource audit tag.
- The helper must return a decision object that is persisted only after the live-reload sequence commit succeeds.

Required tests:
Add focused tests that mirror the Run 121 v1 SIGHUP matrix and the Run 134/136 v2 marker matrix.

At minimum cover:

A1. first accepted v2 SIGHUP live reload
- v2 verifier passes.
- v2 marker decision accepts first write.
- live apply succeeds.
- sequence commit succeeds.
- v2 marker persists after commit.
- persisted marker is V2.
- latest authority-domain sequence matches candidate.
- last_update_source is SIGHUP/live-reload specific.

A2. idempotent v2 SIGHUP
- existing v2 marker has same sequence/digest.
- live reload accepts or stages consistently with existing semantics.
- marker persistence is no-op.
- marker bytes unchanged.

A3. higher-sequence v2 SIGHUP
- existing v2 marker seq=N.
- candidate seq=N+1.
- accepted and persisted after sequence commit.
- marker advances to N+1.

A4. v1-to-v2 SIGHUP migration
- existing v1 marker.
- v2 SIGHUP candidate with higher authority-domain sequence.
- accepted as v2-after-v1 migration.
- marker becomes V2.
- audit tag records SIGHUP/live-reload source.

R1. lower-sequence v2 SIGHUP rejection
- existing v2 marker seq=N.
- candidate seq<N.
- reject before live mutation.
- no swap.
- no eviction.
- no sequence commit.
- no marker write.
- existing marker bytes unchanged.

R2. same-sequence different-digest v2 SIGHUP rejection
- reject before live mutation.
- no sequence commit.
- no marker write.
- marker bytes unchanged.

R3. bad-signature v2 sidecar rejection
- Run 130 verifier fails.
- no live mutation.
- no session eviction.
- no sequence write.
- no marker write.

R4. wrong-domain v2 sidecar rejection
- wrong chain/environment/genesis/domain binding.
- reject before live mutation.
- no sequence write.
- no marker write.

R5. sequence-commit failure after successful live swap/eviction
- marker must not be written.
- live state rollback behavior must remain exactly the existing Run 070 behavior.
- if rollback succeeds, non-fatal invalid/live-reload failure semantics remain unchanged.
- if rollback fails after commit failure, fatal variant remains preserved.

R6. marker-persist failure after successful sequence commit
- must surface operator-actionable fatal/crash-window behavior.
- do not silently continue.
- document stale-by-one replay behavior if this matches existing Run 119/121/131 semantics.

R7. v1 SIGHUP regression
- v1 sidecar still uses existing v1 path.
- no v2 path selected.
- v1 marker behavior unchanged.

R8. no-sidecar / legacy DevNet SIGHUP regression
- existing behavior unchanged.
- no v2 marker written.

Required documentation:
Update:
- docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_138.md
- docs/whitepaper/contradiction.md
- docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md
- docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md

The Run 138 evidence report must state:
- Run 138 is source/test wiring only.
- No release-binary evidence was captured.
- SIGHUP live-reload v2 source/test wiring is now landed.
- Release-binary SIGHUP v2 evidence remains open for Run 139.
- Snapshot/restore v2 marker wiring remains open.
- live inbound 0x05 v2 remains open.
- peer-driven live apply remains open.
- KMS/HSM remains open.
- MainNet governance remains open.
- full C4 remains open.
- C5 remains open.

Validation commands:
Run at minimum:
- cargo build -p qbind-node --lib
- cargo test -p qbind-node --lib pqc_authority_marker_acceptance::
- cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests
- cargo test -p qbind-node --test run_119_authority_marker_acceptance_tests
- cargo test -p qbind-node --test run_121_sighup_authority_marker_tests
  or the actual existing v1 SIGHUP marker test target
- cargo test -p qbind-node --test run_074_live_reload_tests
  or the actual existing SIGHUP live-reload test target
- cargo test -p qbind-node --lib

If target names differ, locate the closest existing Run 074/121 SIGHUP live-reload tests and run them. Document exact commands and results.

Acceptance criteria:
Run 138 is acceptable only if:
1. v2 SIGHUP live-reload dispatch is wired.
2. v2 verifier runs before live mutation.
3. v2 marker decision runs before live mutation but performs no writes.
4. v2 marker persistence runs only after successful sequence commit.
5. all rejected v2 SIGHUP candidates produce no live mutation, no eviction, no sequence write, and no marker write.
6. v1 SIGHUP behavior is unchanged.
7. no runtime schema, CLI, metric, or wire-format drift occurs.
8. docs clearly state that release-binary evidence is deferred to Run 139.
9. contradiction.md is updated.
10. no full C4 or C5 closure is claimed.