Task: Run 187 — Release-Binary Production OnChainGovernance Verifier Boundary Evidence

Current accepted state:

* Run 178: source/test typed OnChainGovernance proof verifier landed.
* Run 184: source/test OnChainGovernance proof payload/carrying landed.
* Run 185: release-binary OnChainGovernance payload accepted-proof evidence landed for fixture proofs.
* Run 186: source/test production OnChainGovernance verifier boundary landed:

  * `pqc_onchain_governance_verifier.rs`;
  * `OnChainGovernanceVerifierKind::{Disabled, FixtureSourceTest, ProductionUnavailable, ProductionVerifierPlaceholder}`;
  * proof-class classifier;
  * verifier policy;
  * typed boundary outcome;
  * `OnChainGovernanceVerifier` trait;
  * four concrete verifier modes;
  * pure entry points and dispatcher;
  * explicit fail-closed MainNet refusal helper;
  * 44 tests covering A1–A7 / R1–R29;
  * full lib 1300 tests passing.
* Run 186 preserves:

  * no real on-chain verifier;
  * no MainNet enablement;
  * no wire/schema/marker/sequence change;
  * no KMS/HSM;
  * no governance execution;
  * no validator-set rotation;
  * full C4 open;
  * C5 open.
* Release-binary verifier-boundary evidence is deferred to Run 187.

Objective:
Produce release-binary evidence for the Run 186 production OnChainGovernance verifier boundary.

This run must prove on release binaries:

1. fixture proof verification remains DevNet/TestNet evidence-only;
2. production-class OnChainGovernance proof verification is fail-closed as unavailable;
3. fixture proofs cannot satisfy MainNet production governance authority;
4. MainNet peer-driven apply remains refused;
5. existing Run 185 fixture accepted-proof paths remain compatible;
6. no real governance execution or real on-chain proof verification is claimed.

Strict scope:

* Release-binary evidence only.
* Use real `target/release/qbind-node`.
* Use release-built helper(s) to exercise the Run 186 verifier boundary.
* No production source change unless a tiny harness-only fix is required.
* No MainNet peer-driven apply enablement.
* No real governance execution engine.
* No real on-chain proof verifier.
* No bridge/light-client integration.
* No KMS/HSM implementation.
* No validator-set rotation.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* No schema/wire changes.
* No authority-marker schema change.
* No sequence-file schema change.
* No trust-bundle core schema change.
* Do not weaken Runs 070, 130–186.
* Do not claim full C4 or C5 closure.

Required deliverables:

1. Add release-binary helper:
   `crates/qbind-node/examples/run_187_onchain_governance_verifier_boundary_release_binary_helper.rs`

2. Add release-binary harness:
   `scripts/devnet/run_187_onchain_governance_verifier_boundary_release_binary.sh`

3. Add evidence archive:
   `docs/devnet/run_187_onchain_governance_verifier_boundary_release_binary/`

4. Add canonical evidence report:
   `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_187.md`

5. Update narrowly:

   * `docs/whitepaper/contradiction.md`
   * `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
   * `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
   * `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

6. Crosscheck all new/modified code and docs against existing design/spec. If contradictions or inconsistencies are found, record them in `contradiction.md` before finalizing.

Required release-built helper matrix:
The helper must exercise the Run 186 verifier boundary in release mode.

Accepted / compatible cases:
A1. DevNet fixture OnChainGovernance Rotate proof accepted under fixture verifier policy.
A2. TestNet fixture OnChainGovernance Rotate proof accepted under fixture verifier policy.
A3. Existing Run 185 reload-check fixture proof path remains compatible.
A4. Existing Run 185 reload-apply fixture proof path remains compatible.
A5. GenesisBound governance proof behavior remains unchanged.
A6. EmergencyCouncil governance proof behavior remains unchanged.
A7. Production verifier boundary is callable and returns typed `ProductionVerifierUnavailable` for production-class proof.
A8. Disabled default remains fail-closed.

Required rejection cases:
R1. fixture proof rejected under Disabled policy.
R2. fixture proof rejected if presented as MainNet production proof.
R3. production-class proof rejected because production verifier is unavailable.
R4. production-class proof rejected on DevNet unless a future real production verifier policy exists.
R5. production-class proof rejected on TestNet unless a future real production verifier policy exists.
R6. wrong environment rejected.
R7. wrong chain rejected.
R8. wrong genesis rejected.
R9. wrong authority root rejected.
R10. wrong governance domain rejected.
R11. wrong proposal digest rejected.
R12. wrong proposal outcome rejected.
R13. wrong lifecycle action rejected.
R14. wrong candidate digest rejected.
R15. wrong authority-domain sequence rejected.
R16. expired proof rejected.
R17. replayed proof rejected.
R18. quorum not met rejected.
R19. threshold not met rejected.
R20. invalid proof bytes rejected.
R21. unsupported proof suite rejected.
R22. malformed production proof rejected.
R23. local operator config rejected.
R24. peer-majority / gossip-count proof rejected.
R25. valid fixture proof but lifecycle invalid rejected.
R26. lifecycle valid but production verifier unavailable rejected.
R27. MainNet peer-driven apply remains refused even if fixture proof or production-class proof is present.
R28. validation-only rejection remains non-mutating.
R29. mutating preflight rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

Required real binary checks:
Use real `target/release/qbind-node` to prove:

* MainNet peer-driven apply remains refused.
* Default production behavior does not enable production OnChainGovernance.
* Hidden fixture selector remains fixture-only and does not become MainNet production authority.
* Existing Run 185 accepted fixture payload behavior remains compatible if the harness can reuse those scenarios.
* No real governance execution claim is emitted.
* No KMS/HSM claim is emitted.
* No validator-set rotation claim is emitted.

Required reachability proof:
The report must include source grep/call-site proof for:

* `pqc_onchain_governance_verifier`;
* `OnChainGovernanceVerifierKind`;
* `ProductionUnavailable`;
* `ProductionVerifierPlaceholder`;
* `OnChainGovernanceVerifier`;
* `ProductionVerifierUnavailable` or equivalent typed outcome;
* MainNet refusal helper;
* boundary dispatch entry points.

Required mutation / no-mutation proof:
For rejected verifier-boundary scenarios:

* no Run 070 apply call;
* no live trust swap;
* no session eviction;
* no sequence write;
* no marker write;
* marker bytes unchanged where present;
* sequence bytes unchanged where present;
* no `.tmp` residue;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead.

For any accepted fixture mutating compatibility scenario:

* proof validation occurs before apply/mutation;
* lifecycle validation occurs before apply/mutation;
* Run 055 sequence commit succeeds;
* v2 marker persists strictly after sequence commit.

Required denylist:
Across the run prove:

* no MainNet apply;
* no autonomous apply;
* no apply on receipt;
* no peer-majority authority;
* no real governance execution claim;
* no real on-chain governance proof claim;
* no KMS/HSM claim;
* no validator-set rotation claim;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead;
* no schema/wire/metric drift;
* no marker write before sequence commit;
* no sequence write on validation-only surfaces;
* no marker write on validation-only surfaces.

Required captured metadata:

* qbind-node SHA-256 and ELF Build ID;
* helper SHA-256 and ELF Build ID;
* git commit hash;
* rustc/cargo versions;
* exact commands;
* stdout/stderr logs;
* per-scenario exit codes;
* fixture and production-class proof paths / hashes;
* marker/sequence JSON and SHA before/after where applicable;
* data-dir inventories;
* denylist grep results.

Validation commands:
Run at minimum:

* `cargo build --release -p qbind-node --bin qbind-node`
* `cargo build --release -p qbind-node --example run_187_onchain_governance_verifier_boundary_release_binary_helper`
* `bash scripts/devnet/run_187_onchain_governance_verifier_boundary_release_binary.sh`
* `cargo test -p qbind-node --test run_186_onchain_governance_production_verifier_boundary_tests`
* `cargo test -p qbind-node --test run_184_onchain_governance_payload_carrying_tests`
* `cargo test -p qbind-node --test run_182_onchain_governance_production_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_180_onchain_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_178_onchain_governance_proof_tests`
* `cargo test -p qbind-node --test run_176_live_0x05_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_173_validation_only_governance_required_policy_tests`
* `cargo test -p qbind-node --test run_171_governance_required_policy_selector_tests`
* `cargo test -p qbind-node --test run_169_governance_proof_loader_surface_integration_tests`
* `cargo test -p qbind-node --test run_167_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_165_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_163_governance_authority_verifier_tests`
* `cargo test -p qbind-node --test run_161_lifecycle_marker_integration_tests`
* `cargo test -p qbind-node --test run_159_authority_signing_key_lifecycle_tests`
* `cargo test -p qbind-node --test run_157_unified_testnet_fixture_universe_tests`
* `cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests`
* `cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests`
* `cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests`
* `cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests`
* `cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests`
* `cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Documentation requirements:
`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_187.md` must state:

* Run 187 is release-binary production OnChainGovernance verifier-boundary evidence.
* Fixture OnChainGovernance remains DevNet/TestNet evidence-only.
* Production-class OnChainGovernance proof verification remains unavailable/fail-closed.
* Fixture proof cannot satisfy MainNet production governance authority.
* Existing Run 185 fixture accepted-proof paths remain compatible.
* MainNet peer-driven apply remains refused.
* governance execution remains unimplemented.
* real on-chain proof verification remains unimplemented.
* bridge/light-client integration remains unimplemented.
* KMS/HSM remains unimplemented.
* validator-set rotation remains open.
* full C4 remains open.
* C5 remains open.

Acceptance criteria:
Run 187 is acceptable only if:

1. Release-built helper proves the Run 186 verifier-boundary corpus.
2. Real `target/release/qbind-node` confirms MainNet peer-driven apply remains refused.
3. Fixture proof verification remains explicitly DevNet/TestNet evidence-only.
4. Production-class proof verification is release-binary evidenced as unavailable/fail-closed.
5. Fixture proof cannot satisfy MainNet production governance authority.
6. Existing Run 185 fixture paths remain compatible.
7. Rejected verifier-boundary cases produce no mutation.
8. No real governance execution / real on-chain proof / KMS-HSM / validator-set rotation claim is made.
9. No full C4 or C5 closure is claimed.
