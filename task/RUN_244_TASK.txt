Run 244 — Source/test modeled governance trust-state mutation applier boundary

You are continuing QBIND after Run 243.

Run 240 is fully closed at source/test level.
Run 241 is fully closed as release-binary evidence for Run 240.
Run 242 is fully closed as source/test governance execution mutation-engine boundary work.
Run 243 is fully closed as release-binary evidence for Run 242.

Strict workflow:

* Execution-first.
* Narrow scope only.
* No broad redesign.
* No silent regressions.
* No hype or overclaiming.
* Keep Full C4 OPEN and C5 OPEN unless real production evidence closes them.
* MainNet peer-driven apply remains refused.
* Fixture/local/loopback material remains DevNet/TestNet evidence-only.
* Rejected paths must remain non-mutating.
* Do not weaken Runs 070, 130–243.

Current state:

* Run 238 defined the typed durable replay-state backend contract.
* Run 239 release-evidenced the durable backend boundary.
* Run 240 wired the Run 238 durable backend into the Run 236 / 232 / 230 replay/freshness + after-success-only consume runtime path as the durable state provider.
* Run 241 release-evidenced the Run 240 durable runtime integration.
* Run 242 added the source/test governance execution mutation-engine boundary in `crates/qbind-node/src/pqc_governance_execution_mutation_engine.rs`.
* Run 243 release-evidenced the Run 242 mutation-engine boundary on real `target/release/qbind-node` plus a release-built helper.
* The Run 242/243 mutation engine makes mutation completion explicit and typed, but the fixture executor still models outcomes. It does not model even an in-memory state transition shape.
* No real governance execution engine is implemented.
* No real production mutation engine is implemented.
* No real on-chain governance proof verifier is implemented.
* No real persistent replay backend is implemented.
* No RocksDB/file/schema/migration/storage-format change is implemented.
* No KMS/HSM/RemoteSigner backend is implemented.
* No MainNet governance enablement.
* No MainNet peer-driven apply enablement.
* Validator-set rotation remains unsupported.
* Full C4 remains OPEN.
* C5 remains OPEN.

Objective:

Implement the smallest source/test-only modeled governance trust-state mutation applier boundary.

This run should add an in-memory model of what a future governance mutation applier would do after all Run 242 mutation-engine gates have already passed: snapshot a modeled trust state, apply a modeled trust-state update, report success/failure/rollback/ambiguous windows, and project the result back into the existing Run 242 mutation outcome / Run 240 durable completion semantics.

This must remain a model only. It must not mutate `LivePqcTrustState`, must not call Run 070, must not evict sessions, must not write sequence files, must not write authority markers, must not touch RocksDB, and must not enable any production or MainNet mutation path.

Suggested implementation:

1. Add a new source/test-only module:

   `crates/qbind-node/src/pqc_governance_modeled_trust_mutation_applier.rs`

2. Register it in:

   `crates/qbind-node/src/lib.rs`

3. Define modeled in-memory trust-state types, for example:

   * `ModeledGovernanceTrustState`
   * `ModeledGovernanceTrustSnapshot`
   * `ModeledGovernanceTrustRoot`
   * `ModeledGovernanceTrustMutation`
   * `ModeledGovernanceTrustMutationInput`
   * `ModeledGovernanceTrustMutationExpectations`
   * `ModeledGovernanceTrustMutationPolicy`
   * `ModeledGovernanceTrustMutationSurface`
   * `ModeledGovernanceTrustMutationEnvironmentBinding`
   * `ModeledGovernanceTrustMutationRuntimeBinding`

4. Define modeled mutation actions, for example:

   * `AddTrustRoot`
   * `RetireTrustRoot`
   * `RevokeTrustRoot`
   * `EmergencyRevokeTrustRoot`
   * `Noop`
   * `ValidatorSetRotationUnsupported`
   * `PolicyChangeUnsupported`

5. Define modeled applier outcomes, for example:

   * `ModeledMutationNotAttempted`
   * `ModeledMutationApplied`
   * `ModeledMutationRejectedBeforeSnapshot`
   * `ModeledMutationRejectedBeforeApply`
   * `ModeledMutationApplyFailed`
   * `ModeledMutationRolledBack`
   * `ModeledMutationRollbackFailedFatal`
   * `ModeledMutationAmbiguousFailClosed`
   * `ProductionModeledMutationUnavailable`
   * `MainNetModeledMutationUnavailable`
   * `MainNetPeerDrivenApplyRefused`
   * `ValidatorSetRotationUnsupported`
   * `PolicyChangeUnsupported`

6. Define a pure/mockable trait boundary, for example:

   * `ModeledGovernanceTrustMutationApplier`
   * `apply_modeled_mutation(...)`
   * `recover_modeled_mutation_window(...)`

7. Provide source/test-only fixture appliers:

   * `FixtureModeledTrustMutationApplier`
   * `ProductionModeledTrustMutationApplier`
   * `MainNetModeledTrustMutationApplier`

   Requirements:

   * DevNet/TestNet fixture applier may mutate only `ModeledGovernanceTrustState`.
   * Production/MainNet appliers must be reachable but unavailable/fail-closed.
   * MainNet peer-driven apply must refuse before any snapshot or apply attempt.
   * The fixture applier must expose an invocation counter so tests prove rejected paths never invoke it.

8. Add a composition helper that maps modeled applier outcomes into Run 242 mutation-engine outcomes:

   * modeled apply success → `GovernanceMutationOutcome::MutationAppliedSuccessfully`;
   * rejected before snapshot/apply → `MutationRejectedBeforeApply`;
   * apply failure → `MutationApplyFailed`;
   * rollback success → `MutationRolledBack`;
   * rollback failure / ambiguous window → `MutationAmbiguousFailClosed`;
   * production unavailable → `ProductionMutationUnavailable`;
   * MainNet unavailable → `MainNetMutationUnavailable`;
   * MainNet peer-driven refused → `MainNetPeerDrivenApplyRefused`;
   * validator-set rotation unsupported → `ValidatorSetRotationUnsupported`;
   * policy-change unsupported → `PolicyChangeUnsupported`.

9. Add a second composition helper that projects modeled applier outcomes through Run 242 into Run 240 durable completion semantics:

   * only modeled apply success becomes consume-eligible;
   * rejected / failed / rollback / ambiguous / unavailable / unsupported outcomes never consume.

10. Preserve strict negative invariants:

* no Run 070 call;
* no `LivePqcTrustState` mutation;
* no real trust swap;
* no session eviction;
* no sequence write;
* no marker write;
* no durable consume by the applier itself;
* no RocksDB/file/schema/migration/storage-format change;
* no wire/schema/marker/sequence/trust-bundle change;
* no autonomous apply;
* no apply-on-receipt;
* no peer-majority authority;
* no MainNet enablement.

11. Add grep-verifiable invariant helpers, for example:

* `modeled_trust_applier_rejection_is_non_mutating`
* `modeled_trust_applier_never_calls_run_070`
* `modeled_trust_applier_never_mutates_live_pqc_trust_state`
* `modeled_trust_applier_success_required_before_durable_consume`
* `modeled_trust_applier_failure_never_consumes`
* `modeled_trust_applier_rollback_never_consumes`
* `modeled_trust_applier_ambiguous_window_fails_closed`
* `production_mainnet_modeled_trust_applier_unavailable`
* `mainnet_peer_driven_apply_refused_by_modeled_trust_applier`
* `validator_set_rotation_unsupported_by_modeled_trust_applier`
* `policy_change_unsupported_by_modeled_trust_applier`
* `modeled_trust_applier_no_rocksdb_file_schema_migration_change`
* `local_operator_cannot_satisfy_modeled_trust_applier_authority`
* `peer_majority_cannot_satisfy_modeled_trust_applier_authority`

Tests:

Add:

`crates/qbind-node/tests/run_244_modeled_governance_trust_mutation_applier_tests.rs`

Required test matrix:

Accepted / compatible:

* Disabled policy preserves legacy bypass and performs no modeled mutation.
* DevNet fixture modeled add-root succeeds and changes only modeled in-memory state.
* TestNet fixture modeled add-root succeeds and changes only modeled in-memory state.
* Modeled retire-root succeeds in fixture state only.
* Modeled revoke-root succeeds in fixture state only.
* Modeled emergency-revoke-root succeeds in fixture state only.
* Modeled noop succeeds without state drift.
* Modeled apply success maps to `MutationAppliedSuccessfully`.
* Modeled apply success projects to consume-eligible Run 240 durable completion.
* Existing Run 242 mutation-engine behavior remains compatible.
* Existing Run 243 release-evidenced boundary assumptions remain compatible.
* Production modeled applier path is reachable but unavailable/fail-closed.
* MainNet modeled applier path is reachable but unavailable/fail-closed.
* MainNet peer-driven apply refused before snapshot and before applier invocation.
* Validator-set rotation unsupported.
* Policy-change unsupported.

Rejected:

* wrong environment rejected before snapshot;
* wrong chain rejected before snapshot;
* wrong genesis rejected before snapshot;
* wrong governance surface rejected before snapshot;
* wrong mutation surface rejected before snapshot;
* wrong candidate digest rejected before snapshot;
* wrong decision digest rejected before snapshot;
* wrong proposal id rejected before snapshot;
* wrong decision id rejected before snapshot;
* wrong authority-domain sequence rejected before snapshot;
* malformed modeled mutation rejected before snapshot;
* duplicate root rejected or handled idempotently under explicit typed outcome;
* retiring missing root rejected before apply;
* revoking missing root rejected before apply;
* stale durable state cannot reach consume;
* consumed durable state cannot reach consume;
* superseded durable state cannot reach consume;
* backend unavailable cannot reach consume;
* apply failure rolls back modeled state and never consumes;
* rollback success never consumes;
* rollback failure produces fatal/ambiguous fail-closed and never consumes;
* unknown crash window fails closed;
* local operator key cannot satisfy MainNet authority;
* peer majority cannot satisfy MainNet authority;
* production modeled applier unavailable;
* MainNet modeled applier unavailable;
* validator-set rotation attempt unsupported;
* policy-change attempt unsupported;
* every rejected path produces no Run 070 call, no `LivePqcTrustState` mutation, no real trust swap, no session eviction, no sequence write, no marker write, no durable consume, and no applier invocation where rejection happens before apply.

Recovery / crash-window cases:

* before-snapshot window recovers as not attempted / no consume;
* after-snapshot-before-apply window rolls back modeled state / no consume;
* after-apply-before-report window fails closed unless an explicit success report exists;
* after-report-ambiguous window fails closed;
* rollback-failed window is fatal/fail-closed;
* unknown window fails closed;
* production/MainNet recovery classification unavailable/fail-closed;
* MainNet peer-driven apply refusal precedes recovery classification where applicable.

Validation commands:

Run at minimum:

* `cargo build -p qbind-node --lib`
* `cargo test -p qbind-node --test run_244_modeled_governance_trust_mutation_applier_tests`
* `cargo test -p qbind-node --test run_242_governance_execution_mutation_engine_tests`
* `cargo test -p qbind-node --test run_240_governance_evaluator_replay_durable_runtime_integration_tests`
* `cargo test -p qbind-node --test run_238_governance_evaluator_replay_durable_backend_tests`
* `cargo test -p qbind-node --test run_236_governance_evaluator_replay_consume_runtime_integration_tests`
* `cargo test -p qbind-node --test run_234_governance_evaluator_replay_consume_boundary_tests`
* `cargo test -p qbind-node --test run_232_governance_evaluator_replay_runtime_integration_tests`
* `cargo test -p qbind-node --test run_230_governance_evaluator_replay_state_tests`
* `cargo test -p qbind-node --test run_228_peer_evaluator_context_representation_tests`
* `cargo test -p qbind-node --test run_226_governance_evaluator_runtime_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_224_governance_evaluator_runtime_integration_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

Documentation:

Create:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_244.md`

Update narrowly:

* `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
* `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
* `docs/whitepaper/contradiction.md`

Documentation must state:

* Run 244 is source/test only.
* It introduces a modeled in-memory trust-state mutation applier boundary, not a real production mutation engine.
* No production mutating behavior is enabled.
* Any production-source module remains pure/source-test bounded and fail-closed.
* The modeled applier mutates only modeled in-memory state in DevNet/TestNet fixture tests.
* It does not mutate `LivePqcTrustState`.
* It does not call Run 070.
* It does not perform a real trust swap.
* It does not evict sessions.
* It does not write sequence files.
* It does not write authority markers.
* It does not perform durable consume by itself.
* No real governance execution engine is implemented.
* No real on-chain governance proof verifier is implemented.
* No real persistent replay backend is implemented.
* No RocksDB/file/schema/migration/storage-format change is implemented.
* No KMS/HSM/RemoteSigner backend is implemented.
* No MainNet governance enablement.
* No MainNet peer-driven apply enablement.
* No validator-set rotation.
* Rejected modeled-applier paths are non-mutating.
* Modeled apply success is required before durable consume.
* Failed apply, rollback, rollback failure, and ambiguous windows never consume.
* Full C4 remains OPEN.
* C5 remains OPEN.

Contradiction handling:

* Crosscheck Run 244 against Runs 050–243.
* If no contradiction is found, add a “no Run 244 contradiction” entry to `docs/whitepaper/contradiction.md`.
* If any contradiction is found, do not hide it. Record it explicitly with the exact file/function/doc reference and recommend the smallest safe fix.

Strict non-goals:

* Do not implement a real production mutation engine.
* Do not wire production runtime mutation.
* Do not call Run 070.
* Do not mutate `LivePqcTrustState`.
* Do not perform a real trust swap.
* Do not evict sessions.
* Do not write sequence files.
* Do not write authority markers.
* Do not implement a real governance execution engine.
* Do not implement a real on-chain governance proof verifier.
* Do not implement validator-set rotation.
* Do not implement a persistent replay backend.
* Do not add RocksDB/file/schema/migration/storage-format changes.
* Do not change wire/schema/marker/sequence/trust-bundle formats.
* Do not enable MainNet governance.
* Do not enable MainNet peer-driven apply.
* Do not add autonomous apply or apply-on-receipt.
* Do not let peer majority satisfy authority.
* Do not weaken any fail-closed behavior from Runs 070 or 130–243.

Expected verdict:

Partial-positive / source-test positive.

Run 244 should advance the modeled in-memory mutation-applier boundary only. It must not claim Full C4 or C5 closure.

Final response format:

1. Exact verdict.
2. Files changed.
3. Tests run and results.
4. Security invariants preserved.
5. Honest limitations.
6. C4/C5 status.
7. Suggested Run 245 next step.
