Run 298 — Release-binary evidence for the production custody attestation verifier

You are continuing QBIND after Run 297.

Current accepted state:

Run 292 is accepted as PASS / release-binary evidence positive for the production durable replay RocksDB backend.

Run 294 is accepted as PASS / release-binary evidence positive for the production RemoteSigner backend.

Run 296 is accepted as PASS / release-binary evidence positive for the production KMS/HSM/cloud-KMS/PKCS#11 custody backend.

Run 297 is accepted as PASS / source-test production custody attestation verifier implementation.

Run 297 added:

`crates/qbind-node/src/pqc_production_custody_attestation_verifier.rs`

The verifier defines `ProductionCustodyAttestationVerifier<V: CustodyAttestationEvidenceVerifier>`, composes with Run 295 KMS/HSM custody request/response/transcript surfaces, defaults to Disabled/fail-closed, binds attestation evidence to provider/key/custody-class/signer/request-id/request/response/backend-transcript/challenge/trust-root/measurement/authority-domain, rejects fixture attestation for MainNet, refuses RemoteSigner-only/local-operator/peer-majority evidence for KMS/HSM custody attestation, keeps production cloud-KMS / PKCS#11 / generic KMS/HSM attestation classes reachable-but-fail-closed absent real trust root / quote verifier / certificate chain / hardware proof, and is non-mutating on every rejected path.

Run 297 is source/test only. It is not release-binary evidence.

Current C4/C5 matrix:

* Production durable replay RocksDB backend — Green for release-binary-evidenced backend behavior only.
* Real production RemoteSigner backend — Green for release-binary-evidenced backend behavior only.
* Real KMS/HSM/cloud-KMS/PKCS#11 custody backend — Green for release-binary-evidenced KMS/HSM custody backend behavior only.
* Real custody attestation verifier — Yellow / source-test implementation landed, release-binary evidence pending Run 298.
* Real on-chain governance proof verifier — Red.
* Governance execution engine — Red.
* Validator-set rotation / authority-set synchronization — Red.
* MainNet authority rotation/revocation under production custody — Red.
* Production signing audit trail / crypto-agility activation / incident response — Red.
* Full MainNet release-binary evidence under production custody — Red.

Full C4 remains OPEN. C5 remains OPEN.

Objective:

Produce release-binary evidence for the Run 297 production custody attestation verifier.

This run must prove, on real `target/release/qbind-node` plus a release-built helper, that the Run 297 verifier symbols are present and exercised in release mode, while the production binary remains default-Disabled/silent, no CLI flag is added, MainNet remains refused absent production authority criteria and verified custody attestation, and C4/C5 remain open.

This is release-binary evidence only.

Do not add a new verifier layer.

Do not add a new backend.

Do not wire custody attestation into default production runtime behavior.

Do not add a public CLI flag.

Do not enable MainNet.

Do not claim C4 closure.

Do not claim C5 closure.

Primary deliverables:

1. New release helper:

`crates/qbind-node/examples/run_298_production_custody_attestation_verifier_release_binary_helper.rs`

2. New release harness:

`scripts/devnet/run_298_production_custody_attestation_verifier_release_binary.sh`

The harness must be LF-clean and executable.

3. New ignored evidence archive directory:

`docs/devnet/run_298_production_custody_attestation_verifier_release_binary/`

Track only:

* `README.md`
* `summary.txt`
* `.gitignore`

Generated logs, raw helper outputs, temp files, stdout/stderr captures, and binary artifacts must be ignored unless the existing evidence convention explicitly requires otherwise.

4. New canonical evidence report:

`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_298.md`

5. Narrow documentation updates:

* `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
* `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
* `docs/whitepaper/contradiction.md`

Strict scope:

Allowed:

* Build real `target/release/qbind-node`.
* Build a release helper that links against the real release-built qbind-node library symbols.
* Exercise the Run 297 custody attestation verifier in release mode.
* Use fixture KMS/HSM attestation and mock evidence verifiers inside the release helper only as release-mode evidence scaffolding.
* Prove production cloud-KMS / PKCS#11 / generic KMS/HSM attestation classes are reachable-but-fail-closed without real verification material.
* Prove attestation class and policy taxonomy behavior in release mode.
* Prove evidence/request/response/transcript/domain binding in release mode.
* Prove missing, unavailable, malformed, replay, wrong-domain, wrong-provider, wrong-key, wrong-signer, wrong-custody-class, wrong-request-id, wrong-backend-transcript, wrong-challenge, wrong-trust-root, wrong-measurement, wrong-proof, unsupported-class, unsupported-protocol, ambiguous-evidence, and MainNet refusal fail-closed behavior in release mode.
* Prove default production binary surfaces remain Disabled/silent.
* Prove no new CLI flag is exposed.
* Prove MainNet remains refused absent production authority criteria and verified production custody attestation.
* Capture release hashes, helper verdict, scenario return codes, denylist results, reachability results, C4/C5 taxonomy greps, regression results, secret scan, and CodeQL result or honest skip/timeout.
* Update docs/evidence narrowly.

Not allowed:

* Do not add new production runtime wiring.
* Do not add a public CLI flag.
* Do not enable custody attestation by default.
* Do not enable MainNet.
* Do not mark custody attestation as full production ready.
* Do not claim C4 closure.
* Do not claim C5 closure.
* Do not claim MainNet readiness.
* Do not implement real cloud-KMS credentials.
* Do not implement real PKCS#11 sessions.
* Do not load real HSM modules.
* Do not commit secrets, tokens, KMS credentials, HSM PINs, cloud account identifiers, real provider endpoints, real certificate chains, or real hardware quotes.
* Do not accept fixture attestation as production attestation.
* Do not allow RemoteSigner-only evidence to satisfy KMS/HSM custody attestation.
* Do not allow local operator material to satisfy MainNet attestation.
* Do not allow peer majority to satisfy MainNet attestation.
* Do not implement real on-chain governance proof verification.
* Do not implement a governance execution engine.
* Do not implement validator-set rotation.
* Do not implement settlement, settlement finality, settlement receipt, settlement-receipt acknowledgement, settlement-finality projection, settlement-outcome report backend, or settlement-outcome publication.
* Do not implement external publication, external-publication confirmation, receipt, acknowledgement, audit finalization, audit completion, audit archive, audit seal, or audit anchor.
* Do not call Run 070.
* Do not mutate `LivePqcTrustState`.
* Do not swap trust state.
* Do not evict sessions.
* Do not write PQC trust-bundle sequence files.
* Do not write authority markers.
* Do not change trust-bundle wire format.
* Do not change peer-candidate wire format.
* Do not change consensus wire format.
* Do not weaken Run 292 durable replay RocksDB Green-for-scope status.
* Do not weaken Run 294 RemoteSigner Green-for-scope status.
* Do not weaken Run 296 KMS/HSM custody Green-for-scope status.
* Do not turn unrelated Red rows Green.

Required release helper behavior:

The release helper must link against the real release-built qbind-node library and exercise the actual Run 297 verifier symbols.

At minimum, it must reference the actual implemented names, including where present:

* `pqc_production_custody_attestation_verifier`
* `ProductionCustodyAttestationVerifier`
* `CustodyAttestationEvidenceVerifier`
* `GovernanceProductionCustodyAttestationVerifier`
* `ProductionCustodyAttestationVerifierPolicy`
* `ProductionCustodyAttestationClass`
* `ProductionCustodyAttestationBinding`
* `ProductionCustodyAttestationEvidence`
* `ProductionCustodyAttestationTrustRoot`
* `ProductionCustodyAttestationMeasurement`
* `ProductionCustodyAttestationChallenge`
* `ProductionCustodyAttestationOutcome`
* `ProductionCustodyAttestationError`
* `ProductionCustodyAttestationRecoveryOutcome`
* `production_custody_attestation_challenge_digest`
* `production_custody_attestation_evidence_digest`
* `production_custody_attestation_transcript_digest`
* `ProductionCustodyAttestationBinding::from_submitted_request`
* `ProductionKmsHsmCustodyBackend`
* `SubmittedCustodyRequest`

If actual names differ, use the real names and document substitutions in the evidence report.

The helper must produce machine-readable summary output with per-check PASS/FAIL counts and a final verdict.

Suggested helper sections:

A. Accepted / compatible release checks

* Disabled default policy is explicit and inert.
* DevNet fixture KMS attestation verifies only under explicit fixture policy.
* DevNet fixture HSM attestation verifies only under explicit fixture policy.
* TestNet fixture KMS/HSM attestation verifies only under explicit allowed policy if existing policy permits.
* Fixture attestation acceptance is release-mode scaffolding only, not production authority.
* Valid fixture attestation binds exactly to the matching Run 295 custody request id.
* Valid fixture attestation binds exactly to the matching Run 295 request envelope digest.
* Valid fixture attestation binds exactly to the matching Run 295 response envelope digest.
* Valid fixture attestation binds exactly to the matching Run 295 backend transcript.
* Valid fixture attestation binds provider identity, provider kind, key handle, key fingerprint, custody class, signer, candidate digest, and action.
* Attestation challenge digest is deterministic across two helper invocations.
* Attestation evidence digest is deterministic across two helper invocations.
* Attestation transcript digest is deterministic across two helper invocations.
* Two identical evidence objects produce identical transcripts.
* Production cloud-KMS attestation class is reachable and fails closed without real verification material.
* Production PKCS#11 HSM attestation class is reachable and fails closed without real quote/cert/session material.
* Production generic KMS/HSM attestation classes are reachable and fail closed.
* Run 295 KMS/HSM custody backend composition point is exercised.
* Run 292/294/296 Green-for-scope rows remain unchanged where representable.

B. Rejection / fail-closed release checks

* Disabled policy produces no verification.
* Missing attestation rejected.
* Malformed attestation rejected.
* Empty/sentinel proof rejected.
* Unsupported attestation class rejected.
* Unsupported protocol version rejected.
* Missing trust root rejected.
* Wrong trust root rejected.
* Missing key binding rejected.
* Wrong provider kind rejected.
* Wrong provider identity rejected.
* Wrong key handle rejected.
* Wrong key fingerprint rejected.
* Wrong signer identity rejected.
* Wrong custody class rejected.
* Wrong request id rejected.
* Wrong request envelope digest rejected.
* Wrong response envelope digest rejected.
* Wrong backend transcript rejected.
* Wrong candidate/proposal digest rejected.
* Wrong authorized action rejected.
* Wrong environment rejected.
* Wrong chain rejected.
* Wrong genesis/domain rejected.
* Wrong authority root rejected.
* Wrong authority-domain sequence rejected.
* Wrong nonce/challenge rejected.
* Replayed/stale nonce rejected.
* Wrong measurement digest rejected.
* Wrong certificate/proof digest rejected.
* Ambiguous evidence rejected.
* Attestation unavailable rejected.
* Attestation unverified rejected.
* Production provider without verification material rejected.
* RemoteSigner-only evidence rejected for KMS/HSM custody attestation.
* Local-operator material rejected.
* Peer-majority evidence rejected.
* Unsupported request kinds remain fail-closed.

C. MainNet / authority policy release checks

* MainNet cannot be satisfied by fixture KMS attestation.
* MainNet cannot be satisfied by fixture HSM attestation.
* MainNet cannot be satisfied by RemoteSigner-only attestation.
* MainNet cannot be satisfied by local operator material.
* MainNet cannot be satisfied by peer majority.
* MainNet cannot bypass missing production attestation.
* MainNet cannot bypass unavailable production attestation.
* MainNet cannot bypass missing on-chain governance verifier.
* MainNet cannot bypass missing governance execution engine.
* MainNet cannot bypass validator-set rotation gate.
* MainNet production attestation unavailable path records no mutation.
* MainNet peer-driven apply remains refused.

D. Replay / recovery / idempotency release checks

If Run 297 recovery helpers are represented:

* no-prior attestation window is clean no-op;
* byte-identical duplicate attestation is idempotent or classified as identical according to policy;
* same attestation id with different transcript fails closed;
* same attestation id with different key handle fails closed;
* same attestation id with different measurement fails closed;
* same nonce/challenge reused across different request id fails closed;
* stale nonce fails closed;
* unrelated request id remains unrelated/no-prior;
* no durable attestation acceptance is claimed.

If replay state is not persisted, document that honestly and prove no durable attestation acceptance is claimed.

E. Non-mutation / no-authority-extension release checks

Every rejected helper path must prove:

* no Run 070 call;
* no `LivePqcTrustState` mutation;
* no trust swap;
* no session eviction;
* no PQC trust-bundle sequence write;
* no authority marker write;
* no durable replay overwrite;
* no custody backend acceptance under wrong policy;
* no fallback to fixture attestation under production policy;
* no fallback to RemoteSigner attestation under KMS/HSM policy;
* no raw local production signing key load;
* no settlement;
* no external publication;
* no governance execution;
* no validator-set rotation;
* no MainNet enablement.

These may be proven through helper invariants, source-reachability greps, denylist checks, and real-binary scenario silence.

Required real-binary scenarios:

The harness must build and run real `target/release/qbind-node`.

At minimum:

S1. `target/release/qbind-node --help`

Expected:

* rc=0.
* no public Run 297/298 custody-attestation verifier flag appears.
* no custody-attestation verifier enablement claim.
* no KMS/HSM custody enablement claim.
* no MainNet readiness claim.
* no C4/C5 closure claim.

S2. Default DevNet binary surface.

Use the smallest safe invocation already used by recent release evidence harnesses.

Expected:

* custody attestation verifier is not constructed by default;
* no custody-attestation enablement banner;
* no helper/evidence-only code path appears;
* no mutation.

S3. Default TestNet binary surface.

Expected:

* custody attestation verifier is not constructed by default;
* no custody-attestation enablement banner;
* no on-chain verifier / governance engine / validator-set-rotation claim;
* no mutation.

S4. Default MainNet binary surface.

Expected:

* custody attestation verifier is not constructed by default;
* no custody-attestation enablement banner;
* no MainNet enablement claim;
* any existing MainNet-required-config failure remains expected fail-closed behavior;
* no mutation.

S5. Existing governance/custody/KMS/HSM/RemoteSigner selectors, if any, remain compatible.

If an existing hidden selector related to governance execution, custody, RemoteSigner, KMS/HSM, or attestation exists, prove it still parses or fails closed exactly as before. If there is no existing selector, document that no new CLI selector was added.

S6. Invalid selector / invalid config fail-closed scenario.

Use the nearest existing invalid governance/custody/KMS/HSM/attestation selector if available. It must fail closed before mutation.

Required release artifact metadata:

Capture in `summary.txt`:

* git commit;
* branch name if available;
* dirty/clean status;
* rustc version;
* cargo version;
* OS/kernel summary if existing harness convention captures it;
* `target/release/qbind-node` SHA-256;
* release helper SHA-256;
* helper verdict;
* helper check counts by category;
* deterministic challenge/evidence/transcript digest samples;
* real-binary scenario return codes;
* denylist result;
* reachability result;
* C4/C5 taxonomy result;
* regression result;
* secret scan result;
* CodeQL result or honest limitation;
* C4/C5 status.

Required denylist:

The harness must assert zero matches in release binary stdout/stderr/help/helper logs for false claims such as:

* `C4 closed`
* `C5 closed`
* `MainNet ready`
* `production ready`
* `custody attestation active`
* `custody attestation enabled`
* `production attestation verified`
* `cloud KMS attestation active`
* `PKCS11 attestation active`
* `HSM attestation active`
* `KMS/HSM custody active`
* `on-chain governance verifier active`
* `governance execution engine active`
* `validator-set rotation active`
* `peer-driven apply enabled`
* `Run 070 applied`
* `LivePqcTrustState mutated`
* `trust swap complete`
* `session eviction complete`
* `authority marker written`
* `trust-bundle sequence written`
* `settlement finalized`
* `settlement receipt recorded`
* `settlement outcome published`
* `external publication completed`
* `external publication confirmed`
* `fallback to fixture attestation`
* `fallback to RemoteSigner attestation`
* `fallback to local signing`
* `raw local production key`
* `DummySig active`
* `DummyKem active`
* `DummyAead active`

Adjust exact grep patterns to avoid false positives from explicit negative statements such as “not active” or evidence non-goal prose. The evidence must clearly distinguish absent enablement claims from documented non-goals.

Required reachability greps:

The harness must prove the release helper and/or source references the Run 297 verifier symbols, including:

* `ProductionCustodyAttestationVerifier`
* `CustodyAttestationEvidenceVerifier`
* `GovernanceProductionCustodyAttestationVerifier`
* `ProductionCustodyAttestationVerifierPolicy`
* `ProductionCustodyAttestationClass`
* `ProductionCustodyAttestationBinding`
* `ProductionCustodyAttestationEvidence`
* `ProductionCustodyAttestationOutcome`
* `ProductionCustodyAttestationError`
* `ProductionCustodyAttestationRecoveryOutcome`
* `ProductionCustodyAttestationBinding::from_submitted_request`
* `ProductionKmsHsmCustodyBackend`
* `SubmittedCustodyRequest`

Use real symbol names and document substitutions.

Required C4/C5 taxonomy verification:

The harness must grep `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md` and prove:

* Status as of Run 298 is present.
* Full C4 remains OPEN.
* C5 remains OPEN.
* Production durable replay RocksDB backend remains Green for release-binary-evidenced scope only.
* Real production RemoteSigner backend remains Green for release-binary-evidenced scope only.
* Real KMS/HSM/cloud-KMS/PKCS#11 custody backend remains Green for release-binary-evidenced scope only.
* Real custody attestation verifier row is updated correctly.
* If Run 298 evidence passes, this row may become Green only for narrow release-binary-evidenced custody-attestation-verifier behavior.
* The custody-attestation row wording must explicitly say: not wired by default, no public CLI flag, MainNet refused absent production authority criteria and verified production custody attestation, production attestation classes remain fail-closed without real verification material, does not implement on-chain verifier / governance engine / validator-set rotation, does not close C4/C5.
* Real on-chain governance proof verifier remains Red.
* Governance execution engine remains Red.
* Validator-set rotation / authority-set synchronization remains Red.
* MainNet authority rotation/revocation under production custody remains Red.
* Production signing audit trail / crypto-agility activation / incident response remains Red.
* Full MainNet release-binary evidence under production custody remains Red.
* Matrix taxonomy still separates boundary readiness from production readiness.
* Green-for-scope does not mean C4/C5 closure unless every closure gate is met.

Important matrix guidance:

If Run 298 proves release-binary behavior for the Run 297 custody attestation verifier, it is acceptable to mark only the narrow “Real custody attestation verifier” capability as Green or Green-for-scope, provided the row wording is precise:

“Green for release-binary-evidenced custody-attestation verifier behavior only; not wired by default into production runtime; no public CLI flag; MainNet refused absent production authority criteria and verified production custody attestation; production cloud-KMS / PKCS#11 / generic KMS/HSM attestation classes remain fail-closed without real trust root / quote verifier / certificate chain / hardware proof; does not implement real on-chain verifier / governance engine / validator-set rotation; does not close C4/C5.”

Do not turn on-chain verifier Green.

Do not turn governance engine Green.

Do not turn validator-set rotation Green.

Do not claim C4/C5 closure.

Required tests / regression commands:

Run at minimum:

* `cargo build -p qbind-node --release --bin qbind-node`
* `cargo build -p qbind-node --release --example run_298_production_custody_attestation_verifier_release_binary_helper`
* `scripts/devnet/run_298_production_custody_attestation_verifier_release_binary.sh`
* `cargo test -p qbind-node --test run_297_production_custody_attestation_verifier_tests`
* `cargo test -p qbind-node --test run_295_production_kms_hsm_custody_backend_tests`
* `cargo test -p qbind-node --test run_293_production_remote_signer_backend_tests`
* `cargo test -p qbind-node --test run_291_production_durable_replay_rocksdb_tests`
* `cargo test -p qbind-node --test run_290_durable_completion_external_publication_audit_anchor_tests`
* `cargo test -p qbind-node --test run_288_durable_completion_external_publication_audit_seal_tests`
* `cargo test -p qbind-node --test run_286_durable_completion_external_publication_audit_archive_tests`
* `cargo test -p qbind-node --test run_284_durable_completion_external_publication_audit_completion_tests`
* `cargo test -p qbind-node --test run_282_durable_completion_external_publication_audit_finalization_tests`
* `cargo test -p qbind-node --test run_280_durable_completion_external_publication_acknowledgement_tests`
* `cargo test -p qbind-node --test run_278_durable_completion_external_publication_receipt_tests`
* `cargo test -p qbind-node --test run_276_durable_completion_external_publication_confirmation_tests`
* `cargo test -p qbind-node --test run_274_durable_completion_settlement_outcome_publication_tests`
* `cargo test -p qbind-node --test run_272_durable_completion_settlement_outcome_report_tests`
* `cargo test -p qbind-node --test run_270_durable_completion_settlement_receipt_acknowledgement_tests`
* `cargo test -p qbind-node --test run_268_durable_completion_settlement_finalization_tests`
* `cargo test -p qbind-node --test run_266_durable_completion_settlement_commitment_tests`
* `cargo test -p qbind-node --test run_264_durable_completion_consumer_settlement_projection_tests`
* `cargo test -p qbind-node --test run_262_durable_completion_acknowledgement_consumer_tests`
* `cargo test -p qbind-node --test run_260_durable_completion_audit_receipt_acknowledgement_tests`
* `cargo test -p qbind-node --test run_258_durable_completion_audit_publication_receipt_tests`
* `cargo test -p qbind-node --test run_256_durable_completion_attestation_backend_tests`
* `cargo test -p qbind-node --test run_254_modeled_durable_completion_attestation_projection_tests`
* `cargo test -p qbind-node --test run_252_modeled_durable_completion_finalization_projection_tests`
* `cargo test -p qbind-node --test run_250_modeled_durable_consume_completion_reporter_tests`
* `cargo test -p qbind-node --test run_248_modeled_durable_consume_projection_sink_tests`
* `cargo test -p qbind-node --test run_246_governance_modeled_end_to_end_pipeline_tests`
* `cargo test -p qbind-node --test run_244_modeled_governance_trust_mutation_applier_tests`
* `cargo test -p qbind-node --test run_242_governance_execution_mutation_engine_tests`
* `cargo test -p qbind-node --test run_240_governance_evaluator_replay_durable_runtime_integration_tests`
* `cargo test -p qbind-node --test run_238_governance_evaluator_replay_durable_backend_tests`
* `cargo test -p qbind-node --test run_236_governance_evaluator_replay_consume_runtime_integration_tests`
* `cargo test -p qbind-node --test run_234_governance_evaluator_replay_consume_boundary_tests`
* `cargo test -p qbind-node --test run_232_governance_evaluator_replay_runtime_integration_tests`
* `cargo test -p qbind-node --test run_230_governance_evaluator_replay_state_tests`
* `cargo test -p qbind-node --test run_228_peer_evaluator_context_representation_tests`
* `cargo test -p qbind-node --test run_226_governance_evaluator_runtime_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_224_governance_evaluator_runtime_integration_tests`
* `cargo test -p qbind-node --test run_203_kms_hsm_backend_boundary_tests` or the actual Run 203 target
* `cargo test -p qbind-node --test run_201_remote_signer_transport_boundary_tests` or the actual Run 201 target
* `cargo test -p qbind-node --test run_194_remote_authority_signer_boundary_tests` or the actual Run 194 target
* `cargo test -p qbind-node --test run_188_authority_custody_boundary_tests` or the actual Run 188 target
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

Important known issue handling:

Run 297 honestly reported an unrelated pre-existing compile failure in `m16_epoch_transition_hardening_tests.rs` due to missing `RocksDbConsensusStorage` test-utils methods. Do not hide this if it still exists. Do not include it in the required Run 298 corpus unless the task explicitly expands scope to fix it. If you encounter it, report it as pre-existing unless Run 298 changes it.

If actual target names differ, use the real names and record substitutions in the evidence.

Security scanning:

* Run secret scanning over changed files and evidence scripts.
* Run CodeQL if available.
* Because Run 298 is release-binary evidence for real custody-attestation verifier code, CodeQL or an honest skip/timeout reason is required.
* If CodeQL is skipped, times out, reports database too large, tooling is unavailable, or otherwise does not complete, report that explicitly and do not claim CodeQL coverage.
* The canonical evidence file must include the exact CodeQL result or exact honest limitation directly.

Required docs:

`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_298.md` must include:

1. Exact verdict.
2. Files changed.
3. Release artifacts and hashes.
4. Helper corpus results.
5. Real-binary scenarios.
6. Attestation class / policy release evidence.
7. Evidence/request/response/transcript/domain binding release evidence.
8. Accepted release evidence.
9. Rejection/fail-closed release evidence.
10. MainNet refusal / authority policy release evidence.
11. Replay/recovery/idempotency release evidence.
12. Production-binary default-disabled/silent evidence.
13. C4/C5 matrix taxonomy verification.
14. Regression results.
15. Security scan results.
16. Honest limitations.
17. C4/C5 status.
18. Suggested Run 299 next step.

Docs must state clearly:

* Run 298 is release-binary evidence for the Run 297 production custody attestation verifier.
* Run 298 does not add new production runtime wiring.
* Run 298 does not add a public CLI flag.
* Run 298 does not enable custody attestation by default.
* Run 298 does not enable MainNet.
* Run 298 does not implement real cloud-KMS / PKCS#11 / HSM provider credential, session, quote-verifier, certificate-chain, or hardware-proof integration.
* Run 298 does not implement real on-chain governance proof verification.
* Run 298 does not implement governance execution engine.
* Run 298 does not implement validator-set rotation.
* Run 298 does not implement settlement or external publication.
* Run 298 does not call Run 070.
* Run 298 does not mutate `LivePqcTrustState`.
* Run 298 does not write trust-bundle sequence or authority marker files.
* Run 298 preserves Run 292 durable replay RocksDB Green-for-scope status.
* Run 298 preserves Run 294 RemoteSigner Green-for-scope status.
* Run 298 preserves Run 296 KMS/HSM custody Green-for-scope status.
* Full C4 remains OPEN.
* C5 remains OPEN.

Contradiction handling:

Update `docs/whitepaper/contradiction.md`.

Add a “Run 298” entry.

If no contradiction is found, state that Run 298 release-evidences the Run 297 production custody attestation verifier while preserving default Disabled/fail-closed runtime posture, no CLI flag, no MainNet enablement, no real cloud-KMS / PKCS#11 / HSM provider credential/session/quote/certificate integration, no on-chain-verifier/governance-engine/validator-set-rotation implementation, no Run 070 call, no `LivePqcTrustState` mutation, no trust-bundle sequence or authority-marker writes, no weakening of Run 292/294/296 Green-for-scope statuses, and Full C4 OPEN / C5 OPEN.

If contradiction is found, record the exact contradiction with file/function/doc reference and recommend the smallest safe fix.

Expected verdicts:

PASS / release-binary evidence positive only if:

* release `qbind-node` builds;
* release helper builds;
* harness exits 0;
* helper corpus passes;
* real-binary scenarios pass;
* release artifacts and hashes are captured;
* denylist is clean;
* reachability greps pass;
* C4/C5 taxonomy greps pass;
* Run 297 source tests still pass;
* required regression corpus passes;
* secret scan is clean;
* CodeQL result or honest skip/timeout is reported directly in the canonical evidence;
* docs/evidence/contradiction are updated;
* Full C4 remains OPEN;
* C5 remains OPEN.

Partial-positive only if:

* release helper proves the verifier in release mode but one bounded, non-critical scenario is incomplete and documented honestly;
* no false closure claim is made.

Blocked only if:

* the Run 297 verifier cannot be linked/exercised in release mode without redesign;
* document the exact blocker and smallest safe next step.

Unacceptable:

* claiming C4 closure;
* claiming C5 closure;
* claiming MainNet readiness;
* claiming runtime default enablement;
* adding hidden production behavior without evidence;
* adding a public CLI flag;
* accepting fixture attestation as production attestation;
* accepting unverified production attestation;
* allowing RemoteSigner-only evidence to satisfy KMS/HSM custody attestation;
* using raw local production signing keys;
* skipping evidence/request/response/transcript/domain-binding release checks;
* skipping missing/unavailable/malformed/replay/wrong-domain/wrong-key/wrong-transcript release checks;
* hiding CodeQL failure/timeout/skip;
* hiding regression failures.

Suggested Run 299 next step after successful Run 298:

Begin the next Red-row closure campaign.

Preferred next target:

Run 299 — source/test real on-chain governance proof verifier implementation.

Reason: after Run 298, durable replay, RemoteSigner, KMS/HSM custody backend, and custody attestation verifier can all be Green-for-scope. The next C4 blocker is the real on-chain governance proof verifier that gates production authority lifecycle decisions before governance execution / validator-set rotation.

Run 299 should be source/test only, default Disabled/fail-closed, MainNet refused unless production authority criteria are satisfied, release-binary evidence deferred to Run 300.

Required final response format:

1. Exact verdict.
2. Files changed.
3. Release artifacts and hashes.
4. Helper corpus results.
5. Real-binary scenarios.
6. Attestation class / policy release evidence.
7. Evidence/request/response/transcript/domain binding release evidence.
8. Accepted release evidence.
9. Rejection/fail-closed release evidence.
10. MainNet refusal / authority policy release evidence.
11. Replay/recovery/idempotency release evidence.
12. Production-binary default-disabled/silent evidence.
13. Tests run and results.
14. C4/C5 matrix taxonomy status.
15. Security invariants preserved.
16. Honest limitations.
17. C4/C5 status.
18. Suggested Run 299 next step.
