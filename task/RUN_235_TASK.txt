Task: Run 235 — Release-Binary Governance Evaluator Replay Consume Boundary Evidence

Current accepted state:

* Run 230: source/test governance evaluator replay/freshness state boundary landed.
* Run 231: release-binary replay/freshness state evidence fully closed.
* Run 232: source/test governance evaluator replay/freshness runtime integration landed.
* Run 233: release-binary replay/freshness runtime integration evidence fully closed.
* Run 234: source/test post-mutation replay consume boundary landed:

  * `crates/qbind-node/src/pqc_governance_evaluator_replay_consume_boundary.rs`;
  * pure fail-closed boundary separating:

    * pre-mutation freshness validation;
    * mutation authorization;
    * mutation completion;
    * explicit replay-state consume after success only;
  * `MutationAuthorizationOutcome`;
  * `MutationCompletionStatus`;
  * `ConsumeBoundaryOutcome`;
  * `evaluate_post_mutation_consume`;
  * `perform_post_mutation_consume`;
  * consume only occurs on the fixture `ConsumeFixtureAfterSuccess` path;
  * deferred, validation-only, before-apply, failed-apply, rolled-back, unsupported-surface, and MainNet-refused outcomes never consume;
  * fixture consume is DevNet/TestNet source-test only;
  * production/MainNet consume is callable but unavailable/fail-closed;
  * MainNet peer-driven apply remains refused and never consumes even when replay state is fresh;
  * no Run 070 call, no live trust swap, no marker/sequence write, no persistent storage;
  * release-binary consume-boundary evidence deferred to Run 235.
* Full C4 remains OPEN.
* C5 remains OPEN.

Objective:
Produce release-binary evidence for the Run 234 post-mutation governance evaluator replay consume boundary.

This run must prove in release mode that consume is after-success-only, fixture-only, non-mutating, and unavailable/fail-closed for production/MainNet. It must also prove that deferred, validation-only, failed, rolled-back, unsupported, and MainNet-refused paths do not consume.

Strict scope:

* Release-binary evidence only.
* Use release-built helper and real `target/release/qbind-node` where applicable.
* No production source behavior change unless a tiny harness-only fix is required.
* No real governance execution engine.
* No real on-chain governance proof verifier.
* No MainNet governance enablement.
* No MainNet peer-driven apply enablement.
* No validator-set rotation.
* No KMS/HSM backend implementation.
* No RemoteSigner backend implementation.
* No RocksDB schema change.
* No file-format change.
* No database migration.
* No wire/schema/marker/sequence/trust-bundle change.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* Do not weaken Runs 070, 130–234.
* Do not claim full C4 or C5 closure.

Required deliverables:

1. Add release-binary helper:
   `crates/qbind-node/examples/run_235_governance_evaluator_replay_consume_boundary_release_binary_helper.rs`

2. Add release-binary harness:
   `scripts/devnet/run_235_governance_evaluator_replay_consume_boundary_release_binary.sh`

3. Add evidence archive:
   `docs/devnet/run_235_governance_evaluator_replay_consume_boundary_release_binary/`

4. Add canonical evidence report:
   `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_235.md`

5. Update narrowly:

   * `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
   * `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
   * `docs/whitepaper/contradiction.md`
   * `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
   * `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
   * `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

6. Crosscheck all new/modified code and docs against existing design/spec. If contradictions are found, record them in `contradiction.md`.

Required release-helper matrix:
The release-built helper must exercise Run 234 consume-boundary symbols through production library code.

Accepted / compatible cases:
A1. legacy bypass does not consume.

A2. `ProceedDeferred` does not consume.

A3. validation-only success does not consume.

A4. authorized-but-not-applied does not consume.

A5. apply failed does not consume.

A6. rolled back mutation does not consume.

A7. unsupported surface does not consume.

A8. MainNet refused does not consume.

A9. DevNet fixture consume records consumed only after `AppliedSuccessfully`.

A10. TestNet fixture consume records consumed only after `AppliedSuccessfully`.

A11. after fixture consume, same decision validates as already consumed / fail-closed through Run 230 state.

A12. consume authorization digest is deterministic in release mode.

A13. consume transcript digest is deterministic in release mode.

A14. post-mutation consume record digest is deterministic in release mode.

A15. consume binding includes replay state key, request digest, response digest, decision digest, lifecycle action, candidate digest, sequence, replay nonce, environment, chain id, genesis hash, validation surface, mutation surface, and mutation completion status.

A16. production consume writer is callable and fails closed unavailable.

A17. MainNet consume writer is callable and fails closed unavailable/refused.

A18. Run 232 replay/freshness runtime integration remains compatible when consume boundary is not wired.

A19. Run 233 release behavior remains compatible.

A20. Run 231 replay/freshness standalone release behavior remains compatible.

A21. Run 229 peer evaluator-context release behavior remains compatible.

Required rejection cases:
R1. wrong replay state key digest rejected.

R2. wrong source identity digest rejected.

R3. wrong request digest rejected.

R4. wrong response digest rejected.

R5. wrong transcript digest rejected.

R6. wrong decision digest rejected.

R7. wrong proposal id rejected.

R8. wrong decision id rejected.

R9. wrong lifecycle action rejected.

R10. wrong candidate digest rejected.

R11. wrong authority-domain sequence rejected.

R12. wrong effective epoch rejected.

R13. wrong expiry epoch rejected.

R14. wrong replay nonce rejected.

R15. wrong environment rejected.

R16. wrong chain rejected.

R17. wrong genesis rejected.

R18. wrong validation surface rejected.

R19. wrong mutation surface rejected.

R20. consume attempted before apply rejected.

R21. consume attempted after failed apply rejected.

R22. consume attempted after rollback rejected.

R23. consume attempted on validation-only surface rejected.

R24. consume attempted on unsupported surface rejected.

R25. production consume unavailable rejected.

R26. MainNet consume unavailable/refused rejected.

R27. local operator cannot satisfy consume policy.

R28. peer majority cannot satisfy consume policy.

R29. validator-set rotation unsupported rejected.

R30. policy-change action unsupported rejected.

R31. malformed consume state rejected.

R32. consume rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

R33. MainNet peer-driven apply remains refused and does not consume even if replay state is fresh.

Required real release checks:
Use release artifacts to prove:

* helper binary SHA-256 and ELF Build ID captured;
* `qbind-node` release binary SHA-256 and ELF Build ID captured if built;
* Run 234 consume-boundary helper corpus passes;
* consume is after-success-only;
* deferred is not consumed;
* validation-only is not consumed;
* failed apply is not consumed;
* rolled-back mutation is not consumed;
* unsupported surface is not consumed;
* MainNet refused is not consumed;
* fixture consume records consumed only after explicit success;
* production/MainNet consume unavailable is fail-closed;
* no storage/schema/migration/RocksDB/file-format drift;
* no marker/sequence write from consume boundary;
* no Run 070 call from consume boundary;
* MainNet peer-driven apply remains refused;
* no real governance engine active claim;
* no real on-chain proof verifier active claim;
* no MainNet governance enabled claim;
* no validator-set rotation claim;
* no KMS/HSM backend active claim;
* no RemoteSigner backend active claim;
* no peer-majority authority claim;
* no automatic apply / apply-on-receipt claim.

Required source/release reachability proof:
The report must include grep/call-site proof for:

* `pqc_governance_evaluator_replay_consume_boundary`;
* `MutationAuthorizationOutcome`;
* `MutationCompletionStatus`;
* `ConsumeBoundaryOutcome`;
* `evaluate_post_mutation_consume`;
* `perform_post_mutation_consume`;
* `ConsumeFixtureAfterSuccess`;
* `FailClosedConsumeUnavailable`;
* `FailClosedProductionConsumeUnavailable`;
* `FailClosedMainNetConsumeUnavailable`;
* consume authorization digest helper;
* consume transcript digest helper;
* post-mutation consume record digest helper;
* fixture consume writer path;
* production consume unavailable path;
* MainNet consume unavailable/refused path;
* MainNet peer-driven refusal guard.

Required mutation / no-mutation proof:
For every rejected consume-boundary case:

* no Run 070 apply call;
* no live trust swap;
* no session eviction;
* no sequence write;
* no marker write;
* no `.tmp` residue;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead;
* validation-only does not consume;
* failed apply does not consume;
* rollback does not consume;
* MainNet refused does not consume.

Required denylist:
Across the run prove:

* no MainNet apply;
* no autonomous apply;
* no apply on receipt;
* no peer-majority authority;
* no real governance execution engine claim;
* no production governance active claim;
* no MainNet governance enabled claim;
* no real on-chain governance proof verifier claim;
* no real KMS backend claim;
* no real HSM backend claim;
* no real RemoteSigner backend claim;
* no custody attestation production-active claim;
* no validator-set rotation claim;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead;
* no RocksDB schema change;
* no file format change;
* no database migration;
* no network wire schema change;
* no trust-bundle schema change;
* no marker schema change;
* no sequence schema change;
* no marker write before sequence commit;
* no sequence write on validation-only surfaces;
* no marker write on validation-only surfaces.

Required captured metadata:

* helper SHA-256 and ELF Build ID;
* qbind-node SHA-256 and ELF Build ID if built;
* git commit hash;
* rustc/cargo versions;
* exact commands;
* stdout/stderr logs;
* per-scenario exit codes;
* consume authorization digests;
* consume transcript digests;
* post-mutation consume record digests;
* consume boundary outcome values;
* mutation completion status values;
* before/after fixture replay-store state snapshots;
* marker/sequence before/after hashes where applicable;
* data-dir inventories;
* denylist grep results.

Validation commands:
Run at minimum:

* `cargo build --release -p qbind-node --bin qbind-node`
* `cargo build --release -p qbind-node --example run_235_governance_evaluator_replay_consume_boundary_release_binary_helper`
* `bash scripts/devnet/run_235_governance_evaluator_replay_consume_boundary_release_binary.sh`
* `cargo test -p qbind-node --test run_234_governance_evaluator_replay_consume_boundary_tests`
* `cargo test -p qbind-node --test run_232_governance_evaluator_replay_runtime_integration_tests`
* `cargo test -p qbind-node --test run_230_governance_evaluator_replay_state_tests`
* `cargo test -p qbind-node --test run_228_peer_evaluator_context_representation_tests`
* `cargo test -p qbind-node --test run_226_governance_evaluator_runtime_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_224_governance_evaluator_runtime_integration_tests`
* `cargo test -p qbind-node --test run_222_governance_execution_evaluator_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Documentation requirements:
`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_235.md` must state:

* Run 235 is release-binary post-mutation replay consume boundary evidence.
* Consume is release-evidenced as after-success-only.
* Deferred, validation-only, failed apply, rollback, unsupported surface, and MainNet refused outcomes do not consume.
* Fixture consume remains DevNet/TestNet evidence-only.
* Production/MainNet consume remains unavailable/fail-closed.
* No RocksDB/file/schema/migration/storage-format change is implemented.
* No real governance engine or on-chain proof verifier is implemented.
* MainNet peer-driven apply remains refused.
* Validator-set rotation remains unsupported.
* Full C4 remains OPEN.
* C5 remains OPEN.

Acceptance criteria:
Run 235 is acceptable only if:

1. Release-built helper exercises the Run 234 consume boundary.
2. Consume is allowed only after successful mutation completion.
3. Deferred is not consumed.
4. Validation-only is not consumed.
5. Failed/rolled-back mutation is not consumed.
6. Fixture consume remains DevNet/TestNet evidence-only.
7. Production/MainNet consume remains unavailable/fail-closed.
8. Rejections are non-mutating.
9. MainNet peer-driven apply remains refused and does not consume.
10. No storage/schema/migration/RocksDB/file-format change is claimed.
11. Existing Run 233, Run 231, Run 229, Run 227, and Run 225 release behavior remains compatible.
12. No full C4 or C5 closure is claimed.
