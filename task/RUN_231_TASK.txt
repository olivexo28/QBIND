Task: Run 231 — Release-Binary Governance Evaluator Replay and Freshness State Evidence

Current accepted state:

* Run 230: source/test governance evaluator replay and freshness state boundary landed:

  * `crates/qbind-node/src/pqc_governance_evaluator_replay_state.rs`;
  * `EvaluatorReplayFreshnessInput`;
  * `EvaluatorReplayFreshnessExpectations`;
  * `ReplayFreshnessState`;
  * `EvaluatorReplayFreshnessOutcome`;
  * deterministic digest helpers:

    * replay state key digest;
    * replay observation digest;
    * consumed decision digest;
    * freshness transcript digest;
  * `GovernanceEvaluatorReplayStateReader`;
  * `GovernanceEvaluatorReplayStateWriter`;
  * fixture DevNet/TestNet replay state store;
  * production replay state reader unavailable/fail-closed;
  * MainNet replay state reader unavailable/fail-closed;
  * read-only validation does not mark consumed;
  * explicit consume marks consumed only in fixture source tests;
  * replay/freshness rejection is pure and non-mutating;
  * MainNet peer-driven apply remains refused even when replay state is fresh;
  * validator-set rotation remains unsupported;
  * no storage/schema/migration/RocksDB/file-format change;
  * release-binary replay/freshness evidence deferred to Run 231.
* Full C4 remains OPEN.
* C5 remains OPEN.

Objective:
Produce release-binary evidence for the Run 230 governance evaluator replay and freshness state boundary.

This run must prove in release mode that the replay/freshness boundary distinguishes fresh, deferred, expired, stale, replayed, already consumed, superseded, wrong-binding, unavailable, production-unavailable, and MainNet-unavailable outcomes; deterministic digests are stable; read-only validation does not consume; explicit fixture consume marks consumed; production/MainNet state remains unavailable/fail-closed; and no mutation can occur from replay/freshness rejection.

Strict scope:

* Release-binary evidence only.
* Use release-built helper and real `target/release/qbind-node` where applicable.
* No production source behavior change unless a tiny harness-only fix is required.
* No real governance execution engine.
* No real on-chain governance proof verifier.
* No MainNet governance enablement.
* No MainNet peer-driven apply enablement.
* No validator-set rotation.
* No KMS/HSM backend implementation.
* No RemoteSigner backend implementation.
* No RocksDB schema change.
* No file format change.
* No database migration.
* No network wire schema change.
* No trust-bundle schema change.
* No authority-marker schema change.
* No sequence schema change.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* Do not weaken Runs 070, 130–230.
* Do not claim full C4 or C5 closure.

Required deliverables:

1. Add release-binary helper:
   `crates/qbind-node/examples/run_231_governance_evaluator_replay_state_release_binary_helper.rs`

2. Add release-binary harness:
   `scripts/devnet/run_231_governance_evaluator_replay_state_release_binary.sh`

3. Add evidence archive:
   `docs/devnet/run_231_governance_evaluator_replay_state_release_binary/`

4. Add canonical evidence report:
   `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_231.md`

5. Update narrowly:

   * `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
   * `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
   * `docs/whitepaper/contradiction.md`
   * `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
   * `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
   * `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

6. Crosscheck all new/modified code and docs against existing design/spec. If contradictions are found, record them in `contradiction.md`.

Required release-helper matrix:
The release-built helper must exercise Run 230 replay/freshness symbols through production library code.

Accepted / compatible cases:
A1. DevNet fixture replay state accepts first-seen fresh evaluator decision.

A2. TestNet fixture replay state accepts first-seen fresh evaluator decision.

A3. fresh but not-yet-effective decision returns `ProceedDeferred`, not mutation approval.

A4. decision at effective epoch returns `ProceedFresh`.

A5. decision before expiry returns `ProceedFresh`.

A6. replay state key digest is deterministic in release mode.

A7. replay observation digest is deterministic in release mode.

A8. consumed decision digest is deterministic in release mode.

A9. freshness transcript digest is deterministic in release mode.

A10. replay state key binds environment, chain id, genesis hash, source identity digest, request digest, response digest, proposal id, decision id, lifecycle action, candidate digest, sequence, and replay nonce.

A11. fixture writer records consumed decision only after explicit consume call.

A12. read-only validation does not mark consumed.

A13. production replay state reader is callable and returns unavailable/fail-closed.

A14. MainNet replay state reader is callable and returns unavailable/fail-closed.

A15. Run 224 integration remains compatible when replay state policy is Disabled / not wired.

A16. Run 228 peer evaluator context remains compatible when replay state policy is Disabled / not wired.

A17. first-seen → observe → replay → consume lifecycle behaves deterministically.

A18. explicit consume converts future same-decision validation to already-consumed / fail-closed.

A19. MainNet peer-driven apply remains refused even when replay state is fresh.

Required rejection cases:
R1. expired decision rejected.

R2. stale decision rejected.

R3. replayed decision rejected.

R4. already-consumed decision rejected.

R5. superseded decision rejected.

R6. wrong effective epoch rejected.

R7. wrong expiry epoch rejected.

R8. wrong environment rejected.

R9. wrong chain rejected.

R10. wrong genesis rejected.

R11. wrong validation surface rejected.

R12. wrong source identity digest rejected.

R13. wrong request digest rejected.

R14. wrong response digest rejected.

R15. wrong transcript digest rejected.

R16. wrong proposal id rejected.

R17. wrong decision id rejected.

R18. wrong lifecycle action rejected.

R19. wrong candidate digest rejected.

R20. wrong authority-domain sequence rejected.

R21. wrong replay nonce rejected.

R22. malformed state rejected.

R23. state unavailable rejected.

R24. production state unavailable rejected.

R25. MainNet state unavailable rejected.

R26. local operator cannot satisfy replay state policy.

R27. peer majority cannot satisfy replay state policy.

R28. validator-set rotation unsupported rejected.

R29. policy-change action unsupported rejected.

R30. validation-only rejection writes no marker and no sequence.

R31. mutating rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

R32. MainNet peer-driven apply remains refused even when replay state is fresh.

Required real release checks:
Use release artifacts to prove:

* helper binary SHA-256 and ELF Build ID captured;
* `qbind-node` release binary SHA-256 and ELF Build ID captured if built;
* Run 230 replay/freshness helper corpus passes;
* fresh/deferred/expired/stale/replay/consumed/superseded outcomes are distinguished;
* read-only validation does not consume;
* explicit consume marks consumed only in fixture evidence;
* production/MainNet state unavailable is fail-closed;
* no storage/schema/migration/RocksDB/file-format drift;
* MainNet peer-driven apply remains refused;
* no real governance engine active claim;
* no real on-chain proof verifier active claim;
* no MainNet governance enabled claim;
* no validator-set rotation claim;
* no KMS/HSM backend active claim;
* no RemoteSigner backend active claim;
* no peer-majority authority claim;
* no automatic apply / apply-on-receipt claim.

Required source/release reachability proof:
The report must include grep/call-site proof for:

* `pqc_governance_evaluator_replay_state`;
* `EvaluatorReplayFreshnessInput`;
* `EvaluatorReplayFreshnessExpectations`;
* `ReplayFreshnessState`;
* `EvaluatorReplayFreshnessOutcome`;
* `GovernanceEvaluatorReplayStateReader`;
* `GovernanceEvaluatorReplayStateWriter`;
* fixture replay state store;
* production replay state reader;
* MainNet replay state reader;
* replay state key digest helper;
* replay observation digest helper;
* consumed decision digest helper;
* freshness transcript digest helper;
* read-only validation path;
* explicit consume path;
* production/MainNet unavailable fail-closed path;
* MainNet peer-driven refusal guard.

Required mutation / no-mutation proof:
For every rejected replay/freshness case:

* no Run 070 apply call;
* no live trust swap;
* no session eviction;
* no sequence write;
* no marker write;
* no `.tmp` residue;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead;
* validation-only rejection writes no marker and no sequence;
* mutating rejection produces no mutation.

Required denylist:
Across the run prove:

* no MainNet apply;
* no autonomous apply;
* no apply on receipt;
* no peer-majority authority;
* no real governance execution engine claim;
* no production governance active claim;
* no MainNet governance enabled claim;
* no real on-chain governance proof verifier claim;
* no real KMS backend claim;
* no real HSM backend claim;
* no real RemoteSigner backend claim;
* no custody attestation production-active claim;
* no validator-set rotation claim;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead;
* no RocksDB schema change;
* no file format change;
* no database migration;
* no network wire schema change;
* no trust-bundle schema change;
* no marker schema change;
* no sequence schema change;
* no marker write before sequence commit;
* no sequence write on validation-only surfaces;
* no marker write on validation-only surfaces.

Required captured metadata:

* helper SHA-256 and ELF Build ID;
* qbind-node SHA-256 and ELF Build ID if built;
* git commit hash;
* rustc/cargo versions;
* exact commands;
* stdout/stderr logs;
* per-scenario exit codes;
* replay state key digests;
* replay observation digests;
* consumed decision digests;
* freshness transcript digests;
* replay/freshness classification values;
* replay/freshness outcome values;
* before/after fixture replay-store state snapshots;
* marker/sequence before/after hashes where applicable;
* data-dir inventories;
* denylist grep results.

Validation commands:
Run at minimum:

* `cargo build --release -p qbind-node --bin qbind-node`
* `cargo build --release -p qbind-node --example run_231_governance_evaluator_replay_state_release_binary_helper`
* `bash scripts/devnet/run_231_governance_evaluator_replay_state_release_binary.sh`
* `cargo test -p qbind-node --test run_230_governance_evaluator_replay_state_tests`
* `cargo test -p qbind-node --test run_228_peer_evaluator_context_representation_tests`
* `cargo test -p qbind-node --test run_226_governance_evaluator_runtime_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_224_governance_evaluator_runtime_integration_tests`
* `cargo test -p qbind-node --test run_222_governance_execution_evaluator_tests`
* `cargo test -p qbind-node --test run_220_governance_execution_runtime_consumption_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Documentation requirements:
`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_231.md` must state:

* Run 231 is release-binary governance evaluator replay/freshness state evidence.
* The replay/freshness boundary distinguishes fresh, deferred, expired, stale, replayed, already-consumed, superseded, wrong-binding, unavailable, production-unavailable, and MainNet-unavailable outcomes.
* Fixture replay state remains DevNet/TestNet evidence-only.
* Production/MainNet replay state remains unavailable/fail-closed.
* Read-only validation does not mark consumed.
* Explicit consume marks consumed only in fixture evidence.
* No RocksDB/file/schema/migration/storage format change is implemented.
* MainNet peer-driven apply remains refused.
* Validator-set rotation remains unsupported.
* No real governance engine or on-chain proof verifier is implemented.
* Full C4 remains OPEN.
* C5 remains OPEN.

Acceptance criteria:
Run 231 is acceptable only if:

1. Release-built helper exercises the Run 230 replay/freshness state boundary.
2. Fresh / expired / stale / replay / consumed / superseded outcomes are release-evidenced.
3. Production/MainNet state remains unavailable/fail-closed.
4. Read-only validation does not mark consumed.
5. Explicit consume marks consumed only in fixture evidence.
6. Rejections are non-mutating.
7. MainNet peer-driven apply remains refused.
8. Validator-set rotation remains unsupported.
9. No storage/schema/migration/RocksDB/file-format change is claimed.
10. Existing Run 229, Run 227, Run 225, and Run 223 release behavior remains compatible.
11. No full C4 or C5 closure is claimed.
