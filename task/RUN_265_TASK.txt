Run 265 — Release-binary durable-completion consumer settlement-projection sink boundary evidence

You are continuing QBIND after Run 264.

Current accepted state:

Run 264 closed the source/test durable-completion consumer settlement-projection sink boundary.

Run 264 added:

* `crates/qbind-node/src/pqc_governance_durable_completion_consumer_settlement_projection.rs`
* `crates/qbind-node/tests/run_264_durable_completion_consumer_settlement_projection_tests.rs`
* `crates/qbind-node/src/lib.rs` registration
* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_264.md`
* narrow protocol/ops doc updates
* `docs/whitepaper/contradiction.md` no-contradiction entry

Run 264 is source/test only. It has no release-binary evidence yet.

Run 264 scan closure is complete:

* Secret scan: clean.
* CodeQL Rust: invoked but no analysis performed because the diff had no analyzable code changes; CodeQL coverage is not claimed.

Full C4 remains OPEN. C5 remains OPEN.

Strict workflow:

* Execution-first.
* Narrow scope only.
* No broad redesign.
* No silent regressions.
* No hype or overclaiming.
* Release-binary evidence only.
* No new source/test feature boundary.
* No production runtime enablement.
* No real settlement backend.
* No real settlement execution.
* No real audit-ledger acknowledgement backend.
* No real external-publication confirmation backend.
* No real external publication.
* No real production attestation backend.
* No real finalization backend.
* No real completion-report backend.
* No real durable consume backend.
* No real persistent replay backend.
* No real governance execution engine.
* No real production mutation engine.
* No real on-chain governance proof verifier.
* No KMS/HSM/RemoteSigner backend.
* No RocksDB/file/schema/migration/storage-format change.
* No wire/schema/marker/sequence/trust-bundle change.
* No Run 070 call.
* No `LivePqcTrustState` mutation.
* No real trust swap.
* No session eviction.
* No sequence write.
* No marker write.
* No network I/O or external settlement.
* No MainNet governance.
* No MainNet peer-driven apply.
* No validator-set rotation.
* Keep Full C4 OPEN.
* Keep C5 OPEN.

Objective:

Produce release-binary evidence for the Run 264 durable-completion consumer settlement-projection sink boundary, mirroring the Run 263 → Run 262 release-binary evidence pattern.

Run 265 must prove on real `target/release/qbind-node` plus a release-built helper that the Run 264 settlement-projection sink boundary symbols are present in release-built code, that the helper exercises the Run 264 production library symbols in release mode, and that the settlement-projection path remains fixture/in-memory/dev-test only — not production-enabled.

Required deliverables:

1. Add release helper:

   `crates/qbind-node/examples/run_265_durable_completion_consumer_settlement_projection_release_binary_helper.rs`

2. Add release harness:

   `scripts/devnet/run_265_durable_completion_consumer_settlement_projection_release_binary.sh`

3. Add evidence archive directory:

   `docs/devnet/run_265_durable_completion_consumer_settlement_projection_release_binary/`

   Track only:

   * `README.md`
   * `summary.txt`
   * `.gitignore`

   Generated logs, helper tables, reachability files, provenance files, and test logs must be regenerated and ignored.

4. Add canonical evidence report:

   `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_265.md`

5. Update narrowly:

   * `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
   * `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
   * `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
   * `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
   * `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
   * `docs/whitepaper/contradiction.md`

Required existing ordering:

1. Run 246 pipeline success is required before any Run 248 sink intent can exist.
2. Run 248 `ConsumeReceiptRecorded` is required before any Run 250 completion-report intent can exist.
3. Run 250 `CompletionReportRecorded` is required before any Run 252 finalization intent can exist.
4. Run 252 `DurableCompletionFinalized` is required before any Run 254 attestation intent can exist.
5. Run 254 `DurableCompletionAttested` is required before any Run 256 backend request can exist.
6. Run 256 `BackendSubmissionRecorded` is required before any Run 258 audit/publication receipt request can exist.
7. Run 258 `AuditReceiptRecorded` is required before any Run 260 acknowledgement / confirmation request can exist.
8. Run 260 `AcknowledgementRecorded` is required before any Run 262 acknowledgement-consumer request can exist.
9. Run 262 `AcknowledgementConsumed` is required before any Run 264 settlement-projection request can exist.
10. Only Run 264 `SettlementProjectionRecorded` authorizes a new modeled settlement-projection state.

Helper requirements:

The release helper must link against release-built production library symbols and exercise the actual Run 264 API, including the exact implemented names for:

* `pqc_governance_durable_completion_consumer_settlement_projection`
* `DurableCompletionConsumerSettlementProjectionInput`
* `DurableCompletionConsumerSettlementProjectionPolicy`
* `DurableCompletionConsumerSettlementProjectionKind`
* `DurableCompletionConsumerSettlementProjectionIdentity`
* `DurableCompletionConsumerSettlementProjectionExpectations`
* `DurableCompletionConsumerSettlementProjectionRequest`
* `DurableCompletionConsumerSettlementProjectionResponse`
* `DurableCompletionConsumerSettlementProjectionRecord`
* `DurableCompletionConsumerSettlementProjectionLedger`
* `DurableCompletionConsumerSettlementProjectionLedgerRecord`
* `DurableCompletionConsumerSettlementProjectionLedgerSnapshot`
* `DurableCompletionConsumerSettlementProjectionLedgerStatus`
* `DurableCompletionConsumerSettlementProjectionDigest`
* `DurableCompletionConsumerSettlementProjectionTranscriptDigest`
* `DurableCompletionConsumerSettlementProjectionOutcome`
* `GovernanceDurableCompletionConsumerSettlementProjectionSink`
* `FixtureDurableCompletionConsumerSettlementProjectionSink`
* `ProductionSettlementProjectionSink`
* `MainNetSettlementProjectionSink`
* `ExternalSettlementProjectionSink`
* `project_consumer_outcome_to_settlement_projection_request`
* `evaluate_durable_completion_consumer_settlement_projection`
* `recover_durable_completion_consumer_settlement_projection_window`
* `settlement_projection_outcome_authorizes_record`
* `settlement_projection_outcome_projects_to_recorded`
* all Run 264 grep-verifiable invariant helpers.

If exact symbol names differ, use the actual implemented Run 264 API names and record substitutions honestly in `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_265.md`. Do not add compatibility shims unless strictly necessary for compilation, and do not change production behavior.

Helper architecture:

Copy the Run 263 release helper structure and adapt it to Run 264.

The helper must attach to the full real modeled chain:

* Run 256 backend submission
* Run 258 audit receipt
* Run 260 acknowledgement
* Run 262 acknowledgement consumer
* Run 264 settlement projection

Do not fabricate a standalone settlement-projection request.

Add or copy attachment helpers from the Run 264 tests:

* attach Run 256 backend submission
* attach Run 258 receipt
* attach Run 260 acknowledgement
* attach Run 262 consumer
* create Run 264 settlement-projection input from the actual Run 262 consumer outcome and digest binding

Critical projection rule:

The Run 265 helper must drive projection from the Run 262 consumer outcome:

* `input.consumer_binding`
* `project_consumer_outcome_to_settlement_projection_request`

Do NOT project from Run 260 acknowledgement, Run 258 receipt, or Run 256 backend outcome directly.

Required projection mapping:

* Run 262 `AcknowledgementConsumed` → creates settlement-projection request.
* Run 262 `AcknowledgementConsumerDuplicateIdempotent` → idempotent-only, never creates a new settlement-projection record by itself.
* Run 262 legacy/no-consumer outcomes → legacy/no-settlement-projection.
* Run 262 rejected-before-consumer / consumer-did-not-record / rejected-before-record / record failure / rollback / rollback-failed / ambiguous window outcomes → no-projection fail-closed.
* Run 262 production/MainNet/external unavailable outcomes → corresponding no-projection unavailable/fail-closed.
* Run 262 `MainNetPeerDrivenApplyRefusedNoConsumer` → `MainNetPeerDrivenApplyRefusedNoProjection`.
* Run 262 `ValidatorSetRotationUnsupportedNoConsumer` → `ValidatorSetRotationUnsupportedNoProjection`.
* Run 262 `PolicyChangeUnsupportedNoConsumer` → `PolicyChangeUnsupportedNoProjection`.
* Every other non-recorded consumer outcome → no-projection fail-closed.

Helper corpus:

The helper must include these tables:

* accepted / compatible
* rejection / fail-closed
* recovery / crash-window
* projection
* stage-ordering
* settlement-projection-ledger
* non-mutation
* reachability

Accepted / compatible cases must prove at minimum:

* Disabled settlement-projection policy preserves legacy no-projection bypass.
* Disabled consumer policy never invokes settlement projection.
* Disabled acknowledgement policy never invokes consumer or settlement projection.
* Disabled receipt policy never invokes acknowledgement, consumer, or settlement projection.
* Disabled backend policy never invokes receipt, acknowledgement, consumer, or settlement projection.
* Disabled attestor/finalizer/reporter/sink/pipeline/evaluator-call-site policies never reach settlement projection.
* DevNet fixture chain records exactly one modeled in-memory settlement-projection record only after the full Run 246 → 248 → 250 → 252 → 254 → 256 → 258 → 260 → 262 → 264 chain.
* TestNet fixture chain records exactly one modeled in-memory settlement-projection record only after the same full chain.
* Add-root / retire-root / revoke-root / emergency-revoke-root / noop modeled action variants preserve ordering and project only after Run 262 `AcknowledgementConsumed`.
* Duplicate identical settlement-projection record is idempotent.
* Run 262 duplicate-idempotent consumer outcome may only match an already-recorded settlement-projection record and never creates a new projection by itself.
* Existing Run 264 source/test behavior remains compatible.
* Existing Run 262 consumer behavior remains compatible.
* Existing Run 260 acknowledgement behavior remains compatible.
* Existing Run 258 receipt behavior remains compatible.
* Existing Run 256 backend behavior remains compatible.
* Existing Run 254 attestation behavior remains compatible.
* Existing Run 252 finalization behavior remains compatible.
* Existing Run 250 completion-reporter behavior remains compatible.
* Existing Run 248 sink behavior remains compatible.
* Existing Run 246 pipeline behavior remains compatible.
* Existing Run 244 modeled applier behavior remains compatible.
* Existing Run 242 mutation-engine behavior remains compatible.
* Existing Run 240 durable completion behavior remains compatible.
* Production settlement-projection path is reachable but unavailable/fail-closed and records no state.
* MainNet settlement-projection path is reachable but unavailable/fail-closed and records no state.
* External settlement-projection path is reachable but unavailable/fail-closed and performs no publication/network I/O.
* MainNet peer-driven apply is refused before pipeline progression, sink invocation, reporter invocation, finalizer invocation, attestor invocation, backend invocation, receipt invocation, acknowledgement invocation, consumer invocation, and settlement-projection invocation.
* Validator-set rotation unsupported and records no state.
* Policy-change unsupported and records no state.

Rejected / fail-closed cases must prove at minimum:

* Every rejection before Run 264 produces no settlement-projection request and no record.
* Consumer did-not-record / rejected-before-record / record failure / rollback / rollback-failed / ambiguous window produces no settlement-projection invocation and no record.
* Settlement projection before Run 262 `AcknowledgementConsumed` is rejected.
* Settlement projection after modeled apply failure, sink failure, reporter failure, finalizer failure, attestor failure, backend failure, receipt failure, acknowledgement failure, consumer failure, consumer rollback, consumer rollback-failed, or consumer ambiguous window is rejected.
* Same settlement-projection record id with different digest fails closed as equivocation.
* Wrong environment / chain / genesis / governance surface / mutation surface fails before settlement-projection invocation.
* Wrong settlement-projection identity / policy / kind / request digest / response digest / record digest fails closed.
* Wrong consumer identity / request / response / record / transcript digest fails closed.
* Wrong acknowledgement identity / request / response / record / transcript digest fails closed.
* Wrong receipt identity / request / response / record / transcript digest fails closed.
* Wrong backend identity / request / response / receipt / transcript digest fails closed.
* Wrong attestation / finalization / completion-report / sink / reporter / pipeline digest fails closed.
* Wrong proposal id / decision id / candidate digest / authority-domain sequence fails closed.
* Malformed request / response fails closed.
* Local operator key cannot satisfy MainNet authority.
* Peer majority cannot satisfy MainNet authority.
* Production/MainNet/external settlement-projection unavailable paths record no state and perform no publication.
* Rejected paths produce no Run 070 call, no `LivePqcTrustState` mutation, no trust swap, no session eviction, no sequence write, no marker write, no RocksDB/file/schema/migration/storage-format change, no external publication, no confirmation, no real audit-ledger write, and no real settlement write.

Recovery / crash-window cases must prove at minimum:

* before-pipeline through after-consumer-success-before-settlement-projection-request windows fail closed unless explicit matching consumer success exists.
* after-settlement-projection-request-before-record fails closed.
* after-settlement-projection-record-before-success fails closed unless explicit matching settlement-projection record exists.
* after-settlement-projection-success recovers as recorded.
* ambiguous, record-failed, rollback-completed, rollback-failed, unknown, production/MainNet/external windows fail closed.
* MainNet peer-driven apply refusal precedes recovery classification where applicable.

Projection cases must prove at minimum:

* Only Run 262 `AcknowledgementConsumed` creates a settlement-projection request.
* Run 246 pipeline success alone creates no settlement-projection request.
* Run 248 sink receipt alone creates no settlement-projection request.
* Run 250 completion report alone creates no settlement-projection request.
* Run 252 finalization alone creates no settlement-projection request.
* Run 254 attestation alone creates no settlement-projection request.
* Run 256 backend submission alone creates no settlement-projection request.
* Run 258 audit receipt alone creates no settlement-projection request.
* Run 260 acknowledgement alone creates no settlement-projection request.
* Run 262 consumer request alone creates no settlement-projection request.
* Run 262 duplicate-idempotent consumer outcome creates no new settlement-projection record unless a matching projection record already exists.
* Only `SettlementProjectionRecorded` authorizes modeled settlement-projection state.
* Every other outcome does not record.

Stage-ordering cases must prove at minimum:

* MainNet peer-driven apply refusal happens before every later stage.
* Evaluator/call-site authorization precedes durable replay.
* Durable replay precedes mutation engine.
* Mutation engine precedes modeled applier.
* Modeled applier precedes sink intent.
* Sink receipt precedes completion report.
* Completion report precedes finalization.
* Finalization precedes attestation.
* Attestation precedes backend request.
* Backend submission precedes audit receipt request.
* Audit receipt precedes acknowledgement request.
* Acknowledgement record precedes consumer request.
* Consumer record precedes settlement-projection request.
* Settlement-projection record happens only after consumer success.
* Rejection before settlement-projection stage leaves settlement-projection invocation count zero.
* Settlement-projection record failure does not invalidate consumer state but does not authorize projection state.
* Settlement-projection rollback failure is fatal/fail-closed and does not authorize projection state.

Settlement-projection ledger cases must prove at minimum:

* One valid settlement-projection record inserts one in-memory record.
* Duplicate identical settlement-projection record is idempotent and does not increase count.
* Same record id with different digest is equivocation and does not increase count.
* Wrong projection / consumer / acknowledgement / receipt / backend / attestation / finalization / report / sink / pipeline / proposal / decision / candidate / sequence digest does not record.
* Malformed request / response does not record.
* Rollback restores in-memory ledger snapshot.
* Rollback failure never claims success.
* Fixture ledger never writes files, RocksDB, markers, sequences, external publications, confirmations, audit-ledger entries, settlement records, or production durable state.

Non-mutation cases must prove at minimum:

* no Run 070 call;
* no `LivePqcTrustState` mutation;
* no trust swap;
* no session eviction;
* no sequence write;
* no marker write;
* no RocksDB/file/schema/migration/storage-format write;
* no wire/schema/marker/sequence/trust-bundle change;
* no external network publication;
* no real settlement;
* no real external-publication confirmation;
* no real audit-ledger write;
* fixture settlement-projection sink mutates only modeled in-memory `DurableCompletionConsumerSettlementProjectionLedger`;
* production/MainNet/external paths remain unavailable/fail-closed.

Real release-binary scenarios:

The harness must run the real `target/release/qbind-node` for at least:

* S1 — `--help`: no settlement-projection / settlement / external-publication / confirmation production enablement banner or public flag drift.
* S2 — default DevNet startup parse/smoke: no settlement-projection / settlement / publication / confirmation enablement claim.
* S3 — default TestNet startup parse/smoke: no settlement-projection / settlement / publication / confirmation enablement claim.
* S4 — default MainNet startup parse/smoke: no settlement-projection / settlement / publication / confirmation enablement claim.
* S5 — hidden governance-execution selector or equivalent hidden selector still parses as before, without exposing the Run 264/265 helper-only path as public production enablement.
* S6 — invalid governance-execution selector or equivalent invalid selector fails closed before mutation if applicable, with no marker write, no sequence write, no live trust swap, no session eviction, no Run 070 call, no receipt sink invocation, no acknowledgement sink invocation, no consumer invocation, no settlement-projection invocation, no audit-ledger write, no external-publication confirmation, no external publication, and no settlement.

Do not add any new public CLI surface for Run 265. If a hidden selector already exists from earlier runs, the harness may use it only as a parse/fail-closed smoke surface.

Denylist:

Prove captured real-binary logs and helper logs do not contain active/enabled claims for:

* real settlement backend enabled;
* durable-completion settlement-projection production enabled;
* MainNet settlement projection enabled;
* external settlement projection enabled;
* real audit-ledger acknowledgement backend enabled;
* real external-publication confirmation backend enabled;
* real external publication backend enabled;
* real production attestation backend enabled;
* real finalization backend enabled;
* real completion-report backend enabled;
* real durable consume backend enabled;
* real persistent replay backend enabled;
* real production mutation engine enabled;
* MainNet mutation engine enabled;
* MainNet governance enabled;
* MainNet peer-driven apply enabled;
* real governance execution engine enabled;
* real on-chain governance proof verifier enabled;
* RocksDB replay backend enabled;
* file replay backend enabled;
* schema migration enabled;
* storage-format migration enabled;
* KMS/HSM backend enabled;
* RemoteSigner backend enabled;
* validator-set rotation enabled;
* policy-change action enabled;
* autonomous apply;
* apply-on-receipt;
* apply-on-acknowledgement;
* apply-on-consumer;
* apply-on-settlement-projection;
* apply-on-settlement;
* peer-majority authority;
* Run 070 apply from the settlement-projection boundary;
* `LivePqcTrustState` mutation from the settlement-projection boundary;
* real trust swap from the settlement-projection boundary;
* session eviction from the settlement-projection boundary;
* marker write from the settlement-projection boundary;
* sequence write from the settlement-projection boundary;
* RocksDB write from the settlement-projection boundary;
* file write from the settlement-projection boundary;
* external publication by the fixture settlement-projection sink;
* external-publication confirmation by the fixture settlement-projection sink;
* audit-ledger write by the fixture settlement-projection sink;
* settlement write by the fixture settlement-projection sink;
* production durable consume by the fixture settlement-projection sink;
* production finalization by the fixture settlement-projection sink;
* production attestation by the fixture settlement-projection sink;
* production backend submission by the fixture settlement-projection sink;
* production audit receipt by the fixture settlement-projection sink;
* production acknowledgement by the fixture settlement-projection sink;
* production consumer by the fixture settlement-projection sink;
* DummySig / DummyKem / DummyAead active on production path.

Source/release reachability proof:

The harness must grep production source for Run 264 symbols and prove the release helper exercises them in release mode.

Required greps include:

* `pqc_governance_durable_completion_consumer_settlement_projection`
* `DurableCompletionConsumerSettlementProjectionInput`
* `DurableCompletionConsumerSettlementProjectionPolicy`
* `DurableCompletionConsumerSettlementProjectionKind`
* `DurableCompletionConsumerSettlementProjectionIdentity`
* `DurableCompletionConsumerSettlementProjectionExpectations`
* `DurableCompletionConsumerSettlementProjectionRequest`
* `DurableCompletionConsumerSettlementProjectionResponse`
* `DurableCompletionConsumerSettlementProjectionRecord`
* `DurableCompletionConsumerSettlementProjectionLedger`
* `DurableCompletionConsumerSettlementProjectionLedgerRecord`
* `DurableCompletionConsumerSettlementProjectionLedgerSnapshot`
* `DurableCompletionConsumerSettlementProjectionLedgerStatus`
* `DurableCompletionConsumerSettlementProjectionDigest`
* `DurableCompletionConsumerSettlementProjectionTranscriptDigest`
* `DurableCompletionConsumerSettlementProjectionOutcome`
* `GovernanceDurableCompletionConsumerSettlementProjectionSink`
* `FixtureDurableCompletionConsumerSettlementProjectionSink`
* `ProductionSettlementProjectionSink`
* `MainNetSettlementProjectionSink`
* `ExternalSettlementProjectionSink`
* `project_consumer_outcome_to_settlement_projection_request`
* `evaluate_durable_completion_consumer_settlement_projection`
* `recover_durable_completion_consumer_settlement_projection_window`
* `settlement_projection_outcome_authorizes_record`
* `settlement_projection_outcome_projects_to_recorded`
* `SettlementProjectionRecorded`
* `SettlementProjectionDuplicateIdempotent`
* `SettlementProjectionRejectedBeforeRecord`
* `SettlementProjectionRecordFailedNoProjection`
* `SettlementProjectionRolledBackNoProjection`
* `SettlementProjectionRollbackFailedFatalNoProjection`
* `SettlementProjectionAmbiguousFailClosedNoProjection`
* `ProductionSettlementProjectionUnavailableNoProjection`
* `MainNetSettlementProjectionUnavailableNoProjection`
* `ExternalSettlementProjectionUnavailableNoProjection`
* `MainNetPeerDrivenApplyRefusedNoProjection`
* `ValidatorSetRotationUnsupportedNoProjection`
* `PolicyChangeUnsupportedNoProjection`
* all Run 264 invariant helper names.

If exact names differ, use the exact implemented Run 264 names and record differences honestly in `QBIND_DEVNET_EVIDENCE_RUN_265.md`.

Validation commands:

Run at minimum:

* `cargo build -p qbind-node --release --bin qbind-node`
* `cargo build -p qbind-node --release --example run_265_durable_completion_consumer_settlement_projection_release_binary_helper`
* `bash scripts/devnet/run_265_durable_completion_consumer_settlement_projection_release_binary.sh`
* `cargo test -p qbind-node --test run_264_durable_completion_consumer_settlement_projection_tests`
* `cargo test -p qbind-node --test run_262_durable_completion_acknowledgement_consumer_tests`
* `cargo test -p qbind-node --test run_260_durable_completion_audit_receipt_acknowledgement_tests`
* `cargo test -p qbind-node --test run_258_durable_completion_audit_publication_receipt_tests`
* `cargo test -p qbind-node --test run_256_durable_completion_attestation_backend_tests`
* `cargo test -p qbind-node --test run_254_modeled_durable_completion_attestation_projection_tests`
* `cargo test -p qbind-node --test run_252_modeled_durable_completion_finalization_projection_tests`
* `cargo test -p qbind-node --test run_250_modeled_durable_consume_completion_reporter_tests`
* `cargo test -p qbind-node --test run_248_modeled_durable_consume_projection_sink_tests`
* `cargo test -p qbind-node --test run_246_governance_modeled_end_to_end_pipeline_tests`
* `cargo test -p qbind-node --test run_244_modeled_governance_trust_mutation_applier_tests`
* `cargo test -p qbind-node --test run_242_governance_execution_mutation_engine_tests`
* `cargo test -p qbind-node --test run_240_governance_evaluator_replay_durable_runtime_integration_tests`
* `cargo test -p qbind-node --test run_238_governance_evaluator_replay_durable_backend_tests`
* `cargo test -p qbind-node --test run_236_governance_evaluator_replay_consume_runtime_integration_tests`
* `cargo test -p qbind-node --test run_234_governance_evaluator_replay_consume_boundary_tests`
* `cargo test -p qbind-node --test run_232_governance_evaluator_replay_runtime_integration_tests`
* `cargo test -p qbind-node --test run_230_governance_evaluator_replay_state_tests`
* `cargo test -p qbind-node --test run_228_peer_evaluator_context_representation_tests`
* `cargo test -p qbind-node --test run_226_governance_evaluator_runtime_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_224_governance_evaluator_runtime_integration_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

Security scanning:

* Run secret scanning if available and report the result.
* Run CodeQL if available and report the result honestly.
* If Rust CodeQL analysis is skipped because the database is too large, or if the checker classifies the change set as trivial and skips analysis, say that explicitly and do not overclaim CodeQL coverage.

Documentation:

Create:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_265.md`
* `docs/devnet/run_265_durable_completion_consumer_settlement_projection_release_binary/README.md`
* `docs/devnet/run_265_durable_completion_consumer_settlement_projection_release_binary/summary.txt`
* `docs/devnet/run_265_durable_completion_consumer_settlement_projection_release_binary/.gitignore`

Update narrowly:

* `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
* `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
* `docs/whitepaper/contradiction.md`

Docs must state:

* Run 265 is release-binary evidence for Run 264.
* No production mutating behavior is enabled.
* The settlement-projection sink boundary is release-evidenced, not production-enabled.
* The release helper exercises Run 264 production library symbols in release mode.
* The release helper remains dead code from production runtime.
* The fixture settlement-projection sink mutates only modeled in-memory `DurableCompletionConsumerSettlementProjectionLedger` state.
* Production settlement-projection, MainNet settlement-projection, and external settlement-projection sinks remain reachable but unavailable/fail-closed.
* Run 262 `AcknowledgementConsumed` is required before any settlement-projection request can exist.
* Only `SettlementProjectionRecorded` authorizes modeled settlement-projection state.
* Every non-recorded consumer outcome produces no settlement-projection request and no settlement-projection record.
* Duplicate identical settlement-projection record is idempotent.
* Same settlement-projection record id with different digest fails closed as equivocation.
* Failed settlement-projection record, rollback, rollback-failed, ambiguous windows, unavailable production/MainNet/external paths, rejected replay states, and unsupported actions never record.
* MainNet peer-driven apply remains refused before pipeline progression, sink invocation, reporter invocation, finalizer invocation, attestor invocation, backend invocation, receipt invocation, acknowledgement invocation, consumer invocation, and settlement-projection invocation.
* Rejected settlement-projection paths are non-mutating.
* No real settlement is implemented.
* No real audit-ledger acknowledgement is implemented.
* No real external-publication confirmation is implemented.
* No real external publication is implemented.
* No real production attestation backend is implemented.
* No real finalization backend is implemented.
* No real completion-report backend is implemented.
* No real durable consume backend is implemented.
* No real persistent replay backend is implemented.
* No real governance execution engine is implemented.
* No real production mutation engine is implemented.
* No real on-chain governance proof verifier is implemented.
* No RocksDB/file/schema/migration/storage-format change is implemented.
* No wire/schema/marker/sequence/trust-bundle change is implemented.
* No KMS/HSM/RemoteSigner backend is implemented.
* No MainNet governance enablement.
* No MainNet peer-driven apply enablement.
* No validator-set rotation.
* The settlement-projection boundary does not call Run 070.
* The settlement-projection boundary does not mutate `LivePqcTrustState`.
* The settlement-projection boundary does not perform a real trust swap.
* The settlement-projection boundary does not evict sessions.
* The settlement-projection boundary does not write sequence files.
* The settlement-projection boundary does not write authority markers.
* The settlement-projection boundary does not perform external publication.
* The settlement-projection boundary does not perform real external-publication confirmation.
* The settlement-projection boundary does not write a real audit ledger.
* The settlement-projection boundary does not perform real settlement.
* Full C4 remains OPEN.
* C5 remains OPEN.

Contradiction handling:

* Crosscheck Run 265 against Runs 050–264.
* If no contradiction is found, add a “no Run 265 contradiction” entry to `docs/whitepaper/contradiction.md`.
* If contradiction is found, record it explicitly with exact file/function/doc reference and recommend the smallest safe fix.

Expected verdict:

Positive release-binary evidence only if:

* release helper builds;
* release harness exits 0;
* S1–S6 pass;
* denylist empty;
* regression corpus passes;
* docs/evidence are updated;
* contradiction entry is present;
* scans are reported honestly.

Run 265 closes only the Run 264 release-binary evidence gap. It must not claim Full C4 or C5 closure.

Final response format:

1. Exact verdict.
2. Files changed.
3. Release artifacts and hashes.
4. Helper corpus results.
5. Real-binary scenarios.
6. Tests run and results.
7. Security invariants preserved.
8. Honest limitations.
9. C4/C5 status.
10. Suggested Run 266 next step.
