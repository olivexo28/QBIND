Run 400 — Public DevNet real operator seed Route-A execution

Objective:
Execute M4 Route A on real operator infrastructure only.

Hard start condition:
Proceed only if all are available:
1. durable public DevNet seed host;
2. real public IP/DNS;
3. inbound-openable P2P port;
4. durable seed identity material;
5. strict KEMTLS / PQC static-root material;
6. release qbind-node runnable on the host;
7. independent off-host vantage;
8. external TCP + KEMTLS/static-root verification capability.

If any item is missing:
Stop immediately.
No loopback.
No harness.
No readiness edit.
No devnet-seeds.live.json.
No M4 Green claim.

Route A tasks:
1. Run release qbind-node on the public seed host with DevNet genesis pinned.
2. Use strict mutual-auth + PQC static-root flags.
3. From the independent vantage, capture:
   - UTC timestamp;
   - external TCP dial result;
   - external KEMTLS/static-root handshake result;
   - observed NodeId / cert-derived identity;
   - match against the public seed identity.
4. Fill RUN_400 reachability evidence.
5. Publish devnet-seeds.live.json only if evidence passes.
6. Run identity register-check --status live --reachability-evidence.
7. Re-evaluate M4 and then M6 only if the matrix criteria permit.

Expected verdict:
PASS only with real external TCP + KEMTLS evidence and schema-valid live seed publication.
Otherwise NEGATIVE-FOR-EXTERNAL with no repo change.