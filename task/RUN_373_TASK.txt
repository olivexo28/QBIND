Run 373 — Cross-process PqcStaticRoot strict-auth M12 hardening evidence

Continue QBIND after accepted Run 372.

Current state:
Run 372 is accepted as PASS / public-DevNet M12 strict-auth multi-peer hardening positive.

Run 371 moved M12 Yellow/Partial → Green for the public DevNet readiness track by proving both deployed live-socket abuse/DoS controls:
1. connection-rate limiting over deployed live sockets;
2. per-peer message-rate limiting over a KEMTLS-admitted deployed live peer socket.

Run 372 preserved that M12 Green result and added hardening evidence:
1. strict `MutualAuthMode::Required` admission;
2. two simultaneous KEMTLS-admitted peers;
3. abusive-peer-only per-peer bucket drops;
4. honest-peer bucket isolation;
5. connection-rate regression preserved;
6. no production source change;
7. no new public CLI flag;
8. no P2P wire-format change;
9. no KEMTLS / trust-bundle / peer-admission weakening;
10. no C4/C5 closure.

Known Run 372 limitation:
`PqcRootMode::PqcStaticRoot` with runtime-generated ML-DSA-44 root and ML-KEM-768 leaf material was proven in-process only. It was not driven cross-process against the standalone `target/release/qbind-node` binary with operator-configured `--p2p-trusted-root`, `--p2p-leaf-cert`, and `--p2p-leaf-cert-key` material.

Current public DevNet readiness:
- M12 is Green for abuse/DoS deployed live-socket controls.
- M4 remains Yellow/launch-blocking.
- M6 remains Yellow/Partial.
- Public DevNet remains NOT launch-ready.
- Full C4 remains OPEN.
- C5 remains OPEN.
- MainNet authority rotation/revocation remains Red.

Primary objective:
Produce release-binary cross-process evidence that the Run 371/372 M12 Green result still holds when the standalone `target/release/qbind-node` is run with strict mutual auth and operator-configured `PqcStaticRoot` material.

This run should extend Run 372 from in-process PqcStaticRoot proof to a standalone-binary, cross-process loopback proof.

Strict scope:
Release-binary hardening evidence only.

No public DevNet launch.
No live external seed/bootnode deployment.
No M4 Green.
No M6 Green.
No faucet.
No RPC gateway.
No explorer.
No status page.
No TestNet readiness claim.
No MainNet readiness claim.
No C4 closure.
No C5 closure.
No authority-lifecycle runtime wiring.
No validator-set mutation.
No epoch transition.
No sequence/marker write.
No LivePqcTrustState mutation.
No P2P wire-format change.
No peer-admission weakening.
No trust-bundle weakening.
No new public CLI flags.
No committed private keys, generated roots, generated leaf certs, generated trust bundles, node data dirs, metrics dumps, or process logs.

Required investigation:
Inspect:
- Run 371 helper/harness/evidence.
- Run 372 helper/harness/evidence.
- `crates/qbind-node/examples/devnet_pqc_root_helper.rs`
- `crates/qbind-node/examples/devnet_pqc_trust_bundle_helper.rs`
- `crates/qbind-node/src/pqc_root_config.rs`
- `crates/qbind-node/src/p2p_tcp.rs`
- `crates/qbind-node/src/p2p_node_builder.rs`
- `crates/qbind-node/src/deployed_inbound_per_peer_limiter.rs`
- `crates/qbind-node/src/metrics.rs`
- `crates/qbind-node/src/metrics_http.rs`
- `crates/qbind-node/src/cli.rs`
- existing KEMTLS/PqcStaticRoot release harnesses from prior runs, especially Runs 037–041, 084–089, and 371–372.
- `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_372.md`
- `docs/release/QBIND_PUBLIC_DEVNET_READINESS_CRITERIA.md`
- `docs/release/public-devnet/p2p/ABUSE_DOS_POSTURE.md`
- `docs/release/public-devnet/p2p/VERIFY.md`

Decision gate:
Record one of:

Route A — Existing public binary/helper surfaces can generate temporary PqcStaticRoot material and run cross-process strict-auth flood with no production source change.
Proceed with helper/harness/docs only.

Route B — A tiny helper-only or evidence-only seam is required.
Keep it outside production source if possible. If production source changes are unavoidable, keep them hidden/default-off, prove no default change, and do not broaden readiness claims.

Route C — Cross-process PqcStaticRoot strict-auth flood cannot be completed with existing surfaces.
Do not downgrade M12 automatically. Record the blocker honestly and suggest the next exact source/test or helper run.

Expected artifacts:
Add release helper:
`crates/qbind-node/examples/run_373_public_devnet_m12_pqc_static_root_cross_process_helper.rs`

Add release harness:
`scripts/devnet/run_373_public_devnet_m12_pqc_static_root_cross_process_release_binary.sh`

Add archive:
`docs/devnet/run_373_public_devnet_m12_pqc_static_root_cross_process_release_binary/README.md`
`docs/devnet/run_373_public_devnet_m12_pqc_static_root_cross_process_release_binary/summary.txt`
`docs/devnet/run_373_public_devnet_m12_pqc_static_root_cross_process_release_binary/.gitignore`

Only those three archive files should be tracked.

Add canonical evidence:
`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_373.md`

Update narrowly:
- `docs/release/QBIND_PUBLIC_DEVNET_READINESS_CRITERIA.md`
- `docs/release/public-devnet/p2p/ABUSE_DOS_POSTURE.md`
- `docs/release/public-devnet/p2p/VERIFY.md`
- `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
- `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
- `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
- `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
- `docs/whitepaper/contradiction.md`

Required evidence:
1. `target/release/qbind-node` builds successfully.
2. Run 373 helper builds successfully.
3. qbind-node SHA-256 captured.
4. helper SHA-256 captured.
5. Build IDs captured where available.
6. rustc/cargo versions captured.
7. temporary ML-DSA-44 root and ML-KEM-768 leaf material generated only in temp dirs or memory.
8. no generated private key, root, leaf key, trust bundle, data dir, metrics dump, or log is committed.
9. standalone node A starts with:
   - `--network-mode p2p`;
   - `--enable-p2p`;
   - `--p2p-mutual-auth required`;
   - `--p2p-pqc-root-mode pqc-static-root` or the exact supported equivalent;
   - `--p2p-trusted-root`;
   - `--p2p-leaf-cert`;
   - `--p2p-leaf-cert-key`;
   - loopback listen address;
   - loopback metrics endpoint.
10. standalone node B or helper-driven peer uses matching operator-configured static-root material.
11. cross-process Required mutual-auth succeeds.
12. listener registers peer under verified cert-derived NodeId.
13. under-budget frames produce no per-peer drops.
14. over-budget frames produce per-peer drops on live `/metrics`.
15. honest peer bucket remains clean if multi-peer is retained.
16. abusive peer bucket records drops.
17. connection-rate regression remains valid or is re-run compactly.
18. connection-rate metric and per-peer metric remain independent.
19. hidden abuse/DoS flags remain hidden.
20. strict-auth flag remains pre-existing public surface.
21. invented flags rejected.
22. invalid configs fail closed.
23. MainNet abuse/DoS config refused.
24. no public DevNet launch claim.
25. no M4/M6 Green claim.
26. no TestNet/MainNet readiness claim.
27. no C4/C5 closure claim.
28. no P2P wire-format change.
29. no peer-admission weakening.
30. no trust-bundle weakening.
31. no LivePqcTrustState mutation.
32. no sequence/marker write.
33. no validator-set mutation.
34. no epoch transition.
35. no Run 070 path.

Required scenarios:
1. `default_preserves_behavior`
2. `temporary_pqc_static_root_material_generated`
3. `cross_process_static_root_required_admission`
4. `cert_derived_node_id_observed`
5. `under_budget_static_root_peer`
6. `over_budget_static_root_peer`
7. `multi_peer_bucket_isolation_if_supported`
8. `connection_rate_regression`
9. `combined_limiter_independence`
10. `invalid_configs_fail_closed`
11. `mainnet_refused`
12. `hidden_cli_surface_checked`
13. `non_mutation_guards`

Required tests:
Always run:
- `cargo build -p qbind-node --release`
- `cargo build -p qbind-node --release --example run_373_public_devnet_m12_pqc_static_root_cross_process_helper`
- Run the Run 373 harness.
- `cargo test -p qbind-node --test run_370_public_devnet_deployed_live_socket_m12_tests`
- `cargo test -p qbind-node --test run_369_public_devnet_deployed_per_peer_limiter_wiring_tests`
- `cargo test -p qbind-node --test run_365_public_devnet_deployed_peer_rate_threading_tests`
- `cargo test -p qbind-node --test run_363_public_devnet_per_peer_message_rate_runtime_tests`
- `cargo test -p qbind-node --test run_362_public_devnet_abuse_dos_runtime_tests`
- `cargo test -p qbind-node --test run_361_public_devnet_abuse_dos_hardening_tests`
- `cargo test -p qbind-node --lib`

Security:
Run secret scanning over all changed files.
Generated PQC/KEMTLS material must remain temporary, dev-only, and gitignored.
No private keys, mnemonics, credentials, tokens, private infrastructure, real production hostnames, or live endpoints may be committed.

CodeQL:
If any Rust example/source changes, invoke CodeQL if available.
If skipped, unavailable, timed out, database-too-large, or not meaningful, record the exact result and do not call it clean.

Readiness rules:
- M12 should remain Green if cross-process PqcStaticRoot strict-auth hardening evidence passes.
- If cross-process PqcStaticRoot evidence cannot be produced, do not automatically downgrade M12; record it as hardening outstanding unless it contradicts Run 371/372.
- Do not alter M4 unless live seed/bootnode reachability lands.
- Do not alter M6 unless stable identity generation lands.
- Do not alter M7–M9/M13–M15 unless genuinely closed.
- Public DevNet remains NOT launch-ready.

Expected verdict:
PASS / public-DevNet M12 cross-process PqcStaticRoot strict-auth hardening positive if standalone release-binary strict-auth flood succeeds with operator-configured static-root material.

Partial-positive if evidence improves but static-root cross-process admission or flood cannot be completed.

Final response format:
1. Exact verdict.
2. Files changed.
3. Release artifacts and hashes.
4. Decision gate route.
5. Static-root material generation.
6. Cross-process strict-auth admission evidence.
7. Cert-derived identity evidence.
8. Per-peer static-root flood evidence.
9. Multi-peer bucket isolation.
10. Connection-rate regression.
11. Combined limiter independence.
12. CLI/config surface.
13. Default compatibility.
14. Rejection/fail-closed evidence.
15. Non-mutation evidence.
16. Readiness matrix delta.
17. Current public DevNet readiness status.
18. Remaining public DevNet blockers.
19. Public TestNet blockers.
20. MainNet blockers.
21. C4/C5 status.
22. Tests run.
23. Security scans.
24. CodeQL.
25. Provenance.
26. Honest limitations.
27. Suggested Run 374 next step.