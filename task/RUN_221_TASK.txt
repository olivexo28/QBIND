Task: Run 221 — Release-Binary Long-Running Governance Execution Runtime Consumption Evidence

Current accepted state:

* Run 211: source/test governance execution policy boundary landed.
* Run 212: release-binary governance execution policy-boundary evidence fully closed.
* Run 213: source/test governance execution payload/carrying and production-context wiring landed.
* Run 214: release-binary governance execution payload/carrying evidence fully closed.
* Run 215: source/test hidden governance execution policy selector landed.
* Run 216: release-binary governance execution policy selector evidence fully closed.
* Run 217: source/test governance execution runtime arming carrier landed.
* Run 218: release-binary governance execution runtime-arming evidence fully closed.
* Run 219: governance execution runtime surface gap audit landed.
* Run 220: source/test long-running governance execution runtime consumption wiring landed:

  * `pqc_governance_execution_runtime_arming.rs` now has the consumption layer:

    * `GovernanceExecutionRuntimeConsumption::{ProceedLegacyBypass, ProceedAccepted, FailClosed}`;
    * `consume_surface`;
    * `consume_surface_from_optional_sidecar_value`;
    * `governance_execution_load_status_from_optional_sidecar_value`.
  * `main.rs` now populates governance-execution load status from the real ratification sidecar.
  * reload-apply, startup `--p2p-trust-bundle`, reload-check, and local peer-candidate-check now consume the verdict and fail closed before mutation.
  * `pqc_live_trust_reload.rs` / SIGHUP computes real sidecar status and fails closed before mutation.
  * default Disabled plus absent carrier preserves pre-Run-217 behavior through `ProceedLegacyBypass`.
  * `run_220_governance_execution_runtime_consumption_tests` passed.
  * full lib passed.
  * release-binary runtime-consumption evidence deferred to Run 221.
* Run 220 preserves:

  * default `GovernanceExecutionPolicy::Disabled`;
  * fixture governance DevNet/TestNet source/test only;
  * emergency council fixture explicit and non-production;
  * production/on-chain/MainNet governance execution unavailable/fail-closed;
  * MainNet peer-driven apply refused;
  * no real governance execution engine;
  * no real on-chain governance proof verifier;
  * no real KMS/HSM backend;
  * no real RemoteSigner backend;
  * validator-set rotation unsupported;
  * Full C4 remains OPEN;
  * C5 remains OPEN.

Objective:
Produce release-binary evidence that real `target/release/qbind-node` long-running runtime paths consume the selected governance-execution policy and real governance-execution sidecar load status where representable.

This run must prove on release binaries:

1. default Disabled + absent governance-execution carrier preserves legacy behavior through `ProceedLegacyBypass`;
2. CLI/env selected policy reaches runtime consumption;
3. governance-execution sidecar material is loaded from the real sidecar where representable;
4. `consume_surface` verdict is consumed, not discarded;
5. invalid selector values fail closed before mutation;
6. missing/malformed governance-execution material fails closed when policy requires it;
7. production/on-chain/MainNet governance execution remains fail-closed as unavailable;
8. accepted fixture governance execution remains DevNet/TestNet evidence-only;
9. rejected runtime-consumption cases produce no mutation;
10. MainNet peer-driven apply remains refused;
11. no real governance execution engine, on-chain proof verifier, KMS-HSM backend, RemoteSigner backend, or validator-set rotation is claimed.

Strict scope:

* Release-binary evidence only.
* Use real `target/release/qbind-node`.
* Use release-built helper(s) to mint governance-execution sidecars / fixture material where needed.
* No production source change unless a tiny harness-only fix is required.
* No real governance execution engine.
* No real on-chain governance proof verifier.
* No MainNet governance enablement.
* No MainNet peer-driven apply enablement.
* No validator-set rotation.
* No authority-set rotation beyond existing lifecycle boundary checks.
* No real KMS implementation.
* No real HSM implementation.
* No real RemoteSigner backend.
* No production signing key custody.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* No schema/wire change.
* No authority-marker schema change.
* No sequence-file schema change.
* No trust-bundle core schema change.
* No authority lifecycle semantics change.
* Do not weaken Runs 070, 130–220.
* Do not claim full C4 or C5 closure.

Required deliverables:

1. Add release-binary helper:
   `crates/qbind-node/examples/run_221_governance_execution_runtime_consumption_release_binary_helper.rs`

2. Add release-binary harness:
   `scripts/devnet/run_221_governance_execution_runtime_consumption_release_binary.sh`

3. Add evidence archive:
   `docs/devnet/run_221_governance_execution_runtime_consumption_release_binary/`

4. Add canonical evidence report:
   `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_221.md`

5. Update narrowly:

   * `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
   * `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
   * `docs/whitepaper/contradiction.md`
   * `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
   * `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
   * `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

6. Crosscheck all new/modified code and docs against existing design/spec. If contradictions or inconsistencies are found, record them in `contradiction.md` before finalizing.

Required release-built helper matrix:
The helper must exercise Run 220 runtime consumption in release mode through production library symbols.

Accepted / compatible cases:
A1. default CLI/env absent resolves through runtime consumption to `ProceedLegacyBypass`.

A2. default Disabled + absent governance-execution carrier preserves legacy no-governance-execution behavior.

A3. CLI `disabled` resolves through runtime consumption to Disabled and absent carrier bypass.

A4. env `disabled` resolves through runtime consumption to Disabled and absent carrier bypass.

A5. CLI `fixture-governance-allowed` plus valid DevNet governance-execution sidecar reaches `ProceedAccepted`.

A6. env `fixture-governance-allowed` plus valid TestNet governance-execution sidecar reaches `ProceedAccepted`.

A7. CLI `emergency-council-fixture-allowed` plus valid DevNet emergency sidecar reaches `ProceedAccepted` only for explicit emergency action.

A8. CLI `production-governance-required` plus production governance material reaches `FailClosed` with production unavailable.

A9. env `mainnet-governance-required` reaches `FailClosed` with MainNet governance unavailable / MainNet refusal.

A10. CLI-over-env precedence is deterministic through runtime consumption:

* env sets `fixture-governance-allowed`;
* CLI sets `disabled`;
* resolved runtime policy is Disabled.

A11. reload-check runtime path consumes selected policy and real sidecar status.

A12. reload-apply runtime path consumes selected policy and real sidecar status.

A13. startup `--p2p-trust-bundle` runtime path consumes selected policy and real sidecar status where representable.

A14. SIGHUP runtime path consumes selected policy and real sidecar status where representable.

A15. local peer-candidate-check runtime path consumes selected policy and real sidecar status where representable.

A16. live inbound `0x05` limitation is evidenced honestly if not representable; invalid candidate remains non-propagated / non-staged / non-applied where tested.

A17. peer-driven drain limitation is evidenced honestly if helper-only or not representable; MainNet peer-driven apply remains refused.

A18. lifecycle rotate action is authorized only when runtime-consumed policy and carried decision authorize rotate with matching candidate digest and sequence.

A19. lifecycle revoke action is authorized only when runtime-consumed policy and carried decision authorize revoke with matching candidate/revoked-key material and sequence.

A20. `arm_surface` outcome is consumed on surfaces that claim consumption.

A21. real sidecar status is not forced to `Absent` when sidecar material is present on representable surfaces.

A22. Run 210 custody-attestation-policy selector behavior remains compatible.

A23. Run 199 RemoteSigner-policy selector behavior remains compatible.

A24. Run 193 custody-policy selector behavior remains compatible.

Required rejection cases:
R1. invalid CLI selector value rejected before mutation.

R2. invalid env selector value rejected before mutation.

R3. unrelated CLI/env does not enable governance execution.

R4. missing governance-execution material rejected under FixtureGovernanceAllowed.

R5. missing governance-execution material rejected under ProductionGovernanceRequired.

R6. malformed governance-execution material rejected.

R7. fixture governance rejected under ProductionGovernanceRequired.

R8. emergency fixture rejected under ProductionGovernanceRequired.

R9. fixture governance rejected under MainnetGovernanceRequired.

R10. production governance rejected as unavailable.

R11. on-chain governance rejected as unavailable.

R12. MainNet governance rejected as unavailable.

R13. wrong lifecycle action rejected.

R14. wrong candidate digest rejected.

R15. wrong authority-domain sequence rejected.

R16. wrong governance proof digest rejected.

R17. expired decision rejected.

R18. stale/replayed decision rejected.

R19. quorum threshold insufficient rejected.

R20. emergency action not authorized rejected.

R21. validator-set rotation unsupported rejected.

R22. policy-change action unsupported rejected.

R23. local operator cannot satisfy governance execution.

R24. peer majority cannot satisfy governance execution.

R25. validation-only rejection writes no marker and no sequence.

R26. mutating rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

R27. invalid live inbound `0x05` governance-execution candidate is not propagated, staged, or applied where live surface is representable.

R28. MainNet peer-driven apply remains refused even with MainnetGovernanceRequired and fixture governance approval.

Required real binary checks:
Use real `target/release/qbind-node` to prove:

* hidden CLI flag is present but hidden from normal help, if feasible to check;
* env selector reaches runtime consumption;
* CLI selector reaches runtime consumption;
* CLI-over-env precedence reaches runtime consumption;
* invalid selector value fails closed before mutation;
* reload-check consumes real sidecar status where representable;
* reload-apply consumes real sidecar status where representable;
* startup `--p2p-trust-bundle` consumes real sidecar status where representable;
* SIGHUP consumes real sidecar status where representable, if feasible in harness;
* local peer-candidate-check consumes real sidecar status where representable;
* MainNet peer-driven apply remains refused;
* default behavior does not expose or enable governance execution;
* no `governance execution active` / `production governance active` / `MainNet governance enabled` claim is emitted;
* no real on-chain governance proof verifier active claim is emitted;
* existing custody-attestation policy selector behavior from Run 210 remains compatible;
* existing KMS/HSM backend behavior from Run 204 remains compatible;
* existing RemoteSigner transport behavior from Run 202 remains compatible;
* existing custody policy selector behavior from Run 193 remains compatible;
* existing governance proof carrier paths remain compatible;
* no validator-set rotation claim is emitted.

Required source/release reachability proof:
The report must include source grep/call-site proof for:

* `pqc_governance_execution_runtime_arming`;
* `GovernanceExecutionRuntimeConsumption`;
* `ProceedLegacyBypass`;
* `ProceedAccepted`;
* `FailClosed`;
* `consume_surface`;
* `consume_surface_from_optional_sidecar_value`;
* `governance_execution_load_status_from_optional_sidecar_value`;
* call sites in `main.rs` consuming the verdict;
* SIGHUP hook consuming the verdict;
* real sidecar load derivation;
* absence of discarded `let _outcome = arming.arm_surface(...)` in production call sites that claim consumption;
* no forced `GovernanceExecutionLoadStatus::Absent` where a real sidecar is present.

Required mutation / no-mutation proof:
For rejected runtime-consumption governance-execution scenarios:

* no Run 070 apply call;
* no live trust swap;
* no session eviction;
* no sequence write;
* no marker write;
* marker bytes unchanged where present;
* sequence bytes unchanged where present;
* no `.tmp` residue;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead.

For accepted fixture governance mutating compatibility scenarios:

* selector resolution occurs before governance-execution payload parse;
* runtime consumption occurs before marker decision;
* governance-execution payload parse occurs before marker decision;
* governance execution evaluation occurs before apply/mutation;
* lifecycle validation occurs before apply/mutation;
* governance proof validation occurs before apply/mutation where applicable;
* custody validation occurs before apply/mutation where applicable;
* custody attestation validation occurs before apply/mutation where applicable;
* Run 055 sequence commit succeeds if a mutating path is exercised;
* v2 marker persists strictly after sequence commit.

Required denylist:
Across the run prove:

* no MainNet apply;
* no autonomous apply;
* no apply on receipt;
* no peer-majority authority;
* no real governance execution claim;
* no production governance active claim;
* no MainNet governance enabled claim;
* no real on-chain governance proof verifier claim;
* no real KMS backend claim;
* no real HSM backend claim;
* no real RemoteSigner backend claim;
* no custody attestation production-active claim;
* no validator-set rotation claim;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead;
* no schema/wire/metric drift;
* no marker write before sequence commit;
* no sequence write on validation-only surfaces;
* no marker write on validation-only surfaces.

Required captured metadata:

* qbind-node SHA-256 and ELF Build ID;
* helper SHA-256 and ELF Build ID;
* git commit hash;
* rustc/cargo versions;
* exact commands;
* stdout/stderr logs;
* per-scenario exit codes;
* governance execution runtime-consumption policy values;
* governance-execution fixture input/decision/payload paths and hashes;
* marker/sequence JSON and SHA before/after where applicable;
* data-dir inventories;
* denylist grep results.

Validation commands:
Run at minimum:

* `cargo build --release -p qbind-node --bin qbind-node`
* `cargo build --release -p qbind-node --example run_221_governance_execution_runtime_consumption_release_binary_helper`
* `bash scripts/devnet/run_221_governance_execution_runtime_consumption_release_binary.sh`
* `cargo test -p qbind-node --test run_220_governance_execution_runtime_consumption_tests`
* `cargo test -p qbind-node --test run_217_governance_execution_runtime_arming_tests`
* `cargo test -p qbind-node --test run_215_governance_execution_policy_selector_tests`
* `cargo test -p qbind-node --test run_213_governance_execution_payload_callsite_tests`
* `cargo test -p qbind-node --test run_211_governance_execution_policy_tests`
* `cargo test -p qbind-node --test run_209_custody_attestation_policy_selector_tests`
* `cargo test -p qbind-node --test run_207_custody_attestation_payload_callsite_tests`
* `cargo test -p qbind-node --test run_205_custody_attestation_verifier_tests`
* `cargo test -p qbind-node --test run_203_kms_hsm_backend_boundary_tests`
* `cargo test -p qbind-node --test run_201_remote_signer_transport_boundary_tests`
* `cargo test -p qbind-node --test run_198_remote_signer_policy_selector_tests`
* `cargo test -p qbind-node --test run_196_remote_signer_payload_callsite_tests`
* `cargo test -p qbind-node --test run_194_remote_authority_signer_boundary_tests`
* `cargo test -p qbind-node --test run_192_authority_custody_policy_selector_tests`
* `cargo test -p qbind-node --test run_190_authority_custody_payload_callsite_tests`
* `cargo test -p qbind-node --test run_188_authority_custody_boundary_tests`
* `cargo test -p qbind-node --test run_186_onchain_governance_production_verifier_boundary_tests`
* `cargo test -p qbind-node --test run_184_onchain_governance_payload_carrying_tests`
* `cargo test -p qbind-node --test run_182_onchain_governance_production_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_180_onchain_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_178_onchain_governance_proof_tests`
* `cargo test -p qbind-node --test run_176_live_0x05_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_173_validation_only_governance_required_policy_tests`
* `cargo test -p qbind-node --test run_171_governance_required_policy_selector_tests`
* `cargo test -p qbind-node --test run_169_governance_proof_loader_surface_integration_tests`
* `cargo test -p qbind-node --test run_167_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_165_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_163_governance_authority_verifier_tests`
* `cargo test -p qbind-node --test run_161_lifecycle_marker_integration_tests`
* `cargo test -p qbind-node --test run_159_authority_signing_key_lifecycle_tests`
* `cargo test -p qbind-node --test run_157_unified_testnet_fixture_universe_tests`
* `cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests`
* `cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests`
* `cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests`
* `cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests`
* `cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests`
* `cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Documentation requirements:
`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_221.md` must state:

* Run 221 is release-binary long-running governance-execution runtime-consumption evidence.
* Real binary paths consume selected governance-execution policy where representable.
* Real binary paths consume real governance-execution sidecar status where representable.
* Default remains `GovernanceExecutionPolicy::Disabled`.
* Absent carrier under Disabled follows `ProceedLegacyBypass`.
* Fixture governance execution remains DevNet/TestNet evidence-only.
* Emergency council fixture execution is explicit and non-production.
* Production/on-chain/MainNet governance execution remains unavailable/fail-closed.
* Live inbound `0x05` and peer-driven drain limitations must be described honestly if still not fully representable.
* MainNet peer-driven apply remains refused.
* No real governance execution engine is implemented.
* No real on-chain governance proof verifier is implemented.
* No real KMS/HSM backend is implemented.
* No real RemoteSigner backend is implemented.
* Validator-set rotation remains unsupported.
* Existing custody/KMS-HSM/RemoteSigner/custody-attestation/governance proof paths remain compatible.
* Full C4 remains OPEN.
* C5 remains OPEN.

Acceptance criteria:
Run 221 is acceptable only if:

1. Real release binary consumes selected governance-execution policy where representable.
2. Real release binary consumes real governance-execution sidecar status where representable.
3. Default Disabled + absent carrier produces legacy-compatible bypass.
4. CLI/env/precedence behavior reaches runtime consumption.
5. Invalid selector values fail closed before mutation.
6. Fixture governance execution passes only where selected runtime policy allows.
7. Emergency council fixture execution remains explicit and non-production.
8. Production/on-chain/MainNet governance execution remains fail-closed as unavailable.
9. `arm_surface` outcome is not discarded on surfaces that claim consumption.
10. Forced `Absent` does not override real sidecar material on representable surfaces.
11. Rejected runtime-consumption cases produce no mutation.
12. MainNet peer-driven apply remains refused.
13. Existing custody/KMS-HSM/RemoteSigner/custody-attestation/governance proof paths remain compatible.
14. No real governance execution / on-chain proof verifier / KMS-HSM backend / RemoteSigner backend / validator-set rotation claim is made.
15. No full C4 or C5 closure is claimed.
