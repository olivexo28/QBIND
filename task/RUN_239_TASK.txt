Task: Run 239 — Release-Binary Durable Governance Evaluator Replay State Backend Boundary Evidence

Current accepted state:

* Run 230: source/test governance evaluator replay/freshness state boundary landed.
* Run 231: release-binary replay/freshness state evidence fully closed.
* Run 232: source/test governance evaluator replay/freshness runtime integration landed.
* Run 233: release-binary replay/freshness runtime integration evidence fully closed.
* Run 234: source/test post-mutation replay consume boundary landed.
* Run 235: release-binary post-mutation replay consume boundary evidence fully closed.
* Run 236: source/test replay consume runtime integration landed.
* Run 237: release-binary replay consume runtime integration evidence fully closed.
* Run 238: source/test durable replay state backend boundary landed:

  * `crates/qbind-node/src/pqc_governance_evaluator_replay_durable_backend.rs`;
  * `DurableBackendDecisionInput`;
  * `DurableBackendDecisionExpectations`;
  * `DurableRecordState`;
  * `DurableBackendOutcome`;
  * `DurableConsumeOutcome`;
  * `CrashWindow`;
  * `DurableBackendKind`;
  * `DurableMutationCompletion`;
  * durable backend reader/writer/atomic traits;
  * pure operations:

    * `read_decision_state`;
    * `observe_decision_if_absent`;
    * `mark_consumed_after_success`;
    * `compare_and_mark_consumed`;
    * fixture restart durability via snapshot value-clone;
    * `classify_crash_window`;
  * DevNet/TestNet in-memory fixture only;
  * production/MainNet durable backends callable but unavailable/fail-closed;
  * no RocksDB/file/schema/migration/storage-format change;
  * no wire/schema/marker/sequence/trust-bundle change;
  * MainNet peer-driven apply remains refused;
  * release-binary evidence deferred to Run 239.
* Full C4 remains OPEN.
* C5 remains OPEN.

Objective:
Produce release-binary evidence for the Run 238 durable governance evaluator replay-state backend boundary.

This run must prove in release mode that the durable backend boundary contract is exercised through release-built code, including observed/fresh/deferred/expired/stale/consumed/replay/superseded classifications, compare-and-mark-consumed atomicity, fixture restart snapshot durability, crash-window classification, production/MainNet unavailable fail-closed behavior, and no mutation side effects.

Strict scope:

* Release-binary evidence only.
* Use release-built helper and real `target/release/qbind-node` where applicable.
* No production source behavior change unless a tiny harness-only fix is required.
* No real persistent replay backend.
* No RocksDB schema.
* No file format.
* No database migration.
* No real governance execution engine.
* No real mutation engine.
* No real on-chain governance proof verifier.
* No MainNet governance enablement.
* No MainNet peer-driven apply enablement.
* No validator-set rotation.
* No KMS/HSM backend implementation.
* No RemoteSigner backend implementation.
* No wire/schema/marker/sequence/trust-bundle change.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* Do not weaken Runs 070, 130–238.
* Do not claim full C4 or C5 closure.

Required deliverables:

1. Add release-binary helper:
   `crates/qbind-node/examples/run_239_governance_evaluator_replay_durable_backend_release_binary_helper.rs`

2. Add release-binary harness:
   `scripts/devnet/run_239_governance_evaluator_replay_durable_backend_release_binary.sh`

3. Add evidence archive:
   `docs/devnet/run_239_governance_evaluator_replay_durable_backend_release_binary/`

4. Add canonical evidence report:
   `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_239.md`

5. Update narrowly:

   * `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
   * `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
   * `docs/whitepaper/contradiction.md`
   * `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
   * `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
   * `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

6. Crosscheck all new/modified code and docs against existing design/spec. If contradictions are found, record them in `contradiction.md`.

Required release-helper matrix:
The release-built helper must exercise Run 238 durable backend symbols through production library code.

Accepted / compatible cases:
A1. first-seen DevNet fixture decision records `ObservedFresh`.

A2. first-seen TestNet fixture decision records `ObservedFresh`.

A3. known fresh decision reads as `ProceedKnownFresh`.

A4. not-yet-effective decision records/reads as deferred and is not mutation approval.

A5. expired decision records/reads as fail-closed expired.

A6. stale decision records/reads as fail-closed stale.

A7. explicit consume after successful mutation marks consumed.

A8. same decision after consume reads as consumed / fail-closed.

A9. read-only validation does not mark consumed.

A10. observe-only state survives fixture restart snapshot.

A11. consumed state survives fixture restart snapshot.

A12. rollback after observe does not mark consumed.

A13. apply-failed after observe does not mark consumed.

A14. after-mutation-before-consume crash window is typed and not silently approved.

A15. after-consume crash window reads consumed / fail-closed for repeat decision.

A16. durable backend key digest is deterministic in release mode.

A17. durable record digest is deterministic in release mode.

A18. durable operation transcript digest is deterministic in release mode.

A19. crash-window transcript digest is deterministic in release mode.

A20. production durable backend is callable and fails closed unavailable.

A21. MainNet durable backend is callable and fails closed unavailable/refused.

A22. Run 236 consume runtime integration remains compatible when durable backend is not wired.

A23. Run 237 release consume-runtime behavior remains compatible.

A24. Run 235 release consume-boundary behavior remains compatible.

A25. Run 233 release replay/freshness runtime behavior remains compatible.

Required rejection cases:
R1. wrong replay state key digest rejected.

R2. wrong source identity digest rejected.

R3. wrong request digest rejected.

R4. wrong response digest rejected.

R5. wrong transcript digest rejected.

R6. wrong decision digest rejected.

R7. wrong proposal id rejected.

R8. wrong decision id rejected.

R9. wrong lifecycle action rejected.

R10. wrong candidate digest rejected.

R11. wrong authority-domain sequence rejected.

R12. wrong effective epoch rejected.

R13. wrong expiry epoch rejected.

R14. wrong replay nonce rejected.

R15. wrong environment rejected.

R16. wrong chain rejected.

R17. wrong genesis rejected.

R18. wrong validation surface rejected.

R19. wrong mutation surface rejected.

R20. malformed backend record rejected.

R21. replay detected rejected.

R22. consumed decision rejected.

R23. superseded decision rejected.

R24. backend unavailable rejected.

R25. production backend unavailable rejected.

R26. MainNet backend unavailable/refused rejected.

R27. compare-and-mark-consumed with wrong expected state rejected.

R28. consume attempted before observe rejected.

R29. consume attempted before successful mutation rejected.

R30. consume attempted after failed apply rejected.

R31. consume attempted after rollback rejected.

R32. local operator cannot satisfy durable replay backend policy.

R33. peer majority cannot satisfy durable replay backend policy.

R34. validator-set rotation unsupported rejected.

R35. policy-change action unsupported rejected.

R36. rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

R37. MainNet peer-driven apply remains refused even when durable backend fixture says fresh.

Required real release checks:
Use release artifacts to prove:

* helper binary SHA-256 and ELF Build ID captured;
* `qbind-node` release binary SHA-256 and ELF Build ID captured if built;
* Run 238 helper corpus passes;
* observed/fresh/deferred/expired/stale/consumed/replay/superseded outcomes are distinguished;
* compare-and-mark-consumed atomicity is release-evidenced;
* fixture restart snapshot preserves observed state;
* fixture restart snapshot preserves consumed state;
* crash-window classifications are release-evidenced;
* production/MainNet durable backend unavailable is fail-closed;
* no real persistent backend active claim;
* no RocksDB schema drift;
* no file format drift;
* no database migration;
* no marker/sequence write from backend boundary;
* no Run 070 call from backend boundary;
* MainNet peer-driven apply remains refused;
* no real governance engine active claim;
* no real mutation engine active claim;
* no real on-chain proof verifier active claim;
* no MainNet governance enabled claim;
* no validator-set rotation claim;
* no KMS/HSM backend active claim;
* no RemoteSigner backend active claim;
* no peer-majority authority claim;
* no automatic apply / apply-on-receipt claim.

Required source/release reachability proof:
The report must include grep/call-site proof for:

* `pqc_governance_evaluator_replay_durable_backend`;
* `DurableBackendDecisionInput`;
* `DurableBackendDecisionExpectations`;
* `DurableRecordState`;
* `DurableBackendOutcome`;
* `DurableConsumeOutcome`;
* `CrashWindow`;
* `DurableBackendKind`;
* `DurableMutationCompletion`;
* durable backend reader/writer/atomic traits;
* `read_decision_state`;
* `observe_decision_if_absent`;
* `mark_consumed_after_success`;
* `compare_and_mark_consumed`;
* fixture restart snapshot;
* `classify_crash_window`;
* durable backend key digest helper;
* durable record digest helper;
* durable operation transcript digest helper;
* crash-window transcript digest helper;
* production unavailable path;
* MainNet unavailable/refused path;
* MainNet peer-driven refusal guard.

Required mutation / no-mutation proof:
For every rejected durable-backend case:

* no Run 070 apply call;
* no live trust swap;
* no session eviction;
* no sequence write;
* no marker write;
* no `.tmp` residue;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead;
* read-only validation does not consume;
* rollback does not consume;
* failed apply does not consume;
* MainNet refused does not consume.

Required denylist:
Across the run prove:

* no MainNet apply;
* no autonomous apply;
* no apply on receipt;
* no peer-majority authority;
* no real governance execution engine claim;
* no real mutation engine claim;
* no production governance active claim;
* no MainNet governance enabled claim;
* no real on-chain governance proof verifier claim;
* no real persistent replay backend claim;
* no real KMS backend claim;
* no real HSM backend claim;
* no real RemoteSigner backend claim;
* no custody attestation production-active claim;
* no validator-set rotation claim;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead;
* no RocksDB schema change;
* no file format change;
* no database migration;
* no network wire schema change;
* no trust-bundle schema change;
* no marker schema change;
* no sequence schema change;
* no marker write before sequence commit;
* no sequence write on validation-only surfaces;
* no marker write on validation-only surfaces.

Required captured metadata:

* helper SHA-256 and ELF Build ID;
* qbind-node SHA-256 and ELF Build ID if built;
* git commit hash;
* rustc/cargo versions;
* exact commands;
* stdout/stderr logs;
* per-scenario exit codes;
* durable backend key digests;
* durable record digests;
* durable operation transcript digests;
* crash-window transcript digests;
* durable record state values;
* durable backend outcome values;
* durable consume outcome values;
* crash-window values;
* before/after fixture durable backend snapshots;
* marker/sequence before/after hashes where applicable;
* data-dir inventories;
* denylist grep results.

Validation commands:
Run at minimum:

* `cargo build --release -p qbind-node --bin qbind-node`
* `cargo build --release -p qbind-node --example run_239_governance_evaluator_replay_durable_backend_release_binary_helper`
* `bash scripts/devnet/run_239_governance_evaluator_replay_durable_backend_release_binary.sh`
* `cargo test -p qbind-node --test run_238_governance_evaluator_replay_durable_backend_tests`
* `cargo test -p qbind-node --test run_236_governance_evaluator_replay_consume_runtime_integration_tests`
* `cargo test -p qbind-node --test run_234_governance_evaluator_replay_consume_boundary_tests`
* `cargo test -p qbind-node --test run_232_governance_evaluator_replay_runtime_integration_tests`
* `cargo test -p qbind-node --test run_230_governance_evaluator_replay_state_tests`
* `cargo test -p qbind-node --test run_228_peer_evaluator_context_representation_tests`
* `cargo test -p qbind-node --test run_226_governance_evaluator_runtime_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_224_governance_evaluator_runtime_integration_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Documentation requirements:
`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_239.md` must state:

* Run 239 is release-binary durable replay state backend boundary evidence.
* The durable backend boundary is release-evidenced as a typed contract only.
* Fixture durable backend remains DevNet/TestNet evidence-only.
* Fixture restart snapshot models durability only for source/test evidence.
* Production/MainNet durable backend remains unavailable/fail-closed.
* No real persistent replay backend is implemented.
* No RocksDB/file/schema/migration/storage-format change is implemented.
* No real governance engine, mutation engine, or on-chain proof verifier is implemented.
* MainNet peer-driven apply remains refused.
* Validator-set rotation remains unsupported.
* Full C4 remains OPEN.
* C5 remains OPEN.

Acceptance criteria:
Run 239 is acceptable only if:

1. Release-built helper exercises the Run 238 durable backend boundary.
2. Fixture backend observed/consumed/replayed/superseded states are release-evidenced.
3. Fixture restart snapshot durability is release-evidenced without real file format.
4. Compare-and-mark-consumed atomicity is release-evidenced.
5. Production/MainNet durable backend remains unavailable/fail-closed.
6. Rejections are non-mutating.
7. MainNet peer-driven apply remains refused.
8. No RocksDB/file/schema/migration/storage-format change is claimed.
9. Existing Run 237, Run 235, Run 233, Run 231, and Run 229 release behavior remains compatible.
10. No full C4 or C5 closure is claimed.
