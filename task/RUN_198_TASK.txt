Task: Run 198 — Source/Test RemoteSigner Policy Selector and Production Preflight Integration

Current accepted state:

* Run 194: source/test RemoteSigner production-custody interface boundary landed:

  * `pqc_remote_authority_signer.rs`;
  * `RemoteSignerPolicy::{Disabled, FixtureLoopbackAllowed, ProductionRemoteSignerRequired, MainnetProductionRemoteSignerRequired}`;
  * `RemoteSignerIdentity`;
  * `RemoteSignerRequest`;
  * `RemoteSignerResponse`;
  * deterministic domain-separated SHA3-256 canonical digest;
  * `RemoteAuthoritySigner` trait;
  * DevNet/TestNet-only `FixtureLoopbackRemoteSigner`;
  * fail-closed `ProductionRemoteSigner`;
  * `validate_remote_signer`;
  * `validate_remote_signer_for_custody_class`;
  * `validate_lifecycle_governance_custody_and_remote_signer`.
* Run 195: release-binary RemoteSigner boundary evidence fully closed.
* Run 196: source/test RemoteSigner payload/carrying and production-context wiring landed:

  * `pqc_remote_signer_payload_carrying.rs`;
  * `RemoteSignerIdentityWire`;
  * `RemoteSignerRequestWire`;
  * `RemoteSignerResponseWire`;
  * `RemoteSignerAttestationWire`;
  * `RemoteSignerLoadStatus::{Absent, Available, Malformed}`;
  * optional `remote_signer_attestation` JSON sibling extraction before strict v2 parse;
  * combined v2-sidecar loader;
  * seven per-surface production-context routing helpers;
  * custody-class routing;
  * `validate_loaded_remote_signer`.
* Run 197: release-binary RemoteSigner payload/carrying evidence fully closed:

  * real `target/release/qbind-node` exposes no production RemoteSigner;
  * no RemoteSigner/KMS/HSM enablement banner;
  * helper proves Run 196 A1–A10 / R1–R34 corpus;
  * fixture loopback RemoteSigner remains DevNet/TestNet evidence-only;
  * production RemoteSigner remains unavailable/fail-closed;
  * RemoteSigner request/response binding remains deterministic and domain-bound;
  * MainNet peer-driven apply remains refused.
* Current limitation:
  RemoteSigner payloads can be carried and validated through production-context helpers, but no hidden production preflight selector exists to choose `RemoteSignerPolicy::{FixtureLoopbackAllowed, ProductionRemoteSignerRequired, MainnetProductionRemoteSignerRequired}` at binary/source call sites.
* No real RemoteSigner backend exists.
* No real KMS/HSM/cloud-KMS/PKCS#11 exists.
* governance execution remains unimplemented.
* real on-chain proof verification remains unimplemented.
* validator-set rotation remains open.
* full C4 remains open.
* C5 remains open.

Objective:
Add a source/test-only hidden RemoteSigner policy selector and wire it into production v2 marker-decision preflight contexts.

This run must allow DevNet/TestNet evidence surfaces to explicitly require fixture loopback RemoteSigner material, while preserving `RemoteSignerPolicy::Disabled` by default and preserving fail-closed production/MainNet RemoteSigner behavior.

Release-binary RemoteSigner policy selector evidence is deferred to Run 199.

Strict scope:

* Source/test only.
* No release-binary harness in this run.
* Hidden selector only.
* Disabled by default.
* No real RemoteSigner backend.
* No networked signer service.
* No real KMS implementation.
* No real HSM implementation.
* No cloud KMS integration.
* No PKCS#11 integration.
* No MainNet peer-driven apply enablement.
* No governance execution engine.
* No real on-chain proof verifier.
* No validator-set rotation.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* No schema/wire changes.
* No authority-marker schema change.
* No sequence-file schema change.
* No trust-bundle core schema change.
* Do not weaken Runs 070, 130–197.
* Do not claim full C4 or C5 closure.

Required selector design:
Add a hidden RemoteSigner policy selector. Prefer one hidden CLI flag plus one env var.

CLI:

* `--p2p-trust-bundle-remote-signer-policy <disabled|fixture-loopback-allowed|production-remote-signer-required|mainnet-production-remote-signer-required>`
* hidden via clap.

Env:

* `QBIND_P2P_TRUST_BUNDLE_REMOTE_SIGNER_POLICY=disabled|fixture-loopback-allowed|production-remote-signer-required|mainnet-production-remote-signer-required`

Required default:

* unset CLI/env => `RemoteSignerPolicy::Disabled`.

Required policy constraints:

* `FixtureLoopbackAllowed`:

  * DevNet/TestNet evidence only.
  * must not satisfy MainNet production RemoteSigner.
* `ProductionRemoteSignerRequired`:

  * must fail closed because no real backend exists.
* `MainnetProductionRemoteSignerRequired`:

  * must fail closed.
  * MainNet peer-driven apply remains refused regardless.
* invalid selector values:

  * fail closed or reject startup/preflight with typed parse error.
* unrelated flags/env vars must not enable RemoteSigner policy.
* if both CLI and env exist, CLI must take precedence unless an existing QBIND selector convention dictates otherwise; document the deterministic precedence.

Required source behavior:

1. Add selector helpers, preferably in or near:

   * `pqc_remote_signer_payload_carrying.rs`;
   * or a new `pqc_remote_signer_policy_surface.rs`.

2. Helpers must include grep-verifiable names similar to:

   * `remote_signer_policy_from_selector`;
   * `remote_signer_policy_from_cli_or_env`;
   * `remote_signer_policy_env_selector`.

3. Wire selected RemoteSigner policy into all seven production v2 marker-decision preflight contexts:

   * reload-check;
   * reload-apply;
   * startup `--p2p-trust-bundle`;
   * SIGHUP;
   * local peer-candidate-check;
   * live inbound `0x05`;
   * peer-driven drain.

4. The selected policy must reach the Run 196 RemoteSigner payload/call-site routing layer.

5. Existing no-RemoteSigner payloads remain compatible under default Disabled.

6. Under explicit RemoteSigner policy:

   * missing RemoteSigner material fails closed where RemoteSigner is required;
   * valid fixture loopback material passes only where policy permits;
   * production RemoteSigner material reaches boundary and fails closed as unavailable;
   * malformed material fails closed;
   * validation-only surfaces remain non-mutating;
   * mutating surfaces preserve sequence-before-marker ordering.

Required accepted scenarios:
A1. default selector absent => Disabled.

* old no-RemoteSigner payload accepted where it was accepted before.

A2. CLI `fixture-loopback-allowed` selects `FixtureLoopbackAllowed`.

* DevNet fixture loopback RemoteSigner material accepted under source/test production-context path.

A3. env `fixture-loopback-allowed` selects `FixtureLoopbackAllowed`.

* TestNet fixture loopback RemoteSigner material accepted under source/test production-context path.

A4. CLI `production-remote-signer-required` selects `ProductionRemoteSignerRequired`.

* production RemoteSigner material reaches boundary and fails closed as unavailable.
* This is an accepted selector parse / expected fail-closed validation outcome.

A5. env `mainnet-production-remote-signer-required` selects `MainnetProductionRemoteSignerRequired`.

* MainNet production RemoteSigner unavailable / MainNet refusal outcome reached.
* This is an accepted selector parse / expected fail-closed validation outcome.

A6. CLI-over-env precedence is deterministic:

* env sets `fixture-loopback-allowed`;
* CLI sets `disabled`;
* resolved policy is Disabled.

A7. no-RemoteSigner payload remains compatible under default Disabled.

A8. GenesisBound / EmergencyCouncil / OnChainGovernance proof behavior remains unchanged when RemoteSigner policy is Disabled.

A9. custody policy selector behavior from Run 193 remains compatible.

A10. mutating DevNet fixture loopback RemoteSigner under FixtureLoopbackAllowed preflight accepts where lifecycle/governance/custody/RemoteSigner all pass and sequence-before-marker ordering remains governed by existing apply path.

A11. live inbound `0x05` receives selected policy if live config supports it; if not, document exact limitation.

Required rejection scenarios:
R1. invalid CLI selector value rejected.

R2. invalid env selector value rejected.

R3. unrelated CLI/env does not enable RemoteSigner policy.

R4. no-RemoteSigner payload rejected under FixtureLoopbackAllowed.

R5. no-RemoteSigner payload rejected under ProductionRemoteSignerRequired.

R6. fixture loopback rejected under ProductionRemoteSignerRequired.

R7. fixture loopback rejected under MainnetProductionRemoteSignerRequired.

R8. production RemoteSigner rejected as unavailable.

R9. MainNet production RemoteSigner rejected as unavailable.

R10. wrong environment rejected.

R11. wrong chain rejected.

R12. wrong genesis rejected.

R13. wrong authority root rejected.

R14. wrong custody key id rejected.

R15. wrong signing-key fingerprint rejected.

R16. wrong lifecycle action rejected.

R17. wrong candidate digest rejected.

R18. wrong authority-domain sequence rejected.

R19. wrong request digest rejected.

R20. stale/replayed request rejected.

R21. stale/replayed response rejected.

R22. expired attestation rejected.

R23. expired response rejected.

R24. unsupported suite rejected.

R25. invalid signature / placeholder signature rejected.

R26. malformed RemoteSigner material rejected.

R27. local operator key cannot satisfy RemoteSigner policy.

R28. peer majority / gossip count cannot satisfy RemoteSigner policy.

R29. RemoteSigner valid but custody metadata invalid rejected.

R30. custody valid but RemoteSigner response invalid rejected.

R31. validation-only rejection writes no marker and no sequence.

R32. mutating rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

R33. invalid live inbound `0x05` RemoteSigner material candidate is not propagated, staged, or applied.

R34. MainNet peer-driven apply remains refused even with MainnetProductionRemoteSignerRequired and fixture loopback material.

Required tests:
Add focused tests, preferably:

`crates/qbind-node/tests/run_198_remote_signer_policy_selector_tests.rs`

Minimum coverage:

* A1–A11 where representable.
* R1–R34.
* selector parsing and precedence:

  * default;
  * CLI;
  * env;
  * CLI-over-env;
  * invalid value fail-closed.
* source reachability:

  * selected policy reaches all seven production-context helpers where representable.
* no-mutation invariants:

  * validation-only non-mutation;
  * mutating rejection non-mutation.
* MainNet refusal invariants.

Required documentation:
Add:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_198.md`

Update:

* `docs/whitepaper/contradiction.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

Documentation must state:

* Run 198 is source/test hidden RemoteSigner policy selector and production preflight integration.
* Default remains `RemoteSignerPolicy::Disabled`.
* Fixture loopback RemoteSigner remains DevNet/TestNet evidence-only.
* Fixture loopback cannot satisfy MainNet production RemoteSigner.
* Production RemoteSigner remains unavailable/fail-closed.
* MainNet peer-driven apply remains refused.
* No real RemoteSigner backend is implemented.
* No KMS/HSM/cloud-KMS/PKCS#11 backend is implemented.
* governance execution remains unimplemented.
* real on-chain proof verification remains unimplemented.
* validator-set rotation remains open.
* release-binary RemoteSigner policy selector evidence is deferred to Run 199.
* full C4 remains open.
* C5 remains open.

Validation commands:
Run at minimum:

* `cargo build -p qbind-node --lib`
* `cargo test -p qbind-node --test run_198_remote_signer_policy_selector_tests`
* `cargo test -p qbind-node --test run_196_remote_signer_payload_callsite_tests`
* `cargo test -p qbind-node --test run_194_remote_authority_signer_boundary_tests`
* `cargo test -p qbind-node --test run_192_authority_custody_policy_selector_tests`
* `cargo test -p qbind-node --test run_190_authority_custody_payload_callsite_tests`
* `cargo test -p qbind-node --test run_188_authority_custody_boundary_tests`
* `cargo test -p qbind-node --test run_186_onchain_governance_production_verifier_boundary_tests`
* `cargo test -p qbind-node --test run_184_onchain_governance_payload_carrying_tests`
* `cargo test -p qbind-node --test run_182_onchain_governance_production_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_180_onchain_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_178_onchain_governance_proof_tests`
* `cargo test -p qbind-node --test run_176_live_0x05_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_173_validation_only_governance_required_policy_tests`
* `cargo test -p qbind-node --test run_171_governance_required_policy_selector_tests`
* `cargo test -p qbind-node --test run_169_governance_proof_loader_surface_integration_tests`
* `cargo test -p qbind-node --test run_167_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_165_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_163_governance_authority_verifier_tests`
* `cargo test -p qbind-node --test run_161_lifecycle_marker_integration_tests`
* `cargo test -p qbind-node --test run_159_authority_signing_key_lifecycle_tests`
* `cargo test -p qbind-node --test run_157_unified_testnet_fixture_universe_tests`
* `cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests`
* `cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests`
* `cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests`
* `cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests`
* `cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests`
* `cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Acceptance criteria:
Run 198 is acceptable only if:

1. A hidden RemoteSigner policy selector exists.
2. Default remains Disabled.
3. Selector reaches production preflight contexts at source/test level.
4. Fixture loopback RemoteSigner material passes only where selected policy allows.
5. Missing/malformed/invalid RemoteSigner material fails closed under explicit policy.
6. Production RemoteSigner remains fail-closed as unavailable.
7. Validation-only surfaces remain non-mutating.
8. Mutating rejection surfaces produce no mutation.
9. MainNet peer-driven apply remains refused even with MainnetProductionRemoteSignerRequired and fixture loopback material.
10. Docs defer release-binary RemoteSigner policy selector evidence to Run 199.
11. No real RemoteSigner / KMS / HSM / governance execution / real on-chain proof / validator-set rotation claim is made.
12. No full C4 or C5 closure is claimed.
