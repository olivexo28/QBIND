Task: Run 155 — Release-Binary TestNet End-to-End Peer-Driven Apply Evidence

Current accepted state:
- Run 153: release-binary DevNet end-to-end peer-driven apply evidence landed through the hidden explicit drain-once hook.
- Run 153 proved the full DevNet path:
  live inbound 0x05 → v2 validation-only acceptance → staging queue → drain-once hook → ProductionDrainInvocationBuilder → ProductionV2MarkerCoordinator → Run 150 drain → Run 148 controller → Run 070 apply → LivePqcTrustState swap → session eviction → Run 055 sequence commit → v2 marker persist after commit.
- Run 153 deferred TestNet A2 because TestNet fixture setup was missing.
- Run 154: source/test TestNet fixture tooling landed and closes the TestNet fixture blocker:
  - TestNet genesis and expected genesis hash;
  - signed TestNet trust bundles;
  - ML-DSA-44 bundle-signing key spec;
  - v1/v2 ratification sidecars;
  - seeded v1/v2 authority markers;
  - valid and negative 0x05 peer-candidate fixtures;
  - all explicitly bound to TestNet environment, TestNet chain ID, TestNet genesis hash, authority-root fingerprint, and v2 authority-domain sequence.
- MainNet remains refused.
- Governance remains unimplemented.
- KMS/HSM remains unimplemented.
- signing-key rotation/revocation lifecycle remains open.
- validator-set rotation remains open.
- full C4 remains open.
- C5 remains open.

Objective:
Produce release-binary evidence that a real `target/release/qbind-node` TestNet process can receive a live peer-candidate `0x05` v2 trust-bundle candidate, validate it, stage it, explicitly drain it once, and apply it through the existing peer-driven apply pipeline.

This run should mirror Run 153’s DevNet end-to-end evidence, but using the Run 154 TestNet fixtures and TestNet domain binding.

Required end-to-end path:

  live inbound 0x05 candidate
    → v2 validation-only acceptance
    → staging queue
    → hidden explicit drain-once hook
    → ProductionDrainInvocationBuilder
    → ProductionV2MarkerCoordinator
    → Run 150 PeerDrivenApplyDrain::try_drain_once
    → Run 148 try_apply_staged_peer_candidate
    → Run 070 apply_validated_candidate_with_previous
    → LivePqcTrustState swap
    → session eviction
    → Run 055 sequence commit
    → v2 authority marker persist after commit

This is release-binary evidence. Do not broaden scope into governance, KMS/HSM, MainNet apply, validator-set rotation, or signing-key lifecycle.

Strict scope:
- Release-binary TestNet end-to-end peer-driven apply evidence.
- Use real `target/release/qbind-node`.
- Use Run 154 TestNet fixtures.
- Use real live P2P `0x05` exchange.
- Use explicit local drain-once only.
- No autonomous background drain.
- No automatic apply on receipt.
- No peer-majority authority.
- No MainNet enablement.
- No governance implementation.
- No KMS/HSM implementation.
- No signing-key rotation/revocation lifecycle.
- No validator-set rotation.
- No new wire format.
- No trust-bundle schema change.
- No ratification sidecar schema change.
- No authority-marker schema change.
- No sequence-file schema change.
- No peer-candidate envelope schema change.
- Do not weaken Runs 070, 142, 143, 145–154.
- Do not claim full C4 or C5 closure.

Deliverables:
1. Add release-binary harness:
   scripts/devnet/run_155_testnet_peer_driven_apply_end_to_end_release_binary.sh

2. Add evidence archive:
   docs/devnet/run_155_testnet_peer_driven_apply_end_to_end_release_binary/

3. Add canonical evidence report:
   docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_155.md

4. Update narrowly:
   - docs/whitepaper/contradiction.md
   - docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md
   - docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md
   - docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md

5. Crosscheck all new/modified code and docs against existing design/spec. If contradictions or inconsistencies are found, record them in contradiction.md before finalizing.

Required release-binary topology:
Use a real TestNet N=3 topology:

- V0: publisher of live `0x05` v2 TestNet peer-candidate.
- V1: TestNet receiver with wire validation, staging, apply-enabled, and drain-once enabled.
- V2: observer / propagation invariant node.

Use:
- real `target/release/qbind-node`;
- real live P2P `0x05` exchange;
- Run 154 TestNet genesis / trust bundle / sidecar / marker fixtures;
- TestNet environment;
- TestNet chain ID;
- TestNet genesis hash;
- real ML-KEM-768 / ML-DSA-44 / ChaCha20-Poly1305 path;
- mutual-auth Required if the existing Run 153 harness uses it;
- no active DummySig / DummyKem / DummyAead.

Required accepted scenarios:

A1. TestNet end-to-end peer-driven apply succeeds
- V0 publishes valid TestNet v2 candidate over live `0x05`.
- V1 validates, stages, and explicitly drains once.
- Expected:
  - live `0x05` path actually exercised;
  - TestNet domain selected;
  - v2 validation-only acceptance visible;
  - candidate staged before drain;
  - drain-once trigger accepted;
  - `ProductionDrainInvocationBuilder` invoked;
  - `ProductionV2MarkerCoordinator` invoked;
  - Run 150 drain invoked;
  - Run 148 controller invoked;
  - Run 070 apply contract invoked;
  - `LivePqcTrustState` swap occurs;
  - session eviction occurs according to Run 070/072 semantics;
  - Run 055 sequence commit succeeds;
  - v2 authority marker persists strictly after sequence commit;
  - Applied outcome visible;
  - no autonomous repeat drain.

A2. TestNet duplicate candidate cannot double-apply
- Same valid TestNet candidate is published twice or drain is triggered twice.
- Expected:
  - one Applied outcome maximum;
  - second trigger returns NoCandidate / AlreadyApplied / deduped;
  - no duplicate sequence write;
  - no duplicate marker write;
  - no duplicate session eviction.

A3. TestNet deterministic highest-sequence selection
- Stage multiple valid TestNet v2 candidates.
- Expected:
  - highest authority-domain sequence selected;
  - tie-break by lexicographically smallest fingerprint if equal sequence;
  - selected candidate applied;
  - lower candidate not accidentally applied first.

A4. TestNet empty queue drain returns NoCandidate
- Drain-once enabled but no candidate staged.
- Expected:
  - typed NoCandidate visible;
  - no live trust swap;
  - no session eviction;
  - no sequence write;
  - no marker write.

A5. TestNet disabled policy refuses drain
- Candidate may be valid/staged, but apply/drain policy disabled.
- Expected:
  - disabled/co-requisite refusal visible;
  - no mutation.

A6. MainNet refusal remains intact
- Attempt to arm or run peer-driven apply drain under MainNet using fixture or command substitution.
- Expected:
  - fail closed before mutation;
  - MainNetRefused / FATAL visible;
  - no P2P apply;
  - no sequence write;
  - no marker write;
  - no live trust swap.

Required rejection/no-op scenarios:

R1. TestNet lower-sequence candidate cannot apply
- Expected:
  - rejected before Run 070 apply;
  - no live trust swap;
  - no session eviction;
  - no sequence write;
  - no marker write.

R2. TestNet same-sequence different-digest candidate cannot apply
- Expected:
  - equivocation/conflict reason visible;
  - no mutation.

R3. TestNet bad-signature candidate cannot apply
- Expected:
  - Run 130 verifier failure visible;
  - no mutation.

R4. wrong-environment candidate cannot apply
- For example DevNet candidate sent to TestNet receiver.
- Expected:
  - wrong-environment reason visible;
  - no mutation.

R5. wrong-chain candidate cannot apply
- Expected:
  - wrong-chain reason visible;
  - no mutation.

R6. wrong-genesis candidate cannot apply
- Expected:
  - wrong-genesis reason visible;
  - no mutation.

R7. ambiguous v1+v2 candidate cannot apply
- Expected:
  - ambiguity refusal visible;
  - no mutation.

R8. expired staged candidate cannot apply, if supported by Run 154 fixtures
- Expected:
  - CandidateExpired or equivalent visible;
  - no mutation.
- If release-binary expiry timing is infeasible without flaky sleeps, document and cite Run 152/154 source-test coverage.

R9. concurrent drain attempt returns AlreadyInProgress, if feasible on release binary
- Trigger two drains close together.
- Expected:
  - one enters;
  - second returns AlreadyInProgress;
  - no double apply;
  - no duplicate sequence/marker/eviction.
- If infeasible with process-start one-shot semantics, document and cite Run 150/152 source-test coverage.

R10. propagation-only behavior unchanged
- With propagation enabled:
  - valid candidate may rebroadcast only after validation;
  - invalid candidate never rebroadcasts;
  - propagation does not imply apply.
- With propagation disabled:
  - no rebroadcast.

R11. DevNet behavior from Run 153 remains unchanged
- Run a minimal DevNet sanity scenario or regression harness if cheap.
- Expected:
  - Run 153 DevNet path still works or is not touched by this run.

Required ordering proof:
For every accepted TestNet apply scenario, prove by ordered logs or deterministic archive artifacts:

1. P2P connection established.
2. live `0x05` candidate received.
3. v2 validation-only accepted under TestNet domain.
4. candidate staged.
5. explicit drain-once triggered.
6. `ProductionDrainInvocationBuilder` built invocation.
7. `ProductionV2MarkerCoordinator` accepted pre-apply marker decision.
8. Run 150 drain invoked.
9. Run 148 controller invoked.
10. Run 070 ordering:
    validate → snapshot previous → swap → evict_sessions → commit_sequence.
11. sequence commit succeeds.
12. v2 marker persists after sequence commit.
13. Applied outcome emitted.

Required captured evidence:
- qbind-node SHA-256 and ELF Build ID;
- helper binary SHA-256 and ELF Build ID;
- git commit hash;
- rustc/cargo versions;
- exact commands;
- node PIDs;
- stdout/stderr logs;
- metrics before/after;
- sequence JSON/SHA before/after;
- authority marker JSON/SHA before/after;
- TestNet genesis hash;
- TestNet chain ID;
- TestNet environment proof;
- active trust root / live-trust fingerprint before/after if observable;
- session-eviction counters before/after;
- staging queue observations if logged;
- drain outcome logs;
- ordered line-number proof for apply ordering;
- denylist grep for out-of-scope behavior.

Required denylist / negative invariants:
Across all scenarios:
- no autonomous background drain;
- no apply on receipt without explicit drain;
- no peer-majority authority;
- no governance claim;
- no KMS/HSM claim;
- no signing-key rotation/revocation claim;
- no validator-set rotation claim;
- no MainNet apply;
- no fallback to `--p2p-trusted-root`;
- no active DummySig / DummyKem / DummyAead;
- no SIGHUP outcome;
- no reload-apply outcome;
- no startup mutation path accidentally selected;
- no snapshot/restore path accidentally selected;
- no schema/wire/metric drift.

Validation commands:
Run at minimum:
- cargo build --release -p qbind-node --bin qbind-node
- cargo build --release -p qbind-node --example run_133_v2_validation_only_fixture_helper
- bash scripts/devnet/run_155_testnet_peer_driven_apply_end_to_end_release_binary.sh
- cargo test -p qbind-node --test run_154_testnet_peer_apply_fixture_tests
- cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests
- cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests
- cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests
- cargo test -p qbind-node --test run_146_live_inbound_0x05_staging_hook_tests
- cargo test -p qbind-node --test run_145_peer_candidate_staging_tests
- cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests
- cargo test -p qbind-node --test run_088_pqc_peer_candidate_propagation_tests
- cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests
- cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests
- cargo test -p qbind-node --lib pqc_authority
- cargo test -p qbind-node --lib

If exact test target names differ, locate the nearest existing Run 145/146/148/150/152/154/142/088 targets and document exact commands/results.

Documentation requirements:
`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_155.md` must state:
- Run 155 is release-binary TestNet end-to-end peer-driven apply evidence.
- It closes the Run 153 TestNet A2 fixture/evidence deferral if successful.
- No autonomous background apply exists.
- No automatic apply on receipt exists.
- MainNet remains refused.
- Governance remains unimplemented.
- KMS/HSM remains unimplemented.
- signing-key rotation/revocation lifecycle remains open.
- validator-set rotation remains open.
- DevNet evidence from Run 153 remains valid.
- Full C4 remains open unless the team has separately defined and satisfied every C4 closure criterion.
- C5 remains open.

Acceptance criteria:
Run 155 is acceptable only if:
1. real `target/release/qbind-node` is used;
2. real TestNet live P2P `0x05` candidate is received and staged;
3. explicit local drain-once trigger applies at most one valid staged TestNet candidate;
4. accepted TestNet apply routes through ProductionDrainInvocationBuilder, ProductionV2MarkerCoordinator, Run 150 drain, Run 148 controller, and Run 070 contract;
5. Run 070 ordering is evidenced;
6. sequence commit happens before v2 marker persistence;
7. session eviction occurs according to Run 070/072 semantics;
8. reject/no-op scenarios produce no mutation;
9. MainNet is refused;
10. no autonomous peer-driven apply exists;
11. no peer-majority/governance/KMS-HSM claim is made;
12. evidence archive is complete;
13. docs are updated narrowly;
14. no C5 closure is claimed.