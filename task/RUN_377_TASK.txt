Run 377 — Public DevNet M4 live seed/bootnode reachability evidence and live seed-list candidate

Continue QBIND after accepted Run 376.

Current state:
Run 376 is accepted as PASS / public-DevNet identity registration/admission-check positive.

Run 376 delivered:
- `qbind-node identity register-check`;
- non-mutating admission validation for `public-identity.json`;
- deterministic NodeId verification from `leaf.cert.bin`;
- schema-compatible planned seed-list candidate mapping;
- fail-closed refusal for live-without-reachability, planned+reachability, embedded private material, malformed identity fields, wrong environment, MainNet/TestNet material, mismatched cert, mismatched validator material, and unknown/mismatched role;
- no socket opened;
- no runtime state mutation;
- no M4 Green;
- no C4/C5 closure.

Current readiness:
- M12 Green.
- M6 generation/verification half Green-for-scope.
- M6 registration/admission-check half Green-for-scope.
- M6 as a whole still Yellow/Partial because live registration depends on M4.
- M4 Yellow/launch-blocking because no externally reachable seed/bootnode has been proven.
- Public DevNet NOT launch-ready.
- C4 OPEN.
- C5 OPEN.
- MainNet authority rotation/revocation Red.

Primary objective:
Produce bounded, honest M4 live seed/bootnode reachability evidence for public DevNet, using Run 375/376 identity material and `register-check --status live --reachability-evidence <ref>`.

This run must determine whether M4 can move Yellow → Green. If real external reachability cannot be proven from outside the seed operator’s own host/NAT, M4 must remain Yellow and the exact blocker must be recorded.

Strict scope:
M4 live seed/bootnode reachability evidence and live seed-list candidate only.

No public DevNet launch unless every must-have is Green, which is not expected.
No faucet.
No RPC gateway.
No explorer.
No status page.
No TestNet readiness claim.
No MainNet readiness claim.
No C4 closure.
No C5 closure.
No authority-lifecycle runtime wiring.
No validator-set mutation.
No epoch transition.
No sequence/marker write.
No LivePqcTrustState mutation.
No trust-bundle live apply.
No peer-driven apply.
No P2P wire-format change.
No peer-admission weakening.
No hidden fallback roots.
No committed private keys, generated roots, generated leaf keys, data dirs, logs with secrets, metrics dumps with private endpoints, or credentials.

Required investigation:
Inspect:
- `crates/qbind-node/src/identity_cli.rs`;
- Run 376 evidence, tests, and harness;
- Run 375 identity command evidence;
- `docs/release/public-devnet/network/devnet-seed-list.schema.json`;
- `docs/release/public-devnet/network/devnet-seeds.placeholder.json`;
- `docs/release/public-devnet/network/README.md`;
- `docs/release/public-devnet/identity/*`;
- `docs/release/public-devnet/operator/QUICKSTART.md`;
- `docs/release/public-devnet/operator/IDENTITY.md`;
- `docs/release/public-devnet/p2p/PEER_ADMISSION_POLICY.md`;
- `docs/release/public-devnet/p2p/VERIFY.md`;
- `docs/release/QBIND_PUBLIC_DEVNET_READINESS_CRITERIA.md`;
- `crates/qbind-node/src/p2p_node_builder.rs`;
- `crates/qbind-node/src/p2p_tcp.rs`;
- `crates/qbind-node/src/pqc_root_config.rs`;
- `crates/qbind-node/src/metrics_http.rs`;
- existing Run 371–373 strict-auth/static-root live-socket helpers.

Decision gate:
Record one of:

Route A — Real externally reachable seed infrastructure is available.
Use it to produce M4 live reachability evidence and a live seed-list candidate.

Route B — Only loopback / local / same-host infrastructure is available.
Do not move M4 Green. Produce a partial-positive preflight and record the missing external reachability proof.

Route C — No safe live seed can be operated in this environment.
Do not move M4 Green. Produce docs/evidence explaining exact blockers and required operator/infrastructure prerequisites.

M4 Green rules:
M4 may move Green only if all of the following are proven:
1. at least one real seed/bootnode entry is intentionally published as `status: live`;
2. the seed uses real public `node_id` / `peer_id` values generated by the Run 375 identity path;
3. no private material is committed;
4. `last_reachability_evidence` is present and reproducible;
5. reachability is proven from outside the seed operator’s own host/NAT;
6. the reaching client performs at minimum a TCP dial and, preferably, a KEMTLS strict-auth handshake;
7. the seed entry pins the Run 356 genesis hash and genesis file SHA correctly;
8. `register-check --status live --reachability-evidence <ref>` accepts the candidate;
9. the full seed-list document validates against `devnet-seed-list.schema.json`;
10. safety labels still state experimental/resettable/no-value/not launch-ready unless every must-have is Green.

Implementation requirements:
If Route A is possible, add or update:

1. A live seed-list candidate artifact:
   `docs/release/public-devnet/network/devnet-seeds.live-candidate.json`
   or another clearly named non-final artifact if public launch is not being claimed.

2. A reachability evidence document:
   `docs/release/public-devnet/network/reachability/RUN_377_<seed-id>.md`

3. A harness:
   `scripts/devnet/run_377_public_devnet_live_seed_reachability.sh`

4. A canonical evidence file:
   `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_377.md`

5. An archive:
   `docs/devnet/run_377_public_devnet_live_seed_reachability/{README.md,summary.txt,.gitignore}`

If Route B/C, use the same evidence paths but clearly mark the verdict Partial-positive and keep M4 Yellow.

Required live seed candidate fields:
The live candidate must include:
- `node_id`;
- `peer_id`;
- `validator_address` if applicable;
- `p2p_host`;
- `p2p_port`;
- `p2p_multiaddr`;
- `transport_security_mode`;
- `pqc_suite`;
- `trust_bundle_required`;
- `expected_genesis_hash`;
- `operator`;
- `status: live`;
- `last_reachability_evidence`;
- notes with safety label and evidence reference.

Required reachability evidence:
Capture:
1. timestamp in UTC;
2. seed identity hash / node_id;
3. source vantage point;
4. target host/port;
5. route type: external, same-cloud-external, same-host, loopback, or unknown;
6. TCP dial result;
7. KEMTLS strict-auth handshake result, if feasible;
8. observed remote NodeId / cert-derived identity, if feasible;
9. metrics endpoint evidence, if safely exposed on localhost only;
10. exact command used;
11. logs with secrets redacted;
12. conclusion: external reachability proven or not proven.

Do not mark M4 Green if the only proof is loopback, localhost, same process, same host, or internal-only private network.

Required register-check evidence:
Run:
- `qbind-node identity generate devnet seed <outdir>` or use a pre-generated operator identity from a secure temp path;
- `qbind-node identity register-check <public-identity.json> --seed-list <seed-list> --role seed --cert <leaf.cert.bin> --status live --reachability-evidence <ref>`;
- prove it accepts only when live reachability evidence is present;
- prove it rejects live without evidence;
- prove planned with reachability is still rejected;
- prove malformed/mismatched identity still fails closed.

Required tests:
Run and record exact counts:
- `cargo build -p qbind-node --release --bin qbind-node`;
- `cargo test -p qbind-node --test run_376_public_devnet_identity_registration_tests`;
- `cargo test -p qbind-node --test run_375_public_devnet_identity_cli_tests`;
- `cargo test -p qbind-node --lib identity_cli`;
- seed-list schema validation;
- identity schema validation;
- Run 377 harness.

If Rust source changes are needed, add a Run 377 test file and run it explicitly. Prefer no production source change.

Docs to update:
- `docs/release/public-devnet/network/README.md`
- `docs/release/public-devnet/network/VERIFY.md`
- `docs/release/public-devnet/identity/VERIFY.md`
- `docs/release/public-devnet/operator/QUICKSTART.md`
- `docs/release/public-devnet/operator/IDENTITY.md`
- `docs/release/public-devnet/p2p/PEER_ADMISSION_POLICY.md`
- `docs/release/public-devnet/p2p/VERIFY.md`
- `docs/release/QBIND_PUBLIC_DEVNET_READINESS_CRITERIA.md`
- `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
- `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
- `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
- `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
- `docs/whitepaper/contradiction.md`

Readiness matrix rules:
- M4 may move Green only with real external reachability evidence for at least one live seed/bootnode.
- M6 may move fully Green only if live registration/admission is now backed by the M4 live seed path and all M6 remaining conditions are satisfied.
- M12 remains Green unless contradicted.
- Public DevNet remains NOT launch-ready unless all must-haves are Green.
- C4/C5 remain OPEN.
- MainNet/TestNet remain untouched.

Security:
Run secret scanning over all changed files.
Do not commit generated private keys, root signing keys, KEM secret keys, data dirs, logs with private material, credentials, tokens, private infrastructure secrets, or unredacted operator secrets.
If real hostnames/IPs are published, confirm they are intentionally public seed endpoints.

CodeQL:
If Rust source changes, invoke CodeQL if available.
If no Rust source changes, record CodeQL as not meaningful/docs-harness-only.
If skipped, database-too-large, timed out, unavailable, or not meaningful, record exact status and do not call it clean.

Expected verdict:
PASS / public-DevNet M4 live seed reachability positive if real externally reachable seed/bootnode evidence lands and M4 Green is justified.

Partial-positive if only loopback/same-host/internal reachability or preflight evidence lands; keep M4 Yellow.

Final response format:
1. Exact verdict.
2. Files changed.
3. Decision gate route.
4. Live seed / bootnode candidate.
5. Identity material and public/private separation.
6. Reachability evidence shape.
7. External vantage-point evidence.
8. KEMTLS / strict-auth evidence.
9. Register-check live admission evidence.
10. Seed-list schema evidence.
11. Rejection / fail-closed evidence.
12. Default compatibility / no runtime behavior change.
13. Readiness matrix delta for M4.
14. Readiness matrix delta for M6.
15. Current public DevNet readiness status.
16. Remaining public DevNet blockers.
17. Public TestNet blockers.
18. MainNet blockers.
19. C4/C5 status.
20. Tests run.
21. Security scans.
22. CodeQL.
23. Provenance.
24. Honest limitations.
25. Suggested Run 378 next step.