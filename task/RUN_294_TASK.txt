Run 294 — Release-binary evidence for the production RemoteSigner backend

You are continuing QBIND after Run 293.

Current accepted state:

Run 292 is accepted as PASS / release-binary evidence positive for the production durable replay RocksDB backend.

The production durable replay RocksDB backend row is Green for narrow release-binary-evidenced backend behavior only. It is not wired by default into the production runtime and does not close C4/C5.

Run 293 is accepted as PASS / source-test production RemoteSigner backend implementation.

Run 293 added the first real production RemoteSigner backend client:

`crates/qbind-node/src/pqc_production_remote_signer_backend.rs`

The backend is source/test only. It defines `ProductionRemoteSignerBackend<T: RemoteSignerBackendTransport>`, composes the existing Run 201 `validate_remote_signer_transport` boundary and the Run 194 `validate_remote_signer` boundary, builds deterministic request envelopes, enforces request/response correlation, enforces backend transcript binding, applies bounded retry/timeout behavior, maps precise outcomes, defaults to `ProductionRemoteSignerBackendPolicy::Disabled`, refuses MainNet absent production authority criteria, and never silently falls back to fixture/loopback/local-key signing under production policy.

Run 293 added no production runtime wiring, no public CLI flag, no default enablement, no MainNet enablement, no KMS/HSM/cloud-KMS/PKCS#11 custody, no custody attestation, no on-chain governance proof verifier, no governance execution engine, no validator-set rotation, no settlement, and no external publication.

The C4/C5 matrix now has:

* Production durable replay RocksDB backend — Green for release-binary-evidenced scope only.
* Real production RemoteSigner backend — Yellow / source-test implementation landed, release-binary evidence pending Run 294.
* Real KMS/HSM/cloud-KMS/PKCS#11 custody backend — Red.
* Real custody attestation verifier — Red.
* Real on-chain governance proof verifier — Red.
* Governance execution engine — Red.
* Validator-set rotation / authority-set synchronization — Red.
* MainNet authority rotation/revocation under production custody — Red.
* Production signing audit trail / crypto-agility activation / incident response — Red.
* Full MainNet release-binary evidence under production custody — Red.

Full C4 remains OPEN. C5 remains OPEN.

Objective:

Produce release-binary evidence for the Run 293 production RemoteSigner backend.

This run must prove, on real `target/release/qbind-node` plus a release-built helper, that the Run 293 production RemoteSigner backend symbols are present and exercised in release mode, while the production binary remains default-Disabled/silent, no CLI flag is added, MainNet remains refused absent production authority criteria, and C4/C5 remain open.

This is release-binary evidence only.

Do not add another modeled layer.

Do not add a new backend.

Do not wire RemoteSigner into default production runtime behavior.

Do not add a public CLI flag.

Do not claim C4 closure.

Do not claim C5 closure.

Primary deliverables:

1. New release helper:

`crates/qbind-node/examples/run_294_production_remote_signer_backend_release_binary_helper.rs`

2. New release harness:

`scripts/devnet/run_294_production_remote_signer_backend_release_binary.sh`

The harness must be LF-clean and executable.

3. New ignored evidence archive directory:

`docs/devnet/run_294_production_remote_signer_backend_release_binary/`

Track only:

* `README.md`
* `summary.txt`
* `.gitignore`

Generated logs, raw helper outputs, temp files, stdout/stderr captures, and binary artifacts must be ignored unless the project’s evidence convention explicitly requires otherwise.

4. New canonical evidence report:

`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_294.md`

5. Narrow documentation updates:

* `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
* `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
* `docs/whitepaper/contradiction.md`

Preflight documentation hygiene:

Before or during Run 294, verify that the Run 293 canonical evidence file records the exact CodeQL result directly. If it still says only “see final task response,” replace that with the actual honest result, e.g. CodeQL attempted but timed out / skipped / did not complete, and no CodeQL coverage is claimed. This is documentation hygiene only; do not change Run 293’s verdict unless a real contradiction is found.

Strict scope:

Allowed:

* Build real `target/release/qbind-node`.
* Build a release helper that links against the real release-built qbind-node library symbols.
* Exercise the Run 293 production RemoteSigner backend in release mode.
* Use source/test loopback/mock transports inside the release helper only as evidence harness scaffolding.
* Prove request/response/transcript/domain binding in release mode.
* Prove timeout, unavailable, malformed, replay, wrong-domain, wrong-request, wrong-action, wrong-signer, wrong-policy, and wrong-protocol fail-closed behavior in release mode.
* Prove default production binary surfaces remain Disabled/silent.
* Prove no new CLI flag is exposed.
* Prove MainNet remains refused absent production authority criteria.
* Capture release hashes, helper verdict, scenario return codes, denylist results, reachability results, C4/C5 taxonomy greps, regression results, secret scan, and CodeQL result or honest skip/timeout.
* Update docs/evidence narrowly.

Not allowed:

* Do not add new production runtime wiring.
* Do not add a public CLI flag.
* Do not enable RemoteSigner by default.
* Do not make MainNet use RemoteSigner.
* Do not claim production custody closure.
* Do not claim C4 closure.
* Do not claim C5 closure.
* Do not claim MainNet readiness.
* Do not claim full production readiness.
* Do not implement KMS/HSM/cloud-KMS/PKCS#11 provider integration.
* Do not implement custody attestation.
* Do not implement real on-chain governance proof verification.
* Do not implement a governance execution engine.
* Do not implement validator-set rotation.
* Do not implement settlement, settlement finality, settlement receipt, settlement-receipt acknowledgement, settlement-finality projection, settlement-outcome report backend, or settlement-outcome publication.
* Do not implement external publication, external-publication confirmation, receipt, acknowledgement, audit finalization, audit completion, audit archive, audit seal, or audit anchor.
* Do not call Run 070.
* Do not mutate `LivePqcTrustState`.
* Do not swap trust state.
* Do not evict sessions.
* Do not write PQC trust-bundle sequence files.
* Do not write authority markers.
* Do not change trust-bundle wire format.
* Do not change peer-candidate wire format.
* Do not change consensus wire format.
* Do not change the Run 291/292 RocksDB durable replay backend except for minimal helper composition if strictly necessary.
* Do not silently fall back to fixture, loopback, local signing, raw local keys, or in-memory signing under production policy.
* Do not mark unrelated Red rows Green.
* Do not mark Full C4 or C5 closed.

Required release helper behavior:

The release helper must link against the real release-built qbind-node library and exercise the actual Run 293 production RemoteSigner backend symbols.

At minimum, it must reference the actual implemented names, including where present:

* `pqc_production_remote_signer_backend`
* `ProductionRemoteSignerBackend`
* `RemoteSignerBackendTransport`
* `ProductionRemoteSignerBackendPolicy`
* `ProductionRemoteSignerBackendConfig` or actual config type
* `ProductionRemoteSignerBackendIdentity` or actual identity type
* `ProductionRemoteSignerError`
* `ProductionRemoteSignerOutcome`
* `PRODUCTION_REMOTE_SIGNER_BACKEND_PROTOCOL_VERSION`
* `production_remote_signer_request_id`
* `production_remote_signer_backend_transcript_digest`
* `LoopbackRemoteSignerService`
* `MockRemoteSignerBackendTransport`
* `validate_remote_signer_transport`
* `validate_remote_signer`

If actual names differ, use the real names and document substitutions in the evidence report.

The helper must produce machine-readable summary output with per-check PASS/FAIL counts and a final verdict.

Suggested helper sections:

A. Accepted / compatible release checks

* DevNet source/test loopback RemoteSigner accepts a valid authority-lifecycle signing request.
* DevNet source/test loopback RemoteSigner accepts a valid governance-execution signing request.
* TestNet source/test loopback RemoteSigner accepts a valid request only under explicit allowed policy.
* Accepted path reaches transport exactly once on the happy path.
* Request id is deterministic across repeated builds.
* Response digest is deterministic across repeated builds if represented.
* Backend transcript digest is deterministic across repeated builds.
* Two identical requests produce identical request/transcript digests.
* Valid response authorizes exactly the matching request.
* Run 201 `validate_remote_signer_transport` composition is exercised.
* Run 194 `validate_remote_signer` composition is exercised through Run 201.
* Optional Run 291 durable replay record digest composition point is exercised if represented.
* Existing fixture/in-memory RemoteSigner boundary behavior remains unchanged where representable.

B. Rejection / fail-closed release checks

* Default Disabled policy fails closed before transport invocation.
* MainNet identity refused before transport invocation.
* Fixture/loopback material refused for MainNet.
* Wrong environment rejected.
* Wrong chain rejected.
* Wrong genesis/domain rejected.
* Wrong authority sequence/epoch rejected if represented.
* Wrong signer identity rejected.
* Wrong signer key rejected.
* Wrong request-id echo rejected.
* Wrong response nonce / stale nonce rejected.
* Wrong transcript digest rejected.
* Wrong candidate/proposal digest rejected.
* Wrong authorized action rejected.
* Wrong protocol version rejected.
* Missing response signature/proof rejected.
* Malformed response rejected.
* Oversized response rejected if sizing exists.
* Response from prior request replay rejected.
* Signer unavailable rejected.
* Signer refused rejected.
* Signer policy rejected rejected.
* Signer attestation unavailable rejected.
* Transport endpoint unavailable rejected.
* Connection refused rejected.
* Timeout rejected.
* Decode error rejected.
* Unsupported protocol rejected.
* Unsupported request kinds rejected before transport:

  * validator-set rotation;
  * policy change;
  * on-chain governance proof verification.
* Ambiguous result rejected fail-closed.

C. MainNet / authority policy release checks

* MainNet cannot be satisfied by local operator material.
* MainNet cannot be satisfied by fixture loopback signer.
* MainNet cannot be satisfied by peer majority.
* MainNet cannot bypass missing custody attestation.
* MainNet cannot bypass missing governance proof verifier.
* MainNet cannot bypass validator-set rotation gate.
* `MainnetProductionRequired` on MainNet returns typed unavailable/fail-closed.
* Non-MainNet production-required path returns RemoteSigner unavailable unless real production authority exists.
* MainNet peer-driven apply remains refused.

D. Replay / recovery / idempotency release checks

* No-prior-request recovery window is clean/no-op.
* Identical requests are idempotent over deterministic request id.
* Same request id with different transcript fails closed.
* Same request id with different response commitment fails closed.
* Retry-then-success is idempotent across attempts.
* Retry budget is bounded.
* Retry exhaustion fails closed.
* Terminal/non-retryable errors are not retried.
* Stale nonce replay rejected.
* Accepted response is evidence-only and does not authorize mutation beyond signer boundary.

E. Non-mutation / no-authority-extension release checks

Every rejected helper path must prove:

* no Run 070 call;
* no `LivePqcTrustState` mutation;
* no trust swap;
* no session eviction;
* no PQC trust-bundle sequence write;
* no authority marker write;
* no durable replay overwrite;
* no settlement;
* no external publication;
* no governance execution engine;
* no validator-set rotation;
* no KMS/HSM/cloud-KMS/PKCS#11 signing;
* no raw local production signing key load;
* no fallback to fixture/loopback/local signing;
* no MainNet enablement.

These may be proven through helper invariants, source-reachability greps, denylist checks, and real-binary scenario silence.

Required real-binary scenarios:

The harness must build and run real `target/release/qbind-node`.

At minimum:

S1. `target/release/qbind-node --help`

Expected:

* rc=0.
* no public Run 293/294 RemoteSigner backend flag appears.
* no RemoteSigner backend enablement claim.
* no custody/KMS/HSM claim.
* no MainNet readiness claim.
* no C4/C5 closure claim.

S2. Default DevNet binary surface.

Use the smallest safe invocation already used by recent release evidence harnesses.

Expected:

* RemoteSigner backend is not constructed by default.
* no RemoteSigner backend enablement banner;
* no helper/evidence-only code path appears;
* no mutation.

S3. Default TestNet binary surface.

Expected:

* RemoteSigner backend is not constructed by default.
* no RemoteSigner backend enablement banner;
* no custody/KMS/HSM/governance/validator-set-rotation claim;
* no mutation.

S4. Default MainNet binary surface.

Expected:

* RemoteSigner backend is not constructed by default.
* no RemoteSigner backend enablement banner;
* no MainNet enablement claim;
* any existing MainNet-required-config failure remains expected fail-closed behavior;
* no mutation.

S5. Existing governance/RemoteSigner/custody selectors, if any, remain compatible.

If an existing hidden selector related to governance execution, custody, or RemoteSigner exists, prove it still parses or fails closed exactly as before. If there is no existing selector, document that no new CLI selector was added.

S6. Invalid selector / invalid config fail-closed scenario.

Use the nearest existing invalid governance/custody/RemoteSigner selector if available. It must fail closed before mutation.

Required release artifact metadata:

Capture in `summary.txt`:

* git commit;
* branch name if available;
* dirty/clean status;
* rustc version;
* cargo version;
* OS/kernel summary if existing harness convention captures it;
* `target/release/qbind-node` SHA-256;
* release helper SHA-256;
* helper verdict;
* helper check counts by category;
* deterministic request/transcript digest samples;
* real-binary scenario return codes;
* denylist result;
* reachability result;
* C4/C5 taxonomy result;
* regression result;
* secret scan result;
* CodeQL result or honest limitation;
* C4/C5 status.

Required denylist:

The harness must assert zero matches in release binary stdout/stderr/help/helper logs for false claims such as:

* `C4 closed`
* `C5 closed`
* `MainNet ready`
* `production ready`
* `production custody active`
* `RemoteSigner active`
* `RemoteSigner enabled`
* `RemoteSigner default enabled`
* `KMS active`
* `HSM active`
* `PKCS11 active`
* `cloud KMS active`
* `custody attestation active`
* `on-chain governance verifier active`
* `governance execution engine active`
* `validator-set rotation active`
* `peer-driven apply enabled`
* `Run 070 applied`
* `LivePqcTrustState mutated`
* `trust swap complete`
* `session eviction complete`
* `authority marker written`
* `trust-bundle sequence written`
* `settlement finalized`
* `settlement receipt recorded`
* `settlement outcome published`
* `external publication completed`
* `external publication confirmed`
* `external publication receipt recorded`
* `external publication acknowledgement recorded`
* `external publication audit finalized`
* `external publication audit completed`
* `external publication audit archived`
* `external publication audit sealed`
* `external publication audit anchored`
* `fallback to fixture`
* `fallback to loopback`
* `fallback to local signing`
* `raw local production key`
* `DummySig active`
* `DummyKem active`
* `DummyAead active`

Adjust exact grep patterns to avoid false positives from explicit negative statements such as “not active” or evidence non-goal prose. The evidence must clearly distinguish absent enablement claims from documented non-goals.

Required reachability greps:

The harness must prove the release helper and/or source references the Run 293 production symbols, including:

* `ProductionRemoteSignerBackend`
* `RemoteSignerBackendTransport`
* `ProductionRemoteSignerBackendPolicy`
* `ProductionRemoteSignerError`
* `ProductionRemoteSignerOutcome`
* `PRODUCTION_REMOTE_SIGNER_BACKEND_PROTOCOL_VERSION`
* `production_remote_signer_request_id`
* `production_remote_signer_backend_transcript_digest`
* `LoopbackRemoteSignerService`
* `MockRemoteSignerBackendTransport`
* `validate_remote_signer_transport`
* `validate_remote_signer`
* `record_replay_event` or Run 291 durable replay digest composition point if represented.

Required C4/C5 taxonomy verification:

The harness must grep `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md` and prove:

* Status as of Run 294 is present.
* Full C4 remains OPEN.
* C5 remains OPEN.
* Production durable replay RocksDB backend remains Green for release-binary-evidenced scope only.
* Real production RemoteSigner backend row is updated correctly.
* If Run 294 evidence passes, this row may become Green only for narrow release-binary-evidenced RemoteSigner backend behavior.
* The RemoteSigner row wording must explicitly say: not wired by default, no public CLI flag, MainNet refused absent production authority criteria, does not close C4/C5.
* Real KMS/HSM/cloud-KMS/PKCS#11 custody backend remains Red.
* Real custody attestation verifier remains Red.
* Real on-chain governance proof verifier remains Red.
* Governance execution engine remains Red.
* Validator-set rotation / authority-set synchronization remains Red.
* MainNet authority rotation/revocation under production custody remains Red.
* Production signing audit trail / crypto-agility activation / incident response remains Red.
* Full MainNet release-binary evidence under production custody remains Red.
* Matrix taxonomy still separates boundary readiness from production readiness.
* Green-for-scope does not mean C4/C5 closure unless every closure gate is met.

Important matrix guidance:

If Run 294 proves release-binary behavior for the Run 293 RemoteSigner backend, it is acceptable to mark only the narrow **Real production RemoteSigner backend** capability as Green or Green-for-scope, provided the row wording is precise:

“Green for release-binary-evidenced RemoteSigner backend behavior only; not wired by default into production runtime; no public CLI flag; MainNet refused absent production authority criteria; does not implement KMS/HSM/custody attestation/on-chain verifier/governance engine/validator-set rotation; does not close C4/C5.”

Do not turn KMS/HSM custody Green.

Do not turn custody attestation Green.

Do not turn on-chain verifier Green.

Do not turn governance engine Green.

Do not turn validator-set rotation Green.

Do not claim C4/C5 closure.

Required tests / regression commands:

Run at minimum:

* `cargo build -p qbind-node --release --bin qbind-node`
* `cargo build -p qbind-node --release --example run_294_production_remote_signer_backend_release_binary_helper`
* `scripts/devnet/run_294_production_remote_signer_backend_release_binary.sh`
* `cargo test -p qbind-node --test run_293_production_remote_signer_backend_tests`
* `cargo test -p qbind-node --test run_291_production_durable_replay_rocksdb_tests`
* `cargo test -p qbind-node --test run_290_durable_completion_external_publication_audit_anchor_tests`
* `cargo test -p qbind-node --test run_288_durable_completion_external_publication_audit_seal_tests`
* `cargo test -p qbind-node --test run_286_durable_completion_external_publication_audit_archive_tests`
* `cargo test -p qbind-node --test run_284_durable_completion_external_publication_audit_completion_tests`
* `cargo test -p qbind-node --test run_282_durable_completion_external_publication_audit_finalization_tests`
* `cargo test -p qbind-node --test run_280_durable_completion_external_publication_acknowledgement_tests`
* `cargo test -p qbind-node --test run_278_durable_completion_external_publication_receipt_tests`
* `cargo test -p qbind-node --test run_276_durable_completion_external_publication_confirmation_tests`
* `cargo test -p qbind-node --test run_274_durable_completion_settlement_outcome_publication_tests`
* `cargo test -p qbind-node --test run_272_durable_completion_settlement_outcome_report_tests`
* `cargo test -p qbind-node --test run_270_durable_completion_settlement_receipt_acknowledgement_tests`
* `cargo test -p qbind-node --test run_268_durable_completion_settlement_finalization_tests`
* `cargo test -p qbind-node --test run_266_durable_completion_settlement_commitment_tests`
* `cargo test -p qbind-node --test run_264_durable_completion_consumer_settlement_projection_tests`
* `cargo test -p qbind-node --test run_262_durable_completion_acknowledgement_consumer_tests`
* `cargo test -p qbind-node --test run_260_durable_completion_audit_receipt_acknowledgement_tests`
* `cargo test -p qbind-node --test run_258_durable_completion_audit_publication_receipt_tests`
* `cargo test -p qbind-node --test run_256_durable_completion_attestation_backend_tests`
* `cargo test -p qbind-node --test run_254_modeled_durable_completion_attestation_projection_tests`
* `cargo test -p qbind-node --test run_252_modeled_durable_completion_finalization_projection_tests`
* `cargo test -p qbind-node --test run_250_modeled_durable_consume_completion_reporter_tests`
* `cargo test -p qbind-node --test run_248_modeled_durable_consume_projection_sink_tests`
* `cargo test -p qbind-node --test run_246_governance_modeled_end_to_end_pipeline_tests`
* `cargo test -p qbind-node --test run_244_modeled_governance_trust_mutation_applier_tests`
* `cargo test -p qbind-node --test run_242_governance_execution_mutation_engine_tests`
* `cargo test -p qbind-node --test run_240_governance_evaluator_replay_durable_runtime_integration_tests`
* `cargo test -p qbind-node --test run_238_governance_evaluator_replay_durable_backend_tests`
* `cargo test -p qbind-node --test run_236_governance_evaluator_replay_consume_runtime_integration_tests`
* `cargo test -p qbind-node --test run_234_governance_evaluator_replay_consume_boundary_tests`
* `cargo test -p qbind-node --test run_232_governance_evaluator_replay_runtime_integration_tests`
* `cargo test -p qbind-node --test run_230_governance_evaluator_replay_state_tests`
* `cargo test -p qbind-node --test run_228_peer_evaluator_context_representation_tests`
* `cargo test -p qbind-node --test run_226_governance_evaluator_runtime_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_224_governance_evaluator_runtime_integration_tests`
* `cargo test -p qbind-node --test run_201_remote_signer_transport_tests` or actual Run 201 test target name
* `cargo test -p qbind-node --test run_194_remote_authority_signer_tests` or actual Run 194 test target name
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If the actual Run 194 / Run 201 test target names differ, use the real names and record the substitution in the evidence.

If running the full old corpus is too expensive, run the full required list unless there is a documented known pre-existing blocker. Do not silently skip older regression targets.

Security scanning:

* Run secret scanning over changed files and evidence scripts.
* Run CodeQL if available.
* Because Run 294 is release-binary evidence for a real RemoteSigner backend, CodeQL or an honest skip/timeout reason is required.
* If CodeQL is skipped, times out, reports database too large, tooling is unavailable, or otherwise does not complete, report that explicitly and do not claim CodeQL coverage.
* The canonical evidence file must include the exact CodeQL result or exact honest limitation directly.

Required docs:

`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_294.md` must include:

1. Exact verdict.
2. Files changed.
3. Release artifacts and hashes.
4. Helper corpus results.
5. Real-binary scenarios.
6. Request/response/transcript/domain binding release evidence.
7. Accepted release evidence.
8. Rejection/fail-closed release evidence.
9. MainNet refusal / authority policy release evidence.
10. Replay/recovery/idempotency release evidence.
11. Production-binary default-disabled/silent evidence.
12. C4/C5 matrix taxonomy verification.
13. Regression results.
14. Security scan results.
15. Honest limitations.
16. C4/C5 status.
17. Suggested Run 295 next step.

Docs must state clearly:

* Run 294 is release-binary evidence for the Run 293 production RemoteSigner backend.
* Run 294 does not add new production runtime wiring.
* Run 294 does not add a public CLI flag.
* Run 294 does not enable RemoteSigner by default.
* Run 294 does not enable MainNet.
* Run 294 does not implement KMS/HSM/cloud-KMS/PKCS#11 custody.
* Run 294 does not implement custody attestation.
* Run 294 does not implement on-chain governance proof verification.
* Run 294 does not implement governance execution engine.
* Run 294 does not implement validator-set rotation.
* Run 294 does not implement settlement or external publication.
* Run 294 does not call Run 070.
* Run 294 does not mutate `LivePqcTrustState`.
* Run 294 does not write trust-bundle sequence or authority marker files.
* Run 294 preserves Run 292 durable replay RocksDB Green-for-scope status.
* Full C4 remains OPEN.
* C5 remains OPEN.

Contradiction handling:

Update `docs/whitepaper/contradiction.md`.

Add a “Run 294” entry.

If no contradiction is found, state that Run 294 release-evidences the Run 293 production RemoteSigner backend while preserving default Disabled/fail-closed runtime posture, no CLI flag, no MainNet enablement, no KMS/HSM/custody-attestation/on-chain-verifier/governance-engine/validator-set-rotation implementation, no Run 070 call, no `LivePqcTrustState` mutation, no trust-bundle sequence or authority-marker writes, and Full C4 OPEN / C5 OPEN.

If contradiction is found, record the exact contradiction with file/function/doc reference and recommend the smallest safe fix.

Expected verdicts:

PASS / release-binary evidence positive only if:

* release `qbind-node` builds;
* release helper builds;
* harness exits 0;
* helper corpus passes;
* real-binary scenarios pass;
* release artifacts and hashes are captured;
* denylist is clean;
* reachability greps pass;
* C4/C5 taxonomy greps pass;
* Run 293 source tests still pass;
* required regression corpus passes;
* secret scan is clean;
* CodeQL result or honest skip/timeout is reported directly in the canonical evidence;
* docs/evidence/contradiction are updated;
* Full C4 remains OPEN;
* C5 remains OPEN.

Partial-positive only if:

* release helper proves the backend in release mode but one bounded, non-critical scenario is incomplete and documented honestly;
* no false closure claim is made.

Blocked only if:

* the Run 293 backend cannot be linked/exercised in release mode without redesign;
* document the exact blocker and smallest safe next step.

Unacceptable:

* claiming C4 closure;
* claiming C5 closure;
* claiming MainNet readiness;
* claiming runtime default enablement;
* adding hidden production behavior without evidence;
* adding a public CLI flag;
* silently falling back to fixture/loopback/local signing;
* using raw local production signing keys;
* skipping request/response/transcript/domain-binding release checks;
* skipping timeout/unavailable/malformed/replay/wrong-domain release checks;
* hiding CodeQL failure/timeout/skip;
* hiding regression failures.

Suggested Run 295 next step after successful Run 294:

Begin the next Red-row closure campaign.

Preferred next target:

Run 295 — source/test production KMS/HSM/cloud-KMS/PKCS#11 custody backend implementation, or source/test production custody attestation verifier implementation.

Choose KMS/HSM custody first if the team wants direct custody provider integration. Choose custody attestation first if the team wants to establish proof/attestation verification before provider-specific signing calls. In either case keep the same pattern: real backend implementation at source/test level, default Disabled/fail-closed, MainNet refused unless production authority criteria are satisfied, release-binary evidence deferred to Run 296.

Required final response format:

1. Exact verdict.
2. Files changed.
3. Release artifacts and hashes.
4. Helper corpus results.
5. Real-binary scenarios.
6. Request/response/transcript/domain binding release evidence.
7. Accepted release evidence.
8. Rejection/fail-closed release evidence.
9. MainNet refusal / authority policy release evidence.
10. Replay/recovery/idempotency release evidence.
11. Production-binary default-disabled/silent evidence.
12. Tests run and results.
13. C4/C5 matrix taxonomy status.
14. Security invariants preserved.
15. Honest limitations.
16. C4/C5 status.
17. Suggested Run 295 next step.
