Continue QBIND from the current state.

Project:
- QBIND post-quantum L1
- security-first, production-grade, execution-first
- C4 is the main live operability tracker
- C5 tracks timeout-verification activation, forged-traffic rejection, and transport-root dependency
- no broad redesign
- no silent regressions
- no hype / no overclaiming

Current state:
- Runs 037–045 hardened PQC transport.
- Runs 046–049 completed live timeout-backoff evidence through the 800-tick cap.
- Runs 050–068 completed the signed trust-bundle lifecycle path:
  - structured PQC trust bundles
  - signed ML-DSA-44 bundle verification
  - chain_id crosscheck
  - sequence anti-rollback persistence
  - activation-height gating
  - minimum activation-height policy
  - root/leaf revocation
  - per-entry revocation activation_height
  - local revoked-leaf startup self-check
  - local revoked issuer-root startup self-check
  - DevNet/TestNet/MainNet evidence
  - N=4 MainNet signed-bundle peer-connection evidence
- Run 069 landed validation-only local reload-check.
- Run 070 landed reload-apply contract and rollback semantics.
- Run 071 introduced LivePqcTrustState and live snapshot reads.
- Run 072 added production P2P session eviction.
- Run 073 landed ProductionLiveTrustApplyContext for process-start local reload-apply.
- Run 074 landed long-running SIGHUP local live reload-apply.
- Run 075 updated the operator runbook for Runs 069–074.
- Run 076 partial-positive:
  - added library-level disabled-by-default peer-candidate validation
  - new pqc_trust_peer_candidate module
  - PeerCandidateEnvelope with strict pre-checks
  - MAX_PEER_CANDIDATE_BUNDLE_BYTES = 256 KiB
  - bounded duplicate LRU
  - fixed-window rate limiter
  - seven peer-candidate metrics
  - valid candidate validates but is NOT applied
  - invalid candidates fail closed
  - sequence file remains unchanged
  - LivePqcTrustState is unreachable from the validator
  - no session eviction
  - no propagation
  - no _applied_total metric family
  - no CLI flag
  - no wire integration
  - no production binary caller
- C4 remains OPEN.
- C5 remains OPEN / narrowed.

Current remaining C4 trust/lifecycle blockers include:
- production binary wire integration for peer-candidate validation
- peer-driven live apply / propagation
- activation_epoch runtime source
- in-binary / on-chain bundle-signing-key ratification
- external KMS/HSM custody integration
- production fast-sync / consensus-storage restore
- per-environment production trust-anchor operation
- selective per-peer session retention if ever wanted
- admin-API or filesystem-watcher triggers if ever desired

Task:
Run 077. Add the first production-binary-facing, disabled-by-default peer-candidate validation surface, without live apply and without propagation.

Primary objective:
Wire the Run 076 PeerCandidateValidator into a controlled qbind-node binary surface so adversarial peer-candidate envelopes can be exercised through the release binary, while preserving the validation-only boundary:
- no LivePqcTrustState mutation
- no sequence persistence
- no session eviction
- no propagation
- no live apply

Important framing:
This is still NOT peer-driven live apply.
This is still NOT gossip propagation.
This is NOT automatic trust-bundle synchronization.
This is NOT consensus ratification.
This is NOT activation_epoch.
This is NOT KMS/HSM.
This is NOT signing-key ratification.
This is NOT fast-sync restore.
Do not redesign KEMTLS.
Do not redesign consensus.
Do not weaken local operator reload-check.
Do not weaken local SIGHUP live reload.
Do not silently fall back to --p2p-trusted-root.
Do not silently fall back to DummySig, DummyKem, or DummyAead.
Do not claim full C4 or C5 closure.

Strict scope:
- Add a disabled-by-default production binary surface for peer-candidate validation.
- Prefer the safest bounded local/operator test surface before true P2P gossip:
  - a hidden local-only CLI/check mode is acceptable, OR
  - a hidden localhost-only test endpoint is acceptable only if already aligned with project style, OR
  - a bounded P2P envelope receive path is acceptable only if an existing safe typed message channel exists.
- If no safe wire channel exists, do not fake one.
- The candidate path must call Run 076 PeerCandidateValidator.
- The path must not call ProductionLiveTrustApplyContext.
- The path must not call LiveReloadController.
- The path must not call LivePqcTrustState::swap_snapshot.
- The path must not call P2pSessionEvictor.
- The path must not call check_and_update_sequence.
- The path must not expose peer/gossiped live apply.
- The path must be disabled by default.
- The path must have top-level partial-config refusal if flags are incomplete.
- Crosscheck all code against existing design/spec. If contradictions or inconsistencies are found, update docs/whitepaper/contradiction.md.

Recommended safest design:
Add a hidden local binary validation mode first, for example:
- --p2p-trust-bundle-peer-candidate-check <ENVELOPE_PATH>
- --p2p-trust-bundle-peer-candidate-validation-enabled

This mode should:
- parse a local PeerCandidateEnvelope JSON or binary test fixture
- run PeerCandidateValidator::try_accept
- print/log the same validated/rejected/disabled/oversize/rate-limited/duplicate outcome
- exit 0 only for Validated
- exit 1 for Rejected/Oversize/RateLimited/Disabled/partial-config
- never start P2P
- never apply
- never persist sequence
- never evict sessions

If the project already has a safe P2P wire envelope:
- implement only disabled-by-default receive + validate + metrics
- no rebroadcast
- no apply
- no sequence write
- no session eviction

Required investigation:

1. Inspect Run 076 implementation
   Inspect:
   - crates/qbind-node/src/pqc_trust_peer_candidate.rs
   - crates/qbind-node/src/metrics.rs Run 076 counters
   - crates/qbind-node/tests/run_076_pqc_peer_candidate_validation_tests.rs
   - docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_076.md
   - docs/whitepaper/contradiction.md

2. Inspect binary and P2P surfaces
   Inspect:
   - crates/qbind-node/src/cli.rs
   - crates/qbind-node/src/main.rs
   - crates/qbind-node/src/p2p_node_builder.rs
   - crates/qbind-node/src/p2p_tcp.rs
   - qbind-net message/framing types
   - any hidden/devnet injection patterns
   - Run 069 reload-check CLI style
   - Run 074 live-reload CLI style

   Determine:
   - whether a hidden local check mode is safer than wire integration
   - whether a safe typed P2P message channel exists today
   - where partial-config validation should live
   - how to feed environment / chain_id / signing keys / sequence path / activation context into PeerCandidateValidator

3. Define binary-surface semantics
   Required:
   - disabled by default.
   - explicit flag required.
   - if local check mode:
     - file path required.
     - no network start.
   - if P2P receive path:
     - explicit enable flag required.
     - bounded payload.
     - rate-limit active.
     - no rebroadcast.
     - no apply.
   - every outcome must be logged with safe metadata only.
   - no private keys in logs.
   - no candidate bytes dumped to logs.
   - no sequence burn.

4. Define state-mutation invariants
   Required:
   - valid candidate:
     - no LivePqcTrustState change
     - no sequence file change
     - no session eviction
     - no propagation
   - invalid candidate:
     - same no-mutation guarantees
   - disabled path:
     - no payload touch / no crypto / no scratch file
   - oversize path:
     - rejected before signature verification
   - duplicate path:
     - suppressed without repeated expensive verification
   - rate-limited path:
     - bounded and no scratch write if possible

5. Define metrics/logs
   Reuse Run 076 metric counters:
   - qbind_p2p_pqc_trust_bundle_peer_candidate_received_total
   - qbind_p2p_pqc_trust_bundle_peer_candidate_validated_total
   - qbind_p2p_pqc_trust_bundle_peer_candidate_rejected_total
   - qbind_p2p_pqc_trust_bundle_peer_candidate_dropped_oversize_total
   - qbind_p2p_pqc_trust_bundle_peer_candidate_rate_limited_total
   - qbind_p2p_pqc_trust_bundle_peer_candidate_duplicate_total
   - qbind_p2p_pqc_trust_bundle_peer_candidate_disabled_total

   Requirements:
   - no _applied_total family.
   - local reload-check metrics must not move.
   - live-reload apply metrics must not move.
   - session eviction metrics must not move.
   - no duplicate metric families.
   - logs must state validation-only / not applied.

Required implementation:

1. Add binary surface
   Add hidden CLI/config flags or safe P2P disabled receive hook as chosen during investigation.

   If local CLI check mode:
   - implement parser for PeerCandidateEnvelope fixture.
   - ensure path is local.
   - run validator.
   - emit canonical outcome line.
   - exit with deterministic code.
   - do not start node.

   If P2P receive hook:
   - feature-gated / flag-gated.
   - bounded message handling.
   - call validator.
   - no apply/propagation.
   - release-binary evidence must prove no state mutation.

2. Preserve all existing paths
   - Run 069 reload-check unchanged.
   - Run 073 process-start apply unchanged.
   - Run 074 SIGHUP live reload unchanged.
   - normal startup unchanged.
   - peer candidate path cannot trigger local live apply.

3. Tests
   Add tests proving:
   - disabled-by-default path refuses or no-ops.
   - partial-config fails closed.
   - valid envelope validates and does not mutate sequence.
   - invalid envelope rejects and does not mutate sequence.
   - oversize envelope rejects before expensive validation.
   - duplicate suppression works through the binary surface if applicable.
   - rate limit works through the binary surface if applicable.
   - no _applied_total metric appears.
   - local reload-check/live reload metrics remain unchanged.
   - no session eviction metrics move.

Required regression commands:
- cargo test -p qbind-node --lib pqc_trust_peer_candidate
- new Run 077 binary peer-candidate tests
- cargo test -p qbind-node --test run_076_pqc_peer_candidate_validation_tests
- cargo test -p qbind-node --test run_069_pqc_trust_bundle_reload_check_tests
- cargo test -p qbind-node --test run_073_pqc_trust_bundle_reload_apply_runtime_tests
- cargo test -p qbind-node --test run_074_pqc_trust_bundle_live_reload_tests
- cargo test -p qbind-node --lib metrics
- cargo test -p qbind-node --lib p2p
- cargo test -p qbind-node --lib
- cargo test -p qbind-net --lib
- cargo test -p qbind-crypto --lib
- cargo build --release -p qbind-node --bin qbind-node
- cargo build --release -p qbind-node --example devnet_pqc_trust_bundle_helper
- cargo build --release -p qbind-node --example devnet_pqc_root_helper

If source touches P2P wire/message handling, also run:
- cargo test -p qbind-node --test run_037_pqc_static_root_mutual_auth_tests
- cargo test -p qbind-node --test run_040_pqc_static_root_real_aead_tests
- cargo test -p qbind-net --test run_052_leaf_revocation_handshake_tests

If source touches timeout verification or binary consensus loop, also run:
- cargo test -p qbind-node --lib binary_consensus
- cargo test -p qbind-node --lib forged_injection
- cargo test -p qbind-node --lib run030
- cargo test -p qbind-consensus --lib timeout

Release-binary evidence:

1. Disabled-by-default baseline
   - run release binary without peer-candidate flag.
   - prove no peer-candidate path active.
   - normal startup/reload-check behavior unchanged if applicable.

2. Valid peer-candidate envelope check
   - construct valid PeerCandidateEnvelope fixture.
   - run release binary candidate-check path.
   - prove outcome Validated.
   - prove sequence file unchanged.
   - prove no live apply metrics move.
   - prove no session eviction metrics move.
   - prove logs say NOT applied / not propagated / sequence not persisted / sessions untouched.

3. Invalid peer-candidate envelope check
   - tampered or wrong-chain candidate.
   - prove Rejected.
   - prove sequence unchanged.
   - prove no live apply.
   - prove no eviction.

4. Oversize envelope check
   - prove dropped before crypto.
   - prove correct metric/log outcome.

5. Partial-config fail-closed
   - missing enable flag / missing path / incompatible flags if applicable.
   - prove deterministic refusal.

Evidence document:
Create:
- docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_077.md

Evidence document must include:
- exact objective
- exact verdict
- exact files changed
- exact commands run
- tests/evidence run and pass/fail status
- investigation findings with file/function references
- chosen binary surface and why
- disabled-by-default behavior
- validation-only semantics
- state non-mutation proof
- sequence non-burn proof
- metrics/logging behavior
- proof local reload paths unchanged
- release-binary evidence
- proof no fallback to --p2p-trusted-root
- proof no DummySig/DummyKem/DummyAead fallback
- exact boundaries:
  - no peer-driven live apply
  - no gossip propagation
  - no automatic trust-bundle synchronization
  - activation_epoch still absent
  - KMS/HSM still absent
  - signing-key ratification still absent
- exact immediate next action

contradiction.md update rules:
- If production binary-facing peer-candidate validation lands, update C4 to record peer/gossiped trust-bundle acceptance narrowed from library-only to disabled binary validation surface.
- If only local CLI check lands and not true wire integration, state that boundary clearly.
- Keep C4 open for:
  - true P2P wire/gossip integration if not landed
  - peer-driven live apply / propagation
  - activation_epoch runtime source
  - in-binary/on-chain bundle-signing-key ratification
  - KMS/HSM custody
  - production fast-sync / consensus-storage restore
  - per-environment production trust-anchor operation
- Do not close full C4.
- Do not claim C5 closure.
- If peer candidate mutates live trust state or burns sequence, record contradiction/regression.
- If invalid peer candidate evicts sessions, record contradiction/regression.
- If peer candidate bypasses signed-bundle validation, record contradiction/regression.
- If failure falls back to static roots or Dummy crypto, record contradiction/regression.

Expected verdict options:
- Strongest positive:
  disabled-by-default production binary peer-candidate validation surface lands; valid candidate validates but is not applied; invalid/oversize/disabled candidates fail closed; sequence, live trust state, sessions, and apply metrics remain unchanged; no fallback; no Dummy crypto; release-binary evidence passes.
- Partial positive:
  binary surface lands only as local check mode, not true P2P wire integration. State exact boundary.
- Negative:
  peer candidate mutates state, burns sequence, bypasses validation, evicts sessions, triggers apply, causes fallback/Dummy crypto, or regressions fail.

Required final response:
1. Exact verdict
2. Exact files changed
3. Exact commands run
4. Tests/evidence run and pass/fail status
5. What was fixed
6. What was proven
7. What remains not solved
8. Whether contradiction.md was updated and why
9. Exact immediate next action recommended