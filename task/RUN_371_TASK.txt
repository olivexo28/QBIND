Run 371 — KEMTLS-admitted deployed per-peer message-rate live-socket flood evidence

Continue QBIND after accepted Run 370.

Current state:
Run 370 is accepted as PARTIAL-POSITIVE / public-DevNet abuse/DoS M12 deployed live-socket release-binary strengthened.

Run 370 proved:
- connection-rate control live-socket on real `target/release/qbind-node`;
- 10 inbound TCP connections with max 3 produced 3 accepted / 7 refused;
- live `qbind_p2p_connection_rate_drop_total = 7`;
- Route B source change threaded `Arc<NodeMetrics>` into the deployed `DeployedInboundPerPeerLimiter`;
- deployed adapter object now exports `qbind_net_per_peer_drops_total{reason="rate_limit"}`;
- helper 10/10 scenarios passed;
- run_370 source tests 20 passed;
- hidden abuse/DoS flags stayed hidden;
- invalid / unbounded / MainNet configs fail closed;
- no public CLI flag added;
- no P2P wire-format change;
- no KEMTLS / trust-bundle / peer-admission weakening;
- no `LivePqcTrustState`, validator-set, epoch, sequence, marker, or authority mutation;
- secret scan clean;
- CodeQL was attempted but skipped due to database size and not claimed clean.

Run 370 did NOT move M12 Green because:
- the fully-live KEMTLS-admitted deployed per-peer socket flood was not driven;
- the deployed per-peer proof exercised the exact deployed adapter object through the release helper, not bytes from a second mutually-authenticated peer through the deployed `TcpKemTlsP2pService::read_loop`;
- M12 Green requires both controls over deployed live sockets.

Current readiness:
- M12 = Yellow/Partial, strengthened.
- M4 = Yellow/launch-blocking.
- M6 = Yellow/Partial.
- Public DevNet = NOT launch-ready.
- Full C4 OPEN.
- C5 OPEN.
- MainNet authority rotation/revocation Red.

Primary objective:
Produce release-binary live-socket evidence that a second KEMTLS-admitted peer can complete mutual auth against the deployed `target/release/qbind-node`, flood over-budget frames through the deployed `TcpKemTlsP2pService::read_loop`, and cause live `/metrics` to expose `qbind_net_per_peer_drops_total{reason="rate_limit"}` increments from the Run 369/370 deployed per-peer limiter.

If this succeeds while preserving Run 367/370 connection-rate evidence, evaluate whether M12 can move Yellow/Partial → Green.

Strict scope:
- Release-binary live-socket evidence for M12 only.
- Helper / harness / evidence / docs preferred.
- Add production source only if the investigation proves a tiny, default-preserving observability or harness unblocker is required.
- No public DevNet launch.
- No live external seed/bootnode deployment.
- No M4 Green.
- No M6 Green.
- No faucet.
- No RPC gateway.
- No explorer.
- No status page.
- No TestNet readiness claim.
- No MainNet readiness claim.
- No C4 closure.
- No C5 closure.
- No authority-lifecycle runtime wiring.
- No validator-set mutation.
- No epoch transition.
- No sequence/marker write.
- No `LivePqcTrustState` mutation.
- No P2P wire-format change.
- No peer-admission weakening.
- No KEMTLS/trust-bundle weakening.
- No new public CLI flags.
- Hidden/devnet-only abuse/DoS flags must remain hidden from `--help`.
- Generated certs, keys, trust bundles, node data dirs, and logs must be temporary and gitignored.

Required investigation:
Inspect before implementation:

- `crates/qbind-node/src/p2p_tcp.rs`
- `crates/qbind-node/src/p2p_node_builder.rs`
- `crates/qbind-node/src/deployed_inbound_per_peer_limiter.rs`
- `crates/qbind-node/src/public_devnet_abuse_dos_runtime.rs`
- `crates/qbind-node/src/public_devnet_abuse_dos_config.rs`
- `crates/qbind-node/src/peer_rate_limiter.rs`
- `crates/qbind-node/src/metrics.rs`
- `crates/qbind-node/src/metrics_http.rs`
- `crates/qbind-node/src/pqc_root_config.rs`
- `crates/qbind-node/src/pqc_trust_bundle.rs`
- `crates/qbind-node/examples/devnet_pqc_root_helper.rs`
- `crates/qbind-node/examples/devnet_pqc_trust_bundle_helper.rs`
- existing KEMTLS / P2P release harnesses from Runs 037–041, 084–089, 367–370 if present
- `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_370.md`
- `docs/release/QBIND_PUBLIC_DEVNET_READINESS_CRITERIA.md`
- `docs/release/public-devnet/p2p/ABUSE_DOS_POSTURE.md`
- `docs/release/public-devnet/p2p/VERIFY.md`

Decision gate:
Record the chosen route before implementation.

Route A — Existing release binary can run a two-node KEMTLS mutual-auth loopback flood with no production source change.
- Build helper/harness only.
- Generate all node material in temp dirs.
- Drive the deployed live socket and scrape live metrics.

Route B — A tiny default-preserving source change is required to expose a test-only or devnet-only harness seam.
- Make the smallest possible source change.
- No public CLI flag.
- No wire-format change.
- No admission/trust weakening.
- Add source tests.
- Then produce release-binary evidence.

Route C — Fully live KEMTLS-admitted flood remains blocked.
- Do not mark M12 Green.
- Record the exact blocker.
- Produce the strongest partial evidence available.
- Suggest the next precise run.

Expected artifacts:
Add release helper:

`crates/qbind-node/examples/run_371_public_devnet_m12_kemtls_per_peer_live_socket_flood_helper.rs`

Add release harness:

`scripts/devnet/run_371_public_devnet_m12_kemtls_per_peer_live_socket_flood_release_binary.sh`

Add archive:

`docs/devnet/run_371_public_devnet_m12_kemtls_per_peer_live_socket_flood_release_binary/README.md`
`docs/devnet/run_371_public_devnet_m12_kemtls_per_peer_live_socket_flood_release_binary/summary.txt`
`docs/devnet/run_371_public_devnet_m12_kemtls_per_peer_live_socket_flood_release_binary/.gitignore`

Only those three archive files should be tracked. Raw logs, generated certs, generated keys, trust bundles, node data dirs, metrics dumps, helper output, and temp files must be gitignored.

Add canonical evidence:

`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_371.md`

If source changes are needed, add:

`crates/qbind-node/tests/run_371_public_devnet_m12_kemtls_live_socket_tests.rs`

Required live-socket evidence:
The Run 371 harness must prove on real release artifacts:

1. `cargo build -p qbind-node --release` succeeds.
2. Run 371 helper builds in release mode.
3. qbind-node SHA-256 captured.
4. helper SHA-256 captured.
5. BuildIDs captured where available.
6. rustc/cargo versions captured.
7. hidden abuse/DoS flags remain absent from `--help`.
8. real hidden flags parse.
9. invented flags rejected by clap.
10. invalid zero per-peer max fails closed.
11. invalid zero connection-rate max fails closed.
12. unsafe/unbounded values fail closed.
13. MainNet abuse/DoS enablement refused.
14. default no-flag behavior preserves:
    - connection limiter disabled;
    - per-peer `1000` msg/s + `100` burst;
    - no unexpected drops.
15. real P2P-capable loopback node starts with:
    - `--network-mode p2p`;
    - `--enable-p2p`;
    - `--p2p-listen-addr 127.0.0.1:<port>`;
    - loopback metrics endpoint enabled.
16. second peer completes KEMTLS mutual-auth against the deployed node.
17. second peer is admitted far enough that frames reach the deployed `TcpKemTlsP2pService::read_loop`.
18. custom `--p2p-max-messages-per-second` / `--p2p-burst-allowance` are active on the deployed receive path.
19. under-budget frames over the admitted KEMTLS socket produce no per-peer drops.
20. over-budget frames over the admitted KEMTLS socket are dropped by the deployed per-peer limiter.
21. live `/metrics` shows `qbind_net_per_peer_drops_total{reason="rate_limit"}` incrementing.
22. connection is not torn down solely because of per-peer drops unless pre-existing semantics require it.
23. per-peer drops do not increment `qbind_p2p_connection_rate_drop_total`.
24. connection-rate refusals do not increment per-peer drops.
25. Run 367/370 connection-rate live-socket proof still passes in the same harness:
    - 10 inbound TCP connections;
    - max 3;
    - 3 accepted / 7 refused;
    - `qbind_p2p_connection_rate_drop_total = 7`.
26. no public launch claim.
27. no M4/M6 Green claim.
28. no TestNet/MainNet readiness claim.
29. no C4/C5 closure claim.
30. no P2P wire-format change.
31. no peer-admission weakening.
32. no trust-bundle behavior weakening.
33. no `LivePqcTrustState` mutation.
34. no sequence/marker write.
35. no validator-set mutation.
36. no epoch transition.
37. no Run 070 path.
38. no committed secrets or live endpoints.

Required scenarios:
At minimum:

1. `default_preserves_behavior`
2. `hidden_cli_surface_checked`
3. `invalid_configs_fail_closed`
4. `mainnet_refused`
5. `connection_rate_live_socket_regression`
6. `kemtls_second_peer_admitted`
7. `per_peer_kemtls_under_budget`
8. `per_peer_kemtls_over_budget`
9. `metrics_export_live`
10. `combined_limiter_independence`
11. `non_mutation_guards`

Tests to run and record:
If source changed:

- `cargo build -p qbind-node --lib`
- `cargo test -p qbind-node --test run_371_public_devnet_m12_kemtls_live_socket_tests`

Always run:

- `cargo build -p qbind-node --release`
- `cargo build -p qbind-node --release --example run_371_public_devnet_m12_kemtls_per_peer_live_socket_flood_helper`
- Run the Run 371 harness.
- `cargo test -p qbind-node --test run_370_public_devnet_deployed_live_socket_m12_tests`
- `cargo test -p qbind-node --test run_369_public_devnet_deployed_per_peer_limiter_wiring_tests`
- `cargo test -p qbind-node --test run_365_public_devnet_deployed_peer_rate_threading_tests`
- `cargo test -p qbind-node --test run_363_public_devnet_per_peer_message_rate_runtime_tests`
- `cargo test -p qbind-node --test run_362_public_devnet_abuse_dos_runtime_tests`
- `cargo test -p qbind-node --test run_361_public_devnet_abuse_dos_hardening_tests`
- `cargo test -p qbind-node --lib`

Docs to update narrowly:
- `docs/release/QBIND_PUBLIC_DEVNET_READINESS_CRITERIA.md`
- `docs/release/public-devnet/p2p/ABUSE_DOS_POSTURE.md`
- `docs/release/public-devnet/p2p/VERIFY.md`
- `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
- `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
- `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
- `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
- `docs/whitepaper/contradiction.md`

Readiness matrix rules:
- M12 may move Yellow/Partial → Green only if Run 371 proves BOTH:
  1. connection-rate limiting over deployed live sockets;
  2. per-peer message-rate limiting over a KEMTLS-admitted deployed live socket with live `/metrics` evidence.
- If per-peer evidence is adapter-only, helper-only, plain-TCP-only, construction-path-only, source/test-only, or lacks live deployed `/metrics` proof, keep M12 Yellow/Partial.
- Do not alter M4 unless live seed/bootnode reachability evidence lands.
- Do not alter M6 unless stable identity-generation support lands.
- Do not alter M7–M9/M13–M15 unless genuinely closed.
- Public DevNet remains NOT launch-ready unless all must-haves are Green.

Security:
Run secret scanning over all changed files.

No private keys, mnemonics, credentials, API keys, tokens, private infrastructure, real production hostnames, or unapproved live endpoints may be committed. Generated KEMTLS material must be temporary, dev-only, and gitignored. Use loopback, RFC 5737, RFC 3849, and temp dirs only.

CodeQL:
If any Rust source or example is added/changed, invoke CodeQL if available.

If skipped, unavailable, timed out, database-too-large, or not meaningful, record the exact result and do not call it clean.

Expected verdict:
PASS / public-DevNet abuse/DoS M12 KEMTLS live-socket release-binary positive if:
- real release `qbind-node` accepts a KEMTLS-authenticated second peer;
- over-budget frames from that peer traverse the deployed receive path;
- per-peer drops are exported on live `/metrics`;
- connection-rate live-socket proof is preserved;
- defaults, hidden CLI posture, fail-closed configs, non-mutation, and all regressions pass.

Partial-positive if evidence improves but the KEMTLS-admitted deployed socket flood is still not fully driven or live metrics are not observed.

Unacceptable:
- marking M12 Green without both deployed live-socket controls proven;
- marking M4 Green without live seed/bootnode reachability;
- marking M6 Green without identity-generation support;
- exposing hidden abuse/DoS flags publicly;
- changing P2P wire format;
- weakening peer admission, KEMTLS, trust-bundle, genesis binding, or mutual-auth;
- silently changing defaults;
- incrementing connection-rate metric for per-peer message drops;
- committing generated private keys or trust material;
- claiming public DevNet launch-ready;
- claiming TestNet/MainNet readiness;
- closing C4 or C5;
- hiding skipped CodeQL.

Final response format:
1. Exact verdict.
2. Files changed.
3. Release artifacts and hashes.
4. Decision gate route.
5. KEMTLS harness shape.
6. Connection-rate live-socket evidence.
7. KEMTLS-admitted per-peer live-socket evidence.
8. Metrics/counter evidence.
9. CLI/config surface.
10. Default compatibility.
11. Rejection/fail-closed evidence.
12. Combined limiter independence.
13. Non-mutation evidence.
14. Readiness matrix delta for M12.
15. Current public DevNet readiness status.
16. Remaining public DevNet blockers.
17. Public TestNet blockers.
18. MainNet blockers.
19. C4/C5 status.
20. Tests run.
21. Security scans.
22. CodeQL.
23. Provenance.
24. Honest limitations.
25. Suggested Run 372 next step.