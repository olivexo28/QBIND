Continue QBIND from the current state.

Project:
- QBIND post-quantum L1
- security-first, production-grade, execution-first
- C4 is the main live operability tracker
- C5 tracks timeout-verification activation, forged-traffic rejection, and transport-root dependency
- no broad redesign
- no silent regressions
- no overclaiming

Current state:
- Runs 050–068 completed signed PQC trust-bundle lifecycle.
- Runs 069–075 completed local operator reload lifecycle.
- Runs 076–086 completed peer-candidate 0x05 validation-only lifecycle.
- Run 087 added peer-driven propagation/apply safety spec.
- Run 088 implemented disabled-by-default validation-before-rebroadcast propagation prototype.
- Run 089 produced strongest-positive N=3 DevNet release-binary propagation evidence.
- Run 090 updated the operator runbook for propagation-only behavior.
- Run 091 made activation_epoch explicitly fail-closed when no canonical runtime epoch source exists.
- Run 092 partial-positive/doc-only:
  - production qbind-node does not currently open RocksDbConsensusStorage.
  - grep -rn "RocksDbConsensusStorage::open" crates/qbind-node/src/**.rs found only doc-comments in storage.rs.
  - real call sites are under crates/qbind-node/tests/** or hotstuff_node_sim::EpochAwareNodeHotstuffHarness.
  - production binary never writes meta:current_epoch.
  - therefore MetaStore current_epoch is not available on the binary path.
  - Run 091 fail-closed CurrentEpochUnavailable boundary remains unchanged.
- C4 remains OPEN.
- C5 remains OPEN / narrowed.

Current open C4 items include:
- canonical production ConsensusStorage / current_epoch wiring
- activation_epoch real runtime source
- snapshot/rejoin epoch parity
- peer-driven live apply
- KMS/HSM custody
- in-binary / on-chain bundle-signing-key ratification
- production fast-sync / consensus-storage restore
- per-environment production trust-anchor operation

Task:
Run 093. Establish the minimum production binary-path ConsensusStorage lifecycle and durable epoch persistence needed before activation_epoch can be safely consumed.

Primary objective:
Make production qbind-node own and open a canonical ConsensusStorage instance on the real binary path, and persist current_epoch through the existing consensus-storage/MetaStore mechanism, without yet consuming current_epoch for PQC trust-bundle activation.

Important framing:
This is storage lifecycle groundwork.
This is NOT activation_epoch consumption.
This is NOT trust-bundle activation acceptance.
This is NOT epoch-transition redesign.
This is NOT consensus redesign.
This is NOT full fast-sync restore.
This is NOT snapshot redesign, except documenting required future parity.
This is NOT peer-driven live apply.
This is NOT KMS/HSM.
This is NOT signing-key ratification.
Do not invent a synthetic epoch.
Do not derive epoch from wall-clock time.
Do not derive epoch from block height unless the repo already defines that mapping as canonical.
Do not silently treat missing epoch as current_epoch = 0.
Do not weaken Run 091/092 CurrentEpochUnavailable fail-closed behavior.
Do not claim full C4 or C5 closure.

Strict scope:
- Define a canonical production consensus-storage location, preferably:
  <data_dir>/consensus
- Open RocksDbConsensusStorage on the production qbind-node binary path.
- Run schema compatibility / recovery checks before consensus starts.
- Ensure the production binary can persist meta:current_epoch through the existing MetaStore / apply_epoch_transition_atomic path if such path already exists.
- Add an explicit distinction between:
  - NoConsensusStorage
  - ConsensusStoragePresentButNoCommittedEpoch
  - CommittedEpoch(u64)
- Fresh genesis must not silently satisfy activation_epoch=0.
- Run 091 fail-closed activation_epoch behavior must remain unchanged.
- Trust-bundle ActivationContext should still receive current_epoch=None unless and until a later run explicitly consumes the canonical epoch source.
- Crosscheck all code/docs against existing design/spec. If contradictions are found, update contradiction.md.

Required investigation:
Inspect:
- RocksDbConsensusStorage::open
- MetaStore::get_current_epoch
- MetaStore current_epoch write path
- apply_epoch_transition_atomic
- storage initialization order in qbind-node main
- NodeConfig data-dir handling
- snapshot restore path
- StateSnapshotMeta
- consensus loop / HotStuff binary path
- tests or harnesses using EpochAwareNodeHotstuffHarness
- docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_091.md
- docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_092.md
- docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md
- docs/protocol/QBIND_PEER_TRUST_BUNDLE_PROPAGATION_SAFETY.md
- docs/whitepaper/contradiction.md

Document exact findings in Run 093 evidence.

Required design:
1. Canonical storage path
   Define deterministic path:
   <data_dir>/consensus

   Requirements:
   - no temp fallback
   - no environment-dependent hidden path
   - fail closed on open/corruption errors
   - documented in evidence/runbook if operator-visible

2. Production binary open
   qbind-node production path must call RocksDbConsensusStorage::open before consensus activity begins.

3. Epoch persistence
   If existing production consensus loop has epoch-transition event points:
   - wire them into apply_epoch_transition_atomic or existing persistence surface.
   - do not invent new epoch-transition rules.
   - do not create synthetic epochs.

   If no production epoch-transition event exists:
   - open storage and document that no binary-path epoch write occurs until consensus epoch transition is wired.
   - do not fake epoch writes.

4. Startup state distinction
   Implement/read/report:
   - storage unavailable
   - storage present but no committed epoch
   - committed epoch exists

   Missing epoch must not be treated as epoch 0 for trust-bundle activation.

5. Trust-bundle behavior preservation
   Run 091/092 CurrentEpochUnavailable must remain unchanged.
   activation_epoch present must still fail closed until a later run explicitly consumes the canonical source.

6. Snapshot/restore boundary
   If StateSnapshotMeta lacks epoch:
   - do not add it unless small and explicitly safe.
   - document snapshot epoch parity as open.
   - do not fake restore epoch from height.

Required tests:
- production NodeConfig resolves canonical consensus storage path.
- RocksDbConsensusStorage opens at <data_dir>/consensus.
- open failure/corruption fails closed.
- storage-present/no-committed-epoch is distinguishable from committed epoch.
- committed epoch persists across restart.
- incomplete epoch-transition recovery works if existing API supports it.
- fresh genesis does not silently become current_epoch=0 for trust-bundle activation.
- activation_epoch present still fails closed with CurrentEpochUnavailable in trust-bundle paths.
- activation_epoch omitted behavior unchanged.
- existing Run 091 tests still pass.
- no sequence burn occurs for activation_epoch rejection.
- reload-check / reload-apply / SIGHUP / peer-candidate behavior unchanged.

Required regression commands:
- cargo test -p qbind-node --test run_091_pqc_trust_bundle_activation_epoch_tests
- cargo test -p qbind-node --lib pqc_trust_activation
- cargo test -p qbind-node --test run_057_pqc_trust_bundle_activation_tests
- cargo test -p qbind-node --test run_065_pqc_min_activation_margin_tests
- cargo test -p qbind-node --test run_069_pqc_trust_bundle_reload_check_tests
- cargo test -p qbind-node --test run_073_pqc_trust_bundle_reload_apply_runtime_tests
- cargo test -p qbind-node --test run_074_pqc_trust_bundle_live_reload_tests
- cargo test -p qbind-node --test run_076_pqc_peer_candidate_validation_tests
- cargo test -p qbind-node --test run_088_pqc_peer_candidate_propagation_tests
- cargo test -p qbind-node --lib metrics
- cargo test -p qbind-node --lib
- cargo test -p qbind-net --lib
- cargo test -p qbind-crypto --lib
- cargo build --release -p qbind-node --bin qbind-node
- cargo build --release -p qbind-node --example devnet_pqc_trust_bundle_helper
- cargo build --release -p qbind-node --example devnet_pqc_root_helper

If startup/storage ordering changes touch snapshot or persistence behavior:
- run relevant snapshot/restore tests.
- document exact command names.

If P2P or wire paths are touched:
- run scripts/devnet/run_084_peer_candidate_0x05_matrix.sh
- run scripts/devnet/run_085_mainnet_peer_candidate_0x05_matrix.sh
- run scripts/devnet/run_089_peer_candidate_propagation_n3.sh

Release-binary evidence:
At minimum:
1. N=1 production qbind-node starts with canonical <data_dir>/consensus opened.
2. storage path exists and is deterministic.
3. restart preserves storage state.
4. committed epoch persistence is proven if an existing epoch-transition path can be exercised.
5. storage-open/corruption error fails closed if testable.
6. activation_epoch trust-bundle still fails closed with CurrentEpochUnavailable.
7. activation_epoch omitted behavior unchanged.
8. no --p2p-trusted-root fallback.
9. no DummySig / DummyKem / DummyAead.
10. no sequence regression.

Evidence:
Create:
- docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_093.md

Update:
- docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md
- docs/protocol/QBIND_PEER_TRUST_BUNDLE_PROPAGATION_SAFETY.md if needed
- docs/whitepaper/contradiction.md

Evidence doc must include:
- exact objective
- exact verdict
- exact files changed
- exact commands run
- tests/evidence pass-fail status
- ConsensusStorage source investigation
- canonical storage path decision
- startup ordering
- epoch persistence behavior
- storage-present/no-epoch vs committed-epoch distinction
- snapshot/restore boundary
- proof Run 091/092 activation_epoch fail-closed behavior preserved
- proof existing activation_height behavior preserved
- exact remaining C4/C5 boundaries
- exact immediate next action

contradiction.md update rules:
- If production binary opens canonical ConsensusStorage and epoch persistence is proven, record C4 narrowed for production consensus-storage lifecycle.
- If storage opens but epoch writes are not yet possible because production epoch-transition events are absent, record partial-positive and exact boundary.
- If safe storage open cannot be implemented without broader redesign, preserve Run 092 boundary and document why.
- Keep C4 open for activation_epoch consumption, snapshot epoch parity, peer-driven live apply, KMS/HSM, signing-key ratification, fast-sync/restore, and production trust-anchor operations.
- Do not close full C4.
- Do not claim C5 closure.

Expected verdict:
- Strongest positive:
  production qbind-node opens canonical ConsensusStorage on binary path; epoch persistence is proven on restart/recovery; activation_epoch fail-closed behavior preserved; regressions pass.
- Partial positive:
  canonical storage open lands but epoch writes/transition wiring or snapshot parity remains incomplete.
- Negative:
  synthetic epoch introduced, activation_epoch accepted without authority, missing epoch treated as 0, storage corruption ignored, or regressions fail.

Required final response:
1. Exact verdict
2. Exact files changed
3. Exact commands run
4. Tests/evidence pass-fail status
5. What was fixed or bounded
6. What was proven
7. What remains not solved
8. Whether contradiction.md was updated and why
9. Exact immediate next action recommended