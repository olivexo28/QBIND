Task: Run 202 — Release-Binary RemoteSigner Transport Boundary Evidence

Current accepted state:

* Run 194: source/test RemoteSigner production-custody interface boundary landed.
* Run 195: release-binary RemoteSigner boundary evidence fully closed.
* Run 196: source/test RemoteSigner payload/carrying and production-context wiring landed.
* Run 197: release-binary RemoteSigner payload/carrying evidence fully closed.
* Run 198: source/test hidden RemoteSigner policy selector landed.
* Run 199: release-binary RemoteSigner policy selector evidence fully closed.
* Run 200: C4/C5 closure criteria and remaining-work consolidation landed.
* Run 201: source/test RemoteSigner transport boundary landed:

  * `pqc_remote_signer_transport.rs`;
  * `RemoteSignerTransportConfig`;
  * request/response envelopes wrapping Run 194 `RemoteSignerRequest` / `RemoteSignerResponse`;
  * deterministic domain-separated request, response, and transcript digests;
  * pure/mockable `RemoteSignerTransport` trait;
  * DevNet/TestNet-only `FixtureLoopbackRemoteSignerTransport`;
  * fail-closed `ProductionRemoteSignerTransport`;
  * typed transport outcome taxonomy;
  * `validate_remote_signer_transport`;
  * `validate_lifecycle_custody_remote_signer_and_transport`;
  * 58 tests covering A1–A10 / R1–R35;
  * full lib suite 1328 passed.
* Run 201 preserves:

  * no real RemoteSigner backend;
  * no networked signer daemon;
  * no real KMS/HSM/cloud-KMS/PKCS#11;
  * no MainNet peer-driven apply enablement;
  * no governance execution;
  * no real on-chain proof verifier;
  * no validator-set rotation;
  * full C4 open;
  * C5 open.
* Release-binary RemoteSigner transport-boundary evidence is deferred to Run 202.

Objective:
Produce release-binary evidence for the Run 201 RemoteSigner transport boundary.

This run must prove on release binaries:

1. fixture loopback transport remains DevNet/TestNet evidence-only;
2. production transport remains unavailable/fail-closed;
3. MainNet production transport remains unavailable/fail-closed;
4. request, response, and transcript digests are deterministic and domain-bound;
5. transport composes with Run 194 RemoteSigner request/response;
6. transport composes with the custody/RemoteSigner validation path at release-helper/library level;
7. MainNet peer-driven apply remains refused;
8. no real RemoteSigner backend, networked signer daemon, KMS/HSM, governance execution, or validator-set rotation is claimed.

Strict scope:

* Release-binary evidence only.
* Use real `target/release/qbind-node`.
* Use release-built helper(s) to exercise the Run 201 transport boundary.
* No production source change unless a tiny harness-only fix is required.
* No real RemoteSigner backend.
* No networked signer daemon.
* No production signing key custody.
* No real KMS implementation.
* No real HSM implementation.
* No cloud KMS integration.
* No PKCS#11 integration.
* No MainNet peer-driven apply enablement.
* No governance execution engine.
* No real on-chain proof verifier.
* No validator-set rotation.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* No marker schema change.
* No sequence-file schema change.
* No trust-bundle core schema change.
* No authority lifecycle semantics change.
* No wire/schema change.
* Do not weaken Runs 070, 130–201.
* Do not claim full C4 or C5 closure.

Required deliverables:

1. Add release-binary helper:
   `crates/qbind-node/examples/run_202_remote_signer_transport_release_binary_helper.rs`

2. Add release-binary harness:
   `scripts/devnet/run_202_remote_signer_transport_release_binary.sh`

3. Add evidence archive:
   `docs/devnet/run_202_remote_signer_transport_release_binary/`

4. Add canonical evidence report:
   `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_202.md`

5. Update narrowly:

   * `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
   * `docs/whitepaper/contradiction.md`
   * `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
   * `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
   * `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

6. Crosscheck all new/modified code and docs against existing design/spec. If contradictions or inconsistencies are found, record them in `contradiction.md` before finalizing.

Required release-built helper matrix:
The helper must exercise the Run 201 RemoteSigner transport corpus in release mode.

Accepted / compatible cases:
A1. fixture loopback transport accepted under explicit fixture policy on DevNet.

A2. fixture loopback transport accepted under explicit fixture policy on TestNet.

A3. request envelope digest deterministic.

A4. response envelope digest deterministic.

A5. request/response transcript digest deterministic.

A6. transport request binds environment, chain, genesis, authority root, signer id, custody key id, lifecycle action, candidate digest, and authority-domain sequence.

A7. transport response binds request id, request digest, signer id, custody key id, response digest, and transcript digest.

A8. production transport boundary callable and returns typed unavailable outcome.

A9. Run 194 RemoteSigner validation remains compatible with a fixture transport response.

A10. GenesisBound / EmergencyCouncil / OnChainGovernance proof behavior remains unchanged when transport policy is disabled.

Required rejection cases:
R1. transport rejected under Disabled policy.

R2. fixture transport rejected under ProductionRemoteSignerRequired.

R3. fixture transport rejected under MainnetProductionRemoteSignerRequired.

R4. production transport rejected as unavailable.

R5. MainNet production transport rejected as unavailable.

R6. endpoint missing rejected.

R7. endpoint malformed rejected.

R8. wrong environment rejected.

R9. wrong chain rejected.

R10. wrong genesis rejected.

R11. wrong signer id rejected.

R12. wrong custody key id rejected.

R13. wrong authority root rejected.

R14. wrong signing-key fingerprint rejected.

R15. wrong request id rejected.

R16. wrong request digest rejected.

R17. wrong response digest rejected.

R18. wrong transcript digest rejected.

R19. stale/replayed request rejected.

R20. stale/replayed response rejected.

R21. timeout rejected.

R22. retry exhausted rejected.

R23. malformed request envelope rejected.

R24. malformed response envelope rejected.

R25. unsupported protocol version rejected.

R26. unsupported suite rejected.

R27. invalid transport attestation rejected.

R28. local operator cannot satisfy RemoteSigner transport.

R29. peer majority cannot satisfy RemoteSigner transport.

R30. transport valid but RemoteSigner response invalid rejected.

R31. RemoteSigner response valid but transport transcript invalid rejected.

R32. lifecycle + governance + custody valid but production transport unavailable rejected.

R33. validation-only rejection remains non-mutating.

R34. mutating preflight rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

R35. MainNet peer-driven apply remains refused even with fixture loopback transport.

Required real binary checks:
Use real `target/release/qbind-node` to prove:

* MainNet peer-driven apply remains refused.
* Default behavior does not expose or enable production RemoteSigner transport.
* No `RemoteSigner transport active` / `RemoteSigner backend connected` / `networked signer daemon active` claim is emitted.
* No KMS/HSM active claim is emitted.
* Existing RemoteSigner policy selector behavior from Run 199 remains compatible.
* Existing custody policy selector behavior from Run 193 remains compatible.
* Existing governance fixture proof paths remain compatible.
* No governance execution claim is emitted.
* No validator-set rotation claim is emitted.

Required source/release reachability proof:
The report must include source grep/call-site proof for:

* `pqc_remote_signer_transport`;
* `RemoteSignerTransportConfig`;
* `RemoteSignerRequestEnvelope`;
* `RemoteSignerResponseEnvelope`;
* `RemoteSignerTransport`;
* `FixtureLoopbackRemoteSignerTransport`;
* `ProductionRemoteSignerTransport`;
* request envelope digest helper;
* response envelope digest helper;
* transcript digest helper;
* `validate_remote_signer_transport`;
* `validate_lifecycle_custody_remote_signer_and_transport`.

Required mutation / no-mutation proof:
For rejected RemoteSigner transport-boundary scenarios:

* no Run 070 apply call;
* no live trust swap;
* no session eviction;
* no sequence write;
* no marker write;
* marker bytes unchanged where present;
* sequence bytes unchanged where present;
* no `.tmp` residue;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead.

For accepted fixture transport mutating compatibility scenarios, if any mutating compatibility path is exercised:

* transport request validation occurs before apply/mutation;
* transport response validation occurs before apply/mutation;
* RemoteSigner request/response validation occurs before apply/mutation;
* custody validation occurs before apply/mutation;
* lifecycle validation occurs before apply/mutation;
* governance validation occurs before apply/mutation where applicable;
* Run 055 sequence commit succeeds if a mutating path is exercised;
* v2 marker persists strictly after sequence commit.

Required denylist:
Across the run prove:

* no MainNet apply;
* no autonomous apply;
* no apply on receipt;
* no peer-majority authority;
* no real RemoteSigner backend claim;
* no RemoteSigner transport active claim;
* no networked signer daemon active claim;
* no real governance execution claim;
* no real on-chain governance proof claim;
* no KMS/HSM enabled or active claim;
* no validator-set rotation claim;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead;
* no schema/wire/metric drift;
* no marker write before sequence commit;
* no sequence write on validation-only surfaces;
* no marker write on validation-only surfaces.

Required captured metadata:

* qbind-node SHA-256 and ELF Build ID;
* helper SHA-256 and ELF Build ID;
* git commit hash;
* rustc/cargo versions;
* exact commands;
* stdout/stderr logs;
* per-scenario exit codes;
* RemoteSigner transport fixture request/response/envelope paths and hashes;
* marker/sequence JSON and SHA before/after where applicable;
* data-dir inventories;
* denylist grep results.

Validation commands:
Run at minimum:

* `cargo build --release -p qbind-node --bin qbind-node`
* `cargo build --release -p qbind-node --example run_202_remote_signer_transport_release_binary_helper`
* `bash scripts/devnet/run_202_remote_signer_transport_release_binary.sh`
* `cargo test -p qbind-node --test run_201_remote_signer_transport_boundary_tests`
* `cargo test -p qbind-node --test run_198_remote_signer_policy_selector_tests`
* `cargo test -p qbind-node --test run_196_remote_signer_payload_callsite_tests`
* `cargo test -p qbind-node --test run_194_remote_authority_signer_boundary_tests`
* `cargo test -p qbind-node --test run_192_authority_custody_policy_selector_tests`
* `cargo test -p qbind-node --test run_190_authority_custody_payload_callsite_tests`
* `cargo test -p qbind-node --test run_188_authority_custody_boundary_tests`
* `cargo test -p qbind-node --test run_186_onchain_governance_production_verifier_boundary_tests`
* `cargo test -p qbind-node --test run_184_onchain_governance_payload_carrying_tests`
* `cargo test -p qbind-node --test run_182_onchain_governance_production_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_180_onchain_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_178_onchain_governance_proof_tests`
* `cargo test -p qbind-node --test run_176_live_0x05_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_173_validation_only_governance_required_policy_tests`
* `cargo test -p qbind-node --test run_171_governance_required_policy_selector_tests`
* `cargo test -p qbind-node --test run_169_governance_proof_loader_surface_integration_tests`
* `cargo test -p qbind-node --test run_167_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_165_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_163_governance_authority_verifier_tests`
* `cargo test -p qbind-node --test run_161_lifecycle_marker_integration_tests`
* `cargo test -p qbind-node --test run_159_authority_signing_key_lifecycle_tests`
* `cargo test -p qbind-node --test run_157_unified_testnet_fixture_universe_tests`
* `cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests`
* `cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests`
* `cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests`
* `cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests`
* `cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests`
* `cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Documentation requirements:
`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_202.md` must state:

* Run 202 is release-binary RemoteSigner transport-boundary evidence.
* No real RemoteSigner backend is implemented.
* No networked signer daemon is implemented.
* Fixture loopback transport is DevNet/TestNet evidence-only.
* Production transport remains unavailable/fail-closed.
* RemoteSigner transport does not enable MainNet peer-driven apply.
* KMS/HSM remain unimplemented.
* governance execution remains unimplemented.
* real on-chain proof verification remains unimplemented.
* validator-set rotation remains open.
* existing custody/governance proof paths remain compatible.
* full C4 remains open.
* C5 remains open.

Acceptance criteria:
Run 202 is acceptable only if:

1. Release-built helper proves the Run 201 RemoteSigner transport corpus.
2. Real `target/release/qbind-node` confirms MainNet peer-driven apply remains refused.
3. Fixture loopback transport remains DevNet/TestNet evidence-only.
4. Production transport remains unavailable/fail-closed.
5. Request, response, and transcript digests are deterministic and domain-bound.
6. Transport composes with Run 194 RemoteSigner request/response.
7. Rejected RemoteSigner transport-boundary cases produce no mutation.
8. Existing custody/governance proof paths remain compatible.
9. No real RemoteSigner backend / networked signer daemon / KMS / HSM / governance execution / validator-set rotation claim is made.
10. No full C4 or C5 closure is claimed.
