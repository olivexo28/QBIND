Run 362 — Public DevNet abuse/DoS runtime wiring + release-binary evidence for M12

Continue QBIND after accepted Run 361.

Current state:
Run 361 is accepted as PASS / public-DevNet-abuse-DoS-hardening source-test positive.

Run 361 added:

- `crates/qbind-node/src/public_devnet_abuse_dos_config.rs`
- `crates/qbind-node/tests/run_361_public_devnet_abuse_dos_hardening_tests.rs`
- `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_361.md`
- narrow public DevNet readiness / P2P posture / protocol / ops documentation updates.

Run 361 introduced a pure source/test abuse/DoS config model and bounded inbound connection-rate limiter boundary. It did not wire the limiter into runtime, did not add CLI flags, did not add metrics, did not change defaults, and did not provide release-binary evidence.

Current public DevNet readiness state:
- M1 canonical DevNet genesis package: Green.
- M2 release binary provenance: Green.
- M3 release binary reproducibility / BuildID: Green for same-host scope.
- M5 validator/full-node onboarding quickstart: Green.
- M10 public P2P port posture: Green.
- M11 peer admission policy: Green.
- M16 incident response: Green.
- M17 public how-to-run-a-node documentation: Green.
- M18 user-facing disclaimers: Green.
- M19 network parameter publication: Green.
- M20 genesis hash publication: Green.
- M4 seed/bootnodes: Yellow and launch-blocking; no live seeds or reachability evidence.
- M6 validator identity guidance: Yellow/Partial.
- M7–M9 remain Yellow.
- M12 abuse / DoS protections: Yellow strengthened by Run 361; Green deferred to Run 362.
- M13–M15 remain Yellow.

Full C4 remains OPEN.
C5 remains OPEN.
MainNet authority rotation/revocation remains Red.
Run 353/354 remains Green-for-scope only and is not wired into runtime.
Public DevNet readiness remains a separate release-readiness track.

Primary objective:
Wire the Run 361 `AbuseDosConfig` / `ConnectionRateLimiter` into the `qbind-node` P2P accept path behind runtime-owned state, add the minimum metric evidence for connection-rate drops, expose validated operator configuration only if necessary, and capture release-binary evidence proving M12 can move Green for public DevNet scope.

M12 may move Green only if the runtime wiring and release-binary evidence genuinely prove:
1. current defaults are preserved unless the operator explicitly configures the limiter;
2. public DevNet can run with operator-configurable connection-rate and per-peer message-rate posture;
3. inbound connection over-budget behavior is observable and bounded;
4. the implementation does not weaken peer admission, KEMTLS, trust-bundle checks, or genesis-hash pinning.

Strict scope:
M12 runtime wiring + release-binary evidence only.

No public DevNet launch.
No live seed-node deployment.
No bootnode deployment.
No faucet.
No RPC gateway.
No explorer.
No status page.
No MainNet enablement.
No public TestNet readiness claim.
No C4 closure.
No C5 closure.
No runtime authority-lifecycle wiring.
No validator-set mutation.
No production epoch transition.
No production execution-sink write.
No P2P wire-format change.
No peer-admission weakening.
No trust-bundle behavior weakening.
No default-open public port.
No fake live infrastructure.
No M4 Green.
No M6 Green.
No public DevNet launch-ready claim unless every must-have is Green, which is not expected.

Required investigation:
Inspect the Run 361 module and current runtime surfaces:

- `crates/qbind-node/src/public_devnet_abuse_dos_config.rs`
- `crates/qbind-node/tests/run_361_public_devnet_abuse_dos_hardening_tests.rs`
- `crates/qbind-node/src/peer_rate_limiter.rs`
- `crates/qbind-node/src/p2p_tcp.rs`
- `crates/qbind-node/src/p2p_inbound.rs`
- `crates/qbind-node/src/cli.rs`
- `crates/qbind-node/src/node_config.rs`
- `crates/qbind-node/src/metrics.rs`
- `crates/qbind-node/src/metrics_http.rs`
- existing P2P listener / accept-loop code
- existing peer registry / disconnect / metrics code
- Run 360 and Run 361 docs:
  - `docs/release/public-devnet/p2p/ABUSE_DOS_POSTURE.md`
  - `docs/release/public-devnet/p2p/P2P_PORT_POSTURE.md`
  - `docs/release/public-devnet/p2p/PEER_ADMISSION_POLICY.md`
  - `docs/release/public-devnet/p2p/VERIFY.md`
- `docs/release/QBIND_PUBLIC_DEVNET_READINESS_CRITERIA.md`

Required implementation:

1. Runtime-owned abuse/DoS state.

Add a runtime-owned state/config holder that can be passed to the P2P accept path without global mutable state.

It should compose the Run 361 model and preserve current behavior by default.

Expected shape, adapt to repo style:

- `PublicDevnetAbuseDosRuntimeConfig`
- `PublicDevnetAbuseDosRuntimeState`
- `ConnectionRateLimiterState` owned by the runtime, not by test-only caller state
- explicit disabled/default profile
- explicit enabled profile for public DevNet

2. Accept-loop integration.

Wire the bounded inbound connection-rate limiter into the actual P2P accept path, preferably in or immediately around `p2p_tcp` accept handling.

Requirements:
- default runtime behavior remains unchanged unless the limiter is explicitly enabled/configured;
- when enabled, incoming connections are checked before expensive peer-admission work where feasible;
- over-budget connections are refused/dropped deterministically;
- refusal does not bypass KEMTLS/trust-bundle/genesis checks for admitted connections;
- refusal does not admit any peer;
- state is bounded;
- no panic on malformed remote address;
- MainNet is refused unless a production policy is implemented.

3. Operator configuration / CLI posture.

Expose operator configuration only if needed to make M12 genuinely operator-configurable.

Acceptable options:
- public CLI flags, if fully validated and documented;
- hidden/devnet-only CLI flags, if public DevNet readiness docs clearly state their status;
- config-file or NodeConfig plumbing, if already supported and operator-facing.

Preferred if adding CLI flags:
- `--p2p-max-messages-per-second <N>`
- `--p2p-burst-allowance <N>`
- `--p2p-connection-rate-window-ms <N>`
- `--p2p-connection-burst <N>`
- optional `--p2p-per-address-connection-rate-window-ms <N>`
- optional `--p2p-per-address-connection-burst <N>`

If names differ, use repo-consistent names.

CLI/config requirements:
- defaults must exactly preserve current behavior;
- invalid zero/unbounded/nonsensical values fail closed at config parse/validation;
- MainNet use refused absent production policy;
- help text must be clear that this is DevNet/public-DevNet abuse/DoS posture, not MainNet readiness.

If public CLI flags are added, docs and release-binary `--help` evidence must prove them.

4. Metric.

Add a metric for connection-rate drops/refusals if metrics architecture supports it.

Preferred name if no conflict:

`qbind_p2p_connection_rate_drop_total`

or a repo-consistent equivalent.

Requirements:
- increment only when the connection-rate limiter refuses/drops an inbound connection;
- labels must not leak private endpoints or secrets;
- metric must not duplicate an existing family;
- metric must be documented in `ABUSE_DOS_POSTURE.md`;
- release-binary evidence must prove the metric is registered or, if metric emission requires runtime traffic, at least that the metric symbol/family is reachable and documented honestly.

5. Load / runtime evidence helper.

Add a release helper and harness to prove the runtime behavior in release mode.

Suggested helper:

`crates/qbind-node/examples/run_362_public_devnet_abuse_dos_runtime_release_binary_helper.rs`

Suggested harness:

`scripts/devnet/run_362_public_devnet_abuse_dos_runtime_release_binary.sh`

Suggested archive:

`docs/devnet/run_362_public_devnet_abuse_dos_runtime_release_binary/{README.md,summary.txt,.gitignore}`

Canonical evidence:

`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_362.md`

Only `README.md`, `summary.txt`, `.gitignore` should be tracked in the archive; raw logs/artifacts should be gitignored.

Required tests:
Add or extend tests:

`crates/qbind-node/tests/run_362_public_devnet_abuse_dos_runtime_tests.rs`

Tests must cover:

Default compatibility:
1. default CLI/config keeps P2P behavior unchanged;
2. default per-peer values remain 1000 msg/s + 100 burst;
3. connection limiter disabled by default;
4. `PeerRateLimiter::with_defaults()` compatibility preserved.

Config validation:
5. valid custom message threshold accepted;
6. valid custom burst accepted;
7. valid global connection-rate config accepted;
8. valid per-address connection-rate config accepted if implemented;
9. zero max messages/sec rejected;
10. zero/invalid burst rejected where unsafe;
11. impossible connection window rejected;
12. unbounded/too-large values rejected or explicitly unsafe;
13. wrong environment rejected;
14. wrong genesis binding rejected if represented;
15. MainNet refused absent production policy.

Runtime accept-loop behavior:
16. under-budget inbound connection allowed to proceed to normal admission;
17. over-budget inbound connection refused before expensive admission where feasible;
18. refill/window behavior deterministic;
19. per-address limiter, if implemented, limits one abusive address without consuming all global capacity incorrectly;
20. disabled limiter does not block;
21. malformed/unknown remote address handled safely;
22. rate-limit refusal does not mark a peer admitted;
23. rate-limit refusal does not mutate trust state;
24. rate-limit refusal does not write sequence/marker files.

Metric behavior:
25. connection-rate drop metric increments on refusal;
26. metric does not increment on allowed connection;
27. metric labels do not include raw private endpoint data unless already accepted in metrics policy;
28. metric family registered once.

Non-mutation / safety:
29. no P2P wire-format change;
30. no peer-admission weakening;
31. no trust-bundle behavior weakening;
32. no `LivePqcTrustState` mutation;
33. no validator-set mutation;
34. no epoch transition;
35. no Run 070 apply path;
36. no public DevNet launch claim.

Release-binary evidence:
The release helper/harness must prove in release mode:

1. real `target/release/qbind-node` built and SHA-256 captured;
2. helper built and SHA-256 captured;
3. toolchain captured;
4. helper links real Run 361/362 runtime symbols;
5. default config preserves old behavior;
6. configured DevNet limiter accepts under-budget connection attempts and refuses over-budget attempts;
7. metric increments on refusal;
8. invalid configs fail closed;
9. MainNet refused;
10. no public DevNet launch claim;
11. no M4 Green;
12. no C4/C5 closure;
13. no MainNet/TestNet readiness;
14. no runtime authority-lifecycle wiring;
15. no trust-bundle/validator-set/epoch mutation.

Production binary scenarios:
Run and record:

1. `qbind-node --help` contains any newly added CLI flags, if added, and no unintended flags.
2. Default DevNet surface remains disabled/silent where expected.
3. Default TestNet/MainNet surfaces remain refused/silent as before.
4. Invalid abuse/DoS config fails closed.
5. Invented abuse/DoS flags are rejected by clap unless implemented.
6. Existing genesis/environment fail-closed surfaces remain unchanged.

If no CLI flags are added, explicitly record that no new public CLI flags were added and how operators configure the posture, or keep M12 Yellow if operator configurability remains unavailable.

Required docs:
Add:

`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_362.md`

If using a release archive, add:

`docs/devnet/run_362_public_devnet_abuse_dos_runtime_release_binary/README.md`
`docs/devnet/run_362_public_devnet_abuse_dos_runtime_release_binary/summary.txt`
`docs/devnet/run_362_public_devnet_abuse_dos_runtime_release_binary/.gitignore`

Update narrowly:

- `docs/release/QBIND_PUBLIC_DEVNET_READINESS_CRITERIA.md`
- `docs/release/public-devnet/p2p/ABUSE_DOS_POSTURE.md`
- `docs/release/public-devnet/p2p/VERIFY.md`
- `docs/release/public-devnet/operator/QUICKSTART.md`, only if new flags/config affect operator commands
- `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
- `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
- `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
- `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
- `docs/whitepaper/contradiction.md`

Docs must state clearly:
- Run 362 does not launch public DevNet;
- M12 moves Green only if runtime/operator-configurable + release-binary evidence lands;
- M4 remains Yellow/launch-blocking;
- M6 remains Yellow unless identity generation is genuinely implemented;
- public DevNet remains NOT launch-ready unless all must-haves are Green;
- C4/C5 remain OPEN.

Required commands:
Run and record at minimum:

- `cargo build -p qbind-node --release`
- `cargo build -p qbind-node --example run_362_public_devnet_abuse_dos_runtime_release_binary_helper --release`
- Run the Run 362 harness.
- `cargo test -p qbind-node --test run_362_public_devnet_abuse_dos_runtime_tests`
- `cargo test -p qbind-node --test run_361_public_devnet_abuse_dos_hardening_tests`
- `cargo test -p qbind-node --lib`

If target names differ, record exact substitutions and why.

Security:
Run secret scanning over all changed files.

No private keys, mnemonics, seed phrases, credentials, API keys, tokens, private infrastructure, real production hostnames, or unapproved live endpoints may be introduced.

If test addresses/endpoints are needed, use RFC 5737 / localhost / explicit non-live values only.

CodeQL:
Because this run changes Rust source, run CodeQL if available.

If CodeQL is skipped, times out, is unavailable, or reports database-too-large / trivial-change / infrastructure failure, record the exact result and do not call it clean.

Provenance:
Record in `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_362.md`:

- git commit;
- branch;
- clean/dirty state;
- files changed;
- source module paths;
- test target paths;
- helper/harness paths;
- release binary SHA-256;
- helper SHA-256;
- metric name;
- CLI/config changes if any;
- commands run;
- test results;
- secret scan result;
- CodeQL result;
- readiness matrix delta;
- exact reason M12 is or is not Green.

Do not leave unexplained `git_status: dirty`.

Readiness matrix update rules:
In `QBIND_PUBLIC_DEVNET_READINESS_CRITERIA.md`:

- M12 may move Yellow → Green only if actual runtime/operator-configurable protection is implemented, documented, release-binary evidenced, and default compatibility is proven.
- If runtime wiring lands but operator configurability is still unavailable, keep M12 Yellow/Partial.
- If operator configurability lands but release-binary/load evidence is incomplete, keep M12 Yellow/Partial.
- Do not alter M4 unless live seed/bootnode reachability evidence actually lands.
- Do not alter M6 unless stable identity-generation support lands.
- Do not alter M7/M8/M9/M13/M14/M15 unless genuinely closed.
- Public DevNet remains NOT launch-ready unless all must-haves are Green.

Expected verdict:
PASS / public-DevNet-abuse-DoS-runtime-release positive if the runtime wiring, operator configurability, metric, release-binary evidence, tests, docs, secret scan, CodeQL provenance, and readiness-matrix update land.

Partial-positive if runtime wiring lands but M12 cannot honestly move Green due to missing operator config, missing metric, missing load/release evidence, or incomplete provenance.

Unacceptable:
- claiming public DevNet launch-ready;
- marking M4 Green without live seed/bootnode reachability evidence;
- marking M6 Green without identity-generation support;
- marking M12 Green without runtime/operator-configurable and release-binary evidence;
- weakening P2P admission;
- changing P2P wire format;
- silently changing runtime defaults;
- adding public CLI flags without tests/docs;
- publishing fake live seed addresses;
- publishing secrets or private infrastructure;
- claiming TestNet readiness;
- claiming MainNet readiness;
- closing C4 or C5;
- marking MainNet authority rotation/revocation Green;
- adding runtime authority-lifecycle wiring;
- deploying seed nodes, faucet, RPC, explorer, or status page in this run;
- hiding skipped CodeQL;
- using marketing language.

Final response format:
1. Exact verdict.
2. Files changed.
3. Runtime wiring summary.
4. Abuse/DoS config and operator configurability.
5. Connection-rate limiter runtime behavior.
6. Metric behavior.
7. Default compatibility.
8. CLI/config surface.
9. Release artifacts and hashes.
10. Helper/harness corpus.
11. Runtime/load evidence.
12. Rejection/fail-closed evidence.
13. Non-mutation evidence.
14. Readiness matrix delta for M12.
15. Current public DevNet readiness status.
16. Remaining public DevNet blockers.
17. Public TestNet blockers.
18. MainNet blockers.
19. C4/C5 status.
20. Tests run.
21. Security scans.
22. CodeQL.
23. Provenance.
24. Honest limitations.
25. Suggested Run 363 next step.