Run 379 — Public DevNet telemetry and monitoring baseline for M13/M14

Continue QBIND after accepted Run 378.

Current state:
Run 378 is accepted as NEGATIVE-FOR-EXTERNAL / Route C for public DevNet M4 external seed reachability.

Run 378 proved:
- `register-check --status live --reachability-evidence <ref>` admission gate still works;
- live without reachability evidence fails closed;
- planned + reachability evidence fails closed;
- loopback / same-host listener preflight still works;
- external reachability is NOT proven;
- no M4 Green;
- no public DevNet launch;
- no production Rust source change;
- no C4/C5 closure.

Current public DevNet readiness:
- M12 Green.
- M6 generation / verification / admission-check halves Green-for-scope, but M6 overall Yellow/Partial because live registration is M4-gated.
- M4 Yellow / launch-blocking.
- M13 telemetry / metrics baseline Yellow.
- M14 monitoring / alerting baseline Yellow.
- Public DevNet NOT launch-ready.
- C4 OPEN.
- C5 OPEN.
- MainNet authority rotation/revocation Red.

Primary objective:
Publish an operator-facing public DevNet telemetry and monitoring baseline that can move:
- M13 telemetry / metrics baseline → Green, if fully evidenced;
- M14 monitoring / alerting baseline → Green, if alert rules / scrape config / verification evidence land.

This is a docs/artifact/verification run, preferably with no production Rust source change.

Strict scope:
M13/M14 only.

No public DevNet launch.
No live seed/bootnode deployment.
No M4 Green.
No M6 Green unless already justified elsewhere; do not alter it here.
No faucet.
No RPC gateway.
No explorer.
No status page.
No TestNet readiness claim.
No MainNet readiness claim.
No C4 closure.
No C5 closure.
No authority-lifecycle runtime wiring.
No validator-set mutation.
No epoch transition.
No sequence/marker write.
No LivePqcTrustState mutation.
No trust-bundle live apply.
No peer-driven apply.
No P2P wire-format change.
No peer-admission weakening.
No new public network endpoint.
No committed secrets, credentials, private hostnames, data dirs, logs, or metrics dumps containing private material.

Required investigation:
Inspect:
- `crates/qbind-node/src/metrics.rs`
- `crates/qbind-node/src/metrics_http.rs`
- `crates/qbind-node/src/main.rs`
- `crates/qbind-node/src/cli.rs`
- existing metrics environment surface, especially `QBIND_METRICS_HTTP_ADDR`
- `docs/ops/QBIND_MONITORING_AND_ALERTING_BASELINE.md`
- `docs/ops/QBIND_INCIDENT_RESPONSE.md`
- `docs/release/public-devnet/p2p/ABUSE_DOS_POSTURE.md`
- `docs/release/public-devnet/p2p/VERIFY.md`
- `docs/release/public-devnet/operator/QUICKSTART.md`
- `docs/release/QBIND_PUBLIC_DEVNET_READINESS_CRITERIA.md`
- Run 360–373 M12 evidence for connection-rate and per-peer metrics
- Run 377/378 M4 evidence, only to preserve the no-launch / no-live-seed boundary

Decision gate:
Record one of:

Route A — existing metrics HTTP and metrics families are sufficient.
Publish docs/artifacts/verification only.

Route B — a small docs-only clarification or helper/harness is needed to scrape and validate existing metrics.
Add helper/harness if useful, but no production source change.

Route C — metrics or alerting are too incomplete to move M13/M14 Green.
Keep the affected item Yellow and document exact gaps.

Expected artifacts:
Add canonical public DevNet observability package:

`docs/release/public-devnet/observability/README.md`
`docs/release/public-devnet/observability/METRICS.md`
`docs/release/public-devnet/observability/SCRAPE_CONFIG.md`
`docs/release/public-devnet/observability/ALERT_RULES.md`
`docs/release/public-devnet/observability/RUNBOOK.md`
`docs/release/public-devnet/observability/VERIFY.md`

Add machine-readable examples if appropriate:

`docs/release/public-devnet/observability/prometheus-scrape.example.yml`
`docs/release/public-devnet/observability/prometheus-alerts.example.yml`

Add release harness:

`scripts/devnet/run_379_public_devnet_observability_baseline.sh`

Add archive:

`docs/devnet/run_379_public_devnet_observability_baseline/{README.md,summary.txt,.gitignore}`

Add canonical evidence:

`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_379.md`

Update narrowly:
- `docs/release/QBIND_PUBLIC_DEVNET_READINESS_CRITERIA.md`
- `docs/release/public-devnet/operator/QUICKSTART.md`
- `docs/release/public-devnet/p2p/ABUSE_DOS_POSTURE.md`
- `docs/release/public-devnet/p2p/VERIFY.md`
- `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
- `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
- `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
- `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
- `docs/whitepaper/contradiction.md`

Required telemetry package content:
Document:

1. How to enable metrics safely:
   - `QBIND_METRICS_HTTP_ADDR=127.0.0.1:<port>` or supported equivalent;
   - localhost-only recommendation;
   - no public internet exposure without reverse proxy/auth/firewall.
2. How to verify metrics endpoint is live.
3. Metrics families relevant to public DevNet operators:
   - process/node liveness if present;
   - P2P connection-rate metric from M12;
   - per-peer rate-limit metric from M12;
   - trust-bundle metrics if present;
   - peer-candidate / propagation metrics if present;
   - consensus/HotStuff metrics if present;
   - snapshot/restore or storage metrics if present;
   - any missing families must be documented honestly as missing.
4. Which metrics are required for launch operations.
5. Which metrics are best-effort / future.
6. Example Prometheus scrape config.
7. Example alert rules for:
   - node down / metrics endpoint down;
   - P2P listener unavailable if metric/log surface exists;
   - connection-rate drops sustained;
   - per-peer rate-limit drops sustained;
   - trust-bundle load/apply failures if metrics exist;
   - consensus no-progress if a metric exists;
   - disk/storage issue if a metric exists;
   - high error/rejection counters where available.
8. Alert severity levels:
   - page;
   - ticket;
   - observe.
9. Operator response actions for each alert.
10. Safety warning that this is DevNet / no value / resettable.
11. Statement that observability does not imply launch, TestNet, MainNet, C4, or C5 readiness.

Required verification evidence:
The harness must prove:

1. `cargo build -p qbind-node --release --bin qbind-node` succeeds.
2. `qbind-node --help` does not expose any new observability CLI flag unless one already existed.
3. A release `qbind-node` process can start with `QBIND_METRICS_HTTP_ADDR=127.0.0.1:<port>`.
4. `/metrics` can be scraped over loopback.
5. The scrape output contains expected baseline metric families that actually exist.
6. The scrape output does not require public exposure.
7. The example Prometheus scrape config parses as YAML.
8. The example alert rules parse as YAML.
9. The docs list only real metric names verified by source or scrape.
10. Any alert based on a currently absent metric is marked future / not enabled.
11. No live seed / M4 claim is made.
12. No TestNet/MainNet/C4/C5 claim is made.

Required tests:
Run and record exact counts:

- `cargo build -p qbind-node --release --bin qbind-node`
- Run the Run 379 harness
- YAML parse check for scrape config
- YAML parse check for alert rules
- grep/check that docs do not claim launch-ready, M4 Green, C4/C5 closure, TestNet/MainNet readiness
- `cargo test -p qbind-node --lib`

If a Rust helper/test is added, run it explicitly.

Security:
Run secret scanning over all changed files.

No credentials, bearer tokens, webhook URLs, private infrastructure hostnames, private IPs intended to remain private, generated keys, data dirs, raw logs with secrets, or metrics dumps with sensitive labels may be committed.

Use localhost, RFC 5737, and redacted examples only.

CodeQL:
If no production Rust source changes, record CodeQL as not meaningful/docs-harness-only.
If Rust source/example changes, invoke CodeQL if available.
If skipped, unavailable, timed out, database-too-large, or not meaningful, record exact status and do not call it clean.

Readiness matrix rules:
- M13 may move Yellow → Green only if operator-facing metrics exposure docs, verification commands, and release-binary scrape evidence land.
- M14 may move Yellow → Green only if alert rules / scrape config / response runbook land and parse successfully, with absent-metric alerts clearly marked future.
- M4 remains Yellow unless real external seed reachability lands.
- M6 remains Yellow/Partial unless live registration backed by M4 lands.
- M12 remains Green unless contradicted.
- Public DevNet remains NOT launch-ready unless every must-have is Green.
- C4/C5 remain OPEN.
- TestNet/MainNet remain untouched.

Expected verdict:
PASS / public-DevNet observability baseline positive if M13/M14 artifacts, scrape evidence, alert config, docs, tests, scan, and provenance all land.

Partial-positive if only M13 or only M14 can move Green, or if configs are docs-only without release-binary scrape evidence.

Unacceptable:
- claiming public DevNet launch-ready;
- marking M4 Green;
- marking M6 fully Green without live registration;
- claiming TestNet/MainNet readiness;
- closing C4/C5;
- inventing metric names not present in source or scrape;
- shipping alert rules for absent metrics without marking them future;
- exposing metrics publicly by default;
- committing secrets or private endpoints.

Final response format:
1. Exact verdict.
2. Files changed.
3. Decision gate route.
4. Observability package contents.
5. Metrics endpoint surface.
6. Verified metric families.
7. Scrape config evidence.
8. Alert rules evidence.
9. Operator runbook evidence.
10. Rejection / non-claim checks.
11. Default compatibility / no runtime behavior change.
12. Readiness matrix delta for M13.
13. Readiness matrix delta for M14.
14. Current public DevNet readiness status.
15. Remaining public DevNet blockers.
16. Public TestNet blockers.
17. MainNet blockers.
18. C4/C5 status.
19. Tests run.
20. Security scans.
21. CodeQL.
22. Provenance.
23. Honest limitations.
24. Suggested Run 380 next step.