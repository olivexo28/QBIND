Run 255 — Release-binary modeled durable-completion attestation-projection evidence

You are continuing QBIND after Run 254.

Run 240 is fully closed at source/test level.
Run 241 is fully closed as release-binary evidence for Run 240.
Run 242 is fully closed as source/test governance execution mutation-engine boundary work.
Run 243 is fully closed as release-binary evidence for Run 242.
Run 244 is fully closed as source/test modeled governance trust-state mutation applier boundary work.
Run 245 is fully closed as release-binary evidence for Run 244.
Run 246 is fully closed as source/test governance modeled end-to-end pipeline boundary work.
Run 247 is fully closed as release-binary evidence for Run 246.
Run 248 is fully closed as source/test modeled durable-consume projection sink boundary work.
Run 249 is fully closed as release-binary evidence for Run 248.
Run 250 is fully closed as source/test modeled durable-consume receipt-acknowledgement / completion reporter boundary work.
Run 251 is fully closed as release-binary evidence for Run 250.
Run 252 is fully closed as source/test modeled durable-completion finalization-projection boundary work.
Run 253 is fully closed as release-binary evidence for Run 252.
Run 254 is fully closed as source/test modeled durable-completion finalization attestation-projection boundary work.

Strict workflow:

* Execution-first.
* Narrow scope only.
* No broad redesign.
* No silent regressions.
* No hype or overclaiming.
* Keep Full C4 OPEN and C5 OPEN unless real production evidence closes them.
* MainNet peer-driven apply remains refused.
* Fixture/local/loopback material remains DevNet/TestNet evidence-only.
* Rejected paths must remain non-mutating.
* Do not weaken Runs 070, 130–254.
* Release-binary evidence only.
* No production source module modification unless required for a compile-only fix, and any such fix must be explicitly justified.

Current state:

* Run 246 models the end-to-end ordering from evaluator/call-site authorization through durable replay, mutation-engine authorization, modeled applier success, and durable consume authorization.
* Run 248 models the durable-consume projection sink: only a Run 246 `ModeledApplierAppliedAndDurableConsumeAuthorized` outcome can create a sink intent, and only `ConsumeReceiptRecorded` records a modeled in-memory receipt.
* Run 249 release-evidenced the Run 248 sink boundary.
* Run 250 models the durable-consume completion reporter: only Run 248 `ConsumeReceiptRecorded` can create a completion-report intent, and only `CompletionReportRecorded` authorizes a new modeled completion-reported state.
* Run 251 release-evidenced the Run 250 completion-reporter boundary.
* Run 252 models durable-completion finalization projection: only Run 250 `CompletionReportRecorded` can create a finalization intent, and only `DurableCompletionFinalized` authorizes a new modeled durable-completion-finalized state.
* Run 253 release-evidenced the Run 252 finalization-projection boundary.
* Run 254 models durable-completion attestation projection: only Run 252 `DurableCompletionFinalized` can create an attestation intent, and only `DurableCompletionAttested` authorizes a new modeled durable-completion-attested state.
* Run 254 has no release-binary evidence yet.
* No real persistent replay backend is implemented.
* No real durable consume backend is implemented.
* No real completion-report backend is implemented.
* No real finalization backend is implemented.
* No real attestation backend is implemented.
* No real audit ledger backend is implemented.
* No real governance execution engine is implemented.
* No real production mutation engine is implemented.
* No real on-chain governance proof verifier is implemented.
* No RocksDB/file/schema/migration/storage-format change is implemented.
* No KMS/HSM/RemoteSigner backend is implemented.
* No MainNet governance enablement.
* No MainNet peer-driven apply enablement.
* Validator-set rotation remains unsupported.
* Full C4 remains OPEN.
* C5 remains OPEN.

Objective:

Produce release-binary evidence for the Run 254 modeled durable-completion finalization attestation-projection boundary, mirroring the Run 241 / 243 / 245 / 247 / 249 / 251 / 253 release-evidence pattern.

Run 255 must prove on real `target/release/qbind-node` plus a release-built helper that the Run 254 attestation-projection boundary is present in release-built code, that the helper exercises the Run 246 pipeline → Run 248 consume-sink projection → Run 250 completion-report projection → Run 252 finalization projection → Run 254 attestation projection → fixture attestation-recording path through production library symbols, and that all fail-closed / non-mutating / no-attestation invariants remain true in release mode.

Run 255 must not implement a real persistent replay backend, durable consume backend, completion-report backend, finalization backend, attestation backend, audit ledger backend, production mutation engine, governance execution engine, on-chain governance proof verifier, KMS/HSM/RemoteSigner backend, validator-set rotation, MainNet governance, or MainNet peer-driven apply.

Required deliverables:

1. Add a release helper:

   `crates/qbind-node/examples/run_255_modeled_durable_completion_attestation_projection_release_binary_helper.rs`

   The helper must link against release-built production library symbols and exercise:

   * `pqc_governance_modeled_durable_completion_attestation_projection`
   * `GovernanceModeledDurableCompletionAttestationInput`
   * `GovernanceModeledDurableCompletionAttestationExpectations`
   * `GovernanceModeledDurableCompletionAttestationPolicy`
   * `GovernanceModeledDurableCompletionAttestationSurface`
   * `GovernanceModeledDurableCompletionAttestationEnvironmentBinding`
   * `GovernanceModeledDurableCompletionAttestationRuntimeBinding`
   * `GovernanceModeledDurableCompletionAttestationReplayBinding`
   * `GovernanceModeledDurableCompletionAttestationPipelineBinding`
   * `GovernanceModeledDurableCompletionAttestationSinkBinding`
   * `GovernanceModeledDurableCompletionAttestationReporterBinding`
   * `GovernanceModeledDurableCompletionAttestationFinalizationBinding`
   * `GovernanceModeledDurableCompletionAttestationRecord`
   * `ModeledDurableCompletionAttestationLedger`
   * `ModeledDurableCompletionAttestationRecord`
   * `ModeledDurableCompletionAttestationSnapshot`
   * `ModeledDurableCompletionAttestationStatus`
   * `ModeledDurableCompletionAttestationDigest`
   * `GovernanceModeledDurableCompletionAttestationOutcome`
   * `GovernanceModeledDurableCompletionAttestor`
   * `FixtureModeledDurableCompletionAttestor`
   * `ProductionModeledDurableCompletionAttestor`
   * `MainNetModeledDurableCompletionAttestor`
   * `project_finalization_outcome_to_attestation_intent`
   * `evaluate_modeled_durable_completion_attestation_projection`
   * `recover_modeled_durable_completion_attestation_window`
   * `attestation_outcome_authorizes_modeled_attestation`
   * `attestation_outcome_projects_to_durable_completion_attested`
   * all Run 254 grep-verifiable invariant helpers

   If any exact symbol name differs from Run 254, use the exact implemented name and record the substitution in `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_255.md`. Do not invent compatibility shims unless needed for compilation, and do not change production behavior.

2. Add a release harness:

   `scripts/devnet/run_255_modeled_durable_completion_attestation_projection_release_binary.sh`

   The harness must:

   * build `target/release/qbind-node`;
   * build the Run 255 release helper;
   * capture git commit, rustc version, cargo version;
   * capture SHA-256 and ELF Build ID for `target/release/qbind-node`;
   * capture SHA-256 and ELF Build ID for the Run 255 helper;
   * run real-binary surface scenarios;
   * run the helper corpus in release mode;
   * run source-reachability greps for the Run 254 symbols;
   * run helper-output reachability checks proving the helper exercised the Run 254 symbols;
   * run a denylist proving no active production/MainNet enablement claims;
   * run the required regression test corpus;
   * write generated evidence into an ignored evidence directory.

3. Add evidence archive directory:

   `docs/devnet/run_255_modeled_durable_completion_attestation_projection_release_binary/`

   Track only:

   * `README.md`
   * `summary.txt`
   * `.gitignore`

   Generated artifacts must be ignored, following the Run 241 / 243 / 245 / 247 / 249 / 251 / 253 convention.

4. Add canonical evidence report:

   `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_255.md`

Release-helper corpus:

The helper must include accepted/compatible, rejection, recovery, projection, stage-ordering, attestation-ledger, non-mutation, and reachability tables.

Accepted / compatible cases must prove at minimum:

* Disabled attestor policy preserves legacy bypass and performs no attestation, no attestation record, and no mutation.
* Disabled finalizer policy preserves legacy bypass and never invokes the attestor.
* Disabled reporter policy preserves legacy bypass and never invokes finalizer or attestor.
* Disabled sink policy preserves legacy bypass and never invokes reporter, finalizer, or attestor.
* Disabled pipeline policy preserves legacy bypass and never invokes sink, reporter, finalizer, or attestor.
* Disabled evaluator/call-site policy preserves legacy bypass and never invokes sink, reporter, finalizer, or attestor.
* DevNet fixture pipeline success + sink receipt recorded + completion report recorded + finalization recorded + attestor record success records exactly one modeled in-memory attestation.
* TestNet fixture pipeline success + sink receipt recorded + completion report recorded + finalization recorded + attestor record success records exactly one modeled in-memory attestation.
* Modeled add-root pipeline success records attestation only after modeled applier success, sink receipt record, completion report record, and finalization record.
* Modeled retire-root pipeline success records attestation only after modeled applier success, sink receipt record, completion report record, and finalization record.
* Modeled revoke-root pipeline success records attestation only after modeled applier success, sink receipt record, completion report record, and finalization record.
* Modeled emergency-revoke-root pipeline success records attestation only after modeled applier success, sink receipt record, completion report record, and finalization record.
* Modeled noop pipeline success records attestation only under explicit success semantics, no trust-state drift, recorded receipt, recorded completion report, and recorded finalization.
* Duplicate identical attestation is idempotent under an explicit typed outcome and creates no second attestation.
* `DurableCompletionDuplicateIdempotent` may only match an already-attested completion and never creates a new attestation by itself.
* Existing Run 252 finalization behavior remains compatible.
* Existing Run 250 completion-reporter behavior remains compatible.
* Existing Run 248 sink behavior remains compatible.
* Existing Run 246 pipeline behavior remains compatible.
* Existing Run 244 modeled applier behavior remains compatible.
* Existing Run 242 mutation-engine behavior remains compatible.
* Existing Run 240 durable completion behavior remains compatible.
* Production attestor path is reachable but unavailable/fail-closed and records no attestation.
* MainNet attestor path is reachable but unavailable/fail-closed and records no attestation.
* MainNet peer-driven apply is refused before pipeline progression, before sink invocation, before reporter invocation, before finalizer invocation, and before attestor invocation.
* Validator-set rotation unsupported and records no attestation.
* Policy-change unsupported and records no attestation.

Rejected / fail-closed cases must prove at minimum:

* evaluator rejection produces no sink intent, no receipt, no reporter intent, no completion report, no finalizer intent, no finalization, no attestor intent, and no attestation;
* call-site rejection produces no sink intent, no receipt, no reporter intent, no completion report, no finalizer intent, no finalization, no attestor intent, and no attestation;
* durable replay stale / expired / consumed / superseded / backend-unavailable produces no attestor intent and no attestation;
* mutation-engine rejection produces no attestor intent and no attestation;
* modeled applier rejected-before-snapshot / rejected-before-apply / apply failure / rollback / rollback-failed / ambiguous window produces no attestor intent and no attestation;
* sink did not record receipt / rejected-before-record / record failure / rollback / rollback-failed / ambiguous window produces no reporter/finalizer/attestor invocation and no attestation;
* reporter did not record completion / rejected-before-record / record failure / rollback / rollback-failed / ambiguous window produces no finalizer/attestor invocation and no attestation;
* finalizer did not finalize / rejected-before-record / record failure / rollback / rollback-failed / ambiguous window produces no attestor invocation and no attestation;
* attestation before durable-completion finalization is rejected;
* attestation after modeled apply failure, sink record failure, reporter record failure, finalizer record failure, finalizer rollback, finalizer rollback-failed, or finalizer ambiguous window is rejected;
* same attestation id with different digest is rejected as equivocation and records no second attestation;
* wrong environment / chain / genesis / governance surface / mutation surface rejected before attestor invocation;
* wrong attestation digest / finalization digest / completion-report digest / receipt digest / sink decision digest / reporter decision digest / finalization decision digest / pipeline decision digest / proposal id / decision id / candidate digest / authority-domain sequence rejected before attestation record;
* malformed attestation record rejected before record;
* local operator key cannot satisfy MainNet authority;
* peer majority cannot satisfy MainNet authority;
* production attestor unavailable records no attestation;
* MainNet attestor unavailable records no attestation;
* validator-set rotation attempt unsupported and records no attestation;
* policy-change attempt unsupported and records no attestation;
* every rejected path produces no Run 070 call, no `LivePqcTrustState` mutation, no real trust swap, no session eviction, no sequence write, no marker write, no RocksDB/file/schema/migration/storage-format change, and no attestor invocation where rejection happens before the attestor stage.

Recovery / crash-window cases must prove at minimum:

* before-pipeline window fails closed / no attestation;
* after-pipeline-success-before-sink-intent window fails closed / no attestation;
* after-sink-intent-before-receipt-record window fails closed / no attestation;
* after-receipt-record-before-report-intent window fails closed / no attestation;
* after-report-intent-before-report-record window fails closed / no attestation;
* after-report-record-before-finalization-intent window fails closed / no attestation;
* after-finalization-intent-before-finalization-record window fails closed / no attestation;
* after-finalization-record-before-attestation-intent window fails closed / no attestation;
* after-attestation-intent-before-attestation-record window fails closed / no durable completion attestation;
* after-attestation-record-before-attestation-success window fails closed unless an explicit matching attestation success exists;
* after-attestation-success recovers as durable-completion attested;
* after-attestation-ambiguous fails closed / no attestation;
* attestation record-failed window fails closed / no attestation;
* attestation rollback-completed window fails closed / no attestation;
* attestation rollback-failed window is fatal/fail-closed / no attestation;
* unknown window fails closed / no attestation;
* production/MainNet recovery classification unavailable/fail-closed;
* MainNet peer-driven apply refusal precedes recovery classification where applicable.

Projection cases must prove at minimum:

* only Run 252 `DurableCompletionFinalized` creates an attestation intent;
* Run 246 pipeline success alone creates no attestation intent;
* Run 248 sink receipt record alone creates no attestation intent;
* Run 250 completion-report record alone creates no attestation intent;
* Run 252 finalization intent alone creates no attestation intent;
* `DurableCompletionDuplicateIdempotent` creates no new attestation unless a matching attestation already exists;
* evaluator success alone creates no attestation intent;
* durable replay freshness alone creates no attestation intent;
* mutation-engine authorization alone creates no attestation intent;
* modeled applier success without sink receipt, completion report, and finalization creates no attestation intent;
* only `DurableCompletionAttested` authorizes modeled durable-completion-attested state;
* `LegacyBypassNoAttestation` does not attest;
* `RejectedBeforeFinalizationNoAttestation` does not attest;
* `FinalizationDidNotFinalizeNoAttestation` does not attest;
* `DurableCompletionAttestationDuplicateIdempotent` does not create a second attestation and must not be counted as a new attestation;
* `DurableCompletionAttestationRejectedBeforeRecord` does not attest;
* `DurableCompletionAttestationRecordFailedNoAttestation` does not attest;
* `DurableCompletionAttestationRolledBackNoAttestation` does not attest;
* `DurableCompletionAttestationRollbackFailedFatalNoAttestation` does not attest;
* `DurableCompletionAttestationAmbiguousFailClosedNoAttestation` does not attest;
* `ProductionAttestorUnavailableNoAttestation` does not attest;
* `MainNetAttestorUnavailableNoAttestation` does not attest;
* `MainNetPeerDrivenApplyRefusedNoAttestation` does not attest;
* `ValidatorSetRotationUnsupportedNoAttestation` does not attest;
* `PolicyChangeUnsupportedNoAttestation` does not attest.

Stage-ordering cases must prove at minimum:

* MainNet peer-driven apply refusal happens before pipeline progression, sink invocation, reporter invocation, finalizer invocation, and attestor invocation.
* Evaluator/call-site authorization happens before durable replay freshness.
* Durable replay freshness happens before mutation-engine authorization.
* Mutation-engine authorization happens before modeled applier invocation.
* Modeled applier success happens before sink intent.
* Sink receipt recording happens after pipeline consume authorization.
* Completion report recording happens after sink receipt-recorded state.
* Finalization recording happens after completion-report recorded state.
* Attestation intent happens only after durable-completion-finalized state.
* Durable-completion attestation recording happens after durable-completion-finalized state.
* A rejection before the attestor stage leaves the attestor invocation count at zero.
* An attestation record failure does not retroactively invalidate the finalization record but does not authorize durable-completion attestation.
* An attestation rollback failure is fatal/fail-closed and does not authorize durable-completion attestation.
* The attestor itself performs no persistent durable completion beyond modeled in-memory fixture state.

Attestation-ledger cases must prove at minimum:

* one valid attestation inserts exactly one modeled in-memory ledger record;
* duplicate identical attestation is idempotent and does not increase record count;
* same attestation id with different digest is equivocation and does not increase record count;
* wrong attestation digest does not record;
* wrong finalization digest does not record;
* wrong completion-report digest does not record;
* wrong receipt digest does not record;
* wrong sink decision digest does not record;
* wrong reporter decision digest does not record;
* wrong finalization decision digest does not record;
* wrong pipeline decision digest does not record;
* wrong proposal id does not record;
* wrong decision id does not record;
* wrong candidate digest does not record;
* wrong authority-domain sequence does not record;
* malformed attestation does not record;
* rollback restores modeled attestation ledger snapshot;
* rollback failure never claims success;
* fixture attestation ledger never writes files, RocksDB, markers, sequences, or production durable state.

Non-mutation cases must prove at minimum:

* no Run 070 call on any helper case;
* no `LivePqcTrustState` mutation on any helper case;
* no real trust swap on any helper case;
* no session eviction on any helper case;
* no sequence write on any helper case;
* no marker write on any helper case;
* no RocksDB/file/schema/migration/storage-format write or format change;
* no wire/schema/marker/sequence/trust-bundle format change;
* fixture attestor mutates only `ModeledDurableCompletionAttestationLedger`;
* production/MainNet paths remain unavailable/fail-closed;
* MainNet peer-driven apply remains refused first.

Real release-binary surface scenarios:

The harness must run the real `target/release/qbind-node` for at least:

* S1 — `--help` / default help surface: no durable-completion attestation enablement banner or visible public flag drift.
* S2 — default DevNet startup parse/smoke surface: no attestation enablement claim.
* S3 — default TestNet startup parse/smoke surface: no attestation enablement claim.
* S4 — default MainNet startup parse/smoke surface: no attestation enablement claim.
* S5 — hidden governance-execution selector or equivalent hidden selector still parses as before, without exposing the Run 254/255 helper-only path as a public production enablement surface.
* S6 — invalid governance-execution selector or equivalent invalid selector fails closed before mutation if applicable, with no marker write, no sequence write, no live trust swap, no session eviction, no Run 070 call, and no attestor invocation.

Do not add any new public CLI surface for Run 255. If a hidden selector already exists from earlier runs, the harness may use it only as a parse/fail-closed smoke surface.

Denylist:

Prove the captured real-binary logs and helper logs do not contain active/enabled claims for:

* real attestation backend enabled;
* modeled attestation production enabled;
* MainNet modeled attestation enabled;
* real audit ledger backend enabled;
* real finalization backend enabled;
* modeled finalization production enabled;
* MainNet modeled finalization enabled;
* real completion-report backend enabled;
* modeled completion-reporter production enabled;
* MainNet modeled completion-reporter enabled;
* real durable consume backend enabled;
* real persistent replay backend enabled;
* modeled durable-consume sink production enabled;
* MainNet modeled durable-consume sink enabled;
* real production mutation engine enabled;
* MainNet mutation engine enabled;
* MainNet governance enabled;
* MainNet peer-driven apply enabled;
* real governance execution engine enabled;
* real on-chain governance proof verifier enabled;
* RocksDB replay backend enabled;
* file replay backend enabled;
* schema migration enabled;
* storage-format migration enabled;
* KMS/HSM backend enabled;
* RemoteSigner backend enabled;
* validator-set rotation enabled;
* policy-change action enabled;
* autonomous apply;
* apply-on-receipt;
* peer-majority authority;
* Run 070 apply from the modeled attestor;
* LivePqcTrustState mutation from the modeled attestor;
* real trust swap from the modeled attestor;
* session eviction from the modeled attestor;
* marker write from the modeled attestor;
* sequence write from the modeled attestor;
* RocksDB write from the modeled attestor;
* file write from the modeled attestor;
* production durable consume by the modeled attestor itself;
* production finalization by the modeled attestor itself;
* production attestation by the modeled attestor itself;
* DummySig / DummyKem / DummyAead active on production path.

Source/release reachability proof:

The harness must grep production source for the Run 254 symbols and prove the release helper exercises them in release mode.

Required greps include:

* `pqc_governance_modeled_durable_completion_attestation_projection`
* `GovernanceModeledDurableCompletionAttestationInput`
* `GovernanceModeledDurableCompletionAttestationExpectations`
* `GovernanceModeledDurableCompletionAttestationPolicy`
* `GovernanceModeledDurableCompletionAttestationSurface`
* `GovernanceModeledDurableCompletionAttestationEnvironmentBinding`
* `GovernanceModeledDurableCompletionAttestationRuntimeBinding`
* `GovernanceModeledDurableCompletionAttestationReplayBinding`
* `GovernanceModeledDurableCompletionAttestationPipelineBinding`
* `GovernanceModeledDurableCompletionAttestationSinkBinding`
* `GovernanceModeledDurableCompletionAttestationReporterBinding`
* `GovernanceModeledDurableCompletionAttestationFinalizationBinding`
* `GovernanceModeledDurableCompletionAttestationRecord`
* `ModeledDurableCompletionAttestationLedger`
* `ModeledDurableCompletionAttestationRecord`
* `ModeledDurableCompletionAttestationSnapshot`
* `ModeledDurableCompletionAttestationStatus`
* `ModeledDurableCompletionAttestationDigest`
* `GovernanceModeledDurableCompletionAttestationOutcome`
* `GovernanceModeledDurableCompletionAttestor`
* `FixtureModeledDurableCompletionAttestor`
* `ProductionModeledDurableCompletionAttestor`
* `MainNetModeledDurableCompletionAttestor`
* `project_finalization_outcome_to_attestation_intent`
* `evaluate_modeled_durable_completion_attestation_projection`
* `recover_modeled_durable_completion_attestation_window`
* `attestation_outcome_authorizes_modeled_attestation`
* `attestation_outcome_projects_to_durable_completion_attested`
* `DurableCompletionAttested`
* `DurableCompletionAttestationDuplicateIdempotent`
* `DurableCompletionAttestationRejectedBeforeRecord`
* `DurableCompletionAttestationRecordFailedNoAttestation`
* `DurableCompletionAttestationRolledBackNoAttestation`
* `DurableCompletionAttestationRollbackFailedFatalNoAttestation`
* `DurableCompletionAttestationAmbiguousFailClosedNoAttestation`
* `ProductionAttestorUnavailableNoAttestation`
* `MainNetAttestorUnavailableNoAttestation`
* `MainNetPeerDrivenApplyRefusedNoAttestation`
* `ValidatorSetRotationUnsupportedNoAttestation`
* `PolicyChangeUnsupportedNoAttestation`
* `modeled_attestation_rejection_is_non_mutating`
* `modeled_attestation_never_calls_run_070`
* `modeled_attestation_never_mutates_live_pqc_trust_state`
* `modeled_attestation_never_writes_sequence_or_marker`
* `modeled_attestation_no_rocksdb_file_schema_migration_change`
* `modeled_attestation_pipeline_success_required_before_attestation`
* `modeled_attestation_sink_receipt_required_before_attestation`
* `modeled_attestation_completion_report_required_before_attestation`
* `modeled_attestation_finalization_required_before_attestation`
* `modeled_attestation_record_required_before_durable_completion_attested`
* `modeled_attestation_failed_record_never_attests`
* `modeled_attestation_rollback_never_attests`
* `modeled_attestation_ambiguous_window_fails_closed`
* `modeled_attestation_mainnet_peer_driven_apply_refused_first`
* `modeled_attestation_production_mainnet_unavailable`
* `modeled_attestation_validator_set_rotation_unsupported`
* `modeled_attestation_policy_change_unsupported`
* `modeled_attestation_local_operator_cannot_satisfy_mainnet_authority`
* `modeled_attestation_peer_majority_cannot_satisfy_mainnet_authority`

If exact symbol names differ, use the exact implemented Run 254 names and record the differences honestly in `QBIND_DEVNET_EVIDENCE_RUN_255.md`.

Validation commands:

Run at minimum:

* `cargo build -p qbind-node --release --bin qbind-node`
* `cargo build -p qbind-node --release --example run_255_modeled_durable_completion_attestation_projection_release_binary_helper`
* `bash scripts/devnet/run_255_modeled_durable_completion_attestation_projection_release_binary.sh`
* `cargo test -p qbind-node --test run_254_modeled_durable_completion_attestation_projection_tests`
* `cargo test -p qbind-node --test run_252_modeled_durable_completion_finalization_projection_tests`
* `cargo test -p qbind-node --test run_250_modeled_durable_consume_completion_reporter_tests`
* `cargo test -p qbind-node --test run_248_modeled_durable_consume_projection_sink_tests`
* `cargo test -p qbind-node --test run_246_governance_modeled_end_to_end_pipeline_tests`
* `cargo test -p qbind-node --test run_244_modeled_governance_trust_mutation_applier_tests`
* `cargo test -p qbind-node --test run_242_governance_execution_mutation_engine_tests`
* `cargo test -p qbind-node --test run_240_governance_evaluator_replay_durable_runtime_integration_tests`
* `cargo test -p qbind-node --test run_238_governance_evaluator_replay_durable_backend_tests`
* `cargo test -p qbind-node --test run_236_governance_evaluator_replay_consume_runtime_integration_tests`
* `cargo test -p qbind-node --test run_234_governance_evaluator_replay_consume_boundary_tests`
* `cargo test -p qbind-node --test run_232_governance_evaluator_replay_runtime_integration_tests`
* `cargo test -p qbind-node --test run_230_governance_evaluator_replay_state_tests`
* `cargo test -p qbind-node --test run_228_peer_evaluator_context_representation_tests`
* `cargo test -p qbind-node --test run_226_governance_evaluator_runtime_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_224_governance_evaluator_runtime_integration_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

Security scanning:

* Run secret scanning if available and report the result.
* Run CodeQL if available and report the result honestly.
* If Rust CodeQL analysis is skipped because the database is too large, say that explicitly and do not overclaim CodeQL coverage.

Documentation:

Create:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_255.md`
* `docs/devnet/run_255_modeled_durable_completion_attestation_projection_release_binary/README.md`
* `docs/devnet/run_255_modeled_durable_completion_attestation_projection_release_binary/summary.txt`
* `docs/devnet/run_255_modeled_durable_completion_attestation_projection_release_binary/.gitignore`

Update narrowly:

* `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
* `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
* `docs/whitepaper/contradiction.md`

Documentation must state:

* Run 255 is release-binary evidence for Run 254.
* No production mutating behavior is enabled.
* The modeled durable-completion attestation-projection boundary is release-evidenced, not production-enabled.
* The release helper exercises the Run 254 production library symbols in release mode.
* The release helper remains dead code from the production runtime.
* The fixture attestor mutates only modeled in-memory `ModeledDurableCompletionAttestationLedger` state.
* Run 246 pipeline success is required before any sink intent can exist.
* Run 248 `ConsumeReceiptRecorded` is required before any completion-report intent can exist.
* Run 250 `CompletionReportRecorded` is required before any finalization intent can exist.
* Run 252 `DurableCompletionFinalized` is required before any attestation intent can exist.
* Only `DurableCompletionAttested` authorizes a new modeled durable-completion-attested state.
* `DurableCompletionDuplicateIdempotent` may only match an already-attested completion and never creates one by itself.
* Duplicate identical attestation is idempotent and creates no second attestation.
* Same attestation id with different digest fails closed as equivocation.
* Every non-finalized finalization outcome produces no attestor invocation and no attestation.
* Failed attestation record, rollback, rollback-failed, ambiguous windows, unavailable production/MainNet attestor paths, rejected replay states, and unsupported actions never attest.
* MainNet peer-driven apply remains refused before pipeline progression, sink invocation, reporter invocation, finalizer invocation, and attestor invocation.
* Rejected attestor paths are non-mutating.
* No real attestation backend is implemented.
* No real audit ledger backend is implemented.
* No real finalization backend is implemented.
* No real completion-report backend is implemented.
* No real durable consume backend is implemented.
* No real persistent replay backend is implemented.
* No real production mutation engine is implemented.
* No real governance execution engine is implemented.
* No real on-chain governance proof verifier is implemented.
* No RocksDB/file/schema/migration/storage-format change is implemented.
* No wire/schema/marker/sequence/trust-bundle change is implemented.
* No KMS/HSM/RemoteSigner backend is implemented.
* No MainNet governance enablement.
* No MainNet peer-driven apply enablement.
* No validator-set rotation.
* The attestor does not call Run 070.
* The attestor does not mutate `LivePqcTrustState`.
* The attestor does not perform a real trust swap.
* The attestor does not evict sessions.
* The attestor does not write sequence files.
* The attestor does not write authority markers.
* Full C4 remains OPEN.
* C5 remains OPEN.

Contradiction handling:

* Crosscheck Run 255 against Runs 050–254.
* If no contradiction is found, add a “no Run 255 contradiction” entry to `docs/whitepaper/contradiction.md`.
* If any contradiction is found, do not hide it. Record it explicitly with the exact file/function/doc reference and recommend the smallest safe fix.

Strict non-goals:

* Do not implement a real persistent replay backend.
* Do not implement a real durable consume backend.
* Do not implement a real completion-report backend.
* Do not implement a real finalization backend.
* Do not implement a real attestation backend.
* Do not implement a real audit ledger backend.
* Do not implement a real production mutation engine.
* Do not wire production runtime mutation.
* Do not call Run 070.
* Do not mutate `LivePqcTrustState`.
* Do not perform a real trust swap.
* Do not evict sessions.
* Do not write sequence files.
* Do not write authority markers.
* Do not implement a real governance execution engine.
* Do not implement a real on-chain governance proof verifier.
* Do not implement validator-set rotation.
* Do not add RocksDB/file/schema/migration/storage-format changes.
* Do not change wire/schema/marker/sequence/trust-bundle formats.
* Do not enable MainNet governance.
* Do not enable MainNet peer-driven apply.
* Do not add autonomous apply or apply-on-receipt.
* Do not let peer majority satisfy authority.
* Do not weaken any fail-closed behavior from Runs 070 or 130–254.

Expected verdict:

Positive for release-binary evidence, if the release helper/harness and regression corpus pass.

Run 255 should close the Run 254 release-binary evidence gap only. It must not claim Full C4 or C5 closure.

Final response format:

1. Exact verdict.
2. Files changed.
3. Release artifacts and hashes.
4. Helper corpus results.
5. Real-binary scenarios.
6. Tests run and results.
7. Security invariants preserved.
8. Honest limitations.
9. C4/C5 status.
10. Suggested Run 256 next step.
