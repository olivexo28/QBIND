Task: Run 212 — Release-Binary Governance Execution Policy Boundary Evidence

Current accepted state:

* Run 163: source/test governance authority verifier landed.
* Run 164: release-binary governance authority verifier boundary evidence landed.
* Run 165: source/test governance gate wired into v2 lifecycle / marker-decision path.
* Run 166: release-binary governance gate evidence landed.
* Run 167–177: governance proof carrier, Required-policy selector, validation-only coverage, and live `0x05` governance proof evidence landed.
* Run 178–187: OnChainGovernance verifier boundary, payload carrying, call-site wiring, and release-binary evidence landed, but production on-chain governance verification remains unavailable/fail-closed.
* Run 188–210: custody, RemoteSigner, KMS/HSM, custody-attestation boundaries, payload carriers, selectors, and release-binary evidence landed.
* Run 211: source/test governance execution policy boundary landed:

  * `pqc_governance_execution_policy.rs`;
  * governance execution class/policy/action/quorum enums;
  * input/decision/expectations structs;
  * SHA3-256 domain-separated input/decision/transcript/policy digests;
  * pure `GovernanceExecutionEvaluator` trait;
  * fixture governance evaluator;
  * production/on-chain/MainNet fail-closed evaluators;
  * typed outcome enum;
  * peer-driven guard composition;
  * fail-closed helpers;
  * 55 tests covering A1–A15 / R1–R38;
  * release-binary evidence deferred to Run 212.
* Run 211 preserves:

  * no real governance execution engine;
  * no real on-chain governance proof verifier;
  * no MainNet governance enablement;
  * no validator-set rotation;
  * no real KMS/HSM backend;
  * no real RemoteSigner backend;
  * no production signing key custody;
  * MainNet peer-driven apply remains refused;
  * full C4 remains OPEN;
  * C5 remains OPEN.

Objective:
Produce release-binary evidence for the Run 211 governance execution policy boundary.

This run must prove on release binaries:

1. fixture governance execution remains DevNet/TestNet evidence-only;
2. emergency council fixture execution remains explicit and separate;
3. production governance execution remains unavailable/fail-closed;
4. on-chain governance execution remains unavailable/fail-closed;
5. MainNet governance execution remains unavailable/fail-closed;
6. governance input/decision/transcript digests are deterministic and domain-bound;
7. governance execution authorizes lifecycle action only when action, candidate digest, and sequence match;
8. validator-set rotation remains unsupported;
9. rejected governance execution cases produce no mutation;
10. MainNet peer-driven apply remains refused;
11. no real governance execution engine, on-chain proof verifier, KMS/HSM backend, RemoteSigner backend, or validator-set rotation is claimed.

Strict scope:

* Release-binary evidence only.
* Use real `target/release/qbind-node`.
* Use release-built helper(s) to exercise the Run 211 governance execution policy boundary.
* No production source change unless a tiny harness-only fix is required.
* No real governance execution engine.
* No real on-chain governance proof verifier.
* No MainNet governance enablement.
* No MainNet peer-driven apply enablement.
* No validator-set rotation.
* No authority-set rotation beyond existing lifecycle boundary checks.
* No real KMS implementation.
* No real HSM implementation.
* No real RemoteSigner backend.
* No production signing key custody.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* No marker schema change.
* No sequence-file schema change.
* No trust-bundle core schema change.
* No wire/schema change.
* Do not weaken Runs 070, 130–211.
* Do not claim full C4 or C5 closure.

Required deliverables:

1. Add release-binary helper:
   `crates/qbind-node/examples/run_212_governance_execution_policy_release_binary_helper.rs`

2. Add release-binary harness:
   `scripts/devnet/run_212_governance_execution_policy_release_binary.sh`

3. Add evidence archive:
   `docs/devnet/run_212_governance_execution_policy_release_binary/`

4. Add canonical evidence report:
   `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_212.md`

5. Update narrowly:

   * `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
   * `docs/whitepaper/contradiction.md`
   * `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
   * `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
   * `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

6. Crosscheck all new/modified code and docs against existing design/spec. If contradictions or inconsistencies are found, record them in `contradiction.md` before finalizing.

Required release-built helper matrix:
The helper must exercise the Run 211 governance execution policy corpus in release mode.

Accepted / compatible cases:
A1. fixture governance execution accepted under explicit fixture policy on DevNet.

A2. fixture governance execution accepted under explicit fixture policy on TestNet.

A3. emergency council fixture accepted under explicit emergency fixture policy on DevNet.

A4. governance execution input digest deterministic.

A5. governance execution decision digest deterministic.

A6. governance execution transcript digest deterministic.

A7. governance policy digest deterministic, if implemented.

A8. governance input binds environment, chain, genesis, proposal id, decision id, authority root, lifecycle action, candidate digest, authority-domain sequence, governance proof digest, effective epoch, expiry epoch, and replay nonce.

A9. governance decision binds proposal id, decision id, approved action, authority root, candidate digest, sequence, effective epoch, expiry epoch, issuer class, and replay nonce.

A10. lifecycle rotate action authorized only when governance decision authorizes rotate.

A11. lifecycle revoke action authorized only when governance decision authorizes revoke.

A12. emergency revoke accepted only under explicit emergency fixture policy.

A13. production governance boundary callable and returns typed unavailable outcome.

A14. on-chain governance execution boundary callable and returns typed unavailable outcome.

A15. GenesisBound / EmergencyCouncil / OnChainGovernance proof-carrier behavior remains unchanged when governance execution policy is Disabled.

A16. custody / RemoteSigner / KMS-HSM / custody-attestation paths remain compatible when governance execution policy is Disabled.

Required rejection cases:
R1. governance execution rejected under Disabled policy.

R2. fixture governance rejected under ProductionGovernanceRequired.

R3. emergency fixture rejected under ProductionGovernanceRequired.

R4. fixture governance rejected under MainnetGovernanceRequired.

R5. production governance rejected as unavailable.

R6. on-chain governance rejected as unavailable.

R7. MainNet governance rejected as unavailable.

R8. unknown governance class rejected.

R9. wrong environment rejected.

R10. wrong chain rejected.

R11. wrong genesis rejected.

R12. wrong authority root rejected.

R13. wrong lifecycle action rejected.

R14. wrong candidate digest rejected.

R15. wrong authority-domain sequence rejected.

R16. wrong governance proof digest rejected.

R17. wrong on-chain proof digest rejected.

R18. wrong custody attestation digest rejected.

R19. wrong proposal id rejected.

R20. wrong decision id rejected.

R21. wrong effective epoch rejected.

R22. expired decision rejected.

R23. stale/replayed decision rejected.

R24. quorum threshold insufficient rejected.

R25. emergency action not authorized rejected.

R26. validator-set rotation unsupported rejected.

R27. policy-change action unsupported rejected.

R28. malformed governance execution input rejected.

R29. malformed governance execution decision rejected.

R30. unsupported governance execution version rejected.

R31. local operator cannot satisfy governance execution.

R32. peer majority cannot satisfy governance execution.

R33. governance valid but lifecycle action mismatch rejected.

R34. lifecycle valid but governance decision invalid rejected.

R35. lifecycle + governance proof + custody valid but production governance execution unavailable rejected.

R36. validation-only rejection remains non-mutating.

R37. mutating preflight rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

R38. MainNet peer-driven apply remains refused even with fixture governance approval.

Required real binary checks:
Use real `target/release/qbind-node` to prove:

* MainNet peer-driven apply remains refused.
* Default behavior does not expose or enable governance execution.
* No `governance execution active` / `production governance active` / `MainNet governance enabled` claim is emitted.
* No real on-chain governance proof verifier active claim is emitted.
* Existing custody-attestation policy selector behavior from Run 210 remains compatible.
* Existing KMS/HSM backend behavior from Run 204 remains compatible.
* Existing RemoteSigner transport behavior from Run 202 remains compatible.
* Existing custody policy selector behavior from Run 193 remains compatible.
* Existing governance proof carrier paths remain compatible.
* No validator-set rotation claim is emitted.

Required source/release reachability proof:
The report must include source grep/call-site proof for:

* `pqc_governance_execution_policy`;
* governance execution class enum;
* governance execution policy enum;
* governance action enum;
* quorum/threshold metadata;
* governance execution input;
* governance execution decision;
* governance execution expectations;
* input digest helper;
* decision digest helper;
* transcript digest helper;
* policy digest helper, if implemented;
* `GovernanceExecutionEvaluator`;
* fixture governance evaluator;
* production/on-chain/MainNet fail-closed evaluators;
* peer-driven guard composition;
* fail-closed helpers.

Required mutation / no-mutation proof:
For rejected governance-execution scenarios:

* no Run 070 apply call;
* no live trust swap;
* no session eviction;
* no sequence write;
* no marker write;
* marker bytes unchanged where present;
* sequence bytes unchanged where present;
* no `.tmp` residue;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead.

For accepted fixture governance mutating compatibility scenarios, if any mutating compatibility path is exercised:

* governance execution evaluation occurs before apply/mutation;
* lifecycle validation occurs before apply/mutation;
* governance proof validation occurs before apply/mutation where applicable;
* custody validation occurs before apply/mutation where applicable;
* custody attestation validation occurs before apply/mutation where applicable;
* Run 055 sequence commit succeeds if a mutating path is exercised;
* v2 marker persists strictly after sequence commit.

Required denylist:
Across the run prove:

* no MainNet apply;
* no autonomous apply;
* no apply on receipt;
* no peer-majority authority;
* no real governance execution claim;
* no production governance active claim;
* no MainNet governance enabled claim;
* no real on-chain governance proof verifier claim;
* no real KMS backend claim;
* no real HSM backend claim;
* no real RemoteSigner backend claim;
* no custody attestation production-active claim;
* no validator-set rotation claim;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead;
* no schema/wire/metric drift;
* no marker write before sequence commit;
* no sequence write on validation-only surfaces;
* no marker write on validation-only surfaces.

Required captured metadata:

* qbind-node SHA-256 and ELF Build ID;
* helper SHA-256 and ELF Build ID;
* git commit hash;
* rustc/cargo versions;
* exact commands;
* stdout/stderr logs;
* per-scenario exit codes;
* governance execution fixture input/decision paths and hashes;
* marker/sequence JSON and SHA before/after where applicable;
* data-dir inventories;
* denylist grep results.

Validation commands:
Run at minimum:

* `cargo build --release -p qbind-node --bin qbind-node`
* `cargo build --release -p qbind-node --example run_212_governance_execution_policy_release_binary_helper`
* `bash scripts/devnet/run_212_governance_execution_policy_release_binary.sh`
* `cargo test -p qbind-node --test run_211_governance_execution_policy_tests`
* `cargo test -p qbind-node --test run_209_custody_attestation_policy_selector_tests`
* `cargo test -p qbind-node --test run_207_custody_attestation_payload_callsite_tests`
* `cargo test -p qbind-node --test run_205_custody_attestation_verifier_tests`
* `cargo test -p qbind-node --test run_203_kms_hsm_backend_boundary_tests`
* `cargo test -p qbind-node --test run_201_remote_signer_transport_boundary_tests`
* `cargo test -p qbind-node --test run_198_remote_signer_policy_selector_tests`
* `cargo test -p qbind-node --test run_196_remote_signer_payload_callsite_tests`
* `cargo test -p qbind-node --test run_194_remote_authority_signer_boundary_tests`
* `cargo test -p qbind-node --test run_192_authority_custody_policy_selector_tests`
* `cargo test -p qbind-node --test run_190_authority_custody_payload_callsite_tests`
* `cargo test -p qbind-node --test run_188_authority_custody_boundary_tests`
* `cargo test -p qbind-node --test run_186_onchain_governance_production_verifier_boundary_tests`
* `cargo test -p qbind-node --test run_184_onchain_governance_payload_carrying_tests`
* `cargo test -p qbind-node --test run_182_onchain_governance_production_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_180_onchain_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_178_onchain_governance_proof_tests`
* `cargo test -p qbind-node --test run_176_live_0x05_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_173_validation_only_governance_required_policy_tests`
* `cargo test -p qbind-node --test run_171_governance_required_policy_selector_tests`
* `cargo test -p qbind-node --test run_169_governance_proof_loader_surface_integration_tests`
* `cargo test -p qbind-node --test run_167_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_165_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_163_governance_authority_verifier_tests`
* `cargo test -p qbind-node --test run_161_lifecycle_marker_integration_tests`
* `cargo test -p qbind-node --test run_159_authority_signing_key_lifecycle_tests`
* `cargo test -p qbind-node --test run_157_unified_testnet_fixture_universe_tests`
* `cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests`
* `cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests`
* `cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests`
* `cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests`
* `cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests`
* `cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Documentation requirements:
`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_212.md` must state:

* Run 212 is release-binary governance execution policy-boundary evidence.
* No real governance execution engine is implemented.
* No real on-chain governance proof verifier is implemented.
* Fixture governance execution is DevNet/TestNet evidence-only.
* Emergency council fixture is explicit and non-production.
* Production governance execution remains unavailable/fail-closed.
* MainNet governance execution remains unavailable/fail-closed.
* Governance execution boundary does not enable MainNet peer-driven apply.
* KMS/HSM/RemoteSigner/custody-attestation remain boundary-only.
* validator-set rotation remains unsupported.
* existing custody/KMS-HSM/RemoteSigner/custody-attestation/governance proof paths remain compatible.
* full C4 remains open.
* C5 remains open.

Acceptance criteria:
Run 212 is acceptable only if:

1. Release-built helper proves the Run 211 governance execution policy corpus.
2. Real `target/release/qbind-node` confirms MainNet peer-driven apply remains refused.
3. Fixture governance execution remains DevNet/TestNet evidence-only.
4. Emergency council fixture remains explicit and non-production.
5. Production and MainNet governance execution remain unavailable/fail-closed.
6. Governance input/decision/transcript digests are deterministic and domain-bound.
7. Governance execution authorizes lifecycle action only when action, candidate digest, and sequence match.
8. Validator-set rotation remains unsupported.
9. Rejected governance-execution cases produce no mutation.
10. Existing custody/KMS-HSM/RemoteSigner/custody-attestation/governance proof paths remain compatible.
11. No real governance execution / on-chain proof verifier / KMS-HSM backend / RemoteSigner backend / validator-set rotation claim is made.
12. No full C4 or C5 closure is claimed.
