Run 370 — Public DevNet abuse/DoS M12 deployed live-socket release-binary evidence

Continue QBIND after accepted Run 369.

Current state:
Run 369 is accepted as PASS / public-DevNet deployed per-peer limiter source-test positive.

Run 369 landed:
- `crates/qbind-node/src/deployed_inbound_per_peer_limiter.rs`;
- deployed TcpKemTls receive-path source/test wiring;
- `TcpKemTlsP2pService::read_loop` now consults the per-peer limiter after `decode_frame(Ok)` and before `inbound_tx.send`;
- over-budget frames are dropped with `continue`, not connection teardown;
- `P2pNodeBuilder::start()` installs the adapter from `deployed_peer_rate_limiter_config()`;
- default per-peer posture remains 1000 msg/s + 100 burst;
- connection-rate limiter remains disabled unless explicitly enabled;
- no new public CLI flag;
- no P2P wire-format change;
- no peer-admission / KEMTLS / trust-bundle weakening;
- no validator-set, epoch, sequence, marker, `LivePqcTrustState`, or authority mutation;
- Run 369 tests: 24 passed;
- predecessor tests: Run 365 = 20, Run 363 = 21, Run 362 = 38, Run 361 = 30, lib = 1390, all 0 failed;
- secret scan clean;
- CodeQL invoked but skipped due to database size, not claimed clean.

Known Run 369 limitation:
The deployed builder installs the adapter with `metrics = None` because it holds `P2pMetrics`, not the `NodeMetrics` that owns `qbind_net_per_peer_drops_total`. The adapter’s own atomic drop counter is always available, and `NodeMetrics` emission is tested, but a live deployed node does not yet prove the exported per-peer metric unless a live `NodeMetrics` handle is installed or an equivalent deployed metric path is exposed.

Current readiness:
- M12 = Yellow/Partial, strengthened by Run 369 source/test receive-path wiring.
- M12 is NOT Green.
- M4 remains Yellow/launch-blocking.
- M6 remains Yellow/Partial.
- Public DevNet remains NOT launch-ready.
- Full C4 OPEN.
- C5 OPEN.
- MainNet authority rotation/revocation Red.

Primary objective:
Produce release-binary live-socket deployed-path evidence that the running `target/release/qbind-node` enforces BOTH public DevNet M12 controls over real loopback sockets:

1. connection-rate limiting over the deployed accept loop;
2. per-peer message-rate limiting over the deployed TcpKemTls receive path wired in Run 369.

If needed, add only the narrowest source change required to install a live `NodeMetrics` handle, or an equivalent existing metrics-backed counter path, into the deployed `DeployedInboundPerPeerLimiter` so live `/metrics` can prove per-peer drops. Any such change must be default-preserving, non-mutating, and source/test + release-evidenced in this run.

Strict scope:
- M12 deployed live-socket evidence only.
- No public DevNet launch.
- No live external seed/bootnode deployment.
- No M4 Green.
- No M6 Green.
- No faucet.
- No RPC gateway.
- No explorer.
- No status page.
- No TestNet readiness claim.
- No MainNet readiness claim.
- No C4 closure.
- No C5 closure.
- No authority-lifecycle runtime wiring.
- No validator-set mutation.
- No epoch transition.
- No sequence/marker write.
- No `LivePqcTrustState` mutation.
- No P2P wire-format change.
- No peer-admission weakening.
- No KEMTLS/trust-bundle weakening.
- No new public CLI flags.
- Hidden/devnet-only abuse/DoS flags must remain hidden from `--help`.

Required investigation:
Inspect before implementation/evidence:

- `crates/qbind-node/src/deployed_inbound_per_peer_limiter.rs`
- `crates/qbind-node/src/p2p_tcp.rs`
- `crates/qbind-node/src/p2p_node_builder.rs`
- `crates/qbind-node/src/p2p_inbound.rs`
- `crates/qbind-node/src/metrics.rs`
- `crates/qbind-node/src/metrics_http.rs`
- `crates/qbind-node/src/async_peer_manager.rs`
- `crates/qbind-node/src/peer_rate_limiter.rs`
- `crates/qbind-node/src/public_devnet_abuse_dos_runtime.rs`
- `crates/qbind-node/src/public_devnet_abuse_dos_config.rs`
- `crates/qbind-node/src/main.rs`
- `crates/qbind-node/src/cli.rs`
- Run 367 live-socket connection-rate helper/harness
- Run 368 admitted-peer message-rate helper/harness
- Run 369 deployed receive-path source/test evidence
- `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_369.md`
- `docs/release/QBIND_PUBLIC_DEVNET_READINESS_CRITERIA.md`
- `docs/release/public-devnet/p2p/ABUSE_DOS_POSTURE.md`
- `docs/release/public-devnet/p2p/VERIFY.md`

Decision gate:
Before implementation, record which route applies.

A. Existing deployed binary path can expose both connection-rate and per-peer drop evidence over live sockets without source change.
   - Proceed with helper/harness/docs only.

B. A narrow metrics-threading source change is needed because Run 369 installs `metrics = None`.
   - Add the smallest safe source change to install a live `NodeMetrics` handle, or an equivalent existing exported metric/counter, into the deployed inbound limiter.
   - Preserve defaults.
   - Add source tests.
   - Produce release-binary evidence.

C. Deployed KEMTLS peer-message flooding is still blocked by missing test harness, admission setup, or handler accessibility.
   - Do not mark M12 Green.
   - Record the exact blocker and produce partial evidence only.

Expected implementation/evidence artifacts:
Add release helper:

`crates/qbind-node/examples/run_370_public_devnet_abuse_dos_m12_deployed_live_socket_helper.rs`

Add release harness:

`scripts/devnet/run_370_public_devnet_abuse_dos_m12_deployed_live_socket_release_binary.sh`

Add archive:

`docs/devnet/run_370_public_devnet_abuse_dos_m12_deployed_live_socket_release_binary/README.md`
`docs/devnet/run_370_public_devnet_abuse_dos_m12_deployed_live_socket_release_binary/summary.txt`
`docs/devnet/run_370_public_devnet_abuse_dos_m12_deployed_live_socket_release_binary/.gitignore`

Only those three archive files should be tracked. Raw logs, generated bundles/certs/keys, helper output, node logs, and temp data dirs must be gitignored.

Add canonical evidence:

`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_370.md`

If a narrow source change is needed, add source tests:

`crates/qbind-node/tests/run_370_public_devnet_deployed_live_socket_m12_tests.rs`

Required runtime evidence:
The Run 370 release harness must prove on real release artifacts:

1. `cargo build -p qbind-node --release` succeeds.
2. Run 370 helper builds in release mode.
3. qbind-node SHA-256 captured.
4. helper SHA-256 captured.
5. BuildIDs captured where available.
6. toolchain versions captured.
7. hidden abuse/DoS flags remain absent from `--help`.
8. real hidden flags parse.
9. invented flags are rejected by clap.
10. invalid zero connection-rate config fails closed.
11. invalid zero per-peer max fails closed.
12. unsafe/unbounded values fail closed.
13. MainNet abuse/DoS enablement refused.
14. default no-flag behavior preserves:
    - connection limiter disabled;
    - per-peer 1000 msg/s + 100 burst;
    - no unexpected drops.
15. real P2P-capable loopback node starts with:
    - `--network-mode p2p`;
    - `--enable-p2p`;
    - `--p2p-listen-addr 127.0.0.1:<port>`;
    - metrics endpoint enabled on loopback.
16. connection-rate live-socket proof:
    - under-budget inbound TCP connections admitted;
    - over-budget inbound TCP connections refused;
    - `qbind_p2p_connection_rate_drop_total` increments exactly by refusal count.
17. per-peer message-rate deployed-path proof:
    - a KEMTLS-admitted peer reaches the deployed TcpKemTls read loop;
    - custom `--p2p-max-messages-per-second` / `--p2p-burst-allowance` reach the deployed inbound limiter;
    - under-budget frames are forwarded;
    - over-budget frames are dropped before demuxer/handler dispatch;
    - connection remains alive unless existing semantics close it;
    - per-peer drop counter increments;
    - live exported metric/counter evidence is captured, preferably `qbind_net_per_peer_drops_total{reason="rate_limit"}`.
18. independence:
    - per-peer drops do not increment `qbind_p2p_connection_rate_drop_total`;
    - connection refusals do not increment per-peer message drops.
19. no public launch claim.
20. no M4/M6 Green claim.
21. no TestNet/MainNet readiness claim.
22. no C4/C5 closure claim.
23. no P2P wire-format change.
24. no peer-admission weakening.
25. no trust-bundle behavior weakening.
26. no `LivePqcTrustState` mutation.
27. no sequence/marker write.
28. no validator-set mutation.
29. no epoch transition.
30. no Run 070 path.

Required scenarios:
At minimum:

1. `default_preserves_behavior`
2. `hidden_cli_surface_checked`
3. `invalid_configs_fail_closed`
4. `mainnet_refused`
5. `connection_rate_live_socket_under_budget`
6. `connection_rate_live_socket_over_budget`
7. `per_peer_deployed_live_socket_under_budget`
8. `per_peer_deployed_live_socket_over_budget`
9. `combined_limiter_independence`
10. `metrics_export_live`

Tests to run and record:
If source changed:

- `cargo build -p qbind-node --lib`
- `cargo test -p qbind-node --test run_370_public_devnet_deployed_live_socket_m12_tests`

Always run:

- `cargo build -p qbind-node --release`
- `cargo build -p qbind-node --release --example run_370_public_devnet_abuse_dos_m12_deployed_live_socket_helper`
- Run the Run 370 harness.
- `cargo test -p qbind-node --test run_369_public_devnet_deployed_per_peer_limiter_wiring_tests`
- `cargo test -p qbind-node --test run_365_public_devnet_deployed_peer_rate_threading_tests`
- `cargo test -p qbind-node --test run_363_public_devnet_per_peer_message_rate_runtime_tests`
- `cargo test -p qbind-node --test run_362_public_devnet_abuse_dos_runtime_tests`
- `cargo test -p qbind-node --test run_361_public_devnet_abuse_dos_hardening_tests`
- `cargo test -p qbind-node --lib`

Docs to update narrowly:
- `docs/release/QBIND_PUBLIC_DEVNET_READINESS_CRITERIA.md`
- `docs/release/public-devnet/p2p/ABUSE_DOS_POSTURE.md`
- `docs/release/public-devnet/p2p/VERIFY.md`
- `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
- `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
- `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
- `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
- `docs/whitepaper/contradiction.md`

Readiness matrix rules:
- M12 may move Yellow/Partial → Green only if release-binary live-socket deployed-path evidence proves BOTH:
  1. connection-rate limiting over deployed live sockets;
  2. per-peer message-rate limiting over the deployed TcpKemTls receive path.
- If per-peer evidence is helper-only, adapter-only, source/test-only, plain-TCP-only, construction-path-only, or lacks live deployed metric/counter proof, keep M12 Yellow/Partial.
- Do not alter M4 unless live seed/bootnode reachability evidence lands.
- Do not alter M6 unless stable identity-generation support lands.
- Do not alter M7–M9/M13–M15 unless genuinely closed.
- Public DevNet remains NOT launch-ready unless all must-haves are Green.

Security:
Run secret scanning over all changed files.

No private keys, mnemonics, credentials, API keys, tokens, private infrastructure, real production hostnames, or unapproved live endpoints may be committed. Use localhost, RFC 5737, RFC 3849, temporary directories, and ignored generated test material only.

CodeQL:
If any Rust source or example is added/changed, invoke CodeQL if available.

If skipped, unavailable, timed out, database-too-large, or not meaningful, record the exact result and do not call it clean.

Expected verdict:
PASS / public-DevNet abuse/DoS M12 deployed live-socket release-binary positive if both controls are proven over deployed live sockets, defaults are preserved, hidden flags remain hidden, metrics/counters are correct, and all tests/scans pass.

Partial-positive if evidence improves but still lacks KEMTLS admission, deployed receive-path proof, live metric/counter proof, or requires another source run.

Unacceptable:
- marking M12 Green without both deployed live-socket controls proven;
- marking M4 Green without live seed/bootnode reachability;
- marking M6 Green without identity-generation support;
- exposing hidden abuse/DoS flags publicly;
- changing P2P wire format;
- weakening peer admission, KEMTLS, trust-bundle, genesis binding, or mutual-auth;
- silently changing defaults;
- incrementing connection-rate metric for per-peer message drops;
- publishing secrets;
- claiming public DevNet launch-ready;
- claiming TestNet/MainNet readiness;
- closing C4 or C5;
- hiding skipped CodeQL.

Final response format:
1. Exact verdict.
2. Files changed.
3. Release artifacts and hashes.
4. Decision gate route.
5. Runtime evidence shape.
6. Connection-rate live-socket evidence.
7. Per-peer deployed live-socket evidence.
8. Metrics/counter evidence.
9. CLI/config surface.
10. Default compatibility.
11. Rejection/fail-closed evidence.
12. Combined limiter independence.
13. Non-mutation evidence.
14. Readiness matrix delta for M12.
15. Current public DevNet readiness status.
16. Remaining public DevNet blockers.
17. Public TestNet blockers.
18. MainNet blockers.
19. C4/C5 status.
20. Tests run.
21. Security scans.
22. CodeQL.
23. Provenance.
24. Honest limitations.
25. Suggested Run 371 next step.