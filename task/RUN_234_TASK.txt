Task: Run 234 — Source/Test Governance Evaluator Replay Consume Post-Mutation Boundary

Current accepted state:

* Run 230: source/test governance evaluator replay/freshness state boundary landed.
* Run 231: release-binary replay/freshness state evidence fully closed.
* Run 232: source/test governance evaluator replay/freshness runtime integration landed.
* Run 233: release-binary governance evaluator replay/freshness runtime integration evidence fully closed:

  * release-built helper exercises Run 232 runtime integration symbols;
  * `ProceedFresh` is the only replay/freshness outcome that can authorize mutation;
  * `ProceedDeferred` is not approval;
  * expired/stale/replayed/consumed/superseded decisions fail closed before mutation;
  * read-only validation does not consume;
  * explicit consume remains fixture-only;
  * production/MainNet replay state remains unavailable/fail-closed;
  * MainNet peer-driven apply remains refused;
  * no RocksDB/file/schema/migration/storage-format change;
  * Full C4 remains OPEN;
  * C5 remains OPEN.
* Current remaining gap:
  Replay/freshness validation is now composed into the runtime integration path before mutation authorization, but the post-mutation consume boundary is not yet modeled as a strict after-success-only step. A governance decision must not be marked consumed before mutation succeeds, and must not be left untracked in fixture evidence after a successful mutation authorization path.

Objective:
Add a source/test-only post-mutation consume boundary for governance evaluator replay/freshness decisions.

This run must define and test a pure boundary that separates:

1. pre-mutation freshness validation;
2. mutation authorization;
3. successful mutation completion;
4. explicit replay-state consume after success only.

Production/MainNet consume backends must remain unavailable/fail-closed. Fixture consume remains DevNet/TestNet source-test only. This run must not implement persistent storage.

Release-binary evidence is deferred to Run 235.

Strict scope:

* Source/test only.
* No release-binary harness.
* No real governance execution engine.
* No real on-chain governance proof verifier.
* No MainNet governance enablement.
* No MainNet peer-driven apply enablement.
* No validator-set rotation.
* No KMS/HSM backend implementation.
* No RemoteSigner backend implementation.
* No RocksDB schema change.
* No file-format change.
* No database migration.
* No wire/schema/marker/sequence/trust-bundle change.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* Do not weaken Runs 070, 130–233.
* Do not claim full C4 or C5 closure.

Required source behavior:

1. Add a focused pure module, preferably:
   `crates/qbind-node/src/pqc_governance_evaluator_replay_consume_boundary.rs`

2. Define typed consume inputs:

   * replay state key digest;
   * evaluator source identity digest;
   * evaluator request digest;
   * evaluator response digest;
   * evaluator transcript digest;
   * governance execution decision digest;
   * proposal id;
   * decision id;
   * lifecycle action;
   * candidate digest;
   * authority-domain sequence;
   * effective epoch;
   * expiry epoch;
   * replay nonce;
   * environment;
   * chain id;
   * genesis hash;
   * validation surface;
   * mutation surface;
   * mutation authorization outcome;
   * mutation completion status.

3. Define mutation completion status:

   * `NotAttempted`;
   * `AuthorizedButNotApplied`;
   * `AppliedSuccessfully`;
   * `ApplyFailed`;
   * `RolledBack`;
   * `ValidationOnly`;
   * `UnsupportedSurface`;
   * `MainNetRefused`.

4. Define consume boundary outcome:

   * `DoNotConsumeLegacyBypass`;
   * `DoNotConsumeDeferred`;
   * `DoNotConsumeValidationOnly`;
   * `DoNotConsumeBeforeApply`;
   * `DoNotConsumeApplyFailed`;
   * `DoNotConsumeRolledBack`;
   * `DoNotConsumeUnsupportedSurface`;
   * `DoNotConsumeMainNetRefused`;
   * `ConsumeFixtureAfterSuccess`;
   * `FailClosedConsumeUnavailable`;
   * `FailClosedProductionConsumeUnavailable`;
   * `FailClosedMainNetConsumeUnavailable`;
   * `FailClosedWrongBinding`.

5. Deterministic digest helpers:

   * consume authorization digest;
   * consume transcript digest;
   * post-mutation consume record digest.

6. Compose with existing Run 230 reader/writer traits where feasible:

   * fixture writer may record consumed only after `AppliedSuccessfully`;
   * production writer must be callable but unavailable/fail-closed;
   * MainNet writer must be callable but unavailable/fail-closed;
   * no real persistent storage.

7. Preserve strict ordering:

   * replay/freshness validation first;
   * evaluator runtime integration authorization;
   * mutation attempt;
   * mutation success proof;
   * consume only after success.

8. Preserve non-mutation:
   Consume boundary must never itself perform:

   * Run 070 call;
   * live trust swap;
   * session eviction;
   * sequence write;
   * marker write.

9. Preserve validation-only behavior:
   Validation-only surfaces must never mark replay state consumed.

10. Preserve MainNet refusal:
    MainNet peer-driven apply remains refused and must never consume.

Required accepted scenarios:
A1. legacy bypass does not consume.

A2. `ProceedDeferred` does not consume.

A3. validation-only success does not consume.

A4. authorized-but-not-applied does not consume.

A5. apply failed does not consume.

A6. rolled back mutation does not consume.

A7. unsupported surface does not consume.

A8. MainNet refused does not consume.

A9. DevNet fixture consume records consumed only after `AppliedSuccessfully`.

A10. TestNet fixture consume records consumed only after `AppliedSuccessfully`.

A11. after fixture consume, same decision validates as already consumed / fail-closed through Run 230 state.

A12. consume authorization digest is deterministic.

A13. consume transcript digest is deterministic.

A14. post-mutation consume record digest is deterministic.

A15. consume binding includes replay state key, request digest, response digest, decision digest, lifecycle action, candidate digest, sequence, replay nonce, environment, chain id, genesis hash, validation surface, mutation surface, and mutation completion status.

A16. production consume writer is callable and fails closed unavailable.

A17. MainNet consume writer is callable and fails closed unavailable/refused.

A18. Run 232 replay/freshness runtime integration remains compatible when consume boundary is not wired.

Required rejection scenarios:
R1. wrong replay state key digest rejected.

R2. wrong source identity digest rejected.

R3. wrong request digest rejected.

R4. wrong response digest rejected.

R5. wrong transcript digest rejected.

R6. wrong decision digest rejected.

R7. wrong proposal id rejected.

R8. wrong decision id rejected.

R9. wrong lifecycle action rejected.

R10. wrong candidate digest rejected.

R11. wrong authority-domain sequence rejected.

R12. wrong effective epoch rejected.

R13. wrong expiry epoch rejected.

R14. wrong replay nonce rejected.

R15. wrong environment rejected.

R16. wrong chain rejected.

R17. wrong genesis rejected.

R18. wrong validation surface rejected.

R19. wrong mutation surface rejected.

R20. consume attempted before apply rejected.

R21. consume attempted after failed apply rejected.

R22. consume attempted after rollback rejected.

R23. consume attempted on validation-only surface rejected.

R24. consume attempted on unsupported surface rejected.

R25. production consume unavailable rejected.

R26. MainNet consume unavailable/refused rejected.

R27. local operator cannot satisfy consume policy.

R28. peer majority cannot satisfy consume policy.

R29. validator-set rotation unsupported rejected.

R30. policy-change action unsupported rejected.

R31. malformed consume state rejected.

R32. consume rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

R33. MainNet peer-driven apply remains refused and does not consume even if replay state is fresh.

Required tests:
Add focused tests, preferably:

`crates/qbind-node/tests/run_234_governance_evaluator_replay_consume_boundary_tests.rs`

Minimum coverage:

* A1–A18.
* R1–R33.
* deterministic digest tests.
* consume-after-success-only tests.
* validation-only never consumes tests.
* failed/rolled-back mutation never consumes tests.
* fixture consume updates state only after explicit success call.
* production/MainNet consume unavailable tests.
* non-mutation tests.
* MainNet refusal tests.
* compatibility with Runs 233, 232, 231, 230, 228, 226, 224.

Required documentation:
Add:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_234.md`

Update:

* `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
* `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
* `docs/whitepaper/contradiction.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

Documentation must state:

* Run 234 is source/test post-mutation replay consume boundary work.
* Consume is modeled as after-success-only.
* Deferred, validation-only, failed apply, rollback, unsupported surface, and MainNet refused outcomes do not consume.
* Fixture consume remains DevNet/TestNet source-test only.
* Production/MainNet consume remains unavailable/fail-closed.
* No RocksDB/file/schema/migration/storage-format change is implemented.
* No real governance engine or on-chain proof verifier is implemented.
* MainNet peer-driven apply remains refused.
* Validator-set rotation remains unsupported.
* Release-binary consume-boundary evidence is deferred to Run 235.
* Full C4 remains OPEN.
* C5 remains OPEN.

Validation commands:
Run at minimum:

* `cargo build -p qbind-node --lib`
* `cargo test -p qbind-node --test run_234_governance_evaluator_replay_consume_boundary_tests`
* `cargo test -p qbind-node --test run_232_governance_evaluator_replay_runtime_integration_tests`
* `cargo test -p qbind-node --test run_230_governance_evaluator_replay_state_tests`
* `cargo test -p qbind-node --test run_228_peer_evaluator_context_representation_tests`
* `cargo test -p qbind-node --test run_226_governance_evaluator_runtime_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_224_governance_evaluator_runtime_integration_tests`
* `cargo test -p qbind-node --test run_222_governance_execution_evaluator_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

Acceptance criteria:
Run 234 is acceptable only if:

1. A typed post-mutation consume boundary exists.
2. Consume is allowed only after successful mutation completion.
3. Deferred is not consumed.
4. Validation-only is not consumed.
5. Failed/rolled-back mutation is not consumed.
6. Fixture consume remains DevNet/TestNet source-test only.
7. Production/MainNet consume remains unavailable/fail-closed.
8. Rejections are non-mutating.
9. MainNet peer-driven apply remains refused and does not consume.
10. No storage/schema/migration/RocksDB/file-format change is claimed.
11. Release-binary evidence is deferred to Run 235.
12. No full C4 or C5 closure is claimed.
