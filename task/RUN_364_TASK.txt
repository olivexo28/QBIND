Run 364 — Release-binary evidence for public DevNet abuse/DoS M12 completion

Continue QBIND after accepted Run 363.

Current state:
Run 363 is accepted as PASS / public-DevNet-per-peer-message-rate runtime source-test positive.

Run 362 wired the public DevNet connection-rate limiter into the live `p2p_tcp` accept path, added `qbind_p2p_connection_rate_drop_total`, exposed hidden/devnet-only operator CLI/config flags, and captured release-binary evidence. Run 362 remained Partial-positive because the per-peer message-rate flags were parsed/validated but inert against the live `PeerRateLimiter`.

Run 363 wires the validated per-peer message-rate thresholds into the live peer-manager construction path at source/test level:
`CliArgs::abuse_dos_runtime_config()` → `PublicDevnetAbuseDosRuntimeConfig::peer_rate_limiter_config()` → `AsyncPeerManagerConfig::with_peer_rate_limiter_config(Some(cfg))` → `AsyncPeerManagerImpl::new/with_metrics` → `PeerRateLimiter::new(cfg)`.

Run 363 is source/test only. No release-binary evidence was produced in Run 363. M12 is Yellow/stronger, not Green.

Current public DevNet readiness:
- Green: M1, M2, M3 same-host scope, M5, M10, M11, M16, M17, M18, M19, M20.
- Yellow/Partial or unresolved: M4, M6, M7, M8, M9, M12, M13, M14, M15.
- M4 remains launch-blocking.
- Public DevNet remains NOT launch-ready.
- Full C4 OPEN.
- C5 OPEN.
- MainNet authority rotation/revocation Red.

Primary objective:
Produce release-binary evidence that the production `qbind-node` binary path honors both public DevNet abuse/DoS runtime controls:

1. runtime-wired connection-rate limiting from Run 362;
2. runtime-wired per-peer message-rate override from Run 363.

If the release-binary evidence genuinely proves operator-configurable connection-rate and per-peer message-rate runtime posture, with defaults preserved and all non-claims intact, then M12 may move Yellow/stronger → Green for public DevNet scope.

Strict scope:
Release-binary evidence for M12 only.

No public DevNet launch.
No live seed-node deployment.
No bootnode deployment.
No faucet.
No RPC gateway.
No explorer.
No status page.
No MainNet enablement.
No TestNet readiness claim.
No C4 closure.
No C5 closure.
No authority-lifecycle runtime wiring.
No validator-set mutation.
No production epoch transition.
No execution-sink write.
No P2P wire-format change.
No peer-admission weakening.
No trust-bundle behavior weakening.
No default-open public port.
No M4 Green.
No M6 Green.
No public DevNet launch-ready claim unless every must-have is Green, which is not expected.

Required investigation:
Inspect:

- `crates/qbind-node/src/public_devnet_abuse_dos_config.rs`
- `crates/qbind-node/src/public_devnet_abuse_dos_runtime.rs`
- `crates/qbind-node/src/peer_rate_limiter.rs`
- `crates/qbind-node/src/async_peer_manager.rs`
- `crates/qbind-node/src/p2p_tcp.rs`
- `crates/qbind-node/src/p2p_node_builder.rs`
- `crates/qbind-node/src/cli.rs`
- `crates/qbind-node/src/main.rs`
- `crates/qbind-node/src/metrics.rs`
- Run 361, Run 362, and Run 363 tests/evidence
- `docs/release/public-devnet/p2p/ABUSE_DOS_POSTURE.md`
- `docs/release/public-devnet/p2p/VERIFY.md`
- `docs/release/QBIND_PUBLIC_DEVNET_READINESS_CRITERIA.md`

Required implementation:
Add release-binary helper:

`crates/qbind-node/examples/run_364_public_devnet_abuse_dos_m12_release_binary_helper.rs`

Add release harness:

`scripts/devnet/run_364_public_devnet_abuse_dos_m12_release_binary.sh`

Add evidence archive:

`docs/devnet/run_364_public_devnet_abuse_dos_m12_release_binary/README.md`
`docs/devnet/run_364_public_devnet_abuse_dos_m12_release_binary/summary.txt`
`docs/devnet/run_364_public_devnet_abuse_dos_m12_release_binary/.gitignore`

Only those three archive files should be tracked. Raw logs, helper JSON, temp binaries, and generated output must be gitignored.

Add canonical evidence:

`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_364.md`

Update narrowly:

- `docs/release/QBIND_PUBLIC_DEVNET_READINESS_CRITERIA.md`
- `docs/release/public-devnet/p2p/ABUSE_DOS_POSTURE.md`
- `docs/release/public-devnet/p2p/VERIFY.md`
- `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
- `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
- `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
- `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
- `docs/whitepaper/contradiction.md`

Do not modify source unless a compile-only evidence-helper visibility correction is required. If any source change is needed, document it explicitly and keep it narrowly bounded.

Release-binary evidence must prove:

1. real `target/release/qbind-node` builds successfully;
2. release helper builds successfully;
3. `qbind-node` SHA-256 is captured;
4. helper SHA-256 is captured;
5. toolchain versions are captured;
6. BuildID / metadata are captured where available;
7. helper links real Run 361/362/363 symbols in release mode;
8. default no-flag behavior preserves current behavior;
9. connection-rate limiter remains disabled by default;
10. per-peer limiter defaults remain `1000` msg/s + `100` burst by default;
11. configured connection-rate limiter allows under-budget connections;
12. configured connection-rate limiter refuses over-budget connections;
13. `qbind_p2p_connection_rate_drop_total` increments on connection-rate refusal;
14. the drop metric does not increment on allowed connections;
15. configured per-peer message-rate limit allows under-budget messages;
16. configured per-peer message-rate limit drops over-budget messages;
17. custom `--p2p-max-messages-per-second` reaches the live peer-manager limiter construction path;
18. custom `--p2p-burst-allowance` reaches the live peer-manager limiter construction path;
19. connection-rate limiting and message-rate limiting are independent;
20. invalid zero message-rate config fails closed;
21. invalid zero connection-rate config fails closed;
22. unbounded/too-large message-rate config fails closed;
23. inconsistent per-address connection-rate config fails closed;
24. MainNet is refused;
25. hidden/devnet-only flags are absent from `--help`;
26. real hidden flags parse;
27. invented flags are rejected by clap;
28. no public DevNet launch claim;
29. no M4 Green;
30. no M6 Green;
31. no TestNet readiness claim;
32. no MainNet readiness claim;
33. no C4/C5 closure claim;
34. no P2P wire-format change;
35. no peer-admission weakening;
36. no trust-bundle behavior weakening;
37. no `LivePqcTrustState` mutation;
38. no sequence/marker write;
39. no validator-set mutation;
40. no epoch transition;
41. no Run 070 path.

Required helper scenarios:
The helper should include at minimum:

1. `default_preserves_behavior`
   - no abuse/DoS flags/config;
   - connection limiter disabled;
   - per-peer defaults `1000` / `100`;
   - drop metric remains zero.

2. `connection_rate_enabled_allows_then_refuses`
   - configure a small connection-rate bucket;
   - prove first N connections allowed;
   - prove N+1 refused;
   - prove metric increments exactly once.

3. `per_peer_message_rate_enabled_allows_then_drops`
   - configure a small per-peer message bucket;
   - prove first N messages allowed;
   - prove N+1 dropped;
   - prove connection-rate metric does not increment for message-rate drop unless a separate message-drop metric already exists.

4. `combined_connection_and_message_rate_independent`
   - configure both;
   - prove connection-rate refusal does not mark peer admitted;
   - prove message-rate drop occurs only after admitted peer/message path;
   - prove counters remain distinct.

5. `invalid_config_fails_closed`
   - zero window / zero max / unbounded unsafe values rejected.

6. `mainnet_refused`
   - MainNet abuse/DoS config refused.

7. `cli_surface_hidden_and_parse_checked`
   - hidden flags absent from `--help`;
   - real hidden flags parse;
   - invented flag rejected.

Required production-binary scenarios:
Run and record:

1. `qbind-node --help` hides all hidden/devnet-only Run 362/363 abuse/DoS flags.
2. Real hidden Run 362/363 abuse/DoS flags parse successfully on DevNet.
3. Invalid abuse/DoS config exits non-zero.
4. MainNet abuse/DoS enablement exits non-zero.
5. Invented abuse/DoS flag exits with clap error.
6. Default DevNet/TestNet/MainNet surfaces remain unchanged.
7. No new public CLI flag is exposed accidentally.

Required tests:
Run and record:

- `cargo build -p qbind-node --release`
- `cargo build -p qbind-node --release --example run_364_public_devnet_abuse_dos_m12_release_binary_helper`
- Run the Run 364 harness.
- `cargo test -p qbind-node --test run_363_public_devnet_per_peer_message_rate_runtime_tests`
- `cargo test -p qbind-node --test run_362_public_devnet_abuse_dos_runtime_tests`
- `cargo test -p qbind-node --test run_361_public_devnet_abuse_dos_hardening_tests`
- `cargo test -p qbind-node --lib`

Record the exact pass/fail counts. In particular, reconcile the Run 361 target count if it differs from prior reports.

Security:
Run secret scanning over all changed files.

No private keys, mnemonics, seed phrases, credentials, API keys, tokens, private infrastructure, real production hostnames, or unapproved live endpoints may be introduced.

Use RFC 5737, RFC 3849, localhost, or explicit non-live values only.

CodeQL:
If no source changes occur and this is evidence/docs/helper/harness-only, record whether CodeQL is meaningful for the diff.

If source changes occur, invoke CodeQL if available.

If CodeQL is skipped, timed out, unavailable, database-too-large, or trivial-change, record the exact result and do not call it clean.

Provenance:
Record in `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_364.md`:

- git commit;
- branch;
- clean/dirty state;
- files changed;
- helper/harness paths;
- release binary SHA-256;
- helper SHA-256;
- BuildIDs where available;
- toolchain versions;
- exact helper scenarios;
- exact production binary scenarios;
- test counts;
- secret scan result;
- CodeQL result;
- readiness matrix delta for M12;
- exact reason M12 is or is not Green.

Readiness matrix rules:
- M12 may move Yellow/stronger → Green only if release-binary evidence proves both runtime/operator-configurable connection-rate limiting and runtime/operator-configurable per-peer message-rate limiting, with defaults preserved.
- If either connection-rate evidence or per-peer message-rate evidence is incomplete, keep M12 Yellow/Partial.
- Do not alter M4 unless live seed/bootnode reachability evidence lands.
- Do not alter M6 unless stable identity-generation support lands.
- Do not alter M7–M9/M13–M15 unless genuinely closed.
- Public DevNet remains NOT launch-ready unless all must-haves are Green.

Expected verdict:
PASS / public-DevNet-abuse-DoS-M12 release-binary positive if the helper, harness, binary scenarios, tests, docs, secret scan, CodeQL provenance, and readiness update land, and M12 Green is justified by evidence.

Partial-positive if release evidence lands but M12 cannot honestly move Green.

Unacceptable:
- claiming public DevNet launch-ready;
- marking M12 Green without both connection-rate and per-peer message-rate release-binary evidence;
- marking M4 Green without live seed/bootnode reachability evidence;
- marking M6 Green without identity-generation support;
- exposing hidden/devnet-only abuse/DoS flags in public help unintentionally;
- silently changing defaults;
- weakening peer admission;
- changing P2P wire format;
- publishing fake live seed addresses;
- publishing secrets or private infrastructure;
- claiming TestNet readiness;
- claiming MainNet readiness;
- closing C4 or C5;
- adding authority-lifecycle runtime wiring;
- deploying seed nodes, faucet, RPC, explorer, or status page in this run;
- hiding skipped CodeQL;
- using marketing language.

Final response format:
1. Exact verdict.
2. Files changed.
3. Release artifacts and hashes.
4. Helper/harness corpus.
5. Production-binary scenarios.
6. Connection-rate runtime evidence.
7. Per-peer message-rate runtime evidence.
8. Combined limiter independence evidence.
9. Rejection/fail-closed evidence.
10. Default compatibility.
11. CLI/config surface.
12. Metric evidence.
13. Non-mutation evidence.
14. Readiness matrix delta for M12.
15. Current public DevNet readiness status.
16. Remaining public DevNet blockers.
17. Public TestNet blockers.
18. MainNet blockers.
19. C4/C5 status.
20. Tests run.
21. Security scans.
22. CodeQL.
23. Provenance.
24. Honest limitations.
25. Suggested Run 365 next step.