# Run 417 corrective/hardening pass — reconcile runtime-security audit with launch governance

## Starting state

Continue from the completed Run 417 commit:

* Expected Run 417 commit: `688dc7c2bcf41be0b154dafbd8b39d3222244477`
* Expected branch: `copilot/run-417-add-new-feature`
* Run 417 verdict remains:

  * `AUDIT-COMPLETE`
  * `NEGATIVE-FOR-RUNTIME-SECURITY`
* Run 417 found F1–F8, including reachable Critical consensus-authentication failures.

If the branch or commit differs, report the actual state before editing. Do not force-push, rewrite history, merge unrelated branches, or open a PR. Add a corrective commit on top of Run 417 and push it to the same branch.

## Objective

Correct the Run 417 documentation, validation record, artifact discoverability, and launch-governance integration before any Run 418 production implementation begins.

This is still part of Run 417. Do not create Run 418 evidence and do not implement F6 in this pass.

## Strict scope

Allowed:

* Markdown, text, JSON, and shell verification-harness changes.
* Artifact hashes and byte-size updates required by changed publish-safe files.
* Removal of the completed `task/RUN_417_TASK.txt` after its instructions have been consumed.
* Strengthening fail-closed documentation consistency checks.

Forbidden:

* No production Rust behavior change.
* No `.rs`, `Cargo.toml`, `Cargo.lock`, `build.rs`, dependency, protocol-wire, storage, CLI, port, or deployment change.
* No seed provisioning or `devnet-seeds.live.json`.
* No runtime mutation.
* No TestNet/MainNet readiness claim.
* No M4/M6/S5/S7 Green move.
* No C4/C5 closure.
* No suppression, weakening, or reclassification of F1–F8 without new evidence.

## Required corrections

### 1. Add an explicit foundational runtime-security launch blocker

Run 417 found reachable Critical/High fail-open consensus controls, but the existing launch rule is centered on M1–M20, while C4/C5 are described as separately tracked.

Add an explicit blocker named consistently, preferably:

`RS1 — Foundational runtime authentication and authorization`

RS1 must be `OPEN / launch-blocking`.

Public DevNet GO must require both:

1. Every required M1–M20 item is Green.
2. RS1 is closed with executable evidence showing that the deployed consensus path is fail-closed.

RS1 must track at least:

* F3 unsigned proposal/vote emission.
* F4 inbound consensus messages accepted without required signature, membership, and suite verification.
* F5 optional/disabled timeout and new-view verification.
* F6 missing authenticated KEMTLS peer-to-consensus-sender binding.
* F7 quorum certificates imported without verified signer evidence.
* F8 toy/default consensus suite accepted on the deployed path.
* F1/F2 transaction-authentication gaps must remain tracked and must be resolved before transaction ingress is enabled; their current lack of ingress is a reachability mitigation, not cryptographic closure.

Add this blocker consistently to:

* `docs/release/public-devnet/BLOCKER_REGISTER.md`
* `docs/release/public-devnet/LAUNCH_GO_NO_GO.md`
* `docs/release/QBIND_PUBLIC_DEVNET_READINESS_CRITERIA.md`
* `docs/release/public-devnet/ARTIFACT_INDEX.md`
* `docs/release/public-devnet/OPERATOR_VERIFICATION_MAP.md`
* `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
* `docs/protocol/QBIND_FOUNDATIONAL_RUNTIME_SECURITY_RECONCILIATION.md`
* `docs/whitepaper/contradiction.md`
* Run 417 canonical evidence and archive summary/README where applicable.

Do not invent M21 unless the repository’s existing schema and documents clearly require that approach. A separately named RS1 blocker is preferred because existing lints freeze M1–M20 coverage.

Strengthen the relevant verification harness so removal of RS1 from the launch gate, blocker register, or readiness record fails closed.

### 2. Correct stale Run 416 reachability wording

Inspect `docs/release/public-devnet/LAUNCH_GO_NO_GO.md` and related operator-facing documents.

Remove statements claiming that no real external/off-host reachability evidence exists.

Use the accurate posture:

* Run 416 produced operator-attested Route A evidence of external TCP and KEMTLS mutual-auth static-root reachability.
* It used a temporary seed identity that was discarded.
* It did not establish a durable operator-controlled seed identity.
* Genesis pinning was not evidenced.
* `devnet-seeds.live.json` remains absent.
* Therefore M4 remains Yellow and launch-blocking.

Do not convert operator-attested transcriptions into independently authenticated raw evidence.

### 3. Resolve the `cargo fmt` validation contradiction

Run the following against the actual corrective tree:

```bash
cargo fmt --all -- --check
```

Record the real exit code and relevant output.

The current records conflict:

* The Copilot final report says formatting detects a pre-existing difference.
* Run 417 `commands.txt` and canonical evidence say formatting was clean with exit 0.

Correct every affected Run 417 record to match the observed result.

If it fails:

* Do not modify unrelated Rust files.
* Demonstrate whether it also fails on the pre-Run-417 parent/base before calling it pre-existing.
* Record the affected path and nonzero exit code accurately.
* State that Run 417 changed no Rust source and claims no formatting pass.

If it passes, record the successful command and exit code. Do not infer either outcome.

### 4. Correct CodeQL reporting

Replace circular or incomplete wording such as “recorded in final response.”

Record one exact outcome:

* `EXECUTED`, including result; or
* `SKIPPED / NOT APPLICABLE`, because the corrective changes contain no analyzable production-code change.

If skipped, explicitly state:

* No CodeQL coverage is claimed.
* Secret scanning is separate and does not substitute for CodeQL.

Keep this consistent in `commands.txt`, canonical evidence, archive summary, and final report.

### 5. Repair readiness-document structure

Inspect the surrounding text where the Run 417 changelog entry was inserted in:

`docs/release/QBIND_PUBLIC_DEVNET_READINESS_CRITERIA.md`

The Run 417 entry appears to interrupt an older historical paragraph before text beginning with language similar to “identity generation + verification package...”.

Move the Run 417 entry to a proper standalone changelog boundary. Preserve all historical text and headings. Do not silently delete or rewrite earlier run history.

### 6. Correct contradiction-ledger semantics

Do not state simply that Run 417 found “no new contradiction.”

Use precise wording:

* Run 417 does not contradict the historical decision to keep C4/C5 OPEN.
* It discovered explicit deployed-path runtime-security gaps.
* It also exposed a launch-governance coverage gap because those findings were not represented by an independent launch blocker.
* This corrective pass closes the documentation/governance gap by adding RS1.
* It does not resolve F1–F8 and does not change the runtime-security verdict.

### 7. Correct the F6 sequencing statement

F6 is a suitable first narrow implementation target, but do not claim that peer-identity binding is what makes cryptographic signature verification meaningful.

Use this distinction:

* F6 is required for binding an authenticated KEMTLS peer/session to an authorized consensus sender and for transport-level accountability.
* Proposal, vote, timeout, new-view, and QC signatures remain independently necessary for message-level cryptographic authorship.
* Closing F6 alone will not close F3, F4, F5, F7, F8, RS1, C4, or C5.

The future Run 418 design must derive the remote `NodeId` from the authenticated KEMTLS session and resolve it through an authoritative, unambiguous `NodeId → ValidatorId` mapping. Unknown, duplicate, ambiguous, or mismatched identities must fail closed. Do not implement that design in this corrective pass.

### 8. Make Run 417 artifacts operator-discoverable

Add accurate references for the following, as appropriate:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_417.md`
* `docs/protocol/QBIND_FOUNDATIONAL_RUNTIME_SECURITY_RECONCILIATION.md`
* `docs/devnet/run_417_foundational_runtime_security_reconciliation_audit/`
* `scripts/devnet/run_417_foundational_runtime_security_reconciliation_audit.sh`

Update:

* `ARTIFACT_INDEX.md`
* `OPERATOR_VERIFICATION_MAP.md`
* Any package-integrity anchor hashes or byte sizes affected by these documentation edits.

Do not falsely classify Run 417 as launch evidence, runtime remediation, or binary provenance.

### 9. Strengthen the Run 417 harness

The Run 417 harness must continue reporting:

* `RESULT=POSITIVE-FOR-AUDIT-COMPLETENESS`
* `SECURITY_VERDICT=NEGATIVE-FOR-RUNTIME-SECURITY`

Add fail-closed checks confirming:

* RS1 exists and is OPEN/launch-blocking.
* The GO rule cannot pass merely because M1–M20 are Green while RS1 is OPEN.
* F1–F8 remain recorded.
* F3/F4/F6/F7 remain Critical and reachable.
* F5/F8 remain High and reachable unless new evidence proves otherwise.
* Run 416 is described accurately.
* M4/M6/S5/S7 remain Yellow.
* C4/C5 remain OPEN.
* `devnet-seeds.live.json` remains absent.
* No TestNet/MainNet readiness or deployment claim exists.
* No production source was changed.

Where practical, include a negative self-test proving that deleting or closing the RS1 guard causes the harness to fail.

## Required verification

Run and record exact exit codes for:

```bash
git diff --check
cargo fmt --all -- --check
cargo test -p qbind-ledger --test tx_auth_verifier_tests
cargo test -p qbind-consensus --lib
bash scripts/devnet/run_404_public_devnet_package_integrity_manifest.sh
bash scripts/devnet/run_405_public_devnet_full_tree_package_integrity.sh
bash scripts/devnet/run_410_public_devnet_package_integrity_stale_prose_lint.sh
bash scripts/devnet/run_411_public_devnet_readiness_contradiction_ledger_lint.sh
bash scripts/devnet/run_412_public_devnet_readiness_status_blocker_lint.sh
bash scripts/devnet/run_413_public_devnet_readiness_recommendation_gap_matrix_lint.sh
bash scripts/devnet/run_414_public_devnet_readiness_cross_section_coverage_lint.sh
bash scripts/devnet/run_415_public_devnet_readiness_artifact_path_reference_lint.sh
bash scripts/devnet/run_417_foundational_runtime_security_reconciliation_audit.sh
```

Also verify:

```bash
(cd docs/devnet/run_417_foundational_runtime_security_reconciliation_audit && sha256sum -c SHA256SUMS.txt)
```

Run JSON validation on every modified JSON file.

Run a tracked-tree secret/private-material scan. Confirm no private keys, KEM secrets, credentials, tokens, raw data directories, or private operator identifiers were added.

Confirm the complete Run 417 changeset contains no production Rust or dependency-file changes.

If an existing harness must be updated to recognize RS1, modify it narrowly and preserve all historical protections. Do not weaken frozen M1–M20 or S1–S7 checks.

## Status that must remain after correction

* Run 417: audit-complete.
* Runtime-security verdict: negative.
* RS1: OPEN / launch-blocking.
* M4: Yellow / launch-blocking.
* M6: Yellow / Partial.
* S5 and S7: Yellow.
* Public DevNet: NO-GO / NOT launch-ready.
* C4 and C5: OPEN.
* TestNet/MainNet: untouched.
* `devnet-seeds.live.json`: absent.
* No production remediation claim.
* No F1–F8 closure claim.

## Commit and final report

Delete `task/RUN_417_TASK.txt` as a completed transient task file unless repository policy explicitly requires retaining it. Report the decision.

Commit and push the corrective changes to the same Run 417 branch. Do not amend or force-push the existing commit. Do not open a PR.

Final response must include:

1. Branch and new corrective commit SHA.
2. Exact files changed.
3. RS1 definition and exact launch rule.
4. Corrected Run 416 wording.
5. Actual `cargo fmt` result and evidence for any “pre-existing” classification.
6. Exact CodeQL status.
7. Run 417 harness verdict and exit code.
8. Results and exit codes for Runs 404/405/410–415.
9. SHA256 verification.
10. Secret/privacy scan result.
11. Confirmation that no production Rust/dependency/runtime/deployment change occurred.
12. Preserved readiness statuses and remaining F1–F8 findings.
13. Confirmation that no PR was opened.
