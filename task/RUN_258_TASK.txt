Run 258 continuation — Complete source/test durable-completion audit-publication receipt boundary evidence

You are continuing QBIND Run 258 after a partial-positive checkpoint.

Current partial Run 258 state:

The core module has already landed and compiles:

* `crates/qbind-node/src/pqc_governance_durable_completion_audit_publication_receipt.rs`
* `crates/qbind-node/src/lib.rs` module registration

The module reportedly includes:

* typed Kind / Policy / Identity;
* domain-separated digest helpers;
* Request / Response / Record;
* in-memory ledger with snapshot / restore;
* Expectations and Input;
* MainNet peer-driven refusal extended through the Run 256 backend binding;
* 16-variant Outcome;
* RequestIntent projection from Run 256 `BackendSubmissionRecorded`;
* Fault;
* sink trait;
* fixture + production/MainNet/external sinks;
* evaluate;
* 21-variant recovery Window;
* recover;
* free helper functions;
* grep-verifiable invariant helpers.

The module builds with:

* `cargo build -p qbind-node --lib`

Secret scanning was clean.

But Run 258 is not yet closed. The following required items were not completed:

* Run 258 test file.
* Run 258 regression corpus.
* Run 258 evidence doc.
* narrow updates to protocol/runbook/trust docs.
* `contradiction.md` “no Run 258 contradiction” entry.
* CodeQL result.

Strict workflow:

* This is still Run 258, not Run 259.
* Do not add a new feature boundary.
* Do not implement release-binary evidence in this continuation.
* Do not enable production runtime behavior.
* Do not implement a real audit ledger.
* Do not implement real external publication.
* Do not implement real persistent storage.
* Do not change RocksDB/file/schema/migration/storage format.
* Do not change wire/schema/marker/sequence/trust-bundle formats.
* Do not call Run 070.
* Do not mutate `LivePqcTrustState`.
* Do not perform a real trust swap.
* Do not evict sessions.
* Do not write sequence files.
* Do not write authority markers.
* Do not enable MainNet governance.
* Do not enable MainNet peer-driven apply.
* Do not implement validator-set rotation.
* Keep Full C4 OPEN.
* Keep C5 OPEN.

Exact objective:

Complete the missing Run 258 source/test deliverables and documentation so Run 258 can be honestly accepted as source/test positive.

Required implementation work:

1. Add the Run 258 test file:

   `crates/qbind-node/tests/run_258_durable_completion_audit_publication_receipt_tests.rs`

2. The tests must consume the actual Run 258 module and actual Run 256 `BackendSubmissionRecorded` path. Do not fake an unattached receipt path.

3. The test matrix must prove, at minimum:

Accepted / compatible:

* Disabled receipt policy preserves legacy bypass: no receipt record, no audit ledger write, no external publication, no mutation.
* Disabled backend policy never invokes receipt sink.
* Disabled attestor/finalizer/reporter/sink/pipeline/evaluator-call-site policy never reaches the receipt sink.
* DevNet fixture chain records exactly one modeled in-memory receipt only after:
  Run 246 pipeline success → Run 248 sink receipt → Run 250 completion report → Run 252 finalization → Run 254 attestation → Run 256 backend submission → Run 258 receipt.
* TestNet fixture chain does the same.
* Add-root / retire-root / revoke-root / emergency-revoke-root / noop cases preserve ordering and record only after backend submission.
* Duplicate identical receipt is idempotent.
* Run 256 duplicate-idempotent backend submission may only match an already-recorded receipt and never creates a new receipt by itself.
* Existing Run 256 / 254 / 252 / 250 / 248 / 246 / 244 / 242 / 240 behavior remains compatible.
* Production audit-ledger, MainNet audit-ledger, and external-publication paths are reachable but unavailable/fail-closed.
* MainNet peer-driven apply is refused before pipeline, sink, reporter, finalizer, attestor, backend, and receipt sink invocation.
* Validator-set rotation and policy-change actions remain unsupported.

Rejected / fail-closed:

* Every rejection before Run 258 produces no receipt request and no receipt.
* Backend did-not-submit / rejected-before-record / record failure / rollback / rollback-failed / ambiguous window produces no receipt sink invocation and no receipt.
* Receipt before backend submission is rejected.
* Receipt after modeled apply failure, sink failure, reporter failure, finalizer failure, attestor failure, backend failure, backend rollback, backend rollback-failed, or backend ambiguous window is rejected.
* Same receipt record id with different digest fails closed as equivocation.
* Wrong environment / chain / genesis / governance surface / mutation surface fails before receipt sink invocation.
* Wrong receipt identity / policy / kind / request digest / response digest / record digest fails closed.
* Wrong backend identity/request/response/receipt/transcript digest fails closed.
* Wrong attestation/finalization/completion-report/sink/reporter/finalization/pipeline digest fails closed.
* Wrong proposal id / decision id / candidate digest / authority-domain sequence fails closed.
* Malformed request / response fails closed.
* Local operator key cannot satisfy MainNet authority.
* Peer majority cannot satisfy MainNet authority.
* Production/MainNet/external-publication unavailable paths record no receipt and perform no publication.
* Rejected paths produce no Run 070 call, no `LivePqcTrustState` mutation, no trust swap, no session eviction, no sequence write, no marker write, no RocksDB/file/schema/migration/storage-format change, no external publication, and no real audit-ledger write.

Recovery / crash-window:

* before-pipeline through after-backend-success-before-receipt-request windows fail closed unless an explicit matching success exists.
* after-receipt-request-before-receipt-record fails closed.
* after-receipt-record-before-receipt-success fails closed unless explicit matching receipt success exists.
* after-receipt-success recovers as receipt recorded.
* ambiguous, record-failed, rollback-completed, rollback-failed, unknown, production/MainNet/external-publication windows fail closed.
* MainNet peer-driven apply refusal precedes recovery classification where applicable.

Projection:

* Only Run 256 `BackendSubmissionRecorded` creates a receipt request.
* Pipeline success alone creates no receipt request.
* Sink receipt alone creates no receipt request.
* Completion report alone creates no receipt request.
* Finalization alone creates no receipt request.
* Attestation alone creates no receipt request.
* Backend request alone creates no receipt request.
* Backend duplicate-idempotent creates no new receipt unless a matching receipt already exists.
* Only `AuditReceiptRecorded` authorizes modeled receipt-recorded state.
* Every other outcome does not record.

Stage-ordering:

* MainNet peer-driven apply refusal happens before every later stage.
* Evaluator/call-site authorization precedes durable replay.
* Durable replay precedes mutation engine.
* Mutation engine precedes modeled applier.
* Modeled applier precedes sink intent.
* Sink receipt precedes completion report.
* Completion report precedes finalization.
* Finalization precedes attestation.
* Attestation precedes backend request.
* Backend submission precedes receipt request.
* Receipt record happens only after backend-submitted state.
* Rejection before receipt stage leaves receipt sink invocation count zero.
* Receipt record failure does not invalidate backend submission but does not authorize receipt.
* Receipt rollback failure is fatal/fail-closed and does not authorize receipt.

Receipt ledger:

* One valid receipt inserts one in-memory record.
* Duplicate identical receipt is idempotent and does not increase count.
* Same record id with different digest is equivocation and does not increase count.
* Wrong receipt/backend/attestation/finalization/report/sink/pipeline/proposal/decision/candidate/sequence digest does not record.
* Malformed request / response does not record.
* Rollback restores in-memory ledger snapshot.
* Rollback failure never claims success.
* Fixture ledger never writes files, RocksDB, markers, sequences, external publications, audit-ledger entries, or production durable state.

Non-mutation:

* no Run 070 call;
* no `LivePqcTrustState` mutation;
* no trust swap;
* no session eviction;
* no sequence write;
* no marker write;
* no RocksDB/file/schema/migration/storage-format write;
* no wire/schema/marker/sequence/trust-bundle change;
* no external network publication;
* no real audit-ledger write;
* fixture receipt sink mutates only `DurableCompletionAuditPublicationReceiptLedger`;
* production/MainNet/external-publication paths remain unavailable/fail-closed.

Validation commands:

Run at minimum:

* `cargo build -p qbind-node --lib`
* `cargo test -p qbind-node --test run_258_durable_completion_audit_publication_receipt_tests`
* `cargo test -p qbind-node --test run_256_durable_completion_attestation_backend_tests`
* `cargo test -p qbind-node --test run_254_modeled_durable_completion_attestation_projection_tests`
* `cargo test -p qbind-node --test run_252_modeled_durable_completion_finalization_projection_tests`
* `cargo test -p qbind-node --test run_250_modeled_durable_consume_completion_reporter_tests`
* `cargo test -p qbind-node --test run_248_modeled_durable_consume_projection_sink_tests`
* `cargo test -p qbind-node --test run_246_governance_modeled_end_to_end_pipeline_tests`
* `cargo test -p qbind-node --test run_244_modeled_governance_trust_mutation_applier_tests`
* `cargo test -p qbind-node --test run_242_governance_execution_mutation_engine_tests`
* `cargo test -p qbind-node --test run_240_governance_evaluator_replay_durable_runtime_integration_tests`
* `cargo test -p qbind-node --test run_238_governance_evaluator_replay_durable_backend_tests`
* `cargo test -p qbind-node --test run_236_governance_evaluator_replay_consume_runtime_integration_tests`
* `cargo test -p qbind-node --test run_234_governance_evaluator_replay_consume_boundary_tests`
* `cargo test -p qbind-node --test run_232_governance_evaluator_replay_runtime_integration_tests`
* `cargo test -p qbind-node --test run_230_governance_evaluator_replay_state_tests`
* `cargo test -p qbind-node --test run_228_peer_evaluator_context_representation_tests`
* `cargo test -p qbind-node --test run_226_governance_evaluator_runtime_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_224_governance_evaluator_runtime_integration_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

Security scanning:

* Run secret scanning and report result.
* Run CodeQL if available.
* If Rust CodeQL analysis is skipped because the database is too large, state that explicitly and do not claim CodeQL coverage.

Documentation:

Create:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_258.md`

Update narrowly:

* `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
* `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
* `docs/whitepaper/contradiction.md`

Documentation must state:

* Run 258 is source/test only.
* Run 258 adds a durable-completion backend audit-ledger / external-publication receipt boundary.
* The boundary models the first post-Run-256 backend-submission receipt interface a future production audit ledger or external publication system would use.
* The fixture receipt sink is DevNet/TestNet evidence-only and in-memory only.
* Production audit-ledger, MainNet audit-ledger, and external-publication sinks are reachable but unavailable/fail-closed.
* No real audit ledger backend is implemented.
* No real external-publication backend is implemented.
* No real production attestation backend is implemented.
* No real finalization backend is implemented.
* No real completion-report backend is implemented.
* No real durable consume backend is implemented.
* No real persistent replay backend is implemented.
* No real governance execution engine is implemented.
* No real production mutation engine is implemented.
* No real on-chain governance proof verifier is implemented.
* No KMS/HSM/RemoteSigner backend is implemented.
* No RocksDB/file/schema/migration/storage-format change is implemented.
* No wire/schema/marker/sequence/trust-bundle change is implemented.
* No MainNet governance is enabled.
* No MainNet peer-driven apply is enabled.
* No validator-set rotation is implemented.
* Run 246 pipeline success is required before any sink intent can exist.
* Run 248 `ConsumeReceiptRecorded` is required before any completion-report intent can exist.
* Run 250 `CompletionReportRecorded` is required before any finalization intent can exist.
* Run 252 `DurableCompletionFinalized` is required before any attestation intent can exist.
* Run 254 `DurableCompletionAttested` is required before any backend request can exist.
* Run 256 `BackendSubmissionRecorded` is required before any audit/publication receipt request can exist.
* Only `AuditReceiptRecorded` authorizes a modeled audit/publication receipt state.
* Every non-submitted backend outcome produces no audit/publication receipt request and no receipt.
* Failed receipt record, rollback, rollback-failed, ambiguous windows, unavailable production/MainNet/external-publication paths, rejected replay states, and unsupported actions never record.
* MainNet peer-driven apply remains refused before pipeline progression, sink invocation, reporter invocation, finalizer invocation, attestor invocation, backend invocation, and receipt sink invocation.
* Rejected receipt paths are non-mutating.
* Full C4 remains OPEN.
* C5 remains OPEN.

Contradiction handling:

* Crosscheck Run 258 against Runs 050–257.
* If no contradiction is found, add a “no Run 258 contradiction” entry to `docs/whitepaper/contradiction.md`.
* If any contradiction is found, record it explicitly with the exact file/function/doc reference and recommend the smallest safe fix.

Expected verdict after continuation:

Source/test positive if the Run 258 tests, regression corpus, docs, evidence file, contradiction update, and scans pass.

If completed, the suggested next step should be:

Run 259 — release-binary evidence for the Run 258 durable-completion audit-ledger / external-publication receipt boundary.

Final response format:

1. Exact verdict.
2. Files changed.
3. Tests run and results.
4. Security invariants preserved.
5. Honest limitations.
6. C4/C5 status.
7. Suggested Run 259 next step.
