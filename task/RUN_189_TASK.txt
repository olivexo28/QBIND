Task: Run 189 — Release-Binary Authority Custody Boundary Evidence

Current accepted state:

* Run 185: release-binary OnChainGovernance fixture payload accepted-proof evidence landed.
* Run 186: source/test production OnChainGovernance verifier boundary landed.
* Run 187: release-binary production OnChainGovernance verifier-boundary evidence landed.
* Run 188: source/test KMS/HSM custody boundary landed:

  * `pqc_authority_custody.rs`;
  * `AuthorityCustodyClass::{FixtureLocalKey, LocalOperatorKey, RemoteSigner, Kms, Hsm, Unknown}`;
  * `AuthorityCustodyPolicy::{Disabled, FixtureOnly, DevnetLocalAllowed, TestnetLocalAllowed, ProductionCustodyRequired, MainnetProductionCustodyRequired}`;
  * `AuthorityCustodyAttestation`;
  * typed `AuthorityCustodyValidationOutcome`;
  * pure `validate_authority_custody_attestation`;
  * pure `validate_lifecycle_governance_and_custody`;
  * named rule helpers for MainNet refusal / peer-majority / local-operator MainNet rules;
  * 48 tests covering A1–A8 / R1–R29;
  * full lib and regression suites green.
* Run 188 preserves:

  * no real KMS;
  * no real HSM;
  * no remote signer production integration;
  * no MainNet apply enablement;
  * no wire/schema/marker/sequence/trust-bundle change;
  * full C4 open;
  * C5 open.
* Release-binary custody-boundary evidence is deferred to Run 189.

Objective:
Produce release-binary evidence for the Run 188 authority custody boundary.

This run must prove on release binaries:

1. fixture/local custody remains DevNet/TestNet evidence-only;
2. fixture/local custody cannot satisfy MainNet production custody;
3. KMS/HSM/remote signer placeholders fail closed as unavailable;
4. combined lifecycle + governance + custody decisions remain pure/non-mutating;
5. MainNet peer-driven apply remains refused;
6. no real KMS/HSM/remote signer/governance execution/validator-set rotation is claimed.

Strict scope:

* Release-binary evidence only.
* Use real `target/release/qbind-node`.
* Use release-built helper(s) to exercise the Run 188 custody boundary.
* No production source change unless a tiny harness-only fix is required.
* No real KMS implementation.
* No real HSM implementation.
* No cloud KMS integration.
* No PKCS#11 integration.
* No remote signer production integration.
* No MainNet peer-driven apply enablement.
* No real governance execution engine.
* No real on-chain proof verifier.
* No validator-set rotation.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* No schema/wire changes.
* No authority-marker schema change.
* No sequence-file schema change.
* No trust-bundle core schema change.
* Do not weaken Runs 070, 130–188.
* Do not claim full C4 or C5 closure.

Required deliverables:

1. Add release-binary helper:
   `crates/qbind-node/examples/run_189_authority_custody_boundary_release_binary_helper.rs`

2. Add release-binary harness:
   `scripts/devnet/run_189_authority_custody_boundary_release_binary.sh`

3. Add evidence archive:
   `docs/devnet/run_189_authority_custody_boundary_release_binary/`

4. Add canonical evidence report:
   `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_189.md`

5. Update narrowly:

   * `docs/whitepaper/contradiction.md`
   * `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
   * `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
   * `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

6. Crosscheck all new/modified code and docs against existing design/spec. If contradictions or inconsistencies are found, record them in `contradiction.md` before finalizing.

Required release-built helper matrix:
The helper must exercise the Run 188 custody-boundary corpus in release mode.

Accepted / compatible cases:
A1. DevNet fixture custody accepted under explicit fixture policy.

A2. TestNet fixture custody accepted under explicit fixture policy.

A3. DevNet local-operator custody accepted under explicit DevNet local policy.

A4. TestNet local-operator custody accepted under explicit TestNet local policy.

A5. Existing GenesisBound / EmergencyCouncil governance proof behavior remains unchanged when custody validation is not required.

A6. Combined lifecycle + governance + fixture custody accepted for DevNet source/helper path.

A7. Combined lifecycle + governance + local custody accepted for TestNet source/helper path where policy allows.

A8. Production custody boundary callable and returns typed unavailable outcome for KMS/HSM/remote signer placeholders.

Required rejection cases:
R1. fixture custody rejected under production custody policy.

R2. local operator custody rejected under production custody policy.

R3. fixture custody rejected for MainNet.

R4. local operator custody rejected for MainNet.

R5. KMS placeholder rejected as unavailable.

R6. HSM placeholder rejected as unavailable.

R7. remote signer placeholder rejected as unavailable.

R8. unknown custody class rejected.

R9. wrong environment rejected.

R10. wrong chain rejected.

R11. wrong genesis rejected.

R12. wrong authority root rejected.

R13. wrong signing-key fingerprint rejected.

R14. wrong candidate digest rejected.

R15. wrong authority-domain sequence rejected.

R16. wrong lifecycle action rejected.

R17. missing custody attestation rejected.

R18. malformed custody attestation rejected.

R19. expired custody attestation rejected.

R20. custody key id mismatch rejected.

R21. unsupported custody suite rejected.

R22. custody valid but governance proof invalid rejected.

R23. governance proof valid but custody invalid rejected.

R24. lifecycle valid + governance valid + custody placeholder unavailable rejected.

R25. MainNet peer-driven apply remains refused even if custody metadata claims KMS/HSM.

R26. local operator config alone cannot satisfy MainNet production custody.

R27. peer majority / gossip count cannot satisfy custody.

R28. validation-only rejection remains non-mutating.

R29. mutating preflight rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

Required real binary checks:
Use real `target/release/qbind-node` to prove:

* MainNet peer-driven apply remains refused.
* No `KMS/HSM enabled` / `KMS/HSM active` / `remote signer production active` claim is emitted.
* Fixture/local custody is not accepted as MainNet production custody.
* Existing Run 185 / Run 187 governance fixture paths remain compatible.
* No real governance execution claim is emitted.
* No validator-set rotation claim is emitted.

Required source reachability proof:
The report must include source grep/call-site proof for:

* `pqc_authority_custody`;
* `AuthorityCustodyClass`;
* `AuthorityCustodyPolicy`;
* `validate_authority_custody_attestation`;
* `validate_lifecycle_governance_and_custody`;
* KMS / HSM / RemoteSigner unavailable outcome variants;
* MainNet custody refusal helper;
* peer-majority / local-operator rejection helpers.

Required mutation / no-mutation proof:
For rejected custody-boundary scenarios:

* no Run 070 apply call;
* no live trust swap;
* no session eviction;
* no sequence write;
* no marker write;
* marker bytes unchanged where present;
* sequence bytes unchanged where present;
* no `.tmp` residue;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead.

For any accepted fixture/local mutating compatibility scenario:

* custody validation occurs before apply/mutation;
* lifecycle validation occurs before apply/mutation;
* governance validation occurs before apply/mutation where applicable;
* Run 055 sequence commit succeeds if a mutating path is exercised;
* v2 marker persists strictly after sequence commit.

Required denylist:
Across the run prove:

* no MainNet apply;
* no autonomous apply;
* no apply on receipt;
* no peer-majority authority;
* no real governance execution claim;
* no real on-chain governance proof claim;
* no KMS/HSM enabled or active claim;
* no remote signer production active claim;
* no validator-set rotation claim;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead;
* no schema/wire/metric drift;
* no marker write before sequence commit;
* no sequence write on validation-only surfaces;
* no marker write on validation-only surfaces.

Required captured metadata:

* qbind-node SHA-256 and ELF Build ID;
* helper SHA-256 and ELF Build ID;
* git commit hash;
* rustc/cargo versions;
* exact commands;
* stdout/stderr logs;
* per-scenario exit codes;
* custody attestation fixture paths / hashes;
* marker/sequence JSON and SHA before/after where applicable;
* data-dir inventories;
* denylist grep results.

Validation commands:
Run at minimum:

* `cargo build --release -p qbind-node --bin qbind-node`
* `cargo build --release -p qbind-node --example run_189_authority_custody_boundary_release_binary_helper`
* `bash scripts/devnet/run_189_authority_custody_boundary_release_binary.sh`
* `cargo test -p qbind-node --test run_188_authority_custody_boundary_tests`
* `cargo test -p qbind-node --test run_186_onchain_governance_production_verifier_boundary_tests`
* `cargo test -p qbind-node --test run_184_onchain_governance_payload_carrying_tests`
* `cargo test -p qbind-node --test run_182_onchain_governance_production_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_180_onchain_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_178_onchain_governance_proof_tests`
* `cargo test -p qbind-node --test run_176_live_0x05_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_173_validation_only_governance_required_policy_tests`
* `cargo test -p qbind-node --test run_171_governance_required_policy_selector_tests`
* `cargo test -p qbind-node --test run_169_governance_proof_loader_surface_integration_tests`
* `cargo test -p qbind-node --test run_167_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_165_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_163_governance_authority_verifier_tests`
* `cargo test -p qbind-node --test run_161_lifecycle_marker_integration_tests`
* `cargo test -p qbind-node --test run_159_authority_signing_key_lifecycle_tests`
* `cargo test -p qbind-node --test run_157_unified_testnet_fixture_universe_tests`
* `cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests`
* `cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests`
* `cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests`
* `cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests`
* `cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests`
* `cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Documentation requirements:
`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_189.md` must state:

* Run 189 is release-binary authority custody-boundary evidence.
* No real KMS/HSM backend is implemented.
* KMS/HSM/remote signer placeholders remain fail-closed.
* Fixture/local custody remains DevNet/TestNet evidence-only.
* Fixture/local custody cannot satisfy MainNet production custody.
* MainNet peer-driven apply remains refused.
* governance execution remains unimplemented.
* real on-chain proof verification remains unimplemented.
* validator-set rotation remains open.
* existing governance fixture proof paths remain compatible.
* full C4 remains open.
* C5 remains open.

Acceptance criteria:
Run 189 is acceptable only if:

1. Release-built helper proves the Run 188 custody-boundary corpus.
2. Real `target/release/qbind-node` confirms MainNet peer-driven apply remains refused.
3. Fixture/local custody remains explicitly DevNet/TestNet evidence-only.
4. Fixture/local custody cannot satisfy MainNet production custody.
5. KMS/HSM/remote signer placeholders are release-binary evidenced as unavailable/fail-closed.
6. Existing governance proof paths remain compatible.
7. Rejected custody-boundary cases produce no mutation.
8. No real KMS/HSM / governance execution / real on-chain proof / validator-set rotation claim is made.
9. No full C4 or C5 closure is claimed.
