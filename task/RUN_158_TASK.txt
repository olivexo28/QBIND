Task: Run 158 — Positive TestNet Release-Binary End-to-End Peer-Driven Apply Evidence Using Unified Fixture Universe

Current accepted state:
- Run 153: DevNet release-binary end-to-end peer-driven apply evidence landed.
- Run 154: initial TestNet fixture tooling landed.
- Run 155: partial-positive TestNet release-binary refusal/domain evidence landed.
- Run 156: real TestNet N=3 positive apply attempt was driven, but blocked because the live transport universe and apply-candidate universe were disjoint.
- Run 157: source/test unified TestNet fixture universe landed:
  - `run_157_unified_testnet_peer_apply_fixture_helper`;
  - `unified_testnet_manifest.json`;
  - TestNet genesis and expected genesis hash;
  - baseline trust bundle seq=1;
  - candidate trust bundle seq=2;
  - v2 ratification sidecar;
  - seeded v2 authority marker seq=1;
  - V0/V1/V2 leaf certificates and ML-KEM keys;
  - shared transport root / authority universe;
  - valid and negative peer-candidate fixtures.
- Run 157 explicitly deferred release-binary positive TestNet apply evidence to Run 158.
- MainNet remains refused.
- Governance remains unimplemented.
- KMS/HSM remains unimplemented.
- signing-key rotation/revocation lifecycle remains open.
- validator-set rotation remains open.
- full C4 remains open.
- C5 remains open.

Objective:
Produce the missing positive TestNet release-binary end-to-end peer-driven apply evidence using the unified TestNet fixture universe from Run 157.

This run must drive a real `target/release/qbind-node` TestNet receiver through the complete positive path:

  live inbound 0x05 candidate
    → v2 validation-only acceptance
    → staging queue
    → hidden explicit drain-once hook
    → ProductionDrainInvocationBuilder
    → ProductionV2MarkerCoordinator
    → Run 150 PeerDrivenApplyDrain::try_drain_once
    → Run 148 try_apply_staged_peer_candidate
    → Run 070 apply_validated_candidate_with_previous
    → LivePqcTrustState swap
    → session eviction
    → Run 055 sequence commit
    → v2 authority marker persist after commit

Strict scope:
- Release-binary positive TestNet end-to-end apply evidence.
- Use real `target/release/qbind-node`.
- Use Run 157 unified TestNet fixtures.
- Use real live P2P 0x05 exchange.
- No source/test substitution for the positive A1 claim.
- No autonomous background drain.
- No automatic apply on receipt.
- No peer-majority authority.
- No MainNet enablement.
- No governance implementation.
- No KMS/HSM implementation.
- No signing-key rotation/revocation lifecycle.
- No validator-set rotation.
- No schema/wire/metric drift.
- Do not claim full C4 or C5 closure.

Required deliverables:
1. Add release-binary harness:
   scripts/devnet/run_158_testnet_positive_peer_driven_apply_release_binary.sh

2. Add evidence archive:
   docs/devnet/run_158_testnet_positive_peer_driven_apply_release_binary/

3. Add canonical evidence report:
   docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_158.md

4. Update narrowly:
   - docs/whitepaper/contradiction.md
   - docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md
   - docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md
   - docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md

Required topology:
Use real TestNet N=3:

- V0: publisher of live 0x05 v2 TestNet candidate from the Run 157 manifest.
- V1: TestNet receiver with wire validation, staging, apply-enabled, and drain-once enabled.
- V2: observer / propagation invariant node.

Use:
- real release `qbind-node`;
- real release `run_157_unified_testnet_peer_apply_fixture_helper`;
- TestNet environment;
- TestNet chain ID;
- TestNet genesis hash from manifest;
- unified baseline seq=1 bundle;
- unified candidate seq=2 bundle;
- unified v2 ratification sidecar;
- unified seeded marker seq=1 if required;
- real ML-KEM-768 / ML-DSA-44 / ChaCha20-Poly1305 path;
- mutual-auth Required if previous harnesses use it;
- no active DummySig / DummyKem / DummyAead.

Required positive scenario:

A1. TestNet end-to-end peer-driven apply succeeds on real release binaries
- V0 publishes valid TestNet v2 candidate over live 0x05.
- V1 validates, stages, and explicitly drains once.
- Expected:
  - P2P connection established;
  - live 0x05 path actually exercised;
  - TestNet domain selected;
  - v2 validation-only acceptance visible;
  - candidate staged before drain;
  - drain-once trigger accepted;
  - ProductionDrainInvocationBuilder invoked;
  - ProductionV2MarkerCoordinator invoked;
  - Run 150 drain invoked;
  - Run 148 controller invoked;
  - Run 070 apply contract invoked;
  - LivePqcTrustState swap occurs;
  - session eviction occurs according to Run 070/072 semantics;
  - Run 055 sequence commit succeeds;
  - v2 authority marker persists strictly after sequence commit;
  - Applied outcome visible;
  - no autonomous repeat drain.

Required focused negative checks:

R1. Run 156 disjoint-universe candidate still rejected
- Use old disjoint candidate shape or recreated disjoint fixture.
- Expected:
  - rejected before staging;
  - drain returns NoCandidate;
  - no mutation.

R2. MainNet refusal remains intact
- Attempt to arm or run drain under MainNet.
- Expected:
  - MainNetRefused / FATAL visible;
  - no P2P apply;
  - no sequence write;
  - no marker write;
  - no live trust swap.

R3. wrong-environment candidate rejected
- DevNet candidate sent to TestNet receiver, if cheap.
- Expected:
  - wrong-domain reason visible;
  - no mutation.

R4. duplicate candidate cannot double-apply, if feasible
- Same valid candidate published twice or drain triggered twice.
- Expected:
  - one Applied outcome maximum;
  - second drain returns NoCandidate / AlreadyApplied / deduped;
  - no duplicate sequence write;
  - no duplicate marker write;
  - no duplicate session eviction.
- If not feasible with process-start drain-once, document and cite Run 150/152 source-test coverage.

Required ordering proof:
The evidence must include ordered release-binary logs or deterministic artifacts proving:

1. P2P connection established.
2. live 0x05 candidate received.
3. v2 validation-only accepted under TestNet domain.
4. candidate staged.
5. explicit drain-once triggered.
6. ProductionDrainInvocationBuilder built invocation.
7. ProductionV2MarkerCoordinator accepted marker decision.
8. Run 150 drain invoked.
9. Run 148 controller invoked.
10. Run 070 ordering:
   validate → snapshot previous → swap → evict_sessions → commit_sequence.
11. sequence commit succeeds.
12. v2 marker persists after sequence commit.
13. Applied outcome emitted.

Required mutation proof:
Capture before/after:
- sequence JSON and SHA-256;
- authority marker JSON and SHA-256;
- TestNet genesis hash;
- TestNet chain ID;
- TestNet environment proof;
- live trust fingerprint or active-root evidence if observable;
- session-eviction counters;
- applied/drain outcome logs;
- node stdout/stderr;
- metrics before/after.

Required denylist:
Across the run prove:
- no autonomous background drain;
- no apply on receipt without explicit drain;
- no peer-majority authority;
- no governance claim;
- no KMS/HSM claim;
- no signing-key rotation/revocation claim;
- no validator-set rotation claim;
- no MainNet apply;
- no fallback to --p2p-trusted-root;
- no active DummySig / DummyKem / DummyAead;
- no SIGHUP / reload-apply / startup-mutation / snapshot-restore apply outcome;
- no schema/wire/metric drift.

If A1 is still blocked:
Stop and document the exact blocker. Do not mark Run 158 strongest-positive by substituting source/test coverage.

Validation commands:
Run at minimum:
- cargo build --release -p qbind-node --bin qbind-node
- cargo build --release -p qbind-node --example run_157_unified_testnet_peer_apply_fixture_helper
- bash scripts/devnet/run_158_testnet_positive_peer_driven_apply_release_binary.sh
- cargo test -p qbind-node --test run_157_unified_testnet_fixture_universe_tests
- cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests
- cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests
- cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests
- cargo test -p qbind-node --test run_146_live_inbound_0x05_staging_hook_tests
- cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests
- cargo test -p qbind-node --lib pqc_authority
- cargo test -p qbind-node --lib

Documentation requirements:
`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_158.md` must state:
- Run 158 is positive TestNet release-binary end-to-end peer-driven apply evidence.
- It uses the Run 157 unified fixture universe.
- It closes the Run 156 disjoint-universe blocker if A1 succeeds.
- No autonomous background apply exists.
- No automatic apply on receipt exists.
- MainNet remains refused.
- Governance remains unimplemented.
- KMS/HSM remains unimplemented.
- signing-key rotation/revocation lifecycle remains open.
- validator-set rotation remains open.
- full C4 remains open unless all separately defined C4 closure criteria are satisfied.
- C5 remains open.

Acceptance criteria:
Run 158 is acceptable only if:
1. real TestNet release binaries execute the positive peer-driven apply path;
2. A1 is proven by process logs/artifacts, not source/test mapping;
3. Run 156 disjoint-universe blocker is demonstrably fixed by Run 157 fixtures;
4. sequence commit precedes v2 marker persistence;
5. session eviction occurs according to Run 070/072 semantics;
6. MainNet remains refused;
7. no autonomous apply exists;
8. no governance/KMS-HSM/peer-majority claim is made;
9. docs are updated narrowly;
10. full C4 and C5 are not overclaimed.