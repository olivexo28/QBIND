Run 267 — Release-binary durable-completion settlement-commitment / ledger-finalization boundary evidence

You are continuing QBIND after Run 266.

Current accepted state:

Run 266 closed the source/test durable-completion settlement-commitment / ledger-finalization interface boundary.

Run 266 added:

* `crates/qbind-node/src/pqc_governance_durable_completion_settlement_commitment.rs`
* `crates/qbind-node/tests/run_266_durable_completion_settlement_commitment_tests.rs`
* `crates/qbind-node/src/lib.rs` module registration after Run 264
* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_266.md`
* narrow protocol/ops doc updates
* `docs/whitepaper/contradiction.md` “Run 266 — no contradiction found” entry

Run 266 is source/test only. It has no release-binary evidence yet.

Run 266 proves:

* only Run 264 `SettlementProjectionRecorded` creates a settlement-commitment request;
* Run 264 `SettlementProjectionDuplicateIdempotent` may only match an already-recorded settlement-commitment record and never creates a new one by itself;
* only `SettlementCommitmentRecorded` authorizes modeled settlement-commitment state;
* duplicate identical settlement-commitment record is idempotent;
* same settlement-commitment record id with different digest fails closed as equivocation;
* fixture commitment sink mutates only in-memory `DurableCompletionSettlementCommitmentLedger`;
* production/MainNet/external commitment sinks remain reachable but unavailable/fail-closed;
* no real settlement, settlement finality, audit ledger, external publication, production backend, MainNet governance, peer-driven apply, validator-set rotation, or policy-change enablement exists.

Full C4 remains OPEN. C5 remains OPEN.

Strict workflow:

* Execution-first.
* Narrow scope only.
* No broad redesign.
* No silent regressions.
* No hype or overclaiming.
* Release-binary evidence only.
* No new source/test feature boundary.
* No production runtime enablement.
* No real settlement backend.
* No real settlement execution.
* No real settlement finality.
* No real audit-ledger acknowledgement backend.
* No real external-publication confirmation backend.
* No real external publication.
* No real production attestation backend.
* No real finalization backend.
* No real completion-report backend.
* No real durable consume backend.
* No real persistent replay backend.
* No real governance execution engine.
* No real production mutation engine.
* No real on-chain governance proof verifier.
* No KMS/HSM/RemoteSigner backend.
* No RocksDB/file/schema/migration/storage-format change.
* No wire/schema/marker/sequence/trust-bundle change.
* No Run 070 call.
* No `LivePqcTrustState` mutation.
* No real trust swap.
* No session eviction.
* No sequence write.
* No marker write.
* No network I/O or external settlement.
* No MainNet governance.
* No MainNet peer-driven apply.
* No validator-set rotation.
* No policy-change enablement.
* Keep Full C4 OPEN.
* Keep C5 OPEN.

Objective:

Produce release-binary evidence for the Run 266 durable-completion settlement-commitment / ledger-finalization interface boundary, mirroring the Run 265 → Run 264 release-binary evidence pattern.

Run 267 must prove on real `target/release/qbind-node` plus a release-built helper that the Run 266 settlement-commitment boundary symbols are present in release-built code, that the helper exercises the Run 266 production library symbols in release mode, and that the settlement-commitment path remains fixture/in-memory/dev-test only — not production-enabled.

Required deliverables:

1. Add release helper:

   `crates/qbind-node/examples/run_267_durable_completion_settlement_commitment_release_binary_helper.rs`

2. Add release harness:

   `scripts/devnet/run_267_durable_completion_settlement_commitment_release_binary.sh`

3. Add evidence archive directory:

   `docs/devnet/run_267_durable_completion_settlement_commitment_release_binary/`

   Track only:

   * `README.md`
   * `summary.txt`
   * `.gitignore`

   Generated logs, helper tables, reachability files, provenance files, and test logs must be regenerated and ignored.

4. Add canonical evidence report:

   `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_267.md`

5. Update narrowly:

   * `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
   * `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
   * `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
   * `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
   * `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
   * `docs/whitepaper/contradiction.md`

Required existing ordering:

1. Run 246 pipeline success is required before any Run 248 sink intent can exist.
2. Run 248 `ConsumeReceiptRecorded` is required before any Run 250 completion-report intent can exist.
3. Run 250 `CompletionReportRecorded` is required before any Run 252 finalization intent can exist.
4. Run 252 `DurableCompletionFinalized` is required before any Run 254 attestation intent can exist.
5. Run 254 `DurableCompletionAttested` is required before any Run 256 backend request can exist.
6. Run 256 `BackendSubmissionRecorded` is required before any Run 258 audit/publication receipt request can exist.
7. Run 258 `AuditReceiptRecorded` is required before any Run 260 acknowledgement / confirmation request can exist.
8. Run 260 `AcknowledgementRecorded` is required before any Run 262 acknowledgement-consumer request can exist.
9. Run 262 `AcknowledgementConsumed` is required before any Run 264 settlement-projection request can exist.
10. Run 264 `SettlementProjectionRecorded` is required before any Run 266 settlement-commitment request can exist.
11. Only Run 266 `SettlementCommitmentRecorded` authorizes a new modeled settlement-commitment state.

Helper requirements:

The release helper must link against release-built production library symbols and exercise the actual Run 266 API, including the exact implemented names for:

* `pqc_governance_durable_completion_settlement_commitment`
* `DurableCompletionSettlementCommitmentKind`
* `DurableCompletionSettlementCommitmentPolicy`
* `DurableCompletionSettlementCommitmentIdentity`
* `DurableCompletionSettlementCommitmentInput`
* `DurableCompletionSettlementCommitmentExpectations`
* `DurableCompletionSettlementCommitmentRequest`
* `DurableCompletionSettlementCommitmentResponse`
* `DurableCompletionSettlementCommitmentRecord`
* `DurableCompletionSettlementCommitmentDigest`
* `DurableCompletionSettlementCommitmentTranscriptDigest`
* `DurableCompletionSettlementCommitmentOutcome`
* `GovernanceDurableCompletionSettlementCommitmentSink`
* `FixtureDurableCompletionSettlementCommitmentSink`
* `ProductionSettlementCommitmentSink`
* `MainNetSettlementCommitmentSink`
* `ExternalSettlementCommitmentSink`
* `DurableCompletionSettlementCommitmentLedger`
* `DurableCompletionSettlementCommitmentLedgerRecord`
* `DurableCompletionSettlementCommitmentLedgerSnapshot`
* `DurableCompletionSettlementCommitmentLedgerStatus`
* `project_settlement_projection_outcome_to_commitment_request`
* `evaluate_durable_completion_settlement_commitment`
* `recover_durable_completion_settlement_commitment_window`
* `settlement_commitment_outcome_authorizes_record`
* `settlement_commitment_outcome_projects_to_recorded`
* all Run 266 grep-verifiable invariant helpers.

If exact symbol names differ, use the actual implemented Run 266 API names and record substitutions honestly in `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_267.md`. Do not add compatibility shims unless strictly necessary for compilation, and do not change production behavior.

Helper architecture:

Copy the Run 265 release helper structure and adapt it to Run 266.

The helper must attach to the full real modeled chain:

* Run 256 backend submission
* Run 258 audit receipt
* Run 260 acknowledgement
* Run 262 acknowledgement consumer
* Run 264 settlement projection
* Run 266 settlement commitment

Do not fabricate a standalone settlement-commitment request.

Add or copy attachment helpers from the Run 266 tests:

* attach Run 256 backend submission
* attach Run 258 receipt
* attach Run 260 acknowledgement
* attach Run 262 consumer
* attach Run 264 settlement projection
* create Run 266 settlement-commitment input from the actual Run 264 settlement-projection outcome and digest binding

Critical projection rule:

The Run 267 helper must drive projection from the Run 264 settlement-projection outcome:

* `input.settlement_projection_binding`
* `project_settlement_projection_outcome_to_commitment_request`

Do NOT project from Run 262 consumer, Run 260 acknowledgement, Run 258 receipt, or Run 256 backend outcome directly.

Required projection mapping:

* Run 264 `SettlementProjectionRecorded` → creates settlement-commitment request.
* Run 264 `SettlementProjectionDuplicateIdempotent` → idempotent-only, never creates a new settlement-commitment record by itself.
* Run 264 legacy/no-projection outcomes → legacy/no-commitment.
* Run 264 rejected-before-projection / projection-did-not-record / record-failure / rollback / rollback-failed / ambiguous-window / production / MainNet / external outcomes → corresponding no-commitment fail-closed / unavailable outcomes.
* Run 264 `MainNetPeerDrivenApplyRefusedNoProjection` → `MainNetPeerDrivenApplyRefusedNoCommitment`.
* Run 264 `ValidatorSetRotationUnsupportedNoProjection` → `ValidatorSetRotationUnsupportedNoCommitment`.
* Run 264 `PolicyChangeUnsupportedNoProjection` → `PolicyChangeUnsupportedNoCommitment`.
* Every other non-recorded settlement-projection outcome → no-commitment fail-closed.

Helper corpus:

The helper must include these tables:

* accepted / compatible
* rejection / fail-closed
* recovery / crash-window
* projection
* stage-ordering
* settlement-commitment-ledger
* non-mutation
* reachability

Accepted / compatible cases must prove at minimum:

* Disabled settlement-commitment policy preserves legacy no-commitment bypass.
* Disabled settlement-projection policy never invokes settlement commitment.
* Disabled consumer policy never invokes settlement projection or settlement commitment.
* Disabled acknowledgement policy never invokes consumer, settlement projection, or settlement commitment.
* Disabled receipt policy never invokes acknowledgement, consumer, settlement projection, or settlement commitment.
* Disabled backend policy never invokes receipt, acknowledgement, consumer, settlement projection, or settlement commitment.
* Disabled attestor/finalizer/reporter/sink/pipeline/evaluator-call-site policies never reach settlement commitment.
* DevNet fixture chain records exactly one modeled in-memory settlement-commitment record only after the full Run 246 → 248 → 250 → 252 → 254 → 256 → 258 → 260 → 262 → 264 → 266 chain.
* TestNet fixture chain records exactly one modeled in-memory settlement-commitment record only after the same full chain.
* Add-root / retire-root / revoke-root / emergency-revoke-root / noop modeled action variants preserve ordering and commit only after Run 264 `SettlementProjectionRecorded`.
* Duplicate identical settlement-commitment record is idempotent.
* Run 264 duplicate-idempotent settlement-projection outcome may only match an already-recorded settlement-commitment record and never creates a new commitment by itself.
* Existing Run 266 source/test behavior remains compatible.
* Existing Run 264 settlement-projection behavior remains compatible.
* Existing Run 262 consumer behavior remains compatible.
* Existing Run 260 acknowledgement behavior remains compatible.
* Existing Run 258 receipt behavior remains compatible.
* Existing Run 256 backend behavior remains compatible.
* Existing Run 254 attestation behavior remains compatible.
* Existing Run 252 finalization behavior remains compatible.
* Existing Run 250 completion-reporter behavior remains compatible.
* Existing Run 248 sink behavior remains compatible.
* Existing Run 246 pipeline behavior remains compatible.
* Existing Run 244 modeled applier behavior remains compatible.
* Existing Run 242 mutation-engine behavior remains compatible.
* Existing Run 240 durable completion behavior remains compatible.
* Production settlement-commitment path is reachable but unavailable/fail-closed and records no state.
* MainNet settlement-commitment path is reachable but unavailable/fail-closed and records no state.
* External settlement-commitment path is reachable but unavailable/fail-closed and performs no publication/network I/O.
* MainNet peer-driven apply is refused before pipeline progression, sink invocation, reporter invocation, finalizer invocation, attestor invocation, backend invocation, receipt invocation, acknowledgement invocation, consumer invocation, settlement-projection invocation, and settlement-commitment invocation.
* Validator-set rotation unsupported and records no state.
* Policy-change unsupported and records no state.

Rejected / fail-closed cases must prove at minimum:

* Every rejection before Run 266 produces no settlement-commitment request and no record.
* Settlement-projection did-not-record / rejected-before-record / record failure / rollback / rollback-failed / ambiguous window produces no settlement-commitment invocation and no record.
* Settlement commitment before Run 264 `SettlementProjectionRecorded` is rejected.
* Settlement commitment after modeled apply failure, sink failure, reporter failure, finalizer failure, attestor failure, backend failure, receipt failure, acknowledgement failure, consumer failure, settlement-projection failure, settlement-projection rollback, settlement-projection rollback-failed, or settlement-projection ambiguous window is rejected.
* Same settlement-commitment record id with different digest fails closed as equivocation.
* Wrong environment / chain / genesis / governance surface / mutation surface fails before settlement-commitment invocation.
* Wrong settlement-commitment identity / policy / kind / request digest / response digest / record digest fails closed.
* Wrong settlement-projection identity / request / response / record / transcript digest fails closed.
* Wrong consumer identity / request / response / record / transcript digest fails closed.
* Wrong acknowledgement identity / request / response / record / transcript digest fails closed.
* Wrong receipt identity / request / response / record / transcript digest fails closed.
* Wrong backend identity / request / response / receipt / transcript digest fails closed.
* Wrong attestation / finalization / completion-report / sink / reporter / pipeline digest fails closed.
* Wrong proposal id / decision id / candidate digest / authority-domain sequence fails closed.
* Malformed request / response fails closed.
* Local operator key cannot satisfy MainNet authority.
* Peer majority cannot satisfy MainNet authority.
* Production/MainNet/external settlement-commitment unavailable paths record no state and perform no publication.
* Rejected paths produce no Run 070 call, no `LivePqcTrustState` mutation, no trust swap, no session eviction, no sequence write, no marker write, no RocksDB/file/schema/migration/storage-format change, no external publication, no confirmation, no real audit-ledger write, no real settlement write, and no real settlement-finality write.

Recovery / crash-window cases must prove at minimum:

* before-pipeline through after-settlement-projection-success-before-settlement-commitment-request windows fail closed unless explicit matching settlement-projection success exists.
* after-settlement-commitment-request-before-record fails closed.
* after-settlement-commitment-record-before-success fails closed unless explicit matching settlement-commitment record exists.
* after-settlement-commitment-success recovers as recorded.
* ambiguous, record-failed, rollback-completed, rollback-failed, unknown, production/MainNet/external windows fail closed.
* MainNet peer-driven apply refusal precedes recovery classification where applicable.

Projection cases must prove at minimum:

* Only Run 264 `SettlementProjectionRecorded` creates a settlement-commitment request.
* Run 246 pipeline success alone creates no settlement-commitment request.
* Run 248 sink receipt alone creates no settlement-commitment request.
* Run 250 completion report alone creates no settlement-commitment request.
* Run 252 finalization alone creates no settlement-commitment request.
* Run 254 attestation alone creates no settlement-commitment request.
* Run 256 backend submission alone creates no settlement-commitment request.
* Run 258 audit receipt alone creates no settlement-commitment request.
* Run 260 acknowledgement alone creates no settlement-commitment request.
* Run 262 consumer success alone creates no settlement-commitment request.
* Run 264 settlement-projection request alone creates no settlement-commitment request.
* Run 264 duplicate-idempotent settlement-projection outcome creates no new settlement-commitment record unless a matching commitment record already exists.
* Only `SettlementCommitmentRecorded` authorizes modeled settlement-commitment state.
* Every other outcome does not record.

Stage-ordering cases must prove at minimum:

* MainNet peer-driven apply refusal happens before every later stage.
* Evaluator/call-site authorization precedes durable replay.
* Durable replay precedes mutation engine.
* Mutation engine precedes modeled applier.
* Modeled applier precedes sink intent.
* Sink receipt precedes completion report.
* Completion report precedes finalization.
* Finalization precedes attestation.
* Attestation precedes backend request.
* Backend submission precedes audit receipt request.
* Audit receipt precedes acknowledgement request.
* Acknowledgement record precedes consumer request.
* Consumer record precedes settlement-projection request.
* Settlement-projection record precedes settlement-commitment request.
* Settlement-commitment record happens only after settlement-projection success.
* Rejection before settlement-commitment stage leaves settlement-commitment invocation count zero.
* Settlement-commitment record failure does not invalidate settlement-projection state but does not authorize commitment state.
* Settlement-commitment rollback failure is fatal/fail-closed and does not authorize commitment state.

Settlement-commitment ledger cases must prove at minimum:

* One valid settlement-commitment record inserts one in-memory record.
* Duplicate identical settlement-commitment record is idempotent and does not increase count.
* Same record id with different digest is equivocation and does not increase count.
* Wrong commitment / settlement-projection / consumer / acknowledgement / receipt / backend / attestation / finalization / report / sink / pipeline / proposal / decision / candidate / sequence digest does not record.
* Malformed request / response does not record.
* Rollback restores in-memory ledger snapshot.
* Rollback failure never claims success.
* Fixture ledger never writes files, RocksDB, markers, sequences, external publications, confirmations, audit-ledger entries, settlement records, settlement-finality records, or production durable state.

Non-mutation cases must prove at minimum:

* no Run 070 call;
* no `LivePqcTrustState` mutation;
* no trust swap;
* no session eviction;
* no sequence write;
* no marker write;
* no RocksDB/file/schema/migration/storage-format write;
* no wire/schema/marker/sequence/trust-bundle change;
* no external network publication;
* no real settlement;
* no real settlement finality;
* no real external-publication confirmation;
* no real audit-ledger write;
* fixture settlement-commitment sink mutates only modeled in-memory `DurableCompletionSettlementCommitmentLedger`;
* production/MainNet/external paths remain unavailable/fail-closed.

Real release-binary scenarios:

The harness must run the real `target/release/qbind-node` for at least:

* S1 — `--help`: no settlement-commitment / settlement-finality / settlement / external-publication / confirmation production enablement banner or public flag drift.
* S2 — default DevNet startup parse/smoke: no settlement-commitment / settlement-finality / settlement / publication / confirmation enablement claim.
* S3 — default TestNet startup parse/smoke: no settlement-commitment / settlement-finality / settlement / publication / confirmation enablement claim.
* S4 — default MainNet startup parse/smoke: no settlement-commitment / settlement-finality / settlement / publication / confirmation enablement claim.
* S5 — hidden governance-execution selector or equivalent hidden selector still parses as before, without exposing the Run 266/267 helper-only path as public production enablement.
* S6 — invalid governance-execution selector or equivalent invalid selector fails closed before mutation if applicable, with no marker write, no sequence write, no live trust swap, no session eviction, no Run 070 call, no receipt sink invocation, no acknowledgement sink invocation, no consumer invocation, no settlement-projection invocation, no settlement-commitment invocation, no audit-ledger write, no external-publication confirmation, no external publication, no settlement, and no settlement finality.

Do not add any new public CLI surface for Run 267. If a hidden selector already exists from earlier runs, the harness may use it only as a parse/fail-closed smoke surface.

Denylist:

Prove captured real-binary logs and helper logs do not contain active/enabled claims for:

* real settlement backend enabled;
* real settlement finality enabled;
* durable-completion settlement-commitment production enabled;
* MainNet settlement commitment enabled;
* external settlement commitment enabled;
* durable-completion settlement-projection production enabled;
* real audit-ledger acknowledgement backend enabled;
* real external-publication confirmation backend enabled;
* real external publication backend enabled;
* real production attestation backend enabled;
* real finalization backend enabled;
* real completion-report backend enabled;
* real durable consume backend enabled;
* real persistent replay backend enabled;
* real production mutation engine enabled;
* MainNet mutation engine enabled;
* MainNet governance enabled;
* MainNet peer-driven apply enabled;
* real governance execution engine enabled;
* real on-chain governance proof verifier enabled;
* RocksDB replay backend enabled;
* file replay backend enabled;
* schema migration enabled;
* storage-format migration enabled;
* KMS/HSM backend enabled;
* RemoteSigner backend enabled;
* validator-set rotation enabled;
* policy-change action enabled;
* autonomous apply;
* apply-on-receipt;
* apply-on-acknowledgement;
* apply-on-consumer;
* apply-on-settlement-projection;
* apply-on-settlement-commitment;
* apply-on-settlement;
* peer-majority authority;
* Run 070 apply from the settlement-commitment boundary;
* `LivePqcTrustState` mutation from the settlement-commitment boundary;
* real trust swap from the settlement-commitment boundary;
* session eviction from the settlement-commitment boundary;
* marker write from the settlement-commitment boundary;
* sequence write from the settlement-commitment boundary;
* RocksDB write from the settlement-commitment boundary;
* file write from the settlement-commitment boundary;
* external publication by the fixture settlement-commitment sink;
* external-publication confirmation by the fixture settlement-commitment sink;
* audit-ledger write by the fixture settlement-commitment sink;
* settlement write by the fixture settlement-commitment sink;
* settlement finality write by the fixture settlement-commitment sink;
* production durable consume by the fixture settlement-commitment sink;
* production finalization by the fixture settlement-commitment sink;
* production attestation by the fixture settlement-commitment sink;
* production backend submission by the fixture settlement-commitment sink;
* production audit receipt by the fixture settlement-commitment sink;
* production acknowledgement by the fixture settlement-commitment sink;
* production consumer by the fixture settlement-commitment sink;
* production settlement projection by the fixture settlement-commitment sink;
* DummySig / DummyKem / DummyAead active on production path.

Source/release reachability proof:

The harness must grep production source for Run 266 symbols and prove the release helper exercises them in release mode.

Required greps include:

* `pqc_governance_durable_completion_settlement_commitment`
* `DurableCompletionSettlementCommitmentKind`
* `DurableCompletionSettlementCommitmentPolicy`
* `DurableCompletionSettlementCommitmentIdentity`
* `DurableCompletionSettlementCommitmentInput`
* `DurableCompletionSettlementCommitmentExpectations`
* `DurableCompletionSettlementCommitmentRequest`
* `DurableCompletionSettlementCommitmentResponse`
* `DurableCompletionSettlementCommitmentRecord`
* `DurableCompletionSettlementCommitmentDigest`
* `DurableCompletionSettlementCommitmentTranscriptDigest`
* `DurableCompletionSettlementCommitmentOutcome`
* `GovernanceDurableCompletionSettlementCommitmentSink`
* `FixtureDurableCompletionSettlementCommitmentSink`
* `ProductionSettlementCommitmentSink`
* `MainNetSettlementCommitmentSink`
* `ExternalSettlementCommitmentSink`
* `DurableCompletionSettlementCommitmentLedger`
* `DurableCompletionSettlementCommitmentLedgerRecord`
* `DurableCompletionSettlementCommitmentLedgerSnapshot`
* `DurableCompletionSettlementCommitmentLedgerStatus`
* `project_settlement_projection_outcome_to_commitment_request`
* `evaluate_durable_completion_settlement_commitment`
* `recover_durable_completion_settlement_commitment_window`
* `settlement_commitment_outcome_authorizes_record`
* `settlement_commitment_outcome_projects_to_recorded`
* `SettlementCommitmentRecorded`
* `SettlementCommitmentDuplicateIdempotent`
* `SettlementCommitmentRejectedBeforeRecord`
* `SettlementCommitmentRecordFailedNoCommitment`
* `SettlementCommitmentRolledBackNoCommitment`
* `SettlementCommitmentRollbackFailedFatalNoCommitment`
* `SettlementCommitmentAmbiguousFailClosedNoCommitment`
* `ProductionSettlementCommitmentUnavailableNoCommitment`
* `MainNetSettlementCommitmentUnavailableNoCommitment`
* `ExternalSettlementCommitmentUnavailableNoCommitment`
* `MainNetPeerDrivenApplyRefusedNoCommitment`
* `ValidatorSetRotationUnsupportedNoCommitment`
* `PolicyChangeUnsupportedNoCommitment`
* all Run 266 invariant helper names.

If exact names differ, use the exact implemented Run 266 names and record differences honestly in `QBIND_DEVNET_EVIDENCE_RUN_267.md`.

Validation commands:

Run at minimum:

* `cargo build -p qbind-node --release --bin qbind-node`
* `cargo build -p qbind-node --release --example run_267_durable_completion_settlement_commitment_release_binary_helper`
* `bash scripts/devnet/run_267_durable_completion_settlement_commitment_release_binary.sh`
* `cargo test -p qbind-node --test run_266_durable_completion_settlement_commitment_tests`
* `cargo test -p qbind-node --test run_264_durable_completion_consumer_settlement_projection_tests`
* `cargo test -p qbind-node --test run_262_durable_completion_acknowledgement_consumer_tests`
* `cargo test -p qbind-node --test run_260_durable_completion_audit_receipt_acknowledgement_tests`
* `cargo test -p qbind-node --test run_258_durable_completion_audit_publication_receipt_tests`
* `cargo test -p qbind-node --test run_256_durable_completion_attestation_backend_tests`
* `cargo test -p qbind-node --test run_254_modeled_durable_completion_attestation_projection_tests`
* `cargo test -p qbind-node --test run_252_modeled_durable_completion_finalization_projection_tests`
* `cargo test -p qbind-node --test run_250_modeled_durable_consume_completion_reporter_tests`
* `cargo test -p qbind-node --test run_248_modeled_durable_consume_projection_sink_tests`
* `cargo test -p qbind-node --test run_246_governance_modeled_end_to_end_pipeline_tests`
* `cargo test -p qbind-node --test run_244_modeled_governance_trust_mutation_applier_tests`
* `cargo test -p qbind-node --test run_242_governance_execution_mutation_engine_tests`
* `cargo test -p qbind-node --test run_240_governance_evaluator_replay_durable_runtime_integration_tests`
* `cargo test -p qbind-node --test run_238_governance_evaluator_replay_durable_backend_tests`
* `cargo test -p qbind-node --test run_236_governance_evaluator_replay_consume_runtime_integration_tests`
* `cargo test -p qbind-node --test run_234_governance_evaluator_replay_consume_boundary_tests`
* `cargo test -p qbind-node --test run_232_governance_evaluator_replay_runtime_integration_tests`
* `cargo test -p qbind-node --test run_230_governance_evaluator_replay_state_tests`
* `cargo test -p qbind-node --test run_228_peer_evaluator_context_representation_tests`
* `cargo test -p qbind-node --test run_226_governance_evaluator_runtime_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_224_governance_evaluator_runtime_integration_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

Security scanning:

* Run secret scanning if available and report the result.
* Run CodeQL if available and report the result honestly.
* If Rust CodeQL analysis is skipped because the database is too large, times out, tooling is unavailable, or the checker classifies the change set as trivial, say that explicitly and do not claim CodeQL coverage.

Documentation:

Create:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_267.md`
* `docs/devnet/run_267_durable_completion_settlement_commitment_release_binary/README.md`
* `docs/devnet/run_267_durable_completion_settlement_commitment_release_binary/summary.txt`
* `docs/devnet/run_267_durable_completion_settlement_commitment_release_binary/.gitignore`

Update narrowly:

* `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
* `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
* `docs/whitepaper/contradiction.md`

Docs must state:

* Run 267 is release-binary evidence for Run 266.
* No production mutating behavior is enabled.
* The settlement-commitment / ledger-finalization boundary is release-evidenced, not production-enabled.
* The release helper exercises Run 266 production library symbols in release mode.
* The release helper remains dead code from production runtime.
* The fixture settlement-commitment sink mutates only modeled in-memory `DurableCompletionSettlementCommitmentLedger` state.
* Production settlement-commitment, MainNet settlement-commitment, and external settlement-commitment sinks remain reachable but unavailable/fail-closed.
* Run 264 `SettlementProjectionRecorded` is required before any settlement-commitment request can exist.
* Only `SettlementCommitmentRecorded` authorizes modeled settlement-commitment state.
* Every non-recorded settlement-projection outcome produces no settlement-commitment request and no settlement-commitment record.
* Duplicate identical settlement-commitment record is idempotent.
* Same settlement-commitment record id with different digest fails closed as equivocation.
* Failed settlement-commitment record, rollback, rollback-failed, ambiguous windows, unavailable production/MainNet/external paths, rejected replay states, and unsupported actions never record.
* MainNet peer-driven apply remains refused before pipeline progression, sink invocation, reporter invocation, finalizer invocation, attestor invocation, backend invocation, receipt invocation, acknowledgement invocation, consumer invocation, settlement-projection invocation, and settlement-commitment invocation.
* Rejected settlement-commitment paths are non-mutating.
* No real settlement is implemented.
* No real settlement finality is implemented.
* No real audit-ledger acknowledgement is implemented.
* No real external-publication confirmation is implemented.
* No real external publication is implemented.
* No real production attestation backend is implemented.
* No real finalization backend is implemented.
* No real completion-report backend is implemented.
* No real durable consume backend is implemented.
* No real persistent replay backend is implemented.
* No real governance execution engine is implemented.
* No real production mutation engine is implemented.
* No real on-chain governance proof verifier is implemented.
* No RocksDB/file/schema/migration/storage-format change is implemented.
* No wire/schema/marker/sequence/trust-bundle change is implemented.
* No KMS/HSM/RemoteSigner backend is implemented.
* No MainNet governance enablement.
* No MainNet peer-driven apply enablement.
* No validator-set rotation.
* No policy-change enablement.
* The settlement-commitment boundary does not call Run 070.
* The settlement-commitment boundary does not mutate `LivePqcTrustState`.
* The settlement-commitment boundary does not perform a real trust swap.
* The settlement-commitment boundary does not evict sessions.
* The settlement-commitment boundary does not write sequence files.
* The settlement-commitment boundary does not write authority markers.
* The settlement-commitment boundary does not perform external publication.
* The settlement-commitment boundary does not perform real external-publication confirmation.
* The settlement-commitment boundary does not write a real audit ledger.
* The settlement-commitment boundary does not perform real settlement.
* The settlement-commitment boundary does not implement real settlement finality.
* Full C4 remains OPEN.
* C5 remains OPEN.

Contradiction handling:

* Crosscheck Run 267 against Runs 050–266.
* If no contradiction is found, add a “no Run 267 contradiction” entry to `docs/whitepaper/contradiction.md`.
* If contradiction is found, record it explicitly with exact file/function/doc reference and recommend the smallest safe fix.

Expected verdict:

Positive release-binary evidence only if:

* release helper builds;
* release harness exits 0;
* S1–S6 pass;
* denylist empty;
* regression corpus passes;
* docs/evidence are updated;
* contradiction entry is present;
* scans are reported honestly.

Run 267 closes only the Run 266 release-binary evidence gap. It must not claim Full C4 or C5 closure.

Final response format:

1. Exact verdict.
2. Files changed.
3. Release artifacts and hashes.
4. Helper corpus results.
5. Real-binary scenarios.
6. Tests run and results.
7. Security invariants preserved.
8. Honest limitations.
9. C4/C5 status.
10. Suggested Run 268 next step.
