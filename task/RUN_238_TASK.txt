Task: Run 238 — Source/Test Durable Governance Evaluator Replay State Backend Boundary

Current accepted state:

* Run 230: source/test governance evaluator replay/freshness state boundary landed.
* Run 231: release-binary replay/freshness state evidence fully closed.
* Run 232: source/test governance evaluator replay/freshness runtime integration landed.
* Run 233: release-binary replay/freshness runtime integration evidence fully closed.
* Run 234: source/test post-mutation replay consume boundary landed.
* Run 235: release-binary post-mutation replay consume boundary evidence fully closed.
* Run 236: source/test replay consume runtime integration landed.
* Run 237: release-binary replay consume runtime integration evidence fully closed:

  * release-built helper exercises Run 236 integration symbols;
  * replay/freshness runtime integration runs before consume;
  * fresh is required before mutation authorization;
  * consume is after-success-only;
  * deferred, validation-only, before-apply, failed, rollback, unsupported, and MainNet-refused paths do not consume;
  * fixture consume remains DevNet/TestNet evidence-only;
  * production/MainNet consume remains unavailable/fail-closed;
  * no storage/schema/migration/RocksDB/file-format change;
  * no wire/schema/marker/sequence/trust-bundle change;
  * MainNet peer-driven apply remains refused;
  * Full C4 remains OPEN;
  * C5 remains OPEN.
* Current remaining gap:
  Replay/freshness and consume are now validated and release-evidenced, but the durable backend boundary for replay-state persistence is still absent. Existing fixture state is source/test only. Production/MainNet storage remains unavailable/fail-closed. Before any real storage implementation, QBIND needs a typed backend contract that specifies durability, atomicity, crash-window behavior, and fail-closed semantics without introducing a RocksDB schema or file format.

Objective:
Add a source/test-only durable replay state backend boundary for governance evaluator replay/freshness and consume state.

This run must define the pure backend contract and source/test fixture behavior for durable replay-state semantics:

* observe first-seen decisions;
* detect replay / consumed / superseded states;
* consume only after successful mutation;
* model restart durability in tests;
* classify crash windows;
* fail closed for production/MainNet backends.

This run must not implement a real RocksDB backend, file format, database migration, or production persistence.

Release-binary durable-backend evidence is deferred to Run 239.

Strict scope:

* Source/test only.
* No release-binary harness.
* No real governance execution engine.
* No real mutation engine.
* No real on-chain governance proof verifier.
* No MainNet governance enablement.
* No MainNet peer-driven apply enablement.
* No validator-set rotation.
* No KMS/HSM backend implementation.
* No RemoteSigner backend implementation.
* No RocksDB schema change.
* No file-format change.
* No database migration.
* No wire/schema/marker/sequence/trust-bundle change.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* Do not weaken Runs 070, 130–237.
* Do not claim full C4 or C5 closure.

Required source behavior:

1. Add a focused pure module, preferably:
   `crates/qbind-node/src/pqc_governance_evaluator_replay_durable_backend.rs`

2. Define typed durable backend key/input structures binding:

   * replay state key digest;
   * evaluator source identity digest;
   * evaluator request digest;
   * evaluator response digest;
   * evaluator transcript digest;
   * governance execution decision digest;
   * proposal id;
   * decision id;
   * lifecycle action;
   * candidate digest;
   * authority-domain sequence;
   * effective epoch;
   * expiry epoch;
   * replay nonce;
   * environment;
   * chain id;
   * genesis hash;
   * validation surface;
   * mutation surface;
   * current canonical epoch.

3. Define durable backend record classifications:

   * `Missing`;
   * `ObservedFresh`;
   * `ObservedDeferred`;
   * `ObservedExpired`;
   * `ObservedStale`;
   * `Consumed`;
   * `ReplayDetected`;
   * `Superseded`;
   * `MalformedRecord`;
   * `BackendUnavailable`;
   * `ProductionBackendUnavailable`;
   * `MainNetBackendUnavailable`.

4. Define backend operation outcomes:

   * `ProceedFirstSeen`;
   * `ProceedKnownFresh`;
   * `ProceedDeferred`;
   * `FailClosedExpired`;
   * `FailClosedStale`;
   * `FailClosedReplay`;
   * `FailClosedConsumed`;
   * `FailClosedSuperseded`;
   * `FailClosedMalformedRecord`;
   * `FailClosedBackendUnavailable`;
   * `FailClosedProductionUnavailable`;
   * `FailClosedMainNetUnavailable`.

5. Define atomic backend operation boundary:

   * `observe_decision_if_absent`;
   * `mark_consumed_after_success`;
   * `read_decision_state`;
   * `compare_and_mark_consumed`;
   * `restart_snapshot` or equivalent source/test fixture method to model restart durability without creating a real file format.

6. Define crash-window classification:

   * `BeforeObserve`;
   * `AfterObserveBeforeMutation`;
   * `AfterMutationBeforeConsume`;
   * `AfterConsume`;
   * `RollbackAfterObserve`;
   * `ApplyFailedAfterObserve`;
   * `UnknownCrashWindow`;
   * `ProductionCrashWindowUnavailable`;
   * `MainNetCrashWindowUnavailable`.

7. Define deterministic digest helpers:

   * durable backend key digest;
   * durable record digest;
   * durable operation transcript digest;
   * crash-window transcript digest.

8. Add backend traits:

   * `GovernanceEvaluatorReplayDurableBackendReader`;
   * `GovernanceEvaluatorReplayDurableBackendWriter`;
   * optionally `GovernanceEvaluatorReplayDurableBackendAtomic` if useful.

9. Backend implementations for this run:

   * DevNet/TestNet fixture durable backend only.
   * Fixture may be in-memory with explicit snapshot/restart modeling.
   * Production backend must be callable but unavailable/fail-closed.
   * MainNet backend must be callable but unavailable/fail-closed.
   * No real RocksDB, filesystem, database, migration, or production persistent backend.

10. Compose with existing boundaries where feasible:

* Run 230 replay/freshness state reader/writer traits;
* Run 234 consume boundary;
* Run 236 consume runtime integration.
  This composition can be helper-level/pure source-test only.

11. Preserve mutation safety:
    Backend rejection must occur before:

* Run 070 call;
* live trust swap;
* session eviction;
* sequence write;
* marker write.

12. Preserve MainNet refusal:
    MainNet peer-driven apply remains refused even if durable backend fixture says fresh.

Required accepted scenarios:
A1. first-seen DevNet fixture decision records `ObservedFresh`.

A2. first-seen TestNet fixture decision records `ObservedFresh`.

A3. known fresh decision reads as `ProceedKnownFresh`.

A4. not-yet-effective decision records/reads as deferred and is not mutation approval.

A5. expired decision records/reads as fail-closed expired.

A6. stale decision records/reads as fail-closed stale.

A7. explicit consume after successful mutation marks consumed.

A8. same decision after consume reads as consumed / fail-closed.

A9. read-only validation does not mark consumed.

A10. observe-only state survives fixture restart snapshot.

A11. consumed state survives fixture restart snapshot.

A12. rollback after observe does not mark consumed.

A13. apply-failed after observe does not mark consumed.

A14. after-mutation-before-consume crash window is typed and not silently approved.

A15. after-consume crash window reads consumed / fail-closed for repeat decision.

A16. durable backend key digest is deterministic.

A17. durable record digest is deterministic.

A18. durable operation transcript digest is deterministic.

A19. crash-window transcript digest is deterministic.

A20. production durable backend is callable and fails closed unavailable.

A21. MainNet durable backend is callable and fails closed unavailable/refused.

A22. Run 236 consume runtime integration remains compatible when durable backend is not wired.

Required rejection scenarios:
R1. wrong replay state key digest rejected.

R2. wrong source identity digest rejected.

R3. wrong request digest rejected.

R4. wrong response digest rejected.

R5. wrong transcript digest rejected.

R6. wrong decision digest rejected.

R7. wrong proposal id rejected.

R8. wrong decision id rejected.

R9. wrong lifecycle action rejected.

R10. wrong candidate digest rejected.

R11. wrong authority-domain sequence rejected.

R12. wrong effective epoch rejected.

R13. wrong expiry epoch rejected.

R14. wrong replay nonce rejected.

R15. wrong environment rejected.

R16. wrong chain rejected.

R17. wrong genesis rejected.

R18. wrong validation surface rejected.

R19. wrong mutation surface rejected.

R20. malformed backend record rejected.

R21. replay detected rejected.

R22. consumed decision rejected.

R23. superseded decision rejected.

R24. backend unavailable rejected.

R25. production backend unavailable rejected.

R26. MainNet backend unavailable/refused rejected.

R27. compare-and-mark-consumed with wrong expected state rejected.

R28. consume attempted before observe rejected.

R29. consume attempted before successful mutation rejected.

R30. consume attempted after failed apply rejected.

R31. consume attempted after rollback rejected.

R32. local operator cannot satisfy durable replay backend policy.

R33. peer majority cannot satisfy durable replay backend policy.

R34. validator-set rotation unsupported rejected.

R35. policy-change action unsupported rejected.

R36. rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

R37. MainNet peer-driven apply remains refused even when durable backend fixture says fresh.

Required tests:
Add focused tests, preferably:

`crates/qbind-node/tests/run_238_governance_evaluator_replay_durable_backend_tests.rs`

Minimum coverage:

* A1–A22.
* R1–R37.
* deterministic digest tests.
* restart snapshot tests.
* observed/fresh/deferred/expired/stale/consumed/replay/superseded tests.
* crash-window classification tests.
* compare-and-mark-consumed atomicity tests.
* read-only validation does not consume tests.
* production/MainNet unavailable tests.
* no-mutation tests.
* MainNet refusal tests.
* compatibility with Runs 236, 234, 232, 230, 228, 226, 224.

Required documentation:
Add:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_238.md`

Update:

* `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
* `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
* `docs/whitepaper/contradiction.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

Documentation must state:

* Run 238 is source/test durable replay state backend boundary work.
* It defines a typed durable backend contract but does not implement production persistence.
* Fixture durable backend is DevNet/TestNet source-test only.
* Production/MainNet durable backend remains unavailable/fail-closed.
* Restart durability is modeled only through source/test fixture snapshot.
* No RocksDB/file/schema/migration/storage-format change is implemented.
* No real governance engine, mutation engine, or on-chain proof verifier is implemented.
* MainNet peer-driven apply remains refused.
* Validator-set rotation remains unsupported.
* Release-binary durable-backend evidence is deferred to Run 239.
* Full C4 remains OPEN.
* C5 remains OPEN.

Validation commands:
Run at minimum:

* `cargo build -p qbind-node --lib`
* `cargo test -p qbind-node --test run_238_governance_evaluator_replay_durable_backend_tests`
* `cargo test -p qbind-node --test run_236_governance_evaluator_replay_consume_runtime_integration_tests`
* `cargo test -p qbind-node --test run_234_governance_evaluator_replay_consume_boundary_tests`
* `cargo test -p qbind-node --test run_232_governance_evaluator_replay_runtime_integration_tests`
* `cargo test -p qbind-node --test run_230_governance_evaluator_replay_state_tests`
* `cargo test -p qbind-node --test run_228_peer_evaluator_context_representation_tests`
* `cargo test -p qbind-node --test run_226_governance_evaluator_runtime_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_224_governance_evaluator_runtime_integration_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

Acceptance criteria:
Run 238 is acceptable only if:

1. A typed durable replay state backend boundary exists.
2. Fixture backend models observed/consumed/replayed/superseded states.
3. Fixture restart snapshot models durability without real file format.
4. Compare-and-mark-consumed atomicity is tested.
5. Production/MainNet durable backend remains unavailable/fail-closed.
6. Rejections are non-mutating.
7. MainNet peer-driven apply remains refused.
8. No RocksDB/file/schema/migration/storage-format change is claimed.
9. Release-binary evidence is deferred to Run 239.
10. No full C4 or C5 closure is claimed.
