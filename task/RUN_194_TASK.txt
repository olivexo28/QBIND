Task: Run 194 — Source/Test RemoteSigner Production-Custody Interface Boundary

Current accepted state:

* Run 188: source/test authority-custody boundary landed.
* Run 189: release-binary authority custody-boundary evidence fully closed.
* Run 190: source/test custody metadata carrying and production-context routing landed.
* Run 191: release-binary custody metadata carrying evidence fully closed.
* Run 192: source/test hidden authority-custody policy selector landed.
* Run 193: release-binary authority-custody policy selector evidence fully closed:

  * real `target/release/qbind-node` exercises hidden custody selector behavior;
  * default remains `AuthorityCustodyPolicy::Disabled`;
  * CLI/env selector behavior works;
  * CLI-over-env precedence proven;
  * invalid selector values fail closed;
  * KMS/HSM/RemoteSigner placeholders remain fail-closed;
  * fixture/local custody cannot satisfy MainNet production custody;
  * MainNet peer-driven apply remains refused.
* Current limitation:
  RemoteSigner / KMS / HSM are still placeholders. No production custody backend exists.
* MainNet peer-driven apply remains refused.
* governance execution remains unimplemented.
* real on-chain proof verification remains unimplemented.
* validator-set rotation remains open.
* full C4 remains open.
* C5 remains open.

Objective:
Add a source/test-only RemoteSigner production-custody interface boundary for authority lifecycle signing/custody.

This run must define a typed remote signer protocol/interface and fail-closed validation path, without enabling a real production remote signer backend and without enabling MainNet apply.

The purpose is to replace the vague `RemoteSigner` placeholder with a precise production-custody boundary that can later be implemented safely.

Release-binary RemoteSigner boundary evidence is deferred to Run 195.

Strict scope:

* Source/test only.
* No release-binary harness in this run.
* No real remote signer backend.
* No networked signer service.
* No real KMS implementation.
* No real HSM implementation.
* No cloud KMS integration.
* No PKCS#11 integration.
* No MainNet peer-driven apply enablement.
* No governance execution engine.
* No real on-chain proof verifier.
* No validator-set rotation.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* No marker schema change.
* No sequence-file schema change.
* No trust-bundle core schema change.
* No wire/schema change unless strictly additive and required for RemoteSigner attestation metadata.
* Do not weaken Runs 070, 130–193.
* Do not claim full C4 or C5 closure.

Required design:
Add a typed RemoteSigner custody interface module, preferably:

`crates/qbind-node/src/pqc_remote_authority_signer.rs`

or similar.

It must define:

1. Remote signer identity:

* signer_id;
* signer_public_identity;
* custody_key_id;
* authority_root_fingerprint;
* bundle-signing key fingerprint;
* environment;
* chain_id;
* genesis_hash;
* supported suite id;
* supported authority lifecycle actions;
* attestation digest / placeholder commitment;
* freshness / expiry.

2. Remote signer request:
   Must bind:

* environment;
* chain_id;
* genesis_hash;
* authority_root_fingerprint;
* lifecycle action;
* candidate digest;
* authority-domain sequence;
* active/new/revoked signing-key fingerprint where applicable;
* governance proof digest where applicable;
* custody attestation digest;
* replay nonce;
* request timestamp / epoch if represented.

3. Remote signer response:
   Must bind:

* request digest;
* signer_id;
* custody_key_id;
* signature suite;
* signature bytes or placeholder signature commitment;
* response nonce / anti-replay material;
* expiry / freshness window.

4. Remote signer verifier/policy:
   At minimum:

* `Disabled` default;
* `FixtureLoopbackAllowed` for source/test only;
* `ProductionRemoteSignerRequired` fail-closed until real backend exists;
* `MainnetProductionRemoteSignerRequired` fail-closed.

5. Typed outcomes:
   Must distinguish:

* fixture loopback accepted;
* disabled;
* production remote signer unavailable;
* MainNet production remote signer unavailable;
* wrong environment;
* wrong chain;
* wrong genesis;
* wrong authority root;
* wrong custody key id;
* wrong signing-key fingerprint;
* wrong lifecycle action;
* wrong candidate digest;
* wrong authority-domain sequence;
* wrong request digest;
* stale/replayed request;
* stale/replayed response;
* expired attestation;
* expired response;
* unsupported suite;
* invalid signature / invalid placeholder signature;
* malformed request;
* malformed response;
* local operator key cannot satisfy remote signer;
* peer majority cannot satisfy remote signer.

Required source behavior:

1. Define a pure `RemoteAuthoritySigner` trait or equivalent boundary.
2. Define a fixture loopback signer for tests only.
3. Define a production remote signer mode that is callable but returns `ProductionRemoteSignerUnavailable` or equivalent.
4. Add conversion/composition with Run 188 custody metadata:

   * `AuthorityCustodyClass::RemoteSigner` should be able to route to this boundary at source/test level.
5. Add a pure composition helper:

   * lifecycle validation;
   * governance validation where applicable;
   * custody validation;
   * remote signer attestation/response validation.
6. No I/O is allowed in the verifier path.
7. No marker write, sequence write, live trust swap, session eviction, or Run 070 call.
8. MainNet peer-driven apply remains refused even if fixture loopback signs successfully.

Required accepted scenarios:
A1. fixture loopback remote signer accepted under explicit fixture policy on DevNet.

A2. fixture loopback remote signer accepted under explicit fixture policy on TestNet.

A3. remote signer request/response binds environment, chain, genesis, authority root, lifecycle action, candidate digest, authority-domain sequence.

A4. custody metadata with `AuthorityCustodyClass::RemoteSigner` routes into the remote signer boundary.

A5. combined lifecycle + governance + custody + fixture remote signer accepted for DevNet source/test.

A6. GenesisBound / EmergencyCouncil / OnChainGovernance proof behavior remains unchanged when remote signer policy is Disabled.

A7. production remote signer boundary callable and returns typed unavailable outcome.

Required rejection scenarios:
R1. remote signer rejected under Disabled policy.

R2. fixture loopback rejected under ProductionRemoteSignerRequired.

R3. fixture loopback rejected under MainnetProductionRemoteSignerRequired.

R4. production remote signer rejected as unavailable.

R5. MainNet production remote signer rejected as unavailable.

R6. wrong environment rejected.

R7. wrong chain rejected.

R8. wrong genesis rejected.

R9. wrong authority root rejected.

R10. wrong custody key id rejected.

R11. wrong signing-key fingerprint rejected.

R12. wrong lifecycle action rejected.

R13. wrong candidate digest rejected.

R14. wrong authority-domain sequence rejected.

R15. wrong request digest rejected.

R16. stale/replayed request rejected.

R17. stale/replayed response rejected.

R18. expired attestation rejected.

R19. expired response rejected.

R20. unsupported suite rejected.

R21. invalid signature / placeholder signature rejected.

R22. malformed request rejected.

R23. malformed response rejected.

R24. local operator key cannot satisfy remote signer policy.

R25. peer majority / gossip count cannot satisfy remote signer policy.

R26. remote signer valid but custody metadata invalid rejected.

R27. custody valid but remote signer response invalid rejected.

R28. lifecycle valid + governance valid + custody valid + production remote signer unavailable rejected.

R29. MainNet peer-driven apply remains refused even with fixture loopback remote signer.

R30. validation-only rejection remains non-mutating.

R31. mutating preflight rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

Required tests:
Add focused tests, preferably:

`crates/qbind-node/tests/run_194_remote_authority_signer_boundary_tests.rs`

Minimum coverage:

* A1–A7.
* R1–R31.
* request/response canonical digest determinism.
* replay/freshness checks.
* fixture-vs-production separation.
* source reachability from Run 188/190 custody composition.
* no-I/O guarantee.
* no-mutation guarantee.
* MainNet refusal invariants.

Required documentation:
Add:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_194.md`

Update:

* `docs/whitepaper/contradiction.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

Documentation must state:

* Run 194 is source/test RemoteSigner production-custody interface boundary work.
* No real RemoteSigner backend is implemented.
* Fixture loopback remote signer is DevNet/TestNet source/test only.
* Production RemoteSigner remains unavailable/fail-closed.
* RemoteSigner does not enable MainNet peer-driven apply.
* KMS/HSM remain unimplemented.
* governance execution remains unimplemented.
* real on-chain proof verification remains unimplemented.
* validator-set rotation remains open.
* release-binary RemoteSigner boundary evidence is deferred to Run 195.
* full C4 remains open.
* C5 remains open.

Validation commands:
Run at minimum:

* `cargo build -p qbind-node --lib`
* `cargo test -p qbind-node --test run_194_remote_authority_signer_boundary_tests`
* `cargo test -p qbind-node --test run_192_authority_custody_policy_selector_tests`
* `cargo test -p qbind-node --test run_190_authority_custody_payload_callsite_tests`
* `cargo test -p qbind-node --test run_188_authority_custody_boundary_tests`
* `cargo test -p qbind-node --test run_186_onchain_governance_production_verifier_boundary_tests`
* `cargo test -p qbind-node --test run_184_onchain_governance_payload_carrying_tests`
* `cargo test -p qbind-node --test run_182_onchain_governance_production_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_180_onchain_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_178_onchain_governance_proof_tests`
* `cargo test -p qbind-node --test run_176_live_0x05_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_173_validation_only_governance_required_policy_tests`
* `cargo test -p qbind-node --test run_171_governance_required_policy_selector_tests`
* `cargo test -p qbind-node --test run_169_governance_proof_loader_surface_integration_tests`
* `cargo test -p qbind-node --test run_167_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_165_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_163_governance_authority_verifier_tests`
* `cargo test -p qbind-node --test run_161_lifecycle_marker_integration_tests`
* `cargo test -p qbind-node --test run_159_authority_signing_key_lifecycle_tests`
* `cargo test -p qbind-node --test run_157_unified_testnet_fixture_universe_tests`
* `cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests`
* `cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests`
* `cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests`
* `cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests`
* `cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests`
* `cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Acceptance criteria:
Run 194 is acceptable only if:

1. A typed RemoteSigner production-custody interface boundary exists.
2. Fixture loopback RemoteSigner is DevNet/TestNet source-test only.
3. Production RemoteSigner remains unavailable/fail-closed.
4. RemoteSigner request/response binding is deterministic and domain-bound.
5. RemoteSigner integrates with custody composition at source/test level.
6. Local operator / peer majority cannot satisfy RemoteSigner policy.
7. Validation-only surfaces remain non-mutating.
8. Mutating rejection paths produce no mutation.
9. MainNet peer-driven apply remains refused.
10. Docs defer release-binary RemoteSigner boundary evidence to Run 195.
11. No real RemoteSigner / KMS / HSM / governance execution / validator-set rotation claim is made.
12. No full C4 or C5 closure is claimed.
