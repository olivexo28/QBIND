Task: Run 203 — Source/Test KMS-HSM Backend Abstraction Boundary

Current accepted state:

* Run 188: source/test authority-custody boundary landed.
* Run 189: release-binary authority custody-boundary evidence fully closed.
* Run 190: source/test custody metadata carrying and production-context routing landed.
* Run 191: release-binary custody metadata carrying evidence fully closed.
* Run 192: source/test hidden authority-custody policy selector landed.
* Run 193: release-binary custody policy selector evidence fully closed.
* Run 194: source/test RemoteSigner production-custody interface boundary landed.
* Run 195: release-binary RemoteSigner boundary evidence fully closed.
* Run 196: source/test RemoteSigner payload/carrying and production-context wiring landed.
* Run 197: release-binary RemoteSigner payload/carrying evidence fully closed.
* Run 198: source/test RemoteSigner policy selector landed.
* Run 199: release-binary RemoteSigner policy selector evidence fully closed.
* Run 200: C4/C5 closure criteria and remaining-work consolidation landed.
* Run 201: source/test RemoteSigner transport boundary landed.
* Run 202: release-binary RemoteSigner transport-boundary evidence fully closed.
* Current limitation:
  KMS/HSM remain typed placeholders only. No backend abstraction exists for a future KMS/HSM provider, key identity, attestation, request/response binding, or fail-closed production dispatch.
* MainNet peer-driven apply remains refused.
* governance execution remains unimplemented.
* real on-chain proof verification remains unimplemented.
* validator-set rotation remains open.
* full C4 remains open.
* C5 remains open.

Objective:
Add a source/test-only KMS/HSM backend abstraction boundary for production authority custody.

This run must define typed provider-neutral interfaces for KMS/HSM signing authorization, key identity, attestation, request/response binding, and fail-closed production behavior. It must not implement a real KMS/HSM backend.

Release-binary KMS/HSM backend-boundary evidence is deferred to Run 204.

Strict scope:

* Source/test only.
* No release-binary harness in this run.
* No real KMS implementation.
* No real HSM implementation.
* No cloud KMS integration.
* No PKCS#11 integration.
* No networked signer daemon.
* No real RemoteSigner backend.
* No production signing key custody.
* No MainNet peer-driven apply enablement.
* No governance execution engine.
* No real on-chain proof verifier.
* No validator-set rotation.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* No marker schema change.
* No sequence-file schema change.
* No trust-bundle core schema change.
* No authority lifecycle semantics change.
* Do not weaken Runs 070, 130–202.
* Do not claim full C4 or C5 closure.

Required design:
Add a typed KMS/HSM backend abstraction module, preferably:

`crates/qbind-node/src/pqc_authority_kms_hsm_backend.rs`

The module must define:

1. Backend kind:

* `Disabled`;
* `FixtureKms`;
* `FixtureHsm`;
* `CloudKmsUnavailable`;
* `Pkcs11HsmUnavailable`;
* `ProductionKmsUnavailable`;
* `ProductionHsmUnavailable`;
* `Unknown`.

2. Backend policy:

* `Disabled` default;
* `FixtureKmsAllowed`;
* `FixtureHsmAllowed`;
* `ProductionKmsRequired`;
* `ProductionHsmRequired`;
* `MainnetProductionCustodyRequired`.

3. Backend identity/config:
   Must bind:

* backend id;
* provider id;
* key id / key label;
* authority root fingerprint;
* bundle-signing key fingerprint;
* environment;
* chain_id;
* genesis_hash;
* suite id;
* attestation digest / certificate digest placeholder;
* key usage policy;
* allowed lifecycle actions;
* freshness / expiry.

4. Backend request:
   Must bind:

* environment;
* chain_id;
* genesis_hash;
* authority root fingerprint;
* lifecycle action;
* candidate digest;
* authority-domain sequence;
* custody class;
* key id / key label;
* governance proof digest where applicable;
* custody attestation digest;
* request nonce;
* request timestamp / epoch if represented.

5. Backend response:
   Must bind:

* request digest;
* backend id;
* provider id;
* key id / key label;
* signature suite;
* signature bytes or placeholder commitment;
* attestation digest;
* response nonce;
* expiry / freshness.

6. Deterministic digest helpers:

* backend identity digest;
* backend request digest;
* backend response digest;
* backend transcript digest.

7. Backend trait/abstraction:
   Define pure/mockable traits such as:

* `AuthorityCustodyBackend`;
* `sign_authority_lifecycle_request`;
* `verify_authority_custody_backend_response`.

For this run:

* fixture KMS/HSM may exist only for DevNet/TestNet source tests;
* production KMS/HSM backends must be callable but fail closed as unavailable;
* no real network, cloud, PKCS#11, or hardware call is allowed.

8. Typed outcomes:
   Must distinguish:

* fixture KMS accepted;
* fixture HSM accepted;
* backend disabled;
* production KMS unavailable;
* production HSM unavailable;
* cloud KMS unavailable;
* PKCS#11 HSM unavailable;
* MainNet production custody unavailable;
* unknown backend rejected;
* wrong environment;
* wrong chain;
* wrong genesis;
* wrong authority root;
* wrong key id / key label;
* wrong signing-key fingerprint;
* wrong lifecycle action;
* wrong candidate digest;
* wrong authority-domain sequence;
* wrong request digest;
* wrong response digest;
* wrong transcript digest;
* stale/replayed request;
* stale/replayed response;
* expired attestation;
* expired response;
* unsupported suite;
* invalid attestation;
* invalid signature / placeholder signature;
* malformed backend identity;
* malformed backend request;
* malformed backend response;
* local operator cannot satisfy backend policy;
* peer majority cannot satisfy backend policy.

Required source behavior:

1. Backend boundary must compose with Run 188 `AuthorityCustodyClass::{Kms, Hsm}`.
2. Backend boundary must compose with Run 190 custody metadata where feasible.
3. Backend boundary must compose with Run 194/201 RemoteSigner only as a separate custody option, not as a replacement for KMS/HSM.
4. Production backend paths must return unavailable/fail-closed.
5. Fixture KMS/HSM must be DevNet/TestNet source/test only.
6. No marker write, sequence write, live trust swap, session eviction, or Run 070 call.
7. MainNet peer-driven apply remains refused even if fixture KMS/HSM returns a valid response.

Required accepted scenarios:
A1. fixture KMS backend accepted under explicit fixture policy on DevNet.

A2. fixture HSM backend accepted under explicit fixture policy on DevNet.

A3. fixture KMS backend accepted under explicit fixture policy on TestNet.

A4. fixture HSM backend accepted under explicit fixture policy on TestNet.

A5. backend identity digest deterministic.

A6. backend request digest deterministic.

A7. backend response digest deterministic.

A8. backend transcript digest deterministic.

A9. backend request binds environment, chain, genesis, authority root, lifecycle action, candidate digest, authority-domain sequence, custody class, and key id/label.

A10. backend response binds request digest, backend id, provider id, key id/label, signature suite, response digest, and attestation digest.

A11. production KMS boundary callable and returns typed unavailable outcome.

A12. production HSM boundary callable and returns typed unavailable outcome.

A13. Run 188 custody validator remains compatible with KMS/HSM custody classes.

A14. RemoteSigner behavior from Runs 194–202 remains unchanged.

A15. GenesisBound / EmergencyCouncil / OnChainGovernance proof behavior remains unchanged when backend policy is Disabled.

Required rejection scenarios:
R1. backend rejected under Disabled policy.

R2. fixture KMS rejected under ProductionKmsRequired.

R3. fixture HSM rejected under ProductionHsmRequired.

R4. fixture KMS/HSM rejected under MainnetProductionCustodyRequired.

R5. production KMS rejected as unavailable.

R6. production HSM rejected as unavailable.

R7. cloud KMS rejected as unavailable.

R8. PKCS#11 HSM rejected as unavailable.

R9. MainNet production custody rejected as unavailable.

R10. unknown backend rejected.

R11. wrong environment rejected.

R12. wrong chain rejected.

R13. wrong genesis rejected.

R14. wrong authority root rejected.

R15. wrong key id / key label rejected.

R16. wrong signing-key fingerprint rejected.

R17. wrong lifecycle action rejected.

R18. wrong candidate digest rejected.

R19. wrong authority-domain sequence rejected.

R20. wrong request digest rejected.

R21. wrong response digest rejected.

R22. wrong transcript digest rejected.

R23. stale/replayed request rejected.

R24. stale/replayed response rejected.

R25. expired attestation rejected.

R26. expired response rejected.

R27. unsupported suite rejected.

R28. invalid attestation rejected.

R29. invalid signature / placeholder signature rejected.

R30. malformed backend identity rejected.

R31. malformed backend request rejected.

R32. malformed backend response rejected.

R33. local operator cannot satisfy backend policy.

R34. peer majority cannot satisfy backend policy.

R35. backend valid but custody metadata invalid rejected.

R36. custody valid but backend response invalid rejected.

R37. lifecycle + governance + custody valid but production KMS unavailable rejected.

R38. lifecycle + governance + custody valid but production HSM unavailable rejected.

R39. validation-only rejection remains non-mutating.

R40. mutating preflight rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

R41. MainNet peer-driven apply remains refused even with fixture KMS/HSM.

Required tests:
Add focused tests, preferably:

`crates/qbind-node/tests/run_203_kms_hsm_backend_boundary_tests.rs`

Minimum coverage:

* A1–A15.
* R1–R41.
* deterministic digest tests.
* request/response/transcript binding tests.
* fixture-vs-production backend separation.
* cloud KMS / PKCS#11 HSM unavailable tests.
* malformed identity/request/response fail-closed tests.
* no-I/O guarantee for production backend path.
* no-mutation guarantee.
* MainNet refusal invariant.
* compatibility with Run 188 custody, Run 190 custody payload, Run 194–202 RemoteSigner path.

Required documentation:
Add:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_203.md`

Update:

* `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
* `docs/whitepaper/contradiction.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

Documentation must state:

* Run 203 is source/test KMS-HSM backend abstraction boundary work.
* No real KMS backend is implemented.
* No real HSM backend is implemented.
* No cloud KMS integration is implemented.
* No PKCS#11 integration is implemented.
* Fixture KMS/HSM is DevNet/TestNet source/test only.
* Production KMS/HSM remain unavailable/fail-closed.
* KMS/HSM boundary does not enable MainNet peer-driven apply.
* RemoteSigner path remains separate and unchanged.
* governance execution remains unimplemented.
* real on-chain proof verification remains unimplemented.
* validator-set rotation remains open.
* release-binary KMS/HSM backend-boundary evidence is deferred to Run 204.
* full C4 remains open.
* C5 remains open.

Validation commands:
Run at minimum:

* `cargo build -p qbind-node --lib`
* `cargo test -p qbind-node --test run_203_kms_hsm_backend_boundary_tests`
* `cargo test -p qbind-node --test run_201_remote_signer_transport_boundary_tests`
* `cargo test -p qbind-node --test run_198_remote_signer_policy_selector_tests`
* `cargo test -p qbind-node --test run_196_remote_signer_payload_callsite_tests`
* `cargo test -p qbind-node --test run_194_remote_authority_signer_boundary_tests`
* `cargo test -p qbind-node --test run_192_authority_custody_policy_selector_tests`
* `cargo test -p qbind-node --test run_190_authority_custody_payload_callsite_tests`
* `cargo test -p qbind-node --test run_188_authority_custody_boundary_tests`
* `cargo test -p qbind-node --test run_186_onchain_governance_production_verifier_boundary_tests`
* `cargo test -p qbind-node --test run_184_onchain_governance_payload_carrying_tests`
* `cargo test -p qbind-node --test run_182_onchain_governance_production_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_180_onchain_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_178_onchain_governance_proof_tests`
* `cargo test -p qbind-node --test run_176_live_0x05_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_173_validation_only_governance_required_policy_tests`
* `cargo test -p qbind-node --test run_171_governance_required_policy_selector_tests`
* `cargo test -p qbind-node --test run_169_governance_proof_loader_surface_integration_tests`
* `cargo test -p qbind-node --test run_167_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_165_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_163_governance_authority_verifier_tests`
* `cargo test -p qbind-node --test run_161_lifecycle_marker_integration_tests`
* `cargo test -p qbind-node --test run_159_authority_signing_key_lifecycle_tests`
* `cargo test -p qbind-node --test run_157_unified_testnet_fixture_universe_tests`
* `cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests`
* `cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests`
* `cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests`
* `cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests`
* `cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests`
* `cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Acceptance criteria:
Run 203 is acceptable only if:

1. A typed KMS/HSM backend abstraction boundary exists.
2. Fixture KMS/HSM are DevNet/TestNet source-test only.
3. Production KMS/HSM remain unavailable/fail-closed.
4. Cloud KMS and PKCS#11 HSM are explicitly unavailable/fail-closed.
5. Backend identity/request/response/transcript digests are deterministic and domain-bound.
6. Backend boundary composes with Run 188 custody classes.
7. RemoteSigner path remains separate and unchanged.
8. Validation-only surfaces remain non-mutating.
9. Mutating rejection paths produce no mutation.
10. MainNet peer-driven apply remains refused.
11. Docs defer release-binary backend-boundary evidence to Run 204.
12. No real KMS/HSM / RemoteSigner backend / governance execution / validator-set rotation claim is made.
13. No full C4 or C5 closure is claimed.
