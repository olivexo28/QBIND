Task: Run 229 — Release-Binary Peer Evaluator Context Representation Evidence

Current accepted state:

* Run 226: source/test governance evaluator runtime call-site wiring landed.
* Run 227: release-binary governance evaluator runtime call-site wiring evidence fully closed.
* Run 228: source/test peer evaluator-context representation boundary landed:

  * `crates/qbind-node/src/pqc_governance_evaluator_peer_context.rs`;
  * typed local/source-test-only evaluator-context representation boundary for live inbound `0x05` and peer-driven drain;
  * carrier taxonomy:

    * `Absent`;
    * `Present`;
    * `Malformed`;
    * `UnsupportedSurface`;
    * `WireSchemaUnavailable`;
    * `PeerMajorityUnsupported`;
    * `MainNetRefused`;
  * live wire inability to carry evaluator binding is represented as typed `WireSchemaUnavailable`, never approval;
  * present well-formed context routes into Run 226 call-site wiring, Run 224 integration, and Run 222 evaluator interface where representable;
  * only routed `ProceedMutate` authorizes apply;
  * all rejections are pure and non-mutating;
  * MainNet peer-driven apply remains refused;
  * production/on-chain/MainNet evaluators fail closed;
  * fixture/emergency evaluators remain non-production;
  * validator-set rotation unsupported;
  * no wire/schema/marker/sequence/trust-bundle change;
  * release-binary evidence deferred to Run 229.
* Full C4 remains OPEN.
* C5 remains OPEN.

Objective:
Produce release-binary evidence for the Run 228 peer evaluator-context representation boundary.

This run must prove in release mode that the local/source-test peer evaluator-context representation boundary behaves deterministically, routes valid local context into the existing integration stack where representable, represents missing wire/schema carrier as typed fail-closed status, preserves no-propagation/no-staging/no-apply for invalid live inbound `0x05`, preserves no-apply for invalid peer-driven drain, and keeps MainNet peer-driven apply refused.

Strict scope:

* Release-binary evidence only.
* Use release-built helper and real `target/release/qbind-node` where applicable.
* No production source behavior change unless a tiny harness-only fix is required.
* No real governance execution engine.
* No real on-chain governance proof verifier.
* No MainNet governance enablement.
* No MainNet peer-driven apply enablement.
* No validator-set rotation.
* No KMS/HSM backend implementation.
* No RemoteSigner backend implementation.
* No network wire schema change.
* No trust-bundle schema change.
* No authority-marker schema change.
* No sequence schema change.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* Do not weaken Runs 070, 130–228.
* Do not claim full C4 or C5 closure.

Required deliverables:

1. Add release-binary helper:
   `crates/qbind-node/examples/run_229_peer_evaluator_context_representation_release_binary_helper.rs`

2. Add release-binary harness:
   `scripts/devnet/run_229_peer_evaluator_context_representation_release_binary.sh`

3. Add evidence archive:
   `docs/devnet/run_229_peer_evaluator_context_representation_release_binary/`

4. Add canonical evidence report:
   `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_229.md`

5. Update narrowly:

   * `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
   * `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
   * `docs/whitepaper/contradiction.md`
   * `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
   * `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
   * `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

6. Crosscheck all new/modified code and docs against existing design/spec. If contradictions are found, record them in `contradiction.md`.

Required release-helper matrix:
The release-built helper must exercise Run 228 peer evaluator-context representation symbols through production library code.

Accepted / compatible cases:
A1. live inbound `0x05` with default Disabled and absent evaluator context preserves legacy validation behavior.

A2. peer-driven drain with default Disabled and absent evaluator context preserves legacy validation behavior.

A3. live inbound `0x05` local context binds selected policy, candidate digest, evaluator request digest, evaluator response digest, lifecycle action, sequence, environment, chain id, and genesis hash.

A4. peer-driven drain local context binds selected policy, candidate digest, evaluator request digest, evaluator response digest, lifecycle action, sequence, environment, chain id, and genesis hash.

A5. live inbound `0x05` with valid DevNet fixture evaluator context routes to Run 226 integration where representable, or returns typed `UnsupportedSurface` / `WireSchemaUnavailable` without propagation/staging/apply if not representable.

A6. peer-driven drain with valid DevNet fixture evaluator context routes to Run 226 integration where representable, or returns typed `UnsupportedSurface` / `WireSchemaUnavailable` without apply if not representable.

A7. explicit emergency fixture context is accepted only for explicit emergency action and only in non-production evidence context.

A8. production evaluator context reaches production unavailable / fail-closed outcome.

A9. on-chain evaluator context reaches on-chain unavailable / fail-closed outcome.

A10. MainNet evaluator context reaches MainNet refused outcome.

A11. MainNet peer-driven apply remains refused even with fixture evaluator approval.

A12. invalid live inbound `0x05` context is not propagated, not staged, and not applied.

A13. invalid peer-driven drain context produces no apply and no mutation.

A14. absence of a wire/schema carrier is represented as typed `WireSchemaUnavailable`, not approval.

A15. `Absent`, `Present`, `Malformed`, `UnsupportedSurface`, `WireSchemaUnavailable`, `PeerMajorityUnsupported`, and `MainNetRefused` statuses are all release-evidenced.

A16. Run 227 call-site wiring release behavior remains compatible.

A17. Run 225 integration release behavior remains compatible.

A18. Run 223 evaluator-interface release behavior remains compatible.

Required rejection cases:
R1. malformed evaluator peer context rejected.

R2. missing evaluator context rejected under explicit evaluator policy.

R3. wrong environment rejected.

R4. wrong chain rejected.

R5. wrong genesis rejected.

R6. wrong candidate digest rejected.

R7. wrong evaluator source identity digest rejected.

R8. wrong evaluator request digest rejected.

R9. wrong evaluator response digest rejected.

R10. wrong lifecycle action rejected.

R11. wrong authority-domain sequence rejected.

R12. expired evaluator request rejected.

R13. stale/replayed evaluator request rejected.

R14. quorum/threshold insufficient rejected.

R15. emergency action not authorized rejected.

R16. production evaluator unavailable rejected.

R17. on-chain evaluator unavailable rejected.

R18. MainNet evaluator unavailable/refused rejected.

R19. validator-set rotation unsupported rejected.

R20. policy-change action unsupported rejected.

R21. peer majority cannot satisfy evaluator policy.

R22. local operator cannot satisfy evaluator policy where policy requires evaluator authority.

R23. live inbound `0x05` unsupported carrier rejected without propagation/staging/apply.

R24. peer-driven drain unsupported carrier rejected without apply.

R25. validation-only rejection writes no marker and no sequence.

R26. mutating rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

R27. MainNet peer-driven apply remains refused even with fixture evaluator approval.

Required real release checks:
Use release artifacts to prove:

* helper binary SHA-256 and ELF Build ID captured;
* `qbind-node` release binary SHA-256 and ELF Build ID captured if built;
* Run 228 peer evaluator-context helper corpus passes;
* live inbound `0x05` invalid context does not propagate/stage/apply where release-evidenced;
* peer-driven drain invalid context does not apply where release-evidenced;
* `WireSchemaUnavailable` is fail-closed and not approval;
* MainNet peer-driven apply remains refused;
* no real governance engine active claim;
* no real on-chain proof verifier active claim;
* no MainNet governance enabled claim;
* no validator-set rotation claim;
* no network wire/schema drift;
* no trust-bundle schema drift;
* no marker/sequence schema drift;
* no KMS/HSM backend active claim;
* no RemoteSigner backend active claim;
* no peer-majority authority claim;
* no automatic apply / apply-on-receipt claim.

Required source/release reachability proof:
The report must include grep/call-site proof for:

* `pqc_governance_evaluator_peer_context`;
* the peer evaluator-context type(s);
* carrier taxonomy:

  * `Absent`;
  * `Present`;
  * `Malformed`;
  * `UnsupportedSurface`;
  * `WireSchemaUnavailable`;
  * `PeerMajorityUnsupported`;
  * `MainNetRefused`;
* selected policy binding;
* governance-execution load-status binding;
* governance-execution payload digest binding;
* evaluator source/request/response digest binding;
* candidate trust-bundle digest binding;
* candidate v2 marker digest binding where present;
* authority-domain sequence binding;
* lifecycle action binding;
* environment / chain id / genesis hash binding;
* live inbound `0x05` validation surface binding;
* peer-driven drain validation surface binding;
* Run 226 integration routing where representable;
* MainNet peer-driven refusal guard.

Required mutation / no-mutation proof:
For every rejected peer evaluator-context representation case:

* no Run 070 apply call;
* no live trust swap;
* no session eviction;
* no sequence write;
* no marker write;
* no `.tmp` residue;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead;
* invalid live inbound `0x05` is not propagated;
* invalid live inbound `0x05` is not staged;
* invalid live inbound `0x05` is not applied;
* invalid peer-driven drain is not applied.

Required denylist:
Across the run prove:

* no MainNet apply;
* no autonomous apply;
* no apply on receipt;
* no peer-majority authority;
* no real governance execution engine claim;
* no production governance active claim;
* no MainNet governance enabled claim;
* no real on-chain governance proof verifier claim;
* no real KMS backend claim;
* no real HSM backend claim;
* no real RemoteSigner backend claim;
* no custody attestation production-active claim;
* no validator-set rotation claim;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead;
* no network wire schema change;
* no trust-bundle schema change;
* no marker schema change;
* no sequence schema change;
* no marker write before sequence commit;
* no sequence write on validation-only surfaces;
* no marker write on validation-only surfaces.

Required captured metadata:

* helper SHA-256 and ELF Build ID;
* qbind-node SHA-256 and ELF Build ID if built;
* git commit hash;
* rustc/cargo versions;
* exact commands;
* stdout/stderr logs;
* per-scenario exit codes;
* evaluator-context digests;
* evaluator source/request/response digests where used;
* governance execution payload digests;
* peer candidate/trust-bundle digests;
* integration outcome values;
* carrier taxonomy outcome values;
* marker/sequence before/after hashes where applicable;
* data-dir inventories;
* denylist grep results.

Validation commands:
Run at minimum:

* `cargo build --release -p qbind-node --bin qbind-node`
* `cargo build --release -p qbind-node --example run_229_peer_evaluator_context_representation_release_binary_helper`
* `bash scripts/devnet/run_229_peer_evaluator_context_representation_release_binary.sh`
* `cargo test -p qbind-node --test run_228_peer_evaluator_context_representation_tests`
* `cargo test -p qbind-node --test run_226_governance_evaluator_runtime_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_224_governance_evaluator_runtime_integration_tests`
* `cargo test -p qbind-node --test run_222_governance_execution_evaluator_tests`
* `cargo test -p qbind-node --test run_220_governance_execution_runtime_consumption_tests`
* `cargo test -p qbind-node --test run_217_governance_execution_runtime_arming_tests`
* `cargo test -p qbind-node --test run_215_governance_execution_policy_selector_tests`
* `cargo test -p qbind-node --test run_213_governance_execution_payload_callsite_tests`
* `cargo test -p qbind-node --test run_211_governance_execution_policy_tests`
* `cargo test -p qbind-node --test run_157_unified_testnet_fixture_universe_tests`
* `cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests`
* `cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests`
* `cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests`
* `cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Documentation requirements:
`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_229.md` must state:

* Run 229 is release-binary peer evaluator-context representation evidence.
* The peer evaluator-context boundary is local/source-test and does not change wire/schema/marker/sequence/trust-bundle formats.
* Missing unsupported carrier status is typed and fail-closed under explicit evaluator policy.
* `WireSchemaUnavailable` is not approval.
* Invalid live inbound `0x05` candidates are not propagated, staged, or applied.
* Invalid peer-driven drain candidates are not applied.
* MainNet peer-driven apply remains refused.
* Production/on-chain/MainNet evaluators remain unavailable/fail-closed.
* Fixture/emergency fixture evaluators remain non-production.
* Validator-set rotation remains unsupported.
* No real governance engine or on-chain proof verifier is implemented.
* Full C4 remains OPEN.
* C5 remains OPEN.

Acceptance criteria:
Run 229 is acceptable only if:

1. Release-built helper exercises the Run 228 peer evaluator-context representation boundary.
2. Carrier taxonomy is release-evidenced.
3. `WireSchemaUnavailable` is fail-closed and not approval.
4. Invalid live inbound `0x05` candidates are not propagated, staged, or applied.
5. Invalid peer-driven drain candidates are not applied.
6. MainNet peer-driven apply remains refused.
7. Production/on-chain/MainNet evaluators remain unavailable/fail-closed.
8. Fixture/emergency fixture evaluators remain non-production.
9. Validator-set rotation remains unsupported.
10. Existing Run 227, Run 225, and Run 223 release behavior remains compatible.
11. No wire/schema/marker/sequence/trust-bundle change is claimed.
12. No full C4 or C5 closure is claimed.
