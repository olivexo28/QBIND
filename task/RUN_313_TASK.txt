Run 313 — Source/test epoch-transition runtime handoff / live-mutation preflight boundary

Continue QBIND after accepted Run 312.

Current state:
Run 312 is accepted as PASS / release-binary evidence positive for the Run 311 guarded epoch-transition mutation executor boundary. That row is now Green-for-scope only. It does not apply a live production validator-set change, does not perform a production epoch transition, does not enable MainNet, and does not close C4/C5.

Primary objective:
Implement the next narrow source/test boundary: an epoch-transition runtime handoff / live-mutation preflight boundary that consumes a verified Run 311/312 guarded mutation-execution decision / prepared record and produces a typed, deterministic, policy-gated runtime handoff package for a future live executor.

Strict scope:
Source/test only. No release-binary evidence in Run 313; defer release-binary evidence to Run 314.

Important safety rule:
This run gets closer to the real live mutation boundary, but it must not perform a production live mutation. The positive path may only produce a typed runtime handoff package and may only exercise caller-owned in-memory fixtures in tests. Production and MainNet runtime handoff implementations must be reachable but fail closed as unavailable.

Non-goals:
Do not wire this boundary into normal `qbind-node` runtime.
Do not add a public CLI flag.
Do not enable MainNet.
Do not apply a live production validator-set change.
Do not call `BasicHotStuffEngine::transition_to_epoch` on production runtime state.
Do not write `meta:current_epoch`.
Do not inject `PAYLOAD_KIND_RECONFIG`.
Do not mutate production consensus validator state.
Do not mutate production epoch counters.
Do not mutate `LivePqcTrustState`.
Do not call Run 070.
Do not write trust-bundle sequence files.
Do not write authority-marker files.
Do not evict sessions.
Do not write settlement, publication, audit-finalization, or external-publication records.
Do not claim live validator-set rotation.
Do not claim production epoch transition.
Do not claim MainNet readiness.
Do not claim C4 closure or C5 closure.

Required implementation:
Add a narrow module, preferably:

`crates/qbind-node/src/pqc_production_epoch_transition_runtime_handoff.rs`

Register it in:

`crates/qbind-node/src/lib.rs`

Add tests:

`crates/qbind-node/tests/run_313_production_epoch_transition_runtime_handoff_tests.rs`

The boundary must:

1. Default `Disabled` / fail-closed.
2. Accept only a verified Run 311/312 guarded mutation-execution decision carrying a prepared guarded mutation record.
3. Reject staged application decision alone, live-application authorization alone, application decision alone, rotation plan alone, governance execution intent alone, governance proof alone, fixture-only authority, local-operator assertion, peer-majority assertion, custody-only evidence, RemoteSigner-only evidence, attestation-only evidence, arbitrary validator-set bytes, and accepted guarded decisions without a prepared record.
4. Bind environment, chain ID, genesis hash, authority root, governance domain/epoch/proposal, governance execution decision/request/intent/transcript digests, rotation decision/request/plan/transcript digests, application decision/request/intent/transcript digests, live authorization id/request/intent/transcript digests, staged application id/request/record/intent/transcript digests, guarded mutation id/request/record/intent/transcript digests, current/proposed/delta validator-set digests, validator-set epoch/version, epoch-transition target, application nonce, live-application nonce, staged-application nonce, guarded-mutation nonce, quorum/threshold, custody/attestation/durable-replay bindings where represented.
5. Produce a typed runtime handoff package with deterministic `handoff_id`, `request_id`, `handoff_digest`, and `transcript_digest`.
6. Encode exact future-executor preconditions: expected current validator-set digest, expected current validator-set epoch/version, proposed validator-set digest, delta digest, target epoch, required governance epoch, required authority sequence, and required replay window.
7. Enforce replay/idempotency/equivocation/freshness fail-closed.
8. Keep MainNet refused absent complete production authority criteria.
9. Keep production runtime handoff kind reachable but fail-closed as unavailable.
10. Preserve prior Green-for-scope rows without reinterpretation.

Suggested taxonomy:

* `ProductionEpochTransitionRuntimeHandoffPolicy`

  * `Disabled`
  * `AllowSourceTestRuntimeHandoff`
  * `RequireProductionRuntimeHandoff`
  * `MainnetProductionRuntimeHandoffRequired`
* `ProductionEpochTransitionRuntimeHandoffKind`

  * `Disabled`
  * `SourceTestRuntimeHandoff`
  * `ProductionRuntimeHandoffUnavailable`
  * `MainNetRuntimeHandoffUnavailable`
* `ProductionEpochTransitionRuntimeHandoffRequest`
* `ProductionEpochTransitionRuntimeHandoffInputs`
* `ProductionEpochTransitionRuntimeHandoffDecision`
* `ProductionEpochTransitionRuntimeHandoffPackage`
* `ProductionEpochTransitionRuntimeHandoffOutcome`
* `ProductionEpochTransitionRuntimeHandoffRecoveryOutcome`
* `EpochTransitionRuntimeHandoffReplaySet`
* optional caller-owned `EpochTransitionRuntimeHandoffFixtureState` for tests only.

Required tests:
Target at least 120 tests covering:

* accepted DevNet/TestNet source-test runtime handoff packages;
* verified Run 311/312 guarded mutation record binding;
* deterministic digests under re-evaluation;
* wrong environment / chain / genesis / authority root failures;
* wrong governance domain / epoch / proposal / execution IDs / execution digests;
* wrong rotation IDs / rotation digests;
* wrong application IDs / application digests;
* wrong live-authorization IDs / authorization digests;
* wrong staged-application IDs / staged-record digests;
* wrong guarded-mutation IDs / guarded-record digests;
* wrong current/proposed/delta validator-set digests;
* wrong validator-set epoch/version;
* wrong epoch-transition target;
* wrong application / live-application / staged-application / guarded-mutation nonce;
* missing/unverified guarded mutation record rejection;
* accepted guarded decision without prepared record rejection;
* staged-application-decision-alone rejection;
* live-authorization-alone rejection;
* application-decision-alone rejection;
* rotation-plan-alone rejection;
* governance-execution-intent-alone rejection;
* governance-proof-alone rejection;
* fixture/local-operator/peer-majority/custody-only/RemoteSigner-only/attestation-only/arbitrary-bytes rejection;
* replay/idempotency/equivocation;
* stale governance epoch / authority sequence / validator-set epoch / validator-set version;
* MainNet refusal;
* production-policy unavailable;
* no runtime wiring / no CLI flag invariants;
* non-mutation invariants;
* C4/C5 taxonomy.

Required docs:
Add:

`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_313.md`

Update narrowly:

* `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
* `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/whitepaper/contradiction.md`

C4/C5 matrix:
Add or move the epoch-transition runtime handoff / live-mutation preflight boundary row Red → Yellow only if source/test evidence lands. Do not mark it Green until Run 314 release-binary evidence. Do not claim live production validator-set mutation, production epoch transition, MainNet readiness, C4 closure, or C5 closure.

Security:
Run secret scanning over changed files.
Run CodeQL if available. If skipped, timed out, unavailable, classified trivial, or database too large, record the exact reason directly in `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_313.md` and explicitly state that no CodeQL coverage is claimed.

Required test commands:

* `cargo build -p qbind-node --lib`
* `cargo test -p qbind-node --test run_313_production_epoch_transition_runtime_handoff_tests`
* `cargo test -p qbind-node --test run_311_production_guarded_epoch_transition_mutation_executor_tests`
* `cargo test -p qbind-node --test run_309_production_staged_live_validator_set_epoch_transition_application_executor_tests`
* `cargo test -p qbind-node --test run_307_production_live_validator_set_application_authorization_tests`
* `cargo test -p qbind-node --test run_305_production_validator_set_rotation_application_executor_tests`
* `cargo test -p qbind-node --test run_303_production_validator_set_rotation_intent_tests`
* `cargo test -p qbind-node --test run_301_production_governance_execution_engine_tests`
* `cargo test -p qbind-node --lib`

If any target name differs, use the real target name and document the substitution.

Provenance hygiene:
If a summary file is generated, generate it after the final commit or explicitly list any dirty/untracked files. Do not leave unexplained `git_status: dirty`.

Expected verdict:
PASS / source-test positive if the module, tests, docs, and security provenance land; the row moves Red → Yellow only; production/MainNet runtime handoff paths fail closed; and Full C4 / C5 remain open.

Partial-positive if the core module lands and compiles but tests/docs/security closure are incomplete.

Unacceptable:

* release-binary claim in Run 313;
* marking the row Green;
* adding default runtime wiring;
* adding a public CLI flag;
* enabling MainNet;
* mutating live production consensus state;
* applying a live production validator-set change;
* calling `transition_to_epoch` on production runtime state;
* writing `meta:current_epoch`;
* injecting a reconfig block;
* calling Run 070;
* mutating `LivePqcTrustState`;
* hiding failed/skipped CodeQL;
* weakening prior Green-for-scope rows.

Final response format:

1. Exact verdict.
2. Files changed.
3. Boundary design summary.
4. Policy/kind/handoff taxonomy.
5. Run 311/312 guarded mutation record binding.
6. Runtime handoff package model.
7. Accepted source/test evidence.
8. Rejection/fail-closed evidence.
9. MainNet refusal evidence.
10. Replay/idempotency evidence.
11. Fixture-state evidence, if any.
12. Non-mutation evidence.
13. Tests run.
14. Security scans.
15. C4/C5 matrix status.
16. Honest limitations.
17. C4/C5 status.
18. Suggested Run 314 next step.
