Task: Run 237 — Release-Binary Governance Evaluator Replay Consume Runtime Integration Evidence

Current accepted state:

* Run 230: source/test governance evaluator replay/freshness state boundary landed.
* Run 231: release-binary replay/freshness state evidence fully closed.
* Run 232: source/test governance evaluator replay/freshness runtime integration landed.
* Run 233: release-binary replay/freshness runtime integration evidence fully closed.
* Run 234: source/test post-mutation replay consume boundary landed.
* Run 235: release-binary post-mutation replay consume boundary evidence fully closed.
* Run 236: source/test replay consume runtime integration landed:

  * `crates/qbind-node/src/pqc_governance_evaluator_replay_consume_runtime_integration.rs`;
  * `ReplayConsumeRuntimeIntegrationInput`;
  * `ReplayConsumeRuntimeOutcome`;
  * `integrate_replay_consume_runtime`;
  * the Run 232 replay/freshness runtime integration runs first;
  * non-`ProceedFresh` outcomes map to non-consuming variants without calling the writer;
  * only `ProceedFresh` can advance to the consume boundary;
  * consume binding authorization is overridden with the Run 232 result so freshness and consume layers cannot disagree;
  * consume is integrated as an after-success-only post-mutation step;
  * deferred, validation-only, before-apply, failed apply, rollback, unsupported surface, and MainNet-refused outcomes do not consume;
  * fixture consume remains DevNet/TestNet source-test only;
  * production/MainNet consume remains unavailable/fail-closed;
  * MainNet peer-driven apply remains refused and does not consume;
  * no RocksDB/file/schema/migration/storage-format change;
  * no wire/schema/marker/sequence/trust-bundle change;
  * release-binary evidence deferred to Run 237.
* Full C4 remains OPEN.
* C5 remains OPEN.

Objective:
Produce release-binary evidence for the Run 236 governance evaluator replay consume runtime integration.

This run must prove in release mode that the full source/test lifecycle composition is exercised through release-built code:

1. replay/freshness runtime integration runs first;
2. fresh is required before mutation authorization;
3. consume is allowed only after successful mutation completion;
4. deferred, validation-only, failed, rollback, unsupported, and MainNet-refused paths do not consume;
5. fixture consume remains DevNet/TestNet evidence-only;
6. production/MainNet consume remains unavailable/fail-closed;
7. rejections are non-mutating.

Strict scope:

* Release-binary evidence only.
* Use release-built helper and real `target/release/qbind-node` where applicable.
* No production source behavior change unless a tiny harness-only fix is required.
* No real governance execution engine.
* No real mutation engine implementation.
* No real on-chain governance proof verifier.
* No MainNet governance enablement.
* No MainNet peer-driven apply enablement.
* No validator-set rotation.
* No KMS/HSM backend implementation.
* No RemoteSigner backend implementation.
* No RocksDB schema change.
* No file-format change.
* No database migration.
* No wire/schema/marker/sequence/trust-bundle change.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* Do not weaken Runs 070, 130–236.
* Do not claim full C4 or C5 closure.

Required deliverables:

1. Add release-binary helper:
   `crates/qbind-node/examples/run_237_governance_evaluator_replay_consume_runtime_integration_release_binary_helper.rs`

2. Add release-binary harness:
   `scripts/devnet/run_237_governance_evaluator_replay_consume_runtime_integration_release_binary.sh`

3. Add evidence archive:
   `docs/devnet/run_237_governance_evaluator_replay_consume_runtime_integration_release_binary/`

4. Add canonical evidence report:
   `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_237.md`

5. Update narrowly:

   * `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
   * `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
   * `docs/whitepaper/contradiction.md`
   * `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
   * `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
   * `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

6. Crosscheck all new/modified code and docs against existing design/spec. If contradictions are found, record them in `contradiction.md`.

Required release-helper matrix:
The release-built helper must exercise Run 236 replay consume runtime integration symbols through production library code.

Accepted / compatible cases:
A1. legacy bypass produces no consume.

A2. deferred replay/freshness produces no consume and no mutation authorization.

A3. validation-only fresh decision produces no consume.

A4. fresh decision authorizes mutation but does not consume before apply.

A5. fresh decision plus `AppliedSuccessfully` consumes in DevNet fixture only.

A6. fresh decision plus `AppliedSuccessfully` consumes in TestNet fixture only.

A7. after fixture consume, same decision validates as already consumed / fail-closed through Run 230 state.

A8. read-only validation path never consumes.

A9. failed apply never consumes.

A10. rollback never consumes.

A11. unsupported surface never consumes.

A12. MainNet refused never consumes.

A13. production consume writer path is reached and fails closed unavailable.

A14. MainNet consume writer path is reached and fails closed unavailable/refused.

A15. MainNet peer-driven apply remains refused even when replay state is fresh and mutation completion is modeled as successful.

A16. Run 232 replay/freshness runtime integration remains compatible.

A17. Run 234 consume boundary remains compatible.

A18. Run 235 release consume-boundary behavior remains compatible.

A19. Run 233 release replay/freshness runtime behavior remains compatible.

A20. Run 231 release replay/freshness standalone behavior remains compatible.

A21. `integrate_replay_consume_runtime` runs replay/freshness before consume.

A22. non-`ProceedFresh` outcomes do not call the writer.

A23. consume authorization uses the exact Run 232 freshness result.

Required rejection cases:
R1. expired decision rejected before consume.

R2. stale decision rejected before consume.

R3. replayed decision rejected before consume.

R4. already-consumed decision rejected before consume.

R5. superseded decision rejected before consume.

R6. wrong environment rejected before consume.

R7. wrong chain rejected before consume.

R8. wrong genesis rejected before consume.

R9. wrong validation surface rejected before consume.

R10. wrong mutation surface rejected before consume.

R11. wrong source identity digest rejected before consume.

R12. wrong request digest rejected before consume.

R13. wrong response digest rejected before consume.

R14. wrong transcript digest rejected before consume.

R15. wrong proposal id rejected before consume.

R16. wrong decision id rejected before consume.

R17. wrong lifecycle action rejected before consume.

R18. wrong candidate digest rejected before consume.

R19. wrong authority-domain sequence rejected before consume.

R20. wrong replay nonce rejected before consume.

R21. malformed replay state rejected before consume.

R22. malformed consume state rejected.

R23. consume attempted before apply rejected.

R24. consume attempted after failed apply rejected.

R25. consume attempted after rollback rejected.

R26. consume attempted on validation-only surface rejected.

R27. consume attempted on unsupported surface rejected.

R28. production consume unavailable rejected.

R29. MainNet consume unavailable/refused rejected.

R30. local operator cannot satisfy consume policy.

R31. peer majority cannot satisfy consume policy.

R32. validator-set rotation unsupported rejected.

R33. policy-change action unsupported rejected.

R34. rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

R35. MainNet peer-driven apply remains refused and does not consume even if replay state is fresh.

Required real release checks:
Use release artifacts to prove:

* helper binary SHA-256 and ELF Build ID captured;
* `qbind-node` release binary SHA-256 and ELF Build ID captured if built;
* Run 236 helper corpus passes;
* replay/freshness runtime integration runs before consume;
* fresh is required before mutation authorization;
* consume is after-success-only;
* deferred is not consumed;
* validation-only is not consumed;
* before-apply is not consumed;
* failed apply is not consumed;
* rollback is not consumed;
* unsupported surface is not consumed;
* MainNet refused is not consumed;
* fixture consume records consumed only after explicit success;
* production/MainNet consume unavailable is fail-closed;
* no storage/schema/migration/RocksDB/file-format drift;
* no marker/sequence write from integration layer;
* no Run 070 call from integration layer;
* MainNet peer-driven apply remains refused;
* no real governance engine active claim;
* no real mutation engine active claim;
* no real on-chain proof verifier active claim;
* no MainNet governance enabled claim;
* no validator-set rotation claim;
* no KMS/HSM backend active claim;
* no RemoteSigner backend active claim;
* no peer-majority authority claim;
* no automatic apply / apply-on-receipt claim.

Required source/release reachability proof:
The report must include grep/call-site proof for:

* `pqc_governance_evaluator_replay_consume_runtime_integration`;
* `ReplayConsumeRuntimeIntegrationInput`;
* `ReplayConsumeRuntimeOutcome`;
* `integrate_replay_consume_runtime`;
* `ProceedLegacyBypassNoConsume`;
* `ProceedDeferredNoConsume`;
* `ProceedValidationOnlyNoConsume`;
* `ProceedFreshMutationAuthorized`;
* `ConsumeFixtureAfterMutationSuccess`;
* `DoNotConsumeBeforeApply`;
* `DoNotConsumeApplyFailed`;
* `DoNotConsumeRolledBack`;
* `DoNotConsumeUnsupportedSurface`;
* `DoNotConsumeMainNetRefused`;
* `ReplayRuntimeFailClosed`;
* `ConsumeFailClosed`;
* `ProductionConsumeUnavailable`;
* `MainNetConsumeUnavailable`;
* `MainNetPeerDrivenApplyRefused`;
* Run 232 replay/freshness runtime integration usage;
* Run 234 consume boundary usage;
* Run 230 replay state reader/writer usage;
* fixture consume writer path;
* production consume unavailable path;
* MainNet consume unavailable/refused path;
* MainNet peer-driven refusal guard.

Required mutation / no-mutation proof:
For every rejected consume-runtime-integration case:

* no Run 070 apply call;
* no live trust swap;
* no session eviction;
* no sequence write;
* no marker write;
* no `.tmp` residue;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead;
* validation-only does not consume;
* failed apply does not consume;
* rollback does not consume;
* MainNet refused does not consume.

Required denylist:
Across the run prove:

* no MainNet apply;
* no autonomous apply;
* no apply on receipt;
* no peer-majority authority;
* no real governance execution engine claim;
* no real mutation engine claim;
* no production governance active claim;
* no MainNet governance enabled claim;
* no real on-chain governance proof verifier claim;
* no real KMS backend claim;
* no real HSM backend claim;
* no real RemoteSigner backend claim;
* no custody attestation production-active claim;
* no validator-set rotation claim;
* no fallback to `--p2p-trusted-root`;
* no active DummySig / DummyKem / DummyAead;
* no RocksDB schema change;
* no file format change;
* no database migration;
* no network wire schema change;
* no trust-bundle schema change;
* no marker schema change;
* no sequence schema change;
* no marker write before sequence commit;
* no sequence write on validation-only surfaces;
* no marker write on validation-only surfaces.

Required captured metadata:

* helper SHA-256 and ELF Build ID;
* qbind-node SHA-256 and ELF Build ID if built;
* git commit hash;
* rustc/cargo versions;
* exact commands;
* stdout/stderr logs;
* per-scenario exit codes;
* replay runtime outcome values;
* consume runtime outcome values;
* mutation completion status values;
* consume authorization digests;
* consume transcript digests;
* post-mutation consume record digests;
* before/after fixture replay-store state snapshots;
* marker/sequence before/after hashes where applicable;
* data-dir inventories;
* denylist grep results.

Validation commands:
Run at minimum:

* `cargo build --release -p qbind-node --bin qbind-node`
* `cargo build --release -p qbind-node --example run_237_governance_evaluator_replay_consume_runtime_integration_release_binary_helper`
* `bash scripts/devnet/run_237_governance_evaluator_replay_consume_runtime_integration_release_binary.sh`
* `cargo test -p qbind-node --test run_236_governance_evaluator_replay_consume_runtime_integration_tests`
* `cargo test -p qbind-node --test run_234_governance_evaluator_replay_consume_boundary_tests`
* `cargo test -p qbind-node --test run_232_governance_evaluator_replay_runtime_integration_tests`
* `cargo test -p qbind-node --test run_230_governance_evaluator_replay_state_tests`
* `cargo test -p qbind-node --test run_228_peer_evaluator_context_representation_tests`
* `cargo test -p qbind-node --test run_226_governance_evaluator_runtime_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_224_governance_evaluator_runtime_integration_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Documentation requirements:
`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_237.md` must state:

* Run 237 is release-binary replay consume runtime integration evidence.
* Replay/freshness runtime integration runs before consume.
* Fresh is required before mutation authorization.
* Consume is release-evidenced as after-success-only.
* Deferred, validation-only, before-apply, failed apply, rollback, unsupported surface, and MainNet refused outcomes do not consume.
* Fixture consume remains DevNet/TestNet evidence-only.
* Production/MainNet consume remains unavailable/fail-closed.
* No RocksDB/file/schema/migration/storage-format change is implemented.
* No real governance engine, mutation engine, or on-chain proof verifier is implemented.
* MainNet peer-driven apply remains refused.
* Validator-set rotation remains unsupported.
* Full C4 remains OPEN.
* C5 remains OPEN.

Acceptance criteria:
Run 237 is acceptable only if:

1. Release-built helper exercises the Run 236 consume runtime integration.
2. Replay/freshness runtime integration is release-evidenced as running before consume.
3. Fresh is required before mutation authorization.
4. Consume is allowed only after successful mutation completion.
5. Deferred is not consumed.
6. Validation-only is not consumed.
7. Failed/rolled-back mutation is not consumed.
8. Fixture consume remains DevNet/TestNet evidence-only.
9. Production/MainNet consume remains unavailable/fail-closed.
10. Rejections are non-mutating.
11. MainNet peer-driven apply remains refused and does not consume.
12. No storage/schema/migration/RocksDB/file-format change is claimed.
13. Existing Run 235, Run 233, Run 231, Run 229, and Run 227 release behavior remains compatible.
14. No full C4 or C5 closure is claimed.
