Continue QBIND from the current state.

Project:
- QBIND post-quantum L1
- security-first, production-grade, execution-first
- C4 is the main live operability tracker
- C5 tracks timeout-verification activation, forged-traffic rejection, and transport-root dependency
- no broad redesign
- no silent regressions
- no hype / no overclaiming

Current state:
- Runs 037–045 hardened the PQC transport path:
  - ML-DSA-44 transport delegation cert verification
  - ML-KEM-768 transport KEM
  - ChaCha20-Poly1305 transport AEAD
  - PQC cert metrics
  - cert validity-window enforcement
- Runs 046–049 completed live timeout-backoff evidence through the 800-tick cap.
- Run 050 landed structured trust bundles.
- Run 051 landed signed ML-DSA-44 trust-bundle verification.
- Run 052 landed leaf-level certificate revocation enforcement.
- Run 053 landed trust-bundle chain_id crosscheck.
- Run 054 produced release-binary leaf-revocation evidence artifacts.
- Run 055 landed trust-bundle sequence-number monotonicity persistence.
- Run 056 produced release-binary anti-rollback evidence artifacts.
- Run 057 implemented trust-bundle activation-height gating:
  - activation_height is enforced at startup
  - activation_height <= current_height loads
  - activation_height > current_height fails closed
  - activation failure happens before sequence persistence and before root merge
  - activation fields are covered by canonical signing bytes and fingerprint
  - tampering activation fields after signing invalidates the signature
  - activation_epoch fails closed because no safe pre-consensus epoch source exists
  - current_height source:
    - restore_baseline.snapshot_height if --restore-from-snapshot is used
    - 0 otherwise
  - qbind-node --lib 898/898 passed
- Run 057 boundary:
  - release-binary activation-gate smoke artifacts were NOT produced
  - epoch gating runtime source remains open
- C4 remains OPEN.
- C5 remains OPEN / narrowed.

Task:
Run 058. Produce release-binary live evidence artifacts for Run 057 trust-bundle activation-height gating.

Primary objective:
Prove on the real release qbind-node binary that activation-height gating works exactly as implemented:
- active/satisfied activation-height bundles load;
- future activation-height bundles fail closed;
- future activation failure happens before trust roots are merged;
- future activation failure does not create or update the sequence persistence file;
- no fallback to --p2p-trusted-root or Dummy crypto occurs.

Important framing:
This is evidence-first.
Do not implement epoch gating in this run.
Do not implement activation gates on revocation entries.
Do not implement per-environment minimum activation-height policy.
Do not implement CA/rotation playbook.
Do not implement MainNet live smoke unless trivial and non-distracting.
Do not redesign trust bundles.
Do not redesign KEMTLS.
Do not modify consensus or timeout logic.
Do not modify source unless the evidence run exposes a real bug.
Do not weaken signed-bundle verification.
Do not weaken sequence anti-rollback persistence.
Do not silently fall back to --p2p-trusted-root.
Do not silently fall back to DummySig, DummyKem, or DummyAead.
Do not claim full C4 or C5 closure.

Required active stack:
- release qbind-node binary
- --p2p-mutual-auth required
- --p2p-pqc-root-mode pqc-static-root
- --p2p-trust-bundle <signed-devnet-bundle>
- --p2p-trust-bundle-signing-key KEYID:100:PK
- explicit --data-dir
- real ML-DSA-44 transport delegation certs
- real ML-KEM-768 leaf KEM material
- real ChaCha20-Poly1305 AEAD
- metrics enabled where process lives long enough
- no DummySig
- no DummyKem
- no DummyAead

Strict scope:
- Evidence-only unless a real bug is found.
- Existing Run 057 implementation should be treated as already landed.
- Existing Run 050/051/052/053/054/055/056 behavior must be preserved.
- Use the existing activation-height source and semantics from Run 057.
- Do not add bypass flags.
- Do not add --ignore-trust-bundle-activation.
- Do not silently create/update sequence state for rejected future-activation bundles.
- Crosscheck all evidence against existing design/spec. If contradictions or inconsistencies are found, update docs/whitepaper/contradiction.md.

Required evidence cases:

1. Build and identify binaries
   - Build release qbind-node.
   - Build release devnet_pqc_trust_bundle_helper.
   - Build release devnet_pqc_root_helper if needed.
   - Record:
     - branch
     - commit
     - dirty/clean status
     - qbind-node sha256
     - qbind-node ELF BuildID
     - helper sha256
     - helper ELF BuildID
   - Confirm --devnet-forged-inject remains hidden from normal --help.
   - Confirm relevant flags:
     - --p2p-trust-bundle
     - --p2p-trust-bundle-signing-key
     - --p2p-pqc-root-mode
     - --p2p-leaf-cert
     - --p2p-leaf-cert-key
     - --p2p-peer-leaf-cert
     - --p2p-mutual-auth
     - --data-dir
     - --restore-from-snapshot if used for satisfied-height evidence

2. Positive active-now activation-height smoke
   Create a valid signed DevNet trust bundle with:
   - environment = DevNet
   - matching or null chain_id according to Run 053 policy
   - sequence = 1
   - activation_height = 0, or no activation gate if used as baseline
   - valid signing key
   - valid active root
   - no revoked local/peer leaf that would block startup

   Run qbind-node with a fresh data dir.

   Prove:
   - node starts.
   - signed bundle verifies.
   - activation gate is satisfied.
   - trust bundle loads.
   - sequence record is written only after activation succeeds.
   - roots are merged only after activation succeeds.
   - qbind_p2p_pqc_trust_bundle_sequence_highest = 1 if metrics are reachable.
   - no fallback to --p2p-trusted-root.
   - no DummySig/DummyKem/DummyAead fallback.
   - no private key material in logs.

3. Positive satisfied-height smoke using restore baseline if feasible
   If feasible with existing tooling:
   - create or reuse a restore baseline/snapshot with snapshot_height >= H.
   - create a signed DevNet trust bundle with activation_height = H.
   - run qbind-node with --restore-from-snapshot and the same signed trust-bundle path.
   - prove current_height is read from restore_baseline.snapshot_height.
   - prove activation_height <= current_height succeeds.

   If not feasible:
   - document boundary honestly and rely on unit/integration tests for restore-height path.
   - do not fake restore height.

4. Negative future-height smoke
   Create a valid signed DevNet trust bundle with:
   - sequence = 1 or a fresh sequence for fresh data dir
   - activation_height > current_height
   - valid signing key
   - valid active root

   Run qbind-node with a fresh data dir and no restore snapshot, so current_height = 0.

   Expected:
   - startup fails closed.
   - precise FATAL/log reason mentions activation height not reached.
   - failure occurs before roots are merged.
   - sequence persistence file is not created or not updated.
   - no fallback to --p2p-trusted-root.
   - no DummySig/DummyKem/DummyAead.
   - if /metrics unavailable due fail-closed startup, document boundary honestly and rely on Run 057 tests for activation rejection metric movement.

5. Negative future-height after existing sequence state
   Use a data dir that already accepted sequence 1 active bundle.
   Then attempt sequence 2 bundle with future activation_height.

   Expected:
   - startup fails closed.
   - existing sequence record remains at sequence 1.
   - sequence is not updated to 2.
   - roots from future bundle are not merged.
   - no fallback.
   - no Dummy crypto.

6. Positive normal sequence upgrade after failed future activation
   After case 5 fails:
   - start with a valid sequence 2 bundle whose activation_height is satisfied.
   - prove sequence 2 is accepted and persisted.
   - this proves the failed future-activation attempt did not poison the sequence state.

7. Optional epoch declaration negative smoke
   If low cost:
   - create a signed bundle with activation_epoch set.
   - prove startup fails closed because no safe pre-consensus epoch source exists.
   - no fallback.
   - no Dummy crypto.
   - do not block Run 058 on this if unit/integration tests already prove it.

Required tests to rerun:
- cargo test -p qbind-node --lib pqc_trust_activation
- cargo test -p qbind-node --lib pqc_trust_sequence
- cargo test -p qbind-node --lib pqc_trust_bundle
- cargo test -p qbind-node --test run_057_pqc_trust_bundle_activation_tests
- cargo test -p qbind-node --test run_055_pqc_trust_bundle_sequence_tests
- cargo test -p qbind-node --test run_050_pqc_trust_bundle_tests
- cargo test -p qbind-node --test run_051_pqc_trust_bundle_signing_tests
- cargo test -p qbind-node --test run_052_pqc_leaf_revocation_tests
- cargo test -p qbind-net --test run_052_leaf_revocation_handshake_tests
- cargo test -p qbind-node --lib metrics
- cargo test -p qbind-node --lib p2p
- cargo test -p qbind-node --test run_037_pqc_static_root_mutual_auth_tests
- cargo test -p qbind-node --test run_040_pqc_static_root_real_aead_tests
- cargo test -p qbind-node --test run_044_pqc_cert_verify_metrics_adapter_tests
- cargo test -p qbind-node --lib
- cargo test -p qbind-net --lib
- cargo test -p qbind-crypto --lib
- cargo build --release -p qbind-node --bin qbind-node
- cargo build --release -p qbind-node --example devnet_pqc_trust_bundle_helper
- cargo build --release -p qbind-node --example devnet_pqc_root_helper

If source touches timeout verification or binary consensus loop, also run:
- cargo test -p qbind-node --lib binary_consensus
- cargo test -p qbind-node --lib forged_injection
- cargo test -p qbind-node --lib run030
- cargo test -p qbind-consensus --lib timeout

Evidence document:
Create:
- docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_058.md

Evidence document must include:
- exact objective
- exact verdict
- exact files changed
- exact commands run
- tests run and pass/fail status
- binary identity
- helper identities
- trust-bundle material procedure
- signed-bundle verification proof
- chain_id compatibility/matching status for bundles used
- activation-height fixture details
- runtime current-height source for each smoke
- sequence persistence file path
- positive active-now smoke
- positive satisfied-height restore smoke or exact boundary
- negative future-height smoke
- negative future-height after existing sequence state
- positive normal sequence upgrade after failed future activation
- optional epoch negative smoke if run
- sequence record contents before/after each relevant smoke
- proof rejected future activation did not create/update sequence state
- proof roots were not merged for future activation
- /metrics excerpts where available
- exact boundary if fail-closed startup prevents metric scraping
- proof no fallback to --p2p-trusted-root
- proof no DummySig/DummyKem/DummyAead fallback
- remaining open items
- exact immediate next action

contradiction.md update rules:
- If release-binary activation-height evidence passes, update C4 to record Run 057’s live-binary artifact boundary as closed/narrowed by Run 058.
- If restore-height satisfied smoke is not feasible, state that boundary honestly.
- Keep C4 open for:
  - epoch gating runtime source
  - activation gates on revocation entries if still absent
  - per-environment minimum activation-height policy if still absent
  - MainNet live signed-bundle smoke if still absent
  - production CA / cert rotation / signing-key rotation operator playbook
  - production fast-sync / consensus-storage restore
  - per-environment production trust-anchor operation if not fully solved
- Do not close full C4.
- Do not claim C5 closure unless explicitly justified.
- If future activation is accepted early, record as contradiction/regression.
- If future activation updates sequence state before becoming active, record as contradiction/regression.
- If failure falls back to static roots or Dummy crypto, record as contradiction/regression.
- If metrics are unavailable due to fail-closed startup, document boundary honestly rather than fabricating.

Expected verdict options:
- Strongest positive:
  Release-binary evidence proves active-height bundle loads, future-height bundle fails closed before root merge and before sequence update, existing sequence is not poisoned by failed future activation, later valid sequence upgrade still works, no fallback, no Dummy crypto, all regressions pass.
- Partial positive:
  Release-binary evidence proves basic activation failure/success but restore-height or sequence-interaction evidence remains incomplete. State exact boundary.
- Negative:
  future activation accepted early, sequence updated on rejected future bundle, fallback/Dummy crypto activates, or regressions fail.

Required final response:
1. Exact verdict
2. Exact files changed
3. Exact commands run
4. Exact tests/evidence run and pass/fail status
5. What was proven
6. What remains not solved
7. Whether contradiction.md was updated and why
8. Exact immediate next action recommended