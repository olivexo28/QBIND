Task: Run 170 — Release-Binary Production-Surface Governance-Proof Carrying Evidence

Current accepted state:
- Run 167: additive governance-proof carrier landed.
- Run 168: release-binary boundary evidence showed proof-carrying sidecars work through a release-built helper, but production marker-decision callers still needed to consume the typed loader.
- Run 169: source/test integration wired the Run 167 typed governance-proof loader into production v2 marker-decision callers:
  - reload-check / reload-apply;
  - startup `--p2p-trust-bundle`;
  - SIGHUP live reload;
  - peer-driven coordinator;
  - validation-only paths where sidecar context exists.
- Run 169 added:
  - `pqc_governance_proof_surface.rs`;
  - `preflight_v2_marker_decision_with_governance_proof_load`;
  - `load_versioned_ratification_with_governance_proof_from_path`;
  - `VersionedRatificationSidecarWithGovernanceProof`;
  - governance-proof load routing through `GovernanceProofLoadStatus::{Absent, Available, Malformed}`;
  - production callers no longer hardcode governance proof context as always unavailable when sidecar paths exist.
- Run 169 is source/test only.
- Release-binary production-surface proof-carrying evidence is deferred to Run 170.
- MainNet peer-driven apply remains refused.
- OnChainGovernance remains unsupported/fail-closed.
- governance execution remains unimplemented.
- KMS/HSM remains unimplemented.
- validator-set rotation remains open.
- full C4 remains open.
- C5 remains open.

Objective:
Produce release-binary evidence that real `target/release/qbind-node` production v2 marker-decision surfaces consume proof-carrying governance sidecars and enforce the Run 165 governance gate through the Run 169 loader wiring.

This run must prove on real release binaries:
1. old no-proof sidecars remain compatible under `NotRequired`;
2. proof-required/no-proof cases fail closed;
3. valid proof-carrying sidecars are parsed as `Available` and accepted when anti-rollback + lifecycle + governance all pass;
4. invalid or malformed proof-carrying sidecars fail closed;
5. mutating accepted cases preserve sequence-before-marker ordering;
6. rejected cases produce no mutation;
7. MainNet peer-driven apply remains refused even with a valid proof.

Strict scope:
- Release-binary evidence only.
- Use real `target/release/qbind-node`.
- Use release-built helper(s) to mint proof-carrying sidecars.
- No production source change unless a tiny harness-only helper adjustment is required.
- No MainNet peer-driven apply enablement.
- No governance execution engine.
- No on-chain governance implementation.
- No KMS/HSM implementation.
- No validator-set rotation.
- No autonomous apply.
- No automatic apply on receipt.
- No peer-majority authority.
- No new schema/wire changes.
- No marker schema change.
- No sequence-file schema change.
- No trust-bundle core schema change.
- No peer-candidate envelope schema change.
- Do not weaken Runs 070, 130–169.
- Do not claim full C4 or C5 closure.

Required deliverables:
1. Add release-binary harness:
   scripts/devnet/run_170_governance_proof_production_surface_release_binary.sh

2. Add evidence archive:
   docs/devnet/run_170_governance_proof_production_surface_release_binary/

3. Add canonical evidence report:
   docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_170.md

4. Update narrowly:
   - docs/whitepaper/contradiction.md
   - docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md
   - docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md
   - docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md

5. Crosscheck all new/modified code and docs against existing design/spec. If contradictions or inconsistencies are found, record them in contradiction.md before finalizing.

Required production surfaces:
Exercise at minimum:

A. Validation-only:
- `--p2p-trust-bundle-reload-check`.

B. Mutating:
- process-start `--p2p-trust-bundle-reload-apply-path`.

If feasible, also include one of:
- startup `--p2p-trust-bundle`;
- SIGHUP live reload;
- peer-driven drain coordinator.

Do not substitute helper-only evidence for the central production-surface accepted-proof claim.

Required accepted scenarios:

A1. reload-check old no-proof sidecar under NotRequired remains accepted
- Expected:
  - real `qbind-node` exits success;
  - proof load status is Absent or equivalent;
  - NotRequired compatibility path visible;
  - validation-only semantics preserved;
  - no sequence write;
  - no marker write.

A2. reload-apply old no-proof sidecar under NotRequired remains accepted
- Expected:
  - real `qbind-node` accepts and applies according to existing Run 134/162/166/169 semantics;
  - Run 055 sequence commit succeeds;
  - v2 marker persists strictly after sequence commit;
  - no governance-proof-required error.

A3. reload-check valid proof-carrying GenesisBound Rotate sidecar under Required policy accepted
- Expected:
  - proof sibling parsed through production loader;
  - `GovernanceProofLoadStatus::Available` or equivalent visible;
  - `GovernanceProofContext::Available` reaches marker decision;
  - governance verifier accepts;
  - lifecycle validation accepts;
  - marker decision accepts;
  - validation-only non-mutation preserved;
  - no sequence write;
  - no marker write.

A4. reload-apply valid proof-carrying GenesisBound Rotate sidecar under Required policy accepted
- Expected:
  - proof sibling parsed through production loader;
  - governance verifier accepts;
  - lifecycle validation accepts;
  - mutating apply succeeds;
  - Run 055 sequence commit succeeds;
  - v2 marker persists strictly after sequence commit.

A5. valid proof-carrying GenesisBound Revoke sidecar accepted where representable
- Prefer reload-check first.
- Mutating surface optional if fixture-supported.
- If not practical on release binary, document exact limitation and cite Run 167/169 source-test coverage.

A6. valid proof-carrying EmergencyCouncil EmergencyRevoke sidecar accepted where representable
- If fixture-supported, cover it.
- If not practical, document exact limitation and cite Run 167/169 source-test coverage.

A7. idempotent proof-carrying sidecar accepted
- same authority sequence / digest / proof;
- accepted as idempotent / no-op according to existing marker rules.

Required rejection scenarios:

R1. proof required but sidecar has no proof
- Expected:
  - `GovernanceAuthorityRequiredButMissing` visible;
  - no sequence write;
  - no marker write.

R2. malformed governance proof sibling rejected
- Expected:
  - typed malformed proof/load error visible;
  - no mutation.

R3. wrong environment proof rejected.

R4. wrong chain proof rejected.

R5. wrong genesis proof rejected.

R6. wrong authority root proof rejected.

R7. wrong lifecycle action proof rejected.

R8. wrong candidate digest proof rejected.

R9. wrong authority-domain sequence proof rejected.

R10. invalid issuer signature rejected.

R11. unsupported issuer suite rejected.

R12. non-PQC suite rejected.

R13. threshold-not-met rejected if representable.

R14. stale/replayed lower authority proof rejected.

R15. OnChainGovernance proof rejected as unsupported/fail-closed.

R16. local operator config alone cannot satisfy proof-required policy.

R17. peer majority / gossip count cannot satisfy proof-required policy.

R18. proof valid but lifecycle invalid rejected.

R19. lifecycle valid but proof invalid rejected.

R20. proof valid but MainNet peer-driven apply remains refused.

Required production-surface reachability proof:
The evidence report must include:
- grep/call-site proof for:
  - `load_versioned_ratification_with_governance_proof_from_path`;
  - `load_v2_ratification_sidecar_with_governance_proof_from_path`;
  - `preflight_v2_marker_decision_with_governance_proof_load`;
  - `GovernanceProofLoadStatus::Available`;
  - `GovernanceProofLoadStatus::Malformed`;
  - `GovernanceProofContext::Available`;
  - `decide_v2_marker_acceptance_with_lifecycle_and_governance`;
- release-binary log proof that production surfaces, not only helper binaries, exercised proof-carrying `Available` and proof-required missing/malformed paths;
- comparison against Run 168’s helper-only proof-carrying boundary.

Required mutation proof for accepted mutating scenarios:
For every accepted mutating proof-carrying scenario:
- proof parse occurs before marker decision;
- governance verification occurs before apply/mutation;
- lifecycle validation occurs before apply/mutation;
- Run 070 / relevant apply ordering preserved;
- Run 055 sequence commit succeeds;
- v2 marker persists strictly after sequence commit;
- marker JSON/SHA before/after captured;
- sequence JSON/SHA before/after captured.

Required no-mutation proof for rejected scenarios:
For every rejected scenario:
- binary exits non-zero or typed refusal according to surface;
- no Run 070 apply call;
- no live trust swap;
- no session eviction;
- no sequence write;
- no marker write;
- marker bytes unchanged;
- sequence bytes unchanged;
- no `.tmp` residue;
- no fallback to `--p2p-trusted-root`;
- no active DummySig / DummyKem / DummyAead.

Required denylist:
Across the run prove:
- no MainNet apply;
- no autonomous apply;
- no apply on receipt;
- no peer-majority authority;
- no governance execution claim;
- no on-chain governance claim;
- no KMS/HSM claim;
- no validator-set rotation claim;
- no fallback to `--p2p-trusted-root`;
- no active DummySig / DummyKem / DummyAead;
- no schema/wire/metric drift;
- no marker write before sequence commit;
- no sequence write on validation-only surfaces;
- no marker write on validation-only surfaces.

Required captured metadata:
- qbind-node SHA-256 and ELF Build ID;
- helper SHA-256 and ELF Build ID;
- git commit hash;
- rustc/cargo versions;
- exact commands;
- stdout/stderr logs;
- per-scenario exit codes;
- proof sidecar paths and hashes;
- marker/sequence JSON and SHA before/after;
- data-dir inventories;
- denylist grep results.

Validation commands:
Run at minimum:
- cargo build --release -p qbind-node --bin qbind-node
- cargo build --release -p qbind-node --example <proof-carrying fixture helper>
- bash scripts/devnet/run_170_governance_proof_production_surface_release_binary.sh
- cargo test -p qbind-node --test run_169_governance_proof_loader_surface_integration_tests
- cargo test -p qbind-node --test run_167_governance_proof_carrier_tests
- cargo test -p qbind-node --test run_165_governance_marker_integration_tests
- cargo test -p qbind-node --test run_163_governance_authority_verifier_tests
- cargo test -p qbind-node --test run_161_lifecycle_marker_integration_tests
- cargo test -p qbind-node --test run_159_authority_signing_key_lifecycle_tests
- cargo test -p qbind-node --test run_157_unified_testnet_fixture_universe_tests
- cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests
- cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests
- cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests
- cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests
- cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests
- cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests
- cargo test -p qbind-node --lib pqc_authority
- cargo test -p qbind-node --lib

If exact test names differ, locate nearest existing targets and document exact commands/results.

Documentation requirements:
`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_170.md` must state:
- Run 170 is release-binary production-surface governance-proof carrying evidence.
- Existing no-proof sidecars remain compatible under NotRequired.
- Required policy fails closed when proof is absent.
- Valid proof-carrying sidecars are accepted through real production surfaces where lifecycle/anti-rollback checks pass.
- Invalid proof-carrying sidecars fail closed through real production surfaces where feasible.
- OnChainGovernance remains unsupported/fail-closed.
- No MainNet apply is enabled.
- governance execution/on-chain proof remains unimplemented.
- KMS/HSM remains unimplemented.
- validator-set rotation remains open.
- full C4 remains open unless separately closed by explicit criteria.
- C5 remains open.

Acceptance criteria:
Run 170 is acceptable only if:
1. Real release binaries parse proof-carrying governance sidecars through production surfaces.
2. No-proof sidecars remain compatible under NotRequired.
3. Proof-required/no-proof cases fail closed.
4. Valid proof-carrying sidecars reach and pass the governance gate on at least one validation-only and one mutating production surface.
5. Invalid proof-carrying sidecars fail closed.
6. Accepted mutating cases preserve sequence-before-marker ordering.
7. Rejected cases produce no mutation.
8. MainNet remains refused even with valid governance proof.
9. No governance execution/KMS-HSM/validator-set rotation claim is made.
10. No full C4 or C5 closure is claimed.