Run 259 — Release-binary durable-completion audit-ledger / external-publication receipt boundary evidence

You are continuing QBIND after Run 258.

Run 240 is fully closed at source/test level.
Run 241 is fully closed as release-binary evidence for Run 240.
Run 242 is fully closed as source/test governance execution mutation-engine boundary work.
Run 243 is fully closed as release-binary evidence for Run 242.
Run 244 is fully closed as source/test modeled governance trust-state mutation applier boundary work.
Run 245 is fully closed as release-binary evidence for Run 244.
Run 246 is fully closed as source/test governance modeled end-to-end pipeline boundary work.
Run 247 is fully closed as release-binary evidence for Run 246.
Run 248 is fully closed as source/test modeled durable-consume projection sink boundary work.
Run 249 is fully closed as release-binary evidence for Run 248.
Run 250 is fully closed as source/test modeled durable-consume completion reporter boundary work.
Run 251 is fully closed as release-binary evidence for Run 250.
Run 252 is fully closed as source/test modeled durable-completion finalization-projection boundary work.
Run 253 is fully closed as release-binary evidence for Run 252.
Run 254 is fully closed as source/test modeled durable-completion attestation-projection boundary work.
Run 255 is fully closed as release-binary evidence for Run 254.
Run 256 is fully closed as source/test production durable-completion attestation backend interface boundary work.
Run 257 is fully closed as release-binary evidence for Run 256.
Run 258 is fully closed as source/test durable-completion backend audit-ledger / external-publication receipt boundary work.

Strict workflow:

* Execution-first.
* Narrow scope only.
* No broad redesign.
* No silent regressions.
* No hype or overclaiming.
* Release-binary evidence only.
* No new production runtime behavior.
* No real audit-ledger backend.
* No real external-publication backend.
* No real production attestation backend.
* No real finalization backend.
* No real completion-report backend.
* No real durable consume backend.
* No real persistent replay backend.
* No real governance execution engine.
* No real production mutation engine.
* No real on-chain governance proof verifier.
* No KMS/HSM/RemoteSigner backend.
* No RocksDB/file/schema/migration/storage-format change.
* No wire/schema/marker/sequence/trust-bundle change.
* No Run 070 call.
* No `LivePqcTrustState` mutation.
* No real trust swap.
* No session eviction.
* No sequence write.
* No marker write.
* No external publication.
* No real audit-ledger write.
* No MainNet governance.
* No MainNet peer-driven apply.
* No validator-set rotation.
* Keep Full C4 OPEN.
* Keep C5 OPEN.

Current state:

Run 258 added the source/test-only durable-completion backend audit-ledger / external-publication receipt boundary:

* module: `crates/qbind-node/src/pqc_governance_durable_completion_audit_publication_receipt.rs`;
* tests: `crates/qbind-node/tests/run_258_durable_completion_audit_publication_receipt_tests.rs`;
* canonical evidence: `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_258.md`.

The Run 258 boundary consumes the Run 256 backend outcome and creates a receipt request only after Run 256 `BackendSubmissionRecorded`.

Required ordering:

1. Run 246 pipeline success is required before any Run 248 sink intent can exist.
2. Run 248 `ConsumeReceiptRecorded` is required before any Run 250 completion-report intent can exist.
3. Run 250 `CompletionReportRecorded` is required before any Run 252 finalization intent can exist.
4. Run 252 `DurableCompletionFinalized` is required before any Run 254 attestation intent can exist.
5. Run 254 `DurableCompletionAttested` is required before any Run 256 backend request can exist.
6. Run 256 `BackendSubmissionRecorded` is required before any Run 258 audit/publication receipt request can exist.
7. Only Run 258 `AuditReceiptRecorded` authorizes a modeled audit/publication receipt state.

Run 258 is source/test only. It has no release-binary evidence yet.

Objective:

Produce release-binary evidence for the Run 258 durable-completion audit-ledger / external-publication receipt boundary, mirroring the Run 247 / 249 / 251 / 253 / 255 / 257 pattern.

Run 259 must prove on real `target/release/qbind-node` plus a release-built helper that the Run 258 receipt boundary symbols are present in release-built code, that the helper exercises the Run 258 production library symbols, and that the audit/publication receipt path is evidenced only as a fixture/in-memory/dev-test boundary — not production-enabled.

Required deliverables:

1. Add a release helper:

   `crates/qbind-node/examples/run_259_durable_completion_audit_publication_receipt_release_binary_helper.rs`

   The helper must link against release-built production library symbols and exercise the actual Run 258 API, including:

   * `pqc_governance_durable_completion_audit_publication_receipt`
   * `DurableCompletionAuditPublicationReceiptInput`
   * `DurableCompletionAuditPublicationReceiptPolicy`
   * `DurableCompletionAuditPublicationReceiptKind`
   * `DurableCompletionAuditPublicationReceiptIdentity`
   * `DurableCompletionAuditPublicationReceiptExpectations`
   * `DurableCompletionAuditPublicationReceiptRequest`
   * `DurableCompletionAuditPublicationReceiptResponse`
   * `DurableCompletionAuditPublicationReceiptRecord`
   * `DurableCompletionAuditPublicationReceiptLedger`
   * `DurableCompletionAuditPublicationReceiptLedgerRecord`
   * `DurableCompletionAuditPublicationReceiptDigest`
   * `DurableCompletionAuditPublicationReceiptTranscriptDigest`
   * `DurableCompletionAuditPublicationReceiptOutcome`
   * `GovernanceDurableCompletionAuditPublicationReceiptSink`
   * `FixtureDurableCompletionAuditPublicationReceiptSink`
   * `ProductionAuditLedgerDurableCompletionReceiptSink`
   * `MainNetAuditLedgerDurableCompletionReceiptSink`
   * `ExternalPublicationDurableCompletionReceiptSink`
   * `project_backend_submission_outcome_to_audit_receipt_request`
   * `evaluate_durable_completion_audit_publication_receipt`
   * `recover_durable_completion_audit_publication_receipt_window`
   * all Run 258 grep-verifiable invariant helpers.

   If exact symbol names differ from the task names, use the actual implemented Run 258 API names and record the substitutions honestly in `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_259.md`. Do not add compatibility shims unless strictly needed for compilation, and do not change production behavior.

2. Add a release harness:

   `scripts/devnet/run_259_durable_completion_audit_publication_receipt_release_binary.sh`

   The harness must:

   * build `target/release/qbind-node`;
   * build the Run 259 release helper;
   * capture git commit, rustc version, cargo version;
   * capture SHA-256 and ELF Build ID for `target/release/qbind-node`;
   * capture SHA-256 and ELF Build ID for the Run 259 helper;
   * run real-binary surface scenarios;
   * run the helper corpus in release mode;
   * run source-reachability greps for Run 258 symbols;
   * run helper-output reachability checks proving the helper exercised the Run 258 symbols;
   * run a denylist proving no production/MainNet/audit-ledger/external-publication enablement claims;
   * run the required regression corpus;
   * write generated evidence into an ignored evidence directory.

3. Add evidence archive directory:

   `docs/devnet/run_259_durable_completion_audit_publication_receipt_release_binary/`

   Track only:

   * `README.md`
   * `summary.txt`
   * `.gitignore`

   Generated logs, helper tables, reachability outputs, binary outputs, test logs, and provenance artifacts must be ignored.

4. Add canonical evidence report:

   `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_259.md`

Helper corpus:

The helper must include accepted/compatible, rejection, recovery, projection, stage-ordering, receipt-ledger, non-mutation, and reachability tables.

Accepted / compatible cases must prove at minimum:

* Disabled receipt policy preserves legacy no-audit-receipt bypass and performs no receipt record, no audit-ledger write, no external publication, and no mutation.
* Disabled backend policy preserves legacy bypass and never invokes receipt sink.
* Disabled attestor policy preserves legacy bypass and never invokes backend or receipt sink.
* Disabled finalizer policy preserves legacy bypass and never invokes attestor, backend, or receipt sink.
* Disabled reporter policy preserves legacy bypass and never invokes finalizer, attestor, backend, or receipt sink.
* Disabled sink policy preserves legacy bypass and never invokes reporter, finalizer, attestor, backend, or receipt sink.
* Disabled pipeline policy preserves legacy bypass and never invokes sink, reporter, finalizer, attestor, backend, or receipt sink.
* Disabled evaluator/call-site policy preserves legacy bypass and never invokes sink, reporter, finalizer, attestor, backend, or receipt sink.
* DevNet fixture chain records exactly one modeled in-memory receipt only after pipeline success, sink receipt, completion report, finalization, attestation, backend submission, and receipt record.
* TestNet fixture chain records exactly one modeled in-memory receipt only after pipeline success, sink receipt, completion report, finalization, attestation, backend submission, and receipt record.
* Modeled add-root / retire-root / revoke-root / emergency-revoke-root / noop variants record receipt only after modeled applier success, sink receipt record, completion report record, finalization record, attestation record, and backend submission record.
* Duplicate identical receipt is idempotent and creates no second receipt.
* Run 256 duplicate-idempotent backend submission may only match an already-recorded receipt and never creates one by itself.
* Existing Run 258 receipt behavior remains compatible.
* Existing Run 256 backend behavior remains compatible.
* Existing Run 254 attestation behavior remains compatible.
* Existing Run 252 finalization behavior remains compatible.
* Existing Run 250 completion-reporter behavior remains compatible.
* Existing Run 248 sink behavior remains compatible.
* Existing Run 246 pipeline behavior remains compatible.
* Existing Run 244 modeled applier behavior remains compatible.
* Existing Run 242 mutation-engine behavior remains compatible.
* Existing Run 240 durable completion behavior remains compatible.
* Production audit-ledger path is reachable but unavailable/fail-closed and records no receipt.
* MainNet audit-ledger path is reachable but unavailable/fail-closed and records no receipt.
* External-publication path is reachable but unavailable/fail-closed and performs no publication.
* MainNet peer-driven apply is refused before pipeline progression, sink invocation, reporter invocation, finalizer invocation, attestor invocation, backend invocation, and receipt sink invocation.
* Validator-set rotation unsupported and records no receipt.
* Policy-change unsupported and records no receipt.

Rejected / fail-closed cases must prove at minimum:

* evaluator rejection produces no sink intent, no sink receipt, no reporter intent, no completion report, no finalizer intent, no finalization, no attestor intent, no attestation, no backend request, no backend submission, no audit/publication receipt request, and no receipt;
* call-site rejection produces no audit/publication receipt request and no receipt;
* durable replay stale / expired / consumed / superseded / backend-unavailable produces no audit/publication receipt request and no receipt;
* mutation-engine rejection produces no audit/publication receipt request and no receipt;
* modeled applier rejected-before-snapshot / rejected-before-apply / apply failure / rollback / rollback-failed / ambiguous window produces no receipt request and no receipt;
* sink did not record receipt / rejected-before-record / record failure / rollback / rollback-failed / ambiguous window produces no reporter/finalizer/attestor/backend/receipt invocation and no receipt;
* reporter did not record completion / rejected-before-record / record failure / rollback / rollback-failed / ambiguous window produces no finalizer/attestor/backend/receipt invocation and no receipt;
* finalizer did not finalize / rejected-before-record / record failure / rollback / rollback-failed / ambiguous window produces no attestor/backend/receipt invocation and no receipt;
* attestor did not attest / rejected-before-record / record failure / rollback / rollback-failed / ambiguous window produces no backend/receipt invocation and no receipt;
* backend did not submit / rejected-before-record / record failure / rollback / rollback-failed / ambiguous window produces no receipt sink invocation and no receipt;
* receipt before backend submission is rejected;
* receipt after modeled apply failure, sink record failure, reporter record failure, finalizer record failure, attestor record failure, backend record failure, backend rollback, backend rollback-failed, or backend ambiguous window is rejected;
* same receipt record id with different digest is rejected as equivocation and records no second receipt;
* wrong environment / chain / genesis / governance surface / mutation surface rejected before receipt sink invocation;
* wrong receipt identity rejected before record;
* wrong receipt policy rejected before record;
* wrong receipt kind rejected before record;
* wrong request digest rejected before record;
* wrong response digest rejected before record;
* wrong receipt record digest rejected before record;
* wrong backend identity digest rejected before record;
* wrong backend request digest rejected before record;
* wrong backend response digest rejected before record;
* wrong backend receipt digest rejected before record;
* wrong backend transcript digest rejected before record;
* wrong attestation digest rejected before record;
* wrong finalization digest rejected before record;
* wrong completion-report digest rejected before record;
* wrong sink decision digest rejected before record;
* wrong reporter decision digest rejected before record;
* wrong finalization decision digest rejected before record;
* wrong pipeline decision digest rejected before record;
* wrong proposal id rejected before record;
* wrong decision id rejected before record;
* wrong candidate digest rejected before record;
* wrong authority-domain sequence rejected before record;
* malformed receipt request rejected before record;
* malformed receipt response rejected before record;
* local operator key cannot satisfy MainNet authority;
* peer majority cannot satisfy MainNet authority;
* production audit-ledger unavailable records no receipt;
* MainNet audit-ledger unavailable records no receipt;
* external-publication unavailable records no receipt and performs no publication;
* validator-set rotation attempt unsupported and records no receipt;
* policy-change attempt unsupported and records no receipt;
* every rejected path produces no Run 070 call, no `LivePqcTrustState` mutation, no real trust swap, no session eviction, no sequence write, no marker write, no RocksDB/file/schema/migration/storage-format change, no external publication, no real audit-ledger write, and no receipt sink invocation where rejection happens before the receipt stage.

Recovery / crash-window cases must prove at minimum:

* before-pipeline window fails closed / no receipt;
* after-pipeline-success-before-sink-intent window fails closed / no receipt;
* after-sink-intent-before-sink-receipt-record window fails closed / no receipt;
* after-sink-receipt-record-before-report-intent window fails closed / no receipt;
* after-report-intent-before-report-record window fails closed / no receipt;
* after-report-record-before-finalization-intent window fails closed / no receipt;
* after-finalization-intent-before-finalization-record window fails closed / no receipt;
* after-finalization-record-before-attestation-intent window fails closed / no receipt;
* after-attestation-intent-before-attestation-record window fails closed / no receipt;
* after-attestation-record-before-backend-request window fails closed / no receipt;
* after-backend-request-before-backend-record window fails closed / no receipt;
* after-backend-record-before-backend-success window fails closed unless an explicit matching backend success exists;
* after-backend-success-before-receipt-request window fails closed / no receipt;
* after-receipt-request-before-receipt-record window fails closed / no receipt;
* after-receipt-record-before-receipt-success window fails closed unless an explicit matching receipt success exists;
* after-receipt-success recovers as receipt recorded;
* after-receipt-ambiguous fails closed / no receipt;
* receipt record-failed window fails closed / no receipt;
* receipt rollback-completed window fails closed / no receipt;
* receipt rollback-failed window is fatal/fail-closed / no receipt;
* unknown window fails closed / no receipt;
* production/MainNet/external-publication recovery classification unavailable/fail-closed;
* MainNet peer-driven apply refusal precedes recovery classification where applicable.

Projection cases must prove at minimum:

* only Run 256 `BackendSubmissionRecorded` creates an audit/publication receipt request;
* Run 246 pipeline success alone creates no receipt request;
* Run 248 sink receipt record alone creates no receipt request;
* Run 250 completion-report record alone creates no receipt request;
* Run 252 finalization record alone creates no receipt request;
* Run 254 attestation record alone creates no receipt request;
* Run 256 backend request alone creates no receipt request;
* Run 256 backend duplicate-idempotent creates no new receipt unless a matching receipt already exists;
* evaluator success alone creates no receipt request;
* durable replay freshness alone creates no receipt request;
* mutation-engine authorization alone creates no receipt request;
* modeled applier success without sink receipt, completion report, finalization, attestation, and backend submission creates no receipt request;
* only `AuditReceiptRecorded` authorizes modeled audit/publication receipt recorded state;
* `AuditReceiptDuplicateIdempotent` does not create a second receipt and must not be counted as a new receipt;
* every no-receipt, unavailable, rejected, rollback, ambiguous, unsupported, and MainNet-refused outcome does not record.

Stage-ordering cases must prove at minimum:

* MainNet peer-driven apply refusal happens before pipeline progression, sink invocation, reporter invocation, finalizer invocation, attestor invocation, backend invocation, and receipt sink invocation.
* Evaluator/call-site authorization happens before durable replay freshness.
* Durable replay freshness happens before mutation-engine authorization.
* Mutation-engine authorization happens before modeled applier invocation.
* Modeled applier success happens before sink intent.
* Sink receipt recording happens after pipeline consume authorization.
* Completion report recording happens after sink receipt-recorded state.
* Finalization recording happens after completion-report recorded state.
* Attestation recording happens after durable-completion-finalized state.
* Backend request happens only after durable-completion-attested state.
* Backend record happens only after durable-completion-attested state.
* Receipt request happens only after backend-submitted state.
* Receipt record happens only after backend-submitted state.
* A rejection before the receipt stage leaves receipt sink invocation count at zero.
* A receipt record failure does not retroactively invalidate the modeled backend submission record but does not authorize receipt.
* A receipt rollback failure is fatal/fail-closed and does not authorize receipt.
* The receipt sink itself performs no persistent durable completion beyond modeled in-memory fixture state.

Receipt-ledger cases must prove at minimum:

* one valid receipt inserts exactly one modeled in-memory ledger record;
* duplicate identical receipt is idempotent and does not increase record count;
* same receipt record id with different digest is equivocation and does not increase record count;
* wrong receipt identity digest does not record;
* wrong receipt request digest does not record;
* wrong receipt response digest does not record;
* wrong receipt record digest does not record;
* wrong backend identity digest does not record;
* wrong backend request digest does not record;
* wrong backend response digest does not record;
* wrong backend receipt digest does not record;
* wrong backend transcript digest does not record;
* wrong attestation digest does not record;
* wrong finalization digest does not record;
* wrong completion-report digest does not record;
* wrong sink decision digest does not record;
* wrong reporter decision digest does not record;
* wrong finalization decision digest does not record;
* wrong pipeline decision digest does not record;
* wrong proposal id does not record;
* wrong decision id does not record;
* wrong candidate digest does not record;
* wrong authority-domain sequence does not record;
* malformed receipt request does not record;
* malformed receipt response does not record;
* rollback restores modeled receipt ledger snapshot;
* rollback failure never claims success;
* fixture receipt ledger never writes files, RocksDB, markers, sequences, external publications, audit-ledger entries, or production durable state.

Non-mutation cases must prove at minimum:

* no Run 070 call on any receipt case;
* no `LivePqcTrustState` mutation on any receipt case;
* no real trust swap on any receipt case;
* no session eviction on any receipt case;
* no sequence write on any receipt case;
* no marker write on any receipt case;
* no RocksDB/file/schema/migration/storage-format write or format change;
* no wire/schema/marker/sequence/trust-bundle format change;
* no external network publication;
* no real audit-ledger write;
* fixture receipt sink mutates only `DurableCompletionAuditPublicationReceiptLedger`;
* production/MainNet/external-publication paths remain unavailable/fail-closed;
* MainNet peer-driven apply remains refused first.

Real release-binary surface scenarios:

The harness must run the real `target/release/qbind-node` for at least:

* S1 — `--help` / default help surface: no audit-ledger, external-publication, or receipt-boundary production enablement banner or visible public flag drift.
* S2 — default DevNet startup parse/smoke surface: no receipt/audit/publication enablement claim.
* S3 — default TestNet startup parse/smoke surface: no receipt/audit/publication enablement claim.
* S4 — default MainNet startup parse/smoke surface: no receipt/audit/publication enablement claim.
* S5 — hidden governance-execution selector or equivalent hidden selector still parses as before, without exposing the Run 258/259 helper-only path as a public production enablement surface.
* S6 — invalid governance-execution selector or equivalent invalid selector fails closed before mutation if applicable, with no marker write, no sequence write, no live trust swap, no session eviction, no Run 070 call, no receipt sink invocation, no audit-ledger write, and no external publication.

Do not add any new public CLI surface for Run 259. If a hidden selector already exists from earlier runs, the harness may use it only as a parse/fail-closed smoke surface.

Denylist:

Prove the captured real-binary logs and helper logs do not contain active/enabled claims for:

* real audit ledger backend enabled;
* real external publication backend enabled;
* durable-completion audit-publication receipt production enabled;
* MainNet audit-ledger receipt enabled;
* MainNet external-publication receipt enabled;
* real production attestation backend enabled;
* real finalization backend enabled;
* real completion-report backend enabled;
* real durable consume backend enabled;
* real persistent replay backend enabled;
* real production mutation engine enabled;
* MainNet mutation engine enabled;
* MainNet governance enabled;
* MainNet peer-driven apply enabled;
* real governance execution engine enabled;
* real on-chain governance proof verifier enabled;
* RocksDB replay backend enabled;
* file replay backend enabled;
* schema migration enabled;
* storage-format migration enabled;
* KMS/HSM backend enabled;
* RemoteSigner backend enabled;
* validator-set rotation enabled;
* policy-change action enabled;
* autonomous apply;
* apply-on-receipt;
* peer-majority authority;
* Run 070 apply from the receipt boundary;
* LivePqcTrustState mutation from the receipt boundary;
* real trust swap from the receipt boundary;
* session eviction from the receipt boundary;
* marker write from the receipt boundary;
* sequence write from the receipt boundary;
* RocksDB write from the receipt boundary;
* file write from the receipt boundary;
* external publication by the fixture receipt sink;
* audit-ledger write by the fixture receipt sink;
* production durable consume by the fixture receipt sink;
* production finalization by the fixture receipt sink;
* production attestation by the fixture receipt sink;
* production backend submission by the fixture receipt sink;
* DummySig / DummyKem / DummyAead active on production path.

Source/release reachability proof:

The harness must grep production source for Run 258 symbols and prove the release helper exercises them in release mode.

Required greps include:

* `pqc_governance_durable_completion_audit_publication_receipt`
* `DurableCompletionAuditPublicationReceiptInput`
* `DurableCompletionAuditPublicationReceiptPolicy`
* `DurableCompletionAuditPublicationReceiptKind`
* `DurableCompletionAuditPublicationReceiptIdentity`
* `DurableCompletionAuditPublicationReceiptExpectations`
* `DurableCompletionAuditPublicationReceiptRequest`
* `DurableCompletionAuditPublicationReceiptResponse`
* `DurableCompletionAuditPublicationReceiptRecord`
* `DurableCompletionAuditPublicationReceiptLedger`
* `DurableCompletionAuditPublicationReceiptLedgerRecord`
* `DurableCompletionAuditPublicationReceiptDigest`
* `DurableCompletionAuditPublicationReceiptTranscriptDigest`
* `DurableCompletionAuditPublicationReceiptOutcome`
* `GovernanceDurableCompletionAuditPublicationReceiptSink`
* `FixtureDurableCompletionAuditPublicationReceiptSink`
* `ProductionAuditLedgerDurableCompletionReceiptSink`
* `MainNetAuditLedgerDurableCompletionReceiptSink`
* `ExternalPublicationDurableCompletionReceiptSink`
* `project_backend_submission_outcome_to_audit_receipt_request`
* `evaluate_durable_completion_audit_publication_receipt`
* `recover_durable_completion_audit_publication_receipt_window`
* `AuditReceiptRecorded`
* `AuditReceiptDuplicateIdempotent`
* `AuditReceiptRejectedBeforeRecord`
* `AuditReceiptRecordFailedNoReceipt`
* `AuditReceiptRolledBackNoReceipt`
* `AuditReceiptRollbackFailedFatalNoReceipt`
* `AuditReceiptAmbiguousFailClosedNoReceipt`
* `ProductionAuditLedgerUnavailableNoReceipt`
* `MainNetAuditLedgerUnavailableNoReceipt`
* `ExternalPublicationUnavailableNoReceipt`
* `MainNetPeerDrivenApplyRefusedNoReceipt`
* `ValidatorSetRotationUnsupportedNoReceipt`
* `PolicyChangeUnsupportedNoReceipt`
* all Run 258 invariant helper names.

If exact names differ, use the exact implemented Run 258 names and record the differences honestly in `QBIND_DEVNET_EVIDENCE_RUN_259.md`.

Validation commands:

Run at minimum:

* `cargo build -p qbind-node --release --bin qbind-node`
* `cargo build -p qbind-node --release --example run_259_durable_completion_audit_publication_receipt_release_binary_helper`
* `bash scripts/devnet/run_259_durable_completion_audit_publication_receipt_release_binary.sh`
* `cargo test -p qbind-node --test run_258_durable_completion_audit_publication_receipt_tests`
* `cargo test -p qbind-node --test run_256_durable_completion_attestation_backend_tests`
* `cargo test -p qbind-node --test run_254_modeled_durable_completion_attestation_projection_tests`
* `cargo test -p qbind-node --test run_252_modeled_durable_completion_finalization_projection_tests`
* `cargo test -p qbind-node --test run_250_modeled_durable_consume_completion_reporter_tests`
* `cargo test -p qbind-node --test run_248_modeled_durable_consume_projection_sink_tests`
* `cargo test -p qbind-node --test run_246_governance_modeled_end_to_end_pipeline_tests`
* `cargo test -p qbind-node --test run_244_modeled_governance_trust_mutation_applier_tests`
* `cargo test -p qbind-node --test run_242_governance_execution_mutation_engine_tests`
* `cargo test -p qbind-node --test run_240_governance_evaluator_replay_durable_runtime_integration_tests`
* `cargo test -p qbind-node --test run_238_governance_evaluator_replay_durable_backend_tests`
* `cargo test -p qbind-node --test run_236_governance_evaluator_replay_consume_runtime_integration_tests`
* `cargo test -p qbind-node --test run_234_governance_evaluator_replay_consume_boundary_tests`
* `cargo test -p qbind-node --test run_232_governance_evaluator_replay_runtime_integration_tests`
* `cargo test -p qbind-node --test run_230_governance_evaluator_replay_state_tests`
* `cargo test -p qbind-node --test run_228_peer_evaluator_context_representation_tests`
* `cargo test -p qbind-node --test run_226_governance_evaluator_runtime_callsite_wiring_tests`
* `cargo test -p qbind-node --test run_224_governance_evaluator_runtime_integration_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

Security scanning:

* Run secret scanning if available and report the result.
* Run CodeQL if available and report the result honestly.
* If Rust CodeQL analysis is skipped because the database is too large, say that explicitly and do not overclaim CodeQL coverage.

Documentation:

Create:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_259.md`
* `docs/devnet/run_259_durable_completion_audit_publication_receipt_release_binary/README.md`
* `docs/devnet/run_259_durable_completion_audit_publication_receipt_release_binary/summary.txt`
* `docs/devnet/run_259_durable_completion_audit_publication_receipt_release_binary/.gitignore`

Update narrowly:

* `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
* `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
* `docs/whitepaper/contradiction.md`

Documentation must state:

* Run 259 is release-binary evidence for Run 258.
* No production mutating behavior is enabled.
* The durable-completion audit-ledger / external-publication receipt boundary is release-evidenced, not production-enabled.
* The release helper exercises the Run 258 production library symbols in release mode.
* The release helper remains dead code from production runtime.
* The fixture receipt sink mutates only modeled in-memory `DurableCompletionAuditPublicationReceiptLedger` state.
* Production audit-ledger, MainNet audit-ledger, and external-publication sinks remain reachable but unavailable/fail-closed.
* Run 246 pipeline success is required before any sink intent can exist.
* Run 248 `ConsumeReceiptRecorded` is required before any completion-report intent can exist.
* Run 250 `CompletionReportRecorded` is required before any finalization intent can exist.
* Run 252 `DurableCompletionFinalized` is required before any attestation intent can exist.
* Run 254 `DurableCompletionAttested` is required before any backend request can exist.
* Run 256 `BackendSubmissionRecorded` is required before any audit/publication receipt request can exist.
* Only `AuditReceiptRecorded` authorizes a modeled audit/publication receipt state.
* Every non-submitted backend outcome produces no audit/publication receipt request and no receipt.
* Duplicate identical receipt is idempotent and creates no second receipt.
* Same receipt record id with different digest fails closed as equivocation.
* Failed receipt record, rollback, rollback-failed, ambiguous windows, unavailable production/MainNet/external-publication paths, rejected replay states, and unsupported actions never record.
* MainNet peer-driven apply remains refused before pipeline progression, sink invocation, reporter invocation, finalizer invocation, attestor invocation, backend invocation, and receipt sink invocation.
* Rejected receipt paths are non-mutating.
* No real audit ledger backend is implemented.
* No real external-publication backend is implemented.
* No real production attestation backend is implemented.
* No real finalization backend is implemented.
* No real completion-report backend is implemented.
* No real durable consume backend is implemented.
* No real persistent replay backend is implemented.
* No real governance execution engine is implemented.
* No real production mutation engine is implemented.
* No real on-chain governance proof verifier is implemented.
* No RocksDB/file/schema/migration/storage-format change is implemented.
* No wire/schema/marker/sequence/trust-bundle change is implemented.
* No KMS/HSM/RemoteSigner backend is implemented.
* No MainNet governance enablement.
* No MainNet peer-driven apply enablement.
* No validator-set rotation.
* The receipt boundary does not call Run 070.
* The receipt boundary does not mutate `LivePqcTrustState`.
* The receipt boundary does not perform a real trust swap.
* The receipt boundary does not evict sessions.
* The receipt boundary does not write sequence files.
* The receipt boundary does not write authority markers.
* The receipt boundary does not perform external publication.
* The receipt boundary does not write a real audit ledger.
* Full C4 remains OPEN.
* C5 remains OPEN.

Contradiction handling:

* Crosscheck Run 259 against Runs 050–258.
* If no contradiction is found, add a “no Run 259 contradiction” entry to `docs/whitepaper/contradiction.md`.
* If any contradiction is found, do not hide it. Record it explicitly with the exact file/function/doc reference and recommend the smallest safe fix.

Strict non-goals:

* Do not implement a real audit ledger backend.
* Do not implement real external publication.
* Do not implement a real persistent replay backend.
* Do not implement a real durable consume backend.
* Do not implement a real completion-report backend.
* Do not implement a real finalization backend.
* Do not implement a real production attestation backend.
* Do not implement a real production mutation engine.
* Do not wire production runtime mutation.
* Do not call Run 070.
* Do not mutate `LivePqcTrustState`.
* Do not perform a real trust swap.
* Do not evict sessions.
* Do not write sequence files.
* Do not write authority markers.
* Do not implement a real governance execution engine.
* Do not implement a real on-chain governance proof verifier.
* Do not implement validator-set rotation.
* Do not add RocksDB/file/schema/migration/storage-format changes.
* Do not change wire/schema/marker/sequence/trust-bundle formats.
* Do not enable MainNet governance.
* Do not enable MainNet peer-driven apply.
* Do not add autonomous apply or apply-on-receipt.
* Do not let peer majority satisfy authority.
* Do not weaken any fail-closed behavior from Runs 070 or 130–258.

Expected verdict:

Positive for release-binary evidence, if the release helper/harness and regression corpus pass.

Run 259 should close the Run 258 release-binary evidence gap only. It must not claim Full C4 or C5 closure.

Final response format:

1. Exact verdict.
2. Files changed.
3. Release artifacts and hashes.
4. Helper corpus results.
5. Real-binary scenarios.
6. Tests run and results.
7. Security invariants preserved.
8. Honest limitations.
9. C4/C5 status.
10. Suggested Run 260 next step.
