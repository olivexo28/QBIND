Task: Run 169 — Wire Governance-Proof Loader into Production v2 Marker-Decision Callers

Current accepted state:

* Run 163: pure governance authority verifier landed.
* Run 165: governance gate wired into lifecycle / marker-decision paths.
* Run 166: release-binary governance gate boundary evidence landed.
* Run 167: additive governance-proof carrier landed:

  * `pqc_governance_proof_wire.rs`;
  * `GovernanceAuthorityProofWire`;
  * schema version 1;
  * optional `governance_authority_proof` JSON sibling;
  * `GovernanceProofLoadStatus::{Absent, Available, Malformed}`;
  * `load_v2_ratification_sidecar_with_governance_proof_from_path`.
* Run 168: release-binary proof-carrier boundary evidence landed:

  * old no-proof sidecars remain compatible under `NotRequired` on real `target/release/qbind-node`;
  * release-built helper proves proof-carrying sidecars parse and pass/fail through the Run 165 governance gate;
  * MainNet remains refused;
  * no production runtime source delta;
  * no schema/wire/metric drift.
* Run 168 explicitly leaves the follow-up:
  production marker-decision callers still need to consume the typed governance-proof loader instead of using `Unavailable`.
* MainNet peer-driven apply remains refused.
* OnChainGovernance remains unsupported/fail-closed.
* governance execution remains unimplemented.
* KMS/HSM remains unimplemented.
* validator-set rotation remains open.
* full C4 remains open.
* C5 remains open.

Objective:
Wire the Run 167 governance-proof loader into the production v2 marker-decision callers so production source paths can pass `GovernanceProofContext::Available(...)`, `Unavailable`, or malformed-proof refusal into the Run 165 governance gate.

This is source/test integration only. Release-binary proof-carrying production-surface evidence is deferred to Run 170.

Strict scope:

* Source/test only.
* No release-binary harness in this run.
* No MainNet peer-driven apply enablement.
* No governance execution engine.
* No on-chain governance implementation.
* No KMS/HSM implementation.
* No validator-set rotation.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* No new governance-proof schema beyond Run 167’s optional sibling field.
* No marker schema change.
* No sequence-file schema change.
* No trust-bundle core schema change.
* No peer-candidate envelope schema change unless a hard blocker is found; if so, stop and document the blocker.
* Do not weaken Runs 070, 130–168.
* Do not claim full C4 or C5 closure.

Implementation target:
Update production source callers that currently pass `GovernanceProofContext::Unavailable` or equivalent into the governance-aware v2 marker-decision helper.

At minimum cover:

1. reload-check validation-only;
2. local peer-candidate-check validation-only, if it has a v2 ratification sidecar path;
3. process-start reload-apply;
4. startup `--p2p-trust-bundle`;
5. SIGHUP live reload;
6. live inbound `0x05` validation-only, if it has sidecar context;
7. peer-driven drain / `ProductionV2MarkerCoordinator`.

Required behavior:

* When a v2 ratification sidecar path is available, use:
  `load_v2_ratification_sidecar_with_governance_proof_from_path`.
* If the sidecar has no `governance_authority_proof`, pass a NotRequired-compatible absent/unavailable context according to current policy.
* If proof is present and well-formed, pass `GovernanceProofContext::Available(...)` or equivalent.
* If proof is malformed, fail closed before mutation.
* If proof is required but absent, fail closed with `GovernanceAuthorityRequiredButMissing`.
* If proof is present but invalid, fail closed with `GovernanceAuthorityRejected(...)`.
* No validation-only path may write marker, write sequence, swap live trust, evict sessions, or call Run 070.
* Mutating paths must still persist marker only after successful Run 055 sequence commit.
* MainNet peer-driven apply remains refused even with valid governance proof.

Governance policy:
Define or reuse a typed policy selection mechanism for production surfaces.

Minimum policy modes:

* `NotRequired`: existing compatibility mode.
* `RequiredForLifecycleSensitive`: governance proof required for Rotate / Retire / Revoke / EmergencyRevoke lifecycle-sensitive transitions.
* `Unavailable`: explicit no-proof-carrier path that fails closed only when proof is required.

If the current binary has no CLI/env/runtime way to select `RequiredForLifecycleSensitive`, do not add public CLI casually. Prefer:

* internal config/test hook for source tests; and
* document that release-binary Required-mode evidence needs a narrow hidden flag or environment gate in Run 170.

If a tiny hidden flag is necessary now to make the production caller path testable, it must be:

* hidden;
* disabled by default;
* DevNet/TestNet-only unless validation-only;
* MainNet-refused for peer-driven apply;
* documented as Run 169 scope.

Required tests:
Add focused tests, preferably:

`crates/qbind-node/tests/run_169_governance_proof_loader_surface_integration_tests.rs`

Minimum acceptance matrix:

A1. reload-check no-proof sidecar under NotRequired remains accepted

* no marker write;
* no sequence write.

A2. reload-check valid proof-carrying Rotate sidecar under Required policy accepted

* proof loads as Available;
* governance verifier accepts;
* lifecycle accepts;
* validation-only non-mutation preserved.

A3. reload-apply valid proof-carrying Rotate sidecar under Required policy accepted

* preflight accepts;
* if full apply is exercised, sequence commit precedes marker persist.

A4. startup valid proof-carrying Rotate sidecar under Required policy accepted

* startup marker preflight consumes proof context;
* marker persistence remains post-sequence boundary if mutation occurs.

A5. SIGHUP valid proof-carrying Rotate sidecar under Required policy accepted

* SIGHUP preflight consumes proof context;
* no marker write before sequence commit.

A6. peer-driven drain `ProductionV2MarkerCoordinator` valid proof-carrying Rotate accepted

* proof context reaches coordinator;
* no MainNet enablement.

A7. old no-proof sidecar remains compatible across production callers under NotRequired.

A8. valid proof-carrying Revoke sidecar accepted where representable.

A9. valid proof-carrying EmergencyRevoke sidecar accepted where representable; otherwise document exact source limitation and keep Run 167 source-test coverage.

Required rejection matrix:

R1. proof required but no proof rejected on reload-check.

R2. proof required but no proof rejected on reload-apply with no Run 070 call.

R3. malformed proof rejected before mutation.

R4. wrong environment proof rejected.

R5. wrong chain proof rejected.

R6. wrong genesis proof rejected.

R7. wrong authority root proof rejected.

R8. wrong lifecycle action proof rejected.

R9. wrong candidate digest proof rejected.

R10. wrong authority-domain sequence proof rejected.

R11. invalid issuer signature rejected.

R12. unsupported issuer suite rejected.

R13. non-PQC suite rejected.

R14. threshold not met rejected if representable.

R15. stale/replayed proof rejected.

R16. OnChainGovernance proof rejected as unsupported/fail-closed.

R17. local operator config alone rejected as governance authority proof.

R18. peer majority / gossip count rejected as governance authority proof.

R19. proof valid but lifecycle invalid rejected.

R20. lifecycle valid but proof invalid rejected.

R21. governance rejection on startup produces no marker write.

R22. governance rejection on SIGHUP produces no live trust swap, no eviction, no sequence write, and no marker write.

R23. governance rejection on peer-driven drain produces no apply, no swap, no eviction, no sequence write, and no marker write.

R24. validation-only surfaces remain non-mutating.

R25. MainNet peer-driven apply remains refused even with valid governance proof.

Required source-reachability proof:
Tests and docs must show that these production source paths no longer hardcode proof context as always unavailable when a sidecar path exists.

Include grep/call-site evidence for:

* `load_v2_ratification_sidecar_with_governance_proof_from_path`;
* `GovernanceProofLoadStatus::Available`;
* `GovernanceProofLoadStatus::Malformed`;
* `GovernanceProofContext::Available`;
* `decide_v2_marker_acceptance_with_lifecycle_and_governance`.

Required backwards compatibility:
All prior tests must remain green:

* Run 134 reload-apply v2 marker tests.
* Run 138 SIGHUP v2 tests.
* Run 142 live inbound `0x05` v2 validation tests.
* Run 148 / 150 / 152 peer-driven apply tests.
* Run 161 lifecycle marker tests.
* Run 163 governance verifier tests.
* Run 165 governance marker integration tests.
* Run 167 governance proof carrier tests.

Required documentation:
Add:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_169.md`

Update:

* `docs/whitepaper/contradiction.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

Documentation must state:

* Run 169 is source/test integration only.
* Production marker-decision callers now consume the governance-proof loader where sidecar paths exist.
* Existing no-proof sidecars remain compatible under NotRequired policy.
* Required policy fails closed when proof is absent.
* Valid proof-carrying sidecars reach governance gate at source/test level.
* Release-binary production-surface proof-carrying evidence is deferred to Run 170.
* MainNet peer-driven apply remains refused.
* OnChainGovernance remains unsupported/fail-closed.
* governance execution remains unimplemented.
* KMS/HSM remains unimplemented.
* validator-set rotation remains open.
* full C4 remains open.
* C5 remains open.

Validation commands:
Run at minimum:

* `cargo build -p qbind-node --lib`
* `cargo test -p qbind-node --test run_169_governance_proof_loader_surface_integration_tests`
* `cargo test -p qbind-node --test run_167_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_165_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_163_governance_authority_verifier_tests`
* `cargo test -p qbind-node --test run_161_lifecycle_marker_integration_tests`
* `cargo test -p qbind-node --test run_159_authority_signing_key_lifecycle_tests`
* `cargo test -p qbind-node --test run_157_unified_testnet_fixture_universe_tests`
* `cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests`
* `cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests`
* `cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests`
* `cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests`
* `cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests`
* `cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Acceptance criteria:
Run 169 is acceptable only if:

1. Production source marker-decision callers consume the governance-proof loader where sidecar paths exist.
2. Valid proof-carrying sidecars reach `GovernanceProofContext::Available` at source/test level.
3. Malformed proof sidecars fail closed.
4. Required policy with absent proof fails closed.
5. NotRequired compatibility remains intact.
6. Rejected governance decisions produce no mutation.
7. Validation-only surfaces remain non-mutating.
8. Mutating surfaces still persist marker only after sequence commit.
9. MainNet peer-driven apply remains refused even with valid proof.
10. Docs defer release-binary production-surface proof-carrying evidence to Run 170.
11. No full C4 or C5 closure is claimed.
