Task: Run 180 — Source/Test Wire OnChainGovernance Verifier into Production Marker-Decision Composition

Current accepted state:

* Run 178: source/test typed `OnChainGovernance` proof verifier landed:

  * `pqc_onchain_governance_proof.rs`;
  * `OnChainGovernanceProof`;
  * `OnChainGovernanceProofPolicy::{Disabled, AllowFixtureSourceTest}`;
  * `OnChainGovernanceProofWire` schema v1;
  * pure `verify_onchain_governance_proof`;
  * pure combined lifecycle helper;
  * MainNet-refusal helper;
  * 46 tests.
* Run 179: release-binary boundary evidence landed:

  * release-built helper exercises the Run 178 verifier corpus through production library symbols;
  * real `target/release/qbind-node` confirms MainNet refusal remains intact;
  * `verify_onchain_governance_proof` has zero production callers under `crates/qbind-node/src/`;
  * `validate_lifecycle_with_onchain_governance_proof` has zero production callers;
  * `OnChainGovernanceProofPolicy::AllowFixtureSourceTest` has zero production callers;
  * `OnChainGovernanceProofWire` has zero production callers;
  * no production source/schema/wire/metric drift;
  * full C4 remains open;
  * C5 remains open.
* Run 179 identifies the next integration run:
  wire `OnChainGovernanceProofPolicy::AllowFixtureSourceTest` and `validate_lifecycle_with_onchain_governance_proof` into a production marker-decision caller behind a hidden DevNet/TestNet-only evidence policy, while preserving `Disabled` as the production default.
* MainNet peer-driven apply remains refused.
* governance execution remains unimplemented.
* real on-chain proof verification remains unimplemented.
* KMS/HSM remains unimplemented.
* validator-set rotation remains open.
* full C4 remains open.
* C5 remains open.

Objective:
Wire the Run 178 `OnChainGovernance` verifier into production source marker-decision composition at source/test level, behind a hidden DevNet/TestNet-only evidence policy.

This run must make `verify_onchain_governance_proof` / `validate_lifecycle_with_onchain_governance_proof` production-source reachable without enabling MainNet apply or claiming real on-chain governance execution.

Release-binary production-surface evidence is deferred to Run 181.

Strict scope:

* Source/test only.
* No release-binary harness in this run.
* Hidden DevNet/TestNet evidence policy only.
* Default production policy remains `OnChainGovernanceProofPolicy::Disabled`.
* No MainNet peer-driven apply enablement.
* No governance execution engine.
* No real on-chain proof verifier.
* No bridge/light-client integration.
* No KMS/HSM implementation.
* No validator-set rotation.
* No autonomous apply.
* No automatic apply on receipt.
* No peer-majority authority.
* No marker schema change.
* No sequence-file schema change.
* No trust-bundle core schema change.
* No additional wire/schema change unless a hard blocker is found and documented.
* Do not weaken Runs 070, 130–179.
* Do not claim full C4 or C5 closure.

Required design:
Add a hidden policy selector and source-level marker-decision composition for `OnChainGovernance` fixture proofs.

Required policy modes:

1. `Disabled`

   * default everywhere;
   * production-safe;
   * current behavior.

2. `AllowFixtureSourceTest`

   * hidden;
   * explicit;
   * DevNet/TestNet-only for evidence;
   * must not enable MainNet peer-driven apply;
   * must be documented as fixture/source-test only;
   * must not be described as real on-chain governance.

Acceptable selector form:
Prefer a hidden CLI/env selector analogous to Run 171:

* CLI: `--p2p-trust-bundle-onchain-governance-fixture-allowed`
* env: `QBIND_P2P_TRUST_BUNDLE_ONCHAIN_GOVERNANCE_FIXTURE_ALLOWED=1|true|yes|on`

The selector must be:

* hidden;
* disabled by default;
* rejected or ignored for MainNet peer-driven apply;
* not a governance execution claim;
* not a KMS/HSM claim.

Required integration:
Wire the selected `OnChainGovernanceProofPolicy` into production marker-decision composition where governance-proof context already flows after Runs 169–177.

At minimum, source/test coverage must show the policy reaches:

1. reload-check validation-only;
2. reload-apply mutating preflight;
3. startup `--p2p-trust-bundle` preflight, if sidecar context exists;
4. SIGHUP preflight, if sidecar context exists;
5. local peer-candidate-check validation-only;
6. live inbound `0x05` validation-only;
7. peer-driven drain coordinator, if proof context exists.

The preferred implementation is a shared helper that composes:

`anti-rollback marker decision`
→ `Run 159 lifecycle validation`
→ `Run 163 governance authority verification`
→ `Run 178 OnChainGovernance fixture verifier when authority class is OnChainGovernance and policy allows it`

The helper must remain pure/preflight and must not write marker, write sequence, mutate live trust, evict sessions, or call Run 070.

Required behavior:

* Default `Disabled`:

  * OnChainGovernance proof remains rejected/fail-closed.
  * Existing GenesisBound and EmergencyCouncil proof behavior remains unchanged.
* `AllowFixtureSourceTest`:

  * DevNet/TestNet fixture OnChainGovernance proof may be accepted if:

    * domain matches;
    * lifecycle transition is valid;
    * candidate digest matches;
    * authority sequence matches;
    * proposal / quorum / threshold / freshness / replay checks pass;
    * governance proof suite is supported by the fixture verifier.
  * wrong-domain / wrong-proposal / expired / replayed / quorum-failed / invalid-proof cases fail closed.
* MainNet:

  * peer-driven apply remains refused even with `AllowFixtureSourceTest` and a valid fixture proof.
  * production MainNet OnChainGovernance remains unsupported unless a future run implements real proof verification and policy.

Required tests:
Add focused tests, preferably:

`crates/qbind-node/tests/run_180_onchain_governance_marker_integration_tests.rs`

Minimum acceptance matrix:

A1. default policy is Disabled.

* OnChainGovernance proof rejected/fail-closed by default.

A2. hidden CLI/env selector enables `AllowFixtureSourceTest`.

* selector parsing works;
* false/unset values preserve Disabled.

A3. reload-check DevNet fixture OnChainGovernance Rotate accepted under `AllowFixtureSourceTest`.

* validation-only;
* no marker write;
* no sequence write.

A4. reload-check TestNet fixture OnChainGovernance Rotate accepted under `AllowFixtureSourceTest`.

A5. reload-apply DevNet fixture OnChainGovernance Rotate accepted under `AllowFixtureSourceTest`.

* preflight accepts;
* if full apply is exercised, sequence-before-marker ordering preserved.

A6. local peer-candidate-check fixture OnChainGovernance Rotate accepted under `AllowFixtureSourceTest`.

A7. live inbound `0x05` fixture OnChainGovernance Rotate accepted under `AllowFixtureSourceTest`, if proof context is available after Run 176.

A8. peer-driven drain coordinator fixture OnChainGovernance Rotate accepted under `AllowFixtureSourceTest`, if proof context is available.

A9. GenesisBound and EmergencyCouncil proof behavior unchanged.

Required rejection matrix:

R1. OnChainGovernance proof rejected under default Disabled policy.

R2. OnChainGovernance proof rejected if selector unset / false.

R3. OnChainGovernance proof rejected on MainNet peer-driven apply even under `AllowFixtureSourceTest`.

R4. wrong environment rejected.

R5. wrong chain rejected.

R6. wrong genesis rejected.

R7. wrong authority root rejected.

R8. wrong governance domain rejected.

R9. wrong proposal digest rejected.

R10. wrong proposal outcome rejected.

R11. wrong lifecycle action rejected.

R12. wrong candidate digest rejected.

R13. wrong authority-domain sequence rejected.

R14. expired governance proof rejected.

R15. replayed governance decision rejected.

R16. quorum not met rejected.

R17. threshold not met rejected.

R18. invalid proof bytes rejected.

R19. unsupported proof suite rejected.

R20. malformed proof rejected.

R21. local operator config rejected.

R22. peer-majority / gossip count rejected.

R23. OnChainGovernance proof valid but lifecycle invalid rejected.

R24. lifecycle valid but OnChainGovernance proof invalid rejected.

R25. validation-only rejection remains non-mutating.

R26. mutating preflight rejection produces no Run 070 call, no live trust swap, no session eviction, no sequence write, and no marker write.

R27. invalid live `0x05` OnChainGovernance proof candidate is not propagated, staged, or applied.

Required source-reachability proof:
Tests and docs must show production `src` call sites outside `pqc_onchain_governance_proof.rs` for:

* `verify_onchain_governance_proof` or the combined helper;
* `validate_lifecycle_with_onchain_governance_proof`;
* `OnChainGovernanceProofPolicy::AllowFixtureSourceTest`;
* `OnChainGovernanceProofWire`, if used in production context;
* selector helpers.

Required backwards compatibility:
All prior tests must remain green:

* Run 178 tests.
* Run 176 live `0x05` governance proof carrier tests.
* Run 173 validation-only Required-policy tests.
* Run 171 selector tests.
* Run 169 loader surface tests.
* Run 167 proof carrier tests.
* Run 165 governance marker tests.
* Run 163 governance verifier tests.
* Run 161 lifecycle marker tests.
* Run 159 lifecycle tests.
* Run 134/138/142/148/150/152 regression suites.

Required documentation:
Add:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_180.md`

Update:

* `docs/whitepaper/contradiction.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`

Documentation must state:

* Run 180 is source/test OnChainGovernance production-source integration only.
* Default remains `OnChainGovernanceProofPolicy::Disabled`.
* `AllowFixtureSourceTest` is hidden, explicit, and DevNet/TestNet fixture-only.
* Production MainNet OnChainGovernance remains unsupported/fail-closed.
* MainNet peer-driven apply remains refused.
* governance execution remains unimplemented.
* real on-chain proof verification remains unimplemented.
* KMS/HSM remains unimplemented.
* validator-set rotation remains open.
* release-binary OnChainGovernance production-surface evidence is deferred to Run 181.
* full C4 remains open.
* C5 remains open.

Validation commands:
Run at minimum:

* `cargo build -p qbind-node --lib`
* `cargo test -p qbind-node --test run_180_onchain_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_178_onchain_governance_proof_tests`
* `cargo test -p qbind-node --test run_176_live_0x05_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_173_validation_only_governance_required_policy_tests`
* `cargo test -p qbind-node --test run_171_governance_required_policy_selector_tests`
* `cargo test -p qbind-node --test run_169_governance_proof_loader_surface_integration_tests`
* `cargo test -p qbind-node --test run_167_governance_proof_carrier_tests`
* `cargo test -p qbind-node --test run_165_governance_marker_integration_tests`
* `cargo test -p qbind-node --test run_163_governance_authority_verifier_tests`
* `cargo test -p qbind-node --test run_161_lifecycle_marker_integration_tests`
* `cargo test -p qbind-node --test run_159_authority_signing_key_lifecycle_tests`
* `cargo test -p qbind-node --test run_157_unified_testnet_fixture_universe_tests`
* `cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests`
* `cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests`
* `cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests`
* `cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests`
* `cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests`
* `cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests`
* `cargo test -p qbind-node --lib pqc_authority`
* `cargo test -p qbind-node --lib`

If exact test names differ, locate nearest existing targets and document exact commands/results.

Acceptance criteria:
Run 180 is acceptable only if:

1. `OnChainGovernance` verifier becomes production-source reachable outside `pqc_onchain_governance_proof.rs`.
2. Default policy remains Disabled and fail-closed.
3. Hidden `AllowFixtureSourceTest` selector exists and is explicit.
4. DevNet/TestNet fixture OnChainGovernance proofs can pass at source/test marker-decision level.
5. Wrong-domain, wrong-proposal, expired, replayed, quorum/threshold failure, invalid-proof, malformed-proof, unsupported-suite cases fail closed.
6. GenesisBound and EmergencyCouncil behavior remains unchanged.
7. Validation-only surfaces remain non-mutating.
8. Mutating surfaces preserve sequence-before-marker ordering.
9. MainNet peer-driven apply remains refused even with a valid fixture OnChainGovernance proof.
10. Docs defer release-binary production-surface evidence to Run 181.
11. No full C4 or C5 closure is claimed.
