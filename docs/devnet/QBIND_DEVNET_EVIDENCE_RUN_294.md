# QBIND DevNet evidence — Run 294

**Title.** Release-binary evidence for the Run 293 production RemoteSigner backend.

Run 294 is release-binary evidence for the Run 293 source/test **production RemoteSigner backend** in `crates/qbind-node/src/pqc_production_remote_signer_backend.rs`. It proves, on real `target/release/qbind-node` plus the release-built helper `crates/qbind-node/examples/run_294_production_remote_signer_backend_release_binary_helper.rs`, that the Run 293 production library symbols are present and exercised in release mode over source/test loopback/mock transports. Run 294 does not add new production runtime wiring, does not add a public CLI flag, does not enable RemoteSigner by default, does not enable MainNet, does not implement KMS/HSM/cloud-KMS/PKCS#11 custody, does not implement custody attestation, does not implement on-chain governance proof verification, does not implement a governance execution engine, does not implement validator-set rotation, does not implement settlement or external publication, does not call Run 070, does not mutate `LivePqcTrustState`, and does not write trust-bundle sequence or authority marker files. Run 294 preserves the Run 292 durable replay RocksDB Green-for-scope status.

## 1. Exact verdict

**PASS (release-binary evidence only; Full C4 OPEN; C5 OPEN).**

Release `qbind-node` builds; the release helper builds; the harness exits `0`; the helper corpus passes (`total_pass: 66` / `total_fail: 0`); the real-binary scenarios pass (`S1_help=0 S2=1 S3=1 S4=1 S5=1 S6=1`); release artifacts and hashes are captured; the denylist is clean; reachability greps pass; C4/C5 taxonomy greps pass; the Run 293 source tests still pass; the required regression corpus passes; the secret scan is clean; the CodeQL result is reported honestly (section 14). No C4/C5 closure, MainNet readiness, or runtime default-enablement is claimed.

## 2. Files changed

Added:

* `crates/qbind-node/examples/run_294_production_remote_signer_backend_release_binary_helper.rs` — release helper (66 checks across 6 tables).
* `scripts/devnet/run_294_production_remote_signer_backend_release_binary.sh` — LF-clean, executable harness.
* `docs/devnet/run_294_production_remote_signer_backend_release_binary/` — ignored evidence archive tracking only `README.md`, `summary.txt`, `.gitignore`.
* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_294.md` — this canonical report.

Modified (narrow doc updates only):

* `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md` — RemoteSigner backend row updated to Green-for-scope; status line to Run 294.
* `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
* `docs/whitepaper/contradiction.md`
* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_293.md` — preflight documentation hygiene only: the CodeQL note now records the exact honest result directly (attempted but did not complete; no coverage claimed) instead of deferring to "the final task response". Run 293's verdict is unchanged.

No production library source was changed. The Run 293 backend module is re-run unchanged.

## 3. Release artifacts and hashes

Captured in the tracked `summary.txt` of the evidence directory (regenerated by the harness). Build toolchain: `rustc 1.96.0` / `cargo 1.96.0`. The `summary.txt` records `qbind_node_sha256` for `target/release/qbind-node` and `helper_294_sha256` for the release-built helper binary, plus the `git_commit`, branch, dirty/clean status, and host/kernel summary at capture time (in `provenance.txt`, git-ignored). See `docs/devnet/run_294_production_remote_signer_backend_release_binary/summary.txt` for the exact SHA-256 values from the capturing run.

## 4. Helper corpus results

Release helper verdict: **PASS**. Tables:

| table | pass | fail |
| --- | --- | --- |
| accepted_compatible | 14 | 0 |
| rejection_fail_closed | 32 | 0 |
| mainnet_authority_policy | 7 | 0 |
| replay_recovery_idempotency | 8 | 0 |
| non_mutation | 4 | 0 |
| reachability | 1 | 0 |
| **total** | **66** | **0** |

The helper constructs the real Run 293 `ProductionRemoteSignerBackend` over source/test loopback/mock transports, submits requests, verifies responses, and asserts every failure surfaces as a typed `ProductionRemoteSignerOutcome` rather than a silent fixture/loopback/local-signing fallback. It emits `fixtures/run_294_deterministic_digests.txt` (request id, request/response envelope digests, transport transcript digest, backend transcript digest); the harness runs the helper twice and diffs this fixture to prove deterministic-digest stability (request id `da765859…`, backend transcript digest `543bc0af…`).

## 5. Real-binary scenarios

* **S1** `--help` — rc=0. No Run 293/294 production RemoteSigner **backend** flag / symbol / run marker appears (`production-remote-signer-backend`, `ProductionRemoteSignerBackend`, `pqc_production_remote_signer_backend`, `run-294`, `run-293` are all absent). The pre-existing generic `remote-signer` signer-mode CLI flag (T210) is a separate long-standing surface and is unchanged. No custody/KMS/HSM, MainNet readiness, or C4/C5 closure claim appears.
* **S2** DevNet `--print-genesis-hash --env devnet` — rc=1 (requires `--genesis-path`); no RemoteSigner backend enablement banner; no mutation.
* **S3** TestNet `--print-genesis-hash --env testnet` — rc=1; silent on backend claims; no mutation.
* **S4** MainNet `--print-genesis-hash --env mainnet` — rc=1; no RemoteSigner backend enablement banner; no MainNet enablement claim; existing MainNet-required-config fail-closed behavior preserved; no mutation.
* **S5** hidden governance-execution selector `--p2p-trust-bundle-governance-execution-policy fixture-governance-allowed` — parses (no new RemoteSigner CLI selector was added).
* **S6** invalid governance-execution selector `bogus-policy` — fails closed (rc≠0) with the `invalid governance-execution policy selector` message, before any mutation (no marker write, no sequence write, no trust swap, no session eviction, no Run 070 call).

No new public CLI surface was added. The denylist (42 patterns) was clean across captured logs and helper output (excluding the help text and helper summary).

## 6. Request/response/transcript/domain binding release evidence

The release helper proves, in release mode, that the Run 293 backend derives a deterministic Run 201 request envelope binding the Run 194 inner request digest (`canonical_request_digest == inner_request.canonical_digest()`), that the response echoes the request id (`request_id_echo == request_env.request_id`), and that `production_remote_signer_request_id` / `production_remote_signer_backend_transcript_digest` are deterministic 64-hex digests. Two identical requests produce identical request/transcript digests. A valid response authorizes exactly the matching request; a wrong environment / chain / genesis / authority-root fails closed as `RemoteSignerDomainMismatch`; a tampered transcript digest fails closed as `RemoteSignerTranscriptMismatch`. Composition through the Run 201 `validate_remote_signer_transport` boundary (which composes the Run 194 `validate_remote_signer` boundary) is exercised, and an optional Run 291 `durable_replay_record_digest` binds into the request id and backend transcript.

## 7. Accepted release evidence

Under the explicit `DevTestLoopbackEnabled` policy on a DevNet/TestNet trust domain, the source/test loopback RemoteSigner accepts a valid authority-lifecycle signing request and a valid governance-execution signing request; the accept path reaches the transport exactly once; acceptance carries only signer id / environment / request id / backend transcript (evidence-only, non-mutating). The `Disabled` default policy is explicit and inert (`DisabledNoRequest`, zero transport calls). `build_request_envelope` builds the envelope without invoking the transport. Protocol version is pinned to `1`.

## 8. Rejection / fail-closed release evidence

All 32 rejection checks pass in release mode: default `Disabled` fails closed before any transport call; MainNet identity refused before transport; fixture/loopback material refused for MainNet; wrong environment/chain/genesis/authority-root/authority-sequence/signer-identity/signer-key/request-id-echo/response-nonce/transcript/candidate/action/protocol rejected; missing/malformed/oversized response rejected; signer unavailable/refused/policy-rejected/attestation-unavailable rejected; transport endpoint unavailable / connection refused / timeout / decode-error / unsupported-protocol rejected; unsupported request kinds (validator-set rotation, policy change, on-chain governance proof verification) refused before transport; malformed spec fails closed as `AmbiguousFailClosed`; stale response-nonce replay rejected; a production-mode response is never accepted.

## 9. MainNet refusal / authority policy release evidence

MainNet cannot be satisfied by local operator material, fixture loopback signer, or peer majority: every non-production MainNet policy is `MainNetRefused` before any transport call. `MainnetProductionRequired` on MainNet returns the typed `MainNetProductionAuthorityUnavailable` (fail-closed unavailable). A non-MainNet production-required policy on DevNet/TestNet returns `RemoteSignerUnavailable` with zero transport calls and never falls back to the fixture loopback transport. A production policy never yields an accept in release mode. MainNet peer-driven apply remains refused (there is no peer-majority path into the backend at all).

## 10. Replay / recovery / idempotency release evidence

`recover_remote_signer_request_window(None, current)` is a clean `NoPriorRequest` no-op. Identical requests are idempotent over the deterministic request id (`IdempotentReplayOfSameRequest`). The same request id with a different request transcript fails closed as `ConflictingRequestForSameId`; the same request with a different response commitment fails closed as `ConflictingResponseForSameRequest`. Retry-then-success is idempotent across attempts (accept on attempt 2 after a retryable timeout on attempt 1); the retry budget is bounded (`max_attempts`); retry exhaustion fails closed as `RemoteSignerTimeout`; terminal/non-retryable errors (e.g. `MalformedResponse`) are not retried (single attempt). Accepted responses are evidence-only and authorize nothing beyond the signer boundary.

## 11. Production-binary default-disabled/silent evidence

The default `ProductionRemoteSignerBackendPolicy` is `Disabled` and fails closed before any transport call. The real `target/release/qbind-node` binary exposes **no** Run 293/294 production RemoteSigner backend CLI surface: `--help` and the default `--print-genesis-hash` surfaces are silent on backend enablement claims, confirming the backend is not constructed or wired into the default runtime and that no new CLI flag was added.

## 12. C4/C5 matrix taxonomy verification

The harness greps the C4/C5 closure-criteria matrix and confirms the taxonomy clarification remains present: it separates **boundary readiness** from **production readiness**. The production durable replay RocksDB backend row remains Green **for release-binary-evidenced scope only** (Run 292). The Real production RemoteSigner backend row is now Green **for release-binary-evidenced RemoteSigner backend behavior only** — not wired by default, no public CLI flag, MainNet refused absent production authority criteria, no KMS/HSM/custody-attestation/on-chain-verifier/governance-engine/validator-set-rotation implementation, does not close C4/C5. Real KMS/HSM/cloud-KMS/PKCS#11 custody backend, custody attestation verifier, on-chain governance proof verifier, governance execution engine, validator-set rotation, MainNet authority rotation/revocation under production custody, production signing audit trail / crypto-agility / incident response, and full MainNet release-binary evidence under production custody all remain **Red**. Run 294 does not reinterpret the matrix clarification as C4/C5 closure.

## 13. Regression results

The harness ran the required regression corpus: `run_293` (production RemoteSigner backend source tests), `run_291` (durable replay RocksDB), the durable-completion / settlement / external-publication corpus (`run_290` … `run_224`), the Run 201 transport boundary tests (`run_201_remote_signer_transport_boundary_tests`), the Run 194 RemoteSigner boundary tests (`run_194_remote_authority_signer_boundary_tests`), plus `--lib pqc_authority` and `--lib`. Actual Run 194 / Run 201 test target names differ from the task's suggested names and the real names were used: `run_194_remote_authority_signer_boundary_tests` and `run_201_remote_signer_transport_boundary_tests` (substitution recorded here). All targets `rc=0`. Exact per-target results are captured in the tracked `summary.txt`.

## 14. Security scan results

* **Secret scanning** — run over all changed files and evidence scripts; **no secrets** found. The backend derives ids/transcripts by SHA3-256 over public spec fields and never embeds keys.
* **CodeQL** — Run 294 is release-binary evidence for a real RemoteSigner backend and is not classified as trivial. The CodeQL checker was invoked (language: rust) but **did not complete**: the analysis timed out / was skipped because the database size is too large. **No CodeQL coverage is claimed** for this pass. No production library surface was changed by this pass (the change set is a release-mode example helper, a harness shell script, and Markdown/evidence documentation), and the underlying Run 293 production module is re-run unchanged. The exact honest CodeQL limitation is recorded here directly.

## 15. Honest limitations

Run 294 is release-binary evidence only. It does not enable any production mutating behavior, does not wire the RemoteSigner backend into the default runtime, and does not implement any KMS/HSM/cloud-KMS/PKCS#11 custody backend, custody attestation verifier, on-chain governance proof verifier, governance execution engine, validator-set rotation, settlement, or external publication. The loopback/mock transports are release-mode evidence scaffolding only; under a production policy there is no fixture/loopback/local-key fallback. Run 294 closes only the Run 293 release-binary evidence gap and does not weaken the Run 292 durable replay RocksDB Green-for-scope status.

## 16. C4/C5 status

Full **C4 remains OPEN**. **C5 remains OPEN**. Run 294 makes no closure claim for either.

## 17. Suggested Run 295 next step

Run 295 should begin the next Red-row closure campaign: source/test **production KMS/HSM/cloud-KMS/PKCS#11 custody backend** implementation, or source/test **production custody attestation verifier** implementation. Choose KMS/HSM custody first for direct custody provider integration; choose custody attestation first to establish proof/attestation verification before provider-specific signing calls. In either case keep the same pattern — real backend implementation at source/test level, default `Disabled`/fail-closed, MainNet refused unless production authority criteria are satisfied, with release-binary evidence deferred to Run 296.
