RUN 422 D4/D5 — VALIDATION COMMANDS
Tested revision: this continuation's worktree tip (A/B commit 43dedea…; docs
commits follow). The task-named prior revision e1b95948… is ABSENT from this
shallow/squashed checkout; re-run targets executed against the current worktree.
Toolchain: repository-pinned stable Rust (cargo/rustc as vendored in CI image).
Profiles/features are noted per command. Exit codes: 0 unless stated.
============================================================================

PRODUCTION CHECK AND RELEASE BUILD (PRESERVED — original tested SHA)
----------------------------------------------------------------------------
  These are NOT re-run against this tip and are NOT relabeled as freshly
  executed: this continuation changed only tests + documentation, so no new
  release-binary evidence campaign is required.

  cargo check -p qbind-node
      profile: dev (check) | features: default (NO test-utils) | exit 0 (prior)

  cargo build --release -p qbind-node --bin qbind-node
      profile: release | features: default (NO test-utils) | exit 0 (prior)
      (Release build is a build artifact, NOT runtime evidence.)

FOCUSED TEST TARGETS (default test profile, default features) — this tip
----------------------------------------------------------------------------
  # Full binary_consensus_loop lib module (includes run030 Timeout crypto,
  # all D5 combined-context tests, and the two NEW D5 tests).
  cargo test -p qbind-node --lib binary_consensus_loop
      features: default | exit 0 | 106 passed (was 104; +run422_d5g, +run422_d5h)

  # D5 locator filter — SUBSET of the 106 above (same lib binary); NOT additive,
  # do not double-count.
  cargo test -p qbind-node --lib run420::run422_d5
      features: default | exit 0 | 12 passed (subset of the 106)

  # D4 startup-ordering PROCESS tests (spawn CARGO_BIN_EXE_qbind-node; bounded
  # DrainedChild runner with a monotonic deadline).
  cargo test -p qbind-node --test run_422_d4_startup_ordering_tests
      features: default | exit 0 | 5 passed
      NOTE: PROCESS tests, not standalone release-binary evidence.

  # Existing Run 422 startup-refusal and genesis-authority loader tests.
  cargo test -p qbind-node --test run_422_startup_refusal_tests
      features: default | exit 0 | 4 passed
  cargo test -p qbind-node --test run_422_genesis_consensus_authority_tests
      features: default | exit 0 | 15 passed

  # Run 420 production-policy reachability guard.
  cargo test -p qbind-node --test run_420_production_policy_reachability_tests
      features: default | exit 0 | 3 passed

  # Run 418 sender-binding.
  cargo test -p qbind-node --test run_418_authenticated_peer_consensus_sender_binding_tests
      features: default | exit 0 | 18 passed

  # Affected B9 late-peer re-emit integration target.
  cargo test -p qbind-node --test b9_late_peer_connect_proposal_reemit_tests
      features: default | exit 0 | 6 passed

LINT / FORMAT (changed code only; no mass-format)
----------------------------------------------------------------------------
  cargo clippy -p qbind-node --lib --tests
      No NEW warnings introduced by the changed regions. (Pre-existing lib
      warnings, e.g. dead_code cert_bound_node_id, remain; unrelated.)
  rustfmt applied to the changed test file only
  (crates/qbind-node/tests/run_422_d4_startup_ordering_tests.rs).
      binary_consensus_loop.rs is pervasively non-rustfmt-clean at baseline;
      new test code follows the file's existing local style — NOT mass
      reformatted, to keep the diff minimal.

TEST-UTILS-ONLY TARGET (recorded separately)
----------------------------------------------------------------------------
  m16_epoch_transition_hardening_tests requires --features test-utils to
  compile (uses cfg(test-utils) storage hooks). This is PRE-EXISTING and
  unrelated to this change; it is NOT part of the production check/release
  build above.

SECURITY ANALYSIS
----------------------------------------------------------------------------
  CodeQL security scan: ATTEMPTED in this environment and SKIPPED / incomplete
  because the database size is too large (tool reported "Analysis was skipped
  because the database size is too large"). The accompanying "0 alerts" is a
  consequence of the skip, NOT a completed zero-alert conclusion; no
  execution/result/timestamp/zero-alert conclusion is invented. The parallel
  code-review invocation returned a model-registry error (claude-sonnet-4.6 not
  in registry) and is NOT counted as an unqualified successful review; a code
  review is not a substitute for CodeQL. See summary.txt. Secret/privacy scan of
  changed files: clean.