RUN 422 D4/D5 — SUMMARY AND VERDICT
============================================================================

RESULT=POSITIVE-FOR-CONSENSUS-SECURITY-PREFLIGHT-AND-CONTEXT-SEPARATION-CODE-TEST
D4_STATUS=PRE-P2P-AND-CONSENSUS-PREFLIGHT-CODE-TEST-POSITIVE
D5_STATUS=MESSAGE-FAMILY-AUTHORITY-SEPARATION-CODE-TEST-POSITIVE
GENESIS_AUTHORITY_ACTIVATION=DISABLED-PENDING-REMAINING-BOUNDARIES
LEGACY_TIMEOUT_CONTEXT=EXISTING-POLICY-PRESERVED
PROPOSAL_VOTE_AUTHORITY_FROM_LEGACY_CLI=DISALLOWED
CONFIGURED_AUTHORITY_RELEASE_BINARY_EVIDENCE=NOT-YET-CAPTURED
SECURITY_POSTURE=RS1-OPEN / PUBLIC-DEVNET-NO-GO

The implementation, validation, and runtime-evidence axes are kept separate:
the POSITIVE label above is a CODE + TEST result. It is not a runtime,
release-binary, or public-deployment claim.

----------------------------------------------------------------------------
WHAT WAS PROVED (D5 — message-family authority separation)
----------------------------------------------------------------------------

The reviewed D5 tests constructed a valid TimeoutVerificationContext but never
passed it through the Proposal/Vote path under test (the helpers passed None
for the Timeout context, the authenticated origin, and the binding gate). This
continuation adds COMBINED-CONTEXT tests that drive, through the real inbound
handler `handle_inbound_consensus_msg` in a single invocation:

  * a valid TimeoutVerificationContext (with a live signer + key provider);
  * proposal_vote_authority = None;
  * ConsensusVerificationPolicy::Required;
  * a real PeerConsensusBindingGate + a matching AuthenticatedConsensusOrigin;
  * encoded Proposal and Vote messages.

Results (see test_results.txt / message_family_matrix.txt):

  A  Authenticated Proposal, Timeout authority only:
       F6 admission succeeds exactly once (gate.accepted()==1),
       Proposal/Vote authority-unavailable increments exactly once,
       cryptographic-verification latency observations stay 0,
       no delivery/engine/reconfig/restore-deferral/outbound effect,
       engine view + committed height unchanged.
  B  Authenticated Vote, Timeout authority only: the equivalent Vote
       boundary, with no contribution to aggregation or QC formation.
  C  F6 rejection precedes Proposal/Vote authority lookup: a mismatched
       authenticated sender increments claimed_sender_mismatch exactly once,
       while authority-unavailable and crypto-verify counters stay 0 (proposal
       AND vote variants). A recording binding-gate metric proves the crypto
       verifier was never reached.
  D  Outbound suppression through the ACTUAL forward_actions_to_facade
       boundary (not merely a helper returning None): a recording facade
       observes NO Proposal/Vote broadcast, NO self-injection, NO directed
       vote; there is no fallback to the Timeout signer; the outbound
       authority-unavailable counters increment instead.
  F  Type / production reachability: a valid Timeout context is usable only
       for its own family; there is no conversion or adapter promoting it into
       ProposalVoteAuthority (a compile-time property of the distinct nominal
       types), and a correctly-signed authenticated Proposal is still rejected
       authority-unavailable. NOTE: run422_d5f does NOT itself sign/verify a
       Timeout — see the Timeout-attribution note below.
  G  NEW (Finding B-A) — Required-policy late-peer re-emission suppression:
       drives the ACTUAL maybe_reemit_on_late_peer_connect (the B9/B10 tests
       exercise it only under LocalFixtureUnsigned) with Required, absent
       Proposal/Vote authority, a cached current-view proposal AND vote, a
       leader/current-view engine, a genuine newly-connected-peer transition,
       and a RECORDING facade. All gates 1..7 pass so control reaches the
       outbound authority/signing boundary (proven by
       outbound_proposal_verification_context_unavailable_total == 1) and the
       fail-closed refusal suppresses the broadcast: no proposal broadcast, no
       vote broadcast, no directed vote, no self-injection, no successful
       re-emit counters, reemitted_for_view stays None. Control-flow honesty:
       the proposal signs FIRST and returns None, so the function returns before
       the paired cached-vote branch; the vote-signing branch is NOT
       independently exercised (outbound vote authority-unavailable stays 0) and
       no such claim is made. No fallback to Timeout credentials (the signing
       fns take Option<&ProposalVoteAuthority> only).
  H  NEW (Finding B-B) — Active restore-mode Proposal authority-unavailable
       rejection: restore catchup mode is demonstrably ACTIVE (snapshot baseline
       at height 5), with a valid Timeout ctx, a real gate + matching origin,
       absent Proposal/Vote authority, Required policy, a RECORDING facade, and
       a Proposal shaped (height 7 > committed+1) to reach restore deferral IF
       admitted. A POSITIVE CONTROL proves the same-shaped proposal IS deferred
       once a Proposal/Vote authority is present. Main case: F6 admission
       succeeds, then the Proposal/Vote authority-unavailable rejection fires
       BEFORE the restore-deferral branch — no deferral, no delivery, no engine
       acceptance, no reconfig observation, no outbound frame (recording facade
       count == 0); restore mode stays active and engine view/committed height
       are unchanged.

Timeout-attribution note (Finding C-B): actual Timeout/NewView cryptographic
coverage is provided by the existing run030 tests in the SAME
binary_consensus_loop lib module (run030_outbound_signs_locally_emitted_timeout
signs + round-trips through verify_timeout_msg;
run030_outbound_fail_closed_when_signer_missing proves no broadcast without a
signer; plus inbound TimeoutMsg verification-gate cases). Fixture: ML-DSA-44
(suite 100) over QBIND_DEVNET_CHAIN_ID; outbound under the context's own signer
policy. No new production Timeout implementation was added for documentation.

Test-setup honesty: NOT every D5 test supplies both contexts + an authenticated
origin + a binding gate + a recording facade. Combined-context inbound tests
(A/B) and F6-mismatch tests (C) use the gate+origin; the action-forwarding test
(D) and the late-peer test (G) use a recording facade with no gate/origin; the
type/source-wiring test (F) inspects types; the pre-existing missing-authority
tests pass None for BOTH the Timeout ctx and origin/gate (compatibility only).

Distinction preserved throughout: a correctly SIGNED message is separate from
an AUTHENTICATED transport origin; one does not establish the other. Publicly
constructible struct fields are NOT themselves an unforgeable capability — the
guarantee is distinct types PLUS verified production construction/wiring that
supplies proposal_vote_authority = None.

----------------------------------------------------------------------------
WHAT WAS PROVED (D4 — startup ordering)
----------------------------------------------------------------------------

Bounded process tests drive the real qbind-node binary via
CARGO_BIN_EXE_qbind-node with valid temporary DevNet configuration:

  * RequireOrFail + missing local signer -> preflight FATAL, exit nonzero,
    BEFORE the P2P-transport boundary.
  * RequireOrFail + invalid peer key-provider (malformed hex) -> FATAL before
    P2P.
  * RequireOrFail + unsupported consensus suite id -> FATAL before P2P.
  * RequireOrFail + local key mismatches the loaded signer -> FATAL before P2P.
  * VALID legacy configuration (matching signer + complete
    --validator-consensus-key, single active validator) -> passes preflight,
    activates the Timeout/NewView context ("timeout verification ACTIVE"), and
    reaches the P2P-transport boundary ("[binary] P2P transport up.").

Each negative case asserts a positive control (the "[binary] Run 032:" /
"[binary] Run 033:" preflight marker IS present) so the refusal is attributed
to the preflight, not to CLI parsing or an unrelated earlier check. Absence of
the P2P-transport marker alone is never treated as sufficient. Source ordering
is recorded in source_trace.txt / startup_order.txt.

Positive-case wiring: the prepared ConsensusSecurityPreflight result is
consumed by production wiring (main.rs) WITHOUT reloading signer/provider
material — see source_trace.txt for the single-preparation source trace. This
is a source trace combined with the behavioral "reaches P2P startup" proof; it
is NOT a claim manufactured from an unrelated fixture counter.

----------------------------------------------------------------------------
METRICS EXCEPTION (see startup_order.txt)
----------------------------------------------------------------------------

main() may start the optional metrics HTTP server BEFORE entering the per-mode
P2P function and its consensus-security preflight. Therefore the D4 boundary is
"preflight before P2P service construction, peer dialing, and consensus-task
startup" — NOT before "any networking", "all listeners", or "every service".
The preflight function itself starts no network service. Unrelated
metrics/storage startup was NOT moved to strengthen the wording.

----------------------------------------------------------------------------
SECURITY (CodeQL) ANALYSIS STATUS
----------------------------------------------------------------------------

CodeQL security-scan tooling was ATTEMPTED in this environment and was SKIPPED
/ did not complete because the database size is too large (the tool reported
"Analysis was skipped because the database size is too large"). The "0 alerts"
figure that accompanies a skipped run is NOT a completed zero-alert conclusion
and is not asserted as one. The parallel code-review invocation returned a
model-registry error (model claude-sonnet-4.6 not found in registry), so it is
NOT described as an unqualified successful review, and a code review is not a
substitute for security analysis in any case. This gap is reported here
separately from the positive scoped behavioral result. The broad Clippy command
(cargo clippy -p qbind-node --lib --tests) is FAILED/INCOMPLETE: the agent
final report records a compile error in the pre-existing
m16_epoch_transition_hardening_tests target, which requires test-utils.
Nonzero exit reported; exact numeric exit code NOT RECORDED. This is not a
successful Clippy pass and was not rerun by this documentation correction.

----------------------------------------------------------------------------
PRESERVED LIMITATIONS / READINESS (UNCHANGED)
----------------------------------------------------------------------------

Genesis activation DISABLED. D6/D7 UNRESOLVED. F3/F4/F8 not fully
remediated/activated; F6 partial; F1/F2/F5/F7 unresolved (F5 not closed).
RS1 + C4/C5 OPEN. M4/M6/S5/S7 Yellow. Public DevNet NO-GO. No live-seed /
TestNet / MainNet readiness claim. Run 423 NOT started. Configured-authority
release-binary adversarial evidence NOT captured. Intentional liveness
limitation: fail-closed Proposal/Vote until a separately validated authority
is activated.