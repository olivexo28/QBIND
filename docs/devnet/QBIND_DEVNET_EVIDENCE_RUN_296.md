# QBIND DevNet evidence — Run 296

**Title.** Release-binary evidence for the Run 295 production KMS/HSM/cloud-KMS/PKCS#11 custody backend.

Run 296 is release-binary evidence for the Run 295 source/test **production KMS/HSM/cloud-KMS/PKCS#11 custody backend** in `crates/qbind-node/src/pqc_production_kms_hsm_custody_backend.rs`. It proves, on real `target/release/qbind-node` plus the release-built helper `crates/qbind-node/examples/run_296_production_kms_hsm_custody_backend_release_binary_helper.rs`, that the Run 295 production library symbols are present and exercised in release mode over source/test fixture KMS/HSM providers, reachable-but-fail-closed production provider stubs, and a programmable mock transport. Run 296 does not add new production runtime wiring, does not add a public CLI flag, does not enable KMS/HSM custody by default, does not enable MainNet, does not implement real provider credentials / real cloud-KMS account integration / real PKCS#11 session integration / real HSM module loading, does not implement custody attestation verifier closure, does not implement on-chain governance proof verification, does not implement a governance execution engine, does not implement validator-set rotation, does not implement settlement or external publication, does not call Run 070, does not mutate `LivePqcTrustState`, and does not write trust-bundle sequence or authority marker files. Run 296 preserves the Run 292 durable replay RocksDB Green-for-scope status and the Run 294 RemoteSigner Green-for-scope status.

## 1. Exact verdict

**PASS (release-binary evidence only; Full C4 OPEN; C5 OPEN).**

Release `qbind-node` builds; the release helper builds; the harness exits `0`; the helper corpus passes (`total_pass: 76` / `total_fail: 0`); the real-binary scenarios pass (`S1_help=0 S2=1 S3=1 S4=1 S5=1 S6=1`); release artifacts and hashes are captured; the denylist is clean; reachability greps pass; C4/C5 taxonomy greps pass; the Run 295 source tests still pass; the required regression corpus passes; the secret scan is clean; the CodeQL result is reported honestly (section 15). No C4/C5 closure, MainNet readiness, or runtime default-enablement is claimed.

## 2. Files changed

Added:

* `crates/qbind-node/examples/run_296_production_kms_hsm_custody_backend_release_binary_helper.rs` — release helper (76 checks across 6 tables).
* `scripts/devnet/run_296_production_kms_hsm_custody_backend_release_binary.sh` — LF-clean, executable harness.
* `docs/devnet/run_296_production_kms_hsm_custody_backend_release_binary/` — ignored evidence archive tracking only `README.md`, `summary.txt`, `.gitignore`.
* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_296.md` — this canonical report.

Modified (narrow doc updates only):

* `docs/protocol/QBIND_C4_C5_CLOSURE_CRITERIA.md` — KMS/HSM/cloud-KMS/PKCS#11 custody backend row updated to Green-for-scope; status line to Run 296.
* `docs/protocol/QBIND_GOVERNANCE_EXECUTION_RUNTIME_SURFACE_AUDIT.md`
* `docs/ops/QBIND_PQC_TRUST_LIFECYCLE_RUNBOOK.md`
* `docs/protocol/QBIND_PEER_DRIVEN_TRUST_BUNDLE_APPLY_SAFETY.md`
* `docs/protocol/QBIND_TRUST_ANCHOR_AUTHORITY_MODEL.md`
* `docs/whitepaper/contradiction.md`
* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_295.md` — preflight documentation hygiene only: the CodeQL note now records the exact honest result directly (attempted but did not complete; no coverage claimed) instead of deferring to "the final task response". Run 295's verdict is unchanged.

No production library source was changed. The Run 295 backend module is re-run unchanged.

## 3. Release artifacts and hashes

Captured in the tracked `summary.txt` of the evidence directory (regenerated by the harness). Build toolchain: `rustc 1.96.0` / `cargo 1.96.0`. The `summary.txt` records `qbind_node_sha256` for `target/release/qbind-node` and `helper_296_sha256` for the release-built helper binary, plus the `git_commit`, branch, dirty/clean status, and host/kernel summary at capture time (in `provenance.txt`, git-ignored). See `docs/devnet/run_296_production_kms_hsm_custody_backend_release_binary/summary.txt` for the exact SHA-256 values from the capturing run.

## 4. Helper corpus results

Release helper verdict: **PASS**. Tables:

| table | pass | fail |
| --- | --- | --- |
| accepted_compatible | 18 | 0 |
| rejection_fail_closed | 36 | 0 |
| mainnet_authority_policy | 9 | 0 |
| replay_recovery_idempotency | 7 | 0 |
| non_mutation | 5 | 0 |
| reachability | 1 | 0 |
| **total** | **76** | **0** |

The helper constructs the real Run 295 `ProductionKmsHsmCustodyBackend` over source/test fixture KMS/HSM providers, reachable-but-fail-closed production provider stubs, and a programmable mock transport, submits requests, verifies responses, and asserts every failure surfaces as a typed `ProductionCustodyOutcome` rather than a silent fixture/RemoteSigner/local-signing fallback. It emits `fixtures/run_296_deterministic_digests.txt` (request id, request/response envelope digests, response transcript digest, backend transcript digest); the harness runs the helper twice and diffs this fixture to prove deterministic-digest stability (request id `a6e0df48…`, backend transcript digest `260917db…`).

## 5. Real-binary scenarios

* **S1** `--help` — rc=0. No Run 295/296 production KMS/HSM custody **backend** flag / symbol / run marker appears (`production-kms-hsm-custody-backend`, `ProductionKmsHsmCustodyBackend`, `pqc_production_kms_hsm_custody_backend`, `run-296`, `run-295` are all absent). No custody/KMS/HSM enablement, MainNet readiness, or C4/C5 closure claim appears.
* **S2** DevNet `--print-genesis-hash --env devnet` — rc=1 (requires `--genesis-path`); no KMS/HSM custody enablement banner; no mutation.
* **S3** TestNet `--print-genesis-hash --env testnet` — rc=1; silent on backend claims; no mutation.
* **S4** MainNet `--print-genesis-hash --env mainnet` — rc=1; no custody backend enablement banner; no MainNet enablement claim; existing MainNet-required-config fail-closed behavior preserved; no mutation.
* **S5** hidden governance-execution selector `--p2p-trust-bundle-governance-execution-policy fixture-governance-allowed` — parses (no new KMS/HSM custody CLI selector was added).
* **S6** invalid governance-execution selector `bogus-policy` — fails closed (rc≠0) with the `invalid governance-execution policy selector` message, before any mutation (no marker write, no sequence write, no trust swap, no session eviction, no Run 070 call).

No new public CLI surface was added. The denylist (32 patterns) was clean across captured logs and helper output (excluding the help text and helper summary).

## 6. Provider taxonomy / policy release evidence

The release helper exercises the full provider/policy taxonomy in release mode. `ProductionCustodyProviderKind` distinguishes `Disabled`, `FixtureKms`, `FixtureHsm`, `ProductionCloudKms`, `ProductionPkcs11Hsm`, `ProductionGenericKms`, and `ProductionGenericHsm`, with `.is_fixture()` / `.is_production()` / `.to_backend_kind()` partitioning fixture from production providers. `ProductionKmsHsmCustodyBackendPolicy` covers `Disabled` (default), `FixtureKmsAllowed`, `FixtureHsmAllowed`, `ProductionCloudKmsRequired`, `ProductionPkcs11HsmRequired`, `ProductionGenericKmsRequired`, `ProductionGenericHsmRequired`, and `MainnetProductionCustodyRequired`. A fixture provider under a fixture-allowed policy on DevNet/TestNet accepts; a fixture provider under any production policy fails closed; a production provider stub (`ProductionCustodyProviderStub::cloud_kms/pkcs11_hsm/generic_kms/generic_hsm`) is reachable but fails closed as unavailable/misconfigured without real provider config; a RemoteSigner-shaped request is rejected as `RemoteSignerIsNotKmsHsmCustody` (custody is not RemoteSigner). Protocol version is pinned to `PRODUCTION_KMS_HSM_CUSTODY_BACKEND_PROTOCOL_VERSION == 1` and responses over `PRODUCTION_KMS_HSM_CUSTODY_MAX_RESPONSE_BYTES` are rejected.

## 7. Request/response/transcript/domain binding release evidence

The release helper proves, in release mode, that the Run 295 backend derives a deterministic request id via `production_kms_hsm_custody_request_id(&spec)` and a deterministic transcript via `production_kms_hsm_custody_transcript_digest(...)` (both 64-hex SHA3-256), that the response echoes the request id, and that request/response envelope digests (`request.envelope_digest()`, `response.envelope_digest()`, `response.transcript_digest`) bind the candidate digest, provider, key handle, signer, and action. Two identical requests produce identical request/transcript digests. A valid response authorizes exactly the matching request; a wrong domain (environment/chain/genesis/authority-root) fails closed as `ProductionCustodyDomainMismatch`; a tampered transcript digest fails closed as `ProductionCustodyTranscriptMismatch`. The Run 203 pure verifier `verify_authority_custody_backend_response` is referenced directly by the helper and is composed internally by the backend, and an optional Run 291 `durable_replay_record_digest` binds into the request id and backend transcript.

## 8. Accepted release evidence

Under an explicit `FixtureKmsAllowed` / `FixtureHsmAllowed` policy on a DevNet/TestNet trust domain, the source/test fixture KMS/HSM providers accept a valid authority-lifecycle signing request; the accept path reaches the transport exactly once (`transport.call_count() == 1`); acceptance carries only provider id / environment / request id / backend transcript (evidence-only, non-mutating, `outcome.is_accept()` with `outcome.is_non_mutating()`). The `Disabled` default policy is explicit and inert (`DisabledNoRequest`, zero transport calls). `build_custody_request` builds the request without invoking the transport. Protocol version is pinned to `1`.

## 9. Rejection / fail-closed release evidence

All 36 rejection checks pass in release mode: default `Disabled` fails closed before any transport call; MainNet identity refused before transport; fixture material refused for MainNet (`FixtureMaterialRejectedForMainNet`); wrong domain / provider / key-handle / signer / candidate-digest / action / request-id-echo / transcript rejected; missing/malformed/oversized response rejected (`ProductionCustodyMalformedResponse`, `ProductionCustodyDecodeFailed`); provider unavailable / misconfigured / signing-refused / attestation-missing / attestation-unavailable rejected; transport endpoint unavailable / connection refused / timeout / decode-error / unsupported-protocol / unsupported-provider rejected; unsupported request kinds (validator-set rotation → `ValidatorSetRotationUnsupported`, policy change → `PolicyChangeUnsupported`, governance verifier → `GovernanceVerifierUnavailable`) refused; a RemoteSigner-shaped request rejected as `RemoteSignerIsNotKmsHsmCustody`; malformed spec fails closed as `AmbiguousFailClosed`; a production-mode response is never accepted. Every error surfaces as a typed `ProductionCustodyError` with `.tag()` and `.is_retryable()`.

## 10. MainNet refusal / authority policy release evidence

MainNet cannot be satisfied by local operator material, fixture KMS/HSM providers, RemoteSigner, or peer majority: every non-production MainNet path is `MainNetRefused` or `FixtureMaterialRejectedForMainNet` before any accept. `MainnetProductionCustodyRequired` on MainNet returns the typed `MainNetProductionCustodyUnavailable` (fail-closed unavailable) — MainNet is refused absent production authority criteria and custody attestation. A non-MainNet production-required policy on DevNet/TestNet returns a typed unavailable/misconfigured outcome with no accept and never falls back to the fixture KMS/HSM providers. A production policy never yields an accept in release mode. MainNet peer-driven apply remains refused (there is no peer-majority path into the backend at all).

## 11. Replay / recovery / idempotency release evidence

`recover_custody_request_window(None, current)` is a clean `NoPriorRequest` no-op. Identical requests are idempotent over the deterministic request id (`IdempotentReplayOfSameRequest`, `.is_idempotent()`). The same request id with a different request transcript fails closed as `ConflictingRequestForSameId`; the same request with a different response commitment fails closed as `ConflictingResponseForSameRequest`; the same request id with a different key handle fails closed as `ConflictingKeyHandleForSameId`. Retry-then-success is idempotent across attempts (accept after a retryable timeout); the retry budget is bounded (`attempts_used`); retry exhaustion fails closed as a typed timeout; terminal/non-retryable errors are not retried. Ambiguous recovery fails closed as `AmbiguousRecoveryFailClosed`. Accepted responses are evidence-only and authorize nothing beyond the custody boundary.

## 12. Production-binary default-disabled/silent evidence

The default `ProductionKmsHsmCustodyBackendPolicy` is `Disabled` and fails closed before any provider/transport call. The real `target/release/qbind-node` binary exposes **no** Run 295/296 production KMS/HSM custody backend CLI surface: `--help` and the default `--print-genesis-hash` surfaces are silent on backend enablement claims, confirming the backend is not constructed or wired into the default runtime and that no new CLI flag was added.

## 13. C4/C5 matrix taxonomy verification

The harness greps the C4/C5 closure-criteria matrix and confirms the taxonomy clarification remains present: it separates **boundary readiness** from **production readiness**. The production durable replay RocksDB backend row remains Green **for release-binary-evidenced scope only** (Run 292) and the Real production RemoteSigner backend row remains Green **for release-binary-evidenced RemoteSigner backend behavior only** (Run 294). The Real KMS/HSM/cloud-KMS/PKCS#11 custody backend row is now Green **for release-binary-evidenced KMS/HSM custody backend behavior only** — not wired by default, no public CLI flag, MainNet refused absent production authority criteria and custody attestation, production cloud-KMS / PKCS#11 / generic provider paths fail-closed without real config, no custody-attestation-verifier / on-chain-verifier / governance-engine / validator-set-rotation implementation, does not close C4/C5. Custody attestation verifier, on-chain governance proof verifier, governance execution engine, validator-set rotation, MainNet authority rotation/revocation under production custody, production signing audit trail / crypto-agility / incident response, and full MainNet release-binary evidence under production custody all remain **Red**. Run 296 does not reinterpret the matrix clarification as C4/C5 closure.

## 14. Regression results

The harness ran the required regression corpus: `run_295` (production KMS/HSM custody backend source tests), `run_293` (production RemoteSigner backend), `run_291` (durable replay RocksDB), the durable-completion / settlement / external-publication corpus (`run_290` … `run_224`), the Run 203 KMS/HSM boundary tests (`run_203_kms_hsm_backend_boundary_tests`), the Run 201 transport boundary tests (`run_201_remote_signer_transport_boundary_tests`), the Run 194 RemoteSigner boundary tests (`run_194_remote_authority_signer_boundary_tests`), the Run 188 authority custody boundary tests (`run_188_authority_custody_boundary_tests`), plus `--lib pqc_authority` and `--lib`. The actual Run 203 test target name differs from the task's suggested `run_203_kms_hsm_backend_tests` and the real name was used: `run_203_kms_hsm_backend_boundary_tests` (substitution recorded here). All targets `rc=0`. Exact per-target results are captured in the tracked `summary.txt`.

## 15. Security scan results

* **Secret scanning** — run over all changed files and evidence scripts; **no secrets** found. The backend derives ids/transcripts by SHA3-256 over public spec fields and never embeds keys.
* **CodeQL** — Run 296 is release-binary evidence for a real KMS/HSM custody backend and is not classified as trivial. The CodeQL checker was invoked (language: rust) but **did not complete**: the analysis timed out / was skipped because the database size is too large. **No CodeQL coverage is claimed** for this pass. No production library surface was changed by this pass (the change set is a release-mode example helper, a harness shell script, and Markdown/evidence documentation), and the underlying Run 295 production module is re-run unchanged. The exact honest CodeQL limitation is recorded here directly.

## 16. Honest limitations

Run 296 is release-binary evidence only. It does not enable any production mutating behavior, does not wire the KMS/HSM custody backend into the default runtime, and does not implement real provider credentials, real cloud-KMS accounts, real PKCS#11 sessions, real HSM module loading, a custody attestation verifier, an on-chain governance proof verifier, a governance execution engine, validator-set rotation, settlement, or external publication. The fixture providers, production provider stubs, and mock transport are release-mode evidence scaffolding only; under a production policy there is no fixture/RemoteSigner/local-key fallback and the production provider paths fail closed without real config. Run 296 closes only the Run 295 release-binary evidence gap and does not weaken the Run 292 durable replay RocksDB or Run 294 RemoteSigner Green-for-scope statuses.

## 17. C4/C5 status

Full **C4 remains OPEN**. **C5 remains OPEN**. Run 296 makes no closure claim for either.

## 18. Suggested Run 297 next step

Run 297 should begin the next Red-row closure campaign: source/test **production custody attestation verifier** implementation. After Run 296 the KMS/HSM custody backend is Green-for-scope, but C5 still requires production custody *with attestation*, so the next highest-value Red row is a real custody attestation verifier that validates provider attestation evidence (`ProductionCustodyAttestationMissing` / `…AttestationUnavailable` are already typed refusal outcomes) and rejects unverified custody material. Keep the same pattern — source/test only, default `Disabled`/fail-closed, MainNet refused unless production authority criteria are satisfied, with release-binary evidence deferred to Run 298.
