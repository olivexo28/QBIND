Run 164: release-binary EVIDENCE / BOUNDARY for the Run 163 typed pure
governance ratification authority verifier
=======================================================================

Verdict: partial-positive: release-binary fixture/evidence boundary
captured; governance authority verifier not yet production-surface
reachable.

Run 164 produces the strongest honest release-binary evidence currently
possible for the Run 163 governance authority verifier
(`qbind_node::pqc_governance_authority::verify_governance_authority_proof`,
`validate_lifecycle_with_governance_authority`).

Run 164 first determines that the Run 163 verifier has NO production
caller in `crates/qbind-node/src/` outside its own module
(`crates/qbind-node/src/pqc_governance_authority.rs`) and the
`pub mod pqc_governance_authority;` declaration in
`crates/qbind-node/src/lib.rs`. None of the eight production
release-binary v2 surfaces enumerated by `task/RUN_164_TASK.txt`
(startup `--p2p-trust-bundle` v2, reload-check validation-only, local
peer-candidate-check validation-only, process-start reload-apply,
SIGHUP live-reload, live inbound `0x05` validation-only, peer-driven
staged queue / drain-once, lifecycle marker-decision from Run 161/162)
calls the Run 163 verifier today. The harness records this as a
partial-positive boundary and identifies Run 165 as the exact next
required integration run.

Harness: scripts/devnet/run_164_governance_authority_release_binary.sh

Surfaces investigated:
  1. startup `--p2p-trust-bundle` v2 path                  — NOT reachable
  2. reload-check validation-only path                     — NOT reachable
  3. local peer-candidate-check validation-only path       — NOT reachable
  4. process-start reload-apply path                       — NOT reachable
  5. SIGHUP live-reload path                               — NOT reachable
  6. live inbound `0x05` validation-only path             — NOT reachable
  7. peer-driven staged queue / drain-once path            — NOT reachable
  8. lifecycle marker-decision path from Run 161 / 162     — NOT reachable
  9. fixture helper / example binary path                  — REACHABLE
     (target/release/examples/run_164_governance_authority_fixture_helper)

Captured release-binary evidence:
  * release-built target/release/qbind-node identity in provenance.txt
    (sha256 + ELF Build ID).
  * release-built target/release/examples/run_164_governance_authority_fixture_helper
    identity in provenance.txt.
  * source-level reachability proof (reachability/src_grep.txt and
    reachability/reachability.txt) confirming zero production callers.
  * release-built helper actually invokes verify_governance_authority_proof
    and validate_lifecycle_with_governance_authority on every accept and
    reject scenario; per-scenario actual typed outcome is asserted to
    contain the expected typed-outcome class string. PASS=21, FAIL=0.

Acceptance scenarios exercised:
  A1  GenesisBound Rotate accepted                          (AcceptedGenesisBound)
  A2  GenesisBound Revoke accepted                          (AcceptedGenesisBound)
  A3  GenesisBound EmergencyRevoke accepted                 (AcceptedGenesisBound)
  A4  EmergencyCouncil EmergencyRevoke accepted             (AcceptedEmergencyCouncil)
  A5  Idempotent same proof / same candidate                (AcceptedGenesisBound;
       lifecycle layer reports Idempotent { sequence: 2 } in
       combined_outcomes.txt)

Rejection scenarios exercised:
  R1  wrong environment                                     (WrongEnvironment)
  R2  wrong chain                                           (WrongChain)
  R3  wrong genesis                                         (WrongGenesis)
  R4  wrong authority root                                  (WrongAuthorityRoot)
  R5  wrong lifecycle action                                (WrongLifecycleAction)
  R6  wrong candidate digest                                (WrongCandidateDigest)
  R7  wrong authority-domain sequence                       (WrongAuthoritySequence)
  R8  invalid issuer signature                              (InvalidIssuerSignature)
  R9  unsupported issuer suite                              (UnsupportedIssuerSuite)
  R10 non-PQC issuer suite (Ed25519)                        (NonPqcSuiteRejected)
  R11 threshold not met                                     (ThresholdNotMet)
  R12 malformed proof (empty signature)                     (MalformedProof)
  R13 stale lower-sequence replay                           (ReplayRejected)
  R14 OnChainGovernance unsupported                         (UnsupportedOnChainGovernance)
  R15 local operator config alone rejected                  (MalformedProof; the typed
       LocalOperatorConfigOnlyRejected enum variant is exercised by Run
       163 R15 source/test coverage)
  R16 peer-majority rejected                                (MalformedProof; the typed
       PeerMajorityProofRejected enum variant is exercised by Run 163
       R16 source/test coverage; the GovernanceAuthorityClass enum has
       no peer-majority variant by construction)

Negative invariants:
  * MainNet peer-driven apply remains refused unconditionally.
  * harness does not start qbind-node, write a sequence file, write an
    authority marker, mutate any live trust state, open a p2p socket,
    or enable MainNet apply on any surface.
  * out-of-scope denylist grep clean post banner-exclusion.
  * no fallback to --p2p-trusted-root.
  * no active DummySig / DummyKem / DummyAead.

Regression suites (all green on this checkout):
  * cargo test -p qbind-node --test run_163_governance_authority_verifier_tests
  * cargo test -p qbind-node --test run_161_lifecycle_marker_integration_tests
  * cargo test -p qbind-node --test run_159_authority_signing_key_lifecycle_tests
  * cargo test -p qbind-node --test run_157_unified_testnet_fixture_universe_tests
  * cargo test -p qbind-node --test run_152_binary_reachable_peer_drain_plumbing_tests
  * cargo test -p qbind-node --test run_150_peer_driven_apply_drain_tests
  * cargo test -p qbind-node --test run_148_peer_driven_apply_devnet_tests
  * cargo test -p qbind-node --test run_142_live_inbound_0x05_v2_validation_tests
  * cargo test -p qbind-node --test run_134_reload_apply_v2_authority_marker_tests
  * cargo test -p qbind-node --test run_138_sighup_v2_authority_marker_tests
  * cargo test -p qbind-node --lib pqc_authority

Out-of-scope explicitly NOT done (and NOT claimed):
  * MainNet enablement.
  * autonomous peer-driven apply.
  * automatic apply on receipt.
  * peer-majority authority.
  * governance execution engine.
  * on-chain governance integration.
  * KMS / HSM custody implementation.
  * validator-set rotation.
  * production wiring of the Run 163 verifier into any mutating apply
    or validation-only marker-decision surface (this is Run 165).
  * any wire-format / marker-schema / sequence-file-schema /
    trust-bundle-schema / peer-candidate-envelope-schema change.

Next required integration run: Run 165 — compose
verify_governance_authority_proof and
validate_lifecycle_with_governance_authority into
qbind_node::pqc_authority_marker_acceptance::decide_marker_acceptance_v2
(or an immediately-upstream typed pre-flight gate). Run 166 will then
be the partner release-binary ENFORCEMENT evidence run for Run 165.

Run 164 does NOT claim strongest-positive. Run 164 does NOT claim full
C4 closure. Run 164 does NOT claim C5 closure.