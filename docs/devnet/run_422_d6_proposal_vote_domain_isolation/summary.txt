RUN 422 D6 — SUMMARY AND VERDICT
============================================================================

RESULT=POSITIVE-FOR-PROPOSAL-VOTE-DOMAIN-ISOLATION-CODE-TEST
D6_STATUS=VERSIONED-PROPOSAL-VOTE-BOUNDARY-CODE-TEST-POSITIVE
D7_STATUS=UNRESOLVED
GENESIS_AUTHORITY_ACTIVATION=DISABLED
PROPOSAL_VOTE_AUTHORITY_FROM_LEGACY_CLI=DISALLOWED
LEGACY_TIMEOUT_CONTEXT=EXISTING-POLICY-PRESERVED
CONFIGURED_AUTHORITY_RELEASE_BINARY_EVIDENCE=NOT-YET-CAPTURED
SECURITY_POSTURE=RS1-OPEN / PUBLIC-DEVNET-NO-GO

============================================================================
WHAT WAS DELIVERED
============================================================================

An explicitly versioned Proposal/Vote signing domain (v2) that cryptographically
binds a consensus signature to:
  * an explicit signing-format version,
  * the message family (Proposal / Vote),
  * the full 64-bit runtime ChainId,
  * the accepted canonical genesis identity,
  * the consensus authority commitment (intended membership/key/suite snapshot),
  * all currently signed, security-relevant message fields (exact v1 body).

Implementation:
  * crates/qbind-wire/src/pv_signing_domain.rs — ProposalVoteSigningDomainV2
    (validated, immutable), format/family enums, typed error, preimage builders.
  * crates/qbind-wire/src/consensus.rs — canonical_body() for Vote/BlockProposal;
    v1 signing_preimage_with_chain_id refactored to reuse it (bytes identical).
  * crates/qbind-consensus/src/proposal_vote_verify.rs — fail-closed public
    message-bound entrypoints verify_proposal_msg_with_domain /
    verify_vote_msg_with_domain (they recompute the canonical preimage from the
    actual message + trusted domain). The raw-preimage helpers
    verify_proposal_msg_with_preimage / verify_vote_msg_with_preimage are
    PRIVATE and are not a caller entrypoint.
  * crates/qbind-node/src/binary_consensus_loop.rs — MANDATORY signing_domain on
    ProposalVoteAuthority (a plain ProposalVoteSigningDomainV2, not Option; there
    is no missing-domain -> legacy-v1 selection); typed inbound WireChainMismatch
    before crypto and outbound wire-chain refusal before signing; F6 before
    domain/crypto; in-module run422_d6 handler tests. Production stays None.

============================================================================
EVIDENCE
============================================================================

Crypto matrix (real ML-DSA-44): 34/34 pass — see replay_matrix.txt and
test_results.txt. Includes the Vote no-backend UnsupportedSuite counterpart
(case_i_unsupported_suite_no_backend_rejected_vote), distinct from the
registered-but-faulting BackendError case.
Node handler tests (real F6 gate + Required policy): 10/10 pass. Includes the
independent outbound Vote wire-chain refusal test through forward_actions_to_facade
(run422_d6_outbound_vote_wire_chain_refusal_through_forward_actions).
Regressions green: qbind-wire lib (9, all pv_signing_domain) + qbind-consensus
lib (182) + integration/doctests, qbind-node --lib (1478), run_420 (3). Run_422
genesis (15) / startup-refusal (4) / D4 ordering (5) and run_418 sender-binding
(18) / newview (3) preserved from the corrective revision (sources unchanged).
See test_results.txt.
cargo check default features and cargo build --release: PRESERVED historical
evidence (exit 0), production/build sources unchanged this pass — see commands.txt.
Focused clippy on changed targets: newly executed, exit 0 — see commands.txt.
Added test blocks rustfmt-clean; the crypto test file is CRLF (LF->CRLF
normalized by the corrective continuation, not unchanged from the original LF D6
file); CRLF source files preserved as CRLF.

============================================================================
BOUNDARIES / BLOCKERS (NOT overclaimed)
============================================================================

  * Production proposal_vote_authority remains None; genesis-authority startup
    refusal preserved; no activation flag/env/override added.
  * Legacy Timeout/NewView context unchanged; cannot establish PV authority.
  * runtime ChainId (u64) -> wire chain_id (u32) mapping UNRESOLVED;
    expected_wire_chain_id supplied from trusted config only (fixtures/tests),
    never from messages. Blocks safe production construction.
  * Downstream engine/QC still reconstruct the legacy preimage; v2 boundary
    success is NOT QC/engine validation.
  * D7 (authority freshness/lifetime) UNRESOLVED.
  * CodeQL: reported separately; not converted into a zero-alert conclusion.
    The previously recorded broad-Clippy failure and CodeQL DB-too-large skip
    from D4/D5 are preserved as historical records.

============================================================================
PRESERVED POSTURE
============================================================================

F3/F4/F8 not fully activated; D7 unresolved; F1/F2/F5/F7 unresolved; F6 partial;
RS1 and C4/C5 OPEN; M4/M6/S5/S7 Yellow; Public DevNet NO-GO. No live seed file
or TestNet/MainNet readiness claim.

============================================================================
SMALLEST D7 FOLLOW-UP (NOT implemented here)
============================================================================

Bind an authority-freshness proof (accepted epoch / activation authorization)
to the signing domain so that a commitment for a superseded snapshot is rejected
at admission — plus a validated runtime-ChainId -> wire-chain_id mapping from
trusted genesis, without which production construction stays unavailable.
Do not start Run 423.