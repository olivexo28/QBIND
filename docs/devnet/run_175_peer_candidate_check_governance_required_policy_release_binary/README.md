# Run 175 — Release-Binary Local Peer-Candidate-Check Required-Policy Governance-Proof Evidence

This directory is the evidence archive for **Run 175**: release-binary
**local peer-candidate-check validation-only** production-surface
evidence that real `target/release/qbind-node` enforces
`GovernanceProofPolicy::RequiredForLifecycleSensitive` via the hidden
Run 171 selector
(`--p2p-trust-bundle-governance-proof-required` /
`QBIND_P2P_TRUST_BUNDLE_GOVERNANCE_PROOF_REQUIRED=1`, truthy
`1|true|yes|on`) on the local
`--p2p-trust-bundle-peer-candidate-check` validation-only v2
marker-decision surface (Run 077 / Run 107 envelope path → Run 132
preflight).

Run 175 closes the Run 174-deferred peer-candidate-check
release-binary cases (A4 / A5 / R15 / R16 in Run 174 numbering;
restated as A1..A5 / R1..R18 in Run 175 numbering). Run 173 wired the
validation-only side at source/test level
(`preflight_run_132_validation_only_v2_marker_check` resolves the
active policy via `governance_proof_policy_from_cli_or_env` and routes
through the validation-only surface shim
`preflight_v2_validation_only_marker_check_with_governance_proof_load`,
which delegates to the existing Run 169 mutating shim — there is **no
second selector path and no second gate path**); Run 174 evidenced the
`--p2p-trust-bundle-reload-check` validation-only call site at
release-binary level; Run 175 evidences the local
`--p2p-trust-bundle-peer-candidate-check` validation-only call site at
release-binary level.

**Tracked files in this directory (only):**

* `README.md` — this file.
* `summary.txt` — verdict + per-scenario exit codes (regenerated by
  the harness; the committed copy reflects the Run 175 closure run).
* `.gitignore` — declares the per-run generated artifacts that are
  intentionally NOT committed back to the repository.

**Per-run generated artifacts (NOT committed; declared in
`.gitignore`):**

* `logs/` — per-scenario stdout/stderr from real
  `target/release/qbind-node`.
* `data/` — per-scenario data-dirs (the `consensus/` RocksDB sub-dir
  and `run077-peer-candidate-scratch/` are expected and benign;
  validation-only invariants assert no `pqc_authority_state.json` /
  `pqc_trust_bundle_sequence.json` write).
* `fixtures/` — the Run 175 helper-minted ratification corpus and
  local PeerCandidateEnvelope JSONs (DevNet + MainNet).
* `exit_codes/` — per-scenario `rc` files.
* `marker_hashes/` / `sequence_hashes/` — pre/post SHA-256 for marker
  and sequence files (validation-only invariant: pre == post).
* `data_inventories/` — per-scenario data-dir inventory snapshots.
* `grep_summaries/` — denylist + CLI-hidden checks.
* `reachability/` — source-reachability greps for the Run 169 / Run
  171 / Run 173 wiring symbols and the local peer-candidate-check
  CLI declaration.
* `test_results/` — release-mode `cargo test -p qbind-node` results
  for the cross-check suites
  (Run 173 / 171 / 169 / 167 / 165 / `--lib pqc_authority`).
* `provenance.txt` — qbind-node + helper SHA-256 / ELF Build ID, git
  commit, rustc/cargo versions, run timestamp.
* `fixture_manifest.txt` — SHA-256 + size of every minted fixture.
* `scenario_assertions.txt` — machine-grep-friendly scenario verdict
  strings.
* `negative_invariants.txt` — negative invariants proven by the
  harness.

**Regeneration:**

```bash
bash scripts/devnet/run_175_peer_candidate_check_governance_required_policy_release_binary.sh
```

**Related canonical evidence:** see
`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_175.md`.

**Honest limitations preserved (NOT closed by Run 175):**

* Live inbound `0x05` peer-candidate envelopes still do NOT carry a
  `governance_authority_proof` sibling; the live `0x05` validation
  surface (`pqc_peer_candidate_wire`) still cannot supply a typed
  `GovernanceProofLoadStatus`. Run 175 covers only the LOCAL
  `--p2p-trust-bundle-peer-candidate-check` binary surface (Run 077 /
  Run 107), where the proof-carrying ratification is supplied
  separately via `--p2p-trust-bundle-ratification` and parsed by the
  production loader
  `load_versioned_ratification_with_governance_proof_from_path`. Live
  `0x05` proof-carrying schema work is **forbidden by task scope** and
  remains OPEN.
* MainNet peer-driven apply remains refused (Run 147 FATAL).
* `OnChainGovernance` remains unsupported / fail-closed at the Run
  163 verifier.
* Governance execution / on-chain governance / KMS / HSM /
  validator-set rotation remain unimplemented.
* Full C4 closure is NOT claimed by Run 175.
* C5 remains OPEN.
* R5 / R6 / R7 (wrong-environment / wrong-chain / wrong-genesis
  proof-carrying Rotate sidecars) cannot be expressed as bit-for-bit
  static fixtures consumable by the binary without changing the
  production environment / chain / genesis the binary is invoked
  with (the Run 130 verifier trips upstream of the governance gate).
  Covered at source level by the Run 173 source-test integration
  suite and at symbol level by the Run 168 release-built helper.
  Mirrors Run 174 precedent.