# Run 149 peer-driven apply release-binary archive

This directory is populated by:

```bash
scripts/devnet/run_149_peer_driven_apply_release_binary.sh
```

The checked-in README documents the archive contract. Generated evidence files include binary identities, command lines, hidden-flag proof, MainNet refusal proof, and scenario verdicts.
