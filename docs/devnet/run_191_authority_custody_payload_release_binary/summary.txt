Run 191 — release-binary authority-custody metadata carrying evidence
(placeholder summary; the harness rewrites this file on every run.)

The canonical PASS/FAIL verdict is produced by:
    bash scripts/devnet/run_191_authority_custody_payload_release_binary.sh

Tracked-in-git files in this archive:
  README.md     — Run 191 scope, strict no-production-source-change
                  contract, layout, reproducibility, and honest
                  limitations.
  summary.txt   — this placeholder; the harness replaces it with the
                  canonical Run 191 verdict line referenced by
                  docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_191.md.
  .gitignore    — declares per-run gitignored generated subtrees
                  (logs/, exit_codes/, helper_evidence/, scenarios/,
                  reachability/, test_results/, fixtures/,
                  provenance.txt, …).

Honest limitations:
  * default AuthorityCustodyPolicy::Disabled fail-closed on every surface
  * no real KMS / HSM / cloud-KMS / PKCS#11 / remote-signer backend wired
  * RemoteSigner / Kms / Hsm placeholders fail closed at the Run 188 validator
  * fixture / local-operator custody remains DevNet/TestNet evidence-only
  * fixture / local-operator custody refused on MainNet ahead of policy gate
  * legacy / no-custody payloads remain compatible under default Disabled
  * existing Run 184 / Run 185 / Run 187 governance fixture proof paths compatible
  * MainNet peer-driven apply remains refused regardless of custody payload
  * no schema/wire/metric drift beyond Run 190 additive optional custody sibling
  * no marker write; no sequence write; no live trust swap; no Run 070 call
  * full C4 remains open; C5 remains open

verdict: placeholder (regenerated by harness)