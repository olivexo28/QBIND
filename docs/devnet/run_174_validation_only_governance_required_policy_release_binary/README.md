# Run 174 — Release-Binary VALIDATION-ONLY Required-Policy Governance-Proof Evidence

This directory is the evidence archive for **Run 174**: release-binary
**validation-only** production-surface evidence that real
`target/release/qbind-node` enforces
`GovernanceProofPolicy::RequiredForLifecycleSensitive` via the hidden
Run 171 selector
(`--p2p-trust-bundle-governance-proof-required` /
`QBIND_P2P_TRUST_BUNDLE_GOVERNANCE_PROOF_REQUIRED=1`, truthy
`1|true|yes|on`) on the validation-only v2 marker-decision surfaces.

Run 174 closes the validation-only side of the Run 172 honest
limitation: Run 172 evidenced Required-policy on the **mutating**
preflight surfaces (process-start `--p2p-trust-bundle-reload-apply-path`,
startup `--p2p-trust-bundle`, SIGHUP, peer-driven `ProductionV2MarkerCoordinator`)
on real `target/release/qbind-node`; Run 173 wired the validation-only
side at source/test level (`preflight_run_132_validation_only_v2_marker_check`
now resolves the active policy via `governance_proof_policy_from_cli_or_env`
and routes through the new validation-only surface shim
`preflight_v2_validation_only_marker_check_with_governance_proof_load`,
which delegates to the existing Run 169 mutating shim — there is **no
second selector path and no second gate path**); Run 174 is the
release-binary evidence run for that wiring.

**Tracked files in this directory (only):**

* `README.md` — this file.
* `summary.txt` — verdict + per-scenario exit codes (regenerated by the
  harness; the committed copy reflects the Run 174 closure run).
* `.gitignore` — declares the per-run generated artifacts that are
  produced by the harness but not committed back (logs, data-dirs,
  fixtures, hashes, inventories, grep summaries, test logs, provenance
  metadata, manifests). These contain absolute paths and timing-
  dependent / ephemeral data.

**Regenerate everything with:**

```sh
bash scripts/devnet/run_174_validation_only_governance_required_policy_release_binary.sh
```

The harness writes outputs under this directory by default
(`OUTDIR=docs/devnet/run_174_validation_only_governance_required_policy_release_binary`).

## What this run proves on the real release binary

Real `target/release/qbind-node` is exercised with the release-built
ratification corpus minted by the existing Run 172 fixture helper
(`crates/qbind-node/examples/run_172_governance_required_policy_release_binary_helper.rs`).
Run 174 introduces no new fixture helper; the proof-carrying / no-proof
/ malformed / wrong-binding / OnChainGovernance corpus is surface-
agnostic (validation-only and mutating consume the same v2 sidecar
bytes). The harness covers:

* **A1** — default `NotRequired` compatibility on validation-only
  `--p2p-trust-bundle-reload-check` (no selector, no env, no proof).
* **A2** — CLI selector `--p2p-trust-bundle-governance-proof-required`
  on validation-only reload-check accepts a valid proof-carrying
  GenesisBound `Rotate` sidecar; `[run-132] reload-check v2 authority-
  marker check passed ... governance policy=RequiredForLifecycleSensitive`
  is logged; no marker write, no sequence write, no Run 070 apply.
* **A3** — env selector `QBIND_P2P_TRUST_BUNDLE_GOVERNANCE_PROOF_REQUIRED=1`
  on validation-only reload-check mirrors A2.
* **A6a / A6b** — env explicitly `false` and `0` preserve `NotRequired`
  (no-proof Rotate sidecar accepted on validation-only reload-check).
* **R1 / R2** — Required + no-proof Rotate sidecar refused on validation-
  only reload-check with the typed
  `Run 165: v2 authority-marker decision requires a governance authority
  proof for lifecycle action 'rotate' but none was available`
  (`MutatingSurfaceMarkerV2Error::GovernanceAuthorityRequiredButMissing { action: Rotate }`)
  under both CLI and env selectors; no Run 070 apply, no marker write,
  no sequence write, no live trust mutation, no session eviction.
* **R3** — Required + malformed proof refused (loader maps `Malformed`
  to `Unavailable`; under Required the gate fails closed for `Rotate`).
* **R4** — Required + invalid issuer signature refused (Run 163 verifier
  `InvalidIssuerSignature` surfaces as `GovernanceAuthorityRejected`).
* **R8 / R9 / R10 / R11** — Required + wrong-root / wrong-action /
  wrong-digest / wrong-sequence proof-carrying sidecars all refused on
  validation-only reload-check.
* **R12** — Required + `OnChainGovernance` class proof refused as
  unsupported / fail-closed (Run 163 verifier).
* **R-extra** — Required + unsupported issuer suite refused.
* **R19** — selector cannot be implicitly enabled by unrelated flags
  (no-proof Rotate sidecar without selector accepted under default
  `NotRequired`).
* **R20** — MainNet peer-driven apply refusal (Run 147 FATAL invariant)
  is preserved even with `--p2p-trust-bundle-governance-proof-required`
  + `--p2p-trust-bundle-peer-candidate-staging-enabled` + a valid
  proof-carrying Rotate sidecar.

The harness additionally:

* runs **`assert_no_mutation`** on every validation-only scenario
  (accept and reject), enforcing `R17` (no marker write, no sequence
  write) and `R18` (no Run 070 apply, no `[run-134] reload-apply v2
  ratification path SELECTED`, no `[run-134] v2 authority-marker
  persisted`, no live trust mutation, no session eviction);
* re-runs `cargo test --release -p qbind-node` for
  `run_173_validation_only_governance_required_policy_tests`,
  `run_171_governance_required_policy_selector_tests`,
  `run_169_governance_proof_loader_surface_integration_tests`,
  `run_167_governance_proof_carrier_tests`,
  `run_165_governance_marker_integration_tests`, and
  `--lib pqc_authority`;
* captures source-reachability greps for
  `preflight_v2_validation_only_marker_check_with_governance_proof_load`
  (the Run 173 shim), `preflight_run_132_validation_only_v2_marker_check`
  (the validation-only entry point in `main.rs`),
  `governance_proof_policy_from_cli_or_env`,
  `GovernanceProofPolicy::RequiredForLifecycleSensitive`,
  `load_versioned_ratification_with_governance_proof_from_path`, and
  `GovernanceProofContext::Available`;
* enforces denylist greps over all logs (no MainNet apply, no
  autonomous apply, no apply on receipt, no peer-majority authority,
  no fallback to `--p2p-trusted-root`, no DummySig/DummyKem/DummyAead,
  no marker-before-sequence, no validation-only marker write, no
  validation-only sequence write, no validation-only Run 070 apply,
  no `[run-134]` mutating apply path SELECTED line on any validation-
  only run).

## Honest limitations (preserved, NOT a Run 174 closure)

1. **Live inbound `0x05` proof-carrying remains OPEN.** The on-the-wire
   peer-candidate envelope schema (`crates/qbind-node/src/pqc_peer_candidate_wire.rs`)
   does **not** carry a `governance_authority_proof` sibling, so the
   live `0x05` validation surface cannot yet supply a typed
   `GovernanceProofLoadStatus`. Lifting live `0x05` to Required policy
   would require a peer-candidate envelope schema change, which is
   explicitly forbidden by `task/RUN_174_TASK.txt`. This boundary is
   documented and deferred without weakening any prior invariant.

2. **Local `--p2p-trust-bundle-peer-candidate-check` release-binary
   coverage is deferred (A4 / A5 / R15 / R16).** The Run 172 fixture
   helper does not mint a peer-candidate envelope; minting one is
   fixture-tooling work outside Run 174 scope. The validation-only
   peer-candidate-check surface shares
   `preflight_run_132_validation_only_v2_marker_check` with reload-
   check by construction (Run 173 wiring), so policy resolution and
   the gate composition are identical. The Run 173 source-test
   integration suite covers both call sites at source level.

3. **`R5` (wrong-environment), `R6` (wrong-chain), `R7` (wrong-genesis)
   proof-carrying Rotate sidecars** cannot be expressed as bit-for-bit
   static fixtures consumable by the binary without changing the
   production environment / chain / genesis the binary is invoked with
   (which would itself trip the Run 130 verifier upstream of the
   governance gate). These three scenarios are covered at source level
   by the Run 173 source-test integration suite (R5–R7) and at symbol
   level by the Run 168 release-built helper, mirroring the deferral
   pattern Run 172 used for `A9` (idempotent) and `R21` (lifecycle-
   invalid + proof-valid).

## Inheritance

This run does **not** weaken any prior invariant. It does **not** claim
governance execution / on-chain governance / KMS-HSM / validator-set
rotation. It does **not** enable MainNet peer-driven apply.
`OnChainGovernance` remains unsupported / fail-closed. Full C4 and C5
remain open per task scope.

See also:

* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_174.md` — canonical evidence
  report for Run 174.
* `task/RUN_174_TASK.txt` — original task specification.
* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_172.md` — Run 172 release-
  binary mutating-surface Required-policy evidence (predecessor).
* `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_173.md` — Run 173 source/test
  validation-only Required-policy wiring (predecessor).