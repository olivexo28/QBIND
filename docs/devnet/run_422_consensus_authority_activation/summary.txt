RUN 422 — SUMMARY

============================================================================
CONTAINMENT CORRECTION (supersedes the reachable-activation state below)
============================================================================
RESULT=PARTIAL-FOR-DEVNET-CONSENSUS-AUTHORITY-ACTIVATION-CODE-TEST
GENESIS_AUTHORITY_ACTIVATION=DISABLED-PENDING-D4-D7
LEGACY_CLI_CONTEXT_ACTIVATION=UNCHANGED-AND-STILL-REACHABLE
CONFIGURED_AUTHORITY_RELEASE_BINARY_EVIDENCE=NOT-YET-CAPTURED
SECURITY_POSTURE=RS1-OPEN / PUBLIC-DEVNET-NO-GO

The prior corrective continuation left the genesis-authority route reachable:
valid genesis + matching signer + `--consensus-authority-from-genesis` still
built an active context and reached the consensus loop. This containment pass
disables the NEW genesis-authority route entirely. A single early startup guard
in `main` (immediately before `match config.network_mode`) refuses the flag with
exit code 1 and the diagnostic "genesis-authority activation is disabled pending
D4-D7", AFTER Run 102 boot verification but BEFORE any P2P service construction
and BEFORE any consensus task, uniformly across every network mode and
environment, with no override/hidden-flag/environment-bypass/unsigned-fallback
and no silent continuation with None.

  D4 P2P service starts before authority validation  CONTAINED (route disabled;
       guard exits before run_p2p_node; reorder NOT attempted) — UNRESOLVED
  D5 shared Proposal/Vote vs Timeout/NewView context  UNRESOLVED (legacy CLI
       route still builds the shared context; no blanket "flag off => None")
  D6 signed-domain / replay isolation  ... UNRESOLVED
  D7 genesis-static lifetime  ............ UNRESOLVED

The corrected authority loader (D1 single-snapshot provenance, D2
membership-count reject) is retained and still tested, but no longer reachable
in a normally built binary. The legacy `--validator-consensus-key` CLI route is
UNCHANGED and still reachable, so no blanket "all production activation is
unavailable" claim is made. See ../QBIND_DEVNET_EVIDENCE_RUN_422.md.

Behavioral tests (this containment): process-level run_422_startup_refusal 4/4
(valid genesis+signer+flag refused before P2P service start; LocalMesh cannot
bypass; flag-absent preserves behavior + reaches LocalMesh service-start;
--help preserved); run_422 provider/loader suite 15/15; regressions run_420 3/3
+ run_418 sender-binding 18/18 + run_418 newview demux 3/3. See
test_results.txt / commands.txt.
============================================================================

--- CORRECTIVE CONTINUATION (HISTORICAL, SUPERSEDED BY CONTAINMENT) ------------
RESULT=PARTIAL-FOR-DEVNET-CONSENSUS-AUTHORITY-ACTIVATION-CODE-TEST
PRODUCTION_ACTIVATION=UNAVAILABLE-FOR-UNRESOLVED-BOUNDARIES (path reachable — corrected)
The continuation corrected D1 (single owned snapshot; hash and keys from the
SAME parse; boot-identity equality gate) and D2 (fail-closed reject of
peer_derived_count != genesis_authority_count) and added a LocalMesh-only flag
reject for D3, but the P2P route still constructed and activated the context.
That state is superseded by the containment correction above.

--- INITIAL CONCLUSION (HISTORICAL, SUPERSEDED) --------------------------------

RESULT=POSITIVE-FOR-DEVNET-CONSENSUS-AUTHORITY-ACTIVATION-CODE-TEST
F3_STATUS=CONFIGURED-AUTHORITY-PRODUCTION-PATH-CODE-TEST-POSITIVE
F4_STATUS=CONFIGURED-AUTHORITY-PRODUCTION-PATH-CODE-TEST-POSITIVE
F8_STATUS=CONFIGURED-AUTHORITY-PRODUCTION-PATH-CODE-TEST-POSITIVE
CONFIGURED_AUTHORITY_RELEASE_BINARY_EVIDENCE=NOT-YET-CAPTURED
AUTHORITY_SCOPE=GENESIS-BOUND-DEVNET
SECURITY_POSTURE=RS1-OPEN / PUBLIC-DEVNET-NO-GO

Axes (kept separate, per task §19.1):
  COMPLETION_AXIS       = code + source/integration tests complete and passing
  VALIDATION_AXIS       = focused + affected-crate cargo tests pass; fmt/clippy
                          clean on changed files; release-binary evidence-
                          preservation harnesses recorded honestly in
                          commands.txt / test_results.txt
  RUNTIME_EVIDENCE_AXIS = configured-authority ADVERSARIAL release-binary
                          evidence = NOT CAPTURED (Run 423 scope)

One-paragraph result:
Run 422 binds the standalone `qbind-node` consensus verification/signing context
to the boot-verified canonical genesis. A new fail-closed opt-in flag
`--consensus-authority-from-genesis` derives the validator set and per-validator
ML-DSA-44 `(suite, public_key)` provider from
`GenesisConfig.validators[].pqc_public_key`, computes a domain-tagged authority
commitment bound to chain_id + genesis hash, requires a matching local signer,
and feeds the SAME `try_build_timeout_verification_context` that `main` uses on
the existing CLI path. Valid explicit DevNet material activates the real
constructor and yields an active immutable context; every invalid/partial
activation exits nonzero before network/consensus start; absent activation keeps
the Run 421 `None + Required` unavailable-authority default. All F3/F4/F8 labels
are SOURCE/TEST-positive only. This run captures NO configured-authority
adversarial release-binary evidence and moves NO readiness item Green; RS1/C4/C5
stay OPEN and public DevNet stays NO-GO.

Why POSITIVE (source/test) and not PARTIAL — all §17 positive criteria met:
  * accepted genesis genuinely commits the canonical authority
    (GenesisValidator.pqc_public_key) under the stated bootstrap trust model;
  * exact membership + suite policy enforced (index->ValidatorId, suite==100,
    key length 1312, duplicate/overflow/malformed reject);
  * local signer matching enforced (signer pk == genesis-committed pk);
  * `main` invokes the validated constructor and obtains `Some` from explicit
    valid DevNet material (integration test drives the real constructor;
    source-guard test asserts main.rs feeds it);
  * invalid activation fails closed before network/consensus activity
    (std::process::exit(1) paths);
  * Proposal/Vote crypto path uses the real MlDsa44Backend (registered suite
    100), reusing the Run 420 verified verifier;
  * TestNet/MainNet strict-negative preserved: the flag never downgrades a
    strict environment; absent/invalid mandatory authority still rejects;
  * shared Timeout/NewView context scope is documented and unchanged
    (shared_context_impact.txt);
  * stale/incompatible authority cannot continue (genesis re-hash + signer
    match are re-enforced every startup; a changed genesis yields a different
    commitment and a mismatched signer fails closed).

Not claimed (kept honest):
  * configured-authority ADVERSARIAL release-binary evidence (Run 423);
  * closure of RS1/C4/C5; any Green readiness movement; TestNet/MainNet
    provisioning; rotation/revocation/replay/double-signing/full consensus
    safety.