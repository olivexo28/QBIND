# QBIND Whitepaper Contradictions and Undocumented Behaviors

**Version**: 1.8  
**Date**: 2026-05-10  
**Status**: Active Tracking Document

This document tracks contradictions between the whitepaper (`docs/whitepaper/QBIND_WHITEPAPER.md`) and the actual implementation, as well as behaviors that are implemented but not documented.

---

## Contradictions

### C1. O3–O5 Offense Penalties Not Implemented

| Field | Value |
|-------|-------|
| **Status** | ✅ **RESOLVED (M9/M11)** |
| **Whitepaper Reference** | Section 8.10, Section 12.2 |
| **Code Location** | `crates/qbind-consensus/src/slashing/mod.rs:2218-2240` (PenaltyEngineConfig defaults), `crates/qbind-consensus/src/slashing/mod.rs:2523-2540` (apply_penalty_if_needed O3-O5 handling) |
| **Evidence** | O3-O5 penalties are now fully implemented and enforced: O3 (Invalid Vote): 300 bps (3%) slash + 3 epoch jail; O4 (Censorship): 200 bps (2%) slash + 2 epoch jail; O5 (Availability): 100 bps (1%) slash + 1 epoch jail. Mode matrix: `EnforceCritical`/`EnforceAll` applies penalties for all O1-O5 offenses. |
| **Tests** | `crates/qbind-node/tests/m11_slashing_penalty_o3_o5_tests.rs` (21 tests) |

### C2. No Minimum Stake Requirement

| Field | Value |
|-------|-------|
| **Status** | ✅ **RESOLVED (M2.1-M2.4)** |
| **Whitepaper Reference** | Section 8.1, Section 12.2, Section 18 |
| **Code Location** | `crates/qbind-system/src/validator_program.rs:130-132` (registration-time enforcement), `crates/qbind-consensus/src/validator_set.rs:116-178` (epoch-boundary filtering via `build_validator_set_with_stake_filter()`), `crates/qbind-node/src/hotstuff_node_sim.rs:936-987` (`with_stake_filtering_epoch_state_provider()`) |
| **Evidence** | Minimum stake is now enforced at both registration and epoch boundary. `min_validator_stake` enforced via `ExecutionContext.min_validator_stake` at registration. `StakeFilteringEpochStateProvider` excludes validators with `stake < min_validator_stake` at epoch transitions. Fail-closed: if all validators excluded, epoch transition fails. |
| **Tests** | `crates/qbind-consensus/tests/validator_set_tests.rs`, `crates/qbind-consensus/tests/m2_2_stake_filter_epoch_transition_tests.rs`, `crates/qbind-node/tests/m2_3_stake_filtering_node_integration_tests.rs`, `crates/qbind-node/tests/m2_4_production_stake_filtering_tests.rs` |

### C4. Production `qbind-node` Binary Does Not Boot a Fully Operating Node

| Field | Value |
|-------|-------|
| **Status** | ⚠️ **OPEN — partial** (B1, B2, B3 (VM-v0 state restore), B5 (restore-aware consensus start), **B6 (multi-validator P2P binary-path interconnect)**, **B7 (binary-path test-grade KEMTLS bring-up + dialer-side peer-validator identity closure)**, **B8 (listener-side test-grade validator-identity closure + bounded initial-dial retry)**, **B9 (leader-side re-emission of `BroadcastProposal` on late peer connect within the same view)**, **B10 (paired leader-vote re-emission on the same B9 trigger + engine-progress recorder wiring + inbound engine-accept metric closure)**, **B12 (mutual-auth `MutualAuthMode::Required` cryptographic peer-identity binding wired end-to-end through the binary path)**, **bounded multi-validator restore catchup above a restored VM-v0 snapshot prefix**, **B13 (bounded post-catchup restore-mode → normal-participation transition)**, and **B14 (smallest honest binary-path view-timeout / view-change primitive — absent-leader recovery isolated by Run 015)** landed; full production fast-sync/consensus-storage restore, signature verification of `TimeoutMsg`/`TimeoutCertificate`, exponential-backoff timeout pacing, and production PQC KEMTLS AEAD/lifecycle completion still outstanding; Run 039 replaced the pqc-static-root transport KEM with ML-KEM-768 but did not replace DummyAead; **DevNet Evidence Run 018 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_018.md`) further narrows the B13 sub-item: the strict-progress-above-baseline exit predicate in `RestoreCatchupModeState::maybe_exit_after_response` is by design correctness-preserving, but in the specific scenario where a snapshot anchor is sourced from the only continuously-alive peer at the exact moment that peer's own `committed_height` equals the snapshot height and that peer is itself parked sub-quorum, restored validators stay in `mode_active=1` indefinitely and therefore never enter normal participation — so even when a `2f+1` numeric alive set is reachable via additional honest `--restore-from-snapshot` returns, B14 timeout certificates do not form because the restored nodes are gated out of B14 emission while in restore-catchup mode. Run 018 confirms this is a narrow, operationally-mitigable boundary (capture the snapshot anchor only after the live cluster has committed at least one block above the captured anchor) rather than a defect in B13 itself, and it does not affect the already-proven Run-016 three-of-four B14 recovery shape.**) |
| **Whitepaper / Doc Reference** | `docs/devnet/QBIND_DEVNET_OPERATIONAL_GUIDE.md` §7–§8 (node bring-up, observability), `docs/ops/QBIND_MONITORING_AND_ALERTING_BASELINE.md` §4 (signal classes A/C/D/E/F), `docs/ops/QBIND_BACKUP_AND_RECOVERY_BASELINE.md` §C1–C5 (recoverability, restore proofs), `docs/release/QBIND_MAINNET_READINESS_CHECKLIST.md` (operability and observability gates) |
| **Code Location** | `crates/qbind-node/src/main.rs` — `run_local_mesh_node` and `run_p2p_node` spawn `binary_consensus_loop::spawn_binary_consensus_loop` (drives `BasicHotStuffEngine` on a tokio interval) and `metrics_http::spawn_metrics_http_server_with_crypto` (gated by `QBIND_METRICS_HTTP_ADDR` via `MetricsHttpConfig::from_env`); `main.rs` also calls `snapshot_restore::apply_snapshot_restore_if_requested` before consensus startup, and (B5) translates the resulting `RestoreOutcome` into a `binary_consensus_loop::RestoreBaseline` that seeds the engine before the first tick. `crates/qbind-node/src/binary_consensus_loop.rs` (B1 wiring + B5 `RestoreBaseline` / `with_restore_baseline`). `crates/qbind-node/src/snapshot_restore.rs` (B3 — validates the existing `meta.json + state/` `StateSnapshotter` format via `validate_snapshot_dir`, materializes the RocksDB checkpoint into `<data_dir>/state_vm_v0`, and writes an append-only `RESTORED_FROM_SNAPSHOT.json` audit marker). `crates/qbind-consensus/src/basic_hotstuff_engine.rs` — **B5** `BasicHotStuffEngine::initialize_from_snapshot_baseline(block_id, height)` and `HotStuffStateEngine::initialize_from_snapshot_baseline(block_id, view, height)` seed `committed_height`, insert the snapshot anchor block into the tree at `height = snapshot_height`, and set `current_view = snapshot_height + 1` so post-restore commits advance above the restored height rather than from zero. `crates/qbind-node/src/cli.rs` — `--restore-from-snapshot <PATH>` plumbed into `FastSyncConfig`. `crates/qbind-ledger/src/state_snapshot.rs` (existing snapshot creation + validation; reused, not re-implemented). |
| **Description** | The canonical operations docs presume an operable validator binary that, on startup: (a) runs the consensus loop and produces/commits blocks, (b) exposes a Prometheus-compatible `/metrics` endpoint, (c) supports recovery from a state snapshot, and (d) when restoring, resumes consensus from the restored height rather than from zero. **All four are now satisfied at the binary level for the single-validator binary path.** **B13 lands the smallest honest post-catchup restore-mode transition.** Before B13, the binary loop keyed restore-catchup behaviour (periodic `RestoreCatchupRequest` broadcasts and inbound-proposal deferral via `should_defer_restore_proposal_for_catchup`) permanently off `cfg.restore_baseline.is_some()`, an immutable config value. DevNet Evidence Run 012 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_012.md`) documented the resulting boundary: V1B restored from snapshot, caught up to `committed_height=339` / `current_view=342`, but then plateaued — it kept emitting `RestoreCatchupRequest` frames every 10 ticks and would still defer any inbound proposal whose height was more than one above its committed anchor, never transitioning into normal participation. B13 introduces an explicit `RestoreCatchupModeState` local to `run_binary_consensus_loop_with_io` whose `active` flag starts at `cfg.restore_baseline.is_some()` and flips to `false` exactly once, when (i) an inbound `RestoreCatchupResponse` is processed without rejection, (ii) the running maximum of all observed `responder_committed_height` values has been reached or exceeded by the local engine's `committed_height()`, and (iii) the local committed height is strictly above the snapshot baseline (so we never declare "caught up" without any peer-learned suffix actually committing). The flip is single-shot and fail-closed: malformed/rejected responses do **not** update the peer-anchor tracker, so a hostile or inconsistent payload cannot drive the transition. After the flip, `maybe_broadcast_restore_catchup_request` and `should_defer_restore_proposal_for_catchup` both gate on the dynamic flag instead of the immutable config, so the restored node stops emitting fresh `RestoreCatchupRequest` frames and stops deferring inbound proposals; from that point forward, all forward-progression decisions go through the same `BasicHotStuffEngine::on_proposal_event` / `on_vote_event` paths the non-restore binary path already uses. Two additive `/metrics` gauges expose the transition: `qbind_restore_catchup_mode_active` (0/1) and `qbind_restore_catchup_mode_exited_at_height` (set exactly once at the flip; never decreases). DevNet Evidence Run 013 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_013.md`) now confirms the B13 transition on real binaries in the bounded non-deadlocked case: V1B restored from live-sourced `S=5`, applied 245 peer-learned certified blocks, exited restore-catchup mode at height `246`, flattened catchup requests, and resumed proposal/vote/QC/commit progression to `committed_height=4098`. Seven new unit tests in `crates/qbind-node/src/binary_consensus_loop.rs` exercise the transition state machine (no-baseline-no-mode, active-until-caught-up, strict-progress-above-baseline, peer-max running-max, malformed-response stays-active fail-closed, empty-response from caught-up peer, and request-broadcast suppression after exit). All 612 existing `qbind-node` lib tests continue to pass; the single-validator and B5 paths are untouched. **What B13 does NOT solve.** The Run-012 plateau also exhibits a separate, pre-existing limitation of the binary loop: there is no view-timeout / view-change driver, so a view that gets stuck because the leader's proposal was missed by a peer (Run 012's view 342: V0 proposed but V1A died before voting and V1B's startup race meant it never received V0's late-peer-reemit) cannot be advanced by either node's own ticker. Run 013 shows B13's transition fix is **sufficient** for resumed forward progression when the restored node exits catchup into an active proposal/vote path, but it is still not by itself **sufficient** to recover from a fully-deadlocked predecessor view. That deeper boundary (binary-path view-timeout / view-change driver, and/or having the responder piggyback its current pending proposal on a `RestoreCatchupResponse` to a fully-caught-up requester) remains explicitly out of scope and unsolved here. DevNet Evidence Run 014 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_014.md`) materially narrows this further on real binaries: it intentionally recreated the Run-012 deadlocked predecessor-view shape (V0+V1A live to `committed_height=283`/`current_view=286`, V1A then stopped so V0 alone parked at `committed_height=284`/`current_view=287`, 15s deadlock hold, V1B started with `--restore-from-snapshot` from a live-`/metrics`-sourced anchor at `S=43`) and observed that V1B applied 245 peer-learned certified blocks across two real `RestoreCatchupResponse` frames, B13 flipped `RestoreCatchupModeState.active` to `false` exactly at the V0 idle anchor `committed_height=284`, and forward proposal/vote/QC/commit progression then resumed automatically and ran for 280s up to `committed_height=2956`/`current_view=2959`/`view_lag=0`. The unblock was driven by the *already-landed* B9/B10 late-peer-reemit primitive (V0 logged exactly one `B9+B10: re-emitted view 0 BroadcastProposal + BroadcastVote after late peer connect`, loop-exit `outbound_proposal_late_peer_reemits=1`); neither a binary-path view-timeout / view-change driver nor a responder-side pending-proposal piggyback on `RestoreCatchupResponse` was needed for *this* shape. The strictly narrower sliver of the boundary that is still untested by real-binary evidence is therefore the `N≥3` shape where the leader for the next view is the still-absent validator and the late-peer-reemit edge cannot consume the deadlock — a 2-of-2 topology cannot reach that case under leader rotation. Production fast-sync / consensus-storage restore and production PQC KEMTLS root-key distribution remain open as before. B1 + B2 landed earlier; B3 lands the smallest honest restore-from-snapshot startup path: when `--restore-from-snapshot <PATH>` is passed, the binary validates the snapshot via the existing `validate_snapshot_dir` (chain-id, layout, height) and materializes the RocksDB checkpoint into the configured `<data_dir>/state_vm_v0` before consensus starts. **B5 lands the smallest honest restore-aware consensus initialization**: the `RestoreOutcome` produced by B3 is translated into a `RestoreBaseline { snapshot_height, snapshot_block_id }` (using only `StateSnapshotMeta::height` and `StateSnapshotMeta::block_hash` — no invented metadata) and applied via `BasicHotStuffEngine::initialize_from_snapshot_baseline` *before the first tick* of the binary consensus loop. After application, `committed_height = snapshot_height`, the snapshot anchor block is registered in the local block tree at `height = snapshot_height` (so subsequent leader proposals are registered at `snapshot_height + 1`, not `0`), and `current_view = snapshot_height + 1`. The synthetic baseline block carries no `own_qc`, which means the 3-chain commit rule cannot use it as G and therefore cannot fabricate a re-commit at the restored height — only *new* commits above the baseline can fire. Failures (missing path, wrong chain id, missing `meta.json` / `state/`, populated target state dir, missing `--data-dir`) cause a non-zero exit with a precise reason; no silent degradation. **B6 lands the smallest honest multi-validator P2P binary-path interconnect.** Two pre-existing wiring gaps closed: (i) `P2pNodeBuilder` previously plugged a freshly-allocated, immediately-dropped `mpsc::channel::<P2pMessage>` into the inbound demuxer, so every frame the transport actually delivered was silently lost regardless of which `ConsensusInboundHandler` was registered — now the builder calls `p2p_service.subscribe().await` and the demuxer receives the real inbound stream; (ii) the binary consensus loop only consumed leader-step ticks, so even if inbound bytes had reached the loop they would never have entered the engine — a new `BinaryConsensusLoopIo { inbound_rx, outbound }` surface plus a `run_binary_consensus_loop_with_io` / `spawn_binary_consensus_loop_with_io` variant now decode `ConsensusNetMsg::Proposal` / `ConsensusNetMsg::Vote` frames into `qbind_wire::consensus::{BlockProposal, Vote}` and feed them into the running `BasicHotStuffEngine` via the existing `on_proposal_event` / `on_vote_event` entry points (the same surfaces that test harnesses already exercise — no new engine API was needed). Engine-emitted `ConsensusEngineAction::{BroadcastProposal, BroadcastVote, SendVoteTo}` flow back out through an existing `ConsensusNetworkFacade` (the real `P2pConsensusNetwork`, which wraps the same `TcpKemTlsP2pService` instance the inbound side reads from — no parallel networking architecture). Sender identity for inbound messages is recovered from `BlockProposal::header.proposer_index` and `Vote::validator_index`, which is the same convention the rest of the codebase already uses (no new peer-identity layer invented). The single-validator / LocalMesh path is preserved bit-equivalent by retaining the existing 4-arg `run_binary_consensus_loop` / `spawn_binary_consensus_loop` entry points that delegate to the new `run_binary_consensus_loop_with_io(..., None)`. Inbound timeout / new-view frames (`ConsensusNetMsg::Timeout`, `ConsensusNetMsg::NewView`) are explicitly counted as "received but unhandled" rather than silently dropped — the typed `TimeoutMsg` decoder is not yet exposed by `qbind-wire` for general-purpose deserialization, and `NewView` is reserved for future view-change protocol extensions; this is recorded honestly rather than faked as a Noop. B3 + B5 + B6 are scoped to the **single-validator** restore semantics (B3/B5) and to the **honest binary-path inbound→engine routing** (B6); restoring non–VM-v0 substate (e.g. consensus storage at `<data_dir>/consensus`), full production restore sync beyond the bounded peer-certified suffix path, production-grade validator/node-id mapping (today the binary uses the `SimpleValidatorNodeMapping` identity scheme — adequate for the first multi-validator binary-path evidence run, not for production), and the real PQC KEMTLS handshake/identity certificate work all remain out of scope for B6 and are not silently claimed. | **B5 lands the smallest honest restore-aware consensus initialization**: the `RestoreOutcome` produced by B3 is translated into a `RestoreBaseline { snapshot_height, snapshot_block_id }` (using only `StateSnapshotMeta::height` and `StateSnapshotMeta::block_hash` — no invented metadata) and applied via `BasicHotStuffEngine::initialize_from_snapshot_baseline` *before the first tick* of the binary consensus loop. After application, `committed_height = snapshot_height`, the snapshot anchor block is registered in the local block tree at `height = snapshot_height` (so subsequent leader proposals are registered at `snapshot_height + 1`, not `0`), and `current_view = snapshot_height + 1`. The synthetic baseline block carries no `own_qc`, which means the 3-chain commit rule cannot use it as G and therefore cannot fabricate a re-commit at the restored height — only *new* commits above the baseline can fire. Failures (missing path, wrong chain id, missing `meta.json` / `state/`, populated target state dir, missing `--data-dir`) cause a non-zero exit with a precise reason; no silent degradation. **B6 lands the smallest honest multi-validator P2P binary-path interconnect.** Two pre-existing wiring gaps closed: (i) `P2pNodeBuilder` previously plugged a freshly-allocated, immediately-dropped `mpsc::channel::<P2pMessage>` into the inbound demuxer, so every frame the transport actually delivered was silently lost regardless of which `ConsensusInboundHandler` was registered — now the builder calls `p2p_service.subscribe().await` and the demuxer receives the real inbound stream; (ii) the binary consensus loop only consumed leader-step ticks, so even if inbound bytes had reached the loop they would never have entered the engine — a new `BinaryConsensusLoopIo { inbound_rx, outbound }` surface plus a `run_binary_consensus_loop_with_io` / `spawn_binary_consensus_loop_with_io` variant now decode `ConsensusNetMsg::Proposal` / `ConsensusNetMsg::Vote` frames into `qbind_wire::consensus::{BlockProposal, Vote}` and feed them into the running `BasicHotStuffEngine` via the existing `on_proposal_event` / `on_vote_event` entry points (the same surfaces that test harnesses already exercise — no new engine API was needed). Engine-emitted `ConsensusEngineAction::{BroadcastProposal, BroadcastVote, SendVoteTo}` flow back out through an existing `ConsensusNetworkFacade` (the real `P2pConsensusNetwork`, which wraps the same `TcpKemTlsP2pService` instance the inbound side reads from — no parallel networking architecture). Sender identity for inbound messages is recovered from `BlockProposal::header.proposer_index` and `Vote::validator_index`, which is the same convention the rest of the codebase already uses (no new peer-identity layer invented). The single-validator / LocalMesh path is preserved bit-equivalent by retaining the existing 4-arg `run_binary_consensus_loop` / `spawn_binary_consensus_loop` entry points that delegate to the new `run_binary_consensus_loop_with_io(..., None)`. Inbound timeout / new-view frames (`ConsensusNetMsg::Timeout`, `ConsensusNetMsg::NewView`) are explicitly counted as "received but unhandled" rather than silently dropped — the typed `TimeoutMsg` decoder is not yet exposed by `qbind-wire` for general-purpose deserialization, and `NewView` is reserved for future view-change protocol extensions; this is recorded honestly rather than faked as a Noop. B3 + B5 + B6 are scoped to the **single-validator** restore semantics (B3/B5) and to the **honest binary-path inbound→engine routing** (B6); restoring non–VM-v0 substate (e.g. consensus storage at `<data_dir>/consensus`), full production restore sync beyond the bounded peer-certified suffix path, production-grade validator/node-id mapping (today the binary uses the `SimpleValidatorNodeMapping` identity scheme — adequate for the first multi-validator binary-path evidence run, not for production), and the real PQC KEMTLS handshake/identity certificate work all remain out of scope for B6 and are not silently claimed. **B14 lands the smallest honest binary-path view-timeout / view-change primitive needed to recover from the absent-leader N=4 deadlock shape isolated by DevNet Evidence Run 015.** Run 015 had observed the cluster park at `current_view=15` / `committed_height=12` after a restored V1B exited bounded restore-catchup mode (B13 transition fired correctly), because round-robin leader for view 15 in N=4 is `ValidatorId(3)`, who was deliberately absent — and there was no honest mechanism on the binary path to leave that view, so no proposal/vote/QC/commit fired during the 200-second post-restore window. The investigation confirmed this was *not* still-in-restore-mode (catchup requests had already flattened) and *not* a B9/B10 late-peer-reemit gap (no late peer was joining; the leader was simply absent) — it was the missing view-timeout driver itself. The engine surface (`BasicHotStuffEngine::create_timeout_msg`, `mark_timeout_emitted`, `timeout_emitted_in_view`, `on_timeout_msg`, `on_timeout_certificate`) had been present and unit-tested for many milestones, but was entirely unwired on the deployed binary path; correspondingly, inbound `ConsensusNetMsg::Timeout` / `NewView` frames were explicitly counted as "received but unhandled" because no canonical wire decoder existed for `TimeoutMsg`/`TimeoutCertificate`. B14 closes that gap with a strictly minimal change: (1) `serde::{Serialize, Deserialize}` derives are added to `qbind_consensus::QuorumCertificate`, `qbind_consensus::TimeoutMsg`, and `qbind_consensus::TimeoutCertificate` so they have a canonical bincode wire encoding; (2) a new `view_timeout_ticks: Option<u64>` field on `BinaryConsensusLoopConfig` (default `Some(50)` ≈ 5s at the existing 100ms tick interval; `None` disables the primitive entirely and preserves pre-B14 behaviour) controls the threshold; (3) a per-loop `ViewTimeoutState` tracks the most recent tick at which forward view-progress (strict `current_view` advance OR new commit) was observed, resetting the window each time real progress fires so an honest live leader is never timed out; (4) a new `maybe_emit_view_timeout` helper, called once per tick after `do_leader_tick` and after the restore-catchup-mode evaluation, gates emission on five explicit conditions (primitive enabled, restore-catchup mode inactive, outbound facade wired, engine has not already emitted a timeout for this view, window elapsed) and on success builds a `TimeoutMsg` via `engine.create_timeout_msg()`, marks the engine, locally ingests via `engine.on_timeout_msg`, broadcasts the bincode-encoded payload as `ConsensusNetMsg::Timeout(bytes)`, and — if local ingestion already crosses the 2/3 quorum threshold — applies the resulting `TimeoutCertificate` via `engine.on_timeout_certificate` and broadcasts it as `ConsensusNetMsg::NewView(bytes)`; (5) the inbound handlers for `ConsensusNetMsg::Timeout` and `ConsensusNetMsg::NewView` now bincode-decode their payloads and route them through `engine.on_timeout_msg` / `engine.on_timeout_certificate`, with all four failure modes (decode error, validator-set non-membership, insufficient quorum, view mismatch) treated fail-closed via the existing `TimeoutValidationError` surface; (6) ten additive `BinaryConsensusLoopInboundStats` counters (`view_timeouts_emitted`, `inbound_timeouts_delivered`, `inbound_timeouts_engine_accepted`, `timeout_certificates_formed`, `outbound_new_views_sent`, `inbound_new_views_delivered`, `inbound_new_views_engine_accepted`, `view_timeout_advances`, `view_timeout_decode_failures`, `view_timeout_engine_rejects`) expose every step of the primitive on `/metrics`-bound state for the next evidence run. View-timeout emission is **always** suppressed while `RestoreCatchupModeState::active = true` so a restored node never times out a live view it is still catching up to (B13 invariant preserved); single-validator and LocalMesh paths have no outbound facade, so `view_timeouts_emitted` stays at zero on those paths (preserving the B1/B5 single-validator behaviour bit-equivalent). Ten new unit tests in `crates/qbind-node/src/binary_consensus_loop.rs` (`b14_view_timeout_emits_after_window_elapses_no_progress`, `b14_view_timeout_suppressed_in_restore_catchup_mode`, `b14_view_timeout_disabled_via_none_emits_nothing`, `b14_view_timeout_progress_resets_window`, `b14_n1_self_quorum_forms_tc_advances_view_and_broadcasts_new_view`, `b14_inbound_malformed_timeout_fails_closed`, `b14_inbound_new_view_with_valid_tc_advances_view`, `b14_inbound_new_view_insufficient_quorum_fails_closed`, `b14_run_015_n4_absent_leader_shape_advances_via_tc`, `b14_single_validator_loop_does_not_emit_timeouts`) exercise the full state machine, including the exact Run-015 N=4 absent-leader shape — local V0 emits its own timeout for view 15, peer V1 and V2 timeouts arrive over the inbound channel, the third one crosses 2f+1 = 3, the engine forms a TC, the loop applies it locally, broadcasts NewView, and `current_view` advances to 16 (whose round-robin leader is V0, not the absent V3); a separate test asserts the single-validator binary path still ends a 60-tick run with `view_timeouts_emitted == 0` because constant QC-driven progress kept the window resetting. All 622 existing `qbind-node` lib tests continue to pass alongside the 10 new ones. **What B14 does NOT solve.** Crypto-signed timeouts (B14 routes `TimeoutMsg` and `TimeoutCertificate` through the engine on the same "no per-message signature verification at the loop level" basis that `Proposal` / `Vote` use today — production hardening of timeout signatures is a separate, additive milestone), exponential-backoff timeout pacing (today the threshold is a fixed tick count; sustained partitions where every view in turn lacks a leader will fire timeouts at the same cadence rather than backing off), per-validator timeout rate limiting on inbound (today only the engine's own duplicate-detection per `(view, validator)` provides bounding), and persistent `TimeoutCertificate` storage across restarts all remain explicitly out of scope. Production fast-sync / consensus-storage restore and production PQC KEMTLS root-key distribution remain open as before. **DevNet Evidence Run 016 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_016.md`) now empirically closes the strict N=4 absent-leader recovery sub-boundary that Run 015 had isolated.** With B14 active by default (`view_timeout_ticks = Some(50)`, ≈ 5 s at 100 ms ticks), the same canonical Run-015 N=4 topology (V0/V1A/V2/V3 live → V1A and V3 SIGINT, V1B restored from a live-`/metrics`-sourced anchor `S=10`) recovered automatically on real `qbind-node` binaries: V1B applied 3 peer-learned certified blocks, B13 exited at `committed_height=11`, B14 then emitted 36 honest `TimeoutMsg` frames per available validator (108 total) — 35 of those per node on `view % 4 == 3` views led by the still-absent V3 (the exact Run-015 plateau shape, recurring under round-robin rotation) — every emit was followed by a successful `TimeoutCertificate` / `NewView` view advance with **zero** decode failures, **zero** engine rejects, and **zero** late-peer-reemit dependencies, and forward proposal/vote/QC/commit progression resumed automatically up to `current_view=155` / `committed_height=116` on V0 / V1B / V2 within ~206 s of post-restore observation (a +105-block advance above the Run-015 plateau). The narrowest things still untested by real-binary evidence are now: multi-absent-leader / sub-quorum-alive shapes where 2 f + 1 timeouts cannot be assembled, large-suffix B13 catchup combined with sustained B14, production-grade signature verification of `TimeoutMsg` / `TimeoutCertificate`, and exponential-backoff timeout pacing. |
| **Impact** | Further reduced — DevNet readiness work, DevNet Evidence Run 003 (restore-then-observe) and DevNet Evidence Run 004 (post-restore committed-height progression above the snapshot height) continue to run honestly against the binary path, and **the first multi-validator binary-path DevNet evidence run is now unblocked by B6**: inbound P2P consensus messages actually reach the engine via the real binary path, and outbound engine actions flow back through the existing `P2pConsensusNetwork`, so cross-node proposal/vote/QC/commit progression is no longer harness-only. Backup/recovery drills can produce real restore evidence shaped against `docs/ops/QBIND_BACKUP_AND_RECOVERY_BASELINE.md` §C1–C5 for the VM-v0 state. A bounded multi-validator restore-catchup path now exists for peer-provided certified suffixes above the restored prefix; full production fast-sync/consensus-storage restore and production-grade validator/node-id identity hardening remain open. Protocol-correctness claims continue to be test-evidenced. **Empirical narrowing from DevNet Evidence Run 005 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_005.md`):** the first attempt to drive two real `qbind-node` processes against each other via `--enable-p2p` / `--p2p-peer …` confirmed B6 wiring is observable in the running binary (multi-validator banner, `BinaryConsensusLoopIo` active, B6 `inbound_*` / `outbound_*` counters surfaced) but did **not** observe any `ConsensusNetMsg` crossing node boundaries (`inbound_msgs = 0` on both nodes) and did **not** observe any cross-node QC / commit progression. The blocker is below B6, in the test-grade transport bring-up: each node's `peer_kem_pk` is currently derived from its own `validator_id` (`crates/qbind-node/src/p2p_node_builder.rs:514–518, 575`), so the KEMTLS handshake fails between two distinct binaries (`Net(Protocol("client handle_server_accept failed"))` from the dialer, matching server-side `Handshake error` on the acceptor), and surviving inbound sessions are admitted under a temporary NodeId rather than the deterministic peer NodeId — so the validator-identity mapping never closes. `/metrics` and shutdown remained honest under this failure mode. **Further empirical narrowing from DevNet Evidence Run 006 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_006.md`):** post-B7, the KEMTLS handshake itself succeeds between two real `qbind-node` processes (V1's stdout `Peer NodeId(4bd96f97b1aaec9d) connected` is byte-identical to V0's deterministic listening NodeId; the Run-005 `Net(Protocol("client handle_server_accept failed"))` shape is gone) and the dialer-side identity closure works as designed (the post-B7 `using per-peer KEM pk + validator-id override` line is observable). However no `ConsensusNetMsg` crossed between the two binaries during Run 006 either — the listener-side temporary-NodeId binding (B7-residual) combined with a single-direction-only TCP topology that emerged from a startup dial-race (V0's first dial got `Connection refused` and was not retried) prevented V0's leader-side `BroadcastProposal` from resolving to a registered transport session for V1. `/metrics` (two scrapes ~56 s apart, all consensus counters held flat at zero) and shutdown (clean SIGINT, listeners released, `%{http_code} = 000` post-shutdown) remained honest under this failure mode as well. An independent single-validator LocalMesh regression check on the same binary observed `commits = 200`, `committed_height = Some(199)`, `view = 202`, IO counters all zero, confirming no regression of B1/B2. **Closure of those two narrowing observations by B8 (this delta):** B8 lands the smallest honest fix to both Run-006 negatives — listener-side validator-identity closure (test-grade) and bounded initial-dial retry — so the joint precondition Run 007 needs (BOTH sides observe the OTHER's deterministic `NodeId` among `connected_peers()`, so `send_to(ValidatorId)` resolves to a real registered transport session in both directions) is now exercised by `crates/qbind-node/tests/b8_listener_identity_closure_and_dial_retry_tests.rs` (`b8_b_listener_side_accepted_session_bound_to_deterministic_node_id`, `b8_c_initial_dial_retry_recovers_from_stagger`, `b8_d_two_node_bidirectional_send_precondition_holds`). This does NOT claim production-grade identity hardening; the resolver consumes the dialer's `client_random` under `MutualAuthMode::Disabled` and is therefore self-asserted (see `Remaining` below). **Empirical narrowing from DevNet Evidence Run 007 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_007.md`):** the post-B8 binary path now empirically satisfies the joint precondition on real `qbind-node` processes for the first time — both nodes report each other under their deterministic NodeIds (V0's stdout `Inbound connection from 127.0.0.1:56970 bound to deterministic NodeId NodeId(92115fddcd4f93a0) via inbound identity resolver (B8, test-grade)` followed by `Peer NodeId(92115fddcd4f93a0) connected`; V1's stdout `Peer NodeId(4bd96f97b1aaec9d) connected`), and the bounded initial-dial retry trace (`dial 127.0.0.1:19001 attempt 1/8 ... 7/8 failed (transient: Connection refused); retrying in {100,200,400,800,1000,1000,1000}ms (B8 initial-dial retry)` then `giving up after 8 attempt(s) ... transient=true, max_attempts=8`) is observable on the binary. However `ConsensusNetMsg` traffic still does not cross between two real `qbind-node` binaries: V0's leader-issued view-0 `BroadcastProposal` action is emitted at consensus-loop start (loop-level `outbound_proposals=1`) but never reaches the wire (`consensus_net_outbound_total{kind="proposal_broadcast"} = 0` across three scrapes), because V1 had not yet established its TCP→V0 inbound at the moment of leader emission and the engine does not re-emit a view-0 proposal once issued. Consequently `inbound_proposals=0` and `qbind_consensus_proposals_total{result="accepted"}=0` on V1, `inbound_votes=0` on V0, `qcs_formed_total=0`, `view=0`, `committed_height=None` on both nodes across loop-exit summaries and three `/metrics` scrapes (A, B, C). `/metrics` (three scrapes ~30–70 s apart, all consensus counters held flat at zero) and shutdown (clean SIGINT, listeners released, `%{http_code} = 000` post-shutdown) remained honest. An independent single-validator LocalMesh regression check on the same binary observed `commits = 345`, `committed_height = Some(344)`, `view = 347`, IO counters all zero, confirming no regression of B1/B2. The Run-007 boundary is **strictly smaller** than Run 006's: it is no longer at the transport-identity layer (which B8 closed and Run 007 observed closed) but at the engine-broadcast layer — leader-side re-emission of `BroadcastProposal` on late peer connect within the same view, which B8 was not designed to address. **Closure of that observation by B9 (this delta):** B9 lands the smallest honest fix on the binary-path consensus loop — when a `PeerConnectivitySource` is wired into `BinaryConsensusLoopIo` (the real `Arc<dyn P2pService>` is the production-path implementation), the loop tracks the leader's last `BroadcastProposal` with its view, observes connected-peer set transitions on every tick, and re-broadcasts the cached current-view proposal exactly once on the next tick after any expected peer goes from "not connected" to "connected", subject to seven boundedness gates (transition required, cached-proposal required, view match, leader of current view, commit not invalidated, per-view single-shot latch, outbound facade required). The new `outbound_proposal_late_peer_reemits` counter is `0` when no late connect occurs, exactly `1` after a single late connect within a view, and stays `1` across arbitrary reconnect churn within the same view. The single-validator / LocalMesh / `peer_connectivity=None` paths are bit-equivalent to pre-B9. This makes the precondition Run 008 will exercise — *first observed cross-node proposal/vote/QC/commit progression on real `qbind-node` processes* — both reachable and bounded. **Empirical narrowing from DevNet Evidence Run 008 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_008.md`):** the post-B9 binary path has now, on real `qbind-node` processes, demonstrably moved a `ConsensusNetMsg::Proposal` and a `ConsensusNetMsg::Vote` between two distinct binaries for the first time in DevNet evidence history. With V0 started ≈12 s before V1 (deliberate late-peer-connect shape) and again in a friendlier comparison sub-run with the stagger reversed, the leader's stderr emits exactly one `B9: re-emitted view 0 BroadcastProposal after late peer connect (newly_connected_peers=1, reemits_total=1)` line; the leader-side loop-exit summary reports `proposals=1 outbound_proposals=1 outbound_proposal_late_peer_reemits=1 inbound_votes=1`, and the peer-side loop-exit summary reports `inbound_msgs=1 inbound_proposals=1 outbound_votes=1` — i.e. the cached leader proposal reaches the wire (B9), reaches the peer engine (B6 inbound demuxing), the peer engine emits a vote action which reaches the wire (B6 outbound), and that vote is decoded by the leader (B6 inbound). However engine-acceptance counters do not advance to QC formation: across three `/metrics` scrapes on each node `qbind_consensus_proposals_total{result="accepted"} = 0` on the receiver, `qbind_consensus_votes_total{result="accepted"} = 0` on the leader, `qbind_consensus_qcs_formed_total = 0` on both, and `committed_height` stays `None` on both. The Run-008 boundary is therefore **strictly smaller** than Run 007's: it is no longer at the network/transport/identity stack (B6/B7/B8 closed and exercised; B9 closes the leader-side re-emission gap and is observed firing exactly once per view as designed) but at the engine accept/QC-formation layer above it. `/metrics` remained honest under all three sub-runs (counters held flat, no fabricated progress) and shutdown remained clean (post-shutdown `%{http_code} = 000` on all listening ports, identical to Run 006/007). One non-progression-blocking metric-coverage gap was surfaced for the first time: the `consensus_net_outbound_total{kind="proposal_broadcast"}` / `…{kind="vote_broadcast"}` / `consensus_net_inbound_total{kind="proposal"}` / `…{kind="vote"}` Prometheus counters remained at `0` while the binary-loop counters honestly recorded `1` of each on the wire — i.e. the Prometheus surface under-reports (does not fabricate) on the `P2pConsensusNetwork` path actually exercised by the binary. An independent single-validator LocalMesh regression check on the same binary observed `proposals=602 commits=600 committed_height=Some(599) view=602 late_peer_reemit=off`, confirming no regression of B1/B2 and the bit-equivalence of the `peer_connectivity = None` path that B9 was bounded against. **Closure of the Run-008 engine accept / QC-formation observation by B10 (this delta):** B10 lands the smallest honest fix at exactly the layer Run 008 surfaced as the new boundary. Three additive changes to `crates/qbind-node/src/binary_consensus_loop.rs`: (i) the leader-step tick now caches **both** the just-emitted `BroadcastProposal` AND the leader's paired same-view `BroadcastVote` (the engine's `on_leader_step` always emits the proposal immediately followed by the leader's self-vote); the existing B9 late-peer-connect helper now re-emits both under the same per-view single-shot latch, on the same tick, only when the cached vote's view matches the cached proposal's view. Without the paired vote re-emit, a 2-validator setup deterministically stalls in the exact Run-008 shape: V1 receives the re-emitted proposal, votes, and V0 ingests V1's vote (so V0 reaches `[V0, V1]` for view 0, forms its QC, and advances to view 1), but V1 only ever has its own self-vote in view 0 — it cannot reach 2/3, cannot form a QC, and stays at view 0; round-robin then makes V1 the leader of view 1, which it cannot enter, so the chain stalls one tick after engine-acceptance would otherwise fire. (ii) the loop now wires an `Arc<dyn ConsensusProgressRecorder>` adapter (`NodeMetricsProgressRecorder`) onto the engine via `BasicHotStuffEngine::set_progress_recorder` before the first tick. The adapter forwards every recorder callback (`record_qc_formed`, `record_qc_formed_with_latency`, `record_vote_observed`, `record_view_change`, `record_leader_change`, `reset_current_view_votes`) to the existing `NodeMetrics::progress()` impl. Pre-B10 the binary-path loop never installed any progress recorder, so even when the engine genuinely formed a QC (as V0 actually did in Run 008's view 0) the `qbind_consensus_qcs_formed_total` counter on `/metrics` stayed at 0. This is metric-coverage closure, not fabrication: every increment continues to come from the engine's existing `record_qc_formed` call site on actually-formed QCs. (iii) the inbound-message handler now increments `consensus_t154.proposals_accepted` on each `engine.on_proposal_event` that returns `Some(action)` (i.e., the engine performed the full accept path and produced a vote) and `consensus_t154.votes_accepted` on each `engine.on_vote_event` that returns `Ok(_)`. The new `BinaryConsensusLoopInboundStats` fields `outbound_vote_late_peer_reemits`, `inbound_proposals_engine_accepted`, `inbound_votes_engine_accepted`, and `qcs_formed_total` mirror these on the loop-exit summary so structured progress is observable independently of `/metrics` scraping. The B9 single-shot latch now covers the proposal+vote pair: at most one paired re-emit per view, never one without the other, no replay across reconnect churn. New tests at `crates/qbind-node/tests/b10_engine_acceptance_qc_closure_tests.rs` (5 tests, all passing) reproduce the Run-008 partial-positive shape in-tree against the real `run_binary_consensus_loop_with_io` surface (test A: `b10_a_pre_fix_run_008_shape_reproduces_in_tree` — modelled by a `SelectiveConnectFacade` that drops the leader's view-0 vote even after the proposal channel re-opens, with V1 stuck at view 0 and `committed_height = None` exactly as Run 008 observed), prove the post-fix engine-acceptance / QC closure on both sides (test B: `b10_b_post_fix_engine_acceptance_qc_closure` — V1 ingests both the re-emitted proposal and the re-emitted leader vote, V1's engine accepts both, V1 forms its own view-0 QC, V1 advances past view 0, and `metrics.progress().qcs_formed_total()` is `≥ 1` on both nodes), and guard regressions across the B6 cross-wired path (test C), B9 single-shot boundedness across reconnect churn (test D), and the `io = None` single-validator / LocalMesh path (test E — still commits, plus `qcs_formed_total ≥ 1` is now honestly observable). All previously-landed B1/B2/B3/B5/B6/B7/B8/B9 tests continue to pass (40+ tests across `b3_…`, `b5_…`, `b7_…`, `b8_…`, `b9_…`, `b10_…`, `c4_b6_…`, `binary_path_b1_b2_b4_…`, `localmesh_…`). The Run-008 boundary is therefore **closed at the engine accept / QC formation layer** for the test-grade binary-path; what remains is the empirical proof on real `qbind-node` processes, which DevNet Evidence Run 009 is now positioned to provide. **Empirical closure on real binaries by DevNet Evidence Run 009 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_009.md`):** the post-B10 binary path now sustains cross-node consensus progression on two real `qbind-node` processes for the first time in DevNet evidence history. With V0 started ≈ 12 s before V1 (the same deliberate late-peer-connect shape Run 008 ran), V0 emits exactly one `B9+B10: re-emitted view 0 BroadcastProposal + BroadcastVote after late peer connect (newly_connected_peers=1, proposal_reemits_total=1, vote_reemits_total=1)` line; loop-exit `outbound_proposal_late_peer_reemits=1` on V0 only and `=0` on V1 confirms the seven-gate envelope (extended by B10 to cover the paired vote) holds for ~ 1 500 subsequent views without a second re-emission. Across three `/metrics` scrapes ~ 30–70 s apart, `qbind_consensus_qcs_formed_total` advances monotonically on **both** V0 and V1 (358/750/1 062 on V0, 359/751/1 063 on V1) and `qbind_consensus_proposals_total{result="accepted"}` / `qbind_consensus_votes_total{result="accepted"}` advance in lockstep — the exact counters that were empirically stuck at 0 in Run 008. Loop-exit summaries record V0 `commits=1 454 committed_height=Some(1 453) view=1 456` and V1 `commits=1 453 committed_height=Some(1 452) view=1 455`, i.e. **`committed_height > None` on both real binaries for the first time in DevNet evidence history**, with the V0/V1 1-tick offset internally consistent with the round-robin 2-validator leader rotation. `/metrics` (three scrapes; counters monotonic with elapsed-time-consistent deltas; no fluctuation downward; no fabricated growth) and shutdown (clean SIGINT, listeners released, `%{http_code} = 000` on `:9100/:9101/:19000/:19001` post-shutdown) remained honest. The Run-008 metric-coverage gap on the `consensus_net_*_total` Prometheus family persists unchanged (loop says 728 proposals × 2 directions and 1 456 votes × 2 directions; Prometheus family stays at 0 across all three scrapes — honest under-report, not over-report) and is the only respect in which Run 009 is not "complete-positive across every metric surface". An independent single-validator LocalMesh regression check on the same binary observed `proposals=384 commits=382 committed_height=Some(381) view=384 interconnect=none late_peer_reemit=off`, confirming no regression of B1/B2 and the bit-equivalence of the `peer_connectivity = None` / `io = None` paths that B9/B10 were bounded against. **The Run-008 boundary is therefore now closed not only at the test-grade binary-path level but also empirically on real `qbind-node` processes.** What remains in C4 is at the level of (a) production identity hardening (mutual-auth `MutualAuthMode::Required`, real PQC certs), (b) multi-validator restore catchup (P2P chain catchup above the snapshot prefix), and (c) the `consensus_net_*_total` Prometheus coverage gap; none of those are surfaced or claimed by Run 009 as newly closed and all are kept honestly open in `Remaining` below. |
| **Remaining (EXE-1 → EXE-2 follow-up)** | • ✅ B1 — `BasicHotStuffEngine` driver wired into `run_local_mesh_node` and `run_p2p_node` via `binary_consensus_loop`. • ✅ B2 — `metrics_http::spawn_metrics_http_server_with_crypto` spawned from the binary path, gated on `QBIND_METRICS_HTTP_ADDR`. • ✅ B3 — `--restore-from-snapshot <path>` ingestion at startup, reusing the existing `StateSnapshotter` (`meta.json + state/`) format; regression-tested by `crates/qbind-node/tests/b3_snapshot_restore_tests.rs`. • ✅ B5 — restore-aware consensus start: `BasicHotStuffEngine::initialize_from_snapshot_baseline` + `binary_consensus_loop::RestoreBaseline` + `main.rs` translation of `RestoreOutcome` → baseline; regression-tested by `crates/qbind-node/tests/b5_restore_aware_consensus_start_tests.rs` and unit-tested in `crates/qbind-consensus/src/basic_hotstuff_engine.rs` (`b5_*`) and `crates/qbind-node/src/binary_consensus_loop.rs` (`b5_*`). • ✅ **B6** — multi-validator P2P binary-path interconnect: `P2pNodeBuilder` subscribes to the real inbound stream via `p2p_service.subscribe().await`; `BinaryConsensusLoopIo` + `run_binary_consensus_loop_with_io` / `spawn_binary_consensus_loop_with_io` route inbound `ConsensusNetMsg::{Proposal, Vote}` into `BasicHotStuffEngine::on_proposal_event` / `on_vote_event` and forward engine actions through `ConsensusNetworkFacade` (`P2pConsensusNetwork`); `run_p2p_node` in `main.rs` wires a `ChannelConsensusHandler` to the demuxer and drives the loop with the resulting receiver + outbound facade; regression-tested by `crates/qbind-node/tests/c4_b6_p2p_binary_path_interconnect_tests.rs` (5 tests, including a bounded two-engine cross-wired binary-path progression proof). • ✅ **B7** — binary-path test-grade KEMTLS bring-up + dialer-side peer-validator identity closure: the smallest honest fix to the negative finding of DevNet Evidence Run 005. Three concrete changes in `crates/qbind-node/src/p2p_node_builder.rs`, `crates/qbind-node/src/p2p_tcp.rs`, and `crates/qbind-node/src/consensus_net_p2p.rs`: (i) the test-grade KEM keypair derivation rule is centralized in `derive_test_kem_keypair_from_validator_id(vid)` and is the single source of truth for both the listener (`ServerHandshakeConfig.local_kem_sk` / delegation cert `leaf_kem_pk`) and the dialer (`ClientConnectionConfig.peer_kem_pk`), so two real `qbind-node` processes can actually complete the KEMTLS handshake instead of failing with `Net(Protocol("client handle_server_accept failed"))`; (ii) the local NodeId, `SimpleValidatorNodeMapping` NodeId, and the dialer's outbound NodeId are now all derived via the same `sha3_256_tagged("QBIND:nodeid:v1", test_kem_pk(vid))` rule (`derive_test_node_id_from_validator_id`), so `send_to(ValidatorId)` resolves to the *same* NodeId the transport actually registered for that connection; (iii) `--p2p-peer` now accepts `vid@addr` syntax (parsed by `parse_peer_spec`), and per-peer `peer_kem_pk` + `validator_id` overrides are threaded into `TcpKemTlsP2pService::dial_peer` via `set_peer_kem_pk_overrides` / `set_peer_validator_id_overrides` so each side dials with the *peer's* test-grade KEM public key and the *peer's* expected validator id (the latter satisfies the protocol-level `delegation_cert.validator_id == client_init.validator_id` check in `qbind_net::handshake::handle_server_accept`). A latent inbound-side bug was also fixed: `TcpStream::into_std()` keeps the socket non-blocking, but `accept_kemtls_async` does blocking reads, so the inbound handshake was tripping `WouldBlock` on the very first read — `handle_inbound_connection` now restores blocking mode before the handshake. Multi-validator binary-path runs that pass bare-addr peers (no `vid@`) are rejected with a clear `B7: --p2p-peer entries must be of the form 'vid@addr'…` error rather than silently producing a broken handshake. A `PeerValidatorMap` is populated at dial-config time (`P2pNodeContext.peer_validator_map`) so the connected peer is recognized as `ValidatorId(N)` on the dialer side. Regression-tested by `crates/qbind-node/tests/b7_kemtls_bringup_identity_closure_tests.rs` (5 integration tests including a bounded two-real-binary KEMTLS handshake proof — the exact case Run 005 failed to demonstrate) and by 6 new unit tests under `p2p_node_builder::tests::b7_*`. The fix is bounded to the test-grade DevNet path and does not invent any new identity / certificate system. • ✅ **B8** — listener-side test-grade validator-identity closure + bounded initial-dial retry: the smallest honest fix to the negative finding of DevNet Evidence Run 006 (the two `Run 006` narrowing items in the previous `Inbound-side validator identity binding (B7-residual)` bullet). Three concrete changes: **(i) Listener-side identity closure.** A new `accept_kemtls_async_with_peer_init` variant in `crates/qbind-node/src/secure_channel.rs` surfaces the dialer-supplied `ClientInit.client_random` and `ClientInit.validator_id` (via `qbind_net::unpack_client_init`) from the server-side accept path. `crates/qbind-node/src/p2p_tcp.rs` now exposes `set_inbound_identity_resolver` / `InboundIdentityResolver`, and `handle_inbound_connection` consults the installed resolver: if it returns `Some(deterministic_node_id)` the accepted session is registered under that NodeId, otherwise the legacy temporary-session-NodeId path is preserved exactly (no silent override). `crates/qbind-node/src/p2p_node_builder.rs` installs a resolver that recovers the dialer's vid from the deterministic `qbind-client-<N>` ASCII prefix `create_connection_configs` already places in `client_random` (parsed by the new `parse_test_validator_id_from_client_random` helper) and returns `derive_test_node_id_from_validator_id(N)` — the SAME NodeId the dialer side registers under. **(ii) Bounded initial-dial retry.** A new `DialRetryPolicy` (default 8 attempts × {100..1000}ms ≈ 5.5 s) and `set_dial_retry_policy` on `TcpKemTlsP2pService`. `start()` no longer blocks on dial; instead it spawns a per-peer background `DialerHandle::dial_with_retry` task that retries only transient TCP errors (`ConnectionRefused`, `ConnectionReset`, `ConnectionAborted`, `TimedOut`, `AddrNotAvailable`, `NotFound`) and bounds attempts + backoff. KEMTLS-handshake errors and configuration errors are NOT retried. `shutdown()` aborts in-flight dial tasks before shutting peer connections. Single-validator / no-static-peers / LocalMesh paths are bit-equivalent (no dial tasks spawned at all). **(iii) Honest scope.** Resolver semantics under the current `MutualAuthMode::Disabled` test-grade path are explicitly documented as self-asserted: a malicious dialer could spoof a vid in `client_random`. Strictly cryptographic peer-identity binding still requires `MutualAuthMode::Required` and is kept as a separate `Remaining` bullet below. The B8 fix is bounded to the test-grade DevNet binary path and does not invent any new identity / certificate system. Files touched: `crates/qbind-node/src/secure_channel.rs`, `crates/qbind-node/src/p2p_tcp.rs`, `crates/qbind-node/src/p2p_node_builder.rs`. Regression-tested by `crates/qbind-node/tests/b8_listener_identity_closure_and_dial_retry_tests.rs` (6 tests: parse round-trip, listener-side deterministic-NodeId binding, initial-dial retry recovery from stagger, two-node bidirectional `connected_peers` precondition, single-validator no-regression, `DialRetryPolicy::no_retry` pin). All B7 tests, all 5 c4_b6 tests, all 16 three_node_kemtls_integration tests, all 9 t172 P2P smoke tests, all 10 t175 wiring tests, and all 604 lib unit tests continue to pass. • ✅ **B9** — leader-side re-emission of `BroadcastProposal` on late peer connect within the same view (the smallest honest fix to the negative finding of DevNet Evidence Run 007). The fix is bounded entirely to the binary-path consensus loop (no engine redesign, no transport redesign): `BinaryConsensusLoopIo` gains an optional `peer_connectivity: Option<Arc<dyn PeerConnectivitySource>>` (a tiny dedicated trait — strictly smaller than `P2pService` — implemented by the new `P2pServicePeerConnectivity` adapter that forwards to the same `Arc<dyn P2pService>` already backing inbound demuxing and outbound `P2pConsensusNetwork`, so connectivity, outbound, and inbound observations all come from the same transport instance). `do_leader_tick` now caches the leader's last `BroadcastProposal` together with the view it was emitted for; on every tick the new `maybe_reemit_on_late_peer_connect` helper runs through seven explicit boundedness gates: (1) **transition required** — at least one NodeId in the current connected set was absent from the previous tick's set; (2) **cached proposal required** — the engine actually emitted a leader-step proposal at some point; (3) **view match** — `engine.current_view() == cached_view` (a view advance drops the cache without re-broadcast); (4) **leader of current view** — `engine.is_leader_for_current_view()` holds; (5) **commit not invalidated** — `committed_height` has not reached or passed the cached view; (6) **per-view single-shot** — a `reemitted_for_view: Option<u64>` latch that fires at most once per view regardless of how many peers connect or how many ticks pass; (7) **outbound facade required** — the loop must actually have somewhere to broadcast. On all-gates-pass the loop calls `outbound.broadcast_proposal(&cached)` exactly once and increments a new `BinaryConsensusLoopInboundStats::outbound_proposal_late_peer_reemits` counter (also surfaced in the `Loop exit:` summary line). Reconnect churn within the same view produces zero additional re-emits (gate 6); a view change between cache-fill and the connectivity poll invalidates the cache and produces zero stale replays (gate 3); when `peer_connectivity = None` (single-validator P2P, LocalMesh, the `io=None` 4-arg loop) the entire B9 path is skipped and the counter stays at 0 — bit-equivalent to the pre-B9 loop. Files touched: `crates/qbind-node/src/binary_consensus_loop.rs` (new trait + new adapter + cache state + reemit helper + counter + `BinaryConsensusLoopIo::peer_connectivity` field), `crates/qbind-node/src/main.rs` (`run_p2p_node` wires the same `Arc<dyn P2pService>` instance through `P2pServicePeerConnectivity`). Regression-tested by `crates/qbind-node/tests/b9_late_peer_connect_proposal_reemit_tests.rs` (6 tests: A — late peer connect triggers exactly one bounded re-emission; B — per-view single-shot latch holds across reconnect churn (exactly 1 re-emit when view 0 stays current); C — view-change invalidation drops stale cache (0 stale replays across multiple view advances); D — peer connected before first proposal stays bounded (≤ 1 across 40 ticks); E — `peer_connectivity = None` regression guard (counter stays 0); F — `io = None` LocalMesh regression guard). All pre-existing tests continue to pass: 604 lib unit tests, 5 c4_b6 tests, 6 b8 tests, 5 b7 tests, 6 b5 tests, 4 b1/b2/b4 tests, 13 LocalMesh tests, 3 b3 tests. The B9 fix does NOT redesign consensus, does NOT redesign transport, does NOT replace B6/B7/B8, does NOT introduce harness-only logic on the binary path, and does NOT broaden into mutual-auth PKI or production identity hardening (those remain in the open `Remaining` bullets below). • ⚠️ Multi-validator restore semantics (P2P chain catchup above the snapshot prefix) — out of scope for B5/B6/B7/B8/B9. • ✅ **B12 — mutual-auth (`MutualAuthMode::Required`) cryptographic peer-identity binding on the binary path.** The smallest honest closure of the C4 residual previously labelled "Mutual-auth (`MutualAuthMode::Required`) cryptographic peer-identity binding (B8-residual)". The `qbind-net` mutual-auth handshake codepath (`ServerHandshake::handle_client_init` parsing + verifying a v2 `ClientInit.client_cert`, transcript-binding the cert into the AEAD key schedule, and producing `HandshakeResult { peer_validator_id, client_node_id, mutual_auth_complete }`) **already existed** before B12 — the identity-binding gap was strictly that the `qbind-node` binary stack discarded those fields at the `Connection` boundary and the listener-side resolver consumed only the dialer's self-asserted `ClientInit.client_random`. B12 wires that already-existing path end-to-end through the binary-path P2P stack with five bounded changes: (i) `qbind-net::Connection` retains `peer_validator_id` / `peer_node_id` / `mutual_auth_complete` after handshake completion (previously dropped) and exposes them via `peer_validator_id()` / `peer_node_id()` / `mutual_auth_complete()` accessors — captured at the *exact* `Established` transition site for both server (`handle_client_init`) and client (`handle_server_accept`) paths so values are populated for any caller that holds an established `Connection`; (ii) `qbind-node::secure_channel::AcceptedPeerInit` gains `verified_peer_validator_id: Option<[u8; 32]>` / `verified_client_node_id: Option<[u8; 32]>` / `mutual_auth_complete: bool`, populated in `SecureChannel::from_accepted_with_peer_init` from the `Connection` accessors, and these fields are surfaced to the inbound resolver alongside the legacy self-asserted `client_random` / `validator_id`; (iii) `qbind-node::p2p_node_builder::P2pNodeBuilder` gains a `with_mutual_auth_mode(MutualAuthMode)` builder that defaults to `Disabled` (preserves pre-B12 behaviour bit-for-bit), and `create_connection_configs` honors it by attaching the local `NetworkDelegationCert` as `local_client_cert` (so the dialer presents a v2 `ClientInit`), setting `ServerHandshakeConfig.mutual_auth_mode = Required`, and installing a `TrustedClientRoots` resolver so the listener actually exercises `parse_and_verify_client_cert`; (iv) the listener-side `InboundIdentityResolver` is split by mode — under `Required`/`Optional` it sources identity ONLY from the verified cert (`AcceptedPeerInit.verified_peer_validator_id` parsed via the new `parse_test_validator_id_from_cert_validator_id` and mapped through `derive_test_node_id_from_validator_id` to the SAME deterministic NodeId both ends register under), and **fails closed** (returns `None`) when `mutual_auth_complete = false` so the self-asserted `client_random` path is unreachable; under `Disabled` the pre-B12 B8 self-asserted resolver is preserved exactly; (v) `qbind-node::main::run_p2p_node` plumbs `--p2p-mutual-auth required|optional|disabled` (and the `QBIND_MUTUAL_AUTH` env var as a fallback) through `with_mutual_auth_mode`, with an explicit `[binary] B12: mutual_auth_mode=… (source: …)` startup banner so evidence runs can prove which mode was active. **Identity property cryptographically bound that was not before:** under `Required` mode the listener-side accepted-session NodeId is derived from `cert.validator_id`, where `cert` is the dialer's `NetworkDelegationCert` whose `leaf_kem_pk` participates in the AEAD transcript hash AND whose `sig_bytes` is verified against the configured `TrustedClientRoots` root — so completing the handshake AND producing a routable session-bound NodeId requires (a) holding the leaf KEM secret matching `cert.leaf_kem_pk` (the protocol-level KEMTLS commitment) and (b) producing a cert whose signature passes the configured signature suite. Under `Disabled` mode (the pre-B12 default and DevNet convenience path) the resolver's input is still the self-asserted `client_random` and is documented as such; B12 explicitly does **not** silently relabel test-grade behaviour as hardened — the two modes are byte-for-byte distinguishable on the wire (v1 vs v2 `ClientInit`) and at the resolver call site (different `match` arm). **Migration / compatibility:** `Disabled` (the pre-B12 default) is preserved bit-for-bit. Files touched: `crates/qbind-net/src/connection.rs` (peer-identity capture + accessors), `crates/qbind-node/src/secure_channel.rs` (`AcceptedPeerInit` extension + populate from `Connection`), `crates/qbind-node/src/p2p_node_builder.rs` (`with_mutual_auth_mode` + cert + roots + cert-verified resolver + `parse_test_validator_id_from_cert_validator_id`), `crates/qbind-node/src/cli.rs` (`--p2p-mutual-auth`), `crates/qbind-node/src/main.rs` (env+CLI plumbing through builder, banner). Regression-tested by `crates/qbind-node/tests/b12_mutual_auth_identity_binding_tests.rs` (6 tests: A — two-node `Required`-mode handshake succeeds and BOTH sides see each other's deterministic NodeId sourced from the verified cert; B — `Required`-mode resolver refuses self-asserted identity when `mutual_auth_complete = false` (the closed-form contract pin: a cert whose `validator_id` field carries the dialer's `qbind-client-N` shape returns `None` from `parse_test_validator_id_from_cert_validator_id`, so the self-asserted shape cannot spoof a routable vid even under cert-shaped delivery); C — bidirectional `broadcast(Heartbeat)` under `Required` mode resolves to the cert-bound session on the listener; D — `Disabled` mode preserves B7 dialer-side closure AND B8 listener-side self-asserted closure bit-for-bit; E — `parse_test_validator_id_from_cert_validator_id` round-trips for valid `qbind-val-N` shapes and rejects all non-conforming bytes (all-zero, `qbind-client-N` shape, `qbind-val-not-a-number`); F — single-validator `Required`-mode build/shutdown is clean). All pre-B12 regression suites continue to pass: 604 lib unit tests, 5 b7, 6 b8, 6 b9, 10 b10, 5 b11, 5 c4_b6, 4 b1/b2/b4, 6 b5, 3 b3, 9 t172, 25 t175 wiring, 10 t175 config, 17 m8 mutual-auth config, 16 three_node_kemtls, plus 13 qbind-net integration suites (m8 mutual-auth net-level included). The B12 fix does NOT redesign consensus, does NOT redesign transport, does NOT invent a parallel identity framework (it strictly wires the existing `qbind-net::handshake` mutual-auth path), and does NOT change the test-grade DummySig signature surface — production-grade PQC root-key distribution and cert lifecycle remain outside scope and are tracked under the remaining bullet below. **What B12 does NOT close:** (a) the `TrustedClientRoots` resolver currently returns a deterministic 32-byte dummy root key for any `root_key_id`, and the test-grade DummySig provider accepts certs whose `sig_bytes` starts with `0x00` — so the cert *signature path* is exercised structurally (a dialer with a non-test-grade or tampered sig would be rejected by the chosen suite) but is not yet exercising real PQC verification against a real long-term validator root key; production root-key distribution (and the operational PKI behind it) is still tracked separately as a `Remaining` bullet below. (b) The deterministic test-grade `qbind-val-<N>` validator-id ↔ KEM-pk mapping is the same one B7/B8 use; replacing it with a production-grade per-validator cert lifecycle is intentionally not part of B12. **Justification for first mutual-auth binary-path evidence run:** yes — this is enough. Under `--p2p-mutual-auth required`, the binary now (1) presents a client cert on every dial, (2) parses + verifies it on the listener via the existing `parse_and_verify_client_cert`, (3) transcript-binds it into the AEAD key schedule, (4) registers the accepted session under a NodeId derived from the cert's `validator_id` (no longer from `client_random`), and (5) routes consensus traffic through the same B6/B7/B8/B9/B10/B11 pipeline that already produces sustained QC formation in Run 009. • ⚠️ Validator/node-id identity hardening for production (today's `SimpleValidatorNodeMapping` is intentionally simple, suitable for the first binary-path evidence run but not production). • ⚠️ Real PQC KEMTLS handshake / identity certificate path on the binary (test-grade transport now actually completes; production identity binding still tracked separately). • ⚠️ Inbound timeout / new-view (`ConsensusNetMsg::Timeout`, `ConsensusNetMsg::NewView`) routing into the engine on the binary path — counted as "received but unhandled" today, intentionally deferred. • ⚠️ Extend restore beyond VM-v0 substate if/when other substores grow snapshot semantics — not currently produced by `StateSnapshotter`. • ⚠️ **Engine-side acceptance / QC-formation closure on the binary path under post-B9 re-emitted leader proposal (B9-residual).** Surfaced empirically by DevNet Evidence Run 008 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_008.md`): two real `qbind-node` binaries now move a cross-node `ConsensusNetMsg::Proposal` and a return `ConsensusNetMsg::Vote` between each other for the first time (B9 re-emit fires exactly once per view as designed; loop-exit counters `outbound_proposals=1`, `inbound_proposals=1` on the peer, `outbound_votes=1` on the peer, `inbound_votes=1` on the leader), but `qbind_consensus_proposals_total{result="accepted"} = 0` on the receiver and `qbind_consensus_votes_total{result="accepted"} = 0` on the leader, so `qcs_formed_total = 0` and `committed_height = None` on both nodes across three scrapes. The reason — view / proposer-index mismatch on the re-emitted cached proposal versus the receiver's local view, an accept-path metric-increment gap, or a test-grade signature/cert field that the engine validates but the post-B7/B8 Disabled-mutual-auth configuration leaves at a non-canonical value — is not yet identified. B10 is the smallest honest closure of this residual on the binary path; it is intentionally below B6/B7/B8/B9 in scope and does not redesign consensus, transport, or identity. **Empirically closed on real binaries by DevNet Evidence Run 009 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_009.md`):** post-B10 sustained QC formation on both V0 and V1 across three monotonic `/metrics` scrapes (`qbind_consensus_qcs_formed_total` 358 → 1 062 on V0, 359 → 1 063 on V1) and `committed_height > None` on both nodes (V0 `Some(1 453)`, V1 `Some(1 452)` at shutdown). This bullet is therefore retained for traceability but its empirical residual on real binaries is closed. • ✅ **B11** — `consensus_net_*_total` Prometheus counter coverage on the `P2pConsensusNetwork` path. Surfaced for the first time by Run 008 (now that messages actually cross the wire): the loop-level `outbound_proposals` / `inbound_proposals` / `outbound_votes` / `inbound_votes` counters in the binary's `Loop exit:` summary honestly record `1` of each, while `consensus_net_outbound_total{kind="proposal_broadcast"}` / `…{kind="vote_broadcast"}` / `consensus_net_inbound_total{kind="proposal"}` / `…{kind="vote"}` stayed at `0` across all three `/metrics` scrapes in Run 008 and remained at `0` in the otherwise-fully-positive Run 009 ("the only respect in which Run 009 is not 'complete-positive across every metric surface'"). The Prometheus family under-reported (did not fabricate) the `P2pConsensusNetwork` path actually used by the binary; the `inc_outbound_proposal_broadcast` / `inc_inbound_proposal` increments lived on the `consensus_net_worker.rs` codepath and were not reached under the binary's actual configuration. **Closed by B11 (this delta).** Two minimal, additive changes to the real binary path: (i) `crates/qbind-node/src/consensus_net_p2p.rs` — `P2pConsensusNetwork` gains an optional `Arc<NodeMetrics>` (wired by the new `with_metrics(...)` builder method) and increments `consensus_net_outbound_total{kind="proposal_broadcast"}` / `…{kind="vote_broadcast"}` / `…{kind="vote_send_to"}` exactly once per successful `broadcast_proposal` / `broadcast_vote` / `send_vote_to` call (counted *after* the underlying `P2pService` call returns, so the counter reflects what was actually pushed to the transport — not what was merely attempted; `send_vote_to` for an unknown validator returns an error and does not increment, by direct test). When `with_metrics(...)` is not called, the facade is bit-equivalent to its pre-B11 behaviour (no counter increments at all). (ii) `crates/qbind-node/src/binary_consensus_loop.rs::handle_inbound_consensus_msg` — every inbound `ConsensusNetMsg` is counted exactly once before any decode/accept gating: `Proposal(...)` → `inbound_total{kind="proposal"}`, `Vote(...)` → `inbound_total{kind="vote"}`, `Timeout(...)` / `NewView(...)` → `inbound_total{kind="other"}` (so the existing "received but unhandled" frames are not silently absorbed). Counts mirror the same frames the loop-level `inbound_msgs_received` / `inbound_proposals_delivered` / `inbound_votes_delivered` counters already observe — no fabricated increments, no double counting. (iii) `crates/qbind-node/src/main.rs::run_p2p_node` wires the production `Arc<NodeMetrics>` into the outbound `P2pConsensusNetwork` via `.with_metrics(Arc::clone(&node_metrics))`. The `forward_actions_to_facade` boundary inside the loop deliberately does **not** count outbound — counting there in addition to the facade would double-count; the engine ↔ facade boundary stays metric-free and the facade is the single increment site for outbound. Files touched: `crates/qbind-node/src/consensus_net_p2p.rs` (+`metrics: Option<Arc<NodeMetrics>>`, `with_metrics(...)`, three `inc_outbound_*` call sites), `crates/qbind-node/src/binary_consensus_loop.rs` (one match in `handle_inbound_consensus_msg` covering `Proposal` / `Vote` / `Timeout` / `NewView`), `crates/qbind-node/src/main.rs` (one `.with_metrics(...)` call). Regression-tested by `crates/qbind-node/tests/b11_consensus_net_prometheus_coverage_tests.rs` (5 high-signal tests: A — pre-fix shape preserved when `with_metrics(...)` is not called (counters stay at 0, no hidden mutation), B — every facade call increments exactly the matching outbound counter exactly once and an erroring `send_vote_to(unknown)` does not increment, C — every inbound `ConsensusNetMsg` increments exactly the matching inbound counter (4 proposals → 4, 3 votes → 3, 2 timeouts → 2 under `kind="other"`), D — B9 single-shot late-peer-connect re-emit moves the outbound counter by exactly +1 with no double-counting, E — `io = None` / single-validator LocalMesh path leaves the entire `consensus_net_*_total` family at 0 (B1/B2 bit-equivalence)). All pre-existing B-series tests continue to pass: 604 lib unit tests, 5 c4_b6 tests, 6 b9 tests, 10 b10 tests, 6 b8 tests, 5 b7 tests, 6 b5 tests, 4 b1/b2/b4 tests, 13 LocalMesh tests, 3 b3 tests. The B11 fix does NOT redesign networking, does NOT redesign consensus, does NOT touch B9/B10 logic, does NOT broaden into production identity hardening, and does NOT silently fix the contradiction-file header `Version`/`Date` mismatch (kept as a separate documentation-hygiene item). What remains in C4 is now strictly (a) production identity hardening (mutual-auth `MutualAuthMode::Required`, real PQC certs) and (b) multi-validator restore catchup (P2P chain catchup above the snapshot prefix). |
| **Tracking** | Recorded by `docs/protocol/QBIND_REPO_CODE_DOC_ALIGNMENT_AUDIT.md` §6.1–§6.3, §9 (B1, B2, B3) as the top three execution blockers exposed by EXE-1. B1 + B2 closed by `crates/qbind-node/src/binary_consensus_loop.rs` + `crates/qbind-node/src/main.rs` (regression-tested by `crates/qbind-node/tests/binary_path_b1_b2_b4_tests.rs`). B3 closed by `crates/qbind-node/src/snapshot_restore.rs` + `--restore-from-snapshot` CLI flag in `crates/qbind-node/src/cli.rs` + restore call in `crates/qbind-node/src/main.rs` (regression-tested by `crates/qbind-node/tests/b3_snapshot_restore_tests.rs`). B5 closed by `BasicHotStuffEngine::initialize_from_snapshot_baseline` + `HotStuffStateEngine::initialize_from_snapshot_baseline` in `crates/qbind-consensus/` and `RestoreBaseline` / `BinaryConsensusLoopConfig::with_restore_baseline` in `crates/qbind-node/src/binary_consensus_loop.rs`, with the `RestoreOutcome → RestoreBaseline` translation in `crates/qbind-node/src/main.rs`; regression-tested by `crates/qbind-node/tests/b5_restore_aware_consensus_start_tests.rs`. **B6** closed by the inbound-subscribe fix in `crates/qbind-node/src/p2p_node_builder.rs`, the new `BinaryConsensusLoopIo` / `run_binary_consensus_loop_with_io` / `spawn_binary_consensus_loop_with_io` surface in `crates/qbind-node/src/binary_consensus_loop.rs`, and the `run_p2p_node` wiring in `crates/qbind-node/src/main.rs`; regression-tested by `crates/qbind-node/tests/c4_b6_p2p_binary_path_interconnect_tests.rs` (inbound proposal → engine vote, two-engine cross-wired progression past view 0, single-validator io=None unchanged, inbound-close survival, non-leader proposal not silently dropped). First end-to-end binary-path multi-validator exercise recorded by `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_005.md`: B6 wiring observable in the running binary (multi-validator banner emitted; `BinaryConsensusLoopIo` active; `inbound_msgs` / `inbound_proposals` / `inbound_votes` counters surfaced in `Loop exit` summary on both nodes), but no `ConsensusNetMsg` crossed between two real `qbind-node` processes due to the binary-path test-grade KEMTLS bring-up gap recorded in the new `Remaining` bullet above. Second end-to-end binary-path multi-validator exercise (post-B7) recorded by `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_006.md`: KEMTLS handshake now succeeds between two real `qbind-node` processes and dialer-side peer-validator identity closure is observable on the binary, but `inbound_msgs = 0` on both nodes still — listener-side temporary-NodeId binding combined with a single-direction-only TCP topology (initial-dial `Connection refused` not retried) prevents V0's `BroadcastProposal` from resolving to a registered transport session for V1; the §11 single-validator LocalMesh regression check on the same binary observed `commits = 200`, `committed_height = Some(199)`, no regression. **Third end-to-end binary-path multi-validator exercise (post-B8) recorded by `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_007.md`: B8 listener-side identity resolver line and B8 bounded-retry trace are both observable on the binary; both sides register each other's deterministic NodeId for the first time in DevNet evidence (`Peer NodeId(92115fddcd4f93a0) connected` on V0 after the resolver bound the inbound, and `Peer NodeId(4bd96f97b1aaec9d) connected` on V1); however `consensus_net_outbound_total{kind="proposal_broadcast"} = 0` on V0 and `inbound_proposals=0`, `qbind_consensus_proposals_total{result="accepted"} = 0` on V1 across three `/metrics` scrapes — V0's leader emitted view 0's `BroadcastProposal` before V1 connected and the engine did not re-emit on late peer connect; §11 single-validator LocalMesh regression check observed `commits = 345`, `committed_height = Some(344)`, `view = 347`, no regression. The Run-007 boundary is strictly below the Run-006 listener-identity boundary that B8 closed; this engine-broadcast boundary is in turn closed by B9 (above), making Run 008 — first observed cross-node proposal/vote/QC/commit progression on real `qbind-node` processes — reachable on the binary path. B9 closure regression-tested by `crates/qbind-node/tests/b9_late_peer_connect_proposal_reemit_tests.rs` (6 tests covering A — exactly-one re-emit on late connect, B — per-view single-shot under reconnect churn, C — view-change invalidation, D — pre-connected peer bounded, E — `peer_connectivity=None` regression guard, F — `io=None` LocalMesh regression guard).** |

**Run 024 C4 snapshot-trigger note (2026-05-09):** DevNet Evidence Run 024 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_024.md`) negatively proved the real binary's periodic `--snapshot-interval-blocks` trigger is not wired end-to-end.
V0 ran N=4 Required-mode with `--snapshot-dir`, `--snapshot-interval-blocks 4`, and `--snapshot-max-snapshots 2`, advanced to `committed_height=54` without SIGUSR1, and stayed alive, but `/tmp/run024/snapshots/` remained empty and `qbind_snapshot_success_total` / `qbind_snapshot_last_height` stayed zero.
This does not regress the Run 022/023 SIGUSR1-created real snapshot path, but it leaves periodic height-triggered real-binary snapshots open under C4.


#### C4 Run 021 evidence update (2026-05-09)

DevNet Evidence Run 021 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_021.md`) materially reopens the latest claimed VM-v0 runtime-open / SIGUSR1 snapshot-trigger closure on real binaries. The tested `qbind-node` at commit `950ce2b5e925b2162b1df3da840fa0b35e05dd36` (sha256 `69223f70ff05ebc0030633c84d0a2c77e8d4052cee2325a7a62262f13c18199f`, BuildID `97fd927c08c5a3eb64ac01f93e20b318f00f30ea`) rejected `--snapshot-dir` as an unexpected argument, exposed no `--snapshot-max-snapshots` help surface, did not create/open `<data_dir>/state_vm_v0` during a secondary vm-v0 live run, and terminated on SIGUSR1 (`wait_status=-10`) rather than logging signal receipt or invoking `StateSnapshotter::create_snapshot`. No SIGUSR1-created snapshot existed, so B3/B5/B13/B14 preservation from that snapshot remains unproven. C4 therefore remains open for real-binary VM-v0 runtime-open and in-process snapshot-trigger operability.

#### C4 Run 022 evidence update (2026-05-09)

DevNet Evidence Run 022 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_022.md`) positively closes the specific Run 021 regression on the tested real binary: `qbind-node --help` exposes `--snapshot-dir`, `--snapshot-interval-blocks`, and `--snapshot-max-snapshots`; `--execution-profile vm-v0 --data-dir <dir>` opens `<data_dir>/state_vm_v0` at startup; SIGUSR1 is handled without process termination; configured SIGUSR1 invokes `StateSnapshotter::create_snapshot` in-process on the opened VM-v0 RocksDB handle; and B3/B5 restore from that SIGUSR1-created snapshot succeeds. C4 remains open overall for the broader production items already listed (including production fast-sync/consensus-storage restore and production PQC root-key distribution), and Run 022 did not repeat the full N=4 Required-mode B13/B14 continuation shape.

### C3. Reporter Rewards Not Implemented

| Field | Value |
|-------|-------|
| **Status** | ⚠️ **OPEN** |
| **Whitepaper Reference** | Section 12.2 (Slashing Model), Section 12.2.3 (Reporter Incentives) |
| **Code Location** | `crates/qbind-types/src/state_governance.rs:52` (`reporter_reward_bps` field) |
| **Description** | `reporter_reward_bps` parameter exists in `ParamRegistry` but no code distributes rewards. Evidence reporting has no incentive mechanism. Whitepaper Section 12.2.3 now explicitly documents this as future work. |
| **Impact** | Low-Medium - No monetary incentive; however reporting is hardened and abuse-resistant (M15). |
| **M15 Mitigation** | Evidence ingestion hardened with 8-step verification ordering: (1) Reporter validation, (2) Size bounds, (3) Per-block cap, (4) Deduplication, (5) Structure validation, (6) Age bounds, (7) Future height check, (8) Cryptographic verification (expensive - last). Config: `require_validator_reporter=true`, `per_block_evidence_cap=10`, `max_evidence_age_blocks=100K`, size limits per offense type. See `crates/qbind-consensus/src/slashing/mod.rs` (`HardenedEvidenceIngestionEngine`). |
| **M20 Documentation** | Whitepaper Section 12.2.3 "Reporter Incentives (Future Work)" added. Explicitly states: no on-chain reporter rewards currently; slashing is purely punitive; any future reward model must build on M15 hardened evidence pipeline. |
| **Remaining** | • Wire `reporter_reward_bps` to slashing engine reward distribution • Add reward transfer in penalty application path • Design and implement tokenomics for reporter incentives |

### C5. `TimeoutCertificate` Wire Shape Carries No Per-Signer Signature Evidence

| Field | Value |
|-------|-------|
| **Status** | ⚠️ **OPEN — narrowed further by Runs 034, 035, 036, 037, 038, and 039** (binary-path activation bridge + `--require-timeout-verification` fail-closed gate landed in Run 031; **signer half landed in Run 032**; **peer-side `SuiteAwareValidatorKeyProvider` half landed in Run 033** — `main.rs::run_p2p_node` constructs a real `StaticConsensusKeyProvider` from explicit `network.static_peer_consensus_keys` config (CLI: `--validator-consensus-key VID:SUITE:HEXPK`) and, when both halves are honestly populated, calls real `try_build_timeout_verification_context` so `BinaryConsensusLoopIo::verification_ctx` becomes `Some(...)`; **Run 034 landed the real-binary verified-timeout B14 evidence** — N=4 Required-mode real-binary recovery succeeded with `verification_ctx=Some(...)` on every node, signed outbound TimeoutMsg (`outbound_timeout_signing_success_total=3`, `failure=0` per live validator), verified inbound TimeoutMsg/NewView/TC (every per-reason `inbound_*_rejected_*=0`, `view_timeout_advances_total=3`, `timeout_certificates_formed_total=3`), and `committed_height` advancing 87→99 past the absent-leader plateau; **Run 035 landed the smallest opt-in dev/test-only forged Timeout/NewView injection harness** (hidden from `--help`; refuses non-DevNet env, missing/incorrect env var, unknown case token, non-P2P mode; injects through the same `ChannelConsensusHandler::sender_clone()` channel as live P2P traffic; never calls engine methods directly; never reads/writes/fabricates metrics; 21 forged-injection unit tests pass; full `qbind-node` lib 746/746); **Run 036 landed the live N=4 Required-mode real-binary forged-injection evidence** — V0 absorbed all 12 harness cases (5 Timeout: `malformed`, `unsigned`, `bad-signature`, `wrong-suite`, `unknown-validator` + 7 NewView/TC: `malformed`, `missing-evidence`, `duplicate-signer`, `insufficient-quorum`, `mixed-view`, `bad-signature`, `high-qc-mismatch`) on top of `--require-timeout-verification` + `--p2p-mutual-auth required` + signer keystore + four `--validator-consensus-key` entries with `verification_ctx=Some(...)`, the T+4s post-injection scrape captured `inbound_timeout_engine_accepted_total=0` and `inbound_newview_engine_accepted_total=0` and `view_timeout_advances_total=0` and `view_advances_due_to_verified_tc_total=0` (forged-only window), every per-reason `inbound_timeout_rejected_*` and `inbound_newview_rejected_*` counter froze at the value implied by the case mix (timeout: `unknown_validator=1`, `wrong_suite=1`, `bad_signature=2`; newview: `missing_evidence=1`, `duplicate_signer=1`, `bad_signature=4` — the latter `=4` reflects a documented boundary where 3 NewView cases (`insufficient-quorum`, `mixed-view`, `high-qc-mismatch`) have their post-signature structural rejection paths preempted by ML-DSA-44 verify failure because the harness uses fresh keypairs; deterministic per-reason coverage for those cases lives in the Run 030 / Run 035 unit tests, re-run green) and `view_timeout_decode_failures_total=2` (the two `Malformed*` cases via the shared decode-failure counter), and never moved by honest traffic; V0 stayed alive (rc=0); the same Run-034-shape B14 absent-leader fault then drove 4 view advances (`view_advances_due_to_verified_tc_total=4`), 3 fresh TimeoutCertificates, `outbound_timeout_signing_failure_total=0` on every live validator, and `committed_height 88→138` on V2A/V3A past the absent-leader plateau, with `proposals_total{result="rejected"}=0` and `votes_total{result="invalid"}=0` on every live node. **Run 038 landed the production-honest PQC static-root + verified-timeout co-running B14 evidence** — N=4 Required-mode real-binary B14 absent-leader recovery succeeded with `--p2p-pqc-root-mode pqc-static-root`, four real ML-DSA-44-signed delegation certs sharing one trusted root, `--require-timeout-verification`, and four `--validator-consensus-key` entries simultaneously: `view_timeout_advances_total 0→3`, `view_advances_due_to_verified_tc_total 0→3`, `outbound_timeout_signing_success_total 0→3 / failure_total=0`, `inbound_timeout_verify_accepted_total 0→6 / rejected_total=0`, `inbound_newview_verify_accepted_total 0→6 / rejected_total=0`, every per-reason `inbound_*_rejected_*` and decode-failure and engine-reject counter `=0` throughout, `committed_height 99→111` and `current_view 102→117` on every live validator, `proposals_total{result="rejected"}=0`, `votes_total{result="invalid"}=0`, no DummySig fallback, no `client handle_server_accept failed`, no `server handle_client_init failed`, no FATAL, no panic. C5's remaining transport-root dependency is **NARROWED** by Run 038 — production-honest PQC root-key + delegation-cert verification on the binary path now co-runs with active timeout verification end-to-end. C5 remains OPEN strictly because **production AEAD on the binary path remains test-grade** (`DummyAead`); Run 039 replaced `DummyKem` with real ML-KEM-768 in `pqc-static-root` Required mode; production CA / cert rotation / cert revocation / signed root-distribution channel are still operator-out-of-band; B12 test-grade `TrustedClientRoots`/`DummySig` is NOT a substitute. Operators who only configure consensus timeout-verification keys are NOT operating production transport PKI; operators using `--p2p-pqc-root-mode pqc-static-root` are operating production-honest cert verification but NOT yet production transport KEM/AEAD.) |
| **Whitepaper / Doc Reference** | `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_028.md` §7.2; `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_029.md`; `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_030.md`; `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_031.md`; `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_032.md`; `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_033.md`; `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_034.md`; `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_035.md`; `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_036.md`; `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_039.md` |
| **Code Location** | `crates/qbind-consensus/src/timeout.rs` (`TimeoutCertificate` carries `signed_timeouts`); `crates/qbind-consensus/src/timeout_verify.rs` (`verify_timeout_msg`, `verify_timeout_certificate_with_evidence`); `crates/qbind-node/src/binary_consensus_loop.rs` (Run 030 — loop-API surface); `crates/qbind-node/src/timeout_verification_bridge.rs` (Run 031 — `try_build_timeout_verification_context` + `enforce_policy`); `crates/qbind-node/src/signer_loader.rs` (Run 032 — `load_validator_signer_from_config`, `LoadedValidatorSigner`, signer-derived `public_key_bytes` exposed in Run 033 for cross-checks); **Run 033** added: `crates/qbind-node/src/peer_key_provider.rs` (`StaticConsensusKeyProvider`, `LoadedValidatorKeyProvider`, `build_validator_set_and_key_provider`, `PeerKeyProviderError`, `decode_strict_hex_pk` — fail-closed under empty/bad-hex/unsupported-suite/duplicate-vid/missing-peer-key/bare-peer-addr/local-key-missing/local-key-vs-signer-mismatch/validator-set-build-failed); `crates/qbind-node/src/node_config.rs` (`StaticPeerConsensusKey { validator_id, suite_id, public_key_hex }` + `NetworkTransportConfig.static_peer_consensus_keys: Vec<StaticPeerConsensusKey>`, default empty); `crates/qbind-node/src/cli.rs` (new `--validator-consensus-key VID:SUITE:HEXPK` Append flag + `parse_validator_consensus_key_spec` + `CliError::InvalidValidatorConsensusKey`); `crates/qbind-node/src/metrics.rs` (new `qbind_timeout_verification_key_provider_loaded` 0/1 and `qbind_timeout_verification_validator_count` u64 gauges); `crates/qbind-node/src/main.rs::run_p2p_node` (single-site flip: when signer + provider both present, call real `try_build_timeout_verification_context` with `SimpleBackendRegistry::with_backend(SUPPORTED_TIMEOUT_SUITE_ID, MlDsa44Backend::new())`; preserve Run 032 disabled probe otherwise; fail-closed under `RequireOrFail` if either half cannot be honestly built). |
| **Description** | Run 033 narrows C5 from **"piece (a) signer landed; pieces (b) peer key-provider and (c) production PQC KEMTLS root-key distribution remain"** to **"pieces (a) signer and (b) peer key-provider both landed honestly. Piece (c) production PQC KEMTLS root-key distribution remains under C4."** The new `--validator-consensus-key VID:SUITE:HEXPK` flag (Append; multiple entries) is the smallest additive shape that covers local + every `--p2p-peer vid@addr` peer with explicit `(validator_id, suite_id, public_key_bytes)` triples; it is **strictly orthogonal to** transport-level KEMTLS root-key distribution and does NOT claim to solve it. The provider builder fail-closes on every documented invariant: empty config (`NoConfiguredKeys` — preserves Run 032 path), invalid hex (no `0x` prefix, even length, hex-only), unsupported suite (≠ ML-DSA-44), duplicate `validator_id`, `--p2p-peer vid@addr` peer with no matching key (`PeerMissingKey`), bare `--p2p-peer addr` without `vid@` on the multi-validator path (`PeerWithoutValidatorId`), no local key when no signer is loaded (`LocalKeyMissing`), explicit local entry whose bytes disagree with the keystore-derived signer public key (`LocalKeyMismatchesSigner` — fingerprints only, no key bytes), and any `ConsensusValidatorSet::new` rejection (`ValidatorSetBuildFailed`). When both halves are honestly populated, `main.rs` calls real `try_build_timeout_verification_context(TimeoutVerificationBridgeInputs { validators, key_provider, backend_registry: ML-DSA-44 SimpleBackendRegistry, chain_id, signer, local_validator_id })` and threads the resulting `Arc<TimeoutVerificationContext>` into `BinaryConsensusLoopIo::verification_ctx` — so `qbind_timeout_verification_active 1` only when verification is honestly active, never when it is fabricated. Twenty Run 030 deterministic loop-API tests, the existing 20 `timeout_verification_bridge` tests, eleven `signer_loader` tests, twenty-two new `peer_key_provider` tests (parser strict-hex × 6, builder positives × 3, builder negatives × 7, fingerprint safety × 1, suite dedup × 1, bridge-activation integration × 3, optional/required-mode × 2, unknown-validator × 1), eight new CLI parser tests for `--validator-consensus-key` (valid, missing parts, bad vid, bad suite, empty/odd-length hex, propagation, default-empty, p2p-peer regression), all 9 vm_v0_runtime snapshot-trigger tests, t144/t145/t153 keystore integration suites, b7/b8/b12 transport-config tests, and t172/t175 P2P smoke + config tests all pass — full `qbind-node` lib **725 / 725 passing**, up from 693. |
| **Impact** | Materially narrower than Run 032. Operators who declare production-grade consensus intent via `--require-timeout-verification` and configure both `--signer-keystore-path` (Run 032) **and** `--validator-consensus-key VID:SUITE:HEXPK` (Run 033, one per active validator) now see `qbind_timeout_verification_signer_loaded 1`, `qbind_timeout_verification_key_provider_loaded 1`, `qbind_timeout_verification_validator_count N`, and **`qbind_timeout_verification_active 1`** with `Arc<TimeoutVerificationContext>` honestly threaded into `BinaryConsensusLoopIo::verification_ctx`. Operators with one half but not the other (or with malformed keys) get a precise startup error naming the missing/invalid piece and refusing to start under `RequireOrFail`. The remaining production gap is exclusively C4 piece (c) — production PQC KEMTLS root-key distribution. **No claim is made that consensus public-key distribution is a substitute for transport PKI.** No claim is made that full C4 is closed. No fake/test-grade peer keys are silently accepted as production. |
| **Resolution path** | (1) ~~Wire `config.signer_keystore_path` → `Arc<dyn ValidatorSigner>`~~ — **landed in Run 032**. (2) ~~Extend `NodeConfig.network` with per-peer `(suite_id, pk_bytes)` and construct a `SuiteAwareValidatorKeyProvider` over the active validator set~~ — **landed in Run 033** as `network.static_peer_consensus_keys: Vec<StaticPeerConsensusKey>` + CLI `--validator-consensus-key VID:SUITE:HEXPK`. (3) ~~Replace the Run 032 signer-only probe site with real `try_build_timeout_verification_context(...)` when all pieces are present~~ — **landed in Run 033** in `main.rs::run_p2p_node`. (4) Land production PQC KEMTLS root-key distribution from C4 so the underlying transport identity binding is not on B12's test-grade `TrustedClientRoots`/`DummySig` — **still OPEN**. (5) ~~Run an N=4 Required-mode real-binary B14 absent-leader recovery with verified timeout traffic and capture per-reason `/metrics` snapshots and rejection counters~~ — **landed in Run 034**. (6) ~~Real-binary forged-traffic negative injection (in-process injection harness or fake-peer rig)~~ — **landed in Runs 035 + 036**: Run 035 added the smallest opt-in dev/test-only `--devnet-forged-inject` harness (hidden from `--help`; gated by `--env devnet` + `QBIND_DEVNET_FORGED_INJECTION=1` + explicit case tokens; refuses Testnet/Mainnet/non-P2P/missing-env-var/unknown-case-token; injects into the same `mpsc::Sender<ConsensusNetMsg>` channel used by live P2P; never calls engine methods directly; never reads/writes/fabricates metrics; 21 forged-injection unit tests + full lib 746/746 pass). Run 036 then ran the live N=4 Required-mode real-binary forged-injection evidence on V0 with all 12 cases and confirmed: forged frames are rejected fail-closed before engine ingestion (`inbound_timeout_engine_accepted_total=0`, `inbound_newview_engine_accepted_total=0` at the T+4s forged-only scrape on V0); no view advance from forged traffic (`view_timeout_advances_total=0` and `view_advances_due_to_verified_tc_total=0` at T+4s); per-reason `inbound_*_rejected_*` counters frozen at the value implied by the case mix from T+4s through graceful shutdown — i.e. honest traffic never exercised any rejection path; V0 stayed alive (rc=0); and the post-injection B14 absent-leader recovery still drove 4 view advances all attributed to verified TCs, 3 fresh TimeoutCertificates, `outbound_timeout_signing_failure_total=0` on every live validator, and `committed_height 88→138` on V2A/V3A past the absent-leader plateau. Documented boundary: 3 NewView cases (`insufficient-quorum`, `mixed-view`, `high-qc-mismatch`) collapse onto the `bad_signature` counter at the live binary path because the harness uses fresh ML-DSA-44 keypairs; deterministic per-reason coverage for those cases lives in Run 030 / Run 035 unit tests. (7) An adversary-with-valid-keys harness or fake-peer transport rig that lets those 3 NewView cases fire their own per-reason live counter — **still OPEN, but no longer in the way of C5/C4 closure** because per-reason coverage is already proven deterministically. |
| **Tests** | `crates/qbind-consensus/src/timeout_verify.rs::tests::*` (Run 028); `crates/qbind-consensus/src/timeout.rs::tests::timeout_certificate_serializes_signed_timeouts`, `tests::timeout_accumulator_tc_carries_exact_signed_evidence` (Run 029); `crates/qbind-node/src/binary_consensus_loop.rs::tests::run030::*` (20 tests, Run 030); `crates/qbind-node/src/timeout_verification_bridge.rs::tests::*` (20 tests, 15 Run 031 + 5 Run 032); `crates/qbind-node/src/signer_loader.rs::tests::*` (11 tests, Run 032); `crates/qbind-node/src/peer_key_provider.rs::tests::*` (22 tests, Run 033: 6 strict-hex parser tests, 3 builder positives, 7 builder negatives, 1 fingerprint safety, 1 suite-id stability, 1 unknown-validator, 3 bridge-activation integration tests with real ML-DSA-44 keypairs, plus optional/required-mode + matching-local-entry tests); `crates/qbind-node/src/cli.rs::tests::run_033_*` (8 tests); existing T144/T145/T153 integration suites unchanged. Run 031/032/033 evidence: `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_031.md`, `_RUN_032.md`, `_RUN_033.md`. |

DevNet Evidence Run 039 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_039.md`) further narrows C4/C5 for the transport-root dependency: `pqc-static-root` Required mode now registers real ML-KEM-768 (`KEM_SUITE_ML_KEM_768 = 100`) instead of `DummyKem`, validates local `NetworkDelegationCert.leaf_kem_pk` + `--p2p-leaf-cert-key` by strict size checks and an encaps/decaps public/secret-key match, and requires explicit certified peer leaf certs (`--p2p-peer-leaf-cert VID:PATH`) before using a peer KEM public key. Run 039 real-binary evidence passed a two-node Required + `pqc-static-root` smoke and an N=4 Required + `pqc-static-root` + `--require-timeout-verification` absent-leader-shaped run with real ML-DSA-44 certs, real ML-KEM-768 KEM, active timeout verification, post-fault `committed_height 79 -> 100`, `current_view 82 -> 105`, timeout/NewView verify accepted counters increasing, and rejection/proposal/invalid-vote counters remaining zero. Negative smokes for mismatched ML-KEM secret, malformed KEM secret, and tampered cert failed closed. `DummyKem` remains available only through explicit test/dev `test-grade-dummy-sig` paths; it is not registered in `pqc-static-root`. C4 remains OPEN because `DummyAead` is still active and CA / rotation / revocation / signed root distribution remain unsolved. The Run 038 `qbind_p2p_pqc_*` live `/metrics` exposure gap remains OPEN; Run 039 uses startup logs for KEM activation proof.

DevNet Evidence Run 040 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_040.md`) further narrows C4/C5 by replacing `DummyAead` with the real `ChaCha20Poly1305Backend` on the `pqc-static-root` Required-mode binary path. `make_pqc_static_root_crypto_provider` in `crates/qbind-node/src/p2p_node_builder.rs` now registers `Arc::new(ChaCha20Poly1305Backend::new())` at suite id `AEAD_SUITE_CHACHA20_POLY1305 = 101` (no `DummyAead` registered on this provider; `provider.aead_suite(2).is_none()` is asserted directly), and `P2pNodeBuilder::build` selects `aead_suite_id = 101` whenever `pqc_active`. The startup log changes from `[Run039]` to `[Run040]` and additionally prints `transport_aead_suite_id=101 transport_aead_suite_name=chacha20-poly1305 dummy_aead_registered=false` on the pqc-static-root path (and the inverted shape `transport_aead_suite_name=dummy-aead dummy_aead_registered=true` on the explicit `test-grade-dummy-sig` default, preserving B7/B8/B12 / T138 / T143 / T160 / T222 etc. bit-for-bit). The KEMTLS key schedule (`crates/qbind-net/src/keys.rs::SessionKeys::derive`) and transcript binding are unchanged — `aead_suite_id` is already mixed into HKDF info and `key_len()` remains 32 — so a mismatched suite id between dialer and listener fails closed at handshake and at the first encrypted frame. The 12-byte `flag(1)||session_id(3)||counter(8)` nonce layout in `crates/qbind-net/src/session.rs` matches ChaCha20-Poly1305's 96-bit nonce; `AeadSession` keys are zeroized on drop. Run 040 real-binary evidence: a two-node `MutualAuthMode::Required` + `PqcRootMode::PqcStaticRoot` + real ML-DSA-44 cert + real ML-KEM-768 KEM + real ChaCha20-Poly1305 AEAD handshake completes through the production `P2pNodeBuilder` path with both sides observing each other's cert-derived `NodeId`; the `[Run040] ... transport_aead_suite_name=chacha20-poly1305 dummy_aead_registered=false` startup log was captured live. Negative coverage: AEAD wrong-key, wrong-nonce, wrong-AAD, tampered-ciphertext, tampered-tag, truncated-frame, malformed-key-length, and malformed-nonce-length all fail closed (14 `chacha20poly1305::tests::*` + 9 new R040.B tests + the existing `kemtls_encrypted_transport_tests::real_aead_detects_ciphertext_tampering`); Run 037 tampered-cert / untrusted-root / wrong-sig-suite cases still fail closed; Run 039 mismatched-ML-KEM-secret / missing-peer-leaf-cert cases still fail closed; no silent fallback to `DummyAead`, `DummyKem`, or `DummySig` in `pqc-static-root`. Tests: 14 new R040.A/B/C/D tests pass; full `qbind-node` lib **767 / 767**; `qbind-crypto` lib **68 / 68** (incl. all 14 ChaCha20-Poly1305 fail-closed tests); `qbind-net` lib **15 / 15**; existing run_037 (12), t138 (15), t139 (4), kemtls_handshake_concurrency (8), kemtls_encrypted_transport (8), t160 cluster harness (7) suites all green. **Boundary explicitly stated:** end-to-end live tampered-encrypted-frame injection on the two-node binary surface is not exercised in this run because no in-process encrypted-frame injection harness exists on the Run 037/038/039 path; AEAD authentication-failure coverage is therefore via the provider/primitive tests, which exercise the same `ChaCha20Poly1305Backend` instance the binary now registers on its `StaticCryptoProvider`. **C4 remains OPEN** for: production CA / cert rotation / cert revocation / signed root distribution lifecycle (operator-out-of-band, not solved), `qbind_p2p_pqc_*` live `/metrics` exposure (Run 038/039 OPEN gap, not addressed by Run 040 — Run 040 relies on the deterministic `[Run040]` startup log + provider-shape unit tests + the two-node handshake test as AEAD-active proof), production fast-sync/consensus-storage restore, and exponential-backoff timeout pacing. **C5's remaining transport-root dependency is now satisfied for: real ML-DSA-44 cert verification (Run 037), real ML-KEM-768 KEM (Run 039), real AEAD (Run 040), active timeout verification (Runs 031–034), and forged-traffic rejection evidence (Runs 035/036).** C5 itself remains OPEN only because lifecycle (CA / rotation / revocation / signed root distribution) is documented as a C4 piece, NOT a C5 piece — so Run 040 does NOT close C5 by fiat; closure is contingent on at least the operator-facing lifecycle policy being explicitly accepted as out of C5 scope. Operators using `--p2p-pqc-root-mode pqc-static-root` post-Run-040 are operating production-honest cert verification + production-honest transport KEM + production-honest transport AEAD, but lifecycle remains operator-out-of-band and is NOT a substitute for production CA tooling.

---

## Undocumented Implementation Details

### 1. In-Memory Slashing Ledger (T230)

| Field | Value |
|-------|-------|
| **Status** | ✅ **RESOLVED (M1/M19)** |
| **Implementation** | `crates/qbind-ledger/src/slashing_ledger.rs:713-1466` (`RocksDbSlashingLedger`) |
| **Evidence** | Persistent slashing ledger implemented via `RocksDbSlashingLedger`. Provides restart-safe persistence for validator slashing state, evidence records, and penalty history. Atomic updates via `apply_slashing_update_atomic()` using RocksDB `WriteBatch` (M1.2). Failure injection tests verify atomicity (M1.3). **M19**: Added `verify_slashing_consistency_on_startup()` for fail-closed corruption detection, explicit consensus-critical vs non-critical classification in docs, and comprehensive M19 test suite. Whitepaper Section 16.8 updated with full persistence model. |
| **Tests** | `crates/qbind-node/tests/m1_slashing_persistence_tests.rs`, `crates/qbind-ledger/tests/slashing_ledger_tests.rs`, `crates/qbind-node/tests/m19_slashing_persistence_canonicalization_tests.rs` |

### 2. Vote History Memory Limits

| Field | Value |
|-------|-------|
| **Status** | ✅ **RESOLVED (M20)** |
| **Implementation** | `crates/qbind-consensus/src/hotstuff_state_engine.rs:107-122` (`votes_by_view`, eviction tracking) |
| **Whitepaper Reference** | Section 8.4.1 (Vote History Retention) |
| **Description** | The vote history (`votes_by_view` HashMap) is bounded with memory limits and eviction (`evict_votes_by_view_if_needed()`). Whitepaper Section 8.4.1 now documents that equivocation detection is best-effort over a sliding window and very old views may no longer be checked. |
| **Security Impact** | Protocol safety unchanged (locking rule and QC formation guarantee safety independent of historical vote tracking). Very old double-votes may not be slashable if their view has been evicted—acceptable trade-off to prevent unbounded memory growth. |

### 3. CRC-32 Checksum for Storage Integrity

| Field | Value |
|-------|-------|
| **Status** | ✅ **RESOLVED (M20)** |
| **Implementation** | `crates/qbind-node/src/storage.rs:256-336` (`compute_crc32()`, `wrap_checksummed()`, `unwrap_checksummed()`) |
| **Whitepaper Reference** | Section 10.5.1 (Storage Integrity Checksums) |
| **Description** | Storage uses CRC-32 checksums (IEEE 802.3 polynomial) for integrity detection. Whitepaper Section 10.5.1 now documents that checksums detect bit-rot and accidental corruption but are non-cryptographic; adversarial tampering must be handled at higher layers (signatures, QCs, consensus verification). |

### 4. Epoch Transition Write Ordering

| Field | Value |
|-------|-------|
| **Status** | ✅ **RESOLVED (M12, M16)** |
| **Implementation** | `crates/qbind-node/src/storage.rs`, Whitepaper Section 18.4.3 |
| **Evidence** | Epoch transition write ordering and persistence semantics formally specified in Whitepaper Section 18 "Validator Set Transition and Epoch Boundary Semantics" (M12). Section 18.4.3 defines persistence ordering: storage commits before in-memory state update. **M16**: Crash-window elimination now proven by 14 tests with failure injection. All epoch-boundary writes (block, QC, last_committed, epoch) commit atomically via RocksDB WriteBatch. `EpochTransitionMarker` detects incomplete transitions on startup with fail-closed behavior. |

### 5. Key Rotation Grace Period Semantics

| Field | Value |
|-------|-------|
| **Status** | ✅ **RESOLVED (M20)** |
| **Implementation** | `crates/qbind-consensus/src/key_rotation.rs:238-298` |
| **Whitepaper Reference** | Section 9.8 (Key Rotation Semantics) |
| **Description** | Key rotation uses an epoch-aligned grace period where both old and new keys are valid during transition. Whitepaper Section 9.8 now documents: (1) partial-epoch rotations not supported, (2) both keys valid for grace epoch, (3) security trade-off of simpler reasoning vs. slower key turnover. |

### 6. DAG Mempool Batch Priority

| Field | Value |
|-------|-------|
| **Status** | ✅ **RESOLVED (M20)** |
| **Implementation** | `crates/qbind-node/src/dag_mempool.rs`, `crates/qbind-node/src/mempool.rs` |
| **Whitepaper Reference** | Section 7.1.1 (Mempool Ordering Semantics) |
| **Description** | Mempool priority uses `arrival_id` which differs per node. Whitepaper Section 7.1.1 now documents: (1) batch selection deterministic per node but depends on local arrival order, (2) transaction ordering within blocks is proposer-determined, not globally canonical, (3) consensus safety unaffected—economic fairness (block inclusion) is proposer-local. |

### 7. Timeout Exponential Backoff Parameters

| Field | Value |
|-------|-------|
| **Status** | ✅ **RESOLVED (M20)** |
| **Implementation** | `crates/qbind-consensus/src/pacemaker.rs:74-109` (`TimeoutPacemakerConfig` with `timeout_multiplier: 2.0`, `max_timeout: 30s`) |
| **Whitepaper Reference** | Section 17.3.1 (Exponential Backoff Parameters) |
| **Description** | The timeout pacemaker uses configurable exponential backoff. Whitepaper Section 17.3.1 now documents: (1) default multiplier 2.0× and max timeout 30 seconds, (2) parameters are environment-tunable, (3) must remain bounded and deterministic for safety and cross-node consistency. |

### 8. Evidence Signature Verification Not Performed

| Field | Value |
|-------|-------|
| **Status** | ✅ **RESOLVED (M9/M11)** |
| **Implementation** | `crates/qbind-consensus/src/slashing/mod.rs:579-594` (`verify_ml_dsa_44_signature()`), lines 664-682 (O1 verification), 762-769 (O2 verification), 860-865 (O3 verification), 945-951 (O4 verification) |
| **Evidence** | Cryptographic signature verification IS implemented for all offense types. `verify_ml_dsa_44_signature()` performs ML-DSA-44 signature verification on evidence payloads. O1 verifies both block signatures; O2 verifies header signature invalidity; O3 verifies vote signature; O4 verifies certificate signatures. Deterministic verification with fail-closed behavior on non-verifiable evidence. |

### 9. Governance Slashing Parameters Not Connected

| Field | Value |
|-------|-------|
| **Status** | ✅ **RESOLVED (M14)** |
| **Implementation** | `crates/qbind-types/src/state_governance.rs:44-117` (`SlashingPenaltySchedule` struct with O1-O5 parameters), `crates/qbind-consensus/src/slashing/mod.rs:2260-2370` (`PenaltyEngineConfig::from_governance_schedule()`, `GovernanceSlashingSchedule` type) |
| **Whitepaper Reference** | Section 11 (Governance) |
| **Description** | `ParamRegistry` now contains a canonical `SlashingPenaltySchedule` with all O1-O5 penalty parameters. `PenaltyEngineConfig::from_governance_schedule()` creates engine configurations directly from governance state. Slashing parameters are governed and upgradeable with epoch-boundary activation semantics. |
| **Evidence** | M14 implementation: (1) `SlashingPenaltySchedule` stores slash_bps and jail_epochs for O1-O5 plus activation_epoch, (2) `GovernanceSlashingSchedule` provides interface between types crate and consensus crate, (3) `PenaltyEngineConfig::from_governance_schedule()` constructs configs deterministically, (4) DevNet allows fallback defaults; TestNet/MainNet require schedule presence (fail-closed). |
| **Tests** | `crates/qbind-node/tests/m14_governance_slashing_params_tests.rs` (15 tests): deterministic schedule load (A1-A3), epoch-boundary activation (B1-B3), fail-closed behavior (C1-C5), O1-O5 regression (D1-D4). |

### 10. Stake Synchronization Gap

| Field | Value |
|-------|-------|
| **Status** | ✅ **PARTIAL (M13)** |
| **Implementation** | `crates/qbind-types/src/state_validator.rs:31-102` (`ValidatorRecord` with canonical fields), `crates/qbind-ledger/src/slashing_ledger.rs:1-50` (M13 documentation) |
| **Evidence** | Canonical economic state unified (M13). `ValidatorRecord.stake` and `ValidatorRecord.jailed_until_epoch` are the single source of truth. `ValidatorSlashingState` mirrors but is documented as non-authoritative. Eligibility predicates (`ValidatorRecord::is_eligible_at_epoch()`) read from canonical fields. |
| **Tests** | `crates/qbind-node/tests/m13_economic_state_unification_tests.rs` (12 tests) |
| **Remaining** | • Full architectural unification (single-write path for slashing penalties to both `ValidatorRecord` and `ValidatorSlashingState`) |

---

## Summary

| Category | Count | Items |
|----------|-------|-------|
| **RESOLVED** | 12 | C1, C2, Item 1, Item 2 (M20), Item 3 (M20), Item 4, Item 5 (M20), Item 6 (M20), Item 7 (M20), Item 8, Item 9 (M14), Item 10 (partial) |
| **OPEN** | 2 | C3, C4 |

**High-Risk Open Items**: C4 (Production binary does not yet boot a fully operating node) — DevNet readiness items B1, B2, B3, B5, **B6 (multi-validator P2P binary-path interconnect)**, **B7 (binary-path test-grade KEMTLS bring-up + dialer-side peer-validator identity closure)**, and **B8 (listener-side test-grade validator-identity closure + bounded initial-dial retry)** have all landed; the next multi-validator binary-path DevNet evidence run (Run 007) is justified, since both sides of a stagger-started two-`qbind-node` cluster can now end up with the *other's* deterministic NodeId among their `connected_peers()`, so `send_to(ValidatorId)` resolves to a registered transport session in both directions. Remaining sub-items are multi-validator restore catchup, mutual-auth (`MutualAuthMode::Required`) cryptographic peer-identity binding (the B8 resolver is test-grade self-asserted), and production PQC KEMTLS / certificate hardening. Protocol-correctness claims remain unaffected (those are test-evidenced).

**Medium-Risk Open Items**:
- C3 (Reporter Rewards): No economic incentive for evidence reporting. Whitepaper Section 12.2.3 documents this as future work; M15 hardened evidence pipeline is the baseline for any future reward implementation.

---

## Update History

| Date | Change | Author |
|------|--------|--------|
| 2026-02-11 | Initial document created during state audit | Audit |
| 2026-02-11 | Added C1-C3 contradictions, items 8-10 undocumented details from validator economics audit | Audit |
| 2026-02-15 | M12: Added formal validator set transition spec (Section 18). Items 4 (Epoch Transition Write Ordering) now documented in Section 18.4.3. C2 (Minimum Stake) partially addressed by M2 epoch-boundary filtering; registration-time validation still pending. | M12 |
| 2026-02-15 | M13: Canonical economic state unified. Item 10 (Stake Synchronization Gap) partially mitigated - canonical source defined (`ValidatorRecord.stake`, `ValidatorRecord.jailed_until_epoch`). `ValidatorSlashingState` documented as mirror. 12 restart safety tests added. | M13 |
| 2026-02-15 | M13.1: Full reconciliation pass. Updated all entries with Status/Evidence fields. Verified: C1 RESOLVED (M9/M11 O3-O5 penalties implemented), C2 RESOLVED (M2 minimum stake enforced), Item 1 RESOLVED (M1 RocksDbSlashingLedger), Item 8 RESOLVED (signature verification implemented). Updated line references and test citations. | M13.1 |
| 2026-02-15 | M14: Governance slashing parameters wired into penalty engine. Item 9 RESOLVED. `SlashingPenaltySchedule` added to `ParamRegistry` with O1-O5 penalty parameters + activation_epoch. `PenaltyEngineConfig::from_governance_schedule()` provides deterministic config from governance state. DevNet allows fallback; TestNet/MainNet fail-closed on missing schedule. 15 M14 tests added. | M14 |
| 2026-02-15 | M16: Epoch transition hardening completed. Item 4 updated with crash-window elimination. `EpochTransitionBatch` and `apply_epoch_transition_atomic()` implement atomic RocksDB WriteBatch for all epoch-boundary writes. `EpochTransitionMarker` detects incomplete transitions on startup. 14 M16 tests added with failure injection. Spec Gap 2.6 in QBIND_PROTOCOL_REPORT.md now Mitigated. | M16 |
| 2026-02-15 | M17: Formal slashing penalty schedule added to whitepaper. Section 12.2 updated with: (1) O1-O5 penalty table (offense, evidence type, verification rule, slash bps, jail epochs), (2) governance/activation semantics paragraphs (SlashingPenaltySchedule in ParamRegistry, epoch-boundary activation, fail-closed behavior). Protocol Report Spec Gap 2.5 status changed to "✅ Mitigated (spec added M17)", risk level reduced from Medium to Low. C3 (Reporter Rewards) remains OPEN unchanged. | M17 |
| 2026-02-16 | M19: Slashing state persistence and canonicalization hardening. Item 1 updated to M1/M19 RESOLVED with `verify_slashing_consistency_on_startup()` for fail-closed corruption detection. Whitepaper Section 16.8 updated with full persistence model (consensus-critical vs non-critical classification, atomic update guarantees, fail-closed behavior). `SlashingStateCorrupt` error variant added. `ValidatorSlashingState` documentation enhanced with M19 NON-AUTHORITATIVE warnings. Protocol Report section 3.15 added. 8 M19 tests added. | M19 |
| 2026-02-16 | M20: Documentation hardening milestone (no Rust changes). Closed Items 2, 3, 5, 6, 7. Whitepaper updates: Section 8.4.1 (vote history retention), Section 10.5.1 (CRC-32 checksums), Section 9.8 (key rotation semantics), Section 7.1.1 (mempool ordering), Section 17.3.1 (timeout backoff parameters), Section 12.2.3 (reporter incentives - future work). C3 remains OPEN by design—no tokenomics yet; updated with M15 hardening reference and new whitepaper note. | M20 |
| 2026-05-03 | EXE-1: Repo/code↔doc alignment audit. Added C4 (Production `qbind-node` binary does not boot a fully operating node) — covers binary lacking consensus driver, `/metrics` HTTP not spawned by binary, and missing restore-from-snapshot path. Recorded as the top three execution blockers (B1/B2/B3) in `docs/protocol/QBIND_REPO_CODE_DOC_ALIGNMENT_AUDIT.md`. Protocol-correctness items remain unchanged; gap is at the binary/operability layer. | EXE-1 |
| 2026-05-03 | B3: Restore-from-snapshot startup path landed in `qbind-node`. New `crates/qbind-node/src/snapshot_restore.rs` validates a snapshot via the existing `validate_snapshot_dir` (T215 `meta.json + state/` format), refuses to overwrite a populated `<data_dir>/state_vm_v0`, copies the RocksDB checkpoint into place, and writes a `RESTORED_FROM_SNAPSHOT.json` audit marker; `--restore-from-snapshot <PATH>` CLI flag wired in `crates/qbind-node/src/cli.rs`; `main.rs` calls the restore path before consensus startup and exits non-zero with a precise reason on any failure. Regression-tested by `crates/qbind-node/tests/b3_snapshot_restore_tests.rs` (10 tests, including a restore-then-reopen-and-observe proof). C4 status updated: B1+B2+B3 all landed; multi-validator P2P binary-path interconnect remains the residual sub-item. | B3 |
| 2026-05-04 | B6: Multi-validator P2P binary-path consensus interconnect landed in `qbind-node`. Two pre-existing wiring gaps closed: (i) `crates/qbind-node/src/p2p_node_builder.rs` no longer plugs a freshly-allocated, immediately-dropped `mpsc::channel::<P2pMessage>` into the inbound demuxer — it now calls `p2p_service.subscribe().await` so the demuxer receives the real inbound stream; (ii) `crates/qbind-node/src/binary_consensus_loop.rs` now exposes a `BinaryConsensusLoopIo { inbound_rx, outbound }` surface and `run_binary_consensus_loop_with_io` / `spawn_binary_consensus_loop_with_io` variants that decode `ConsensusNetMsg::{Proposal, Vote}` and feed them into `BasicHotStuffEngine::on_proposal_event` / `on_vote_event`, forwarding engine-emitted `ConsensusEngineAction`s back through an existing `ConsensusNetworkFacade` (the real `P2pConsensusNetwork`). `crates/qbind-node/src/main.rs::run_p2p_node` wires a `ChannelConsensusHandler` onto the demuxer and threads its receiver + a `P2pConsensusNetwork` outbound facade into the loop. The single-validator / LocalMesh path is preserved via the existing 4-arg `run_binary_consensus_loop` / `spawn_binary_consensus_loop` entry points (which delegate to the new variant with `io=None`). Regression-tested by `crates/qbind-node/tests/c4_b6_p2p_binary_path_interconnect_tests.rs` (5 tests: inbound proposal → engine vote, two-engine cross-wired binary-path progression past view 0, single-validator io=None unchanged, inbound-channel close survival, non-leader proposal not silently dropped). C4 status updated: B1+B2+B3+B5+B6 all landed; the first multi-validator binary-path DevNet evidence run is unblocked. Multi-validator restore catchup, validator/node-id identity hardening for production, and real PQC KEMTLS handshake on the binary path remain open. | B6 |
| 2026-05-04 | B7: Binary-path test-grade KEMTLS bring-up + dialer-side peer-validator identity closure landed in `qbind-node`. Smallest honest fix to the negative finding of DevNet Evidence Run 005 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_005.md` §11 #2–#3, §13). Three concrete changes: (i) test-grade KEM keypair derivation centralized in `derive_test_kem_keypair_from_validator_id(vid)` (single source of truth shared by listener + dialer); (ii) local NodeId, `SimpleValidatorNodeMapping` NodeId, and dialer outbound NodeId all unified on `sha3_256_tagged("QBIND:nodeid:v1", test_kem_pk(vid))` so `send_to(ValidatorId)` resolves to the actually-registered connection; (iii) `--p2p-peer` extended with `vid@addr` syntax (`parse_peer_spec`), and per-peer `peer_kem_pk` + expected-`validator_id` overrides threaded into `TcpKemTlsP2pService::dial_peer` via `set_peer_kem_pk_overrides` / `set_peer_validator_id_overrides` so each side dials with the *peer's* test KEM pk + expected validator id (the latter satisfies the `delegation_cert.validator_id == client_init.validator_id` check in `qbind_net::handshake::handle_server_accept`). A latent inbound-side bug also fixed: `TcpStream::into_std()` in `handle_inbound_connection` now restores blocking mode before `accept_kemtls_async` (the KEMTLS handshake does blocking reads, but the tokio-derived std stream was non-blocking → `WouldBlock` on first read, dropping every inbound session). A new `PeerValidatorMap` is populated at dial-config time on `P2pNodeContext` to bind the connected peer to its `ValidatorId(N)` on the dialer side. Multi-validator runs that pass bare-addr peers without `vid@` are rejected with a clear configuration error rather than silently producing a broken handshake. Files touched: `crates/qbind-node/src/p2p_node_builder.rs`, `crates/qbind-node/src/p2p_tcp.rs`, `crates/qbind-node/src/consensus_net_p2p.rs`. Regression-tested by `crates/qbind-node/tests/b7_kemtls_bringup_identity_closure_tests.rs` (5 integration tests: dialer/listener KEM-pk agreement, dialer-side identity closure to deterministic NodeId, **bounded two-real-binary KEMTLS handshake completion proof — the exact case Run 005 failed to demonstrate**, parser surface, single-validator no-regression) plus 6 new `p2p_node_builder::tests::b7_*` unit tests (including a bare-addr-peer rejection test and a malformed-spec rejection test). All 604 lib tests + all integration tests pass (the only failure observed was a pre-existing, unrelated compile error in `m16_epoch_transition_hardening_tests` against `RocksDbConsensusStorage`). C4 status updated: the "binary-path test-grade KEMTLS bring-up — empirically blocked (Run 005)" item is now closed for the dialer side; inbound-side cryptographic peer-identity binding (mutual KEMTLS auth) is the strictly smaller residual item, kept honest. The next multi-validator binary-path evidence run (Run-005 shape) is justified. | B7 |
| 2026-05-04 | B8: Listener-side test-grade validator-identity closure + bounded initial-dial retry landed in `qbind-node`. Smallest honest fix to the two negative findings of DevNet Evidence Run 006 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_006.md` §10–§13): (a) listener-side accepted sessions previously admitted under a temporary session NodeId rather than the deterministic peer NodeId, so V0's `send_to(ValidatorId(1))` could not resolve to the V1→V0 inbound session; (b) initial outbound dial losing the startup race with `Connection refused` was not retried, so a stagger-started two-node cluster ended up with a one-direction-only TCP topology. **Three concrete changes:** (i) **Listener-side identity closure (test-grade).** New `accept_kemtls_async_with_peer_init` in `crates/qbind-node/src/secure_channel.rs` surfaces `ClientInit.{client_random, validator_id}` from the server-side accept path via `qbind_net::unpack_client_init`. New `InboundIdentityResolver` + `set_inbound_identity_resolver` on `TcpKemTlsP2pService` in `crates/qbind-node/src/p2p_tcp.rs`; `handle_inbound_connection` consults the resolver and, if it returns `Some(node_id)`, registers the accepted session under that NodeId (otherwise legacy temporary-session-NodeId path preserved exactly — no silent override). `crates/qbind-node/src/p2p_node_builder.rs` installs a resolver that recovers the dialer's vid from the deterministic `qbind-client-<N>` ASCII prefix `create_connection_configs` already places in `client_random` (parsed by new `parse_test_validator_id_from_client_random` helper) and returns `derive_test_node_id_from_validator_id(N)` — the SAME NodeId the dialer side registers under. (ii) **Bounded initial-dial retry.** New `DialRetryPolicy` (default 8 attempts × {100..1000}ms ≈ 5.5s) and `set_dial_retry_policy`. `start()` no longer blocks on dial; instead it spawns a per-peer background `DialerHandle::dial_with_retry` task that retries only transient TCP errors (`ConnectionRefused`, `ConnectionReset`, `ConnectionAborted`, `TimedOut`, `AddrNotAvailable`, `NotFound`) and bounds attempts + backoff. KEMTLS-handshake errors and configuration errors are NOT retried. `shutdown()` aborts in-flight dial tasks before peer connections. Single-validator / no-static-peers / LocalMesh paths are bit-equivalent (no dial tasks spawned). (iii) **Honest scope.** The resolver consumes `client_random` under `MutualAuthMode::Disabled` and is therefore self-asserted by the dialer at the protocol level — a malicious dialer could spoof a vid in `client_random`. Strictly cryptographic inbound peer-identity binding still requires the existing `MutualAuthMode::Required` codepaths in `qbind-net` and is kept as a separate `Remaining` bullet (mutual-auth (`MutualAuthMode::Required`) cryptographic peer-identity binding — B8-residual). Files touched: `crates/qbind-node/src/secure_channel.rs`, `crates/qbind-node/src/p2p_tcp.rs`, `crates/qbind-node/src/p2p_node_builder.rs`. Regression-tested by `crates/qbind-node/tests/b8_listener_identity_closure_and_dial_retry_tests.rs` (6 tests: parse round-trip, listener-side deterministic-NodeId binding, **initial-dial retry recovery from stagger — the exact recovery DevNet Evidence Run 006 reported missing**, two-node bidirectional `connected_peers` precondition, single-validator no-regression, `DialRetryPolicy::no_retry` pin). All previously landed paths verified non-regressed: 5/5 b7 tests, 5/5 c4_b6 tests, 16/16 three_node_kemtls_integration tests, 9/9 t172 P2P smoke tests, 10/10 t175 wiring tests, 8/8 async kemtls integration tests, peer/manager roundtrip tests, 604/604 lib unit tests. C4 status updated: the Run 006 listener-side temp-NodeId binding and initial-dial-retry residual items are now closed at test-grade; mutual-auth cryptographic peer-identity binding is the strictly smaller residual item, kept honest. **DevNet Evidence Run 007 is justified** — both sides of a two-real-binary stagger-started cluster can now end up with the other's deterministic NodeId among their `connected_peers()`, so the test-grade transport precondition for first cross-node `ConsensusNetMsg::{Proposal, Vote}` traffic now holds. | B8 |
| 2026-05-06 | DevNet Evidence Run 007 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_007.md`): third multi-validator binary-path DevNet evidence exercise, on the post-B8 binary. Both sides of the two-`qbind-node` cluster registered each other under the deterministic NodeId for the first time (B8 listener-side identity-resolver line and B8 bounded initial-dial retry trace both observable on the binary), but `ConsensusNetMsg` traffic still did not cross between binaries: V0's leader-issued view-0 `BroadcastProposal` was emitted at consensus-loop start before V1's TCP→V0 inbound was established, and the engine did not re-emit on late peer connect (`consensus_net_outbound_total{kind="proposal_broadcast"} = 0`, `inbound_proposals = 0`, `qbind_consensus_proposals_total{result="accepted"} = 0`, `qcs_formed_total = 0`, `committed_height = None` on both nodes across three `/metrics` scrapes). New residual recorded as the engine-broadcast layer (strictly below the Run-006 listener-identity boundary that B8 closed). `/metrics` and shutdown remained honest; single-validator LocalMesh regression check on the same binary observed `commits = 345`, `committed_height = Some(344)`, `view = 347`, no regression of B1/B2. | Run 007 |
| 2026-05-06 | B9: Leader-side re-emission of `BroadcastProposal` on late peer connect within the same view landed in `qbind-node`. Smallest honest fix to the negative finding of Run 007. The fix is bounded entirely to the binary-path consensus loop (no engine redesign, no transport redesign): `BinaryConsensusLoopIo` gains an optional `peer_connectivity: Option<Arc<dyn PeerConnectivitySource>>` (a tiny dedicated trait — strictly smaller than `P2pService` — implemented by the new `P2pServicePeerConnectivity` adapter that forwards to the same `Arc<dyn P2pService>` already backing inbound demuxing and outbound `P2pConsensusNetwork`); `do_leader_tick` caches the leader's last `BroadcastProposal` together with the view it was emitted for; on every tick `maybe_reemit_on_late_peer_connect` runs through seven explicit boundedness gates (transition required, cached proposal required, view match, leader of current view, commit not invalidated, per-view single-shot latch, outbound facade required) and on all-gates-pass calls `outbound.broadcast_proposal(&cached)` exactly once, incrementing the new `BinaryConsensusLoopInboundStats::outbound_proposal_late_peer_reemits` counter (also surfaced in the `Loop exit:` summary). Reconnect churn within the same view produces zero additional re-emits; a view change between cache-fill and the connectivity poll invalidates the cache; when `peer_connectivity = None` (single-validator P2P, LocalMesh, the `io = None` 4-arg loop) the entire B9 path is skipped — bit-equivalent to pre-B9. Files touched: `crates/qbind-node/src/binary_consensus_loop.rs`, `crates/qbind-node/src/main.rs`. Regression-tested by `crates/qbind-node/tests/b9_late_peer_connect_proposal_reemit_tests.rs` (6 tests covering exactly-one re-emit on late connect, per-view single-shot under reconnect churn, view-change invalidation, pre-connected peer bounded, `peer_connectivity = None` regression guard, `io = None` LocalMesh regression guard). C4 status updated: the Run-007 engine-broadcast residual is closed at the binary-path level, making the Run-008 precondition reachable. The B9 fix does NOT redesign consensus, does NOT redesign transport, does NOT replace B6/B7/B8, does NOT broaden into mutual-auth PKI or production identity hardening. | B9 |
| 2026-05-06 | DevNet Evidence Run 008 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_008.md`): first post-B9 multi-validator binary-path DevNet evidence run on real `qbind-node` processes. With V0 started ≈ 12 s before V1 (deliberate late-peer-connect shape), the leader's stderr emitted exactly one `B9: re-emitted view 0 BroadcastProposal after late peer connect (newly_connected_peers=1, reemits_total=1)` line; the leader-side loop-exit summary reported `proposals=1 outbound_proposals=1 outbound_proposal_late_peer_reemits=1 inbound_votes=1`, and the peer-side loop-exit summary reported `inbound_msgs=1 inbound_proposals=1 outbound_votes=1` — i.e. for the first time in DevNet evidence history a `ConsensusNetMsg::Proposal` and a return `ConsensusNetMsg::Vote` actually crossed between two distinct `qbind-node` binaries. However engine-acceptance counters did not advance to QC formation: `qbind_consensus_proposals_total{result="accepted"} = 0` on the receiver, `qbind_consensus_votes_total{result="accepted"} = 0` on the leader, `qbind_consensus_qcs_formed_total = 0` on both, and `committed_height = None` on both nodes across three `/metrics` scrapes. New residual recorded as the engine-accept / QC-formation layer (B9-residual, strictly below the Run-007 boundary). One non-progression-blocking metric-coverage gap surfaced for the first time on the `consensus_net_outbound_total{kind="proposal_broadcast"}` / `…{kind="vote_broadcast"}` / `consensus_net_inbound_total{kind="proposal"}` / `…{kind="vote"}` Prometheus counters (loop counters honestly recorded `1` of each on the wire while the Prometheus family stayed at `0` — under-report, not over-report). `/metrics` and shutdown remained honest; single-validator LocalMesh regression check on the same binary observed `proposals=602 commits=600 committed_height=Some(599) view=602 late_peer_reemit=off`, confirming no regression of B1/B2 and the bit-equivalence of the `peer_connectivity = None` path that B9 was bounded against. | Run 008 |
| 2026-05-06 | B10: Engine accept / QC-formation closure on the binary path under post-B9 re-emitted leader proposal landed in `qbind-node`. Smallest honest fix to the negative finding of Run 008. Three additive changes to `crates/qbind-node/src/binary_consensus_loop.rs`: (i) the leader-step tick now caches **both** the just-emitted `BroadcastProposal` AND the leader's paired same-view `BroadcastVote` (the engine's `on_leader_step` always emits the proposal immediately followed by the leader's self-vote); the existing B9 late-peer-connect helper now re-emits both under the same per-view single-shot latch on the same tick, only when the cached vote's view matches the cached proposal's view. Without the paired vote re-emit a 2-validator setup deterministically stalls in the exact Run-008 shape. (ii) The loop now wires an `Arc<dyn ConsensusProgressRecorder>` adapter (`NodeMetricsProgressRecorder`) onto the engine via `BasicHotStuffEngine::set_progress_recorder` before the first tick. Pre-B10 the binary-path loop never installed any progress recorder, so even when the engine genuinely formed a QC (as V0 actually did in Run 008's view 0) the `qbind_consensus_qcs_formed_total` counter on `/metrics` stayed at 0 — metric-coverage closure, not fabrication: every increment continues to come from the engine's existing `record_qc_formed` call site on actually-formed QCs. (iii) The inbound-message handler now increments `consensus_t154.proposals_accepted` on each `engine.on_proposal_event` that returns `Some(action)` and `consensus_t154.votes_accepted` on each `engine.on_vote_event` that returns `Ok(_)`; new `BinaryConsensusLoopInboundStats` fields `outbound_vote_late_peer_reemits`, `inbound_proposals_engine_accepted`, `inbound_votes_engine_accepted`, `qcs_formed_total` mirror these on the loop-exit summary. The B9 single-shot latch now covers the proposal+vote pair: at most one paired re-emit per view, never one without the other, no replay across reconnect churn. Regression-tested by `crates/qbind-node/tests/b10_engine_acceptance_qc_closure_tests.rs` (5 tests including in-tree reproduction of the Run-008 partial-positive shape via a `SelectiveConnectFacade` and post-fix engine-acceptance / QC closure proof on both sides). All previously-landed B1/B2/B3/B5/B6/B7/B8/B9 tests continue to pass. C4 status updated: the Run-008 boundary is closed at the engine accept / QC formation layer for the test-grade binary-path; empirical proof on real `qbind-node` processes is what Run 009 is now positioned to provide. | B10 |
| 2026-05-06 | DevNet Evidence Run 009 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_009.md`): first post-B10 multi-validator binary-path DevNet evidence run. Same Run-008 deliberate late-peer-connect topology (V0 leader, ≈ 12 s stagger, V1 dials V0 second) on the post-B10 binary. **Empirical strongest-positive result on real `qbind-node` binaries:** sustained cross-node QC formation on both V0 and V1 (`qbind_consensus_qcs_formed_total` 358/750/1 062 on V0, 359/751/1 063 on V1 across three monotonic `/metrics` scrapes), B9+B10 paired re-emit fires exactly once on V0 (`B9+B10: re-emitted view 0 BroadcastProposal + BroadcastVote after late peer connect (newly_connected_peers=1, proposal_reemits_total=1, vote_reemits_total=1)`; `outbound_proposal_late_peer_reemits=1` on V0 only and `=0` on V1 across ≈ 1 500 subsequent views), and `committed_height > None` on both nodes for the first time in DevNet evidence history (V0 `commits=1 454 committed_height=Some(1 453) view=1 456`, V1 `commits=1 453 committed_height=Some(1 452) view=1 455` at clean shutdown). All twelve task-brief §7 strongest-positive checklist items are satisfied on the binary path with the single carried-forward honest under-report on `consensus_net_*_total` (Prometheus stays at 0 while loop counters honestly record 728 proposals × 2 directions and 1 456 votes × 2 directions — under-report, not over-report; recorded again in `Remaining` below, unchanged from Run 008). Independent single-validator LocalMesh regression check on the same binary observed `proposals=384 commits=382 committed_height=Some(381) view=384 interconnect=none late_peer_reemit=off`, confirming no regression of B1/B2 and the bit-equivalence of the `peer_connectivity = None` path that B9/B10 were bounded against. C4 narrowed: the "engine-side acceptance / QC-formation closure on the binary path under post-B9 re-emitted leader proposal (B9-residual)" `Remaining` bullet is updated to record empirical closure on real binaries; other `Remaining` bullets unchanged. **Documentation hygiene note:** the header `Version: 1.3` and `Date: 2026-05-03` of this document are stale relative to the body (which has substantive narrowings through B6/B7/B8/B9/B10 and Runs 005–009) and the Update History (which had no rows after 2026-05-04 / B8 prior to this row). Run 009 explicitly does not silently bump the header; the hygiene fix (header bump + Update-History rows for B9, B10, Run 007, Run 008) is recommended as a separate documentation-only delta to keep the mismatch surfaced rather than absorbing it into an evidence-run delta. | Run 009 |
| 2026-05-07 | Documentation-hygiene pass on this file (no code touched, no contradiction-row substance changed, no body wording rewritten). Header `Version` bumped 1.3 → 1.4 and header `Date` bumped 2026-05-03 → 2026-05-06 to reflect the latest substantive update already represented in the body (Run 009). Update History rows added for Run 007, B9, Run 008, B10 — all four were substantively present in the C4 body (Description / Impact / Remaining / Tracking) but had no corresponding Update-History entries, exactly the mismatch surfaced by `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_009.md` §14 ("the hygiene fix … is recommended as a separate documentation-only delta to keep the mismatch surfaced rather than absorbing it into an evidence-run delta"). The new rows summarize material already present in the body and add no new technical claims. Run 005 and Run 006 are intentionally not added as new rows — both are already substantively recorded in C4's Description / Impact text and were not listed in the Run-009 §14 hygiene-fix recommendation. The pre-existing 2026-05-06 Run 009 row's "Documentation hygiene note" paragraph is left unchanged for historical traceability — it accurately records the state of the document at the moment Run 009 was written; this row records the resolution. | Docs Hygiene |

| 2026-05-08 | Run 018: Narrow quorum-restoration follow-up to Run 017. Reproduced the two-of-four sub-quorum window honestly on the real binary path, then restored a third validator (V2C) via the explicit `--restore-from-snapshot` path so a numerically-compatible `2f+1` alive set existed. Result: B14 timeout certificates did **not** form, `current_view` did not advance, normal proposal/vote/QC/commit did not resume. The narrow new boundary isolated by Run 018 is the interaction between the B13 strict-progress-above-baseline exit predicate (`local_committed_height > snapshot_baseline_height`) and a snapshot anchor whose responder is itself parked at the snapshot height — both restored validators stay in `mode_active=1` indefinitely, are gated out of B14 emission, and therefore cannot contribute to a `2f+1` timeout cohort. C4 status row updated to record this narrowing of the B13 sub-item. The boundary is operationally mitigable (capture the snapshot anchor only after the live cluster has committed at least one block above the captured anchor) and does **not** affect the already-proven Run-016 three-of-four B14 recovery shape. No QBIND code changed by this entry. | Run 018 |
| 2026-05-08 | DevNet Evidence Run 019 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_019.md`): narrow disambiguation of the Run-018 B13 strict-progress boundary on the same binary (sha256 `8479f426…3388`, BuildID `e8cecf09…`). Same N=4 Required-mode shape as Run 018 with the single deliberate difference that V0 was started first so the live cluster committed strictly above the captured snapshot anchor before fault injection. Result: **strongest-positive closure** of the B13 operational sub-item Run 018 narrowed. Snapshot anchor `S=5` captured from V0 `/metrics`; V0 advanced to `H=8` post-fault; V1B `--restore-from-snapshot` applied 5 peer-learned certified blocks via the real binary path, raised `committed_height` from 5 → 8, satisfied the B13 strict-progress predicate `8 > 5`, and emitted `[restore-catchup] exit: caught up to peer anchor — local committed_height=8 peer_max_observed=Some(8)` (`mode_active=1→0`, `mode_exited_at_height=0→8`). V2C restored from the same anchor exited B13 identically. With three of four alive and **both** restored validators in normal participation, B14 cleared the absent-V3 plateau exactly as Run 016 proved it should: `view_timeouts_emitted_total=21`, `timeout_certificates_formed_total=20` on V0 (18 on V1B, 20 on V2C), `outbound_new_views_sent_total=20`, `view_timeout_advances_total=21`, `view_timeout_decode_failures_total=0`, `view_timeout_engine_rejects_total=0`. `current_view` advanced 11 → 95 on all three nodes; `committed_height` advanced 8 → 71 on all three nodes (63 commits, 84 view advances) without operator intervention. C4 status row updated to record positive closure of the Run-018 B13 narrowing under the documented stagger; this is a sharpening, not a new contradiction. The broader open C4 sub-items (production fast-sync / consensus-storage restore, signature verification of `TimeoutMsg`/`TimeoutCertificate`, exponential-backoff timeout pacing, production PQC KEMTLS root-key distribution) remain open exactly as before. No QBIND code changed by this entry; no previously landed binary-path capability appears regressed. | Run 019 |
| 2026-05-09 | DevNet Evidence Run 020 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_020.md`): first production-grade snapshot/restore evidence run on the same binary (sha256 `8479f426…3388`, byte-for-byte identical to Run 019). Same N=4 Required-mode topology, V0-first stagger, and V1B/V2C restore shape as Run 019, with the single deliberate substrate change that the snapshot is now a **real RocksDB checkpoint** produced by the supported `RocksDbAccountState::create_snapshot` path (`crates/qbind-ledger/src/execution.rs` lines 1571–1675; same impl exercised by `crates/qbind-ledger/tests/t215_state_snapshot_tests.rs` and the canonical `build_real_snapshot` helper in `crates/qbind-node/tests/b3_snapshot_restore_tests.rs`). The snapshot directory `snap1/` contains real `CURRENT`, `MANIFEST-000013`, `000009.sst`, `OPTIONS-000015` files — not placeholder content. Result: **B3 restore-from-snapshot, B5 restore-aware consensus start, B13 catchup + exit, B14 absent-leader timeout / new-view, and resumed forward proposal/vote/QC/commit all behave honestly on top of a real RocksDB checkpoint substrate.** S=4 captured from V0 `/metrics`; V0 advanced to H=15 before V1B started; V1B applied 13 peer-learned certified blocks (`committed_height=15`), exited B13 at height 15, and (once V2C joined) all three alive nodes (V0, V1B, V2C) advanced together to `committed_height=64` and `current_view=83` with 16 timeout certificates formed on each, `view_timeout_decode_failures_total=0`, `view_timeout_engine_rejects_total=0`, `proposals_total{result="rejected"}=0`, `votes_total{result="invalid"}=0`. All three exited rc=0. **Run 020 also isolates a previously-unrecorded narrower sub-item of C4's "production fast-sync / consensus-storage restore"**: `binary_consensus_loop` does not currently instantiate `RocksDbAccountState` for `<data_dir>/state_vm_v0` at runtime (no `[T164] VM v0 using persistent state at …` line is emitted by **any** node in this run, even with `--execution-profile vm-v0`); B3 materializes the restored RocksDB checkpoint into `state_vm_v0/` correctly but the running validator never opens it. The sub-item therefore now decomposes into (a) **production-grade snapshot creation** — proven; (b) **production-grade snapshot restore copy + B3/B5/B13/B14 reaction on real RocksDB checkpoint** — proven; (c) **binary-path runtime open of the restored VM-v0 state DB** — open; (d) **operator surface to trigger `StateSnapshotter::create_snapshot` from inside a running validator** (no CLI subcommand, no SIGUSR/HTTP trigger, no metrics-driven trigger; `SnapshotConfig::should_create_snapshot` exists in `crates/qbind-node/src/node_config.rs:388` but is not wired into the binary-consensus loop) — open. This is a sharpening, not a new contradiction. Other open C4 sub-items (signature verification of `TimeoutMsg`/`TimeoutCertificate`, exponential-backoff timeout pacing, production PQC KEMTLS root-key distribution) remain open exactly as before. No QBIND production source code changed by this entry; the only code added is two `crates/qbind-ledger/examples/` reproducibility helpers (`qbind_state_snapshot`, `qbind_seed_vm_v0_state`) that invoke library paths already covered by the B3/T215 test suites. No previously landed binary-path capability appears regressed. | Run 020 |
| 2026-05-09 | DevNet Evidence Run 023 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_023.md`): N=4 Required-mode B3/B5/B13/B14 continuation evidence on the post-Run-022 binary (commit `83655bd`, sha256 `376fd59c…06e`, BuildID `9b16a227…7bd`), with the snapshot under test produced **in-process by the running real `qbind-node` via SIGUSR1 → `StateSnapshotter::create_snapshot`** rather than by an out-of-binary helper. **Strongest-positive verdict** for this specific operational sub-shape: V0 (live throughout, started first per the Run 019 stagger) opened `state_vm_v0`, installed the SIGUSR1 trigger, received SIGUSR1 without terminating, invoked `StateSnapshotter::create_snapshot` at `committed_height=10`, and produced a real RocksDB checkpoint at `/tmp/run023/snapshots/10/` containing `CURRENT`, `MANIFEST-000013`, `000009.sst`, and `OPTIONS-000015` plus a valid `meta.json`. V1A/V2A/V3A were SIGINT'd; V1B (vid=1) and V2C (vid=2) were started with `--restore-from-snapshot /tmp/run023/snapshots/10`, fresh `--data-dir`, and `--execution-profile vm-v0`. B3 audit markers were written, the checkpoint was materialized into each restored validator's `state_vm_v0`, and B5 applied `snapshot_height=10`, `starting_view=11`, engine `committed_height=Some(10)`. B13 strict-progress exit fired on both restored validators (`mode_exited_at_height=14 > 10`, `restore_catchup_blocks_applied_total=6` each, `mode_active=1→0`). B14 cleared the absent-V3 plateau: `view_timeouts_emitted_total=10`, `timeout_certificates_formed_total=10/10/9` on V0/V1B/V2C, `outbound_new_views_sent_total=10/10/9`, `view_timeout_advances_total=10`, `view_timeout_decode_failures_total=0`, `view_timeout_engine_rejects_total=0`. `current_view` advanced 11 → 55 and `committed_height` advanced 10 → 42 on all three alive nodes; `proposals_total{result="rejected"}=0`, `votes_total{result="invalid"}=0` on every node. `qbind_snapshot_success_total=1`, `qbind_snapshot_last_height=10` on V0 confirms the SIGUSR1 path used the production metrics surface. A separate disabled-SIGUSR1 recheck preserved the Run 022 shape (`alive_after_SIGUSR1=True`, snapshot trigger ignored when `--snapshot-dir` is unset, clean `rc=0`). V0's `state_vm_v0` was pre-seeded with one normal account (`RocksDbAccountState::open` → `put_account_state` → `flush`) before V0 started so that the SIGUSR1-created RocksDB checkpoint would contain an `*.sst` file; the seeder did not create a snapshot, did not write any `meta.json`/`state/` layout, and did not bypass `StateSnapshotter::create_snapshot`. **No QBIND source code changed** by this entry; the only repository change is `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_023.md` and this row. The C4 status-row body is **not** re-narrowed: the SIGUSR1-trigger and VM-v0 runtime-open gaps were already closed by the Run 022 row, and the broader C4-open items (production fast-sync/consensus-storage restore, signature verification of `TimeoutMsg`/`TimeoutCertificate`, exponential-backoff timeout pacing, production PQC KEMTLS root-key distribution) are unchanged. This is a sharpening (the SIGUSR1-created-snapshot variant of the Run 020 / Run 019 shape now lands strongest-positive end-to-end on the same binary), not a new contradiction. No previously landed binary-path capability appears regressed. | Run 023 |

---

*This document should be updated whenever a contradiction is discovered between the whitepaper and implementation, or when significant undocumented behaviors are identified.*
#### C4 Run 025 evidence update (2026-05-09)

Run 024 remains the negative N=4 Required-mode baseline for the periodic `--snapshot-interval-blocks` trigger. The current implementation narrows that gap by wiring a committed-height periodic VM-v0 trigger into the real binary consensus progress path, reusing the same `StateSnapshotter::create_snapshot` runtime path and serialization guard as SIGUSR1. Local seeded Run 025 evidence (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_025.md`) proves a real binary creates a periodic snapshot at committed height 4 without SIGUSR1, with `meta.json`, `CURRENT`, `MANIFEST-*`, `OPTIONS-*`, and `*.sst`, and restores from it. This does not claim full C4 closure or a completed N=4 Required-mode Run 024 repeat; full multi-validator periodic-snapshot continuation evidence remains open.

#### C4 Run 026 evidence update (2026-05-09)

DevNet Evidence Run 026 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_026.md`) closes the specific Run 024 negative result and the Run 025 single-node boundary on the tested real binary: the same post-Run-025 `qbind-node` (sha256 `af4e4d7388e5fd1b386c889570ae8caf369b4d8a113e4ed5ea11d8c199a54216`, BuildID `318c08f9c7ef296026eb10d020587752971f295e`) ran N=4 Required-mode (V0/V1A/V2A/V3A, `--p2p-mutual-auth required`, `--execution-profile vm-v0`) with V0 holding `--snapshot-interval-blocks 4 --snapshot-max-snapshots 2` and **no SIGUSR1**. V0 created in-process `StateSnapshotter::create_snapshot` snapshots at committed heights 4, 8, 12, 16, 20, with `--snapshot-max-snapshots 2` honestly pruning heights 4, 8, 12 and retaining only 16 and 20. V1B and V2C restored from `/tmp/run026/snapshots/16` and the periodic snapshot supports full B3 (audit marker + checkpoint copy), B5 (`snapshot_height=16`, `starting_view=17`, engine `committed_height=Some(16)`), B13 (`mode_active 1->0`, `mode_exited_at_height=17 > 16`, `restore_catchup_blocks_applied_total=3`), and B14 (timeouts emitted, certificates formed, `current_view 17->26`, `committed_height 16->21` on V0/V1B/V2C with V3 absent) continuation. Snapshot metrics are honest (`qbind_snapshot_success_total=5`, `qbind_snapshot_last_height=20`, `qbind_snapshot_last_size_bytes=8588`, `qbind_snapshot_in_progress=0`, `qbind_snapshot_failure_total=0`); decode failures, engine rejects, rejected proposals, and invalid votes are all zero. C4 remains open overall for the broader production items already listed (production fast-sync / consensus-storage restore, signature verification of `TimeoutMsg` / `TimeoutCertificate`, exponential-backoff timeout pacing, production PQC KEMTLS root-key distribution), and Run 026 does not claim full C4 closure.
#### C4 Run 027 evidence update (2026-05-09)

DevNet Evidence Run 027 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_027.md`) is a combined snapshot-trigger regression pass on the same post-Run-026 `qbind-node` (sha256 `af4e4d7388e5fd1b386c889570ae8caf369b4d8a113e4ed5ea11d8c199a54216`, BuildID `318c08f9c7ef296026eb10d020587752971f295e`, branch `copilot/continue-qbind-status`, commit `40cc3d54316bff251516f5c11fc29cd16405e824`; binary byte-identical to Run 026 — Run 026 → Run 027 delta is documentation-only). **Strongest-positive verdict** for the four trigger paths under test, all on the same binary in one evidence run: (A) periodic `--snapshot-interval-blocks 4` in N=4 Required-mode (V0/V1/V2/V3, `--p2p-mutual-auth required`, V0-first stagger, **no SIGUSR1**) created 10 in-process `StateSnapshotter::create_snapshot` snapshots at heights 4,8,12,16,20,24,28,32,36,40, pruned to `[36,40]` under `--snapshot-max-snapshots 2`, all four nodes alive, V0 metrics `qbind_snapshot_success_total=10 / failure_total=0 / in_progress=0 / last_height=40 / last_size_bytes=8588`, with one node restored from `/tmp/run027/A/snapshots/40` applying B3 marker + B5 baseline (`snapshot_height=40, starting_view=41, engine committed_height=Some(40)`) and `committed_anchor` advancing strictly past restore height; (B) single-node SIGUSR1 produced a real snapshot at height 7 with `meta.json + CURRENT + MANIFEST-000013 + 000009.sst + OPTIONS-000015`, alive across SIGUSR1, metrics `success_total=1 / failure_total=0`, restore B3/B5 OK; (C) disabled (no `--snapshot-dir`) handled 3× SIGUSR1 with three explicit `[snapshot] SIGUSR1 ignored: VM-v0 snapshot trigger disabled (--snapshot-dir not configured)` log lines, no snapshot directory created, every `qbind_snapshot_*` metric zero; (D) mixed periodic+SIGUSR1 (20 SIGUSR1s during ~6 s of committing) produced 32 successful snapshots (16 periodic + 16 SIGUSR1) through the same `VmV0RuntimeState::create_snapshot` runtime path — both trigger types share the same runtime, `snapshot_in_progress` AtomicBool guard, and `state` Mutex — with all 3 retained dirs `[56,60,64]` validating against the canonical `state/CURRENT + MANIFEST-* + *.sst + OPTIONS-*` layout and `NON_NUMERIC_ENTRIES=[]`. Required negative checks (no crash, no placeholder, no fallback, no overlap corruption, no partial dirs, no fake metrics, no unexpected deletions, no regression of any prior trigger path) all pass. **Material operator-facing observation** (sharpening, not a new contradiction): in mixed-trigger workloads, when SIGUSR1 lands at the same `committed_height` as a just-completed periodic snapshot, `StateSnapshotter::create_snapshot` correctly refuses to overwrite the existing valid directory and returns `StateSnapshotError::AlreadyExists` (`crates/qbind-ledger/src/state_snapshot.rs:268–283`, `crates/qbind-ledger/src/execution.rs:1620–1622`); this is logged honestly as `[snapshot] ERROR: snapshot creation failed at .../<h>: snapshot already exists at: .../<h>` and increments `qbind_snapshot_failure_total` (4 such events in sub-run D, exactly matching `failure_total=4`). The previously-written snapshot directory is unmodified — this is a snapshot-format safety property, not corruption — so operators that alarm on `qbind_snapshot_failure_total > 0` should treat this as expected on nodes that run *both* triggers concurrently; bare-periodic nodes (Run 026) and bare-SIGUSR1 nodes (Run 022/023) keep `failure_total=0`. The wall-clock overlap path (`SnapshotAlreadyInProgress`) was not exercised end-to-end here because each snapshot completes in ~1 ms; it remains covered by the existing `vm_v0_snapshot_trigger_rejects_overlap_without_metric_failure` unit test. **No QBIND source code changed** by this entry; the only repository changes are `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_027.md` and this row. The C4 status-row body is **not** re-narrowed: the SIGUSR1, periodic, and disabled-trigger paths were already closed by Runs 022/023/025/026, and the broader C4-open items (production fast-sync / consensus-storage restore, signature verification of `TimeoutMsg` / `TimeoutCertificate`, exponential-backoff timeout pacing, production PQC KEMTLS root-key distribution, longer-window / larger-state snapshot stability) are unchanged. This is a sharpening (the four trigger paths under test all stay stable together on the same binary in one run, plus an explicit operator-facing nuance for the mixed-trigger metric shape), not a new contradiction.
#### C5 Run 034 evidence update (2026-05-10)

DevNet Evidence Run 034 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_034.md`) lands the missing N=4 Required-mode real-binary B14 absent-leader recovery proof under **active timeout verification** that Runs 030–033 left as the C5 Resolution path item (5). The post-Run-033 `qbind-node` debug binary (sha256 `0869335b8c9bf3232c3206c41acd11a9fad4b8ecfadf36156a2b46fd739beb5f`, BuildID `9127454ecc0e8616cde2a7805a1d24f5fb62c520`, branch `copilot/continue-qbind-current-state-yet-again`, commit `dd6eabe67e11159d3fd76bb0676eee257cc2bd08`, clean working tree) ran V0/V1A/V2A/V3A (`--p2p-mutual-auth required`, `--execution-profile vm-v0`, V0-first stagger) with per-validator `--signer-keystore-path` (plaintext `LocalKeystorePlain` `validator-{N}.json`, `0o600`, ephemeral DevNet ML-DSA-44 keys generated out-of-tree under `/tmp/keygen`), four explicit `--validator-consensus-key VID:100:HEXPK` entries (one per validator, the same set on every node), `--require-timeout-verification`, and metrics endpoints on `127.0.0.1:32000..32003`. Every node logged `[binary] Run 033: timeout verification ACTIVE — Arc<TimeoutVerificationContext> threaded into BinaryConsensusLoopIo::verification_ctx … signer_loaded=1 key_provider_loaded=1 validator_count=4` and `[binary-consensus] Starting consensus loop: … timeout_verification=verify+sign`, with pre-fault `/metrics` shape `qbind_timeout_verification_active 1`, `_signer_loaded 1`, `_key_provider_loaded 1`, `_validator_count 4` on every node. **Strongest-positive verdict.** With V1A (leader of views 93, 97, 101 under round-robin `view mod 4`) sent `signal.SIGINT` at `T=09:50:21Z` against the recorded numeric PID, the three remaining live validators (V0/V2A/V3A) recovered through three full B14 cycles: each emitted three signed `TimeoutMsg`s with explicit `[binary-consensus] Run 030: signing timeout view={93,97,101} validator=ValidatorId({0,2,3}) suite_id=100` / `Run 030: timeout signing OK …` pairs (`outbound_timeout_signing_success_total=3`, `outbound_timeout_signing_failure_total=0` on every live node); each verified six inbound TimeoutMsgs (`inbound_timeouts_delivered_total=6`, `inbound_timeout_verify_accepted_total=6`, `inbound_timeout_verify_rejected_total=0`, every `inbound_timeout_rejected_*` counter `=0`, `inbound_timeout_engine_accepted_total=6`, `inbound_timeout_engine_rejected_total=0`); each verified six inbound NewViews (`inbound_new_views_delivered_total=6`, `inbound_newview_verify_accepted_total=6`, `inbound_newview_verify_rejected_total=0`, every `inbound_newview_rejected_*` counter — `missing_evidence`, `evidence_mismatch`, `duplicate_signer`, `mixed_view`, `insufficient_quorum`, `unknown_validator`, `missing_key`, `wrong_suite`, `unsupported_suite`, `bad_signature`, `high_qc_mismatch` — `=0`); three TCs formed (`timeout_certificates_formed_total=3`, three `[binary-consensus] B14: TimeoutCertificate advanced view {93→94, 97→98, 101→102}` lines per live node) and drove three `view_timeout_advances_total`, advancing `current_view 90→105` and `committed_height 87→99` on every live validator with `proposals_total{result="rejected"}=0`, `votes_total{result="invalid"}=0`, `view_timeout_decode_failures_total=0`, `view_timeout_engine_rejects_total=0`, no fallback to `verification_ctx=None`, no unsigned timeout broadcast, no process crash (V0/V2A/V3A reached graceful `Loop exit:` after the post-recovery scrape; V1A's exit was the planned SIGINT). **C5 Resolution path item (5) is now closed.** Real-binary forged-traffic negative injection (item (6) above; would require a fake-peer rig holding a valid B12 transport identity but emitting crafted invalid TimeoutMsg/NewView payloads, or an opt-in in-process injection harness) was **not** built in Run 034 and is explicitly deferred to the existing deterministic per-reason test corpus (Run 028's `verify_timeout_msg` / `verify_timeout_certificate_with_evidence` positive/negative coverage, Run 030's 20 `binary_consensus_loop::tests::run030::*` deterministic tests, Run 031–033's 20+11+22+8 `timeout_verification_bridge` / `signer_loader` / `peer_key_provider` / CLI parser tests; full `qbind-node` lib `725 / 725` passing in Run 034). C5 remains OPEN strictly because **production PQC KEMTLS root-key distribution (a C4 sub-item) is still not solved** — Run 034 distributed consensus timeout-verification public keys via `--validator-consensus-key`, **not** transport KEMTLS root keys; B12 `TrustedClientRoots` / `DummySig` is still test-grade and is NOT a substitute for production transport PKI. Run 034 does **not** claim full C4 closure, does **not** claim production transport PKI is solved by consensus-public-key distribution, and does **not** claim real-binary forged-traffic negative injection is covered. **No QBIND source code changed** by this entry; the only repository changes are `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_034.md` and this row.
#### C5 Run 035 evidence update (2026-05-10)

DevNet Evidence Run 035 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_035.md`) lands the smallest opt-in dev/test-only forged Timeout/NewView negative injection harness as the deterministic / real-binary safety-gate half of C5 Resolution path item (6) (real-binary forged-traffic negative injection). The post-Run-034 `qbind-node` debug binary on this branch (sha256 `5c90fcc486b1132c76c1f4aca76d93fae3c48374c3de21499f5d32fc5aeee4af`, ELF BuildID `63e756e7fc87c7df870c8c3c88c4774d63453e6c`, branch `copilot/continue-qbind-activity`, working tree carrying the new `crates/qbind-node/src/forged_injection.rs` module + the five touched files) exposes a hidden `--devnet-forged-inject CASE` (`Append`) CLI flag whose activation requires THREE concurrent affirmative signals: (1) one or more `--devnet-forged-inject CASE` tokens drawn from the closed 12-case set (`malformed-timeout`, `unsigned-timeout`, `bad-signature-timeout`, `wrong-suite-timeout`, `unknown-validator-timeout`, `malformed-newview`, `missing-evidence-newview`, `duplicate-signer-newview`, `insufficient-quorum-newview`, `mixed-view-newview`, `bad-signature-newview`, `high-qc-mismatch-newview`); (2) `--env devnet` (Testnet/Mainnet are refused with `[binary] FATAL: Run 035 forged-injection harness is dev/test-only and cannot run on environment={Testnet,Mainnet}; refusing startup`); and (3) the explicit env var `QBIND_DEVNET_FORGED_INJECTION=1` (any other value, including unset, empty, `"0"`, `"true"`, `"yes"`, `"on"`, or `"1 "`, is refused with `[binary] FATAL: Run 035 forged-injection harness gated: --devnet-forged-inject was supplied but QBIND_DEVNET_FORGED_INJECTION=1 is not set in the environment; refusing to activate`). A fourth top-level gate also refuses non-P2P modes (LocalMesh has no inbound channel; activation under `--network-mode local-mesh` fails with `[binary] FATAL: --devnet-forged-inject requires --network-mode p2p`). The flag is hidden from `--help` (`hide = true`; `grep -c forged-inject` against `--help` is `0`). Once activated, `maybe_spawn_run035_forged_injection_harness` clones `ChannelConsensusHandler::sender_clone() -> mpsc::Sender<ConsensusNetMsg>` — the **same** channel the P2P inbound demuxer feeds — and pushes one bincode-encoded `ConsensusNetMsg::Timeout(bytes)` or `ConsensusNetMsg::NewView(bytes)` per case into it. The binary loop's existing `pub(crate) fn handle_inbound_consensus_msg` consumes these frames identically to live P2P traffic and runs them through `verify_timeout_msg` / `verify_timeout_certificate_with_evidence` BEFORE any call to `engine.on_timeout_msg` / `engine.on_timeout_certificate`. The harness never calls into the engine, never reads or writes any metrics counter (so it cannot fabricate metrics — every per-reason rejection-counter motion is performed by the existing Run 030 binary-loop code), and never logs signature bytes / signing keys / public keys / signing preimages (logged metadata is limited to case label, frame kind, and encoded byte length). For the cases that need a "decode-OK then signature-verify-FAILS" shape (`bad-signature-timeout`, `wrong-suite-timeout`, `bad-signature-newview`, `mixed-view-newview`, `duplicate-signer-newview`, `insufficient-quorum-newview`, `high-qc-mismatch-newview`), the runtime fixture generates fresh ephemeral ML-DSA-44 keypairs at process start that are NOT registered with any production `SuiteAwareValidatorKeyProvider`; signatures decode but verification fails against the real per-validator public keys distributed via `--validator-consensus-key VID:100:HEXPK`. **Twenty-one new deterministic tests pass on this binary**: 5 safety-gate tests (default-disabled, refuse-Testnet, refuse-Mainnet, refuse-no-env-var across 8 sub-values, accept-only-with-Devnet-and-env-var-1) + 1 case-parser round-trip + 12 per-case tests (each asserting the precise per-reason rejection counter increments by 1, every engine-accept counter stays 0, view did not advance) + 1 aggregate test (every case in `ForgedInjectionCase::ALL` rejects-before-engine and never advances view) + 1 honest-traffic-after-injection survival test (after BadSignatureTimeout increments `inbound_timeout_rejected_bad_signature=1`, an honest signed TimeoutMsg from the same validator id still advances `inbound_timeout_verify_accepted=1` and `inbound_timeout_engine_accepted=1`, with `bad_signature` NOT bumped a second time) + 1 `mpsc` channel round-trip test (proves pushing through `ChannelConsensusHandler::sender_clone()` delivers into the same `handle_inbound_consensus_msg` path the binary loop drives). All 20 Run 030 deterministic loop-API tests still pass, all 9 vm_v0_runtime tests still pass, and full `qbind-node` lib **746 / 746 passing** (= 725 pre-Run-035 + 21 new Run 035), zero regressions. On the real binary, all 7 safety-gate negative smoke checks fail closed (testnet, mainnet, devnet without env var, `QBIND_DEVNET_FORGED_INJECTION=0`, unknown case token, devnet+env-var-1 under LocalMesh, `--help` does not expose the flag). **PARTIAL POSITIVE verdict.** The deterministic / real-binary safety-gate half of item (6) is proven on this binary; a live N=4 Required-mode multi-process forged-injection run (where four `qbind-node` processes serve real P2P traffic and the harness is activated against one target node mid-run, then honest B14 absent-leader recovery is exercised after the harness completes) was NOT executed in this evidence run because the CI sandbox does not provide the network namespaces Run 034 used for its live multi-process evidence; that live N=4 step is the operator's immediate-next action and is not blocked by code (the harness, the safety gates, and the per-case verification logic all pass deterministically and on the real binary's startup gate today). C5 remains OPEN strictly because **production PQC KEMTLS root-key distribution (a C4 sub-item) is still not solved** — Run 035 changes nothing about transport PKI; B12 `TrustedClientRoots` / `DummySig` is still test-grade and is NOT a substitute. Run 035 does **not** claim full C4 closure, does **not** claim production transport PKI is solved, does **not** claim live N=4 multi-process forged-injection real-binary evidence (only the deterministic / real-binary-startup-gate evidence on the same `qbind-node` binary). Files changed in Run 035: `crates/qbind-node/src/forged_injection.rs` (NEW), `crates/qbind-node/src/lib.rs` (`pub mod forged_injection`), `crates/qbind-node/src/cli.rs` (hidden `--devnet-forged-inject CASE` Append flag), `crates/qbind-node/src/binary_consensus_loop.rs` (`handle_inbound_consensus_msg` and `RestoreCatchupModeState` promoted to `pub(crate)`; new `pub(crate) fn deliver_inbound_for_run035` test helper), `crates/qbind-node/src/p2p_inbound.rs` (new `ChannelConsensusHandler::sender_clone`), `crates/qbind-node/src/main.rs` (top-level safety gate after `validate_p2p_config` + `maybe_spawn_run035_forged_injection_harness` wiring after `spawn_binary_consensus_loop_with_io` + shutdown await), `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_035.md` (NEW), and this row.
#### C4 Run 037 evidence update (2026-05-10) — piece (c) NARROWED

DevNet Evidence Run 037 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_037.md`) lands the smallest production-honest PQC KEMTLS root-key distribution + delegation-cert verification path on the real binary, narrowing C4 piece (c) (production-honest PQC KEMTLS root-key distribution / certificate lifecycle) from "OPEN — only B12 test-grade `TrustedClientRoots`/`DummySig` available" to "NARROWED — production-honest PQC static-root mode landed on the binary path, real ML-DSA-44-signed delegation certs verify end-to-end, real-binary two-node mutual-auth succeeds without DummySig; full operational PKI lifecycle (rotation / revocation / OOB root distribution / per-environment trust anchors / production KEM + AEAD wiring) remains explicitly out of scope and is tracked under the surviving C4 pieces". Run 037 does NOT claim full C4 closure, does NOT claim production KEM (ML-KEM-768) or production AEAD wiring is solved (those are separate C4 pieces, NOT C4(c) — the binary still uses test-grade `DummyKem` / `DummyAead` for KEM/AEAD), does NOT remove the test-grade DummySig path (it remains available behind the explicit `--p2p-pqc-root-mode test-grade-dummy-sig` selection — refused on MainNet, warned on TestNet, allowed on DevNet — for backward compatibility with all pre-Run-037 evidence runs), does NOT claim a CA / rotation / revocation flow has landed, and does NOT silently downgrade to DummySig under any combination of CLI flags. Files changed: `crates/qbind-crypto/src/ml_dsa44_signature_suite.rs` (NEW — real `MlDsa44SignatureSuite` impl reusing existing `MlDsa44Backend`, no parallel crypto path), `crates/qbind-crypto/src/lib.rs` (`pub mod ml_dsa44_signature_suite`; `pub use MlDsa44SignatureSuite`), `crates/qbind-net/src/lib.rs` (`pub use verify_delegation_cert` made re-exported for test access — function body unchanged), `crates/qbind-node/src/pqc_root_config.rs` (NEW — `PqcRootMode { TestGradeDummySig, PqcStaticRoot }`, `PqcTrustedRoot { root_key_id: [u8;32], suite_id: u8, root_pk: Vec<u8> }` with strict hex parsing, `PqcStaticRootConfig`, `PqcLeafCredentials`, `parse_pqc_root_mode`, `parse_pqc_trusted_root_specs` with strict-parse / duplicate-id / unsupported-suite / malformed-pk / missing-root fail-closed, `PQC_TRANSPORT_SUITE_ML_DSA_44 = 100`, `PqcLeafCredentialPaths::load`, 12 unit tests pass), `crates/qbind-node/src/pqc_devnet_helper.rs` (NEW — offline `mint_devnet_root() -> DevNetRoot { root_key_id, root_pk, root_sk }` using existing `MlDsa44Backend::keygen`, `LeafCertSpec`, `issue_leaf_delegation_cert`, `encode_cert` building real ML-DSA-44-signed `NetworkDelegationCert`s; never logs private key material; 5 unit tests pass round-trip against `qbind_net::verify_delegation_cert`), `crates/qbind-node/src/p2p_node_builder.rs` (`with_pqc_root_config(...)`; `make_pqc_static_root_crypto_provider(...)` registers real `MlDsa44SignatureSuite` (suite_id 100) instead of `DummySig`; `TrustedClientRoots` resolver consults configured root pks under `PqcStaticRoot` mode, returns `None` for unknown root_key_id; per-validator leaf cert / KEM-sk overrides come from `PqcLeafCredentials` instead of `make_dummy_delegation_cert`; `root_network_pk` for `ClientHandshakeConfig` / `ServerHandshakeConfig` set to the configured root pk under PQC mode; metrics `pqc_root_mode` and `pqc_roots_configured` populated at build time), `crates/qbind-node/src/cli.rs` (4 NEW flags — `--p2p-pqc-root-mode`, `--p2p-trusted-root` (Append), `--p2p-leaf-cert <PATH>`, `--p2p-leaf-cert-key <PATH>`; help text references contradiction.md; 2 NEW CLI tests pass: help-exposes-flags + parse-roundtrip), `crates/qbind-node/src/main.rs` (Run 037 wiring — refuses unknown `--p2p-pqc-root-mode` value, refuses malformed / duplicate / unsupported-suite `--p2p-trusted-root` entries, refuses `--p2p-leaf-cert` without `--p2p-leaf-cert-key` (and vice versa), refuses `--p2p-mutual-auth required` + `--p2p-pqc-root-mode pqc-static-root` without configured roots OR without leaf credentials with FATAL exit 1; banner line `[binary] Run 037: pqc_root_mode=… configured_roots=N leaf_credentials_present=… (root fingerprints: [id=XXXXXXXX.. suite=100 fp=YYYYYYYY])` shows the active mode and SAFE fingerprints (truncated id + 8-byte SHA-256 of root pk; never raw root pk, never any private key); existing MainNet refuse-startup guard kept for the test-grade DummySig path, relaxed to a warning for the production-honest `pqc-static-root` path with explicit caveat that full MainNet readiness still requires the surviving non-C4(c) pieces), `crates/qbind-node/src/lib.rs` (`pub mod pqc_root_config; pub mod pqc_devnet_helper`), `crates/qbind-node/Cargo.toml` (no new deps; `qbind-crypto` and `qbind-net` already present; `hex = "0.4"` added for parser strict hex decoding), `crates/qbind-node/src/metrics.rs` (10 NEW PQC observability counters / gauges: `qbind_p2p_pqc_root_mode` (0=test-grade, 1=pqc-static-root), `qbind_p2p_pqc_roots_configured`, `qbind_p2p_pqc_cert_verify_accepted_total`, `qbind_p2p_pqc_cert_verify_rejected_total`, plus per-reason `unknown_root_total`, `wrong_suite_total`, `bad_signature_total`, `validator_mismatch_total`, `malformed_total`, `expired_total`; `format_metrics()` exports them; aggregate counter increments on every per-reason `inc_*` call so the four-reason aggregate equals the sum of the per-reason counters), `crates/qbind-node/examples/devnet_pqc_root_helper.rs` (NEW — offline DevNet helper that mints one ML-DSA-44 root + N leaf delegation certs into an output directory; root SK is held in memory only and is never written to disk in any form; emits a single `--p2p-trusted-root ROOTID:SUITE:PK` spec on stdout for direct copy-paste into operator commands; clearly labeled DevNet-ephemeral), `crates/qbind-node/tests/run_037_pqc_static_root_mutual_auth_tests.rs` (NEW — 10 tests: R037.A two-node mutual-auth Required + pqc-static-root + real ML-DSA-44 cert succeeds with NO DummySig, R037.B tampered signature fails closed at real PQC verifier, R037.C untrusted root fails closed (both verifier-level AND `lookup_root_pk` lookup), R037.D wrong sig_suite_id fails closed, R037.E tampered validator_id fails closed, R037.F default test-grade DummySig path preserved bit-for-bit, R037.G PQC mode without leaf credentials at builder level (boundary doc-pin), R037.H cert wire round-trip is byte-exact, R037.I metrics reflect PQC mode and root count + Prometheus exposition contains all 10 new metric names, R037.J metrics under default test-grade path report `pqc_root_mode = 0`), `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_037.md` (NEW), and this row. **Strongest-positive verdict for the C4(c) scope.** Test evidence on the post-Run-036 working tree: (a) `qbind-crypto::ml_dsa44_signature_suite::tests` 3/3 (sign/verify/wrong-key); (b) `qbind-node::pqc_root_config::tests` 12/12 (valid parse, malformed root_key_id, malformed pk hex, duplicate root_key_id, unsupported suite, missing-required-when-PqcStaticRoot, mode parsing, fingerprint format); (c) `qbind-node::pqc_devnet_helper::tests` 5/5 (mint root, issue cert, encode/decode round-trip, verify against root pk, fail against unrelated root pk); (d) `qbind-node tests/run_037_pqc_static_root_mutual_auth_tests` 10/10 including R037.A (two `P2pNodeBuilder`-built nodes with `MutualAuthMode::Required` + `PqcRootMode::PqcStaticRoot` + real ML-DSA-44-signed leaf certs sharing one configured root complete the full mutual-auth handshake and observe each other's cert-derived NodeIds); (e) `qbind-node tests/b12_mutual_auth_identity_binding_tests` 6/6 (pre-Run-037 test-grade DummySig path preserved bit-for-bit), `tests/b7_kemtls_bringup_identity_closure_tests` 5/5, `tests/b8_listener_identity_closure_and_dial_retry_tests` 6/6, `tests/m8_mutual_auth_config_tests` 17/17, `tests/t146_timeout_view_change_tests` 15/15 (Run 034 timeout-verification active path does not regress); (f) `qbind-node --lib` 765/765 (= 763 pre-Run-037 + 2 new CLI tests), zero regressions; (g) full `qbind-net` 16/16. **Real-binary smoke evidence on this branch's release `qbind-node` binary** (built via `cargo build --release -p qbind-node --bin qbind-node`): (i) `cargo run --release -p qbind-node --example devnet_pqc_root_helper -- /tmp/run037-mat 2` minted a fresh ML-DSA-44 root (`root_id=c63bb7a1…`, `suite=100`, root pk length = 1312 bytes = ML-DSA-44 public key size) and two leaf delegation certs (2544-byte `v0.cert.bin`, 2544-byte `v1.cert.bin`) with the root signing key held in memory only and never written to disk (verified by `ls /tmp/run037-mat | wc -l == 7` — `root.id.hex`, `root.pk.hex`, `trusted-root.spec`, `v0.cert.bin`, `v0.kem.sk.bin`, `v1.cert.bin`, `v1.kem.sk.bin`; no `root.sk.*` ever produced); (ii) two-node POSITIVE smoke (V0 on `127.0.0.1:19410`, V1 on `127.0.0.1:19411`, both with `--p2p-mutual-auth required --p2p-pqc-root-mode pqc-static-root --p2p-trusted-root <SAME_SPEC> --p2p-leaf-cert v{0,1}.cert.bin --p2p-leaf-cert-key v{0,1}.kem.sk.bin`) succeeds: both processes log `[binary] Run 037: pqc_root_mode=pqc-static-root configured_roots=1 leaf_credentials_present=true (root fingerprints: [id=c63bb7a1.. suite=100 fp=7672bcc4])` and `[Run037] P2pNodeBuilder: pqc_root_mode=pqc-static-root sig_suite_id=100 configured_roots=1 leaf_credentials_present=true`; both processes stay alive past the handshake window; V0 emits `[binary-consensus] B9+B10: re-emitted view 0 BroadcastProposal + BroadcastVote after late peer connect (newly_connected_peers=1, proposal_reemits_total=1, vote_reemits_total=1)` — that line is only emitted by `BinaryConsensusLoopIo` after `P2pService::on_peer_connected` fires, which under `MutualAuthMode::Required` requires `mutual_auth_complete=true` AND a verified cert-derived NodeId (i.e. is GATED on real ML-DSA-44 cert verify success, with no DummySig in the registered crypto provider); the previous `client handle_server_accept failed` error from the dialer's mismatched `peer_root_network_pk` is GONE, evidencing that the dialer's listener-cert ML-DSA-44 verify against the configured root pk succeeded; (iii) two-node NEGATIVE smoke (same setup but V1's `v1.cert.bin` flipped its trailing signature byte): no `newly_connected_peers` line emitted on either node, V0 listener logs `[P2P] Inbound connection error: Handshake error: channel error: Net(Protocol("server handle_client_init failed"))` (the listener's `parse_and_verify_client_cert` rejected the bad ML-DSA-44 signature inside `verify_delegation_cert` BEFORE calling `engine.on_*`), V1 dialer logs `[P2P] dial 127.0.0.1:19420 giving up after 1 attempt(s): Handshake error`; both processes stay alive (rc=running through the 8 s smoke window); no silent fallback to DummySig (the registered signature suite under `pqc-static-root` mode is `MlDsa44SignatureSuite` and there is no DummySig fallback path in `make_pqc_static_root_crypto_provider`); (iv) startup fail-closed smoke: `--p2p-mutual-auth required --p2p-pqc-root-mode pqc-static-root --p2p-trusted-root <SPEC>` with NO `--p2p-leaf-cert` exits with rc=1 and FATAL message `[binary] FATAL: --p2p-mutual-auth required + --p2p-pqc-root-mode pqc-static-root requires --p2p-leaf-cert and --p2p-leaf-cert-key (production-honest mode must not silently fall back to test-grade certs). See docs/whitepaper/contradiction.md C4(c).`. Smoke logs preserved at `docs/devnet/run_037_smoke_v0.stderr.log`, `…v1.stderr.log`, `…run_037_smoke_negative_v0.stderr.log`, `…v1.stderr.log`. **What is now production-honest on the binary path**: ML-DSA-44 signature verification of `NetworkDelegationCert`s; explicit operator-supplied root public keys with strict parsing and fail-closed configuration validation; per-validator real ML-DSA-44-signed leaf certs loaded from disk; cert-derived peer identity binding under `MutualAuthMode::Required`; explicit metric exposure of mode + root count + per-reason cert-verify counters. **What remains DevNet-only / explicit-test-only**: KEM key agreement still uses `DummyKem`; AEAD still uses `DummyAead`; no CA, no cert rotation, no cert revocation, no OOB / signed root-distribution channel (operators distribute the root pk hex out-of-band today); the `--p2p-pqc-root-mode test-grade-dummy-sig` path remains available behind the explicit operator selection (refused on MainNet, warned on TestNet, allowed on DevNet) and is the path exercised by every pre-Run-037 evidence run including Runs 010A/B, 030, 034, 036. **Remaining open**: real ML-KEM-768 wiring on the binary path (separate C4 piece, not C4(c)); real production AEAD wiring on the binary path (separate C4 piece, not C4(c)); operational PKI lifecycle (CA / rotation / revocation / signed root-distribution channel — surviving non-C4(c) work); per-environment trust anchors (per-DevNet/TestNet/MainNet root sets); cert validity-window enforcement on the binary path (the cert format already carries `not_before` / `not_after`, but the listener-side code path that increments `pqc_cert_rejected_expired_total` has metric scaffolding only — production-grade clock-source enforcement is deferred). **Verdict**: C4 piece (c) is **NARROWED** (production-honest PQC static-root cert-verification mode lands on the binary path; real-binary two-node mutual-auth succeeds without DummySig; negative cert cases fail closed at the live binary path); C4 piece (c) is NOT marked closed because production-grade CA / rotation / revocation / signed root distribution remain operator-out-of-band, and the surviving non-C4(c) C4 pieces (production KEM, production AEAD, fast-sync, exponential-backoff timeout pacing) are unchanged. C5 is NOT marked closed by this entry: although Run 037 narrows the dependency C5 names (B12 `TrustedClientRoots` / `DummySig` is no longer the only available transport identity stack on the binary path), C5's enclosing `production PQC KEMTLS root-key distribution` requirement is satisfied for the *cert-verification surface only* and not yet for the broader transport stack (KEM + AEAD), so per the strict scope rule "Do not mark C5 closed unless production PQC KEMTLS root-key distribution is solved enough to remove the remaining C5 dependency", C5's status text is updated to reflect that the C4(c) blocker is NARROWED but not yet fully closed. Operators MUST NOT rely on `--p2p-pqc-root-mode pqc-static-root` for production transport security today; it is honest about the cert-verification surface, but the surrounding KEM / AEAD primitives on the binary path remain test-grade until the surviving non-C4(c) C4 pieces close.

#### C4 Run 038 evidence update (2026-05-10) — piece (c) FURTHER NARROWED on N=4 Required-mode real-binary B14 recovery

DevNet Evidence Run 038 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_038.md`) lands the missing N=4 Required-mode real-binary B14 absent-leader recovery proof under `--p2p-pqc-root-mode pqc-static-root` (Run 037's production-honest PQC static-root cert-verification mode) that Run 037 explicitly did not cover (Run 037 was a two-node mutual-auth smoke). The post-Run-037 release `qbind-node` binary on this branch (sha256 `9c085f4bba6e610bb88c30b352c927fc84b6bbba2028fe12d5af6e22cbed55b7`, ELF BuildID `16faab89ef33d374fb2b2703338e4fdea0759119`, branch `copilot/continue-qbind-current-state-one-more-time`, base commit `8b7c31b01723eb3eaf8cbd086e2e913ed525cdd6`, working tree pre-doc clean apart from a one-line `Cargo.lock` regeneration for the already-declared `sha3` dep at `crates/qbind-node/Cargo.toml:45`) and the post-Run-037 release `devnet_pqc_root_helper` example (sha256 `2f267a55187934744f92ab840bf1bc5bd0a2a2eafd9044966d65aa122b3a5b22`, ELF BuildID `d28cbec2482c2543db26711c37b54651420ea4ea`) ran V0 / V1A / V2A / V3A (`--env devnet`, `--network-mode p2p`, `--enable-p2p`, `--p2p-mutual-auth required`, `--p2p-pqc-root-mode pqc-static-root`, one shared trusted root `9e09dea9...:100:11418230...` with `fp=d66752dd` distributed via `--p2p-trusted-root`, per-validator real ML-DSA-44-signed `NetworkDelegationCert`s loaded via `--p2p-leaf-cert v{N}.cert.bin` + `--p2p-leaf-cert-key v{N}.kem.sk.bin`, `--execution-profile vm-v0`, `--require-timeout-verification`, per-validator `--signer-keystore-path /tmp/run038/keystores/v{N}` with `0o600` plaintext `LocalKeystorePlain` ML-DSA-44 entries, four explicit `--validator-consensus-key VID:100:HEXPK` entries on every node (one per validator), `/metrics` HTTP enabled on `127.0.0.1:38000..38003`, V0-first stagger matching Runs 019/023/026/034, transport cert material and consensus signing material kept distinct: cert material from `devnet_pqc_root_helper /tmp/run038-mat 4`, consensus material from out-of-tree `/tmp/keygen` calling `fips204::ml_dsa_44::try_keygen()` — the same FIPS-204 keygen reused by `crates/qbind-crypto/src/ml_dsa44.rs::generate_keypair` — fingerprints `v0:s100:5957695f`, `v1:s100:eb2f6b61`, `v2:s100:2e024f87`, `v3:s100:90e235ac`, the materials are explicitly DevNet-ephemeral and the helper's root signing key was never written to disk in any form: `ls /tmp/run038-mat | wc -l == 11` produced exactly the `root.id.hex` / `root.pk.hex` / `trusted-root.spec` / 4× `v{N}.cert.bin` / 4× `v{N}.kem.sk.bin` set with no `root.sk.*`). Every node logged the Run 037 startup banner `[binary] Run 037: pqc_root_mode=pqc-static-root configured_roots=1 leaf_credentials_present=true (root fingerprints: [id=9e09dea9.. suite=100 fp=d66752dd])` and `[Run037] P2pNodeBuilder: pqc_root_mode=pqc-static-root sig_suite_id=100 configured_roots=1 leaf_credentials_present=true` — registering the real `MlDsa44SignatureSuite` (suite_id=100) and the configured root pk in the `TrustedClientRoots` resolver instead of `DummySig` (no `pqc_root_mode=test-grade-dummy-sig` log line on any node anywhere in the run, no active `DummySig` registration); every node also logged the Run 033 timeout-verification banner `[binary] Run 033: timeout-verification probe: active=true reason=n/a policy=RequireOrFail validators=4 chain_id=chain_51424e4444455600 supported_suite_ids=[100] local_signer=loaded(...) peer_key_provider=loaded(validators=4,peer_ids=[...],suite_ids=[100],fingerprints=[...])` and `[binary] Run 033: timeout verification ACTIVE — Arc<TimeoutVerificationContext> threaded into BinaryConsensusLoopIo::verification_ctx ... signer_loaded=1 key_provider_loaded=1 validator_count=4` and `[binary-consensus] Starting consensus loop: ... timeout_verification=verify+sign`, with `qbind_timeout_verification_active 1`, `_signer_loaded 1`, `_key_provider_loaded 1`, `_validator_count 4` on every node throughout (no fallback to `verification_ctx=None` on any node). The B9+B10 late-peer-reemit log `[binary-consensus] B9+B10: re-emitted view 0 BroadcastProposal + BroadcastVote after late peer connect (newly_connected_peers=3, ...)` from V0 — gated on `mutual_auth_complete=true` AND a verified cert-derived NodeId — proves all three peers (V1A/V2A/V3A) mutually authenticated under `MutualAuthMode::Required` with real ML-DSA-44 cert verification. **Strongest-positive verdict.** With V1A (round-robin leader of views v with v%4==1, i.e. views 105, 109, 113, 117, ...) sent `signal.SIGINT` against the recorded numeric PID at `2026-05-10T14:46:23Z`, the three remaining live validators (V0/V2A/V3A) recovered through three full B14 cycles — sampled at `T+6s` (midfault), `T+14s` (postrecovery), `T+18s` (final): each emitted three signed `TimeoutMsg`s (`outbound_timeout_signing_success_total 0→1→2→3`, `outbound_timeout_signing_failure_total 0→0→0→0` on every live node); each verified six inbound `TimeoutMsg`s (`inbound_timeout_verify_accepted_total 0→2→4→6` on every live node, `inbound_timeout_verify_rejected_total 0→0→0→0`, every per-reason `inbound_timeout_rejected_*` counter — `unknown_validator`, `missing_key`, `wrong_suite`, `unsupported_suite`, `bad_signature`, `duplicate` — `=0` throughout, `inbound_timeout_engine_rejected_total=0`, `view_timeout_decode_failures_total=0`, `view_timeout_engine_rejects_total=0`); each verified six inbound `NewView`/`TimeoutCertificate` frames (`inbound_newview_verify_accepted_total 0→2→4→6` on every live node, `inbound_newview_verify_rejected_total 0→0→0→0`, every per-reason `inbound_newview_rejected_*` counter — `missing_evidence`, `evidence_mismatch`, `duplicate_signer`, `mixed_view`, `insufficient_quorum`, `unknown_validator`, `missing_key`, `wrong_suite`, `unsupported_suite`, `bad_signature`, `high_qc_mismatch` — `=0` throughout, `inbound_newview_engine_rejected_total=0`); three TCs formed and drove three `view_timeout_advances_total` and three `view_advances_due_to_verified_tc_total` (the verified-TC attribution counter advancing in lock-step with the timeout-advance counter is the metric-level proof that `verify_timeout_certificate_with_evidence(&tc, &tc.signed_timeouts, &state)` ran ahead of every `engine.on_timeout_certificate` / `engine.on_new_view` call), advancing `current_view 102→117` (past the absent-leader plateau through the V1A-led views 105/109/113 in the rotation) and `committed_height 99→111` on every live validator with `proposals_total{result="rejected"}=0`, `votes_total{result="invalid"}=0`, no fallback to `verification_ctx=None`, no unsigned timeout broadcast, no process crash (V0/V2A/V3A reached graceful `[binary] Shutdown complete.` after the final scrape; V1A's exit was the planned SIGINT). The post-Run-038 negative cert smoke on the **same** Run 038 binary (`qbind-node` sha256 `9c085f4b...`, V0 with the honest `v0.cert.bin` + V1 with a single-byte-XOR-tampered `v1.cert.bin.tampered`, alternate ports `127.0.0.1:39050/39051`, same `--p2p-mutual-auth required --p2p-pqc-root-mode pqc-static-root --p2p-trusted-root <SAME_SPEC>`) still rejects the bad cert at the listener (`[P2P] Inbound connection error: Handshake error: channel error: Net(Protocol("server handle_client_init failed"))`) AND the dialer (`[P2P] dial 127.0.0.1:39051 giving up after 2 attempt(s): Handshake error: channel error: Net(Protocol("client handle_server_accept failed"))`), with `newly_connected_peers=0` on both sides and the active mode still `pqc_root_mode=pqc-static-root` on both sides — the honest-vs-tampered fail-closed boundary on the binary path is preserved by Run 038 just as Run 037 proved it (the negative-smoke log line containing the literal substring `DummySig` is the unrelated `Run 033: timeout-verification probe: active=false reason=production pieces unavailable in current qbind-node binary path: ... runs on test-grade TrustedClientRoots/DummySig — see docs/whitepaper/contradiction.md C4/C5 policy=OptionalActivate ...` *prose diagnostic text* of the timeout-verification probe — the negative smoke deliberately omitted the consensus signer keystore + `--validator-consensus-key` entries to keep it small, so the probe correctly logged its `active=false reason=production pieces unavailable` diagnostic referring textually to the test-grade DummySig path; the active runtime mode was still `pqc-static-root` and the registered crypto provider was still `MlDsa44SignatureSuite` with no DummySig fallback path in `make_pqc_static_root_crypto_provider`). **C4 piece (c) is now FURTHER NARROWED** — Run 037's production-honest PQC static-root cert-verification mode is now proven on the same N=4 Required-mode binary topology that Run 034 proved verified B14 recovery on, with both subsystems running clean simultaneously. **C5's remaining transport-root dependency is NARROWED but NOT closed** — production-honest PQC root-key + delegation-cert verification on the binary path now co-runs with active timeout verification end-to-end and the prior C5 status item that "B12 `TrustedClientRoots`/`DummySig` is the only available transport-identity stack on the binary path" is no longer accurate at the cert-verification surface, but C5 remains OPEN strictly because **production AEAD on the binary path remains test-grade** (`DummyAead`); Run 039 replaced `DummyKem` with real ML-KEM-768 in `pqc-static-root` Required mode, and production CA / cert rotation / cert revocation / signed root-distribution channel are still operator-out-of-band; per the strict policy "Do not mark C5 closed unless production PQC KEMTLS root-key distribution is solved enough to remove the remaining C5 dependency", C5 stays OPEN. **Full C4 remains OPEN** for the surviving non-C4(c) pieces (production KEM, production AEAD, fast-sync, exponential-backoff timeout pacing) and for the operational PKI lifecycle items above. **One honest evidence inconsistency vs the Run 037 documentation** is recorded by Run 038 and not silently fixed: the `qbind_p2p_pqc_*` family of metrics (`qbind_p2p_pqc_root_mode`, `qbind_p2p_pqc_roots_configured`, `qbind_p2p_pqc_cert_verify_accepted_total`, `qbind_p2p_pqc_cert_verify_rejected_total`, plus the per-reason `unknown_root_total`, `wrong_suite_total`, `bad_signature_total`, `validator_mismatch_total`, `malformed_total`, `expired_total`) declared in `crates/qbind-node/src/metrics.rs::P2pMetrics::format_metrics` (lines 6230–6275) is **not** invoked from `NodeMetrics::format_metrics` / `NodeMetrics::format_metrics_with_crypto`, so the live `/metrics` HTTP endpoint served by `metrics_http::format_metrics_output` does not include them; the test-level export proven by Run 037's R037.I (`P2pMetrics::format_metrics()` direct API) is unchanged — but the Run 037 evidence document's claim that those counters are observable on the live binary's `/metrics` endpoint is overstated. Run 038 records this as an observability-wiring follow-up rather than a HotStuff/B14/snapshot/restore/KEMTLS bug; the same proof of PQC mode active is delivered redundantly by the explicit `pqc_root_mode=pqc-static-root` startup log lines, the `newly_connected_peers=3` mutual-auth proof, and the strict absence of `client handle_server_accept failed` / `server handle_client_init failed` lines. **No QBIND source code changed** by this entry; the only repository changes are `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_038.md`, this row, and a one-line `Cargo.lock` regeneration for the already-declared `sha3` dep. **Tests rerun green**: `cargo test -p qbind-crypto` (12+2+26+28+12+11+3+3=97 lib + 5 doctest pass), `cargo test -p qbind-net` (16 pass + 2 ignored), `cargo test -p qbind-node --lib` (765/765 — same as Run 037), `cargo test -p qbind-node --test run_037_pqc_static_root_mutual_auth_tests` (10/10), `cargo test -p qbind-node --test t146_timeout_view_change_tests` (15/15 — the Run 034 timeout-verification active path does not regress under pqc-static-root), `cargo test -p qbind-node --test b7_kemtls_bringup_identity_closure_tests` (5/5), `cargo test -p qbind-node --test b8_listener_identity_closure_and_dial_retry_tests` (6/6), `cargo test -p qbind-node --test b12_mutual_auth_identity_binding_tests` (6/6), `cargo test -p qbind-node --test m8_mutual_auth_config_tests` (17/17), `cargo build --release -p qbind-node --bin qbind-node` (3 pre-existing warnings unchanged from Run 037), `cargo build --release -p qbind-node --example devnet_pqc_root_helper`. Operators MUST NOT rely on `--p2p-pqc-root-mode pqc-static-root` for production transport security today; it is honest about the cert-verification surface (now proven across the full N=4 Required-mode binary B14 topology), but the surrounding KEM / AEAD primitives on the binary path remain test-grade until the surviving non-C4(c) C4 pieces close.

#### C4 Run 041 evidence update (2026-05-11) — Run 040 binary confirmation; full C4 still OPEN

DevNet Evidence Run 041 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_041.md`) is a documentation/evidence-only confirmation pass on the Run 040 real-AEAD `pqc-static-root` binary path. **No source code was modified.** Run 041 builds the release `qbind-node` (sha256 `63dd94b56355e9a66132ff96724be706b029bcfd2283cbb67740733d4790b1da`, ELF BuildID `3e912b5f42fc7e85cd5e9f0e1d84e1cc2de87fa6`, commit `fdafbfde3681896ab898a0e1d7db7d2d83a76665`, clean working tree pre-doc) and the release `devnet_pqc_root_helper` (sha256 `c85538bb7cb7204fde9b01b8188ccb720950fc4d92a7a61e1dbb46c2897798e2`, ELF BuildID `d9a30d4f12d197bf072936d88b5949869829b002`), captures the deterministic `[Run040] P2pNodeBuilder: pqc_root_mode=pqc-static-root sig_suite_id=100 transport_kem_suite_id=100 transport_kem_suite_name=ml-kem-768 dummy_kem_registered=false transport_aead_suite_id=101 transport_aead_suite_name=chacha20-poly1305 dummy_aead_registered=false configured_roots=1 leaf_credentials_present=true` startup banner live from the actual release binary (`/tmp/run041/logs/v0-probe.log`) when supplied real ML-DSA-44-signed delegation cert + real ML-KEM-768 leaf secret + four certified `--p2p-peer-leaf-cert` entries, confirms `--devnet-forged-inject` stays hidden from `--help` (Run 035 contract preserved), and reruns the required regression suites green on this binary: `cargo test -p qbind-node --test run_040_pqc_static_root_real_aead_tests` 14/14, `cargo test -p qbind-node --test run_037_pqc_static_root_mutual_auth_tests` 12/12 (incl. R037.B tampered-cert, R037.C untrusted-root, R037.D wrong-sig-suite, R039 mismatched-ML-KEM-secret, R039 missing-peer-leaf-cert), `cargo test -p qbind-node --lib` 767/767, `cargo test -p qbind-crypto --lib` 68/68 (incl. all 14 `chacha20poly1305::tests::*` fail-closed primitives), `cargo test -p qbind-net --lib` 15/15. The release binary's `make_pqc_static_root_crypto_provider` recipe registers real `MlDsa44SignatureSuite` (sig suite id 100), real `MlKem768Backend` (KEM suite id 100), and real `ChaCha20Poly1305Backend` (AEAD suite id 101); the provider-shape unit tests on this binary directly assert `provider.aead_suite(2).is_none()`, `provider.kem_suite(1).is_none()`, and `provider.signature_suite(3).is_none()`, and the live startup banner additionally prints `dummy_kem_registered=false dummy_aead_registered=false`. The same `P2pNodeBuilder` recipe completes two-node `MutualAuthMode::Required` + `PqcRootMode::PqcStaticRoot` + real ML-DSA-44 cert + real ML-KEM-768 KEM + real ChaCha20-Poly1305 AEAD mutual handshake end-to-end on this binary build (`r040_c_two_node_required_pqc_static_root_real_aead_succeeds`). **Boundary explicitly recorded:** Run 041 did NOT execute a fresh N=4 multi-process B14 absent-leader recovery orchestration under this Run 040 binary in this evidence pass — the Run 038 (cert-only) and Run 039 (cert + ML-KEM-768) N=4 multi-process B14 evidence records remain the authoritative N=4 multi-process B14 recovery proofs on the binary path, and the only delta from Run 039 on the binary path is the AEAD-provider registration swap, which Run 041 confirms is bit-for-bit consistent on the release-binary `P2pNodeBuilder` recipe by (a) the deterministic live `[Run040]` startup banner, (b) the R040.A provider-shape assertions, (c) the R040.B AEAD fail-closed primitives, (d) the R040.C two-node real-binary Required + pqc-static-root + real ML-DSA-44 + real ML-KEM-768 + real ChaCha20-Poly1305 handshake test on the production builder, and (e) the R040.D test-grade-path preservation invariant. Re-executing the Run 038/039 N=4 multi-process B14 orchestrator under this Run 040 binary and capturing the §11–§13 metric slate live is recorded as the immediate next operator action in `docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_041.md` §19. **C4 remains OPEN** in full for: CA / cert rotation / cert revocation / signed root distribution lifecycle (operator-out-of-band, not solved); `qbind_p2p_pqc_*` live `/metrics` exposure (Run 038/039/040/041 inherited OPEN gap — declared in `P2pMetrics::format_metrics` but not wired through `NodeMetrics::format_metrics`); production fast-sync / consensus-storage restore; exponential-backoff timeout pacing; per-environment trust anchors; clock-source cert-validity-window enforcement. **C5 remains NOT closed** by Run 041; the transport-crypto dependency (real ML-DSA-44 cert verification — Run 037, real ML-KEM-768 — Run 039, real ChaCha20-Poly1305 AEAD — Run 040, active timeout verification — Runs 031–034, forged-traffic rejection — Runs 035–036) is *further confirmed* on the Run 040 release binary by Run 041, but lifecycle (CA / rotation / revocation / signed root distribution) remains a C4 piece, so Run 041 does NOT close C5 by fiat. Operators on `--p2p-pqc-root-mode pqc-static-root` post-Run-041 are operating production-honest cert verification + production-honest transport KEM + production-honest transport AEAD on a byte-identified release binary, but lifecycle is still operator-out-of-band and is NOT a substitute for production CA tooling.

#### C4 Run 042 evidence update (2026-05-11) — fresh N=4 multi-process B14 absent-leader recovery on the Run 040 real-AEAD binary; full C4 still OPEN

DevNet Evidence Run 042 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_042.md`) executes the operator-recommended Run 041 §19 immediate next action: a fresh N=4 multi-process Required-mode B14 absent-leader recovery capture under the Run 040 real-AEAD `pqc-static-root` release binary, matching the Run 038/039 topology shape, with full live pre-fault / mid-fault / post-recovery / final `/metrics` capture from every reachable node. **No QBIND source code was modified by Run 042 — this is evidence-only.** Run 042 builds the release `qbind-node` binary (sha256 `63dd94b56355e9a66132ff96724be706b029bcfd2283cbb67740733d4790b1da`, ELF BuildID `3e912b5f42fc7e85cd5e9f0e1d84e1cc2de87fa6`, branch `copilot/continue-qbind-progress-again`, pre-doc commit `d93d00648395a597aa574457831ed6d7de3f9123`, clean working tree pre-doc) and the release `devnet_pqc_root_helper` (sha256 `c85538bb7cb7204fde9b01b8188ccb720950fc4d92a7a61e1dbb46c2897798e2`, ELF BuildID `d9a30d4f12d197bf072936d88b5949869829b002`) — **byte-identical to the Run 040 / Run 041 release binaries**, the only commit-level delta from Run 041's pre-doc commit being Run 041's documentation appendage. The Run 042 orchestrator re-establishes the out-of-tree consensus signer keystore helper (`/tmp/run042-keygen/`, calling `fips204::ml_dsa_44::try_keygen()` directly — the same FIPS-204 keygen reused by `crates/qbind-crypto/src/ml_dsa44.rs::generate_keypair`, kept outside the repository so it cannot accidentally land in source) to write four `0o600` plaintext `LocalKeystorePlain` ML-DSA-44 entries at `/tmp/run042/keystores/v{N}/validator-{N}.json` (5157 bytes each — same shape Run 038 reported), prepares the four `--p2p-peer-leaf-cert` entries + per-validator `--p2p-leaf-cert v{N}.cert.bin` + `--p2p-leaf-cert-key v{N}.kem.sk.bin` via `devnet_pqc_root_helper /tmp/run042-mat 4` (DevNet-ephemeral root `9bec797f...`, sig suite 100, KEM suite 100, root pk fingerprint `fp=1b1b157f`; helper's root signing key never written to disk in any form: only `root.id.hex`/`root.pk.hex`/`trusted-root.spec`/4×`v{N}.cert.bin`/4×`v{N}.kem.sk.bin` produced), and starts V0/V1A/V2A/V3A with `--env devnet --network-mode p2p --enable-p2p --p2p-mutual-auth required --p2p-pqc-root-mode pqc-static-root --p2p-trusted-root <shared> --execution-profile vm-v0 --require-timeout-verification` plus four `--validator-consensus-key VID:100:HEXPK` entries on every node (consensus PK fingerprints `v0:s100:6b59d5c0`, `v1:s100:77a9ec59`, `v2:s100:3c1e96f0`, `v3:s100:c0b77fdc` on the binary side; helper-side SHA3-256 prefixes `dfdd6b31` / `16b81c45` / `4a516481` / `4df40b03`), `/metrics` HTTP enabled on `127.0.0.1:381{00,01,02,03}`, V0-first stagger matching Runs 019/023/026/034/038/039. Every live node logged the deterministic Run 040 startup banner `[Run040] P2pNodeBuilder: pqc_root_mode=pqc-static-root sig_suite_id=100 transport_kem_suite_id=100 transport_kem_suite_name=ml-kem-768 dummy_kem_registered=false transport_aead_suite_id=101 transport_aead_suite_name=chacha20-poly1305 dummy_aead_registered=false configured_roots=1 leaf_credentials_present=true` (verified by `grep -c "[Run040] P2pNodeBuilder" /tmp/run042/logs/{v0,v1a,v2a,v3a}.log` = 1 each) and the Run 033 timeout-verification ACTIVE banner with `signer_loaded=1 key_provider_loaded=1 validator_count=4`, with `qbind_timeout_verification_active 1`, `_signer_loaded 1`, `_key_provider_loaded 1`, `_validator_count 4` on every node throughout (no fallback to `verification_ctx=None`). V0 emitted `newly_connected_peers=3` on the B9+B10 late-peer-reemit line — gated on `mutual_auth_complete=true` AND a verified cert-derived NodeId — proving all three peers (V1A/V2A/V3A) mutually authenticated under `MutualAuthMode::Required` with real ML-DSA-44 cert verification + real ML-KEM-768 KEM + real ChaCha20-Poly1305 AEAD on the live `P2pNodeBuilder::build` production path. **Strongest-positive verdict.** With V1A (round-robin leader of views v%4==1, i.e. 105/109/113/117/...) sent `signal.SIGINT` against the recorded numeric PID 11784 at `2026-05-11T10:03:16Z`, the three remaining live validators (V0/V2A/V3A) recovered through three full B14 cycles — sampled at `T+6s` (midfault), `T+14s` (postrecovery), `T+18s` (final): `outbound_timeout_signing_success_total 0→3` and `outbound_timeout_signing_failure_total 0→0` on every live node; `inbound_timeout_verify_accepted_total 0→6` and `inbound_timeout_verify_rejected_total 0→0` on every live node with every per-reason `inbound_timeout_rejected_*` counter (`unknown_validator`, `missing_key`, `wrong_suite`, `unsupported_suite`, `bad_signature`, `duplicate`) `=0` throughout, `view_timeout_decode_failures_total=0`, `view_timeout_engine_rejects_total=0`; `inbound_newview_verify_accepted_total 0→6` and `inbound_newview_verify_rejected_total 0→0` on every live node with every per-reason `inbound_newview_rejected_*` counter (`missing_evidence`, `evidence_mismatch`, `duplicate_signer`, `mixed_view`, `insufficient_quorum`, `unknown_validator`, `missing_key`, `wrong_suite`, `unsupported_suite`, `bad_signature`, `high_qc_mismatch`) `=0` throughout, `inbound_newview_engine_rejected_total=0`; three TimeoutCertificates formed and drove three `view_timeout_advances_total` plus three `view_advances_due_to_verified_tc_total` (the verified-TC attribution counter advancing in lock-step with the timeout-advance counter — the metric-level proof that `verify_timeout_certificate_with_evidence(&tc, &tc.signed_timeouts, &state)` ran ahead of every `engine.on_timeout_certificate` / `engine.on_new_view` call); `current_view 104/105/105 → 117/117/117` (past V1A-led views 105, 109, 113) and `committed_height 101/102/102 → 111/111/111` on V0/V2A/V3A — exactly the +9 committed-height delta Run 038 captured. `proposals_total{result="rejected"}=0`, `votes_total{result="invalid"}=0` on every live node. V1A's exit was the planned SIGINT (graceful self-shutdown trace recorded with `[binary] Shutdown complete.`), and V0/V2A/V3A reached graceful `[binary] Shutdown complete.` after the final scrape — no process crash, no FATAL, no panic. **No `client handle_server_accept failed` / `server handle_client_init failed` / `DummySig` / `DummyKem` / `DummyAead` / `test-grade-dummy-sig` active-mode log line / cert-verify failure / unknown-root / wrong-suite / KEM-decapsulation-failure / AEAD-decrypt-or-authentication-failure / private-key-material log line** appeared on any node at any point during the honest run. **Tests rerun green on this binary**: `cargo test -p qbind-node --test run_040_pqc_static_root_real_aead_tests` 14/14, `cargo test -p qbind-node --test run_037_pqc_static_root_mutual_auth_tests` 12/12 (incl. R037.B tampered-cert, R037.C untrusted-root, R037.D wrong-sig-suite, R037.E DummySig-not-registered-in-pqc-static-root-provider, R039 mismatched-ML-KEM-secret, R039 missing-peer-leaf-cert), `cargo test -p qbind-node --lib` 767/767, `cargo test -p qbind-crypto --lib` 68/68 (incl. all 14 `chacha20poly1305::tests::*` fail-closed primitives), `cargo test -p qbind-net --lib` 15/15, `cargo test -p qbind-node --test t146_timeout_view_change_tests` 15/15, `cargo build --release -p qbind-node --bin qbind-node` (3 pre-existing warnings unchanged from Run 040/041), `cargo build --release -p qbind-node --example devnet_pqc_root_helper`. **C4 piece (c) and the surrounding KEM/AEAD primitives are now FURTHER NARROWED** by Run 042: the Run 037 (cert verification) + Run 039 (ML-KEM-768) + Run 040 (ChaCha20-Poly1305 AEAD) full transport-crypto stack and the Runs 031–034 active timeout-verification stack co-run cleanly under fresh N=4 multi-process B14 absent-leader recovery on the byte-identified Run 040 release binary, with live `/metrics` capture from every reachable node. **C5's remaining transport-crypto dependency is NARROWED — and now LIVE-DEMONSTRATED under N=4 multi-process B14 recovery on the Run 040 binary** — but C5 closure remains contingent on the operator-facing lifecycle policy being explicitly accepted as out-of-C5 scope, because per the strict policy "Do not mark C5 closed unless production PQC KEMTLS root-key distribution is solved enough to remove the remaining C5 dependency", and lifecycle is documented as a C4 piece, **Run 042 does NOT close C5 by fiat**. **Full C4 remains OPEN** for: production CA / cert rotation / cert revocation / signed root-distribution lifecycle (operator-out-of-band, not solved); `qbind_p2p_pqc_*` live `/metrics` exposure (declared in `P2pMetrics::format_metrics` at `crates/qbind-node/src/metrics.rs` but not yet wired through `NodeMetrics::format_metrics` — Run 038/039/040/041/042 inherited OPEN gap, not addressed by Run 042 — recorded for the fifth time as an observability-wiring follow-up rather than a HotStuff/B14/snapshot/restore/KEMTLS/AEAD bug; the same proof of PQC mode active is delivered redundantly by the live `[Run040]` startup banner on every node, the `newly_connected_peers=3` mutual-auth proof on V0, the strict absence of handshake failures on every node, and the `r040_a_*` provider-shape unit tests on the same binary); production fast-sync / consensus-storage restore; exponential-backoff timeout pacing; per-environment trust anchors; clock-source cert-validity-window enforcement. **No silent fallback** to DummySig, DummyKem, DummyAead, or `test-grade-dummy-sig` active mode occurred on any node at any time — recording this here as a non-regression fact, not a discovered contradiction. **No new contradiction** was discovered by Run 042 — the only inconsistencies surfaced are the four operability/lifecycle items above that were already OPEN before Run 042. Operators using `--p2p-pqc-root-mode pqc-static-root` post-Run-042 are operating production-honest cert verification + production-honest transport KEM + production-honest transport AEAD on a byte-identified release binary that has now been live-proven through N=4 multi-process B14 absent-leader recovery, but lifecycle (CA / rotation / revocation / signed root distribution) is still operator-out-of-band and is NOT a substitute for production CA tooling.
#### C4 Run 043 evidence update (2026-05-11) — `qbind_p2p_pqc_*` live `/metrics` exposure NARROWED (observability-only); full C4 and C5 still OPEN

DevNet Evidence Run 043 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_043.md`) closes the inherited Run 038/039/040/041/042 observability gap recorded by the Run 041/042 C4 rows above — "`qbind_p2p_pqc_*` counters are declared in `crates/qbind-node/src/metrics.rs::P2pMetrics::format_metrics` but they are not currently emitted through the live `/metrics` endpoint because `NodeMetrics::format_metrics` / `format_metrics_with_crypto` does not delegate to `self.p2p.format_metrics`" — by wiring `self.p2p.format_metrics()` into `NodeMetrics::format_metrics` exactly once (inserted between the `peer_network` and `connection_limit` blocks; `format_metrics_with_crypto` inherits this via composition without any second call) **AND** by sharing the same `Arc<P2pMetrics>` instance between the live transport path (`P2pNodeBuilder::build()`) and the live `/metrics` HTTP scrape (`metrics_http::format_metrics_output` → `NodeMetrics::format_metrics`). The instance-sharing layer is required because pre-Run-043 `P2pNodeBuilder::build()` minted a fresh local `Arc<P2pMetrics>` at `crates/qbind-node/src/p2p_node_builder.rs:1091` that was never reached by `NodeMetrics::format_metrics`, so a formatter-wiring-only fix would emit the family names with every value pinned at 0 even under live `pqc-static-root` operation — Run 043 honestly identifies this as the *truly missing link* beyond what the problem-statement preferred minimal edit captured and addresses both layers. Source changes: (1) `crates/qbind-node/src/metrics.rs` — insert `output.push_str(&self.p2p.format_metrics());` into `NodeMetrics::format_metrics` exactly once; change `NodeMetrics::p2p: P2pMetrics` to `Arc<P2pMetrics>`; add `pub fn p2p_arc(&self) -> Arc<P2pMetrics>` accessor; existing `pub fn p2p(&self) -> &P2pMetrics` API preserved bit-for-bit at all call sites via deref coercion. (2) `crates/qbind-node/src/p2p_node_builder.rs` — add `p2p_metrics: Option<Arc<P2pMetrics>>` field on `P2pNodeBuilder` (defaults `None`, preserving builder-only test behaviour bit-for-bit); add `P2pNodeBuilder::with_p2p_metrics(Arc<P2pMetrics>)` setter; replace `let metrics = Arc::new(P2pMetrics::new());` with `let metrics = self.p2p_metrics.clone().unwrap_or_else(|| Arc::new(P2pMetrics::new()));`. (3) `crates/qbind-node/src/main.rs` — append `.with_p2p_metrics(node_metrics.p2p_arc())` to the `P2pNodeBuilder::new()` chain in `run_p2p_node`. **No** changes to `qbind-net`, `qbind-crypto`, KEMTLS handshake, cert-verify logic, KEM, AEAD, consensus, signer loader, forged-traffic rejection, or timeout-verification surfaces. Release binary `qbind-node` sha256 `9ecfb8a4d61cb51457f533a903c80d51dbc5da92f6c1e05816284e48b8a1e63f`, `devnet_pqc_root_helper` sha256 `43ab13016bba36dc3db4e43df1fbc177456a8a4075ddcf01968ee3aef8d463c8`. Tests: `cargo test -p qbind-node --lib metrics` (100/100 — includes 8 new deterministic `run_043_*` tests proving family-present on both formatter paths, emitted exactly once, zero-valued visibility, non-zero propagation through `inc_*`/`set_*`, no regression to `consensus_net_*` / KEM / timeout-verification surfaces, and section-header-emitted-once); `cargo test -p qbind-node --lib p2p` (138/138); `cargo test -p qbind-node --test run_040_pqc_static_root_real_aead_tests` (14/14); `cargo test -p qbind-node --test run_037_pqc_static_root_mutual_auth_tests` (12/12); `cargo test -p qbind-node --lib` (775/775 = 767 pre-Run-043 + 8 new `run_043_*`); `cargo test -p qbind-crypto --lib` (68/68); `cargo test -p qbind-net --lib` (15/15); `cargo build --release -p qbind-node --bin qbind-node` (clean, same 3 pre-existing warnings as Run 041); `cargo build --release -p qbind-node --example devnet_pqc_root_helper` (clean). **Real-binary positive smoke** (release binary above, `target/release/examples/devnet_pqc_root_helper /tmp/run043/mat 2` mints DevNet-ephemeral root `4abf0794...` fp=`b52fcdbb`, two-node V0/V1 on `127.0.0.1:19450/19451` with `/metrics` on `127.0.0.1:43050/43051`, both with `--env devnet --network-mode p2p --enable-p2p --p2p-mutual-auth required --p2p-pqc-root-mode pqc-static-root --p2p-trusted-root <SHARED_SPEC>`, per-validator real ML-DSA-44-signed leaf cert + real ML-KEM-768 leaf secret, one `--p2p-peer-leaf-cert` each): both nodes logged the Run 039 `[binary] Run 039: pqc_root_mode=pqc-static-root transport_kem_suite=ml-kem-768 configured_roots=1 leaf_credentials_present=true peer_leaf_certs=1 (root fingerprints: [id=4abf0794.. suite=100 fp=b52fcdbb])` and Run 040 `[Run040] P2pNodeBuilder: pqc_root_mode=pqc-static-root sig_suite_id=100 transport_kem_suite_id=100 transport_kem_suite_name=ml-kem-768 dummy_kem_registered=false transport_aead_suite_id=101 transport_aead_suite_name=chacha20-poly1305 dummy_aead_registered=false configured_roots=1 leaf_credentials_present=true` startup banners; V0 logged `[binary-consensus] B9+B10: re-emitted view 0 BroadcastProposal + BroadcastVote after late peer connect (newly_connected_peers=1, ...)` proving mutual-auth under `MutualAuthMode::Required` completed with a verified cert-derived NodeId — i.e. the dialer's listener-cert ML-DSA-44 verify against the configured root pk succeeded; live `/metrics` on both nodes shows the full 10-line `qbind_p2p_pqc_*` family with `qbind_p2p_pqc_root_mode 1`, `qbind_p2p_pqc_roots_configured 1`, and `# P2P transport metrics (T172)` header appearing exactly once on every scrape (`grep -c "^# P2P transport metrics (T172)"` returns 1 on both `/tmp/run043/v0-pqc-metrics.txt` and `/tmp/run043/v1-pqc-metrics.txt`); pre-existing `qbind_timeout_verification_*`, `qbind_net_kem_*`, and `consensus_net_*` surfaces all preserved on the scrape. **Real-binary negative smoke** (same topology with V1's leaf cert flipped on its trailing signature byte, `bytes[-1] ^= 0x01`): V1 fails closed *at builder time* with `[binary] ERROR: Failed to build P2P node: Config("delegation cert verification failed: KeySchedule(\"signature verify error\")")` — the production cert-verify path correctly refuses the tampered cert before metrics server scrape becomes meaningful for that case; V0 stays alive with its `/metrics` family still visible (all values zero — no peer ever reached mutual-auth completion, and per the increment-call-site boundary recorded below the per-reason rejection counters do not yet move on the live path). **Honest boundary explicitly recorded** (per the strict "do not fake increments" policy): the 8 cert-verify event counters (`qbind_p2p_pqc_cert_verify_accepted_total`, `qbind_p2p_pqc_cert_verify_rejected_total`, and the 6 per-reason `qbind_p2p_pqc_cert_rejected_*_total`) are now scrape-visible exactly once on the live `/metrics` endpoint but their `inc_*` setters in `crates/qbind-node/src/metrics.rs:6297–6335` are not yet called from any production code path — `grep -rn 'inc_pqc_cert_verify_(accepted|rejected)' crates/` finds only the metrics-module definitions and Run 043's own deterministic unit tests. Wiring the increments into `qbind-net`'s live cert-verify success / error branches (in `verify_delegation_cert` and the listener/dialer `NetError::ClientCertInvalid`/`KeySchedule` arms) is a separate, well-scoped follow-up explicitly recorded in §15 of the Run 043 evidence doc; Run 043 did **not** add placeholder call sites and did **not** fabricate increments. Two of the ten gauges, `qbind_p2p_pqc_root_mode` and `qbind_p2p_pqc_roots_configured`, **are** wired live via the pre-existing `metrics.set_pqc_root_mode(pqc_mode_n)` / `metrics.set_pqc_roots_configured(pqc_roots_n)` calls at `crates/qbind-node/src/p2p_node_builder.rs:1107–1108` and now correctly reflect the active production-honest configuration on the live binary path because of the instance-sharing fix above. **Live N=4 multi-process B14 capture under the Run 043 binary not run in this evidence pass** (CI sandbox does not provide reliable network-namespace isolation for N=4 multi-process orchestration; the two-node positive smoke is the authoritative live-binary proof in this pass — same release binary recipe as Run 040/041/042, so the §11–§13 §metric slate recorded by Run 042 on the byte-identical Run 040/041 binary remains applicable for the consensus/timeout-verification/B14 surfaces, while Run 043 specifically delivers the `qbind_p2p_pqc_*` live `/metrics` visibility). **C4 piece (c) inherited observability gap is NARROWED but NOT fully closed**: the formatter-path and instance-sharing layers are closed (`qbind_p2p_pqc_*` family is now scrape-visible exactly once on live `/metrics`, gauges reflect live binary state under `pqc-static-root`); the cert-verify-event increment-call-site layer remains a separate open item, recorded honestly here. **Full C4 remains OPEN** for: CA / cert rotation / cert revocation / signed root distribution lifecycle (operator-out-of-band, not solved); `qbind-net` cert-verify-counter increment wiring (Run 043 declared-but-not-incremented boundary above); production fast-sync / consensus-storage restore; exponential-backoff timeout pacing; per-environment trust anchors; clock-source cert-validity-window enforcement. **C5 remains NOT closed** by Run 043; the transport-crypto dependency (real ML-DSA-44 cert verification — Run 037, real ML-KEM-768 — Run 039, real ChaCha20-Poly1305 AEAD — Run 040, active timeout verification — Runs 031–034, forged-traffic rejection — Runs 035–036, end-to-end live-binary N=4 B14 recovery — Run 042) is *further confirmed scrape-observable at the `qbind_p2p_pqc_*` gauge level on the live binary path* by Run 043, but lifecycle (CA / rotation / revocation / signed root distribution) and per-event cert-verify counter wiring remain C4 pieces, so Run 043 does NOT close C5 by fiat. Operators using `--p2p-pqc-root-mode pqc-static-root` post-Run-043 can now scrape the full `qbind_p2p_pqc_*` family from the live `/metrics` endpoint exactly once and observe `qbind_p2p_pqc_root_mode 1` and `qbind_p2p_pqc_roots_configured N` honestly reflecting the active production-honest configuration; the per-event accepted/rejected counters honestly read 0 today because their `inc_*` setters are not yet called from the live cert-verify path — operators MUST NOT interpret the present zero values as evidence that no cert verification happened, only as evidence that the per-event call sites are not yet wired (this is the documented boundary).
#### C4 Run 044 evidence update (2026-05-11) — Run 043 cert-verify increment-call-site observability gap NARROWED (observability-only); full C4 and C5 still OPEN

DevNet Evidence Run 044 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_044.md`) closes the inherited Run 043 §15 honest-boundary gap recorded by the Run 043 C4 row above — "the 8 cert-verify event counters (`qbind_p2p_pqc_cert_verify_accepted_total`, `qbind_p2p_pqc_cert_verify_rejected_total`, and the 6 per-reason `qbind_p2p_pqc_cert_rejected_*_total`) are now scrape-visible exactly once on the live `/metrics` endpoint but their `inc_*` setters in `crates/qbind-node/src/metrics.rs:6297–6335` are not yet called from any production code path" — by wiring the existing `P2pMetrics::inc_pqc_cert_verify_*` setters into the **actual** live cert-verification success/failure boundaries in `qbind-net::handshake` without changing verification behaviour, error return types, or any protocol shape. **Observability-only — no `qbind-net`/`qbind-crypto`/`qbind-node` consensus, KEMTLS, KEM, AEAD, signer-loader, timeout-verification, or forged-traffic-rejection surface was modified.** Release binary `qbind-node` sha256 `fe805315c2be8aebd1133b3a2082dff1eeab3000a18abdcb7cbc265897377e4a` ELF BuildID `15bac93f1def29b90e2ea713dcc6b5ca09692900`, `devnet_pqc_root_helper` sha256 `c42c9b875452c712539b4fcece674f962ff998cca1655b9e5fe34f4323f94e1b` ELF BuildID `a8d47c8ee5f4f61af8df0837930f4df55a5678da`, branch `copilot/continue-qbind-development-50412889-86c3-4f75-b33e-20d0a95a07ed`, pre-doc commit `02f4e4c667f16f5c0549fce729ae428eea01875b`, clean working tree pre-doc. **Crate-layering-clean seam.** `P2pMetrics` lives in `qbind-node` and `qbind-net` must not depend on `qbind-node`; the smallest safe shape is a new tiny trait `qbind_net::CertVerifyMetricsSink` in `crates/qbind-net/src/cert_verify_metrics.rs` with default no-op methods (`inc_accepted`, `inc_rejected_unknown_root`, `inc_rejected_wrong_suite`, `inc_rejected_bad_signature`, `inc_rejected_validator_mismatch`, `inc_rejected_malformed`, `inc_rejected_expired`) threaded through `qbind_net::ClientHandshakeConfig` and `qbind_net::ServerHandshakeConfig` as `pub cert_verify_metrics: Option<Arc<dyn CertVerifyMetricsSink>>` and invoked at each existing return arm of the listener-side `ServerHandshake::parse_and_verify_client_cert` and the dialer-side `ClientHandshake::handle_server_accept` cert-verification region. `qbind-node` implements the trait for `P2pMetrics` (forwarding each method onto the existing pre-Run-044 `inc_pqc_cert_verify_*` setters that preserve the Run 037 aggregate-counter contract), and `P2pNodeBuilder::create_connection_configs` installs the sink **only** on the production-honest path `(MutualAuthMode::{Required,Optional}, PqcRootMode::PqcStaticRoot, Some(Arc<P2pMetrics>) shared via the Run-043 with_p2p_metrics setter)`. **Reason mapping (Run 044 §4 table):** listener-side `NetworkDelegationCert::decode → Err` → `inc_rejected_malformed` → `NetError::ClientCertInvalid("parse error")`; listener-side `TrustedClientRoots::lookup → None` → `inc_rejected_unknown_root` → `NetError::ClientCertInvalid("untrusted root")`; either-side `verify_delegation_cert → Err(UnsupportedSuite(_))` → `inc_rejected_wrong_suite` → `NetError::UnsupportedSuite(_)`; either-side `verify_delegation_cert → Err(KeySchedule("signature verify error"))` → `inc_rejected_bad_signature` → `NetError::KeySchedule("signature verify error")`; dialer-side `delegation_cert.validator_id != client_init.validator_id` → `inc_rejected_validator_mismatch` → `NetError::KeySchedule("validator_id mismatch in cert")`; dialer-side cert-bytes parse fail → `inc_rejected_malformed` → `NetError::KeySchedule("failed to parse delegation cert")`; success on either side (parse + root lookup + signature verify + validator-id match where applicable) → `inc_accepted`. **`inc_rejected_expired` is currently unused at the live boundary** because validity-window enforcement (`not_before` / `not_after`) is not yet implemented in `qbind_net::handshake::verify_delegation_cert`; the counter stays visible at zero, explicitly documented as such (see Run 044 §4 last row and `expired_counter_documented_unused_at_live_boundary` unit test). **Tests:** `cargo test -p qbind-net --lib` 17/17 (15 pre-existing + 2 new `cert_verify_metrics::tests::*` — default-no-op + counting-sink); `cargo test -p qbind-net --test run_044_cert_verify_metrics_tests` 13/13 (every reason at the real handshake boundary, both roles, no-sink preserves verification result, expired stays zero); `cargo test -p qbind-net` (all targets green incl. handshake/m6/m8/wire/transport-frame); `cargo test -p qbind-crypto --lib` 68/68; `cargo test -p qbind-node --lib` 775/775; `cargo test -p qbind-node --test run_037_pqc_static_root_mutual_auth_tests` 12/12 (incl. R037.B tampered-cert, R037.C untrusted-root, R037.D wrong-sig-suite); `cargo test -p qbind-node --test run_040_pqc_static_root_real_aead_tests` 14/14; `cargo test -p qbind-node --test run_044_pqc_cert_verify_metrics_adapter_tests` 10/10 (adapter mapping + per-reason aggregate-counter Run-037-contract preservation + family-still-emitted-by-`format_metrics`); `cargo test -p qbind-node --test kemtls_pqc_metrics_tests --test metrics_http_tests --test kem_metrics_node_integration_tests` 4+19+15=38/38; `cargo build --release -p qbind-node --bin qbind-node` clean; `cargo build --release -p qbind-node --example devnet_pqc_root_helper` clean. **Real-binary positive smoke** (release binary above, `target/release/examples/devnet_pqc_root_helper /tmp/run044/mat 2` mints DevNet-ephemeral root `3779a0e00c71b4225d25dea2cd61fd3c1b4e2cc8919dbf2e83ce396319209eb4` fp=`773d0e9f`, two-node V0/V1 on `127.0.0.1:19550/19551` with `/metrics` on `127.0.0.1:43150/43151`, both with `--env devnet --network-mode p2p --enable-p2p --p2p-mutual-auth required --p2p-pqc-root-mode pqc-static-root --p2p-trusted-root <SHARED_SPEC>`, per-validator real ML-DSA-44-signed leaf cert + real ML-KEM-768 leaf secret, one `--p2p-peer-leaf-cert` each): both nodes logged the Run 039 `[binary] Run 039: pqc_root_mode=pqc-static-root transport_kem_suite=ml-kem-768 configured_roots=1 leaf_credentials_present=true peer_leaf_certs=1 (root fingerprints: [id=3779a0e0.. suite=100 fp=773d0e9f])` and Run 040 `[Run040] P2pNodeBuilder: pqc_root_mode=pqc-static-root sig_suite_id=100 transport_kem_suite_id=100 transport_kem_suite_name=ml-kem-768 dummy_kem_registered=false transport_aead_suite_id=101 transport_aead_suite_name=chacha20-poly1305 dummy_aead_registered=false configured_roots=1 leaf_credentials_present=true` startup banners (real ML-DSA-44 cert verify + real ML-KEM-768 + real ChaCha20-Poly1305 AEAD active, no DummySig/DummyKem/DummyAead fallback); live `/metrics` on **both** nodes shows `qbind_p2p_pqc_root_mode 1`, `qbind_p2p_pqc_roots_configured 1`, **`qbind_p2p_pqc_cert_verify_accepted_total 2`** (1 listener-side + 1 dialer-side accept per node — exactly the expected two-node mutual-handshake wire cardinality), `qbind_p2p_pqc_cert_verify_rejected_total 0`, and every per-reason `qbind_p2p_pqc_cert_rejected_{unknown_root,wrong_suite,bad_signature,validator_mismatch,malformed,expired}_total 0` (full slate at `/tmp/run044/logs/{v0,v1}.metrics.txt`); the `qbind_p2p_pqc_*` family is emitted exactly once on each scrape (Run 043 invariant preserved); pre-existing `qbind_timeout_verification_*`, `qbind_net_kem_*`, and `consensus_net_*` surfaces all preserved. **Real-binary negative tampered-cert smoke**: v1's leaf cert tampered by `bytes[-8..] ^= 0xFF`; v1 fails closed at the **startup self-verification boundary** inside `P2pNodeBuilder::build` with `[binary] ERROR: Failed to build P2P node: Config("delegation cert verification failed: KeySchedule(\"signature verify error\")")` — i.e. **before** the live listener/dialer paths in `qbind-net` are ever invoked and **before** the v1-side sink is plumbed into `Server/ClientHandshakeConfig`; this is the documented (Run 037 / Run 042) fail-closed boundary and Run 044 §6 contract REQUIRES that the per-reason counter is NOT bumped on the live binary path in that case (no fake increments, no double-counting). Tampered-cert per-reason counter movement is instead proven by the focused `listener_bad_signature_increments_bad_signature_once` and `dialer_bad_signature_increments_bad_signature_once` unit tests, which drive the real `parse_and_verify_client_cert` / `handle_server_accept` boundaries through the existing public `qbind-net` API with a provider whose signature suite returns `Err`, and assert (a) the bit-identical `NetError::KeySchedule("signature verify error")` is returned AND (b) `bad_signature_counter == 1` AND (c) every other counter == 0; matching tests cover `unknown_root`, `wrong_suite`, `malformed`, and `validator_mismatch` cells of the §4 mapping table. **No silent fallback** to DummySig / DummyKem / DummyAead occurred on any node at any time. **C4 piece "Run 043 declared-but-not-incremented cert-verify event counters" is now NARROWED on the live binary path**: `qbind_p2p_pqc_cert_verify_accepted_total` moves on every honest two-node mutual handshake; every per-reason `qbind_p2p_pqc_cert_rejected_*_total` (except `expired`) is wired to the corresponding `qbind-net::handshake` failure arm with unit-test-enforced exactly-once semantics; the aggregate `qbind_p2p_pqc_cert_verify_rejected_total` moves in lockstep with the per-reason counters via the Run 037 contract; `expired` stays at 0 and is the only documented-unused per-reason cell because validity-window enforcement is not yet implemented in `verify_delegation_cert` (the `inc_rejected_expired` adapter mapping is unit-test-asserted so no follow-up adapter work is required when validity enforcement lands). **Full C4 remains OPEN** for: CA / cert rotation / cert revocation / signed root distribution lifecycle (operator-out-of-band, not solved); cert validity-window enforcement (`not_before` / `not_after` — not yet enforced by `verify_delegation_cert`, the only remaining wired-but-unused per-reason cell); production fast-sync / consensus-storage restore; exponential-backoff timeout pacing; per-environment trust anchors. **C5 remains NOT closed** by Run 044; the transport-crypto dependency (real ML-DSA-44 cert verification — Run 037, real ML-KEM-768 — Run 039, real ChaCha20-Poly1305 AEAD — Run 040, active timeout verification — Runs 031–034, forged-traffic rejection — Runs 035–036, end-to-end live-binary N=4 B14 recovery — Run 042, live cert-verify family scrape-visible — Run 043, live per-event cert-verify counter movement — Run 044) is *further confirmed live-binary-observable at the per-event counter level* by Run 044, but lifecycle (CA / rotation / revocation / signed root distribution) and validity-window enforcement remain C4 pieces, so Run 044 does NOT close C5 by fiat. Operators using `--p2p-pqc-root-mode pqc-static-root` post-Run-044 now have actionable live `/metrics` observability of every PQC cert-verification success/failure event on the live binary path through the full `qbind_p2p_pqc_cert_verify_*` family — `accepted_total` truthfully reflects every cert verified by the live KEMTLS handshake engine; per-reason rejection counters truthfully reflect every cert rejected by the live engine for that exact reason; lifecycle and validity-window enforcement remain operator-out-of-band and are NOT a substitute for production CA tooling.
DevNet Evidence Run 045 (`docs/devnet/QBIND_DEVNET_EVIDENCE_RUN_045.md`) closes the Run 044 §4-last-row "`inc_rejected_expired` is currently unused at the live boundary because validity-window enforcement is not yet implemented in `verify_delegation_cert`" gap by adding the smallest-possible `not_before` / `not_after` enforcement to `qbind_net::handshake::verify_delegation_cert`, routing the three resulting failure shapes — expired, not-yet-valid, and inverted-window — through the already-wired Run 044 `inc_rejected_expired` cert-verify metrics sink, without changing any other verification semantics. **Narrow validity-window enforcement only — no `qbind-net`/`qbind-crypto`/`qbind-node` consensus, KEMTLS, KEM, AEAD, signer-loader, timeout-verification, forged-traffic-rejection, mutual-auth, or trust-anchor surface was modified.** Release binary `qbind-node` sha256 `e9b79590ec9610d34fbec476bb0b94b425d9a50e8f696e075d1201b881b1ccee` ELF BuildID `c17f96cfcc88f1f0b5dd220988ff5eacdcd7bc9c`, `devnet_pqc_root_helper` sha256 `a86d96d1259ac6eeb200543f3c520a3213a8a8724c6485e731ce92d820d6dc4d` ELF BuildID `ee14b1b7be65bbf73b1639a76ace33a3a8721ab6`, branch `copilot/continue-qbind-development-9b9570c8-3ffd-4012-b84f-c69dda0603a5`, pre-doc commit `942fc3b8915b0ad25c11657f98b203ba8f8fcff3`, working tree dirty only with Run 045 changes pre-doc-commit. **Format already had the fields.** `qbind_wire::net::NetworkDelegationCert` (and `qbind_hash::net::network_delegation_cert_digest`) already carried `not_before: u64` / `not_after: u64` Unix-seconds fields in the signed digest preimage from Run 037 onwards; Run 045 does NOT change the wire format, the digest preimage, or any test cert fixture (every pre-existing helper / test fixture uses `not_before: 0, not_after: u64::MAX` which is currently-valid under any realistic wall-clock and therefore unaffected). **Validity-window semantics.** Inclusive on both ends: a cert is valid iff `not_before <= validation_time <= not_after`. Validation runs **after** signature verification, so a tampered validity field surfaces as `NetError::KeySchedule("signature verify error")` (preserving the existing tampered-cert contract from Run 037 §10), and a malformed inverted window (`not_before > not_after`) is fail-closed *before* any time comparison so it can't accidentally be accepted on a clock-skew corner case. Fail-closed cases on `verify_delegation_cert` / `verify_delegation_cert_at`: `not_before > not_after` → `NetError::ClientCertInvalid("cert invalid validity window")`; `validation_time < not_before` → `NetError::ClientCertInvalid("cert not yet valid")`; `validation_time > not_after` → `NetError::ClientCertInvalid("cert expired")`. **Time-source model.** Two entry points: `verify_delegation_cert(crypto, cert, root_pk)` is the binary-path convenience wrapper that reads `SystemTime::now()` once per call and is **explicitly** scoped as a transport-layer operational freshness check, NOT a consensus time source — consensus safety remains entirely independent of wall-clock; `verify_delegation_cert_at(crypto, cert, root_pk, validation_time_secs)` is the deterministic seam for tests and for any future callers that need explicit clock injection. Smallest-injection-seam — no new field was added to `ClientHandshakeConfig` / `ServerHandshakeConfig` (which would have forced every one of the ~120 historical call sites under `crates/{qbind-net,qbind-node}/tests/*` to be edited, expanding the change-surface against the "smallest code change" requirement); the seam is the public `verify_delegation_cert_at` function itself, which is used by the focused `crates/qbind-net/tests/run_045_cert_validity_tests.rs` deterministic tests and by `crates/qbind-node/src/pqc_devnet_helper.rs` tests. No dangerous operator bypass (no `--ignore-cert-validity` or equivalent) was added; the wall-clock wrapper is the only path the live binary uses. **Reason-mapping update.** The `cert_verify_metrics::rejection-reason` table is extended with one row (Run 045) folded into the existing `inc_rejected_expired` cell: dialer-side and listener-side `verify_delegation_cert → Err(NetError::ClientCertInvalid("cert expired" | "cert not yet valid" | "cert invalid validity window"))` → `inc_rejected_expired` → bumps `qbind_p2p_pqc_cert_rejected_expired_total` AND the aggregate `qbind_p2p_pqc_cert_verify_rejected_total` exactly once via the Run 037 per-reason-bumps-aggregate contract. Every other Run 044 mapping row is preserved bit-identically. Critically, validity-window failures do NOT collapse into `bad_signature` — the dialer-side and listener-side `match &e { ... }` blocks in `handle_server_accept` and `parse_and_verify_client_cert` explicitly precede the `NetError::KeySchedule(_) => inc_rejected_bad_signature` arm with a `NetError::ClientCertInvalid("cert expired" | "cert not yet valid" | "cert invalid validity window") => inc_rejected_expired` arm; any other (hypothetical future) `ClientCertInvalid(_)` shape from `verify_delegation_cert` still falls back to `bad_signature` rather than mis-classify as expired. **Accepted-counter and expired-counter boundary.** `inc_accepted` is reached only after parse + (listener-side) trusted-root lookup + signature verify + validity-window check (Run 045) + (dialer-side) validator-id match all succeed. `inc_rejected_expired` is reached only on the three validity-window failure shapes above. Accepted and expired are mutually exclusive on the same handshake event (any validity-window failure short-circuits before `inc_accepted`), enforced both by the `return Err(e)` immediately after the `match`-on-`e` block and by the `dialer_valid_cert_still_increments_only_accepted` regression test (which asserts a currently-valid cert under wall-clock bumps only `accepted = 1` and zero rejection counters). **No duplicate increments.** Each `verify_delegation_cert` invocation produces at most one `inc_*` call by construction (the `match &e {...}` block runs once per `Err` and the success path bumps `inc_accepted` exactly once); the listener wrapper and dialer wrapper layers each invoke `verify_delegation_cert` once per handshake event, and the sink is `Option<Arc<dyn CertVerifyMetricsSink>>` with the same `Arc` cloned into both `ClientHandshakeConfig::cert_verify_metrics` and `ServerHandshakeConfig::cert_verify_metrics` via `P2pNodeBuilder::create_connection_configs` (Run 044 shape preserved), so no double-counting between listener/dialer wrapper layers is introduced. **Tests.** `cargo test -p qbind-net --lib` 17/17 (no count delta vs Run 044 — Run 045 adds no new lib unit-test module); `cargo test -p qbind-net --test run_044_cert_verify_metrics_tests` 13/13 (Run 044 contract preserved, including the `expired_counter_documented_unused_at_live_boundary` test which still passes because it only asserts the expired counter is *zero* in the specific malformed-listener + validator-mismatch-dialer scenarios it exercises — both of which short-circuit before `verify_delegation_cert` is ever invoked); `cargo test -p qbind-net --test run_045_cert_validity_tests` **20/20** (new — boundary-inclusive, expired, not-yet-valid, inverted-window, signature-runs-before-validity, validity-fields-in-digest-preimage, wall-clock wrapper accepts eternally-valid / rejects clearly-expired / rejects clearly-not-yet-valid, plus listener-side and dialer-side counting-sink tests for each failure shape and a valid-cert regression on the dialer); `cargo test -p qbind-net` all targets green; `cargo test -p qbind-crypto --lib` 68/68; `cargo test -p qbind-node --lib` **780/780** (5 new in `pqc_devnet_helper::tests`: `helper_default_cert_is_currently_valid`, `helper_can_create_expired_cert_that_fails_closed`, `helper_can_create_not_yet_valid_cert_that_fails_closed`, `helper_rejects_inverted_window_at_issuance`, `helper_validity_fields_are_encoded_and_signature_covered`); `cargo test -p qbind-node --test run_037_pqc_static_root_mutual_auth_tests` 12/12 (Run 037 mutual-auth path preserved); `cargo test -p qbind-node --test run_040_pqc_static_root_real_aead_tests` 14/14 (real-AEAD path preserved); `cargo test -p qbind-node --test run_044_pqc_cert_verify_metrics_adapter_tests` 10/10 (adapter mapping preserved); `cargo build --release -p qbind-node --bin qbind-node` clean; `cargo build --release -p qbind-node --example devnet_pqc_root_helper` clean. **Real-binary positive smoke** (release binary above, `target/release/examples/devnet_pqc_root_helper /tmp/run045/mat 2` mints DevNet-ephemeral root `d408262d15f306822b2b3233f57d63930513648c740cb35b8b58643bf1ea3491` with default `currently-valid` validity mode, two-node V0/V1 on `127.0.0.1:19650/19651` with `/metrics` on `127.0.0.1:43250/43251`, both with `--env devnet --network-mode p2p --enable-p2p --p2p-mutual-auth required --p2p-pqc-root-mode pqc-static-root --p2p-trusted-root <SHARED_SPEC>`, per-validator real ML-DSA-44-signed leaf cert + real ML-KEM-768 leaf secret, one `--p2p-peer-leaf-cert` each): both nodes logged Run 039 + Run 040 startup banners (real ML-DSA-44 cert verify + real ML-KEM-768 + real ChaCha20-Poly1305 AEAD active, no DummySig/DummyKem/DummyAead fallback); live `/metrics` on **both** nodes shows `qbind_p2p_pqc_root_mode 1`, `qbind_p2p_pqc_roots_configured 1`, **`qbind_p2p_pqc_cert_verify_accepted_total 2`** (1 listener-side + 1 dialer-side accept per node — Run 044 cardinality preserved under validity-window enforcement), `qbind_p2p_pqc_cert_verify_rejected_total 0`, and every per-reason `qbind_p2p_pqc_cert_rejected_{unknown_root,wrong_suite,bad_signature,validator_mismatch,malformed,expired}_total 0` (full slate at `/tmp/run045/{v0,v1}_pqc_metrics.txt`); the `qbind_p2p_pqc_*` family is emitted exactly once on each scrape (Run 043 invariant preserved); both nodes remained alive throughout the smoke window. **Real-binary negative expired-cert smoke** (`target/release/examples/devnet_pqc_root_helper /tmp/run045/exp 2 expired` mints root `4a5619639c6fb94246f2b5f09f2ebedbf6305003ab73e74f5f354c535a8c6c21` with leaf certs `not_before=0, not_after=1`, both nodes use the same root spec so the failure mode under test is *validity-window* not *unknown-root*): both nodes fail closed at the **startup self-verification boundary** inside `P2pNodeBuilder::build`'s `verify_cert_with_configured_root` invocation with `[binary] ERROR: Failed to build P2P node: Config("delegation cert verification failed: ClientCertInvalid(\"cert expired\")")` — i.e. **before** the metrics HTTP server is reachable for an honest scrape, **before** any `qbind-net` listener/dialer cert-verify sink is invoked, and **without** any silent fallback to DummySig/DummyKem/DummyAead. **Real-binary negative not-yet-valid-cert smoke** (`target/release/examples/devnet_pqc_root_helper /tmp/run045/nyv 2 not-yet-valid`, leaf certs `not_before=u64::MAX-1, not_after=u64::MAX`): identical fail-closed shape, `[binary] ERROR: Failed to build P2P node: Config("delegation cert verification failed: ClientCertInvalid(\"cert not yet valid\")")`. Live `inc_rejected_expired` counter movement (and aggregate `inc_pqc_cert_verify_rejected` movement, and `inc_accepted` non-movement) on the real `qbind-net::handshake` boundary is therefore proven by the **qbind-net handshake-boundary unit tests** in `crates/qbind-net/tests/run_045_cert_validity_tests.rs::listener_expired_cert_increments_expired_once`, `listener_not_yet_valid_cert_increments_expired_once`, `listener_inverted_window_cert_increments_expired_once`, `dialer_expired_cert_increments_expired_once`, `dialer_not_yet_valid_cert_increments_expired_once`, and `dialer_inverted_window_cert_increments_expired_once`, each of which drives the real `parse_and_verify_client_cert` / `handle_server_accept` boundaries through the existing public `qbind-net` API and asserts (a) the bit-identical typed `NetError::ClientCertInvalid(...)` is returned AND (b) `expired_counter == 1` AND (c) `aggregate_rejected_counter` movement via the Run 037 per-reason-bumps-aggregate contract AND (d) every other counter == 0 AND (e) `accepted == 0` for the same event. This is exactly the "partial-positive: validity enforcement lands in qbind-net tests, but real-binary negative metric movement is not observable due to startup self-verification" boundary anticipated in the Run 045 task description, recorded honestly as such — Run 045 does NOT claim live-binary expired-counter movement on the *negative-cert* smoke because the binary, by design (Run 037 / Run 042 startup self-verification), exits before serving `/metrics`; this is the correct fail-closed behaviour and is *more* defensive than allowing the binary to come up with an unusable cert. **C4 piece "cert validity-window enforcement (`not_before` / `not_after` — not yet enforced by `verify_delegation_cert`, the only remaining wired-but-unused per-reason cell)" is now NARROWED on the live binary path** — `verify_delegation_cert` enforces `not_before` / `not_after`, the `qbind_p2p_pqc_cert_rejected_expired_total` cell is no longer the "documented-unused" per-reason cell, and the only fail-mode previously wired-but-unreachable at the live boundary is now fully reachable through `qbind-net::handshake` (proven by unit tests on the real boundary, by startup self-verification on the live binary path, and indirectly by digest-preimage signature coverage of `not_before` / `not_after` from Run 037 onwards). **Full C4 remains OPEN** for: production CA / cert rotation / cert revocation / signed root distribution lifecycle (operator-out-of-band, not solved — Run 045 does NOT claim CA lifecycle); production fast-sync / consensus-storage restore; exponential-backoff timeout pacing; per-environment trust anchors. **C5 remains NOT closed** by Run 045; the transport-crypto dependency is now further hardened on the cert-freshness axis but lifecycle (CA / rotation / revocation / signed root distribution) remains a C4 piece, so Run 045 does NOT close C5 by fiat. **No fabricated metrics.** **No silent regression.** **No protocol behavior change beyond validity-window rejection.** **No removed tests, no DummySig/DummyKem/DummyAead fallback path introduced or strengthened.** **No operator bypass.** Operators using `--p2p-pqc-root-mode pqc-static-root` post-Run-045 now have actionable live `/metrics` observability of expired / not-yet-valid / inverted-window PQC delegation-cert verification failures through `qbind_p2p_pqc_cert_rejected_expired_total` on the live binary path whenever a peer connecting to a healthy node presents such a cert (the local-cert-against-self failure path stops at startup self-verification by design); the live binary itself refuses to come up at all if its own leaf cert is outside its validity window — both surfaces are honest fail-closed behaviour.
#### C4 Run 046 evidence update (2026-05-11)

Run 045 left the documented C4 remaining-items list as: production CA / cert rotation / cert revocation / signed root distribution lifecycle; production fast-sync / consensus-storage restore; **exponential-backoff timeout pacing**; per-environment trust anchors. Run 046 narrows the "exponential-backoff timeout pacing" cell on the binary B14 path by replacing the pre-Run-046 fixed-cadence view-timeout primitive (`view_timeout_ticks = 50` ticks, constant) with a bounded, tick-deterministic exponential-backoff pacer (`ViewTimeoutBackoffState` in `crates/qbind-node/src/binary_consensus_loop.rs`, defaults `base = 50` ticks / `multiplier = 2` / `max = 800` ticks). The pacer threshold grows by an integer multiplier after each consecutive local `TimeoutMsg` emission for a view without committed-height progress, saturating at the cap; it resets to base on real committed-height progress only. View-only advances (including TC-driven view advances, which are themselves timeout-driven and therefore not a recovery signal) do **not** reset the pacer, matching the standard HotStuff pacemaker convention that only commits demonstrate liveness. The pacer is observable on `/metrics` through five new lines (`qbind_consensus_view_timeout_current_threshold_ticks`, `qbind_consensus_view_timeout_backoff_level`, `qbind_consensus_view_timeout_backoff_resets_total`, `qbind_consensus_view_timeout_backoff_increases_total`, `qbind_consensus_view_timeout_max_cap_hits_total`); all five are strict accounts of state transitions that actually occurred (no fabricated resets, no increase without an emitted timeout, no cap-hit unless the threshold actually saturated). Run 046 is proven by 11 new `run046_*` unit tests (default-base-equals-old-fixed-threshold, doubles-then-saturates, reset-counts-real-only, multiplier-1-preserves-fixed-cadence, invalid-config-rejected-fail-closed, disabled-primitive-inert, second-view-emits-at-doubled-threshold-after-self-fire, committed-height-progress-resets-pacer, view-only-progress-does-not-reset-pacer, metrics-export-reflects-pacer-state-exactly, metrics-export-disabled-primitive-reads-zero) which together pin the pacer state machine, the wiring into `maybe_emit_view_timeout`, and the metric exposition. All 791 pre-existing `qbind-node` lib tests, all 12 Run 037 PQC static-root mutual-auth tests, all Run 030 timeout-verification tests, and the B14 Run 015 absent-leader / self-quorum / restore-catchup-suppression / disabled-via-None tests pass unchanged — preserving B14 safety, active timeout verification, forged-traffic rejection, single-validator no-timeout behaviour, `--require-timeout-verification` semantics, restore-catchup gating, and Run 037–045 transport behaviour. **C4 piece "exponential-backoff timeout pacing" is now NARROWED on the binary B14 path** — the binary path no longer fires `TimeoutMsg`s at a constant aggressive cadence under sustained absent-leader conditions, the pacer is fully deterministic and integer-only, and the schedule is observable end-to-end on `/metrics`. **Full C4 remains OPEN** for: production CA / cert rotation / cert revocation / signed root distribution lifecycle (operator-out-of-band, not solved — Run 046 does NOT claim CA lifecycle); production fast-sync / consensus-storage restore; per-environment trust anchors. **C5 remains NOT closed** by Run 046; Run 046 does not touch transport-root dependency, transport KEM, transport AEAD, forged-traffic rejection, or timeout-verification activation. **No fabricated metrics.** **No silent regression.** **No protocol behaviour change beyond the threshold value the pacer uses to decide whether to emit a local timeout.** **No removed tests, no DummySig/DummyKem/DummyAead fallback path introduced or strengthened.** **No new CLI flag introduced; no `--ignore-timeout-validation`-style bypass.** **Fail-closed on invalid backoff config** (`base = 0`, `max < base`, `multiplier < 1` all rejected): an operator who misconfigures the pacer gets the disabled primitive (no timeout emission) plus a warning, NOT a silent fallback to the pre-Run-046 fixed cadence. **A live-binary N=4 multi-validator backoff smoke proving multi-view backoff progression on a real binary at real `/metrics` was NOT performed in Run 046**; Run 046 proves the pacer state machine and wiring through unit tests and through the unconditional `/metrics` exposition (the five new lines render at any reachable `/metrics` scrape, with the in-process pacer state), and explicitly does NOT claim live-binary multi-view backoff smoke evidence — that is reasonable future work.